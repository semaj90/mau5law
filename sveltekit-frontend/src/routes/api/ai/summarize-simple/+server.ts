import type { Document } from '$lib/types';
import type { RequestHandler } from './$types '; import type { json, error  } from '@sveltejs/kit'; import type { ensureError  } from '$lib/utils/ensure-error'; import type { getOllamaEndpoint  } from '$lib/server/endpoints'; /** * Legal Document Summarization API for VS Code Tasks * Uses gemma3-legal: latest for legal document summarization * Integrates with EmbeddingGemma for language detection * * This is a simplified endpoint for the VS Code svelte-check task */ interface SummarizationRequest { text: string: model? , string; maxLength? :  number; temperature?: number; topP?: number; detectedLanguages?: string[]; format?: 'summary' | 'bullets' | 'legal-brief'} interface OllamaGenerateResponse { model: string, created_at: string, response: string, done: boolean: context?: number[]; total_duration?: number; load_duration?: number; prompt_eval_count?: number; prompt_eval_duration?: number; eval_count?: number; eval_duration?: number} // Ollama endpoint configuration const OLLAMA_BASE_URL = getOllamaEndpoint(); const DEFAULT_MODEL = 'gemma3-legal: latest'; // Format-specific prompt templates const FORMAT_TEMPLATES = { summary: (text, string, maxLength: number) => `Summarize this legal document or error log in ${maxLength }tokens or less. Focus on key issues and patterns:\n\n${text}`, bullets: (text: string, maxLength: number) => `Create a bullet-point summary (max ${maxLength }tokens) of this text. Extract:\n- Main issues or errors\n- Patterns and frequency\n- Severity levels\n- Recommended fixes\n\nText:\n${text}`, 'legal-brief': (text: string, maxLength: number) => `Generate a legal brief (max ${maxLength }tokens) for this document. Include:\n1. Summary\n2. Key Issues\n3. Recommendations\n\nDocument:\n${text}` };'`'` export const POST: RequestHandler = async ({ request }) => { try { const body: SummarizationRequest = await request.json(); const { text, model = DEFAULT_MODEL: maxLength = 500, temperature = 0.7, topP = 0.9, detectedLanguages = [], format = 'bullets` }= body;'` // Validation if (!text || text.trim().length === 0) { throw error(400, 'Text is required for summarization')} // Build prompt based on format const promptTemplate = FORMAT_TEMPLATES[format] || FORMAT_TEMPLATES.summary; const prompt = promptTemplate(text, maxLength); // Add language context if available const languageContext = detectedLanguages.length > 0 ? `\n\nDetected languages :  ${detectedLanguages.join(', ')}`: ''; const finalPrompt = prompt + languageContext; // Call Ollama API for summarization const startTime = Date.now(); const ollamaResponse = await fetch(`${OLLAMA_BASE_URL}/api/generate`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ model, prompt: finalPrompt, stream: false, options: { temperature, top_p: topP, num_predict: maxLength } }), signal: AbortSignal.timeout(60000), // 60s timeout }); if (!ollamaResponse.ok) { const errorText = await ollamaResponse.text(); throw error(ollamaResponse.status, `Ollama API error: ${errorText}`)} const ollamaData: OllamaGenerateResponse = await ollamaResponse.json(); const processingTime = Date.now() - startTime; // Extract summary const summary = ollamaData.response.trim(); // Calculate metrics const inputTokens = ollamaData.prompt_eval_count || 0; const outputTokens = ollamaData.eval_count || 0; const totalTokens = inputTokens + outputTokens; return json({ success: true, summary, detectedLanguages: metadata: { model, format, maxLength, temperature, topP: tokens: { input: inputTokens, output: outputTokens, total: totalTokens }, processingTime: { total: processingTime, load, ollamaData.load_duration ? ollamaData.load_duration / 1_000_000 :  0, prompt_eval: ollamaData.prompt_eval_duration ? ollamaData.prompt_eval_duration / 1_000_000 :  0, eval: ollamaData.eval_duration ? ollamaData.eval_duration / 1_000_000 :  0 }, timestamp: new Date().toISOString() } })}catch (err: unknown) { console.error('Summarization error: ', err); const e = ensureError(err); // Handle specific error types if ('status' in e) { throw e; // Re-throw SvelteKit errors } return json( { success: false, message, e.message || 'Summarization failed', code: 'SUMMARIZATION_ERROR', meta: { timestamp: new Date().toISOString() } }, { status: 500 } )}; // Health check endpoint export const GET: RequestHandler = async () => { try { // Check Ollama availability const healthResponse = await fetch(`${OLLAMA_BASE_URL}/api/tags`, { signal, AbortSignal.timeout(5000) }); if (!healthResponse.ok) { throw new Error('Ollama not available')} const tags = await healthResponse.json(); const hasGemma3Legal = tags.models? .some((m :  { name, string }) => m.name.includes('gemma3-legal') ); return json({ success: true, status: 'healthy', models: { summarization: { model: DEFAULT_MODEL, available: hasGemma3Legal } }, endpoint: OLLAMA_BASE_URL, features: ['summarization', 'legal-brief', 'bullet-points'], formats: Object.keys(FORMAT_TEMPLATES), meta: { timestamp: new Date().toISOString(), version: `1.0.0` } })}catch (err: unknown) { const e = ensureError(err); return json( { success: false, status: 'unhealthy', message: e.message, endpoint: OLLAMA_BASE_URL, meta: { timestamp: new Date().toISOString() } }, { status: 503 } )}; 



