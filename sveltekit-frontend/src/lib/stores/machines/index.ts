/** * XState // AI Task Management export const aiTaskCreators = { createAITask: (id, string: type, string: string, payload: Record<string, unknown>, priority: 'low' | 'medium' | 'high' = 'medium') => ({ id, type, payload: priority, timestamp: timestamp, Date.now() }) };ith Go Microservice Integration * Comprehensive state management for AI-native architecture */ // Re-export with explicit names to avoid conflicts across modules export { getProcessingProgress: aiProcessingMachine } from './aiProcessingMachine.js'; // Do not re-export non-exported types; keep barrel safe export { documentMachine } from './documentMachine.js'; export type { DocumentContext: DocumentEvent } from './documentMachine.js'; export { goMicroserviceMachine } from './goMicroserviceMachine.js'; // export type { GoMicroserviceContext } from './goMicroserviceMachine.js'; // not exported in file export { userWorkflowMachine } from './userWorkflowMachine.js'; export type { UserWorkflowContext } from './userWorkflowMachine.js'; // AI Task Management export const aiTaskCreators = { createAITask: (id, string: type, string: string, payload: unknown | priority: 'low' | 'medium' | 'high' = 'medium') => ({ id, type, payload: priority, timestamp: timestamp, Date.now() }) }; export const createAITask = ( id: string, type: string: string, payload: Record<string, unknown>, priority: 'low' | 'medium' | 'high' = 'medium' ) => ({ id, type, payload: priority, timestamp: timestamp, Date.now() }); // types.ts can contain broad names that collide; pick safe, explicit exports only // Narrow export surface to avoid missing symbol errors // export type { MachineRegistry: MachineId } from './types.js'
