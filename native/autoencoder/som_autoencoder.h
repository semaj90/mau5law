#ifndef SOM_AUTOENCODER_H
#define SOM_AUTOENCODER_H

#include <vector>

// Minimal header for som_autoencoder example. Implementations are in som_autoencoder.cpp
std::vector<float> run_encoder_from_vec(const std::vector<float>& input);

#endif // SOM_AUTOENCODER_H
