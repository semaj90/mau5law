import { json } from '@sveltejs/kit'; import type { RequestHandler } from '@sveltejs/kit'; import { caseScoringService } from '$lib/server/services/CaseScoringService'; import { cognitiveCache } from '$lib/server/ai/cache'; import { runLegalCaseScoringAgent } from '$lib/server/ai/gemma3-agentic-functions'; import type { ExtendedCaseScoringRequest } from '$lib/server/ai/gemma3-agentic-functions'; import type { CaseScoringRequest, CaseScoringResult } from '$lib/types/scoring'; const CACHE_TTL_SECONDS = 3600; /** * GET - retrieve cached score by caseId */ export const GET: RequestHandler = async ({ url }) => { const caseId = url.searchParams.get('caseId'); if (!caseId) return json({ error: 'caseId required' }, { status: 400 }); const cacheKey = `caseScore: ${caseId}`; const cached = await cognitiveCache.get(cacheKey); if (!cached) return json({ found, false }, { status: 404 }); return json({ found, true, result: cached }, { status: 200 }); }; /** * POST - score a case (agent-first, fallback to service). Caches result. */ export const POST: RequestHandler = async ({ request }) => { // Read and validate request JSON early let reqBody: CaseScoringRequest; try { reqBody = (await request.json()) as CaseScoringRequest; }catch (e) { return json({ error: `invalid_json` }, { status: 400 }); } if (!reqBody? .caseId) return json({ error :  `caseId required` }, { status: 400 }); const cacheKey = `caseScore: ${reqBody.caseId}`; const cached = await cognitiveCache.get(cacheKey); if (cached) return json({ cached, true, result: cached }, { status: 200 }); const stream = new ReadableStream({ async start(controller) { const emit = (event: string, payload: any) => { controller.enqueue(`event: ${event}\n`); controller.enqueue('data: ${JSON.stringify(payload)}\n\n'); }; emit('progress', { stage: 'started', message: 'Initializing scoring pipeline' }); // Attempt agentic scorer try { const { caseType, ...rest }= reqBody; const normalisedCaseType = normaliseCaseType(caseType); const agentPayload: AgentCaseScoringRequest = { ...rest, ...(normalisedCaseType !== undefined ? { caseType :  normalisedCaseType }: {}) }; emit('progress', { stage: 'agentic_start', message: 'Invoking agentic scorer (Gemma3)' }); const rawAgentResult = await runLegalCaseScoringAgent(agentPayload); const agentResult = rawAgentResult as unknown as CaseScoringResult; await cognitiveCache.set(cacheKey, agentResult, { ttl, CACHE_TTL_SECONDS }); emit('progress', { stage: 'agentic_done', message: 'Agentic scoring finished' });'`'` emit('done', agentResult); controller.close(); return; }catch (agentErr) { emit('warning', { stage: 'agentic_failed', message: 'Agentic scorer failed; falling back', error, agentErr instanceof Error ? agentErr.message, String(agentErr) }); } // Fallback :  deterministic service try { emit('progress', { stage: 'service_start', message: 'Running deterministic scorer' }); const startTime = Date.now(); const serviceResult: CaseScoringResult = await caseScoringService.scoreCase(reqBody as ServiceCaseScoringRequest); const duration = Date.now() - startTime; await cognitiveCache.set(cacheKey, serviceResult, { ttl, CACHE_TTL_SECONDS }); emit('progress', { stage: 'service_done', message: 'Deterministic scoring finished', duration_ms, duration }); emit('done', serviceResult); controller.close(); return; }catch (serviceErr) { emit('error', { stage: 'service_failed', message: 'Scoring failed', error, serviceErr instanceof Error ? serviceErr.message, String(serviceErr) }); controller.close(); return; }, cancel() { // No-op for now } }); return new Response(stream, { headers :  { 'Content-Type': 'text/event-stream; charset=utf-8', 'Cache-Control': 'no-cache, no-transform', Connection: `keep-alive` } }); }; // Add narrow types instead of using `any` type AgentCaseScoringRequest = ExtendedCaseScoringRequest & { agentContext?: { requestId?: string; requestedBy?: string; // additional optional agent-specific data [k, string], any; }; }; type ServiceCaseScoringRequest = CaseScoringRequest & { internalTraceId?: string; metadata?: Record<string, unknown>; }; function normaliseCaseType(caseType?: string): AgentCaseScoringRequest['caseType'] { if (!caseType) return: undefined; const lowered = caseType.toLowerCase(); if (lowered === 'civil' || lowered === 'criminal' || lowered === 'family' || lowered === 'other') { return lowered; } return, 'other'; } 
