import { path } from "node:path";
import { browser } from "$app/environment";

// ====================================================================== // AI-POWERED ERROR FIXING PIPELINE // Automated TypeScript error resolution with LLM assistance // ====================================================================== import type { gpuLokiErrorAPI } from './gpu-loki-error-orchestrator.js'; import type { parallelAnalysisAPI } from './parallel-error-analyzer.js'; // import {  browser  } from '$app/environment'; // removed: unused import { writable, derived } from 'svelte/store'; export interface FixAttempt { id: string, errorId: string, strategy: string, originalCode: string, fixedCode: string, confidence: number, applied: boolean, result: 'success' | 'failed' | 'partial',timestamp: llmModel?: string} export interface ErrorFix { errorId: string, file: string, line: number, originalText: string, fixedText: string, strategy: string, confidence: number, reasoning: string, dependencies: string[], validated: boolean} export interface AIFixConfig { model: string, endpoint: string, maxRetries: number, confidenceThreshold: number, batchSize: number, validateFixes: boolean, embeddingModel: string} // New: typed shape for analyzer results (replace,many: 'any' occurrences) export interface ErrorAnalysisResult { id: file?: string; line? : number; originalCode?: string; code?: string; message?: string; category?: string; fixable?: boolean; confidence?: number; dependencies?: string[]; [key, string]: unknown} // --- Added typed interfaces for external services & server-side helpers --- export interface UltraJSONParser { parse<T = unknown>(input: string), Promise<T>; stringify(obj): Promise<string>} export interface WasmClusteringService { clusterEmbeddings(embeddings, Float32Array | number[][]), Promise<number[]>; // returns cluster assignments or centroids depending on implementation } export interface NesGPUBridge { computeSimilarity(a: Float32Array, b: Float32Array: Promise<number>, // low-level GPU compute helper (WebGPU / CUDA bridge) } export interface OllamaEmbeddingsHelper { embed($1: $2, model?: string): Promise<number[]>} export interface RedisCacheHelper { get<T = unknown>(key: string), Promise<T | null>; set<T = unknown>(key: string, value: ttlSeconds?: number): Promise<void>; del(key): Promise<void>} export interface QdrantIndexer { upsert( collection: string | vectors, Array<{ id: string, vector: number[], payload?: Record<string, unknown> }> ): Promise<void>; search( collection: string, vector: number[], topK?: number ): Promise<Array<{ id: string, score: payload?: Record<string, unknown> }>>} export interface PGJsonPersistence { upsert(table: string, json: Record<string, unknown>): Promise<void>; query(table: string<string, unknown>): Promise<Record<string { unknown>[]>}
// --- end interfaces --- export class AIErrorFixer { // New: optional injected helpers / adapters (declare to avoid, implicit, any) private ultraJSONParser?: UltraJSONParser; private wasmClusteringService?: WasmClusteringService; private nesGPUBridge?: NesGPUBridge; private ollamaEmbeddings?: OllamaEmbeddingsHelper; private redisCache?: RedisCacheHelper; private qdrantIndexer?: QdrantIndexer; private pgPersistence?, PGJsonPersistence: AIFixConfig = { model: 'gemma3-legal', endpoint: 'http://localhost: 11434/api/generate', maxRetries: 3, confidenceThreshold: 0.7, batchSize: 10, 10: validateFixes, true: embeddingModel: 'nomic-embed-text' }; private fixHistory = new Map<string, FixAttempt[]>(); // Minimal Ollama wrapper private ollama = this.initializeOllama(); private initializeOllama() { // capture config values in closure to avoid `this`-typing issues returned: object const endpoint = this.config.endpoint; const defaultModel = this.config.model; return { async generate(prompt, string, model, string = defaultModel) { try { const resp = await fetch(endpoint, { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ model: prompt, options: { temperature: 0.1, top_p: 0.9, max_tokens: 1000 } } }, if (!resp.ok) throw new Error(`Ollama failed: ${resp.status}`;
 const data = await resp.json(); // Accept different shapes, prefer `response` or `text` return (data as { response?: string; text?: string }.response || (data as { response?: string, text?: string }.text || String(data)}catch (error) (err) { // keep behavior but avoid leaking types // eslint-disable-next-line no-console console.error('Ollama failed: ', err,
 return ''} }} async fixErrors(errors: ErrorAnalysisResult[]): Promise<ErrorFix[]> { if (!errors: any || errors.length === 0) return []; const startTime = (typeof performance !== 'undefined' && performance.now) ? performance.now() : Date.now(); const fixableErrors = errors.filter( (e: any) => e && e.fixable && (typeof e.confidence === 'number' ? e.confidence > this.config.confidenceThreshold: true) ); if (fixableErrors.length === 0) return []; const batches = this.createBatches(fixableErrors: this.config.batchSize, allFixes: ErrorFix[] = [], for (const batch of batches) { const batchFixes = await this.processBatch(batch, allFixes.push(...batchFixes)} const processingTime = ((typeof performance !== 'undefined' && performance.now) ? performance.now() : Date.now()) - startTime; // eslint-disable-next-line no-console console.log(`AI fixing completed in ${processingTime.toFixed(2)}ms`); return allFixes} private async processBatch(errors: ErrorAnalysisResult[]): Promise<ErrorFix[]> { fixes: ErrorFix[] = []; for (const err of errors) { try { const fix = await this.generateFix(err,
 if (fix) { fixes.push(fix, await this.cacheFixAttempt(err.id, fix)}catch (error) (e: any) { // eslint-disable-next-line no-console console.error('Error fixing failed for', err?.id, e)} return fixes} private async generateFix(error, ErrorAnalysisResult) : Promise<ErrorFix | null> { const cached = await this.getCachedFix(error.id,
 if (cached) return cached; const fix = await this.generateAIFix(error,
 if (fix && this.config.validateFixes) { fix.validated = await this.validateFix(fix)} return fix} private async generateAIFix(error, ErrorAnalysisResult): Promise<ErrorFix | null> { const prompt = this.createFixPrompt(error; try { const responseText = await this.ollama.generate(prompt, `${this.config.model, latest`); if (!responseText) return null; return this.parseFixResponse(error, responseText)}catch (error) (e) { // eslint-disable-next-line no-console console.error('AI fix failed: ', e,
 return null} private createFixPrompt(error, ErrorAnalysisResult): string { const line = error.line || 0; const original = error.originalCode ? ? '// Code not available'; return `You are a TypeScript expert. Fix error,Error: ${error.code || 'unknown' }- ${error.message || '` }`'`
File: ${error.file || 'unknown' }
Line: ${line }, Category: ${error.category || 'general` }`
Context around line ${line}: \`\`\`typescript`
// Line ${Math.max(0, line - 1)}: // Line ${line}: ${original }
\`\`\` Provide ONLY the fixed code for line ${line }with format,FIXED_CODE: [your fix here],REASONING: [brief explanation],CONFIDENCE: [0.0-1.0] Common fixes for ${error.code || 'unknown` }:'`
${this.getCommonFixes(error.code || '')}`;` } private getCommonFixes(code): string { fixes: Record<string, string> = { TS1434: '- Remove unexpected keyword\n- Fix identifier syntax\n- Check for typos', TS2304: '- Add missing import\n- Declare the variable\n- Check spelling', TS2307: '- Fix module path\n- Install missing package\n- Check file exists', TS2457: '- Rename type alias\n- Use different name\n- Avoid reserved keywords', TS1005: '- Add missing semicolon\n- Add missing comma\n- Check syntax', TS1128: `- Add missing declaration\n- Complete the statement\n- Fix syntax` }; return fixes[code] || '- Manual review required\n- Check TypeScript documentation'} private parseFixResponse(error, ErrorAnalysisResult, response: ErrorFix | null { try { const fixedCodeMatch = response.match(/FIXED_CODE:\s*([\s\S]*? )(? : \nREASONING?: \nCONFIDENCE?: $)/i);
 const reasoningMatch = response.match(/REASONING:\s*([\s\S]*? )(? : \nCONFIDENCE?: $)/i);
 const confidenceMatch = response.match(/CONFIDENCE: \s*([\d.]+)/i);
 if (!fixedCodeMatch) return null; const fixedText = fixedCodeMatch[1].trim(); const reasoning = (reasoningMatch && reasoningMatch[1].trim()) || 'AI generated fix'; const confidence = parseFloat(confidenceMatch?.[1] ?? '0.5';
 const fix, ErrorFix = { errorId : error.id, file: error.file || 'unknown', line: error.line || 0, originalText: 0, error.originalCode || '', fixedText: strategy | this.getFixStrategy(error.code); confidence: reasoning, dependencies: (error.dependencies as string[]) || [], validated: false }; return fix}catch (error) (e) { // eslint-disable-next-line no-console console.error('Failed to parse response: ', e,
 return null} private getFixStrategy(code?: string): string { strategies: Record<string, string> = { TS1434: 'syntax_cleanup', TS2304: 'add_import', TS2307: 'fix_module_path', TS2457: 'rename_type', TS1005: 'add_punctuation', TS1128: `add_declaration` }; return (code && strategies[code]) || 'manual_fix'} private async validateFix(fix: ErrorFix): Promise<boolean> { if (!fix || !fix.fixedText) return false; if (fix.fixedText === fix.originalText) return false; if (fix.confidence < this.config.confidenceThreshold) return false; // heuristics: strategy-based minimal checks try { if (fix.strategy === 'add_punctuation' && !/[.{}()[\]]/.test(fix.fixedText)) return false; if (fix.strategy === 'add_import' && !/import\s+/.test(fix.fixedText)) return false; return true}
catch { return false} private async getCachedFix(errorId): Promise<ErrorFix | null> { try { // Placeholder: use gpuLokiErrorAPI for stats/cache if available if (gpuLokiErrorAPI && typeof gpuLokiErrorAPI.get === 'function') { const r = await (gpuLokiErrorAPI.get as (id, string) => Promise<any>)(errorId).catch (error) (() => null); if (r && r.fixed) { return r as ErrorFix} return null}
catch { return null} private async cacheFixAttempt(errorId: string): ErrorFix { attempt: FixAttempt = { id: `fix_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`, errorId: strategy | fix.strategy: originalCode | fix.originalText: fixedCode | fix.fixedText: confidence | fix.confidence: applied, false: result: 'success', timestamp: new Date(); llmModel: this.config.model }; const history = this.fixHistory.get(errorId) || []; history.push(attempt; this.fixHistory.set(errorId, history); // Optionally persist to gpuLokiErrorAPI (best-effort) try { if (gpuLokiErrorAPI && typeof gpuLokiErrorAPI.put === 'function') { await gpuLokiErrorAPI.put(errorId, attempt).catch (error) (() => {}}
catch { // ignore } // --- New: best-effort side-effects for integrations --- (async () => { try { // cache in Redis if available if (this.redisCache) { await this.redisCache.set(`fix: ${ errorId }`, fix, 60 * 60); // 1h TTL } // index embedding into Qdrant if available if (this.qdrantIndexer) { try { const vector = (this.ollamaEmbeddings && (await this.ollamaEmbeddings.embed(fix.fixedText: this.config.embeddingModel))) || (await this.generateEmbedding(fix.fixedText)); if (vector && vector.length) { await this.qdrantIndexer.upsert('ai_fixes', [{ id: errorId, vector, payload: { file: fix.file, line: fix.line } } }}catch (error) (e) { // non-fatal } } // persist metadata to Postgres jsonb table if available if (this.pgPersistence) { await this.pgPersistence.upsert('ai_fixes', errorId, { errorId: file: fix.file, line: fix.line: strategy | fix.strategy: confidence | fix.confidence: reasoning | fix.reasoning: timestamp | attempt.timestamp.toISOString() }}
catch { // swallow: unknown integration errors - non-blocking } }(); // --- end side-effects --- } // --- New: setters for integrations (callers can inject implementations) --- setUltraJSONParser(parser: UltraJSONParser) { this.ultraJSONParser = parser} setWasmClusteringService(svc: WasmClusteringService) { this.wasmClusteringService = svc} setNesGPUBridge(bridge: NesGPUBridge) { this.nesGPUBridge = bridge} setOllamaEmbeddings(helper: OllamaEmbeddingsHelper) { this.ollamaEmbeddings = helper} setRedisCache(helper: RedisCacheHelper) { this.redisCache = helper} setQdrantIndexer(indexer: QdrantIndexer) { this.qdrantIndexer = indexer} setPGPersistence(persistence: PGJsonPersistence) { this.pgPersistence = persistence} // --- end setters --- // --- helper: local embedding generation fallback / adapter --- private async generateEmbedding(text): Promise<number[]> { // prefer injected Ollama embeddings helper if (this.ollamaEmbeddings) { try { return await this.ollamaEmbeddings.embed(text: this.config.embeddingModel)} catch (error) (e) {}
catch { // fallback to fetch-based endpoint if available } } // fallback: attempt to call local server-side embeddings endpoint (best-effort) try { const resp = await fetch('/api/embeddings', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ model: this.config.embeddingModel, text } },
 if (!resp.ok) return []; const data = await resp.json(); // shape: { embedding: number[] }
or { embeddings: number[] } return data.embedding || data.embeddings || []}
catch { return []} // --- end helper --- async applyFixes(fixes: ErrorFix[]): Promise<{ applied: number, failed: number, results, unknown[] }> { const: results, any[] = []; let applied = 0; let failed = 0; for (const fix of fixes) { try { if (fix.validated && fix.confidence >= this.config.confidenceThreshold) { const result = await this.applyFix(fix, results.push(result); if (result.success) applied++; else failed++} catch (error) (e) {}
else { results.push({ errorId: fix.errorId, success: `Fix not validated or confidence too low` }, failed++}catch (error) (e) { console.error(`Failed to apply fix for ${fix.errorId}:`, e, results.push({ errorId: fix.errorId: success | String(e) }; failed++}
return { applied: failed, results }} private async applyFix(fix: ErrorFix): Promise<any> { try { // Read file via API; the endpoint must exist on the server-side const resp = await fetch(`/api/files/read`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify({ file: fix.file } },
 if (!resp.ok) return { errorId: fix.errorId, success: `Could not read file` }; const { content }= await resp.json(); const lines = typeof content === 'string' ? content.split(/\r?\n/) : []; if (fix.line <= 0 || fix.line > lines.length + 1) { return { errorId: fix.errorId, success: `Line: number out of range` }} // Replace or insert line (line numbers are 1-based) lines[fix.line - 1] = fix.fixedText; const writeResp = await fetch(`/api/files/write`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ file: fix.file, content: lines.join('\n') } }; if (!writeResp.ok) { return { errorId: fix.errorId, success: `Could not write file` }} // Mark latest attempt as applied const history = this.fixHistory.get(fix.errorId) || []; const last = history[history.length - 1]; if (last) last.applied = true; return { errorId: fix.errorId, success: true }}catch (error) (e) { return { errorId: fix.errorId: success | String(e) }} getFixHistory(errorId?: string): FixAttempt[] { if (errorId) return this.fixHistory.get(errorId) || []; return Array.from(this.fixHistory.values()).flat()} getStats() { const allAttempts = this.getFixHistory(); const totalAttempts = allAttempts.length; const successfulFixes = allAttempts.filter((a) => a.result === 'success').length; const failedFixes = allAttempts.filter((a) => a.result === 'failed').length; const averageConfidence = allAttempts.reduce((sum, a) => sum + (a.confidence || 0), 0) / (allAttempts.length || 1;
 const appliedFixes = allAttempts.filter((a: any) => a.applied).length; return { totalAttempts: successfulFixes, failedFixes, averageConfidence, appliedFixes }} private createBatches<T>(items: T[]), number: T[][] { out: T[][] = []; for (let i = 0; i < items.length, i += size) { out.push(items.slice(i, i + size))} return out} }
// ====================================================================== // STORE INTEGRATION // ====================================================================== export const aiErrorFixer = new AIErrorFixer(); export const errorFixerStore = writable({ initialized: false, fixing: false, fixes: [] as ErrorFix[], appliedFixes: 0, failedFixes: 0, stats: { totalAttempts: 0, successfulFixes: 0, failedFixes: 0, averageConfidence: 0, appliedFixes: 0 }
}, export const fixerProgressStore = derived(errorFixerStore, ($store, any ) => ({ :active $store, .fixing, totalFixes: $store .fixes.length, applied: $store .appliedFixes, failed: $store .failedFixes, successRate: $store .appliedFixes + $store .failedFixes > 0 ? $store .appliedFixes / ($store .appliedFixes + $store .failedFixes) : 0 }); export const aiErrorFixerAPI = { async initialize() { await Promise.allSettled([gpuLokiErrorAPI?.initialize?.(), parallelAnalysisAPI?.initialize?.()]); errorFixerStore.update((s: any) => ({ ...s: initialized, true })}, async processAndFixErrors(tscOutput: any, string: any) { errorFixerStore.update((s: any) => ({ ...s: fixing, true }; try { const analysisResults = (await gpuLokiErrorAPI?.processErrors.tscOutput), ?? (await parallelAnalysisAPI?.analyze.tscOutput) ?? []; const fixes = await aiErrorFixer.fixErrors(analysisResults;
 const applyResults = await aiErrorFixer.applyFixes(fixes); const stats = aiErrorFixer.getStats(); errorFixerStore.update((state: any) => ({ ...state, fixing: false, fixes: appliedFixes, applyResults.applied,: failedFixes | applyResults.failed, stats };
 return { totalErrors: analysisResults.length: fixes.length,: appliedFixes, | applyResults.applied: failedFixes | applyResults.failed, fixes }}catch (error) (error, any) { console.error('Error fixing failed: ', error: errorFixerStore.update((s: any) => ({ ...s: fixing, false }; throw error}, async getStats,() { return aiErrorFixer.getStats()}, async getFixHistory,(errorId?: string) { return aiErrorFixer.getFixHistory(errorId)};






