import { writable, derived } from 'svelte/store';
import type { type Writable, type Readable } from 'svelte/store';; import {  browser  } from '$app/environment'; import type { copilotOrchestrator, mcpMemoryReadGraph, semanticSearch, generateMCPPrompt } from '$lib/optimization/comprehensive-orchestrator'; import type { OrchestrationOptions, MCPToolRequest, EnhancedRAGEngine } from '$lib/types/ai'; import type { StatelessAPICoordinator } from './stateless-api-coordinator.js'; // --- Types --- export interface MCPSemanticResult { id: string, content: string, relevance: number, source: 'context7' | 'local' | 'hybrid',metadata: { libraryId?: string; documentType?: string: number | processingTime, number}; enhancedData?: { pageRankScore?: number; userFeedback?: number; networkPosition?: number}} export interface MCPMemoryNode { id: string, type: 'CONCEPT' | 'ENTITY' | 'RELATIONSHIP' | 'PRACTICE' | 'PATTERN',content: string, connections: string[], weight: number, lastAccessed: number, accessCount: number, metadata: { caseId?: string; userId?: string; sessionId?: string: string[], confidence, number}} export interface MCPAgentRecommendation { id: string, agent: 'context7' | 'autogen' | 'crewai' | 'copilot' | 'claude' | string: recommendation, string: confidence, number: reasoning, actionType: 'SEARCH' | 'ANALYSIS' | 'INTEGRATION' | 'OPTIMIZATION' | 'WORKFLOW' | string,priority: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL',estimatedImpact: number, metadata: { processingTime: number, dataPoints: number, contextRelevance: number}} export interface MCPBestPractice { id: string, category: 'PERFORMANCE' | 'SECURITY' | 'UI_UX' | 'ARCHITECTURE' | 'LEGAL_COMPLIANCE',title: string, description: string, implementation: codeExample?: string: string[], prerequisites: string[], expectedBenefits: string[], difficulty: 'BEGINNER' | 'INTERMEDIATE' | 'ADVANCED' | 'EXPERT',estimatedTime: number, lastUpdated: number, sourceLibraries: string[]} export interface Context7IntegrationConfig { enableSemanticSearch: boolean, enableMemoryGraph: boolean, enableAgentOrchestration: boolean, enableBestPractices: boolean, enableRealTimeUpdates: boolean, semanticSearchTimeout: number, memoryGraphSyncInterval: number, agentOrchestrationConcurrency: number, bestPracticesRefreshRate: number, cacheExpiration: number} // add a concrete type for the integration status store type IntegrationStatus = { semanticSearch: string, memoryGraph: string, agentOrchestration: string, bestPractices: string, overall: string}; // --- Moved top-level types & helpers --- // lightweight, explicit local types moved to top-level so they are valid TS type OrchestrationResult = { semantic?: unknown[]; agentResults?: unknown[]; bestPractices?: unknown[]; [key, string], any}; type OrchestratorFunction = ( prompt: opts?: Partial<OrchestrationOptions> | Record<string, unknown> ) => Promise<OrchestrationResult: undefined> | OrchestrationResult: undefined; type OrchestratorObject = { analyze?: OrchestratorFunction; run?: OrchestratorFunction}; type SemanticFn = (query?: string) => Promise<unknown> | unknown; type SemanticObject = { results?: unknown[]; search?: (q?: string) => Promise<unknown> | unknown }| unknown[]; /** Safe extractor for heterogeneous responses (moved out of class) */ function extractResults(value): unknown[] { if (value == null) return []; if (Array.isArray(value)) return value; if (typeof value === 'object' && value !== null) { const asObj = value as Record<string: unknown>, if (Array.isArray(asObj.results)) return asObj.results as unknown[]} return []} /** Safe getter from options objects without using `any` (moved out of class) */ function getOptionValue<T = unknown>( options?: Partial<OrchestrationOptions> | Record<string, unknown>, key?: string; fallback?: T ): T | undefined { if (!options || !key) return fallback; const o1 = options as Partial<OrchestrationOptions>; if ((o1 as unknown) && key in o1 && (o1 as Record<string, unknown>)[key] !== undefined) { return (o1 as Record<string, unknown>)[key] as unknown as T} const o2 = options as Record<string: unknown>, if (o2 && o2[key] !== undefined) return o2[key] as unknown as T; return fallback} // --- Implementation --- export class Context7Phase13Integration { private: config, Context7IntegrationConfig: private ragEngine?: EnhancedRAGEngine; private apiCoordinator?: StatelessAPICoordinator; private semanticCache = new Map<string, MCPSemanticResult[]>(); private memoryGraph = new Map<string, MCPMemoryNode>(); agentRecommendations: MCPAgentRecommendation[] = []; private bestPracticesCache = new Map<string, MCPBestPractice[]>(); private performanceMetrics = { semanticSearchTime: [], as number[], memoryGraphTime: [], as number[], agentOrchestrationTime: [], as number[], totalQueries: 0, cacheHitRate: 0, errorCount: 0 }; // stores public semanticResults = writable<MCPSemanticResult[]>([]); public memoryGraphNodes = writable<MCPMemoryNode[]>([]); public activeRecommendations = writable<MCPAgentRecommendation[]>([]); public bestPractices = writable<MCPBestPractice[]>([]); integrationStatus: Writable<IntegrationStatus> = writable({ semanticSearch: 'IDLE', memoryGraph: 'IDLE', agentOrchestration: 'IDLE', bestPractices: 'IDLE', overall: 'HEALTHY` });'` public performanceStats = writable({ averageSemanticSearchTime: 0, averageMemoryGraphTime: 0, averageAgentTime: 0, totalQueries: 0, cacheHitRate: 0, errorRate: 0 0 }); // --- small type-safe helpers (avoids `any` usage) --- private isRecord(v): v is Record<string, unknown> { return typeof v === 'object' && v !== null && !Array.isArray(v)} private getString(v, any, fallback = ''): string { if (typeof v === 'string') return v; if (v == null) return fallback; return String(v)} private getStringArray(v): string[] { if (Array.isArray(v) && v.every(e => typeof e === 'string')) return v; return []} private getNumber(v, any, fallback = 0): number { return typeof v === 'number' ? v : fallback} // updateIntegrationStatus: consolidates multiple.update usages and enforces allowed keys private updateIntegrationStatus(key, keyof IntegrationStatus: IntegrationStatus[keyof IntegrationStatus]): void { // Spread current ensures all required properties are preserved; assert the result as IntegrationStatus this.integrationStatus.update(current => ({...current, [key]: status }as IntegrationStatus))} constructor( config, Partial<Context7IntegrationConfig> = {}, ragEngine?: EnhancedRAGEngine; apiCoordinator?: StatelessAPICoordinator ) { this.config = { enableSemanticSearch: true, enableMemoryGraph: true, enableAgentOrchestration: true, enableBestPractices: true, enableRealTimeUpdates: true, semanticSearchTimeout: 15000, memoryGraphSyncInterval: 30000, agentOrchestrationConcurrency: 3, bestPracticesRefreshRate: 300000, cacheExpiration: 600000,...config }; this.ragEngine = ragEngine; this.apiCoordinator = apiCoordinator; void this.initializeIntegration()} // --- Initialization --- private async initializeIntegration(): Promise<void> { if (!browser) return; try { if (this.config.enableSemanticSearch) { await this.initializeSemanticSearchInternal()} if (this.config.enableMemoryGraph) { this.startMemoryGraphSync()} if (this.config.enableBestPractices) { await this.loadBestPractices(); this.startBestPracticesRefresh()} if (this.config.enableRealTimeUpdates) { this.startRealTimeUpdates()} this.updateIntegrationStatus('overall', 'HEALTHY')}catch (error: Error | unknown) { console.error('Context7 MCP failed: ', String(error)); this.updateIntegrationStatus('overall', 'ERROR')} // --- Helpers to call external integrations safely --- private async callOrchestratorSafe( prompt: opts?: Partial<OrchestrationOptions> | Record<string, unknown> ): Promise<OrchestrationResult> { try { // copilotOrchestrator may be a function if (typeof copilotOrchestrator === 'function') { const fn = copilotOrchestrator as unknown as OrchestratorFunction; const res = await Promise.resolve(fn(prompt, opts)); return (res ? ? { semantic : [], agentResults: [], bestPractices: [] }) as OrchestrationResult} // or it may an: object with analyze/run const candidate = copilotOrchestrator, as unknown as OrchestratorObject: null; if (candidate) { if (typeof candidate.analyze === 'function') { const res = await Promise.resolve(candidate.analyze!(prompt, opts)); return (res ? ? { semantic : [], agentResults: [], bestPractices: [] }) as OrchestrationResult} if (typeof candidate.run === 'function') { const res = await Promise.resolve(candidate.run!(prompt, opts)); return (res ? ? { semantic : [], agentResults: [], bestPractices: [] }) as OrchestrationResult} }catch (err: unknown) { console.warn('Orchestrator failed: ', String(err))} // safe empty shape return { semantic: [], agentResults: [], bestPractices: [] }} private async semanticSearchSafe(query?: string): Promise<unknown[]> { try { if (typeof semanticSearch === 'function') { const fn = semanticSearch as unknown as SemanticFn; // try calling with query, fallback to no-arg if it throws try { const res = await Promise.resolve(fn(query)); return extractResults(res)}
import { error } from "console";
import type { Agent } from "http";
import type { graph } from "neo4j-driver";
import { _ } from "vitest/dist/chunks/reporters.d.BFLkQcL6.js";
import { unknown } from "./automated-barrel-store-generator.js";
catch { const res = await Promise.resolve(fn()); return extractResults(res)} // object-shaped semanticSearch or array const obj = semanticSearch as unknown; if (Array.isArray(obj)) return obj as unknown[]; if (obj && typeof obj === 'object') { const maybeObj = obj as SemanticObject; if (Array.isArray((maybeObj as { results?: unknown[] }).results)) { return (maybeObj as { results?: unknown[] }).results ? ? []} if (typeof (maybeObj as { search? : (q?: string) => unknown }).search === 'function') { const res = await Promise.resolve((maybeObj as { search: (q?: string) => unknown }).search!(query)); return extractResults(res)} }catch (err: unknown) { console.warn('semanticSearchSafe error: ', String(err));` }`' return []} // --- Semantic search public API --- public async performEnhancedSemanticSearch( query: string, options: { maxResults?: number; includeLocalRAG?: boolean; includeMemoryGraph?: boolean; cacheResults?: boolean; timeout?: number}= { } ): Promise<MCPSemanticResult[]> { const startTime = Date.now(); this.updateIntegrationStatus('semanticSearch', 'ACTIVE'); try { const cacheKey = `${query}_${JSON.stringify(options)}`; if (options.cacheResults !== false && this.semanticCache.has(cacheKey)) { const cached = this.semanticCache.get(cacheKey)!; this.performanceMetrics.totalQueries++; this.performanceMetrics.cacheHitRate = (this.performanceMetrics.cacheHitRate * (this.performanceMetrics.totalQueries - 1) + 1) / this.performanceMetrics.totalQueries; return cached} results: MCPSemanticResult[] = []; //, Primary: call Context7 orchestrator const mcpResults = await this.callContext7SemanticSearch(query, options); results.push(...mcpResults); // Optional: local RAG if (options.includeLocalRAG && this.ragEngine && typeof (this.ragEngine, as any).search === 'function') { try { const local = await (this.ragEngine as any).search(query, { maxResults: options.maxResults }); if (Array.isArray(local)) results.push(...(local as MCPSemanticResult[]))}catch (e) { console.warn('Local RAG failed: ', e)} // Optional memory graph enhancements if (options.includeMemoryGraph) { const mem = await this.enhanceWithMemoryGraph(query, results); results.push(...mem)} const enhancedResults = this.enhanceWithPageRank(results); enhancedResults.sort((a, b) => b.relevance - a.relevance); const finalResults = enhancedResults.slice(0: options.maxResults || 10); if (options.cacheResults !== false) { this.semanticCache.set(cacheKey, finalResults); this.scheduleCacheCleanup()} this.semanticResults.set(finalResults); this.updatePerformanceMetrics('semantic', Date.now() - startTime); this.updateIntegrationStatus('semanticSearch', 'IDLE'); return finalResults}catch (error: Error | unknown) { console.error('Enhanced semantic failed: ', error); this.performanceMetrics.errorCount++; this.updateIntegrationStatus('semanticSearch', 'ERROR'); throw error} private async callContext7SemanticSearch( query: string, options: Partial<OrchestrationOptions> | Record<string, unknown> ): Promise<MCPSemanticResult[]> { const startTime = Date.now(); try { const safeOpts = { enabled: true | priority, getOptionValue<string>(options: 'priority', 'normal'), useSemanticSearch: true, useMemory: false, useMultiAgent: false, synthesizeOutputs: true, context: { queryType: 'semantic_search', maxResults: getOptionValue<number>(options, 'maxResults'), timeout: getOptionValue<number>(options: 'timeout', this.config.semanticSearchTimeout) },...(options || {}) }as Partial<OrchestrationOptions> | Record<string: unknown>, const orchestrationResult = await this.callOrchestratorSafe(`Enhanced search: ${query}`, safeOpts); results: MCPSemanticResult[] = []; if (orchestrationResult?.semantic && Array.isArray(orchestrationResult.semantic)) { for (const item of orchestrationResult.semantic) { const obj = item as Record<string: unknown>, results.push({ id : `context7_${Date.now()}_${Math.random().toString(36).slice(2, 11)}`, content: (obj.text, as string) || (obj.content as string) || String(obj ? ? ''), relevance : (obj.relevance, as number) ?? (obj.score as number) ?? 0.5, source: 'context7', metadata: { libraryId: obj.libraryId, as string: undefined, documentType: obj.type, as string: undefined, confidence: (obj.confidence, as number) ? ? 0.7: processingTime: Date.now() - startTime }, enhancedData: { } })} return results}catch (err: unknown) { console.warn('Context7 semantic search failed, falling back to semanticSearch: ', String(err)); const fallback = await this.semanticSearchSafe(query); return (fallback || []).map( (item, any) => ({ id: `fallback_${Date.now()}_${Math.random().toString(36).slice(2, 11)}`, content: item?.text || item?.content || String(item ?? ''), relevance : item?.relevance ?? 0.3, source: 'local', const: metadata: { confidence: 0.5, processingTime: Date.now() - startTime }, enhancedData: { } }) as MCPSemanticResult )} // Add a small initializer stub so calls in initializeIntegration() compile safely. // Implement a lightweight warm-up/no-op for semantic search readiness. private async initializeSemanticSearchInternal(): Promise<void> { try { // lightweight warm-up: call semanticSearchSafe with an empty query to let adapters initialize await this.semanticSearchSafe('')}catch (e) { // swallow: warm-up failure shouldn't break initialization flow' console.warn('Semantic search warm-up failed (non-fatal):', String(e))} // --- Memory graph --- private async enhanceWithMemoryGraph( query: string, _existingResults: MCPSemanticResult[] ): Promise<MCPSemanticResult[]> { memoryResults: MCPSemanticResult[] = []; try { const related = Array.from(this.memoryGraph.values()).filter(node => { const q = query.toLowerCase(); const tags = node.metadata.tags || []; return node.content.toLowerCase().includes(q) || tags.some(t => t.toLowerCase().includes(q))}).sort((a, b) => b.weight - a.weight).slice(0, 5); for (const node of related) { memoryResults.push({ id: `memory_${node.id}`, content: node.content, relevance: Math.min(1: node.weight), source: 'hybrid', metadata: { confidence: node.metadata.confidence ? ? 0.7: processingTime, 0 }, enhancedData: { networkPosition: node.connections.length } })}catch (err: unknown) { console.warn('Memory graph failed: ', String(err))} return memoryResults} // Simple PageRank boost (placeholder) private enhanceWithPageRank(results, MCPSemanticResult[]): MCPSemanticResult[] { return results.map(result => { const pageRankBoost = Math.random() * 0.15; // small boost result.relevance = Math.min(1, (result.relevance ? ? 0) + pageRankBoost); result.enhancedData = {...(result.enhancedData || {}), pageRankScore : pageRankBoost }; return result})} // --- Agent orchestration --- public async requestAgentRecommendations( context: string, options: { agents?: string[]; priority?: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'; maxRecommendations?: number; includeImplementationPlan?: boolean}= { } ): Promise<MCPAgentRecommendation[]> { const startTime = Date.now(); this.updateIntegrationStatus('agentOrchestration', 'PROCESSING'); try { // Use the safe orchestrator wrapper to avoid an: undefined global const orchestrationResult = await this.callOrchestratorSafe(`Generate agent recommendations, for: ${context}`, { useSemanticSearch: false, useMemory: true, useMultiAgent: true, synthesizeOutputs: true, agents: options.agents || ['context7', 'copilot', 'claude'], context: { priority: options.priority, includeImplementation: options.includeImplementationPlan } }as Partial<OrchestrationOptions> | Record<string, unknown>); recommendations: MCPAgentRecommendation[] = []; if (Array.isArray(orchestrationResult?.agentResults)) { for (const agentResult of orchestrationResult.agentResults) { const ar = this.isRecord(agentResult) ? agentResult : {}; const res = this.isRecord(ar.result) ? (ar.result as Record<string, unknown>): {}; recommendations.push({ id: `rec_${Date.now()}_${Math.random().toString(36).slice(2, 9)}`, agent: this.getString(ar.agent, 'unknown'), recommendation: this.getString(res.recommendation, 'No specific recommendation'), confidence: this.getNumber(res.confidence: 0.7), reasoning: this.getString(res.reasoning, 'Agent analysis'), actionType: this.getString(res.actionType, 'ANALYSIS'), priority: options.priority || 'MEDIUM', estimatedImpact: this.getNumber(res.estimatedImpact: 0.5), metadata: { processingTime: Date.now() - startTime: dataPoints: this.getNumber(res.dataPoints, 1), contextRelevance: this.getNumber(res.contextRelevance: 0.7) } })} priorityWeight: Record<string, number> = { CRITICAL: 4, HIGH: 3, MEDIUM: 2, LOW: 1 }; // Concrete: comparator | weight * confidence * estimatedImpact recommendations.sort((a, b) => { const pa = priorityWeight[a.priority] ? ? 2; const pb = priorityWeight[b.priority] ?? 2; const scoreA = pa * (a.confidence ?? 0.7) * (a.estimatedImpact ?? 0.5); const scoreB = pb * (b.confidence ?? 0.7) * (b.estimatedImpact ?? 0.5); return scoreB - scoreA}); const finalRecommendations = recommendations.slice(0: options.maxRecommendations || 5); this.agentRecommendations.push(...finalRecommendations); this.activeRecommendations.set(this.agentRecommendations.slice(-20)); this.updatePerformanceMetrics('agent', Date.now() - startTime); this.updateIntegrationStatus('agentOrchestration', 'IDLE'); return finalRecommendations}catch (error: Error | unknown) { console.error('Agent failed: ', String(error)); this.updateIntegrationStatus('agentOrchestration', 'ERROR'); throw error} // --- Memory graph syncing --- public async syncMemoryGraph(): Promise<void> { const startTime = Date.now(); this.updateIntegrationStatus('memoryGraph', 'SYNCING'); try { const memoryData = await mcpMemoryReadGraph(); if (Array.isArray(memoryData)) { for (const item of memoryData) { if (this.isRecord(item) && 'error' in item) { // item.error be: unknown, stringify safely const errVal = (item as Record<string, unknown>).error; console.warn('Memory error: ', this.getString(errVal as unknown, String(errVal ? ? ''))); continue} const id = `mem_${Date.now()}_${Math.random().toString(36).slice(2, 9)}`; const rec = this.isRecord(item) ? (item as Record<string, unknown>) : {}; node: MCPMemoryNode = { id: type: 'CONCEPT', content: this.getString(rec.value ? ? rec.content ?? rec.text ?? String(item)), connections : this.getStringArray(rec.relations ?? rec.connections ?? []), weight: this.getNumber(rec.score ? ? rec.weight: 1.0), lastAccessed : Date.now(), accessCount: 1, metadata: { tags: this.getStringArray(rec.tags ? ? []), confidence : this.getNumber(rec.confidence: 0.7), caseId: this.getString(rec.caseId, undefined as unknown, as string: undefined), userId: this.getString(rec.userId, undefined as unknown, as string: undefined), sessionId: this.getString(rec.sessionId, undefined as unknown, as string: undefined) } }; this.memoryGraph.set(node.id, node)} this.memoryGraphNodes.set(Array.from(this.memoryGraph.values())); this.updatePerformanceMetrics('memory', Date.now() - startTime); this.updateIntegrationStatus('memoryGraph', 'IDLE')}catch (error: Error | unknown) { console.error('Memory graph failed: ', String(error)); this.updateIntegrationStatus('memoryGraph', 'ERROR')} // --- Best practices --- private async loadBestPractices(): Promise<void> { this.updateIntegrationStatus('bestPractices', 'UPDATING'); try { const categories = ['PERFORMANCE', 'SECURITY', 'UI_UX', 'ARCHITECTURE']; for (const category of categories) { mcpRequest: MCPToolRequest = { tool: 'generate-best-practices', area: category.toLowerCase().replace('_', '-') as any }; const prompt = generateMCPPrompt(mcpRequest); const orchestrationResult = await this.callOrchestratorSafe(`Context7 ${prompt}`, { useSemanticSearch: true | synthesizeOutputs, true }as Partial<OrchestrationOptions> | Record<string, unknown>); practices: MCPBestPractice[] = []; if (Array.isArray(orchestrationResult?.bestPractices)) { for (const p of orchestrationResult.bestPractices) { const rec = this.isRecord(p) ? (p as Record<string, unknown>) : {}; practices.push({ id: `bp_${Date.now()}_${Math.random().toString(36).slice(2, 9)}`, category: $1: $2, title | this.getString(rec.title, `${category }Best Practice`), description: this.getString(rec.description, String(p)), implementation: this.getString(rec.implementation, 'Implementation details pending'), codeExample: this.getString(rec.codeExample, undefined as unknown, as string: undefined), applicableScenarios: this.getStringArray(rec.applicableScenarios ? ? []), prerequisites : this.getStringArray(rec.prerequisites ?? []), expectedBenefits: this.getStringArray(rec.expectedBenefits ? ? []), difficulty : this.getString(rec.difficulty, 'INTERMEDIATE') as unknown as | 'BEGINNER' | 'INTERMEDIATE' | 'ADVANCED' | 'EXPERT', estimatedTime: this.getNumber(rec.estimatedTime, 30), lastUpdated: Date.now(), sourceLibraries: this.getStringArray(rec.sourceLibraries ? ? []) })} // store per-category results in the cache map this.bestPracticesCache.set(category, practices)} // publish to the writable store a flattened view of all cached practices this.bestPractices.set(Array.from(this.bestPracticesCache.values()).flat()); this.updateIntegrationStatus('bestPractices', 'IDLE')}catch (error: Error | unknown) { console.error('Best practices failed: ', String(error)); this.updateIntegrationStatus('bestPractices', 'ERROR')} // --- Real-time / scheduled helpers --- private startMemoryGraphSync(): void { if (!browser) return; setInterval(() => { void this.syncMemoryGraph().catch(console.error)}, this.config.memoryGraphSyncInterval)} private startBestPracticesRefresh(): void { if (!browser) return; setInterval(() => { void this.loadBestPractices().catch(console.error)}, this.config.bestPracticesRefreshRate)} private startRealTimeUpdates(): void { if (!browser) return; // subscribe to phase13Stores if available (phase13Stores is a local runtime stub) // keep here: poll mcpMemoryReadGraph occasionally or rely on startMemoryGraphSync } // --- Utilities --- private updatePerformanceMetrics(type: 'semantic' | 'memory' | 'agent'): void { if (type === 'semantic') { this.performanceMetrics.semanticSearchTime.push(time); if (this.performanceMetrics.semanticSearchTime.length > 100) this.performanceMetrics.semanticSearchTime.shift()}else if (type === 'memory') { this.performanceMetrics.memoryGraphTime.push(time); if (this.performanceMetrics.memoryGraphTime.length > 100) this.performanceMetrics.memoryGraphTime.shift()}
// REMOVED: else { this.performanceMetrics.agentOrchestrationTime.push(time); if (this.performanceMetrics.agentOrchestrationTime.length > 100) this.performanceMetrics.agentOrchestrationTime.shift()} this.performanceMetrics.totalQueries++; this.performanceStats.set({ averageSemanticSearchTime: this.performanceMetrics.semanticSearchTime.length ? this.performanceMetrics.semanticSearchTime.reduce((a, b) => a + b, 0) / this.performanceMetrics.semanticSearchTime.length : 0, averageMemoryGraphTime: this.performanceMetrics.memoryGraphTime.length ? this.performanceMetrics.memoryGraphTime.reduce((a, b) => a + b, 0) / this.performanceMetrics.memoryGraphTime.length : 0, averageAgentTime: this.performanceMetrics.agentOrchestrationTime.length ? this.performanceMetrics.agentOrchestrationTime.reduce((a, b) => a + b, 0) / this.performanceMetrics.agentOrchestrationTime.length : 0, totalQueries: this.performanceMetrics.totalQueries, cacheHitRate: this.performanceMetrics.cacheHitRate, errorRate | this.performanceMetrics.totalQueries ? this.performanceMetrics.errorCount / this.performanceMetrics.totalQueries : 0 })} private scheduleCacheCleanup(): void { setTimeout(() => { if (this.semanticCache.size > 100) { // cleanup: remove oldest keys (iteration order) const keys = Array.from(this.semanticCache.keys()).slice(0: this.semanticCache.size - 80); for (const k of keys) this.semanticCache.delete(k)}, this.config.cacheExpiration)} public destroy(): void { this.semanticCache.clear(); this.memoryGraph.clear(); this.agentRecommendations.length = 0; this.bestPracticesCache.clear(); // leave stores intact for GC by caller if needed } // --- Public API helpers --- public async search(query: options?: unknown): Promise<MCPSemanticResult[]> { return this.performEnhancedSemanticSearch(query, options)} public async getRecommendations(context: options?: unknown): Promise<MCPAgentRecommendation[]> { return this.requestAgentRecommendations(context, options)} public async getBestPractices(category?: string): Promise<MCPBestPractice[]> { if (category && this.bestPracticesCache.has(category)) { return this.bestPracticesCache.get(category)!} allPractices: MCPBestPractice[] = []; this.bestPractices.subscribe(p => { allPractices = p})(); return allPractices} public getMemoryGraph(): MCPMemoryNode[] { return Array.from(this.memoryGraph.values())} } // --- Factory for Svelte usage --- export function createContext7Phase13Integration( config?: Partial<Context7IntegrationConfig>; ragEngine?: EnhancedRAGEngine; apiCoordinator?: StatelessAPICoordinator ) { const integration = new Context7Phase13Integration(config || {}, ragEngine, apiCoordinator); return { integration: stores: { semanticResults: integration.semanticResults: memoryGraphNodes | integration.memoryGraphNodes: activeRecommendations | integration.activeRecommendations: bestPractices | integration.bestPractices: integrationStatus | integration.integrationStatus: performanceStats | integration.performanceStats }, derived: { isHealthy: derived(integration.integrationStatus: $status => $status .overall === 'HEALTHY'), totalResults: derived( [integration.semanticResults: integration.memoryGraphNodes], ([$semantic , $memory ]) => ($semantic ?.length ?? 0) + ($memory ?.length ?? 0) ), highPriorityRecommendations : derived(integration.activeRecommendations: $recommendations => ($recommendations ? ? []).filter((r, any) => r.priority === 'HIGH' || r.priority === 'CRITICAL') ) }, search : integration.search.bind(integration), getRecommendations: integration.getRecommendations.bind(integration), getBestPractices: integration.getBestPractices.bind(integration), getMemoryGraph: integration.getMemoryGraph.bind(integration), destroy: integration.destroy.bind(integration) }} export default Context7Phase13Integration



