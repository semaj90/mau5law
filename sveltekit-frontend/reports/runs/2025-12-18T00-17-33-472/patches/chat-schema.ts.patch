--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\db\chat-schema.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\db\chat-schema.ts (fixed)

@@ Line 2, Col 167 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'pgTable' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadata'

@@ Line 2, Col 315 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'sql' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadata').de

@@ Line 2, Col 345 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'timestamp' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadat

@@ Line 2, Col 404 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'timestamp' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadat

@@ Line 2, Col 466 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'integer' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadata'

@@ Line 2, Col 523 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'boolean' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadata'

@@ Line 2, Col 600 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'pgTable' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadata'

@@ Line 2, Col 667 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'uuid' cannot be used as a value because it was imported using 'import type'. // Database schema for chat functionality with pgvector support import { text, jsonb } from 'drizzle-orm/pg-core'; import type { pgTable, uuid, timestamp, boolean, integer, decimal } from 'drizzle-orm/pg-core';; import type { sql  } from 'drizzle-orm'; export const chatSessions = pgTable('chat_sessions', { id, uuid('id').primaryKey(), model: text('model').notNull().default('gemma3-legal'), metadata: jsonb('metadata').d

