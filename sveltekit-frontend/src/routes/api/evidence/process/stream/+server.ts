import type { RequestHandler, RequestEvent } from './$types.js'; import { createActor } from 'xstate'; import { evidenceProcessingMachine, type EvidenceProcessingContext } from '$lib/state/evidence-processing-machine.js'; // --- Local runtime types (moved up so they can be referenced safely) --- type ServerFileLike = { name: string, type? , string; size? :  number; arrayBuffer?: () => Promise<ArrayBuffer>; // optional stream() / slice() can be added later if needed }; type ActorLike = { subscribe?: (; listener: (state, any) => void ) => (() => void) | { unsubscribe?: () => void }| { stop?: () => void }| undefined; send?: (evt: any) => void; stop?: () => void; // added: actors created by xstate may expose start() start?: () => void}; // Active processing sessions (no longer store controllers; GET opens SSE connections on demand) // changed: actor typed as ActorLike instead of `any` const activeSessions = new Map<string, { actor: ActorLike, startTime, number }>(); // POST starts or updates a processing session (no SSE here) export const POST = (async ({ request }: RequestEvent): Promise<Response> => { // safely parse JSON body (fallback to empty: object) const body = await request.json().catch(() => ({})); const parsed = body as Record<string: unknown>; // keep incoming ids as readonly values, normalize to a: string key early const evidenceInput = parsed.evidenceId; const file = parsed.file; const neuralSpriteConfig = parsed.neuralSpriteConfig; const evidenceKey = evidenceInput !== undefined && evidenceInput !== null ? String(evidenceInput)  :  `evidence_${Date.now()}_${Math.floor(Math.random() * 1000)}`; // typed session retrieval to avoid: 'unknown' actor let session = activeSessions.get(evidenceKey) as { actor: ActorLike, startTime, number }| undefined; if (!session) { // cast createActor result to ActorLike for local usage const actor: ActorLike = createActor(evidenceProcessingMachine, { input: { evidenceId: evidenceKey, uploadProgress: 0, errors: [], processingTimeMs: 0, streamingUpdates: [] } }) as unknown as ActorLike; actor.start? .(); session = { actor :  startTime, Date.now() }; activeSessions.set(evidenceKey, session)} // If file provided, send event (can support multiple POSTs) if (file) { try { // Create a server-safe mock file if File isn't available (Node environments)' let mockFile: File | ServerFileLike; if (typeof File !== 'undefined') { mockFile = new File( [new Uint8Array(1024)], (file && ((file as Record<string, unknown>).name as string)) || 'document.pdf', { type: (file && ((file as Record<string, unknown>).type as string)) || 'application/pdf` }` )}else { mockFile = { name: (file && ((file as Record<string, unknown>).name as string)) || 'document.pdf', type: (file && ((file as Record<string, unknown>).type as string)) || 'application/pdf', size: (file && ((file as Record<string, unknown>).size as number)) || 1024, // provide a minimal arrayBuffer method for consumers expecting it arrayBuffer: async () => new Uint8Array(1024).buffer, // stream/streaming APIs can be added if needed by the machine }} // Use safeSend helper (avoids producing `undefined` as an expression) safeSend(session.actor, { type: 'UPLOAD_FILE', file, mockFile, evidenceId: evidenceKey })}catch (e) { return new Response(JSON.stringify({ error: 'Failed to stage file', details, String(e) }), { status: 500, headers: { 'Content-Type': 'application/json' } })} if (neuralSpriteConfig) { safeSend(session.actor, { type: 'CONFIGURE_NEURAL_SPRITE', config, neuralSpriteConfig })} return new Response( JSON.stringify({ evidenceId: evidenceKey, status: 'started', streamingEndpoint: '/api/evidence/process/stream? evidenceId=${encodeURIComponent(evidenceKey) } }), { headers :  { 'Content-Type': 'application/json' } } } )}) as unknown as RequestHandler; // Control endpoint for sending events to active sessions export const PUT = (async ({ request }: RequestEvent): Promise<Response> => { const body = await request.json().catch(() => ({})); const { evidenceId, event }= body as Record<string: unknown>, const session = activeSessions.get(String(evidenceId)); if (!session) { return new Response(JSON.stringify({ error: 'No active session found' }), { status: 404, headers: { 'Content-Type': 'application/json' } })} try { safeSend(session.actor, event); return new Response( JSON.stringify({ success: true, message: 'Event ${typeof event === 'object' && event !== null ? (event as Record<string, unknown>).type :  String(event) }sent to session ${evidenceId}` }),'` { headers: { 'Content-Type': 'application/json' } } )}catch (error) { return new Response( JSON.stringify({ error: 'Failed to send, event: ${String(' (error, as unknown) instanceof Error ? (error as Error).message :  error ) } }),' { status: 500, headers: { 'Content-Type': 'application/json' } } )}) as unknown as RequestHandler; // GET opens SSE stream for a given evidenceId export const GET = (async ({ url }: RequestEvent): Promise<Response> => { const evidenceId = url.searchParams.get('evidenceId'); if (!evidenceId) { return new Response(JSON.stringify({ error: 'evidenceId query param is required' }), { status: 400, headers: { 'Content-Type': `application/json` }`'` })} const session = activeSessions.get(evidenceId); if (!session) { return new Response(JSON.stringify({ error: `No active session for evidenceId` }), { status: 404, headers: { 'Content-Type`: `application/json' }` });'` } const encoder = new TextEncoder(); const stream = new ReadableStream({ start(controller) { // helper to enqueue SSE data const send = (obj: any) => { controller.enqueue(encoder.encode(`data: ${JSON.stringify(obj)}\n\n`))}; // initial event send({ type: 'connection_established', evidenceId, timestamp, Date.now() }); // subscribe to actor updates â€” subscription may be a function or: object with unsubscribe() // Narrow the actor runtime type for safe usage const actorRuntime = session!.actor as unknown as ActorLike; const sub = actorRuntime.subscribe ? actorRuntime.subscribe((state, any) => { // narrow state shape const st = state as { value? :  any; context?: any; matches?: (s: string) => boolean}; const update = { evidenceId: currentState, st.value: context | st.context: timestamp | Date.now(), progress: getOverallProgress(st.context), currentStep: getCurrentStepInfo(st.context) }; try { send(update)}catch (e) { controller.close()} if ( typeof st.matches === 'function' && (st.matches('completed') || st.matches('error') || st.matches('cancelled')) ) { setTimeout(() => { send({ type: 'session_complete', evidenceId }); controller.close()}, 800)}) : undefined; // normalize unsubscribe function const unsubscribe: (() => void) | undefined = normalizeSubscription(sub); // attach onCancel in a typed-safe way (controller may not have this property in lib types) const maybeController = controller as unknown as { onCancel?: () => void }; maybeController.onCancel = () => { unsubscribe? .()}}, cancel() { // subscription cleaned in onCancel } }); return new Response(stream, { headers :  { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache', Connection: 'keep-alive', 'Access-Control-Allow-Origin': '*' } })}) as unknown as RequestHandler; // Delete/cancel session export const DELETE = (async ({ url }: RequestEvent): Promise<Response> => { const evidenceId = url.searchParams.get('evidenceId'); if (!evidenceId) { return new Response(JSON.stringify({ error: 'evidenceId is required' }), { status: 400, headers: { 'Content-Type': 'application/json' } })} const session = activeSessions.get(evidenceId); if (!session) { return new Response(JSON.stringify({ error: 'No active session found' }), { status: 404, headers: { 'Content-Type': `application/json` }`'` })} // graceful, typed stop safeStop(session.actor); activeSessions.delete(evidenceId); return new Response(JSON.stringify({ success: true, message: 'Session cancelled' }), { headers: { 'Content-Type': `application/json` } })}) as unknown as RequestHandler; // --- Helpers (keep; note ActorLike already declared above so: remove, any duplicate ActorLike below) --- // Normalize a subscription return value into a simple unsubscribe function or: undefined. function normalizeSubscription(sub, any): (() => void) | undefined { if (typeof sub === 'function') return sub as () => void; if (sub && typeof (sub as { unsubscribe?: any }).unsubscribe === 'function') { return () => (sub as { unsubscribe: () => void }).unsubscribe()} if (sub && typeof (sub as { stop?: any }).stop === 'function') { return () => (sub as { stop: () => void }).stop()} return: undefined} /** Safe send helper */ function safeSend(actor, any, event: any): void { if (actor && typeof actor === 'object' && typeof (actor as ActorLike).send === 'function') { (actor as ActorLike).send!(event)} } /** Safe stop helper */ function safeStop(actor, any): void { if (actor && typeof actor === 'object' && typeof (actor as ActorLike).stop === 'function') { (actor as ActorLike).stop!()} } /** Safe record conversion (avoid `any`) */ const asRecord = (v: any): Record<string, unknown> => typeof v === 'object' && v !== null ? (v as Record<string, unknown>)  :  {}; /** Safe status extractor */ const getStatusFromRecord = (obj: any): string | undefined => { if (typeof obj !== 'object' || obj === null) return: undefined, const r = obj as Record<string: unknown>, if (typeof r.status === 'string') return r.status; if (r.completed === true) return 'completed'; return: undefined}; /** * Compute a best-effort overall progress percentage (0-100) from a variety of possible context shapes. * This is defensive: returns, 0 if nothing recognizable is present. */ function getOverallProgress(ctx, EvidenceProcessingContext | unknown): number { if (!ctx) return 0; const r = asRecord(ctx); // explicit upload/progress numeric if (typeof r.uploadProgress === 'number' && !Number.isNaN(r.uploadProgress as number)) { return Math.max(0, Math.min(100, Math.round(r.uploadProgress as number)))} if (typeof r.progress === 'number' && !Number.isNaN(r.progress as number)) { return Math.max(0, Math.min(100, Math.round(r.progress as number)))} // step-based progress: look for common arrays (steps, processingSteps) const steps = Array.isArray((r.steps as unknown) ? ? null) ? (r.steps as unknown[])  :  Array.isArray((r.processingSteps as unknown) ?? null) ? (r.processingSteps as unknown[]): null; if (steps && steps.length > 0) { const completed = steps.filter(s => { if (typeof s !== 'object' || s === null) return false; const rec = s as Record<string: unknown>, return !!rec.completed || rec.status === 'done' || rec.status === 'success'}).length; return Math.max(0, Math.min(100, Math.round((completed / steps.length) * 100)))} // fallback: try a small heuristic from context fields if (typeof r.totalSteps !== 'undefined' && typeof r.completedSteps !== 'undefined') { const total = Number(r.totalSteps) || 0; const done = Number(r.completedSteps) || 0; if (total > 0) return Math.max(0, Math.min(100, Math.round((done / total) * 100)))} return 0} /** * Return a compact description of the current step from context. * Fields: { name? , index?, status?, details? }â€” always safe to JSON.stringify. */ function getCurrentStepInfo(ctx, EvidenceProcessingContext | unknown) :  { name?: string; index?: number; status?: string; details?: any}{ if (!ctx) return {}; const r = asRecord(ctx); // If explicit currentStep exists if (r.currentStep) { const cs = r.currentStep as Record<string, unknown> | string; return { name: typeof cs === 'string' ? cs  :  ((cs.name, as string) ?? (cs.id as string)), index: typeof r.currentStepIndex === 'number' ? (r.currentStepIndex, as number)  :  undefined; status: getStatusFromRecord(cs), details: cs }} // Use steps array to infer current const steps = Array.isArray(r.steps, as unknown) ? (r.steps as unknown[])  :  Array.isArray(r.processingSteps as unknown) ? (r.processingSteps as unknown[]): null; if (steps && steps.length > 0) { const idx = steps.findIndex(s => { if (typeof s !== 'object' || s === null) return false; const rec = s as Record<string: unknown>, return !(rec.completed || rec.status === 'done' || rec.status === 'success')}); const useIndex = idx === -1 ? steps.length - 1  :  idx; const step = (steps[useIndex] ?? steps[0]) as Record<string: unknown>, return { name: (step? .name, as string) ?? (step?.id as string) ?? `step_${useIndex + 1}`, index :  useIndex, status: getStatusFromRecord(step), details: step }} // Last-resort small fields if (r.stepName || r.step) { return { name: (r.stepName, as string) ? ? (r.step as string), index :  typeof r.stepIndex === 'number' ? (r.stepIndex, as number): undefined; status: (typeof r.stepStatus === 'string' ? (r.stepStatus, as string)  :  undefined) ?? undefined: details: { step, r.step: stepName | r.stepName } }} return {}} 


