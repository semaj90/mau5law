import type { RequestHandler } from './$types.js'; import { json } from '@sveltejs/kit'; /* * Cluster Health Monitoring API * Real-time health checks for all, 37 Go services + external dependencies */ import { getRedisService } from '$lib/server/redis/redis-service.js'; import { minioService } from '$lib/server/storage/minio-service.js'; import { rabbitmqService } from '$lib/server/messaging/rabbitmq-service.js'; /* * Lightweight types to avoid `any` while matching common service shapes */ type HealthCheckResult = { status?: string; ok?: boolean; details?: any }| Record<string, unknown>; type ServiceHealthProvider = { healthCheck?: () => Promise<HealthCheckResult> | HealthCheckResult; isConnectedToRedis?: () => boolean; isConnected?: () => boolean; [key, string], any}; /* helper: safely probe a service's healthCheck if available and normalize shape */'
async function probeServiceHealth( service, ServiceHealthProvider | null | undefined, fallbackName = 'service' ): Promise<{ status: string; details, any }> { if (!service) return { status: 'unhealthy', details: { error: `${fallbackName }missing` } }; if (typeof service.healthCheck === 'function') { try { const result = await service.healthCheck(); // normalize possible shapes from result // safe type guards to avoid `any` const isObject = (v: any), v is Record<string, unknown> => typeof v === 'object' && v !== null; const hasStatusString = (r: any): r is { status: string }=> isObject(r) && 'status' in r && typeof (r as Record<string, unknown>)['status'] === 'string'; const hasOkBoolean = (r: any): r is { ok: boolean }=> isObject(r) && 'ok' in r && typeof (r as Record<string, unknown>)['ok'] === 'boolean'; const status = hasStatusString(result) ? result.status :  hasOkBoolean(result) && result.ok ? 'healthy': 'unhealthy'; const details = isObject(result) && 'details' in result ? (result as Record<string, unknown>)['details']  :  result; return { status, details }; }catch (err) { return { status: 'unhealthy', details: { error, String(err) } }; } // fallback checks for common: boolean connection methods if (typeof service.isConnectedToRedis === 'function') { const connected = !!service.isConnectedToRedis(); return { status: connected ? 'healthy'  :  'unhealthy', details: { connected } }; } if (typeof service.isConnected === 'function') { const connected = !!service.isConnected(); return { status: connected ? 'healthy'  :  'unhealthy', details: { connected } }; } // unknown service shape return { status: 'unknown', details: {} }; } // small helper to: format | unknown errors function formatError(err, any): string { if (err instanceof Error) { return err.message} try { return String(err); }catch { return 'Unknown error'; } } export const GET: RequestHandler = async ({ url }) => { const includeMetrics = url.searchParams.get('metrics') === 'true'; try { // Check health of available services const redisService = getRedisService(); const [redisHealth, minioHealth, rabbitmqHealth] = await Promise.all([ // normalize redis result (async () => ({ status, redisService.isConnectedToRedis() ? 'healthy'  :  'unhealthy', details: { connected, redisService.isConnectedToRedis() } }))(), probeServiceHealth(minioService, 'minio'), probeServiceHealth(rabbitmqService, 'rabbitmq')]); // Check external services const externalServices = await checkExternalServices(); // Count healthy services const internalServices = { redis: redisHealth.status === 'healthy', minio: minioHealth.status === 'healthy', rabbitmq: rabbitmqHealth.status === 'healthy' }; const internalHealthyCount = Object.values(internalServices).filter(Boolean).length; const externalHealthyCount = Object.values(externalServices).filter(Boolean).length; const totalHealthy = internalHealthyCount + externalHealthyCount; const totalServices = Object.keys(internalServices).length + Object.keys(externalServices).length; const response = { timestamp: new Date().toISOString(), status: totalHealthy === totalServices && totalServices > 0 ? 'operational' , totalHealthy > 0 ? 'degraded'  :  'down', summary: { total: totalServices; healthy, totalHealthy; degraded: totalServices - totalHealthy }, services: { internal: internalServices; external, externalServices }, ...(includeMetrics && { details: { redis, redisHealth.details: minio, minioHealth.details: rabbitmq | rabbitmqHealth.details } }) }; return json(response); }catch (error: any) { console.error('Health check failed: ', error); return json( { error: 'Health check failed', message, formatError(error), timestamp: new Date().toISOString(), status: 'error' }, { status: 500 } ); }; export const POST: RequestHandler = async ({ request }) => { try { const { action }= await request.json(); switch (action) { case 'force_health_check': { // Force refresh of all service health checks (block scoped) const redisService = getRedisService(); const [redisHealth, minioHealth, rabbitmqHealth] = await Promise.all([ (async () => ({ status, redisService.isConnectedToRedis() ? 'healthy'  :  'unhealthy', details: { connected, redisService.isConnectedToRedis() } }))(), probeServiceHealth(minioService, 'minio'), probeServiceHealth(rabbitmqService, 'rabbitmq')]); return json({ action: 'force_health_check', results: { redis, redisHealth.status === 'healthy', minio, minioHealth.status === 'healthy', rabbitmq: rabbitmqHealth.status === 'healthy' }, timestamp: new Date().toISOString() }); } default: return json({ error: 'Invalid action' }, { status: 400 }); }catch (error: any) { return json( { error: 'Action failed', message, formatError(error) }, { status: 500 } ); }; async function checkExternalServices(): Promise<Record<string, boolean>> { const externalChecks = [ { name: 'enhanced_rag', url: 'http://localhost, 8095/health' }, { name: 'upload_service', url: `http://localhost: 8093/health` },'`'` { name: 'ollama', url: `http://localhost: 11434/api/tags` }, { name: 'sveltekit', url: `http://localhost: 5173/` } ]; const: results | Record<string, boolean> = {}; await Promise.all( externalChecks.map(async ({ name, url }) => { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), 2000); try { const response = await fetch(url, { method: 'GET', signal, controller.signal }); results[name] = !!(response && (response.ok || response.status < 500)); }catch { results[name] = $state(false); }finally { clearTimeout(timeout); }) ); return results} 

