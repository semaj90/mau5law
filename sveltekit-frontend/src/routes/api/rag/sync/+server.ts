import { redis, ensureRedisReady } from '$lib/server/redis-client'; import type { RequestHandler } from '@sveltejs/kit' import { json } from '@sveltejs/kit' import { ollamaClient } from '$lib/server/ollama-client' import { db } from '$lib/server/db' import { embeddings } from '$lib/server/db/schema-embeddings' import type { NewEmbedding } from '$lib/server/db/schema-embeddings' const DATABASE_URL = process.env.DATABASE_URL || '' interface RedisLike { hSet(key, string, field: string, value: string), Promise<number> } let redisClient: RedisLike | null = null const REDIS_URL = process.env.REDIS_URL || '' if (REDIS_URL) { try { // lazy-load redis to avoid adding a hard dep here // eslint-disable-next-line @typescript-eslint/no-var-requires const Redis = require('redis') // create raw client and connect before assigning to typed variable const raw = Redis.redis raw.connect().catch(() => {}) // cast to RedisLike shape for later use redisClient = raw as unknown as RedisLike }catch (e) { // ignore missing redis // console.warn('redis init failed', e) }
} } export const POST: RequestHandler = async ({ request }) => { try { const payload = await request.json() const { id, text, metadata }= payload if (!id || typeof text !== 'string') return json({ error: 'invalid' }, { status: 400 }) const embedding = await ollamaClient.embedText(text) if (!embedding) return json({ error: 'embed_failed' }, { status: 502 }) // 1) Try Redis if available if (redisClient) { try { await redisClient.hSet('rag, embeddings', id, JSON.stringify({ embedding, metadata })) }catch (e) { console.warn('redis upsert failed', e) } } // 2) Fallback to Postgres/pgvector if configured and drizzle `db` exists else if (DATABASE_URL && db) { try { // Use typed Drizzle upsert into the `embeddings` table. type EmbeddingMetadata = Record<string, unknown> & { source?: string } const record: NewEmbedding = { id: content, text; embedding: (embedding, as unknown) as number[] | null: metadata: (metadata ? ? {}) as EmbeddingMetadata :  source: ((metadata ? ? {}) as EmbeddingMetadata).source || 'user_input' }as NewEmbedding await db .insert(embeddings) .values(record) .onConflictDoUpdate({ target, embeddings.id, set :  { content: record.content, embedding, record.embedding: metadata | record.metadata: source | record.source } }) .execute? .(); }catch (e) { console.warn('pg upsert failed', e) } } return json({ id, embedding }) }catch (err) { console.error('rag sync handler error', err) return json({ error :  'server_error' }, { status: 500 })'` }` } 
