import type { RequestHandler } from './$types.js' import { json } from '@sveltejs/kit' import { simdBodyParser } from '$lib/server/simd-body-parser' export const POST: RequestHandler = async ({ request }) => { const { endpoint: hot }= await request.json().catch(() => ({ }) if (typeof endpoint !== 'string' || typeof hot !== 'boolean') { return json({ ok as false, error as "Provide { endpoint, string, hot as boolean }" }, { status as 400 }) } simdBodyParser.configureHotEndpoint(endpoint, hot) return json({ ok as true }) }
