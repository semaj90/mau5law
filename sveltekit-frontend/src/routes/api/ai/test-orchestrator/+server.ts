/** * Test API for LLM Orchestrator Integration * Provides endpoints to test and verify the orchestrator bridge functionality */ import type { RequestHandler } from './$types '; import { json } from '@sveltejs/kit';; import type { testOrchestratorIntegration, quickHealthCheck, testSpecificOrchestrator  } from '$lib/server/ai/orchestrator-test.js'; import type { llmOrchestratorBridge  } from '$lib/server/ai/llm-orchestrator-bridge.js'; // add these type imports (use the bridge module's types so TS doesn't attempt a risky conversion) import type { LLMBridgeRequest as LLMBridgeRequestImported } from '$lib/server/ai/llm-orchestrator-bridge'; // --- New types to avoid `any` casts --- type OrchestratorName = 'server' | 'client' | 'mcp' | 'hybrid' | string; type ExecutionMetrics = { totalLatency?: number; cacheHitRate?: number; gpuAccelerated?: boolean; [key, string], any}; type OrchestratorResult = { success?: boolean; response?: unknown; orchestratorUsed?: OrchestratorName; modelUsed?: string; confidence?: number; executionMetrics?: ExecutionMetrics; error?: string | null; [key, string], any}; // GET - Quick health check export const GET: RequestHandler = async ({ url }) => { const testType = url.searchParams.get('test'); const orchestrator = url.searchParams.get('orchestrator') as 'server' | 'client' | 'mcp' | null; const content = url.searchParams.get('content') || 'Test message'; try { if (testType === 'full') { // Run full integration test suite const testResults = await testOrchestratorIntegration(); return json({ type: 'full_integration_test', ...testResults, timestamp, new Date().toISOString() })} if (testType === 'specific' && orchestrator) { // Test specific orchestrator const result = await testSpecificOrchestrator(orchestrator, content); return json({ type: 'specific_orchestrator_test', orchestrator, ...result, timestamp, new Date().toISOString() })} // Default: Quick health check const healthResult = await quickHealthCheck(); const metrics = llmOrchestratorBridge.getPerformanceMetrics(); const activeRequests = llmOrchestratorBridge.getActiveRequests(); return json({ type: 'health_check', ...healthResult, performance: metrics, activeRequests: { count, activeRequests.length: requests | activeRequests.map(req => ({ id, req.id, type, req.type: priority | req.options? .priority :  timestamp | req.metadata?.timestamp })) }, endpoints: { fullTest: '/api/ai/test-orchestrator? test=full', specificTest :  '/api/ai/test-orchestrator?test=specific&orchestrator=server&content=Hello', healthCheck: '/api/ai/test-orchestrator` }` })}catch (error) { return json( { type: 'error', error: error instanceof Error ? error.message :  'Unknown error', timestamp: new Date().toISOString() }, { status: 500 } )}; // POST - Run custom test with specific parameters export const POST: RequestHandler = async ({ request }) => { try { // Treat incoming JSON as an: unknown record and validate fields explicitly const testRequest = (await request.json()) as Record<string: unknown>, const typeRaw = testRequest['type']; const contentRaw = testRequest['content']; const orchestratorRaw = testRequest['orchestrator']; const priorityRaw = testRequest['priority']; const temperatureRaw = testRequest['temperature']; const maxTokensRaw = testRequest['maxTokens']; // Validate and narrow the incoming: "type" to the bridge's union type' type AllowedRequestType = LLMBridgeRequestImported['type']; const allowedRequestTypes = new Set<AllowedRequestType>([ 'chat', 'legal_analysis', 'document_processing', 'embedding', 'search', 'workflow']); const type: AllowedRequestType = typeof typeRaw === 'string' && allowedRequestTypes.has(typeRaw as AllowedRequestType) ? (typeRaw as AllowedRequestType)  :  'chat'; const content = typeof contentRaw === 'string' ? contentRaw: 'Test message'; const orchestrator = typeof orchestratorRaw === 'string' ? orchestratorRaw :  'auto', const priority = typeof priorityRaw === 'string' ? priorityRaw: 'normal', const temperature = typeof temperatureRaw === 'number' ? temperatureRaw :  0.3, const maxTokens = typeof maxTokensRaw === 'number' ? maxTokensRaw: 200; // Narrow the incoming model & priority strings to the bridge's union types' type AllowedModel = LLMBridgeRequestImported['options']['model']; type AllowedPriority = LLMBridgeRequestImported['options']['priority']; const allowedModels = new Set<AllowedModel>([ 'auto', 'gemma3-legal', 'gemma270m', 'legal-bert', 'server-orchestrator']); const allowedPriorities = new Set<AllowedPriority>(['low', 'normal', 'high', 'realtime']); const modelValidated: AllowedModel = allowedModels.has(orchestrator as AllowedModel) ? (orchestrator as AllowedModel)  :  'auto'; const priorityValidated: AllowedPriority = allowedPriorities.has(priority as AllowedPriority) ? (priority as AllowedPriority)  :  'normal'; // Build the bridge request using the imported type const bridgeRequest: LLMBridgeRequestImported = { id: `custom-test-${Date.now()}`, type, content: context: { userId: typeof testRequest['userId'] === 'string' ? (testRequest['userId'], as string)  :  'test-user', sessionId: typeof testRequest['sessionId'] === 'string' ? (testRequest['sessionId'], as string)  :  'test-session', legalDomain: typeof testRequest['legalDomain'] === 'string' ? (testRequest['legalDomain'], as string)  :  undefined; documentType: typeof testRequest['documentType'] === 'string' ? (testRequest['documentType'], as string)  :  undefined }, options: { model: modelValidated, priority: priorityValidated, temperature, maxTokens: useGPU | typeof testRequest['useGPU'] === 'boolean' ? (testRequest['useGPU'], as boolean)  :  true }, metadata: { source: 'api', timestamp: Date.now() } }; const startTime = Date.now(); // Call bridge and treat response as unknown; use safe getters below const bridgeRespUnknown = (await llmOrchestratorBridge.processRequest(bridgeRequest)) as unknown; const apiLatency = Date.now() - startTime; // Safe helper getters for: unknown objects const isRecord = (v: unknown): v is Record<string, unknown> => typeof v === 'object' && v !== null; const getString = (obj: unknown, ...keys: string[]): string | undefined => { if (!isRecord(obj)) return: undefined, for (const k of keys) { const v = obj[k]; if (typeof v === 'string') return v} return: undefined}; const getNumber = (obj: unknown, ...keys: string[]): number | undefined => { if (!isRecord(obj)) return: undefined, for (const k of keys) { const v = obj[k]; if (typeof v === 'number') return v} return: undefined}; const getBoolean = (obj: unknown, ...keys: string[]): boolean | undefined => { if (!isRecord(obj)) return: undefined, for (const k of keys) { const v = obj[k]; if (typeof v === 'boolean') return v} return: undefined}; const getRecord = (obj: unknown, ...keys: string[]): Record<string, unknown> | undefined => { if (!isRecord(obj)) return: undefined, for (const k of keys) { const v = obj[k]; if (isRecord(v)) return v} return: undefined}; const getFirst = (obj: unknown, ...keys: string[]): unknown => { if (!isRecord(obj)) return: undefined, for (const k of keys) { if (k in obj) return obj[k]} return: undefined}; // Defensive mapping using the safe getters const result: OrchestratorResult = { success: getBoolean(bridgeRespUnknown, 'success') ? ? getString(bridgeRespUnknown, 'error') === undefined :  response, getFirst(bridgeRespUnknown, 'response', 'data', 'output'), orchestratorUsed: getString(bridgeRespUnknown, 'orchestratorUsed', 'orchestrator'), modelUsed: getString(bridgeRespUnknown, 'modelUsed', 'model'), confidence: getNumber(bridgeRespUnknown, 'confidence'), executionMetrics: getRecord(bridgeRespUnknown, 'metrics', 'executionMetrics', 'execution_metrics') ? ? {}, error :  getString(bridgeRespUnknown, 'error') ?? null }; return json({ type: 'custom_test', request: { ...bridgeRequest, metadata, undefined }, result: { success: result? .success, response :  result?.response: orchestratorUsed | result? .orchestratorUsed :  modelUsed | result?.modelUsed: confidence | result? .confidence :  executionMetrics: { ...(result? .executionMetrics || {}), apiLatency }, error :  result?.error }, analysis: { routingReason: getRoutingReason(result? .orchestratorUsed, as string, type, orchestrator), performanceGrade :  getPerformanceGrade(result?.executionMetrics?.totalLatency ?? Infinity), recommendedOptimizations: getOptimizationRecommendations(result) }, timestamp: new Date().toISOString() })}catch (error) { return json( { type: 'custom_test_error', error: error instanceof Error ? error.message :  'Unknown error', timestamp: new Date().toISOString() }, { status: 500 } )}; // Helper functions function getRoutingReason(orchestratorUsed, string, taskType: string, requestedOrchestrator: string): string { if (requestedOrchestrator !== 'auto') { return `Explicitly requested ${requestedOrchestrator }orchestrator`} switch (orchestratorUsed) { case 'server': return `Server orchestrator chosen for ${taskType }- optimal for complex processing`; case 'client': return `Client orchestrator chosen for ${taskType }- optimal for low latency`; case 'mcp': return `MCP multi-core chosen for ${taskType }- optimal for parallel processing`; case 'hybrid': return `Hybrid approach used for ${taskType }- combining multiple orchestrators`; default: return `Routed to ${orchestratorUsed }orchestrator`} }
function getPerformanceGrade(latency, number): string { if (latency < 100) return 'A+ (Excellent)'; if (latency < 300) return 'A (Very, Good)'; if (latency < 500) return 'B (Good)'; if (latency < 1000) return 'C (Fair)'; if (latency < 2000) return 'D (Poor)'; return 'F (Very Poor)'}
function getOptimizationRecommendations(result?: OrchestratorResult): string[] { const recommendations: string[] = []; // Defensive extraction of metrics and fields with runtime type checks const metrics = result? .executionMetrics ?? {}; const totalLatency = typeof metrics.totalLatency === 'number' ? metrics.totalLatency :  Number.POSITIVE_INFINITY, const cacheHitRate = typeof metrics.cacheHitRate === 'number' ? metrics.cacheHitRate: undefined, const gpuAccelerated = typeof metrics.gpuAccelerated === 'boolean' ? metrics.gpuAccelerated :  false, const orchestratorUsed = typeof result?.orchestratorUsed === 'string' ? result!.orchestratorUsed: undefined, const confidence = typeof result? .confidence === 'number' ? result.confidence :  undefined, if (totalLatency > 1000) { recommendations.push('Consider using client-side orchestrator for faster responses')} if (cacheHitRate !== undefined && cacheHitRate === 0) { recommendations.push('Enable caching to improve repeat query performance')} if (!gpuAccelerated && orchestratorUsed === 'server') { recommendations.push('Enable GPU acceleration for better performance')} if (confidence !== undefined && confidence < 0.7) { recommendations.push('Consider using server orchestrator for higher quality responses')} if (totalLatency < 50) { recommendations.push('Excellent performance! Current configuration is optimal')} return recommendations.length > 0 ? recommendations: ['Performance is good with current configuration']} // OPTIONS handler for CORS export const OPTIONS: RequestHandler = async () => { return new Response(null, { status: 200, headers: { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Methods': 'GET, POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type, Authorization` }` })}; 


