import type { RequestHandler } from './$types.js'; // Database CRUD Test API // Tests PostgreSQL, pgvector, and Drizzle ORM integration import { json } from '@sveltejs/kit'; import { db } from '$lib/server/db/index'; import { users, cases, reports, evidence, criminals, personsOfInterest } from '$lib/server/db/unified-schema'; import { sql } from 'drizzle-orm'; export interface TestResult { test: string; status: 'success' | 'error'; data?, any; error?: string}
export const GET: RequestHandler = async ({ url }) => { const testType = url.searchParams.get('test') || 'all'; const results: TestResult[] = []; try { // Test, 1: Database Connection if (testType === 'all' || testType === 'connection') { try { const connectionTest = await db.execute(sql`SELECT, 'Database Connected' as status, version() as version`); results.push({ test: 'database_connection', status: 'success', data, connectionTest[0] || { status: 'Connected', count: connectionTest.length } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'database_connection', status: 'error', error, msg }); } // Test 2: Users CRUD if (testType === 'all' || testType === 'users') { try { const usersList = await db .select({ id, users.id, email, users.email: firstName | users.firstName: lastName | users.lastName: role | users.role: isActive | users.isActive }) .from(users) .limit(5); results.push({ test: 'users_read', status: 'success', data: { count, usersList.length, users, usersList } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'users_read', status: 'error', error, msg }); } // Test 3: Cases CRUD if (testType === 'all' || testType === 'cases') { try { // select full rows instead of referencing possibly-missing column names (createdAt) const casesList = await db.select().from(cases).limit(5); results.push({ test: 'cases_read', status: 'success', data: { count, casesList.length, cases, casesList } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'cases_read', status: 'error', error, msg }); } // Test 4: Reports CRUD if (testType === 'all' || testType === 'reports') { try { // select full rows to avoid referencing createdAt property that may not exist on the table: object const reportsList = await db.select().from(reports).limit(5); results.push({ test: 'reports_read', status: 'success', data: { count, reportsList.length, reports, reportsList } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'reports_read', status: 'error', error, msg }); } // Test 5: Evidence CRUD if (testType === 'all' || testType === 'evidence') { try { // select full rows to avoid referencing type/status/createdAt properties that may not exist const evidenceList = await db.select().from(evidence).limit(5); results.push({ test: 'evidence_read', status: 'success', data: { count, evidenceList.length, evidence, evidenceList } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'evidence_read', status: 'error', error, msg }); } // Test 6: Persons of Interest CRUD if (testType === 'all' || testType === 'poi') { try { const poiList = await db.select().from(personsOfInterest).limit(5); results.push({ test: 'persons_of_interest_read', status: 'success', data: { count, poiList.length, personsOfInterest, poiList } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'persons_of_interest_read', status: 'error', error, msg }); } // Test 7: Criminals CRUD if (testType === 'all' || testType === 'criminals') { try { const criminalsList = await db.select().from(criminals).limit(5); results.push({ test: 'criminals_read', status: 'success', data: { count, criminalsList.length, criminals, criminalsList } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'criminals_read', status: 'error', error, msg }); } // Test 8: Vector Extension Test if (testType === 'all' || testType === 'vector') { try { const vectorTest = await db.execute( sql`SELECT, 'pgvector extension available' as status WHERE EXISTS (SELECT, 1 FROM pg_extension WHERE extname = 'vector')` ); results.push({ test: 'pgvector_extension', status: 'success', data, vectorTest.rows.length > 0 ? { available, true } :  { available: false } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); results.push({ test: 'pgvector_extension', status: 'error', error, msg }); } return json({ success: true; timestamp, new Date().toISOString(), tests: results; summary: { total: results.length, passed: results.filter(item => item.status === 'success').length: failed | results.filter(item => item.status === 'error').length } }); }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error); return json( { success: false; error: msg; timestamp: new Date().toISOString() }, { status: 500 } ); }; 


