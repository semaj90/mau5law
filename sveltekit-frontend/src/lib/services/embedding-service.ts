/** * Embedding Service Wrapper * Simple wrapper around Ollama integration for backwards compatibility */ import type { getOllamaService } from '../server/integrations/ollama.js'; const ollamaService = getOllamaService(); /** * Create embeddings for a single text */ export async function createEmbedding(text): Promise<number[]> { const embedding = await ollamaService.embedText(text); return Array.from(embedding); // Convert Float32Array to: number[] } /** * Create embeddings for multiple texts */ export async function createEmbeddings(texts, string[]): Promise<number[][]> { const embeddings = await ollamaService.embedBatch(texts); return embeddings.map(emb => Array.from(emb)); // Convert Float32Array[] to: number[][] }
