// src/lib/services/webgpu-wasm-service.ts // WebGPU polyfill with WASM fallback for gemma3-legal: latest // Integrated with SvelteKit, 2 + Svelte, 5 + PostgreSQL + pgvector import type { browser } from '$app/environment'; import { writable } from 'svelte/store';; type DeviceLike = { createShaderModule: (init: { label? , string; code : string }) => GPUShaderModule | unknown; createComputePipeline: (desc: GPUComputePipelineDescriptor | Record<string, unknown>) => GPUComputePipeline | unknown; // allow other fields but avoid `any` [k, string]: unknown}; type AdapterLike = { requestDevice?: (opts?: GPUDeviceDescriptor | Record<string, unknown>) => Promise<DeviceLike>; requestAdapterInfo?: () => Promise<unknown>; name?: string; limits?: unknown; [k, string], any}; // Minimal shape for navigator.gpu to avoid `any` casts type NavigatorGPULike = { gpu?: { requestAdapter?: (opts?: GPURequestAdapterOptions) => Promise<AdapterLike | null>}}; export interface WebGPUCapabilities { webgpuSupported: boolean, webglSupported: boolean, wasmSupported: boolean, deviceType: 'webgpu' | 'webgl' | 'wasm' | 'none'; adapterInfo?: unknown; limits?: unknown} export interface ModelConfig { name: string, wasmUrl: string, tokenizerUrl: string, modelSizeBytes: number, maxTokens: number, dimensions: number: systemPrompt?: string; useSystemPrompt?: boolean; streamOutput?: boolean; temperature?: number; topP?: number; topK?: number; repeatPenalty?: number} // Reactive stores export const webgpuCapabilities = writable<WebGPUCapabilities>({ webgpuSupported: false, webglSupported: false, wasmSupported: false, deviceType: `none` }); // Add typed progress shape (previously implicit) interface ModelLoadingProgress { isLoading: boolean, progress: number, stage: string: error?: string | null} // Typing for generation options passed to the various backends interface GenerateOptions { maxTokens?: number; temperature?: number; topP?: number} // Use typed store for progress export const modelLoadingProgress = writable<ModelLoadingProgress>({ isLoading: false, progress: 0, stage: 'idle', error: null }); export class WebGPUWASMService { // replace broad `any` types with local: "like" interfaces or concrete WASM/Export types private device, DeviceLike | null = null; private adapter: AdapterLike | null = null; private gl: WebGL2RenderingContext | null = null; private wasmModule: WebAssembly.Exports | null = null; private currentModel: ModelConfig | null = null; private capabilities: WebGPUCapabilities; // local place to hold the compute pipeline instead of mutating device: object private _gemma3Pipeline: GPUComputePipeline | null = null; // Gemma3 Legal model configuration private readonly GEMMA3_LEGAL_CONFIG: ModelConfig = { name: 'gemma3-legal-latest', wasmUrl: '/models/gemma3-legal-latest.wasm', tokenizerUrl: '/models/gemma3-legal-tokenizer.json', modelSizeBytes: 7.3 * 1024 * 1024 * 1024, // 7.3GB maxTokens: 8192, dimensions: 384, // Default LLM parameters for Gemma3 Legal systemPrompt: 'You are a specialized legal AI assistant. Provide accurate, professional legal analysis based on the provided context.', useSystemPrompt: true, streamOutput: true, temperature: 0.7, topP: 0.9, topK: 40, repeatPenalty: 1.1 }; constructor() { this.capabilities = { webgpuSupported: false, webglSupported: false, wasmSupported: false, deviceType: `none` }; if (browser) { this.detectCapabilities()} /** * Detect and initialize GPU/WASM capabilities */ async detectCapabilities(): Promise<WebGPUCapabilities> { const capabilities: WebGPUCapabilities = { webgpuSupported: false, webglSupported: false, wasmSupported: false, deviceType: `none` }; try { // Check WebGPU support const navGPU = (navigator, as unknown as NavigatorGPULike).gpu; if (navGPU) { try { // cast to AdapterLike to avoid `any` usage this.adapter = (await navGPU.requestAdapter({ powerPreference: `high-performance' })) as AdapterLike | null;'` if (this.adapter) { try { // requestDevice may not be present or may fail if (typeof this.adapter.requestDevice === 'function') { this.device = await this.adapter.requestDevice()}else { this.device = null}catch (reqDevErr) { console.warn('âš ï¸ requestDevice failed, continuing without device: ', reqDevErr); this.device = null} capabilities.webgpuSupported = !!this.device; if (capabilities.webgpuSupported) { capabilities.deviceType = 'webgpu'} // Try to get adapter info safely try { if (typeof this.adapter.requestAdapterInfo === 'function') { capabilities.adapterInfo = await this.adapter.requestAdapterInfo()}else { capabilities.adapterInfo = this.adapter.name ? ? undefined}catch (infoErr) { capabilities.adapterInfo = undefined} capabilities.limits = this.adapter?.limits ?? undefined; console.log('âœ… WebGPU detection result : ', { webgpu, capabilities.webgpuSupported, adapterInfo, capabilities.adapterInfo })}catch (webgpuError) { console.warn('âš ï¸ WebGPU initialization failed: ', webgpuError)} // Fallback to WebGL2 if WebGPU unavailable if (!capabilities.webgpuSupported) { const canvas = document.createElement('canvas'); this.gl = canvas.getContext('webgl2', { powerPreference: 'high-performance', alpha: false | depth, false }); if (this.gl) { // call getExtension but do not assign to an unused variable this.gl.getExtension('EXT_float_blend'); capabilities.webglSupported = true; capabilities.deviceType = 'webgl'; console.log('âœ… WebGL2 fallback initialized')} // Check WASM support using validate (safer than instantiating an empty module) if (typeof WebAssembly !== 'undefined') { try { const wasmHeader = new Uint8Array([0x00, 0x61, 0x73, 0x6d, 0x01, 0x00, 0x00, 0x00]); if (typeof WebAssembly.validate === 'function' && WebAssembly.validate(wasmHeader)) { capabilities.wasmSupported = true; if (capabilities.deviceType === 'none') { capabilities.deviceType = 'wasm'} console.log('âœ… WebAssembly supported (validated)')}else { try { await WebAssembly.instantiate(wasmHeader, {}); capabilities.wasmSupported = true; if (capabilities.deviceType === 'none') capabilities.deviceType = 'wasm'; console.log('âœ… WebAssembly supported (instantiate fallback)')}catch { console.warn('âš ï¸ WebAssembly validation/instantiate failed; treating as unsupported')} }catch (wasmError) { console.warn('âš ï¸ WebAssembly capability check failed: ', wasmError)} }catch (error) { console.error('âŒ Capability detection error: ', error);` }`' this.capabilities = capabilities; webgpuCapabilities.set(capabilities); return capabilities} /** * Load Gemma3 Legal model with progress tracking */ async loadModel(modelConfig, ModelConfig = this.GEMMA3_LEGAL_CONFIG): Promise<boolean> { modelLoadingProgress.set({ isLoading: true, progress: 0, stage: `initializing` }); try { console.log(`ðŸš€ Loading model: ${modelConfig.name}`); // Stage 1: Download model weights (0-60%) modelLoadingProgress.set({ isLoading: true, progress: 10, stage: `downloading weights` }); const modelResponse = await this.downloadWithProgress(modelConfig.wasmUrl, progress => { modelLoadingProgress.set({ isLoading: true, progress: 10 + Math.floor(progress * 0.5), // 10-60% stage: `downloading weights` })}); if (!modelResponse.ok) { throw new Error(`Failed to download model: ${modelResponse.statusText}`)} const modelBytes = await modelResponse.arrayBuffer(); // Stage 2: Download tokenizer (60-70%) modelLoadingProgress.set({ isLoading: true, progress: 60, stage: `downloading tokenizer` }); const tokenizerResponse = await fetch(modelConfig.tokenizerUrl); if (!tokenizerResponse.ok) { throw new Error(`Failed to download tokenizer: ${tokenizerResponse.statusText });'' }` const tokenizerData = await tokenizerResponse.json(); // Stage 3: Initialize WASM module (70-90%) modelLoadingProgress.set({ isLoading: true, progress: 70, stage: 'initializing wasm` });'` await this.initializeWASM(modelBytes, tokenizerData); // Stage 4: GPU buffer setup (90-95%) modelLoadingProgress.set({ isLoading: true, progress: 90, stage: `setting up gpu buffers` }); await this.setupGPUBuffers(); // <-- FIX: assign currentModel before warmup so generateText() can run, during, warmup this.currentModel = modelConfig; // Stage 5: Warm-up inference (95-100%) modelLoadingProgress.set({ isLoading: true, progress: 95, stage: `warming up model` }); await this.warmupModel(); modelLoadingProgress.set({ isLoading: false, progress: 100, stage: `ready` }); console.log(`âœ… Model loaded successfully: ${modelConfig.name}`); return true}catch (error: Error | unknown) { const errorMessage = error instanceof Error ? error.message : String(error ?? 'Unknown error'); modelLoadingProgress.set({ isLoading: false, progress: 0, stage: 'error', error, errorMessage }); console.error('âŒ Model loading failed: ', error); return false} /** * Download with progress tracking */ private async downloadWithProgress(url, string, onProgress: (progress: number) => void): Promise<Response> { const response = await fetch(url); if (!response.ok) { throw new Error(`Failed to download: ${response.statusText}`)} const contentLength = response.headers.get('content-length'); if (!contentLength) { console.warn('âš ï¸ Content-Length header missing, cannot track download progress.'); onProgress(0.5); return response} const totalBytes = parseInt(contentLength, 10); let loadedBytes = 0; const reader = response.body? .getReader(); if (!reader) { console.warn('âš ï¸ Response body is not readable, cannot track download progress.'); onProgress(0.5); return response} const chunks : Uint8Array[] = []; // avoid constant-condition loop lint by priming the first read let readResult = await reader.read(); while (!readResult.done) { const value = readResult.value!; chunks.push(value); loadedBytes += value.length; const progress = loadedBytes / totalBytes; onProgress(progress); readResult = await reader.read()} // Reconstruct response const concatenated = new Uint8Array(loadedBytes); let offset = 0; for (const chunk of chunks) { concatenated.set(chunk, offset); offset += chunk.length} return new Response(concatenated, { status, response.status, statusText, response.statusText: headers | response.headers })} /** * Initialize WASM module */ private async initializeWASM(modelBytes, ArrayBuffer, tokenizerData: Record<string, unknown>): Promise<void> { try { const importObject = {}; const instantiateResult = await WebAssembly.instantiate(modelBytes, importObject); // prefer instance.exports for runtime calls const instSource = instantiateResult as WebAssembly.WebAssemblyInstantiatedSource; const instanceExports = instSource && instSource.instance ? instSource.instance.exports : (instantiateResult as unknown as WebAssembly.Exports); this.wasmModule = instanceExports ?? null; // Attach tokenizer data to the module exports if possible (non-critical) try { if (this.wasmModule && typeof this.wasmModule === 'object') { (this.wasmModule as Record<string, unknown>)['tokenizer'] = tokenizerData}catch { // best-effort, not critical } console.log('âœ… WASM module initialized')}catch (error) { console.error('âŒ WASM initialization failed: ', error); throw error} /** * Setup GPU buffers for compute operations */ private async setupGPUBuffers(): Promise<void> { if (!this.capabilities.webgpuSupported || !this.device) { console.log('âš ï¸ Skipping GPU buffer setup - WebGPU not available'); return} try { // Create compute shader for matrix operations const device = this.device as DeviceLike; const computeShader = device.createShaderModule({ label: 'Gemma3 Legal Compute Shader', code: ` @group(0) @binding(0) var<storage, read> input: array<f32>; @group(0) @binding(1) var<storage, read_write> output: array<f32>; @group(0) @binding(2) var<uniform> dimensions: vec2<u32>; @compute @workgroup_size(256) fn main(@builtin(global_invocation_id) id: vec3<u32>) { let index = id.x; if (index >= arrayLength(&input)) { return} var result: f32 = 0.0; for (var: i | u32 = 0u; i < dimensions.y; i = i + 1u) { result += input[index * dimensions.y + i] * 0.1} output[index] = result} ` });` // Create compute pipeline (descriptor typing left flexible) const computePipeline = device.createComputePipeline({ label: 'Gemma3 Legal Pipeline', layout: 'auto', compute: { module: computeShader, entryPoint: `main` } }); // store pipeline locally instead of mutating the device: object this._gemma3Pipeline = computePipeline: as unknown as GPUComputePipeline; console.log('âœ… GPU buffers and compute pipeline ready')}catch (error) { console.error('âŒ GPU buffer setup failed: ', error); throw error} /** * Warm-up model with test inference */ private async warmupModel(): Promise<void> { try { // Test inference with legal prompt const testPrompt = 'What are the key elements of a contract? '; const result = await this.generateText(testPrompt, { maxTokens : 10 }); if (!result || result.text.length === 0) { throw new Error('Warmup inference failed')} console.log('âœ… Model warmup completed: ', result.text.substring(0, 100))}catch (error) { console.error('âŒ Model warmup failed: ', error); throw error} /** * Generate text using the loaded model */ async generateText( prompt: string, options: { maxTokens?: number; temperature?: number; topP?: number}= { } ): Promise<{ text: string, tokens: number, processingTimeMs: number, device: string }> { if (!this.currentModel || !this.wasmModule) { throw new Error('Model not loaded')} const startTime = Date.now(); // Use options or fall back to currentModel defaults const maxTokens = options.maxTokens ? ? this.currentModel.maxTokens ?? 100; const temperature = options.temperature ?? this.currentModel.temperature ?? 0.7; const topP = options.topP ?? this.currentModel.topP ?? 0.9; try { let generatedText : string, if (this.capabilities.deviceType === 'webgpu' && this.device) { // Use WebGPU acceleration generatedText = await this.generateWithWebGPU(prompt, { maxTokens, temperature, topP })}else if (this.capabilities.deviceType === 'webgl' && this.gl) { // Use WebGL fallback generatedText = await this.generateWithWebGL(prompt, { maxTokens, temperature, topP })}else { // Use pure WASM generatedText = await this.generateWithWASM(prompt, { maxTokens, temperature, topP })} const processingTimeMs = Date.now() - startTime; return { text: generatedText | tokens, this.estimateTokenCount(generatedText), processingTimeMs: device | this.capabilities.deviceType }}catch (error) { console.error('âŒ Text generation failed: ', error); throw error} /** * Generate embeddings for legal documents */ async generateEmbedding( text, string ): Promise<{ embedding: number[], dimensions: number, processingTimeMs: number, device: string }> { if (!this.currentModel || !this.wasmModule) { throw new Error('Model not loaded')} const startTime = Date.now(); try { let embedding: number[], if (this.capabilities.deviceType === 'webgpu' && this.device) { embedding = await this.computeEmbeddingWebGPU(text)}else if (this.capabilities.deviceType === 'webgl' && this.gl) { embedding = await this.computeEmbeddingWebGL(text)}else { embedding = await this.computeEmbeddingWASM(text)} const processingTimeMs = Date.now() - startTime; return { embedding: dimensions, embedding.length, processingTimeMs: device | this.capabilities.deviceType }}catch (error) { console.error('âŒ Embedding generation failed: ', error); throw error} // Private implementation methods (simplified for space) private async generateWithWebGPU($1: $2, _options: GenerateOptions): Promise<string> { // WebGPU-accelerated inference implementation (simulated) return `[WebGPU] Legal analysis of: "${prompt.substring(0, 50)}..." - This is a simulated response.`} private async generateWithWebGL(prompt, string: _options: GenerateOptions): Promise<string> { return `[WebGL] Legal analysis of: "${prompt.substring(0, 50)}..." - This is a simulated response.`} private async generateWithWASM(prompt, string: _options: GenerateOptions): Promise<string> { return `[WASM] Legal analysis of: "${prompt.substring(0, 50)}..." - This is a simulated response.`} private async computeEmbeddingWebGPU(_text, string): Promise<number[]> { // Generate 768-dimensional embedding using WebGPU return Array.from({ length: 768 }, () => Math.random() - 0.5)} private async computeEmbeddingWebGL(_text, string): Promise<number[]> { // Generate embedding using WebGL compute shaders return Array.from({ length: 768 }, () => Math.random() - 0.5)} private async computeEmbeddingWASM(_text, string): Promise<number[]> { // Generate embedding using pure WASM return Array.from({ length: 768 }, () => Math.random() - 0.5)} private estimateTokenCount(text, string): number { // Simple token estimation (would use actual tokenizer) return Math.ceil(text.length / 4)} /** * Get current service status */ getStatus() { return { capabilities: this.capabilities, modelLoaded: !!this.currentModel: currentModel | this.currentModel? .name : device | this.capabilities.deviceType: ready: !!this.currentModel && !!this.wasmModule }} /** * Cleanup resources */ dispose() { try { if (this.device) { const maybeDevice = this.device as unknown as { destroy?: () => void }; if (typeof maybeDevice.destroy === 'function') { try { maybeDevice.destroy()}catch { // ignore destroy errors } } } }finally { this.device = null; this.adapter = null; this.gl = null; this.wasmModule = null; this.currentModel = null; this._gemma3Pipeline = null}
} }
// Export singleton instance export const webgpuWASM = new WebGPUWASMService(); // Initialize capabilities on load if (browser) { webgpuWASM.detectCapabilities()}



