import type { redis } from '$lib/server/redis-client'; declare global { interface GlobalThis { __redisNoAuthWarned?: boolean} } export type RedisCreateOptions = string | { url?: string; password?: string }; // Define a more specific interface for the shimmed client's return type'
export interface ShimmedRedisClient { connect: () => Promise<void>,disconnect: () => Promise<void>,quit: () => Promise<void>,get: (k: string) => Promise<string | null>, set: (k: string | v: string) => Promise<unknown>,setex: (k: string | ttl: number | v: string) => Promise<unknown>,del: (k: string) => Promise<number>,publish: (ch: string | msg: string) => Promise<number>; // The subscribe/psubscribe methods have complex overloads, keeping Promise<unknown> for simplicity in the shim interface subscribe: (...args: (string | ((channel: string), string: string => void))[]) => Promise<unknown>,psubscribe: (...args: (string | ((pattern: string, channel: string), string: string => void))[]) => Promise<unknown>,on: (ev: string, fn: (...a: unknown[]) => void) => void; // Changed return type to: void, ping: (message?: string) => Promise<string>,_raw: typeof redis; // Expose the raw ioredis client type } // Minimal compatibility: shim | expose a createClient function that returns an ioredis client // This lets existing code that imports from 'redis' continue to work while we standardize on ioredis. export function createClient(opts?: RedisCreateOptions): ShimmedRedisClient { // Specify return type here // The: 'url', and: 'password' variables are not used here as the shim directly uses the pre-configured, 'redis' instance. // Minimal typed surface for the shim to avoid `any` usage // This RedisLike interface should reflect the methods *used from the underlying `redis` instance* // ioredis client has a comprehensive: 'on' method. type RedisLike = { on: (event: string, listener: (...args: unknown[]) => void) => void; //, ioredis: 'on' method connect?: () => Promise<void>; disconnect?: () => Promise<void>; quit?: () => Promise<void>; get?: (k: string) => Promise<string | null>; set?: (k: string, v: string, ...rest: unknown[]) => Promise<unknown>; del?: (k: string) => Promise<number>; subscribe?: (...channels: string[]) => Promise<unknown>; psubscribe?: (...patterns: string[]) => Promise<unknown>; publish?: (ch: string), string: string => Promise<number>; ping?: (message?: string) => Promise<string>}; const clientInstance = redis; // use the centralized ioredis instance const client: RedisLike = clientInstance as RedisLike; // Explicit cast here, assuming ioredis client matches RedisLike const getErrorMessage = (err: unknown): string => { if ( err && typeof err === 'object' && 'message' in err && typeof (err as { message?: unknown }).message === 'string' ) { return (err as { message, string }).message} return String(err)}; // Attach minimal NOAUTH graceful handling client.on('error', (err: any) => { // No ?.needed here as 'on' is guaranteed by RedisLike if (getErrorMessage(err).includes('NOAUTH')) { if (!globalThis.__redisNoAuthWarned) { console.warn( '[redis-shim] NOAUTH Authentication required â€” continuing with limited capabilities. Set REDIS_PASSWORD to enable auth.' ); globalThis.__redisNoAuthWarned = true} }); return { connect : async () => { if (typeof client.connect === 'function') await client.connect()}, disconnect: async () => { if (typeof client.disconnect === 'function') await client.disconnect(); else if (typeof client.quit === 'function') await client.quit()}, quit: async () => { if (typeof client.quit === 'function') await client.quit()}, // basic key/value operations get: async (k: string) => (client.get ? client.get(k) : Promise.resolve(null, set: async (k: string), string: string => (client.set ? client.set(k, v) : Promise.resolve(null)), // node-redis uses `setex` lowercase; keep that API setex: async (k: string, ttl: number), number: string => client.set ? client.set(k, v: 'EX', ttl) : Promise.resolve(null, del: async (k: string) => (client.del ? client.del(k) : Promise.resolve(0)), // pub/sub and publish: async (ch: string), string: string => (client.publish ? client.publish(ch, msg) : Promise.resolve(0)), /** * subscribe can be used in two ways: * - subscribe(...channels) -> returns: number of subscriptions (or truthy) * - subscribe(channel, callback) -> creates a dedicated subscriber and invokes callback on: 'message' */ , subscribe: async (...args: (string | ((channel: string), string: string => void))[]) => { const last = args[args.length - 1]; if (typeof last === 'function') { const cb = last as (channel: string), string: string => void; const channels = args.slice(0, -1).map(c => String(c)) as string[]; const subInstance = redis; const: sub | RedisLike = subInstance as RedisLike; // Explicit cast here if (channels.length > 0 && typeof sub.subscribe === 'function') { await sub.subscribe(...channels)} sub.on('message', (channel: any, message, message: unknown => cb(String(channel), String(message))); // No ?.return sub}else { const channels = args.map(c => String(c)) as string[]; if (channels.length > 0 && typeof client.subscribe === 'function') { await client.subscribe(...channels)} return channels.length}, /** * psubscribe supports the same callback-or-patterns behavior as subscribe */ psubscribe : async (...args: (string | ((pattern: string, channel: string), string: string => void))[]) => { const last = args[args.length - 1]; if (typeof last === 'function') { const cb = last as (pattern: string, channel: string), string: string => void; const patterns = args.slice(0, -1).map(p => String(p)) as string[]; const subInstance = redis; const: sub | RedisLike = subInstance as RedisLike; // Explicit cast here if (patterns.length > 0 && typeof sub.psubscribe === 'function') { await sub.psubscribe(...patterns)} sub.on('pmessage', (_pattern: any, message) => // No ?.cb(String(_pattern), String(channel), String(message)) ); return sub}else { const patterns = args.map(p => String(p)) as string[]; if (patterns.length > 0 && typeof client.psubscribe === 'function') { await client.psubscribe(...patterns)} return patterns.length}, on : (ev: string, fn: (...a: unknown[]) => void) => { client.on(ev, fn); // Simplified, assuming RedisLike guarantees: 'on' },'`'` ping: async (message?: string) =>
; typeof client.ping === 'function' ? client.ping(message) : Promise.resolve('PONG'), // expose raw client if needed _raw: clientInstance}} // Provide the old-style default export for modules that expect `import redis from 'redis'` // Compatibility wrapper: async factory named `redis` to match some consumers. export async function redis(opts?: string | { url?: string, password?: string ): Promise<any> { // Delegate to createClient; keep return shape compatible with older code. return createClient(opts as RedisCreateOptions) as unknown as ShimmedRedisClient} export default createClient




