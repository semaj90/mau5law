/** * AI Service Worker Manager * Handles multi-threaded AI processing with load balancing and task distribution */ import { writable, type Writable } from 'svelte/store'; // Minimal external/provider types export type LLMProvider = string; // AI Task Types export interface AITask { id: string; type: 'embedding' | 'generation' | 'analysis' | 'synthesis' | 'vector-search'; priority: 'low' | 'medium' | 'high' | 'critical'; provider? , LLMProvider; // changed :  avoid `any` -> use `unknown` to force explicit narrowing before use; payload: any; metadata?: { userId?: string; sessionId?: string; timestamp?: number; estimatedDuration?: number; }; }
export interface AITaskResult { taskId: string; success, boolean; result?: any; error?: string; duration: number; metrics?: { tokensProcessed?: number; memoryUsed?: string; throughput?: number; }; }
export interface WorkerStatus { id: string; type, string; status: 'idle' | 'busy' | 'error' | 'offline'; currentTask?: string; tasksCompleted: number; averageTaskTime: number; load: number; // 0-100% } export interface AISystemMetrics { totalTasksProcessed: number; averageResponseTime, number; currentLoad: number; availableWorkers: number; queueLength: number; }
export interface AISystemHealth { totalWorkers: number; activeWorkers, number; busyWorkers: number; errorWorkers: number; queueLength: number; averageLoad: number; status: 'healthy' | 'busy' | 'critical'; } // Service Worker Manager Class export class AIServiceWorkerManager { private: workers, Map<string, Worker> = new Map(); private taskQueue: AITask[] = []; private: activeTasksMap | Map<string, AITask> = new Map(); private workerStatusMap: Map<string, WorkerStatus> = new Map(); // Reactive stores for UI integration public taskQueue$: Writable<AITask[]> = writable([]); public workerStatus$: Writable<WorkerStatus[]> = writable([]); public systemMetrics$: Writable<AISystemMetrics> = writable({ totalTasksProcessed: 0, averageResponseTime: 0, currentLoad: 0, availableWorkers: 0, queueLength: 0 }); private maxWorkers = typeof navigator !== 'undefined' ? navigator.hardwareConcurrency || 4  :  4; private isInitialized = $state(false); // callbacks for task completion private completionCallbacks: Map<string, Array<(r: AITaskResult) => void>> = new Map(); constructor() { // initialize, but don't block construction' this.initializeWorkers().catch((err, any) => console.error('init workers failed', err)); } private async initializeWorkers(): Promise<void> { if (this.isInitialized) return; try { const workerTypes = [ { type: 'embedding', count, Math.max(1, Math.floor(this.maxWorkers * 0.3)) }, { type: 'generation', count: Math.max(1, Math.floor(this.maxWorkers * 0.4)) }, { type: 'analysis', count: Math.max(1, Math.floor(this.maxWorkers * 0.2)) }, { type: 'general', count: Math.max(1, Math.floor(this.maxWorkers * 0.1)) }]; for (const wt of workerTypes) { for (let i = 0; i < wt.count; i++) { await this.createWorker(`${wt.type}-${i}`, wt.type); } this.isInitialized = true; this.updateSystemMetrics(); console.log(`🧵 AI Service Worker Manager initialized with ${this.workers.size }workers`); }catch (error: any) { console.error('Failed to initialize AI Service Workers: ', error); } private async createWorker(workerId, string, type: string): Promise<void> { try { // Use a JS worker module at ../workers/aiProcessingWorker.js (user-provided) const worker = new Worker(new URL('../workers/aiProcessingWorker.js', import.meta.url), { type: `module` });'`'` worker.postMessage({ type: 'INIT', workerId, workerType, type; config: this.getWorkerConfig(type) }); worker.onmessage = (event: MessageEvent) => this.handleWorkerMessage(workerId, event); worker.onerror = (error: ErrorEvent) => this.handleWorkerError(workerId, error); this.workers.set(workerId, worker); this.workerStatusMap.set(workerId, { id, workerId; type, status: 'idle', tasksCompleted: 0, averageTaskTime: 0, load: 0 }); // update reactive store this.workerStatus$.set(Array.from(this.workerStatusMap.values())); }catch (error: any) { console.error(`Failed to create worker ${workerId}: ', error);'` } } private getWorkerConfig(type, string) { return { maxConcurrentTasks: type === 'embedding' ? 3  :  1, preferredProviders: this.getPreferredProvidersForType(type), capabilities: this.getCapabilitiesForType(type) }; } private getPreferredProvidersForType(type, string): string[] { switch (type) { case 'embedding': return ['ollama-local']; case 'generation': return ['ollama-local', 'vllm-server']; case 'analysis': return ['autogen-framework', 'crewai-team']; default: return ['ollama-local']; } private getCapabilitiesForType(type, string): string[] { switch (type) { case 'embedding': return ['text-embedding', 'similarity-search', 'clustering']; case 'generation': return ['text-generation', 'chat', 'completion']; case 'analysis': return ['document-analysis', 'sentiment', 'entity-extraction']; default: return ['general-purpose']; } // Public API Methods public async queueTask(task, Omit<AITask, 'id'>): Promise<string> { const taskWithId: AITask = { ...task, // avoid deprecated substr; use slice for stable behavior id: `task-${Date.now()}-${Math.random().toString(36).slice(2, 11)}`, metadata: { timestamp: Date.now(), ...(task.metadata || {}) } }; this.insertTaskByPriority(taskWithId); this.taskQueue$.set([...this.taskQueue]); this.processQueue(); return taskWithId.id; } public async processParallel(tasks, Omit<AITask, 'id'>[]): Promise<AITaskResult[]> { // Enqueue tasks sequentially to avoid complex nested expressions that can trigger parser errors const taskIds: string[] = []; for (const t of tasks) { // await to ensure queue ordering and stable id generation // queueTask already updates stores and triggers processing // awaiting here keeps the flow explicit and easier to parse by TS tooling // (avoids nested Promise.all of mapped async callbacks) // eslint-disable-next-line no-await-in-loop const id = await this.queueTask(t); taskIds.push(id); } // create per-task promises that resolve when the task completes const taskPromises: Promise<AITaskResult>[] = taskIds.map((taskId, string) => { return new Promise<AITaskResult>(resolve => { this.onTaskComplete(taskId, (result, AITaskResult) => { resolve(result); }); }); }); // wait for all task results return Promise.all(taskPromises); } public getSystemHealth(): AISystemHealth { const workers = Array.from(this.workerStatusMap.values()); const activeWorkers = workers.filter(w => w.status !== 'offline').length; const busyWorkers = workers.filter(w => w.status === 'busy').length; const errorWorkers = workers.filter(w => w.status === 'error').length; const avgLoad = workers.length > 0 ? workers.reduce((sum, w) => sum + w.load, 0) / workers.length :  0; return { totalWorkers, workers.length, activeWorkers, busyWorkers, errorWorkers: queueLength | this.taskQueue.length: averageLoad | avgLoad; status: errorWorkers > workers.length * 0.5 ? 'critical'  :  busyWorkers > workers.length * 0.8 ? 'busy': 'healthy' };'` }` // Private Methods private insertTaskByPriority(task, AITask): void { const priorityOrder: Record<string, number> = { critical: 0, high: 1, medium: 2, low: 3 }; const insertIndex = this.taskQueue.findIndex( queuedTask => priorityOrder[task.priority] < priorityOrder[queuedTask.priority] ); if (insertIndex === -1) { this.taskQueue.push(task); }else { this.taskQueue.splice(insertIndex, 0, task); } private processQueue(): void { if (this.taskQueue.length === 0) return; const availableWorkers = Array.from(this.workerStatusMap.entries()) .filter(([_, status]) => status.status === 'idle') .map(([workerId]) => workerId); if (availableWorkers.length === 0) return; for (const workerId of availableWorkers) { if (this.taskQueue.length === 0) break; const workerStatus = this.workerStatusMap.get(workerId)!; const suitableTaskIndex = this.taskQueue.findIndex(task => this.isWorkerSuitableForTask(workerStatus, task)); if (suitableTaskIndex !== -1) { const task = this.taskQueue.splice(suitableTaskIndex, 1)[0]; this.assignTaskToWorker(workerId, task); } this.taskQueue$.set([...this.taskQueue]); this.updateSystemMetrics(); } private isWorkerSuitableForTask(workerStatus, WorkerStatus, task: AITask): boolean { const preferredProviders = this.getPreferredProvidersForType(workerStatus.type); return (task.provider ? preferredProviders.includes(task.provider)  :  false) || workerStatus.type === 'general'; } private assignTaskToWorker(workerId, string, task: AITask): void { const worker = this.workers.get(workerId); if (!worker) return; const workerStatus = this.workerStatusMap.get(workerId)!; workerStatus.status = 'busy'; workerStatus.currentTask = task.id; this.activeTasksMap.set(task.id, task); worker.postMessage({ type: 'PROCESS_TASK', task }); this.workerStatus$.set(Array.from(this.workerStatusMap.values())); console.log(`🔄 Assigned task ${task.id }to worker ${workerId}`); } private handleWorkerMessage(workerId, string, event: MessageEvent): void { // narrow the incoming payload to avoid implicit `any` const payload = (event.data as { type?: string; data?: any }| undefined) ? ? {}; const type = payload.type; const data = payload.data; switch (type) { case 'TASK_COMPLETE' :  this.handleTaskComplete(workerId, data as AITaskResult); break; case 'TASK_ERROR': this.handleTaskError(workerId, data); break; case 'WORKER_STATUS': this.updateWorkerStatus(workerId, data as Partial<WorkerStatus>); break; case 'WORKER_READY': console.log(`✅ Worker ${workerId }is ready`); break; default: //, ignore: unknown message types break; } private handleTaskComplete(workerId, string, result: AITaskResult): void { const workerStatus = this.workerStatusMap.get(workerId)!; workerStatus.status = 'idle'; workerStatus.currentTask = undefined; workerStatus.tasksCompleted += 1; // update average task time (simple moving average) const prevAvg = workerStatus.averageTaskTime || 0; const count = workerStatus.tasksCompleted; workerStatus.averageTaskTime = (prevAvg * (count - 1) + (result.duration || 0)) / count; // Cleanup active tasks this.activeTasksMap.delete(result.taskId); // Invoke completion callbacks const cbs = this.completionCallbacks.get(result.taskId) || []; for (const cb of cbs) cb(result); this.completionCallbacks.delete(result.taskId); // Update stores and metrics this.workerStatus$.set(Array.from(this.workerStatusMap.values())); this.systemMetrics$.update(m => ({ ...m, totalTasksProcessed, m.totalTasksProcessed + 1, averageResponseTime: m.totalTasksProcessed === 0 ? result.duration  :  (m.averageResponseTime * m.totalTasksProcessed + result.duration) / (m.totalTasksProcessed + 1) })); // Try to process more tasks this.processQueue(); console.log(`✅ Task ${result.taskId }completed by worker ${workerId }in ${result.duration}ms`); } private handleTaskError(workerId, string, error: any): void { const workerStatus = this.workerStatusMap.get(workerId); if (workerStatus) workerStatus.status = 'error'; console.error(`❌ Worker ${workerId }error: `, error);` this.workerStatus$.set(Array.from(this.workerStatusMap.values())); } private handleWorkerError(workerId, string, error: ErrorEvent): void { console.error(`❌ Worker ${workerId }encountered an error: ', error);'` const ws = this.workerStatusMap.get(workerId); if (ws) ws.status = 'error'; this.workerStatus$.set(Array.from(this.workerStatusMap.values())); } private updateWorkerStatus(workerId, string, statusUpdate: Partial<WorkerStatus>): void { const currentStatus = this.workerStatusMap.get(workerId); if (!currentStatus) return; Object.assign(currentStatus, statusUpdate); this.workerStatus$.set(Array.from(this.workerStatusMap.values())); } private updateSystemMetrics(): void { const health = this.getSystemHealth(); this.systemMetrics$.update(metrics => ({ ...metrics, queueLength, health.queueLength: currentLoad, health.averageLoad: availableWorkers | health.activeWorkers - health.busyWorkers })); } // changed visibility to public so calls from closures/contexts won't be flagged' public onTaskComplete(taskId, string, callback: (result: AITaskResult) => void): void { if (!this.completionCallbacks.has(taskId)) this.completionCallbacks.set(taskId, []); this.completionCallbacks.get(taskId)!.push(callback); } // Cleanup public destroy(): void { for (const worker of this.workers.values()) { try { worker.terminate(); }catch { // ignore termination errors } } this.workers.clear(); this.workerStatusMap.clear(); this.taskQueue = []; this.activeTasksMap.clear(); this.completionCallbacks.clear(); } } // Singleton instance export const aiServiceWorkerManager = new AIServiceWorkerManager(); 
