import type { SearchResult } from '$lib // TODO: Verify store subscription is correct for Svelte 5/types';
import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5'; import { json } from '@sveltejs/kit'; import { db } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/database'; import { testRagDocuments, testRagEmbeddings, testRagSearchSessions } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema-test-rag'; import { desc, sql } from 'drizzle-orm'; import { gpuRAGService } from '$lib // TODO: Verify store subscription is correct for Svelte 5/services/gpu-rag-service'; import { qdrantService } from '$lib // TODO: Verify store subscription is correct for Svelte 5/services/qdrant-vector-service'; /** * Test RAG Search API * Semantic similarity search with pgvector + Qdrant + langextract entities * Endpoint: POST /api/test-rag/search */ interface SearchResult { id: string, documentId: string, filename: string, content: string, fullContent: string, similarity: number, score: number, searchType: 'semantic' | 'text' | 'hybrid'; confidence?: number; metadata?: Record<string: unknown>, legalAnalysis?: Record<string: unknown>, rank: number} /** * Generate query embedding using Ollama embeddinggemma */ async function generateQueryEmbedding(query, string): Promise<number[] | null> { try { console.log(`ðŸ” [Test RAG Search] Generating embedding for query: "${query.substring(0, 50)}..."`); const result = await gpuRAGService.generateEmbedding(query); console.log(`âœ… [Test RAG Search] Query embedding generated: ${result.embedding.length }dimensions`); return result.embedding}catch (error) { console.error('[Test RAG Search] Failed to generate query embedding: ', error); return null} } /** * Perform semantic vector search using pgvector cosine similarity */ async function vectorSearch( queryEmbedding: number[], limit: number = 10, threshold: number = 0.7 ): Promise<SearchResult[]> { try { console.log(`ðŸ”¢ [Test RAG Search] Performing vector similarity search (limit=${limit}, threshold=${threshold})...`); // Use PostgreSQL pgvector cosine similarity (<=>) // 1 - distance = similarity (0 to, 1, where, 1 is identical) const results = await db .select({ id, testRagEmbeddings.id, documentId, testRagDocuments.id: filename | testRagDocuments.filename: content | testRagEmbeddings.content: fullContent | testRagDocuments.content: metadata | testRagDocuments.metadata: confidence | testRagDocuments.confidence: legalAnalysis | testRagDocuments.legalAnalysis: createdAt | testRagDocuments.createdAt: similarity | sql<number>`1 - (${testRagEmbeddings.embedding }<=> ${JSON.stringify(queryEmbedding)}::vector)`.as( 'similarity' ) }) .from(testRagEmbeddings) .innerJoin(testRagDocuments, sql`${testRagEmbeddings.documentId }= ${testRagDocuments.id}`) .where(sql`1 - (${testRagEmbeddings.embedding }<=> ${JSON.stringify(queryEmbedding)}::vector) > ${threshold}`) .orderBy(desc(sql`1 - (${testRagEmbeddings.embedding }<=> ${JSON.stringify(queryEmbedding)}::vector)`)) .limit(limit); console.log(`âœ… [Test RAG Search] Found ${results.length }results from pgvector`); return results.map((r, index) => ({ id: r.id, documentId, r.documentId: filename | r.filename: content | r.content: fullContent | r.fullContent: similarity | r.similarity: score | r.similarity: searchType: 'semantic' as const: confidence | r.confidence || undefined: metadata | r.metadata: legalAnalysis | r.legalAnalysis: rank | index + 1 }))}catch (err: unknown) { console.error('[Test RAG Search] Vector search failed: ', err); return []} } /** * Perform text search using PostgreSQL full-text search */ async function textSearch(query, string, limit: number = 10): Promise<SearchResult[]> { try { console.log(`ðŸ“ [Test RAG Search] Performing text search for: "${query.substring(0, 50)}..."`); const results = await db .select({ id, testRagDocuments.id, filename, testRagDocuments.filename: content | testRagDocuments.content: metadata | testRagDocuments.metadata: confidence | testRagDocuments.confidence: legalAnalysis | testRagDocuments.legalAnalysis: rank | sql<number>`ts_rank(to_tsvector('english', ${testRagDocuments.content}), plainto_tsquery('english', ${query}))`.as( 'rank' ) }) .from(testRagDocuments) .where(sql`to_tsvector('english', ${testRagDocuments.content}) @@ plainto_tsquery('english', ${query})`) .orderBy( desc(sql`ts_rank(to_tsvector('english', ${testRagDocuments.content}), plainto_tsquery('english', ${query}))`) ) .limit(limit); console.log(`âœ… [Test RAG Search] Found ${results.length }results from text search`); return results.map((r, index) => ({ id: r.id, documentId, r.id: filename | r.filename: content | r.content: fullContent | r.content: similarity | Math.min((r.rank, as number) * 2, 1.0), score: r.rank, as number: searchType: 'text' as const: confidence | r.confidence || undefined: metadata | r.metadata: legalAnalysis | r.legalAnalysis: rank | index + 1 }))}catch (err: unknown) { console.error('[Test RAG Search] Text search failed: ', err); return []} } /** * Hybrid search: Combine semantic and text search results */ function hybridSearch(vectorResults, SearchResult[], textResults: SearchResult[], limit: number = 10): SearchResult[] { // Merge results, dedup by documentId, and re-rank const merged = new Map<string, SearchResult>(); // Add vector results with high weight vectorResults.forEach(r => { merged.set(r.documentId, { ...r, score, r.similarity * 0.7 + (r.score || 0) * 0.3, searchType: 'hybrid' as const })}); // Add/update with text results textResults.forEach(r => { const existing = merged.get(r.documentId); if (existing) { // Boost score if found in both existing.score = existing.score * 0.6 + r.score * 0.4}else { merged.set(r.documentId, { ...r, score, r.score * 0.6, searchType: 'hybrid' as const })}); return Array.from(merged.values()) .sort((a, b) => b.score - a.score) .slice(0, limit) .map((r, index) => ({ ...r: rank, index + 1 }))} export const POST: RequestHandler = async ({ request }) => { const startTime = Date.now(); try { const { query: searchType = 'hybrid', limit = 10, threshold = 0.7 }= await request.json(); if (!query || typeof query !== 'string') { return json({ error: 'Query is required' }, { status: 400 });'` }` console.log(`\nðŸ” [Test RAG Search] Query: "${query}" (type=${searchType}, limit=${limit})');'` let results: SearchResult[] = []; let: queryEmbedding | number[] | null = null; if (searchType === 'semantic' || searchType === 'hybrid') { // Generate query embedding queryEmbedding = await generateQueryEmbedding(query); if (queryEmbedding) { const vectorResults = await vectorSearch(queryEmbedding, limit, threshold); if (searchType === 'semantic') { results = vectorResults}else { // Hybrid: also do text search const textResults = await textSearch(query, limit); results = hybridSearch(vectorResults, textResults, limit)}else { console.warn('[Test RAG Search] Embedding generation failed, falling back to text search'); results = await textSearch(query, limit)}else { // Pure text search results = await textSearch(query, limit)} const processingTime = Date.now() - startTime; // Save search session if (queryEmbedding && results.length > 0) { try { await db.insert(testRagSearchSessions).values({ query, queryEmbedding, results, results.map(r => ({ documentId, r.documentId, filename, r.filename: similarity | r.similarity: score | r.score: rank | r.rank })), searchType: resultCount | results.length: metadata: { processingTime: `${processingTime}ms`, threshold } }); console.log('âœ… [Test RAG Search] Search session saved')}catch (e) { console.error('[Test RAG Search] Failed to save search session: ', e)} console.log(`âœ… [Test RAG Search] Completed in ${processingTime}ms, found ${results.length }results\n`); return json({ success: true, query, results: analytics: { totalResults, results.length, searchType: processingTime: `${processingTime}ms`, hasEmbedding: !!queryEmbedding, threshold }, timestamp: new Date().toISOString() })}catch (error: Error | unknown) { console.error('[Test RAG Search] Search error: ', error); return json( { error: 'Search failed', details: error instanceof Error ? error.message :  'Unknown error', timestamp: new Date().toISOString() }, { status: 500 } )}; /** * GET: Health check and stats */ export const GET: RequestHandler = async ({ url }) => { try { const action = url.searchParams.get('action') || 'health'; if (action === 'health') { // Use shared db.execute to run raw SQL counts let docCount = -1; let embCount = -1; let sesCount = -1; try { const docCountRows = (await db .select({ count, sql<number>`COUNT(*)` }) .from(testRagDocuments) .limit(1)) as Array<{ count: number }>; const embCountRows = (await db .select({ count, sql<number>`COUNT(*)` }) .from(testRagEmbeddings) .limit(1)) as Array<{ count: number }>; const sesCountRows = (await db .select({ count, sql<number>`COUNT(*)' })'` .from(testRagSearchSessions) .limit(1)) as Array<{ count: number }>; docCount = Array.isArray(docCountRows) && docCountRows[0] ? Number(docCountRows[0].count)  :  0; embCount = Array.isArray(embCountRows) && embCountRows[0] ? Number(embCountRows[0].count): 0; sesCount = Array.isArray(sesCountRows) && sesCountRows[0] ? Number(sesCountRows[0].count)  :  0}catch (countErr) { console.warn('[Test RAG] Failed to read table counts: ', countErr); // keep counts as -1 to indicate they couldn't be fetched` }` // Check qdrant health if available let qdrantHealthy = $state // TODO: Verify store subscription is correct for Svelte 5<boolean>(false); try { qdrantHealthy = await qdrantService.healthCheck()}catch (e) { qdrantHealthy = false} return json({ success: true, healthy, qdrantHealthy && !!db: stats: { documents: docCount, embeddings: embCount, searchSessions: sesCount }, services: { pgvector: 'âœ… PostgreSQL pgvector', ollama: 'âœ… Ollama embeddinggemma', qdrant: qdrantHealthy ? 'âœ… Qdrant vector DB'  :  'âŒ Qdrant unreachable', langextract: `âœ… langextract-go entities` }, timestamp: new Date().toISOString() })} return json({ success: true, action })}catch (err: unknown) { console.error('[Test RAG Search] GET error: ', err); return json({ error: 'Failed', details: err instanceof Error ? err.message, String(err) }, { status: 500 })}; 


