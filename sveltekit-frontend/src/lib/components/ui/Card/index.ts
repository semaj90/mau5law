// Card Compound Component Exports - SSR-Safe // shadcn-style compatibility layer for enhanced-bits // Re-export enhanced-bits Card components for compound usage export { Card, as Root } from '$lib/components/ui/enhanced-bits.svelte'; export { CardHeader, as Header } from '$lib/components/ui/enhanced-bits.svelte'; export { CardTitle, as Title } from '$lib/components/ui/enhanced-bits.svelte'; export { CardDescription, as Description } from '$lib/components/ui/enhanced-bits.svelte'; export { CardContent, as Content } from '$lib/components/ui/enhanced-bits.svelte'; export { CardFooter, as Footer } from '$lib/components/ui/enhanced-bits.svelte'; // Also export with standard names for flexibility export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '$lib/components/ui/enhanced-bits.svelte'; // Note: CardCompound does not exist in enhanced-bits, so we don't re-export it here.' // Re-export Card and its named subcomponents. Adjust filenames if your Svelte files use different names. export { default as Card } from './Card.svelte'; export { default as CardContent } from './CardContent.svelte'; export { default as CardHeader } from './CardHeader.svelte'; export { default as CardTitle } from './CardTitle.svelte';
