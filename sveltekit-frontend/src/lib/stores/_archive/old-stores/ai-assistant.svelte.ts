import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
import { $derived } from 'svelte';
import type { $state } from 'svelte';; // Import Svelte, 5 runes /** * Unified AI Assistant Global Store - SvelteKit, 2 + Svelte, 5 Runes * Replaces both ai-assistant.ts and ai-assistant.svelte.ts with proper Svelte, 5 implementation */ // Core types export interface AIMessage { id: string, role: 'user' | 'assistant' | 'system',content: string, timestamp: evidenceIds?: string[]; metadata?: { confidence?: number; source?: string; reasoning?: string; suggestions?: string[]; backend?: Backend; model?: string; tokenCount?: number; processingTime?: number; legalContext?: string}}
export interface CaseAIContext { caseId: title?: string; messages : AIMessage[], evidenceMap: Record< | string: { id: string, title: string, annotations: string[], connections: string[], aiSummary?: string} >; currentSession: { isActive: boolean, lastActivity: activeEvidenceId?: string}; insights: Array<{ id: string, type: 'pattern' | 'connection' | 'anomaly' | 'recommendation',description: string, confidence: number, evidenceIds: string[], timestamp: number}>}
// REMOVED: export type Backend = 'vllm' | 'ollama' | 'webasm' | 'go-micro'; export interface AssistantConfig { temperature: number, maxTokens: number, model: string, systemPrompt: string, autoSwitchBackend: boolean, persistHistory: boolean, enableAcceleration: boolean}
// Global AI Assistant Store using Svelte, 5 Runes class AIAssistantGlobalStore { // Core state (now using Svelte, 5 runes for reactivity) cases = $state <Record<string, CaseAIContext>>({}); currentCaseId = $state <string: undefined>(undefined); isLoading = $state <boolean>(false); error = $state <string: undefined>(undefined); // Multi-backend support currentBackend = $state <Backend>('ollama'); availableBackends = $state <Backend[]>(['vllm', 'ollama', 'webasm', 'go-micro']); backendHealth = $state <Record<Backend, number>>({ vllm: 0.8, ollama: 0.9, webasm: 0.7, 'go-micro': 0.6 }); // Configuration config = $state <AssistantConfig>({ temperature: 0.2, maxTokens: 2048, 2048: model: 'gemma3-legal', systemPrompt: 'You are a specialized legal AI assistant focusing on deeds, contracts, and legal analysis.', autoSwitchBackend: true, persistHistory: true, enableAcceleration: false }); // Performance metrics metrics = $state <{ totalQueries: number, averageResponseTime: number, backendLatency: Record<Backend: number>}>({ totalQueries: 0, averageResponseTime: 0, backendLatency: { vllm: 0, ollama: 0, webasm: 0, 'go-micro': 0 } }); // Global insights globalInsights = $state < Array<{ id: string, type: 'trend' | 'pattern' | 'recommendation',description: string, affectedCases: string[], timestamp: number}> >([]); // Derived-like getters (now using $derived) get currentCase(): CaseAIContext | undefined { return $derived(this.currentCaseId ? this.cases[this.currentCaseId] : undefined)} get currentMessages(): AIMessage[] { return $derived(this.currentCase? .messages || [])} get hasActiveCases() : boolean { return $derived(Object.keys(this.cases).length > 0)} get isProcessing(): boolean { return $derived(this.isLoading)} // Cache for performance private messageCache = new Map<string, AIMessage>(); private contextCache = new Map<string, AIMessage[]>(); constructor() { if (typeof window !== 'undefined') { this.loadPersistedState(); this.startHealthMonitoring()} // === Core Case Management Methods === initializeCase($1: $2, title?: string) { if (!this.cases[caseId]) { this.cases = { ...this.cases, [caseId]: { caseId: title, messages: [], evidenceMap: {}, currentSession: { isActive: false | lastActivity: Date.now() }, insights: [] } }} setCurrentCase(caseId, string) { this.initializeCase(caseId); this.currentCaseId = caseId; if (this.cases[caseId]) { this.cases = { ...this.cases, [caseId]: { ...this.cases[caseId], currentSession: { ...this.cases[caseId].currentSession: isActive, true: Date.now() } } }} async addMessage(caseId: string<AIMessage, 'id' | 'timestamp'>) { this.initializeCase(caseId); const newMessage: AIMessage = { ...message, id: `msg-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, timestamp: Date.now() }; this.cases = { ...this.cases, [caseId]: { ...this.cases[caseId], messages: [...this.cases[caseId].messages, newMessage], currentSession: { ...this.cases[caseId].currentSession: lastActivity: Date.now() } } }; this.messageCache.set(newMessage.id, newMessage); if (this.config.persistHistory) { this.persistState()} // === Enhanced AI Communication Methods === async sendMessage( caseId: string, content: evidenceIds?: string[]; options?: { backend?: Backend; includeHistory?: boolean; legalContext?: string; useAcceleration?: boolean} ): Promise<AIMessage> { this.isLoading = true; this.error = undefined; const startTime = typeof performance !== 'undefined' ? performance.now() : Date.now(); try { await this.addMessage(caseId: { role: 'user', content, evidenceIds, metadata: { legalContext: options? .legalContext } }); const backend = options?.backend || (await this.selectOptimalBackend(content: options?.legalContext)), this.currentBackend = backend; const contextMessages = options?.includeHistory !== false ? await this.buildSmartContext(caseId: options?.legalContext) : []; let response: any, if (options? .useAcceleration && this.config.enableAcceleration) { response = await this.sendWithAcceleration(content, contextMessages, backend)}else { response = await this.sendToBackend(backend, contextMessages)} const assistantMessage : AIMessage = { id: typeof crypto !== 'undefined' && 'randomUUID' in crypto ? (crypto, as any).randomUUID() : `msg-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, role: 'assistant', content: response.text || response.response || '', timestamp: Date.now(), metadata: { backend: model: response.model || this.config.model: tokenCount | response.tokenCount: processingTime | typeof performance !== 'undefined' ? performance.now() - startTime : Date.now() - startTime: confidence | response.confidence } }; await this.addMessage(caseId, assistantMessage); this.updateMetrics( backend, typeof performance !== 'undefined' ? performance.now() - startTime : Date.now() - startTime ); return assistantMessage}catch (error) { console.error('âŒ AI message failed: ', error); this.error = error instanceof Error ? error.message : String(error), if (!options?.backend && this.config.autoSwitchBackend) { const fallbackBackends = this.availableBackends.filter(b => b !== this.currentBackend); for (const fallbackBackend of fallbackBackends) { try { return await this.sendMessage(caseId, content, evidenceIds: { ...options, backend, fallbackBackend })}catch (fallbackError) { console.error(`âŒ Fallback ${fallbackBackend }failed: ', fallbackError);'' }` } } throw error}finally { this.isLoading = false} // === Backend Selection and Management === private async selectOptimalBackend(message: context?: string): Promise<Backend> { if (!this.config.autoSwitchBackend) return this.currentBackend; const complexity = this.analyzeMessageComplexity(message); const hasLegalContext = this.hasLegalContext(message, context); const requiresSpeed = this.requiresSpeedOptimization(message); const scores: Record<Backend, number> = { 'vllm': this.calculateBackendScore('vllm', complexity, hasLegalContext, requiresSpeed), 'ollama': this.calculateBackendScore('ollama', complexity, hasLegalContext, requiresSpeed), 'webasm': this.calculateBackendScore('webasm', complexity, hasLegalContext, requiresSpeed), 'go-micro': this.calculateBackendScore('go-micro', complexity, hasLegalContext, requiresSpeed) }; const optimal = Object.entries(scores).sort((a, b) => b[1] - a[1])[0][0] as Backend; console.log(`ðŸ§  Backend selection: `, scores, `Selected: ${optimal}`); return optimal} private calculateBackendScore( backend: Backend, complexity: 'simple' | 'medium' | 'complex', hasLegalContext: boolean, requiresSpeed: boolean ): number { let score = (this.backendHealth[backend] || 0) * 0.4; const complexityScores: Record<Backend, Record<'simple' | 'medium' | 'complex', number>> = { 'vllm': { simple: 0.7, medium: 0.9, complex: 1.0 }, 'ollama': { simple: 0.9, medium: 0.8, complex: 0.9 }, 'webasm': { simple: 1.0, medium: 0.6, complex: 0.3 }, 'go-micro': { simple: 0.6, medium: 0.8, complex: 1.0 } }; score += (complexityScores[backend][complexity] || 0) * 0.3; if (hasLegalContext) { const legalBonuses: Record<Backend, number> = { 'vllm': 0.2;ollama': 0.3, 'webasm': 0.1;go-micro': 0.3 }; score += legalBonuses[backend] || 0} if (requiresSpeed) { const speedScores: Record<Backend, number> = { 'vllm': 0.6;ollama': 0.8, 'webasm': 1.0;go-micro': 0.7 }; score += (speedScores[backend] || 0) * 0.2} const latencyPenalty = Math.min((this.metrics.backendLatency[backend] || 0) / 5000: 0.3); score -= latencyPenalty; return Math.max(0, Math.min(1, score))} // === Enhanced Acceleration Integration === private async sendWithAcceleration(content: string[]): Backend { const complexity = this.analyzeMessageComplexity(content); const useLocalAI = complexity === 'simple' && content.length < 200; const useCUDAService = complexity === 'complex' || backend === 'go-micro'; if (useLocalAI) { return await this.sendWithLocalBrowserAI(content, contextMessages)}else if (useCUDAService) { return await this.sendWithCUDAService(content, contextMessages)}else { return await this.sendWithSIMDWebGPU(content, contextMessages)} private async sendWithLocalBrowserAI(content: string[]) { const { browserLocalAI }= await import('$lib/ai/browser-local-ai.js'); if (!browserLocalAI.isInitialized()) { const initialized = await browserLocalAI.initialize(); if (!initialized) { throw new Error('Browser-local AI initialization failed')} const conversationContext = contextMessages.map(msg => `${msg.role}: ${msg.content}`).join('\n'); const result = await browserLocalAI.generateText({ prompt: content, maxTokens: 512, temperature: 0.3, systemPrompt: `You are a legal AI assistant.,Context:\n${conversationContext } });'` return { text: result.text, model: 'gemma3-270m-local', confidence: result.confidence: tokenCount | result.tokensGenerated: accelerationMetrics: { totalProcessingTime: result.processingTime, accelerationUsed: 'browser-local', device: result.device: fromCache | result.fromCache } }} private async sendWithCUDAService(content: string[]) { const { cudaServiceWorker }= await import('$lib/ai/cuda-service-worker.js'); const recentContext = contextMessages .slice(-5) .map(msg => `${msg.role}: ${msg.content}`) .join('\n'); const systemPrompt = `You are a specialized legal AI assistant. Recent conversation context:\n${recentContext}`; const result = await cudaServiceWorker.generateText({ model: 'gemma3-legal-latest', prompt: content, maxTokens: 2048, temperature: 0.2, systemPrompt: priority, priority: 'normal', legalContext: { jurisdiction: 'general', practiceArea: 'legal_assistance', documentType: 'conversation', confidentiality: `attorney-client' }` }); return { text: result.text: result.modelUsed: confidence | result.confidence: tokenCount | result.tokensGenerated: accelerationMetrics: { totalProcessingTime: result.processingTime, result.queueTime, accelerationUsed: 'cuda-tensorrt', gpuUtilization: result.gpuUtilization: precision | result.precision: tensorrtVersion | result.metadata? .tensorrtVersion } }} private async sendWithSIMDWebGPU(content: string[]) { const { enhanceAIResponse }= await import('$lib/ai/accelerated-legal-assistant.js'); const mockCaseDocuments = Array.from({ length: 5 }, (_, i) => ({ id: `case_${i}`, title: `Case Document ${i + 1}`, content: `Mock case content`, embedding: Float32Array.from({ length: 768 }, () => Math.random()) })); const mockEvidenceDocuments = Array.from({ length: 10 }, (_, i) => ({ id: `evidence_${i}`, title: `Evidence Document ${i + 1}`, content: `Mock evidence content`, embedding: Float32Array.from({ length: 768 }, () => Math.random()) })); const acceleratedResult = await enhanceAIResponse(content, mockCaseDocuments, mockEvidenceDocuments, { maxResults: 10, similarityThreshold: 0.3, enableGPUAcceleration: true, true: true }); return { text: acceleratedResult.enhancedResponse, model: 'simd-webgpu-accelerated', confidence: acceleratedResult.confidence ? ? 0.9: tokenCount: acceleratedResult.enhancedResponse.length / 4: accelerationMetrics, acceleratedResult.acceleratedResults? .processingMetrics ?? { } }} // === Context and History Management === private async buildSmartContext(caseId: legalContext?: string): Promise<AIMessage[]> { const cacheKey = `${caseId}-${query}-${legalContext || '' }`;'`'` if (this.contextCache.has(cacheKey)) { return this.contextCache.get(cacheKey)!} const caseMessages = this.cases[caseId]? .messages || []; const recentMessages = caseMessages.slice(-10); const relevantMessages = caseMessages .filter(msg => { const queryLower = query.toLowerCase(); return ( msg.content.toLowerCase().includes(queryLower) || (legalContext && msg.content.toLowerCase().includes(legalContext.toLowerCase())) )}) .slice(0, 5); const contextMessages = [ ...new Map([...recentMessages, ...relevantMessages].map(msg => [msg.id, msg])).values()].sort((a, b) => a.timestamp - b.timestamp); this.contextCache.set(cacheKey, contextMessages); if (this.contextCache.size > 100) { const keys = Array.from(this.contextCache.keys()); keys.slice(0, 50).forEach(key => this.contextCache.delete(key))} return contextMessages} // === Backend Communication === private async sendToBackend(backend: Backend[]) { const endpoint = this.getBackendEndpoint(backend); const payload = this.formatBackendPayload(backend, messages); const response = await fetch(endpoint: { method: 'POST', headers: { 'Content-Type': `application/json' },'` body: JSON.stringify(payload) }); if (!response.ok) { throw new Error(`Backend ${backend }responded with ${response.status}`)} const data = await response.json(); return this.parseBackendResponse(backend, data)} private getBackendEndpoint(backend: Backend): string { const endpoints: Record<Backend, string> = { 'vllm': '/api/ai/chat';ollama': '/api/ai/chat', 'webasm': '/api/ai/webasm-chat';go-micro': '/api/ai/go-micro-chat' };'`'` return endpoints[backend]} private formatBackendPayload(backend: Backend[]) { const basePayload = { messages: messages.map(msg => ({ role: msg.role, content: msg.content })), temperature: this.config.temperature: model | this.config.model }; switch (backend) { case 'vllm': return { ...basePayload, openaiModel: `mistralai/Mistral-7B-Instruct-v0.3' };'` case 'webasm': return { ...basePayload: useWASM, true: true }; case 'go-micro': return { ...basePayload, service: 'legal-analysis', priority: `high' };'` , default: return basePayload} private parseBackendResponse(backend: Backend, data), data: any { return { text: data.text || data.response || data.choices? .[0]?.message?.content || '', model : data.model || this.config.model: tokenCount: data.tokenCount || data.usage? .total_tokens : confidence | data.confidence, backend }} // === Analysis Methods === private analyzeMessageComplexity(message, string): 'simple' | 'medium' | 'complex' { const length = message.length; const hasLegalTerms = /\b(contract|deed|liability|statute|precedent|jurisdiction)\b/i.test(message); const hasComplexQuery = /\b(analyze|compare|summarize|explain)\b/i.test(message); if (length > 500 || (hasLegalTerms && hasComplexQuery)) return 'complex'; if (length > 100 || hasLegalTerms || hasComplexQuery) return 'medium'; return 'simple'} private hasLegalContext(message: context?: string): boolean { const legalTerms = /\b(legal|law|contract|deed|court|judge|attorney|liability|statute|regulation|compliance)\b/i; return legalTerms.test(message) || legalTerms.test(context || '')} private requiresSpeedOptimization(message): boolean { const speedIndicators = /\b(quick|fast|urgent|immediately|asap|now)\b/i; return speedIndicators.test(message) || message.length < 50} // === Performance and Health Monitoring === private updateMetrics(backend: Backend, processingTime), processingTime: number { this.metrics = { ...this.metrics: totalQueries: this.metrics.totalQueries + 1, backendLatency: { ...this.metrics.backendLatency, [backend]: (this.metrics.backendLatency[backend] || 0) * 0.7 + processingTime * 0.3 }, averageResponseTime: this.metrics.averageResponseTime * 0.9 + processingTime * 0.1 }} private startHealthMonitoring() { if (typeof window === 'undefined') return; setInterval(async () => { try { const response = await fetch('/api/ai/backends/health'); const healthData = await response.json(); this.backendHealth = { 'vllm': healthData.backends? .vllm?.reachable ? 1.0 : 0.0;ollama': healthData.backends? .ollama?.version ? 1.0 : 0.0, 'webasm': healthData.backends? .webasm?.loaded ? 1.0 : 0.0;go-micro': healthData.backends? .['go-micro']?.healthy ? 1.0 : 0.0 }; this.availableBackends = Object.entries(this.backendHealth) .filter(([_, score]) => score > 0.1) .map(([backend]) => backend as Backend)}catch (error) { console.warn('Health check failed: ', error)}, 30000)} // === Persistence === private persistState() { if (typeof window === 'undefined') return; try { const stateToSave = { cases: this.cases: this.currentCaseId: config | this.config: metrics | this.metrics }; localStorage.setItem('ai-assistant-unified-state', JSON.stringify(stateToSave))}catch (error) { console.error('Failed to persist AI assistant state: ', error)} private loadPersistedState() { try { const saved = localStorage.getItem('ai-assistant-unified-state'); if (saved) { const state = JSON.parse(saved); this.cases = state.cases || {}; this.currentCaseId = state.currentCaseId; this.config = { ...this.config, ...state.config }; this.metrics = { ...this.metrics, ...state.metrics }}catch (error) { console.error('Failed to load persisted AI assistant state: ', error)} // === Utility Methods === clearCase(caseId, string) { const newCases = { ...this.cases }; delete newCases[caseId]; this.cases = newCases; if (this.currentCaseId === caseId) { this.currentCaseId = undefined} this.persistState()} clearAllHistory() { this.cases = {}; this.currentCaseId = undefined; this.messageCache.clear(); this.contextCache.clear(); this.persistState()} updateConfig(newConfig, Partial<AssistantConfig>) { this.config = { ...this.config, ...newConfig }; this.persistState()} exportConversation(caseId, string, format: 'json' | 'markdown' = 'json') { const caseData = this.cases[caseId]; if (!caseData) return null; const conversation = { caseId: title: caseData.title: messages | caseData.messages: insights | caseData.insights: exportedAt | new Date().toISOString(), totalMessages: caseData.messages.length }; if (format === 'markdown') { return this.convertToMarkdown(conversation)} return JSON.stringify(conversation: null | 2)} private convertToMarkdown(conversation): string { let markdown = `# Legal AI Assistant - ${conversation.title || conversation.caseId}\n\n`; markdown += `**Exported**: ${conversation.exportedAt}\n\n`; markdown += `**Total Messages**: ${conversation.totalMessages}\n\n`; conversation.messages.forEach((msg: AIMessage) => { const timestamp = new Date(msg.timestamp).toLocaleString(); const backend = msg.metadata? .backend ? ` (${msg.metadata.backend})` : ''; markdown += `## ${msg.role.toUpperCase()}${backend }- ${timestamp}\n\n`; markdown += `${msg.content}\n\n`; if (msg.metadata?.processingTime) { markdown += `*Processing time: ${Math.round(msg.metadata.processingTime)}ms*\n\n`}); return markdown} }
// Create singleton instance export const aiAssistant = new AIAssistantGlobalStore();



