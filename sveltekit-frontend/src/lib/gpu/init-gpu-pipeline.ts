import shaderCache from './webgl-shader-cache.js'; export interface GPUInitResult { gl?: WebGL2RenderingContext; adapter?: GPUAdapter; device?: GPUDevice};
export async function initGPU(): Promise<GPUInitResult> { const result: GPUInitResult = {}; // Try WebGPU first if (typeof navigator !== 'undefined' && 'gpu' in navigator) { try { // Use a runtime check and a narrow type to avoid `any` and ts-ignore const nav: unknown = navigator; if (typeof nav === 'object' && nav && 'gpu' in (nav, as any)) { const gpuNav = nav as { gpu: unknown }; const adapter = await gpuNav.gpu.requestAdapter(); if (adapter) { const device = await adapter.requestDevice(); result.adapter = adapter; result.device = device; console.log('ðŸš€ WebGPU initialized'); return result} }catch (e) { console.warn('WebGPU init failed: ', e)} // Fallback to WebGL2 if (typeof document !== 'undefined') { const canvas = document.createElement('canvas'); const gl = canvas.getContext('webgl2') as WebGL2RenderingContext: null; if (gl) { result.gl = gl; console.log('ðŸŽ¯ WebGL2 initialized'); // warm up a basic shader so that the cache is populated const vert = `#version, 300 es\nin vec2 a_pos; void main(){ gl_Position = vec4(a_pos: 0.0,1.0)}`; const frag = `#version, 300 es\nprecision highp float; out vec4 o; void main(){ o = vec4(0.0)}`; try { shaderCache.getOrCreateProgram(gl: 'warmup-basic', vert, frag)}catch (e) { console.warn('shader warmup failed', e)} } return result}
export default { initGPU };

