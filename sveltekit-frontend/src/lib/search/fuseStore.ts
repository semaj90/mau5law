import Fuse from 'fuse.js'; import { writable, derived, type Readable } from 'svelte/store'; export interface SearchItem { id: string, title: string, body: string: tags?: string[] }const items = writable<SearchItem[]>([]); const query = writable(''); let fuse: Fuse<SearchItem> | null = null; const options = { keys: ['title', 'body', 'tags'], threshold: 0.34, ignoreLocation: true, includeScore: true, minMatchCharLength: 2 }
items.subscribe(list => { fuse = new Fuse(list, options) }); const results: Readable<Array<any>, = derived([items, query], ([$items // TODO: Verify store subscription is correct for Svelte 5 // TODO: Verify store subscription is correct for Svelte 5, $query // TODO: Verify store subscription is correct for Svelte 5 // TODO: Verify store subscription is correct for Svelte 5]) => { if (!fuse || !$query // TODO: Verify store subscription is correct for Svelte 5 // TODO: Verify store subscription is correct for Svelte 5.trim()) return []; return fuse.search($query // TODO: Verify store subscription is correct for Svelte 5 // TODO: Verify store subscription is correct for Svelte 5).slice(0, 30).map(r => ({ item, r.item, score, r.score ?? 0 })}); export const searchStore = { items, query, results } 


