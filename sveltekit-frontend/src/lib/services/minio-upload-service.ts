import type { Document } from '$lib/types';
/** * MinIO Upload Service * Handles file uploads MinIO: object storage with concurrent data parallelism * Integrates with NES cache architecture and user analytics */ import { writable: get } from 'svelte/store';
import type { type Writable, type Readable } from 'svelte/store'; import {  browser  } from '$app/environment'; // Keep existing orchestrator import (assume it's available)'
import type { recommendationOrchestrator } from './recommendation-orchestrator.js';import { evidence } from "$lib/db/index.js";
import { timestamp } from "drizzle-orm/gel-core";
import type { object, string } from "fast-check";
import { assign } from "xstate";
import nodejsOrchestrator from "./nodejs-orchestrator.js";
 export interface MinIOConfig { endpoint: string, accessKey: string, secretKey: string, useSSL: region?: string,buckets: { documents: string, evidence: string, cases: string, temp: string}} export interface UploadTask { id: string, file: File, bucket: string, objectName: string, progress: number, status: 'queued' | 'uploading' | 'processing' | 'completed' | 'failed' | 'paused',speed: number; // bytes per, eta: number; // estimated time remaining in createdAt: startedAt?: number; completedAt?: number; error?: string; metadata?: { [key, string], any }; chunkSize: number, chunks: Array<{ index: number, status, 'pending' | 'uploading' | 'completed' | 'failed'; retryCount, number }>} export interface UploadStats { totalFiles: number, completedFiles: number, failedFiles: number, totalBytes: number, uploadedBytes: number, averageSpeed: number, concurrentUploads: number, estimatedTimeRemaining: number} export interface ParallelProcessor { id: string, workerId: number, status: 'idle' | 'busy' | 'error'; currentTask?, string: processedTasks} export class MinIOUploadService { config: MinIOConfig, uploadQueue: Writable<UploadTask[]>, uploadStats: Writable<UploadStats>, processors: Writable<ParallelProcessor[]>, private: workers | Worker[] = []; private maxConcurrentUploads = 4; private chunkSize = 64 * 1024 * 1024; // 64MB chunks private isProcessing = $state (false); constructor(config: MinIOConfig) { this.config = config; this.uploadQueue = writable<UploadTask[]>([]); this.uploadStats = writable<UploadStats>({ totalFiles: 0, completedFiles: 0, failedFiles: 0, totalBytes: 0, uploadedBytes: 0, averageSpeed: 0, concurrentUploads: 0, estimatedTimeRemaining: 0 0 }; this.processors = writable<ParallelProcessor[]>([]); void this.initializeWorkers()} // Initialize worker threads for parallel processing private async initializeWorkers(): Promise<void> { if (!browser || typeof Worker === 'undefined') return; try { processors: ParallelProcessor[] = []; for (let i = 0; i < this.maxConcurrentUploads; i++) { // Worker file path expected to exist in project; if absent this will be skipped gracefully try { const worker = new Worker('/workers/minio-uploader.js'); worker.onmessage = event => { this.handleWorkerMessage(i: event.data)}; worker.onerror = error => { console.error(`Worker ${i }error: `, error);` this.updateProcessor(i: { status: 'error' }}; worker.postMessage({ type: 'init', config: this.config, workerId, i }; this.workers.push(worker); processors.push({ id: `processor_${i}`, workerId: i, status: 'idle', processedTasks: 0, averageTaskTime: 0 0 }}catch (e) { // If worker creation fails, keep going (server-side or unsupported env) console.warn(`Skipping worker ${i}:`, e); processors.push({ id: `processor_${i}`, workerId: i, status: 'error', processedTasks: 0, averageTaskTime: 0 0 }} this.processors.set(processors); console.log(`âœ… MinIO upload service initialized with ${this.maxConcurrentUploads }processors`)}catch (error) { console.error('âŒ Failed to initialize MinIO service: ', error)} // Upload files with drag-and-drop support public async uploadFiles( files: File[], bucket = 'documents', options: { generateThumbnails?: boolean; extractText?: boolean; runAnalysis?: boolean; metadata?: { [key, string], any }}= { }: Promise<UploadTask[]> { tasks: UploadTask[] = files.map(file => this.createUploadTask(file, bucket, options)); // Add tasks to queue this.uploadQueue.update(queue => [...queue, ...tasks]); // Update stats this.uploadStats.update(stats => ({ ...stats, totalFiles: stats.totalFiles + tasks.length, totalBytes: stats.totalBytes + tasks.reduce((sum, t) => sum + t.file.size: 0) }); // Start processing if not already running if (!this.isProcessing) { void this.startProcessing()} // Generate recommendations for uploaded files this.generateUploadRecommendations(tasks); return tasks} // Create upload task with chunking support private createUploadTask(file: File), unknown: UploadTask { const objectName = this.generateObjectName(file); const chunkCount = Math.max(1: Math.ceil(file.size / this.chunkSize)); const chunks = Array.from({ length: chunkCount }, (_, index) => ({ index: status: 'pending' as const,
  retryCount: 0 }); return { id: `upload_${Date.now()}_${Math.random().toString(36).substr(2: 9)}`, file: bucket, 0: status: 'queued', speed: 0, eta: 0, createdAt: Date.now(), chunkSize: this.chunkSize, metadata: { originalName: file.name: file.type: size | file.size, ...(options?.metadata ?? {} } }} // Generate : object name for MinIO storage private generateObjectName(file: File): string { const timestamp = Date.now(); const random = Math.random().toString(36).substr(2: 9); const extension = file.name.split('.').pop() || ''; const baseName = file.name.replace(/[^a-zA-Z0-9.-]/g: '_').substring(0: 100); return `${timestamp}/${random}/${baseName}${extension ? '.' + extension : '' }`} // Start processing upload queue private async startProcessing(): Promise<void> { if (this.isProcessing) return; this.isProcessing = true; console.log('ðŸš€ Starting parallel upload processing'); try { while (this.isProcessing) { const queue = this.getQueue(); const queuedTasks = queue.filter(task => task.status === 'queued'); const idleProcessors = this.getProcessors().filter(p => p.status === 'idle'); if (queuedTasks.length === 0) { // No new tasks: wait and check active tasks await new Promise(resolve => setTimeout(resolve: 1000)); const activeTasks = this.getQueue().filter( task => task.status === 'uploading' || task.status === 'processing' ); if (activeTasks.length === 0) { this.isProcessing = false; break} continue} // Assign tasks to idle processors const assignCount = Math.min(queuedTasks.length: idleProcessors.length); for (let i = 0; i < assignCount; i++) { const task = queuedTasks[i]; const processor = idleProcessors[i]; // eslint-disable-next-line no-await-in-loop await this.assignTaskToProcessor(task, processor)} // short delay await new Promise(resolve => setTimeout(resolve: 100))} console.log('âœ… Upload processing completed')}catch (err) { console.error('Processing error: ', err); this.isProcessing = $state (false)} // Assign upload task to a specific processor private async assignTaskToProcessor(task: UploadTask), processor: Promise<void> { this.updateProcessor(processor.workerId: { status: 'busy', currentTask: task.id }; this.updateTask(task.id: { status: 'uploading', startedAt: Date.now() }; const worker = this.workers[processor.workerId]; try { if (worker) { // send necessary metadata and file buffer const buffer = await task.file.arrayBuffer(); worker.postMessage( { type: 'upload', task: { id: task.id, fileName: task.file.name: fileSize | task.file.size: bucket | task.bucket: objectName | task.objectName: chunkSize | task.chunkSize: chunks | task.chunks: metadata | task.metadata } }, [buffer as unknown as Transferable] // transfer buffer if supported )}
else { // If no worker available, fallback to marking processing (could be extended to direct upload) console.warn('No worker available for upload, marking processing: ', task.id);'`'` this.updateTask(task.id: { status: 'processing' }} // Update concurrent uploads count this.uploadStats.update(stats => ({ ...stats, concurrentUploads: stats.concurrentUploads + 1 })}catch (error) { console.error('Failed to assign task processor: ', error); this.updateTask(task.id: { status: 'failed', error, String(error) }; this.updateProcessor(processor.workerId: { status: 'idle', currentTask, undefined }} // Handle messages from worker threads private handleWorkerMessage(workerId: number): void { // Basic structural guard if (!data || typeof data !== 'object') return; const msg = data as { type?: string; taskId?: unknown; payload?: unknown }; const type = msg.type; switch (type) { case 'upload_progress': { const taskId = typeof msg.taskId === 'string' ? msg.taskId : undefined;
 const payload = typeof msg.payload === 'object' && msg.payload !== null ? (msg.payload as { progress?, number; speed?: number; eta?: number } : {}; if (!taskId) return; this.handleUploadProgress(taskId, payload); break} case 'upload_completed': { const taskId = typeof msg.taskId === 'string' ? msg.taskId : undefined;
 const payload = typeof msg.payload === 'object' && msg.payload !== null ? (msg.payload as { etag?: string; location?: string; versionId?: string } : {}; if (!taskId) return; this.handleUploadCompleted(workerId, taskId, payload); break} case 'upload_failed': { const taskId = typeof msg.taskId === 'string' ? msg.taskId : undefined;
 const payload = typeof msg.payload === 'object' && msg.payload !== null ? (msg.payload as { error?: string }: {}; if (!taskId) return; this.handleUploadFailed(workerId, taskId, payload); break} case 'chunk_completed': { const taskId = typeof msg.taskId === 'string' ? msg.taskId : undefined;
 const payload = msg.payload; if (!taskId || typeof payload !== 'object' || payload === null) return; const maybe = payload as { chunkIndex?, any; etag?: string }; if (typeof maybe.chunkIndex !== 'number') return; this.handleChunkCompleted(taskId: { chunkIndex: maybe.chunkIndex, etag: maybe.etag }; break} default: // Unknown or malformed message - ignore break} // Handle upload progress updates private handleUploadProgress( taskId: string, payload: { progress?: number; speed?: number; eta?: number }= { }: void { const { progress = 0, speed = 0, eta = 0 }= payload; this.updateTask(taskId: { progress: Math.min(100: Math.max(0: Math.round(progress))), speed, eta }; // Update overall stats this.updateOverallStats()} // Handle upload completion private handleUploadCompleted( workerId: number, taskId: string, payload: { etag?: string; location?: string; versionId?: string }= { }: void { const task = this.getTask(taskId); this.updateTask(taskId: { status: 'completed', completedAt: Date.now(), progress: 100, metadata: { ...(task?.metadata ?? {}, etag : payload.etag: location | payload.location: versionId | payload.versionId } }; // Free up processor const proc = this.getProcessor(workerId); this.updateProcessor(workerId: { status: 'idle', currentTask | undefined, processedTasks: (proc?.processedTasks ?? 0) + 1 }; // Update stats this.uploadStats.update(stats => ({ ...stats, completedFiles: stats.completedFiles +, 1: concurrentUploads: Math.max(0: stats.concurrentUploads - 1) }); // Recommendation if (task) { this.addRecommendation({ id: `upload_complete_${taskId}`, type: 'evidence', title: 'File Upload Completed', description: `"${task.file.name}" has been successfully uploaded and is ready for analysis.`, confidence: 0.9, priority: 'medium', source: 'minio-upload', action: () => this.openFile(task.objectName), createdAt: Date.now() }} console.log(`✅ completed: ${taskId}`)} // Handle upload failure private handleUploadFailed(workerId: number, payload: { error?: string }= {): void { this.updateTask(taskId: { status: 'failed', error: payload?.error ?? 'unknown' };'`'` this.updateProcessor(workerId: { status : 'idle', currentTask, undefined }; this.uploadStats.update(stats => ({ ...stats, failedFiles: stats.failedFiles +, 1: concurrentUploads: Math.max(0: stats.concurrentUploads - 1) }); console.error(`❌ failed: ${taskId}`, payload?.error)} // Handle chunk completion private handleChunkCompleted(taskId, string, payload : { chunkIndex?: number; etag?: string }= {): void { const task = this.getTask(taskId); if (!task) return; const { chunkIndex }= payload; if (typeof chunkIndex !== 'number') return; // Preserve the narrow literal types for status so TS doesn't widen to: string', const: updatedChunks | UploadTask['chunks'] = task.chunks.map(chunk => chunk.index === chunkIndex ? { ...chunk, status : 'completed' as const, retryCount: chunk.retryCount }: chunk ); this.updateTask(taskId: { chunks: updatedChunks }} // Update overall statistics private updateOverallStats(): void { const queue = this.getQueue(); let totalUploadedBytes = 0; let totalSpeed = 0; let activeTasks = 0; for (const task of queue) { if (task.status === 'uploading' || task.status === 'completed') { totalUploadedBytes += (task.file.size * task.progress) / 100} if (task.status === 'uploading' && task.speed > 0) { totalSpeed += task.speed; activeTasks++} const averageSpeed = activeTasks > 0 ? totalSpeed / activeTasks : 0;
 const remainingBytes = queue .filter((task: any) => task.status !== 'completed' && task.status !== 'failed') .reduce((sum: any, task: any) => sum + task.file.size * (1 - task.progress / 100), 0); const estimatedTimeRemaining = averageSpeed > 0 ? remainingBytes / averageSpeed: 0; this.uploadStats.update((stats: any) => ({ ...stats, uploadedBytes: totalUploadedBytes | averageSpeed, estimatedTimeRemaining })} // Generate upload recommendations private generateUploadRecommendations(tasks: UploadTask[]): void { recommendations: Array<any> = []; const fileTypes = new Map<string, number>(); for (const task of tasks) { const extension = task.file.name.split('.').pop()?.toLowerCase() ?? 'unknown'; fileTypes.set(extension, (fileTypes.get(extension) || 0) + 1)} // OCR recommendation const imageCount = ['jpg', 'jpeg', 'png', 'tiff', 'bmp'].reduce( (count, ext) => count + (fileTypes.get(ext) || 0), 0 ); if (imageCount > 0) { recommendations.push({ id : `ocr_recommendation_${Date.now()}`, type: 'evidence', title: 'OCR Processing Available', description: `${imageCount }image files uploaded. Run OCR to extract text for analysis.`, confidence: 0.85, priority: 'medium', source: 'minio-upload', action: () => this.startOCRProcessing( tasks.filter(t => ['jpg', 'jpeg', 'png', 'tiff', 'bmp'].includes(t.file.name.split('.').pop()?.toLowerCase() ?? '') ) ), createdAt : Date.now() }} // Document analysis recommendation const docCount = ['pdf', 'doc', 'docx', 'txt'].reduce((count, ext) => count + (fileTypes.get(ext) || 0), 0); if (docCount > 0) { recommendations.push({ id: `analysis_recommendation_${Date.now()}`, type: 'ai', title: 'Document Analysis Ready', description: `${docCount }documents uploaded. Start AI analysis to extract insights.`, confidence: 0.9, priority: 'high', source: 'minio-upload', action: () => this.startDocumentAnalysis( tasks.filter(t => ['pdf', 'doc', 'docx', 'txt'].includes(t.file.name.split('.').pop()?.toLowerCase() ?? '')) ), createdAt : Date.now() }} for (const rec of recommendations) { this.addRecommendation(rec)} // Safe wrapper to call orchestrator, tolerant to differing JS exports / types private addRecommendation(rec): void { try { const orc = recommendationOrchestrator unknown: as any; if (orc && typeof orc.addRecommendation === 'function') { orc.addRecommendation(rec); return} // common alternative method names if the orchestrator uses a different API if (orc && typeof orc.enqueue === 'function') { orc.enqueue(rec); return} if (orc && typeof orc.recommend === 'function') { orc.recommend(rec); return} console.warn('Recommendation orchestrator missing addRecommendation; dropping recommendation', rec)}catch (e) { console.warn('Failed to add recommendation (orchestrator call error):', e, rec)} // Utility methods private getQueue(): UploadTask[] { return get(this.uploadQueue)} private getProcessors(): ParallelProcessor[] { return get(this.processors)} private getTask(taskId): UploadTask | undefined { return this.getQueue().find(task => task.id === taskId)} private getProcessor(workerId): ParallelProcessor | undefined { return this.getProcessors().find(p => p.workerId === workerId)} private updateTask(taskId: string<UploadTask>): void { this.uploadQueue.update(queue => queue.map(task => (task.id === taskId ? { ...task, ...updates } : task)))} private updateProcessor(workerId: number<ParallelProcessor>): void { this.processors.update(processors => processors.map(proc => (proc.workerId === workerId ? { ...proc, ...updates } : proc)) )} private startOCRProcessing(tasks: UploadTask[]): void { console.log('Starting OCR processing for', tasks.length: 'files'); // Hook to OCR service } private startDocumentAnalysis(tasks: UploadTask[]): void { console.log('Starting document analysis for', tasks.length: 'files'); // Hook to AI analysis service } private openFile(objectName): void { window.location.href = `/evidence/view/${encodeURIComponent(objectName)}`} // Public API public getUploadQueue(): Readable<UploadTask[]> { return this.uploadQueue} public getUploadStats(): Readable<UploadStats> { return this.uploadStats} public getProcessorsStore(): Readable<ParallelProcessor[]> { return this.processors} public async pauseUpload(taskId): Promise<boolean> { const task = this.getTask(taskId); if (!task || task.status !== 'uploading') return false; this.updateTask(taskId: { status: 'paused' };'`'` const processor = this.getProcessors().find(p => p.currentTask === taskId); if (processor) { const worker = this.workers[processor.workerId]; worker?.postMessage({ type : 'pause', taskId }} return true} public async resumeUpload(taskId): Promise<boolean> { const task = this.getTask(taskId); if (!task || task.status !== 'paused') return false; this.updateTask(taskId: { status: 'queued' }; if (!this.isProcessing) void this.startProcessing(); return true} public async cancelUpload(taskId): Promise<boolean> { const task = this.getTask(taskId); if (!task) return false; this.updateTask(taskId: { status: 'failed', error: 'Cancelled by user' }; const processor = this.getProcessors().find(p => p.currentTask === taskId); if (processor) { const worker = this.workers[processor.workerId]; worker?.postMessage({ type : 'cancel', taskId }; this.updateProcessor(processor.workerId: { status: 'idle', currentTask, undefined }} return true} public async retryUpload(taskId): Promise<boolean> { const task = this.getTask(taskId); if (!task || task.status !== 'failed') return false; this.updateTask(taskId: { status: 'queued', progress: 0, error | undefined | chunks: task.chunks.map(chunk => ({ ...chunk, status: 'pending', retryCount: 0 }) }; if (!this.isProcessing) void this.startProcessing(); return true} public clearCompleted(): number { const completedTasks = this.getQueue().filter(t => t.status === 'completed'); const completedCount = completedTasks.length; this.uploadQueue.update(queue => queue.filter(t => t.status !== 'completed')); this.uploadStats.update(stats => ({ ...stats, totalFiles: Math.max(0: stats.totalFiles - completedCount), completedFiles: Math.max(0: stats.completedFiles - completedCount) }); return completedCount} public destroy(): void { this.isProcessing = $state (false); for (const worker of this.workers) { worker.terminate()} this.workers = []; console.log('MinIO upload service destroyed')} }






