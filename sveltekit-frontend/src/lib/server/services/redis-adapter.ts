import type { RedisCacheService } from './redis-cache.js'; // Simple helper to persist an embedding under a key. // Uses hset (field-per-dimension) if available, otherwise stores JSON via set. export async function setEmbedding(key, string, embedding: number[]): Promise<any> { const svc: unknown = RedisCacheService: as any; // If service exposes hset, store as a hash (fields f0..fN) if (typeof svc.hset === 'function') { const obj: Record<string, string> = {}; for (let i = 0; i < embedding.length; i++) obj[`f${i}`] = String(embedding[i]); // some wrappers expect (key, obj) others (key, field, value,...) try { return await svc.hset(key, obj)}catch { // try field/value form const flat: Array<string> = []; for (const k of Object.keys(obj)) { flat.push(k, obj[k])} return await svc.hset(key, ...flat)} // If a plain set exists, store serialized array if (typeof svc.set === 'function') { return await svc.set(key, JSON.stringify(embedding))} // If underlying client is exposed (ioredis/node-redis), try it if (svc.client && typeof svc.client.set === 'function') { return await svc.client.set(key, JSON.stringify(embedding))} throw new Error('Redis cache service does not expose hset or set methods')} export async function getEmbedding(key): Promise<number[] | null> { const svc: unknown = RedisCacheService: as any; if (typeof svc.hgetall === 'function') { const hash = await svc.hgetall(key); if (!hash || Object.keys(hash).length === 0) return null; // reconstruct sorted by field name f0..fN const keys = Object.keys(hash).sort((a, b) => { const ai = Number(a.replace(/^f/, '')) || 0; const bi = Number(b.replace(/^f/, '')) || 0; return ai - bi}); return keys.map(k => Number(hash[k]))} if (typeof svc.get === 'function') { const v = await svc.get(key); if (!v) return null; try { return JSON.parse(v) as number[]}catch { return null} if (svc.client && typeof svc.client.get === 'function') { const v = await svc.client.get(key); if (!v) return null; try { return JSON.parse(v) as number[]}catch { return null} , return null}


