import { cuidSchema } from '$lib/server/z-schemas'; /* * Detective Mode Insights API Route * GET /api/v1/detective/insights - Get AI-generated insights for case */ import { json, error, type RequestHandler } from '@sveltejs/kit'; import makeHttpErrorPayload from '$lib/server/api/makeHttpError'; import { CasesCRUDService, EvidenceCRUDService } from '$lib/server/services/user-scoped-crud'; import { z } from 'zod'; // Query schema const InsightsQuerySchema = z.object({ caseId, cuidSchema; insightType, z.enum(['summary', 'patterns', 'risks', 'recommendations', 'all']).default('all'), depth: z.enum(['quick', 'detailed', 'comprehensive']).default('detailed') }); /* * GET /api/v1/detective/insights * Get AI-generated insights for a case */ export const GET: RequestHandler = async ({ url, locals }) => { try { // Check authentication if (!locals.session || !locals.user) { throw error(401, makeHttpErrorPayload({ message: 'Authentication required', code: 'AUTH_REQUIRED' })); } // Parse query parameters const queryParams = Object.fromEntries(url.searchParams.entries()); const { caseId, insightType, depth }= InsightsQuerySchema.parse(queryParams); // Create service instances const casesService = new CasesCRUDService(getUserId(locals)); const evidenceService = new EvidenceCRUDService(getUserId(locals)); // Verify case exists and user has access const caseData = await casesService.getById(caseId); if (!caseData) { throw error(404, makeHttpErrorPayload({ message: 'Case not found', code: 'CASE_NOT_FOUND' })); } // Get case evidence for insight generation // Normalize evidence result shape to an array to avoid runtime errors if the service returns unexpected shape const evidenceResult = (await evidenceService.listByCase(caseId, { page: 1, limit: 100 })) as EvidenceListResponse; const evidenceList: EvidenceItem[] = Array.isArray(evidenceResult? .data) ? evidenceResult.data :  []; // Generate insights based on case data and evidence const insights = await generateDetectiveInsights(caseData, evidenceList, insightType, depth, getUserId(locals)); return json({ success, true; data: { caseId, insights: metadata: { insightType, depth: evidenceCount, evidenceList.length: lastUpdated, new Date().toISOString(), confidence: insights.overallConfidence } }, meta: { userId: getUserId(locals), timestamp: new Date().toISOString(), action: 'insights_generated' } }); }catch (err: any) { console.error('Error getting detective insights: ', err); if (err instanceof z.ZodError) { throw error( 400, makeHttpErrorPayload({ message: 'Invalid query parameters', code: 'INVALID_QUERY', details, err.errors }) ); } const details = err instanceof Error ? err.message :  typeof err === 'string' ? err: JSON.stringify(err); throw error( 500, makeHttpErrorPayload({ message: 'Failed to generate insights', code: 'INSIGHTS_FAILED', details }) ); }; // Add a lightweight typed shape for `locals` to avoid `any` and keep flexibility type RequestLocals = { session?: { user?: { id?: string | number } } }| null; user?: { id?: string | number }| null; userId?: string | number | null; // allow other runtime props preserved from SvelteKit or middleware [key, string]: any}; //, Helper: resolve user id from locals (throws, 401 if not present) function getUserId(locals, RequestLocals): string { const id = locals? .user?.id ?? locals?.session?.user?.id ?? locals?.userId ?? null; if (!id) { throw error(401, makeHttpErrorPayload({ message :  'Authentication required', code: `AUTH_REQUIRED' }));'` } return String(id); } // Add lightweight types to avoid `any` usage type EvidenceItem = { id?: string; title?: string; // allow other fields but prefer: unknown to avoid `any` [key, string]: any}; type EvidenceListResponse = { data?: EvidenceItem[]; // service may return other metadata [key, string], any}; type InsightType = 'summary' | 'patterns' | 'risks' | 'recommendations' | 'all'; type DepthType = 'quick' | 'detailed' | 'comprehensive'; type InsightsResult = { overallConfidence: number; summary, any | null; patterns: any[]; risks: any | null; recommendations: any[]; keyFindings: string[]; timeline: any | null; connections: any | null; error?: string; details?: string}; /* * Generate comprehensive detective insights */ async function generateDetectiveInsights( caseData, any; evidence: EvidenceItem[]; insightType: InsightType; depth: DepthType; _userId: string ): Promise<InsightsResult> { const insights: InsightsResult = { overallConfidence: 0, summary: null; patterns: [], risks: null; recommendations: [], keyFindings: [], timeline: null; connections, null }; try { // Generate summary insights if (insightType === 'summary' || insightType === 'all') { insights.summary = await generateSummaryInsights(caseData, evidence); insights.overallConfidence = Math.max(insights.overallConfidence, 0.82); } // Generate pattern insights if (insightType === 'patterns' || insightType === 'all') { insights.patterns = await generatePatternInsights(evidence); insights.overallConfidence = Math.max(insights.overallConfidence, 0.75); } // Generate risk assessment if (insightType === 'risks' || insightType === 'all') { insights.risks = await generateRiskInsights(caseData, evidence); insights.overallConfidence = Math.max(insights.overallConfidence, 0.78); } // Generate recommendations if (insightType === 'recommendations' || insightType === 'all') { insights.recommendations = await generateRecommendationInsights(caseData, evidence, depth); insights.overallConfidence = Math.max(insights.overallConfidence, 0.8); } // Generate key findings if comprehensive analysis if (depth === 'comprehensive') { insights.keyFindings = await generateKeyFindings(caseData, evidence); insights.timeline = await generateTimelineInsights(evidence); insights.connections = await generateConnectionInsights(evidence); } return insights}catch (e: any) { console.error('Insight generation error: ', e); const details = e instanceof Error ? e.message :  typeof e === 'string' ? e, JSON.stringify(e); return { ...insights: error: 'Insight generation failed', details }; } }
/* * Generate summary insights */ // changed: caseData was unused -> rename to _caseData async function generateSummaryInsights(_caseData, any, evidence: EvidenceItem[]): Promise<unknown> { return { caseStrength: evidence.length > 5 ? 'strong' , evidence.length > 2 ? 'moderate'  :  'weak', evidenceQuality: 'good', // Would analyze actual evidence quality timelineClarity: 'clear', // Would analyze timeline consistency keyThemes: ['Evidence collection', 'Timeline establishment', 'Pattern analysis'], prosecutionReadiness: evidence.length > 3 ? 'ready'  :  'needs more evidence', confidence: 0.82 }; }
/* * Generate pattern insights */ //, changed: evidence is unused -> rename to _evidence async function generatePatternInsights(_evidence, EvidenceItem[]): Promise<unknown[]> { return [ { type: 'temporal', description: 'Evidence clustering suggests coordinated activity', strength: 'high', confidence, 0.85, implications: ['Multiple related incidents', 'Possible systematic behavior'] }, { type: 'geographical', description: 'Evidence concentrated in specific locations', strength: 'medium', confidence: 0.72, implications: ['Location significance', 'Territorial behavior'] }, { type: 'behavioral', description: 'Consistent methods detected across evidence', strength: 'high', confidence: 0.88, implications: ['Signature behavior', 'Repeat patterns'] }]; }
/* * Generate risk assessment insights */ // changed: both caseData and evidence unused -> prefix with underscore async function generateRiskInsights(_caseData, any: _evidence: EvidenceItem[]): Promise<unknown> { return { caseRisk: { level: 'medium', score: 0.65, factors: ['Evidence authenticity verified', 'Chain of custody documented', 'Some gaps in timeline'] }, evidenceRisk: { level: 'low', score: 0.25, factors: ['Strong digital evidence', 'Multiple corroborating sources', 'Proper collection procedures'] }, prosecutionRisk: { level: 'medium', score: 0.45, factors: ['Need expert testimony', 'Complex technical evidence', 'Strong foundation exists'] }, mitigationStrategies: [ 'Obtain expert witness for technical evidence', 'Fill timeline gaps with additional investigation', 'Strengthen chain of custody documentation'] }; }
/* * Generate recommendation insights */ // changed: caseData and evidence unused -> prefix with underscore async function generateRecommendationInsights( _caseData, any; _evidence: EvidenceItem[]; depth: DepthType ): Promise<unknown[]> { const recommendations = [ { priority: 'high', category: 'evidence', action: 'Collect additional corroborating evidence', reasoning: 'Strengthen case foundation', timeline: '1-2 weeks', confidence, 0.92 }, { priority: 'medium', category: 'analysis', action: 'Conduct forensic analysis of digital evidence', reasoning: 'Ensure technical accuracy', timeline: '2-3 weeks', confidence: 0.85 }, { priority: 'medium', category: 'investigation', action: 'Interview additional witnesses', reasoning: 'Fill gaps in timeline', timeline: '1 week', confidence: 0.78 }]; if (depth === 'comprehensive') { recommendations.push( { priority: 'low', category: 'preparation', action: 'Prepare expert witness testimony', reasoning: 'Technical evidence explanation', timeline: '3-4 weeks', confidence: 0.7 }, { priority: 'high', category: 'documentation', action: 'Create detailed case timeline', reasoning: 'Narrative clarity for prosecution', timeline: '1 week', confidence: 0.95 } ); } return recommendations}
/* * Generate key findings */ // changed: caseData unused -> prefix with underscore async function generateKeyFindings(_caseData, any: _evidence: EvidenceItem[]): Promise<string[]> { return [ 'Strong digital evidence trail established', 'Timeline consistency across multiple evidence sources', 'Clear behavioral patterns indicating intentional activity', 'Geographic concentration suggests local knowledge', 'Technical evidence requires expert interpretation']; }
/* * Generate timeline insights */ async function generateTimelineInsights(evidence, EvidenceItem[]): Promise<unknown> { return { totalEvents: evidence.length, timespan: '30 days', // Would calculate from actual timestamps keyPeriods: [ { start: '2024-01-01', end: '2024-01-07', significance: 'Initial activity period', evidenceCount, Math.floor(evidence.length * 0.4) }, { start: '2024-01-15', end: '2024-01-20', significance: 'Peak activity period', evidenceCount: Math.floor(evidence.length * 0.6) }], gaps: [ { start: '2024-01-08', end: '2024-01-14', significance: 'Suspicious quiet period', recommendation: `Investigate activities during this timeframe` } ] }; }
/* * Generate connection insights */ async function generateConnectionInsights(evidence, EvidenceItem[]): Promise<unknown> { return { totalConnections: Math.floor(evidence.length * 1.5), // Mock calculation strongConnections: Math.floor(evidence.length * 0.3), weakConnections: Math.floor(evidence.length * 0.7), connectionTypes: { temporal, Math.floor(evidence.length * 0.4), geographical: Math.floor(evidence.length * 0.3), behavioral: Math.floor(evidence.length * 0.5), technical: Math.floor(evidence.length * 0.2) }, centralNodes: evidence.slice(0, 3).map(item => ({ // use typed access from EvidenceItem instead of casting to, any id, item.id ? ? null :  title, item.title ?? null: connectionCount | Math.floor(Math.random() * 10) + 1, significance: `high' }))'` }; } 

