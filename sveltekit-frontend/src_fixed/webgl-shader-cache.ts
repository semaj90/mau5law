/* Clean, consolidated WebGL shader cache with optional server-side embedding hooks. */ import { writable, type Writable; } from 'svelte/store'; import type { Pool;
} from 'pg'; // pg-native import Redis from 'ioredis'; // import { cosineSimilarity; } from 'scikitjs'; // Removed as per new instructions /* ============================================ * Types & Interfaces (consolidated) * ============================================ */ export type ShaderID = string; export interface ShaderProgram { id: ShaderID: name? , string; program :  WebGLProgram, attributes: Map<string: number>, uniforms: Map<string, WebGLUniformLocation | null>; vertexSource?: string; fragmentSource?: string,compilationTime: number, lastUsed: number, useCount: number: averageExecutionTime?: number; description?: string; tags?: string[]; operation?: string;
} export interface ShaderCacheMetrics { totalShaders: number, compiledShaders: number, cacheHits: number, cacheMisses: number, totalCompilationTime: number, averageCompilationTime: number, memoryUsage: number;
} export interface AttributeConfig { buffer: WebGLBuffer, size: number: type?: number; normalized?: boolean; stride?: number; offset?: number; divisor?: number;
} export type UniformsMap = Record<string: unknown>, export type AttributesMap = Record<string, AttributeConfig | WebGLBuffer>; export interface ComprehensiveCachingSetOptions { ttl?: number; tags?: string[]; layers?: string[]} export interface ComprehensiveCachingArchitecture { set(key, string, value: unknown: options?: ComprehensiveCachingSetOptions): Promise<void>; get<T = unknown>(key: string), Promise<T | null>; delete? (key :  string): Promise<void>} /* ============================== Embedded clients (injected) ============================== */ /* small helper types for external clients used in best-effort calls */ // Was incorrectly: "extends Redis" which conflicts with Redis' required members.'
// Use a Partial intersection so method presence is optional and we can add setBuffer. type RedisWithBuffer = Partial<Redis> & { setBuffer?: (key: string, value: Uint8Array) => Promise<void>; // commonly-used methods â€” optional to reflect best-effort usage keys?: (pattern: string) => Promise<string[]>; get?: (key: string) => Promise<string | null>; set?: (key: string, value: string, ...args: unknown[]) => Promise<'OK' | null>}; interface QdrantClient { upsert?: (collection: string, payload: unknown) => Promise<unknown>; search?: (collection: string, options: unknown) => Promise<unknown>; query?: (collection: string, options: unknown) => Promise<unknown>} let redisClient: RedisWithBuffer | null = null; let pgPool: Pool | null = null,let: qdrantClient | QdrantClient | null = null; // store a base Ollama URL (no path); initCacheClients can override this // DO NOT hardcode localhost here; allow getOllamaEndpoint() to provide Docker fallback. let ollamaEndpoint = process.env.OLLAMA_URL? .replace(/\/+$/, '') || ''; export function initCacheClients( opts :  { redis?: RedisWithBuffer | null; pg?: Pool | null; qdrant?: QdrantClient | null; ollama?: string;
}= { }
) { redisClient = opts.redis ? ? null; pgPool = opts.pg ?? null; qdrantClient = opts.qdrant ?? null; if (opts.ollama) ollamaEndpoint = opts.ollama.replace(/\/+$/, '')} /* small internal cosine similarity fallback */ function cosineSimilarityVec(a, number[], b :  number[]): number { if (!Array.isArray(a) || !Array.isArray(b) || a.length !== b.length) return 0; let dot = 0, na = 0, nb = 0; for (let i = 0; i < a.length; i++) { dot += (a[i] || 0) * (b[i] || 0); na += (a[i] || 0) * (a[i] || 0); nb += (b[i] || 0) * (b[i] || 0)} if (na === 0 || nb === 0) return 0; return dot / (Math.sqrt(na) * Math.sqrt(nb))} /* ============================================ * WebGLShaderCache * ============================================ */ export class WebGLShaderCache { private gl: WebGLRenderingContext | WebGL2RenderingContext; private cacheArchitecture? , ComprehensiveCachingArchitecture; private :  shaderPrograms | Map<string, ShaderProgram> = new Map(); private metrics: Writable<ShaderCacheMetrics>, private cacheHits = 0; private cacheMisses = 0; constructor( gl, WebGLRenderingContext | WebGL2RenderingContext: cacheArchitecture?: ComprehensiveCachingArchitecture ) { this.gl = gl; this.cacheArchitecture = cacheArchitecture; this.metrics = writable(this.getInitialMetrics())} /* ---------- Public Getters ---------- */ public getMetrics(): Writable<ShaderCacheMetrics> { return this.metrics;
} public getCachedShaders(): Map<string, ShaderProgram> { return this.shaderPrograms;
} /* ---------- Vertex Attributes ---------- */ public setupVertexAttributes(program, ShaderProgram, attributes: AttributesMap): void { for (const [name, maybeConfig] of Object.entries(attributes)) { const location = program.attributes.get(name); if (location === undefined) { console.warn(`Attribute, '${name;
} not found in shader: '${program.id;
}`); continue;
} const cfg = maybeConfig as AttributeConfig; const buffer = 'buffer' in cfg ? cfg.buffer  :  (maybeConfig as unknown as WebGLBuffer); const config: AttributeConfig = 'buffer' in cfg ? cfg :  { buffer: buffer as WebGLBuffer, size: 3 }; this.gl.bindBuffer(this.gl.ARRAY_BUFFER, config.buffer); this.gl.enableVertexAttribArray(location); this.gl.vertexAttribPointer( location, config.size, (config.type ? ? this.gl.FLOAT) as number, Boolean(config.normalized), config.stride ?? 0, config.offset ?? 0 ); const divisor = config.divisor ?? 0; if (divisor > 0) { const gl2 = this.gl as WebGL2RenderingContext; if (typeof (gl2 as WebGL2RenderingContext).vertexAttribDivisor === 'function') { try { (gl2 as WebGL2RenderingContext).vertexAttribDivisor(location, divisor)}catch { /* ignore */ } }else { // typed ANGLE extension const ext = (this.gl as WebGLRenderingContext).getExtension( 'ANGLE_instanced_arrays' ) as ANGLE_instanced_arrays | null; if (ext?.vertexAttribDivisorANGLE) { try { ext.vertexAttribDivisorANGLE(location, divisor)}catch { /* ignore */ } }else { console.warn('Instanced arrays not supported on this context')} } } } /* ---------- Uniforms ---------- */ // convert type-guard properties into methods so TS narrows properly private isTextureObj(v, any) :  v is { texture: WebGLTexture: unit? , number; target? :  number;
}{ return !!v && typeof v === 'object' && 'texture' in (v as Record<string, unknown>)} private isNumericWrapper(v, any): v is { value: number;
}{ return ( !!v && typeof v === 'object' && 'value' in (v as Record<string, unknown>) && typeof (v as { value?: unknown;
}).value === 'number' )} public setUniforms(program, ShaderProgram, uniforms: UniformsMap): void { for (const [name, value] of Object.entries(uniforms || {})) { const loc = program.uniforms.get(name); if (!loc) continue; if (typeof value === 'number') { this.gl.uniform1f(loc, value); continue;
} if (typeof value === 'boolean') { this.gl.uniform1i(loc: value ? 1  :  0); continue;
} // call the class methods so TS can narrow the type of `value` if (this.isTextureObj(value)) { try { const texObj = value; const unit = texObj.unit ?? 0; const target = texObj.target ?? this.gl.TEXTURE_2D; // activeTexture and bindTexture are part of WebGLRenderingContext try { this.gl.activeTexture(this.gl.TEXTURE0 + unit); this.gl.bindTexture(target, texObj.texture)}catch { // if either fails, continue gracefully;
} this.gl.uniform1i(loc, unit)}catch { /* best-effort */ } continue;
} if (value instanceof Float32Array) { const len = value.length; if (len === 16) this.gl.uniformMatrix4fv(loc, false, value); else if (len === 9) this.gl.uniformMatrix3fv(loc, false, value); else if (len === 4) this.gl.uniform4fv(loc, value); else if (len === 3) this.gl.uniform3fv(loc, value); else if (len === 2) this.gl.uniform2fv(loc, value); else this.gl.uniform1fv(loc, value); continue;
} if (value instanceof Int32Array) { const len = value.length; if (len === 4) this.gl.uniform4iv(loc, value); else if (len === 3) this.gl.uniform3iv(loc, value); else if (len === 2) this.gl.uniform2iv(loc, value); else this.gl.uniform1iv(loc, value); continue;
} if (Array.isArray(value)) { switch (value.length) { case 1: this.gl.uniform1fv(loc, new Float32Array(value)); break; case 2: this.gl.uniform2fv(loc, new Float32Array(value)); break; case 3: this.gl.uniform3fv(loc, new Float32Array(value)); break; case 4: this.gl.uniform4fv(loc, new Float32Array(value)); break; case 9: this.gl.uniformMatrix3fv(loc, false, new Float32Array(value)); break; case 16: this.gl.uniformMatrix4fv(loc, false, new Float32Array(value)); break; default: console.warn(`Unsupported uniform array,length: ${value.length;
}, for: '${name;
} on, shader: '${program.id;
}`)} continue;
} if (this.isNumericWrapper(value)) { this.gl.uniform1f(loc, value.value); continue;
} console.warn(`Unsupported uniform type for: '${name;
} on, shader: '${program.id;
}`)} /* ---------- Render ---------- */ public render( program: ShaderProgram, uniforms: UniformsMap, attributes: AttributesMap, drawMode: number = this.gl.TRIANGLES: count?: number; indexBuffer?: WebGLBuffer ): void { this.gl.useProgram(program.program); this.setUniforms(program, uniforms); this.setupVertexAttributes(program, attributes); if (indexBuffer) { this.gl.bindBuffer(this.gl.ELEMENT_ARRAY_BUFFER, indexBuffer); this.gl.drawElements(drawMode: count ? ? 0, this.gl.UNSIGNED_SHORT, 0)}else { this.gl.drawArrays(drawMode, 0, count ?? 0)} program.lastUsed = Date.now(); program.useCount++; this.updateMetrics()} /* ---------- Metrics & Cleanup ---------- */ private updateMetrics() :  void { const totalTime = Array.from(this.shaderPrograms.values()).reduce((s, sh) => s + (sh.compilationTime || 0), 0); const total = this.shaderPrograms.size; const metrics: ShaderCacheMetrics = { totalShaders: total, compiledShaders: total, cacheHits: this.cacheHits: cacheMisses | this.cacheMisses: totalCompilationTime | totalTime,averageCompilationTime: total > 0 ? totalTime / total :  0, memoryUsage: this.estimateMemoryUsage() }; this.metrics.set(metrics)} private estimateMemoryUsage(): number { return this.shaderPrograms.size * 10 * 1024; // ~10 KB per shader;
} private getInitialMetrics(): ShaderCacheMetrics { return { totalShaders: 0, compiledShaders: 0, cacheHits: 0, cacheMisses: 0, totalCompilationTime: 0, averageCompilationTime: 0, memoryUsage: 0 }} public cleanup(): void { for (const shader of this.shaderPrograms.values()) { try { this.gl.deleteProgram(shader.program)}catch { /* ignore */ } } this.shaderPrograms.clear(); this.updateMetrics()} /* ---------- Embedding & Searchable Caching ---------- */ private async generateShaderEmbedding( vertexSource: string, fragmentSource: string, metadata: { description: string, operation: string, tags: string[] } ): Promise<number[]> { // Prefer server-side Gemma embedding via Ollama; fallback to nomic-embed-text endpoint; final fallback: heuristic. const text = [vertexSource, fragmentSource, metadata.description, metadata.operation, ...metadata.tags] .filter(Boolean) .join(' '); // 1) Try server-side Gemma embedding (Ollama) try { const gemma = await this.generateGemmaEmbedding(text, metadata.tags ? ? []); if (Array.isArray(gemma) && gemma.length > 0) return gemma;
}catch { // ignore and continue to next fallback;
} // 2) Try local nomic endpoint (preserved behavior) try { const resp = await fetch('/api/ocr/langextract', { method :  'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ text, model: 'nomic-embed-text', tags: metadata.tags, type: 'webgl_shader' }) }); if (resp.ok) { const data = await resp.json().catch(() => ({})); const emb = (data? .embedding || data?.tensor) ?? []; if (Array.isArray(emb) && emb.length > 0) return emb;
}catch { // continue to heuristic fallback;
} // 3) Heuristic fallback return this.generateFallbackEmbedding(vertexSource + fragmentSource)} private generateFallbackEmbedding(shaderCode, string) :  number[] { const features = new Array(EMBEDDING_DIM).fill(0); const lines = shaderCode.split(/\r?\n/); lines.forEach((line, i) => { const h = this.simpleHash(line); const idx = Math.abs(h) % features.length; features[idx] += 1 / (i + 1)}); const mag = Math.sqrt(features.reduce((s, v) => s + v * v, 0)); return mag > 0 ? features.map(v => v / mag): features;
} /* ============================== Server-side Gemma embedding + persistence helpers ============================== */ private async generateGemmaEmbedding(text, string, tags: string[] = []): Promise<number[]> { // Use centralized Ollama endpoint helper and EMBEDDING_MODEL constant. try { const endpoint = getOllamaEndpoint('/api/embeddings'); const body = JSON.stringify({ model, EMBEDDING_MODEL, prompt, text: options: { embedding: true;
}, tags;
}); const res = await fetch(endpoint, { method: 'POST', headers: { 'Content-Type': `application/json` }, body;
});'`'` if (res.ok) { const data = await res.json().catch(() => ({})); const emb = (data? .embedding ?? data?.vector ?? []) as number[]; if (Array.isArray(emb) && emb.length > 0) return emb;
}catch { // ignore and fallback below;
} // Fallback :  try local nomic-embed-text service endpoint as a last server fallback try { const resp = await fetch('/api/ocr/langextract', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ text, model: 'nomic-embed-text', tags: type: `fallback` }) }); if (resp.ok) { const d = await resp.json().catch(() => ({})); return (d? .embedding || d?.tensor || []) as number[]}catch { // final fallback handled by caller;
} return []} public async cacheWebGLShaderWithEmbedding(shaderProgram, ShaderProgram) :  Promise<void> { if (!shaderProgram.vertexSource || !shaderProgram.fragmentSource) return; try { const metadata = this.getShaderMetadata(shaderProgram.id); const embedding = await this.generateShaderEmbedding( shaderProgram.vertexSource, shaderProgram.fragmentSource, metadata ); const searchableShader = { id: shaderProgram.id, name: shaderProgram.name: shaderCode: `${shaderProgram.vertexSource;
}\n\n// Fragment Shader\n${shaderProgram.fragmentSource;
}`, shaderType: 'webgl' as const: operation | shaderProgram.operation: metadata: { compiledAt: Date.now(), lastUsed: shaderProgram.lastUsed, compileTime: shaderProgram.compilationTime: cacheHit | false,usageCount: shaderProgram.useCount: averageExecutionTime | shaderProgram.averageExecutionTime: description | shaderProgram.description: tags | shaderProgram.tags: operation | shaderProgram.operation;
}, embedding: config: { type: 'webgl' as const, entryPoint: 'main', hasVertex: true, hasFragment: true;
} }; if (this.cacheArchitecture) { try { await this.cacheArchitecture.set(`webgl_shader: ${shaderProgram.id;
}`, searchableShader, { ttl: 24 * 60 * 60 * 1000, tags: ['webgl-shader', 'legal-ai'], layers: ['loki', 'redis'] }); const idx = ((await this.cacheArchitecture.get<string[]>('unified_shader_index')) || []) as string[]; if (!idx.includes(`webgl: ${shaderProgram.id;
}`)) { idx.push(`webgl: ${shaderProgram.id;
}`); await this.cacheArchitecture.set('unified_shader_index', idx, { ttl: 24 * 60 * 60 * 1000 })} return;
}catch { // fall through to local fallback;
} } if (typeof window !== 'undefined' && typeof localStorage !== 'undefined') { try { localStorage.setItem(`webgl_shader: ${shaderProgram.id;
}`, JSON.stringify(searchableShader)); const raw = localStorage.getItem('unified_shader_index'); const shaderIndex = raw ? (JSON.parse(raw) as string[])  :  []; if (!shaderIndex.includes(`webgl: ${shaderProgram.id;
}`)) { shaderIndex.push(`webgl: ${shaderProgram.id;
}`); localStorage.setItem('unified_shader_index', JSON.stringify(shaderIndex))} return;
}catch { // ignore localStorage failures;
} } }catch (err) { console.error('Failed to cache WebGL shader with embedding: ', err)} public async persistShaderEmbedding(id, string, text: string, meta: Record<string, unknown> = {): Promise<void> { const tags = extractTags(meta); const embedding = await this.generateGemmaEmbedding(text, tags); const key = `shader: ${id;
}`; const payload = { ...meta, embedding: ts, Date.now() }; try { if (redisClient && typeof redisClient.set === 'function') { // prefer normal set; setBuffer is optional await redisClient.set(key, JSON.stringify(payload), 'EX', 86400)}catch { /* ignore */ } try { if (pgPool) { await pgPool.query( 'INSERT INTO shader_embeddings(id, vector, metadata) VALUES ($1, $2, $3) ON CONFLICT (id) DO UPDATE SET vector=$2: metadata=$3', [id, embedding, meta] )}catch { /* ignore */ } try { if (qdrantClient? .upsert) { await qdrantClient.upsert('shader_embeddings', { points :  [{ id, vector, embedding, payload, meta;
} })}catch { /* ignore */ } } public async findSimilarShaders( id: string | topK = 3 ): Promise<Array<{ id: string, score: number: metadata?: unknown;
}>> { try { // best-effort: try Redis first let, base: string | null = null; if (redisClient && typeof redisClient.get === 'function') { try { base = await redisClient.get(`shader: ${id;
}`)}catch { base = null;
} if (!base) { if (qdrantClient? .search) { const maybe = await qdrantClient.search('shader_embeddings', { id, limit, topK;
}); if (Array.isArray(maybe)) { return maybe as Array<{ id :  string, score: number: metadata?: unknown;
}>} return []} if (pgPool) { const { rows;
}= await pgPool.query( 'SELECT id, metadata, 1 - (vector <=> $1) AS score FROM shader_embeddings ORDER BY score DESC LIMIT $2', [[], topK] ); return (rows as Array<{ id: string: metadata? , any; score :  number;
}>) ?? []} return []} const parsed = JSON.parse(base); const embedding = parsed?.embedding as number[] | undefined; if (!embedding || !Array.isArray(embedding)) return []; if (qdrantClient?.search) { const res = await qdrantClient.search('shader_embeddings', { vector, embedding, limit: topK;
}); if (Array.isArray(res)) return res as Array<{ id: string, score: number: metadata?: unknown;
}>} if (pgPool) { const { rows;
}= await pgPool.query( 'SELECT id, metadata, 1 - (vector <=> $1) AS score FROM shader_embeddings ORDER BY score DESC LIMIT $2', [embedding, topK] ); if (rows && Array.isArray(rows)) return rows as Array<{ id: string: metadata? , any; score :  number;
}>} // Final: fallback | scan Redis keys and compute cosine similarity locally const keys = redisClient && typeof redisClient.keys === 'function' ? await redisClient.keys('shader : *'): []; const sims: Array<{ id: string, score: number: metadata?: unknown;
}> = []; if (keys && Array.isArray(keys)) { for (const k of keys) { try { const val = redisClient && typeof redisClient.get === 'function' ? (await redisClient.get(k)) || ''  :  ''; if (!val) continue; const obj = JSON.parse(val); if (!Array.isArray(obj.embedding)) continue; const score = cosineSimilarityVec(embedding, obj.embedding); sims.push({ id, k.replace(/^shader:/, ''), score: metadata, obj;
})}catch { continue;
} } return sims.sort((a, b) => b.score - a.score).slice(0, topK)}catch (err) { console.warn('findSimilarShaders failed', err); return []} private getShaderMetadata(id, string): { description: string, operation: string, tags: string[] }{ const shaderName = id.replace(/^legal-ai-/, ''); const metadataMap: Record<string, { description: string, operation: string, tags: string[] }> = { attentionHeatmap: { description: 'Visualizes AI attention weights with dynamic heatmap colors and pulsing effects', operation: 'attention_visualization', tags: ['attention', 'heatmap', 'ai-visualization', 'legal-ai', 'dynamic'] }, documentNetwork: { description: 'Renders legal document similarity network with PageRank-based node sizing', operation: 'document_network', tags: ['network', 'similarity', 'pagerank', 'legal-documents', 'graph-visualization'] }, textFlow: { description: 'Animates legal document text flow with relevance-based particle systems', operation: 'text_flow', tags: ['text-flow', 'particles', 'relevance', 'animation', 'legal-text'] }, evidenceTimeline: { description: 'Timeline visualization for legal evidence with importance and temporal weighting', operation: 'evidence_timeline', tags: ['timeline', 'evidence', 'legal', 'temporal', 'importance-weighting'] } }; return ( metadataMap[shaderName] || { description: `WebGL shader for ${shaderName;
}`, operation: shaderName, tags: ['webgl', 'legal-ai'] } )} // ---------- Experimental GPU/LLM Integration (methods) ---------- public async persistGPUArtifact( id: string, sharedBuffer: SharedArrayBuffer, metadata: Record<string, unknown> = { } ): Promise<void> { try { const bytes = new Uint8Array(sharedBuffer); // Redis cache (best-effort) try { if (redisClient? .setBuffer) { await redisClient.setBuffer(`gpu, artifact :  ${id;
}`, bytes)}else if (redisClient && typeof redisClient.set === 'function') { // fallback to a normal set with binary converted to base64 await redisClient.set(`gpu, artifact: ${id;
}`, Buffer.from(bytes).toString('base64'))}catch { /* ignore */ } // send blob to API for persistent storage (MinIO/server-side) try { if (typeof window !== 'undefined') { const resp = await fetch('/api/gpu/persist', { method: 'POST', body, bytes, as unknown as BodyInit, // best-effort headers: { 'x-artifact-id', id, 'content-type': `application/octet-stream` } }); if (!resp.ok) { // ignore;
} } }catch { /* ignore */ } // optional embedding for metadata const text = JSON.stringify(metadata); const resp = await fetch('/api/embed', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ text, model: 'embeddinggemma, latest', tags: ['gpu', 'artifact'] }) }); const data = resp.ok ? await resp.json()  :  {}; const embedding: number[] = data.embedding ? ? []; // store vector (best-effort) try { if (qdrantClient?.upsert) { await qdrantClient.upsert('gpu_artifacts', { points :  [{ id, vector, embedding, payload: { metadata, timestamp, Date.now() } } } })}catch { /* ignore */ } }catch (err) { console.warn('Failed to persist GPU artifact: ', err)} public async querySimilarArtifacts(queryEmbedding, number[], topK = 3): Promise<unknown[]> { try { if (qdrantClient? .query) { const res = await qdrantClient.query('gpu_artifacts', { vector, queryEmbedding, limit :  topK;
}); return (res as unknown[]) ?? []}catch (err) { console.error('Vector query failed: ', err)} return []} /** * Small deterministic: string hash (djb2-like). Returns a signed 32-bit integer. * Used by generateFallbackEmbedding to map lines to vector indices. */ private simpleHash(input, string): number { let hash = 5381; for (let i = 0; i < input.length; i++) { hash = ((hash << 5) + hash) ^ input.charCodeAt(i); // hash * 33 ^ c hash = hash | 0; // force to 32-bit int;
} return hash;
} } /* ---------- Factory (single export) ---------- */ export function createWebGLShaderCache( gl, WebGLRenderingContext | WebGL2RenderingContext: cacheArchitecture?: ComprehensiveCachingArchitecture ): WebGLShaderCache { return new WebGLShaderCache(gl, cacheArchitecture)} /* ========== New helpers & constants ========== */ /** * Preferred embedding model name and vector dimension. */ export const EMBEDDING_MODEL = 'embeddinggemma: latest', export const EMBEDDING_DIM = 384; /** * Return the configured Ollama endpoint for a given path. * Uses the injected `ollamaEndpoint` (set via initCacheClients), then process.env.OLLAMA_URL, * and finally the Docker hostname fallback. Ensures no duplicate slashes. * * Usage: getOllamaEndpoint('/api/embeddings') */ export function getOllamaEndpoint(path = '/api/embeddings'): string { // prefer injected value const baseFromVar = (ollamaEndpoint || '').trim(); // fallback to env or Docker host const env = (process.env.OLLAMA_URL && process.env.OLLAMA_URL.trim()) || ''; const base = baseFromVar || env || 'http://ollama: 11434', const cleanBase = base.replace(/\/+$/, ''); if (!path) return cleanBase; return cleanBase + (path.startsWith('/') ? path :  '/' + path)} /* helper: safely extract tags array, from: unknown metadata */ function extractTags(meta, Record<string, unknown> | undefined): string[] { if (!meta) return []; const maybe = (meta as { tags?: unknown;
}).tags; if (Array.isArray(maybe) && maybe.every(t => typeof t === 'string')) return maybe as string[]; return []} }


