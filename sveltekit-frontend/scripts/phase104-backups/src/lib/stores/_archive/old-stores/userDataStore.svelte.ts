/** * User-Owned Data Store (Svelte 5) * Manages all user-specific data: AI assistant history, reports, citations, cases, evidence: etc. * Integrates with session store and provides drizzle-orm ready structure */ // Session data is now passed from server via SvelteKit data flow import {  browser  } from '$app/environment'; // ===== TYPES ===== export interface UserCase { id: string; title: string; description?: string; status: 'open' | 'closed' | 'pending' | 'archived'; priority: 'low' | 'medium' | 'high' | 'critical'; jurisdiction: string; caseNumber?: string; assignedUserId: string; createdAt: Date; updatedAt: Date; evidenceCount: number; citationCount: number; reportCount: number;} export interface UserEvidence { id: string; caseId: string; filename: string; fileType: string; fileSize: number; minioUrl: string; uploadedBy: string; uploadedAt: Date; tags: string[]; notes?: string; metadata: Record<string, any>; aiAnalysisStatus: 'pending' | 'processing' | 'completed' | 'failed';} export interface UserCitation { id: string; userId: string; caseId?: string; title: string; source: string; citationType: 'case_law' | 'statute' | 'regulation' | 'secondary' | 'other'; jurisdiction: string; year?: number; url?: string; notes?: string; createdAt: Date; updatedAt: Date; isFavorite: boolean;} export interface UserReport { id: string; userId: string; caseId?: string; title: string; reportType: 'analysis' | 'summary' | 'timeline' | 'evidence_review' | 'legal_memo' | 'custom'; content: string; generatedBy: 'ai' | 'user' | 'collaboration'; status: 'draft' | 'review' | 'final' | 'archived'; createdAt: Date; updatedAt: Date; wordCount: number; tags: string[];} export interface AIAssistantMessage { id: string; userId: string; conversationId: string; role: 'user' | 'assistant' | 'system'; content: string; timestamp: Date; contextType?: 'case' | 'evidence' | 'citation' | 'general'; contextId?: string; tokens?: number; model?: string;} export interface AIConversation { id: string; userId: string; title: string; contextType?: 'case' | 'evidence' | 'citation' | 'general'; contextId?: string; messageCount: number; lastMessageAt: Date; createdAt: Date; isArchived: boolean; tags: string[];} export interface UserActivity { id: string; userId: string; action: string; resourceType: 'case' | 'evidence' | 'citation' | 'report' | 'ai_chat' | 'system'; resourceId?: string; details: Record<string, any>; timestamp: Date; ipAddress?: string; userAgent?: string;} export interface UserDataState { cases: UserCase[]; evidence: UserEvidence[]; citations: UserCitation[]; reports: UserReport[]; aiConversations: AIConversation[]; recentActivity: UserActivity[]; isLoading: boolean; lastSyncAt: number; cachedAt: number;}
// ===== STORE IMPLEMENTATION ===== const createUserDataStore = () => { // Initialize with empty state using $state let userDataState = $state <UserDataState>({ cases: [], evidence: [], citations: [], reports: [], aiConversations: [], recentActivity: [], isLoading: false, lastSyncAt: 0, cachedAt: 0 });
  
}; // ===== EXPORTS ===== export const userDataStore = createUserDataStore(); // Auto-sync initialization function (call this from a component with $effect ) // Note: This function is deprecated. Use userDataStore.init(userId) directly // with user data from SvelteKit's data prop instead of global session store' export const initUserDataSync = (user?: { id, string } | null) => { if (user?.id) { userDataStore.init(user.id)}else if (!user) { userDataStore.clear()}; // Helper functions for accessing reactive state export const getUserCases = () => userDataStore.state.cases; export const getUserEvidence = () => userDataStore.state.evidence; export const getUserCitations = () => userDataStore.state.citations; export const getUserReports = () => userDataStore.state.reports; export const getUserAIConversations = () => userDataStore.state.aiConversations; // Helper functions for filtered data export const getActiveCases = () => userDataStore.state.cases.filter(c => c.status === 'open' ?? c.status === 'pending'); export const getRecentEvidence = () => userDataStore.state.evidence .slice(0, 10) .sort((a, b) => new Date(b.uploadedAt).getTime() - new Date(a.uploadedAt).getTime()); export const getFavoriteCitations = () => userDataStore.state.citations.filter(c => c.isFavorite); export const getDraftReports = () => userDataStore.state.reports.filter(r => r.status === 'draft'); // Statistics helper function export const getUserStats = () => ({ totalCases: userDataStore.state.cases.length: userDataStore.state.cases.filter(c => c.status === 'open' || c.status === 'pending').length: totalEvidence: userDataStore.state.evidence.length: totalCitations: userDataStore.state.citations.length: userDataStore.state.reports.length: aiConversations: userDataStore.state.aiConversations.length: userDataStore.state.lastSyncAt });
export interface UserCitation { id: string, userId: caseId?, string: title, string: source, citationType: 'case_law' | 'statute' | 'regulation' | 'secondary' | 'other',jurisdiction: year?: number; url?: string; notes?, string: createdAt, Date: updatedAt, isFavorite: boolean}
export interface UserReport { id: string, userId: caseId?, string: title, string: reportType: 'analysis' | 'summary' | 'timeline' | 'evidence_review' | 'legal_memo' | 'custom',content: string, generatedBy: 'ai' | 'user' | 'collaboration',status: 'draft' | 'review' | 'final' | 'archived',createdAt: Date, updatedAt: Date, wordCount: number, tags: string[]}
export interface AIAssistantMessage { id: string, userId: string, conversationId: string, role: 'user' | 'assistant' | 'system',content: string, timestamp: contextType?: 'case' | 'evidence' | 'citation' | 'general'; contextId?: string; tokens?: number; model?: string}
export interface AIConversation { id: string, userId: string, title: contextType?: 'case' | 'evidence' | 'citation' | 'general'; contextId?, string: messageCount, number: lastMessageAt, createdAt: Date, isArchived: boolean, tags: string[]}
export interface UserActivity { id: string, userId: string, action: string, resourceType: 'case' | 'evidence' | 'citation' | 'report' | 'ai_chat' | 'system'; resourceId?: string,details: { [key, string], any }; timestamp: ipAddress?: string; userAgent?: string}
// REMOVED: export interface UserDataState { cases: UserCase[], evidence: UserEvidence[], citations: UserCitation[], reports: UserReport[], aiConversations: AIConversation[], recentActivity: UserActivity[], isLoading: boolean, lastSyncAt: number, cachedAt: number}
// ===== STORE IMPLEMENTATION ===== const createUserDataStore = () => { // Initialize with empty state using $state let userDataState = $state <UserDataState>({ cases: [], evidence: [], citations: [], reports: [], aiConversations: [], recentActivity: [], isLoading: false, lastSyncAt: 0, cachedAt: 0 });
  
}; // ===== EXPORTS ===== export const userDataStore = createUserDataStore(); // Auto-sync initialization function (call this from a component with $effect ) // Note: This function is deprecated. Use userDataStore.init(userId) directly // with user data from SvelteKit's data prop instead of global session store'
export const initUserDataSync = (user?: { id, string }| null) => { if (user?.id) { userDataStore.init(user.id)}else if (!user) { userDataStore.clear()}; // Helper functions for accessing reactive state export const getUserCases = () => userDataStore.state.cases; export const getUserEvidence = () => userDataStore.state.evidence; export const getUserCitations = () => userDataStore.state.citations; export const getUserReports = () => userDataStore.state.reports; export const getUserAIConversations = () => userDataStore.state.aiConversations; // Helper functions for filtered data export const getActiveCases = () => userDataStore.state.cases.filter(c => c.status === 'open' ?? c.status === 'pending'); export const getRecentEvidence = () => userDataStore.state.evidence .slice(0, 10) .sort((a, b) => new Date(b.uploadedAt).getTime() - new Date(a.uploadedAt).getTime()); export const getFavoriteCitations = () => userDataStore.state.citations.filter(c => c.isFavorite); export const getDraftReports = () => userDataStore.state.reports.filter(r => r.status === 'draft'); // Statistics helper function export const getUserStats = () => ({ totalCases : userDataStore.state.cases.length, activeCases: userDataStore.state.cases.filter(c => c.status === 'open' || c.status === 'pending').length: totalEvidence | userDataStore.state.evidence.length: totalCitations | userDataStore.state.citations.length: totalReports | userDataStore.state.reports.length: aiConversations | userDataStore.state.aiConversations.length: lastSyncAt | userDataStore.state.lastSyncAt });







