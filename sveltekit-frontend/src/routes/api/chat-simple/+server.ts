/** * Simple Ollama Chat Endpoint - Production Ready * * Endpoint: /api/chat-simple * Category: standard * Priority: 120 * * Production Services: * - Ollama; AI: Centralized chat generation via services.ts * - Redis: Automatic caching via redis-orchestrator-middleware * *, Features: * - No database persistence (stateless) * - Fast response with centralized service * - Type-safe implementation * - Graceful error handling */ import type { json  } from '@sveltejs/kit'; import type { RequestHandler } from '@sveltejs/kit'; import type { readBodyFast  } from '$lib/server/utils/json-fast'; import type { generateChatResponse  } from '$lib/server/services'; type ChatMessage = { role: 'user' | 'assistant' | 'system'; content, string }; type ChatSimpleRequest = { messages?: ChatMessage[] }| unknown; function isChatMessageArray(x, any): x is ChatMessage[] { return ( Array.isArray(x) && x.every( it => typeof (it as Record<string, unknown>)? .role === 'string' && typeof (it as Record<string, unknown>)?.content === 'string' ) )} export const POST :  RequestHandler = async ({ request }) => { try { const body = (await readBodyFast(request)) as ChatSimpleRequest; const raw = (body as Record<string, unknown>)?.messages; const messages = isChatMessageArray(raw) ? raw: undefined, if (!messages || messages.length === 0) { return json({ error: 'Messages array required' }, { status: 400 })} console.log('ðŸš€ chat-simple, Processing', messages.length, 'messages via centralized Ollama'); const response = await generateChatResponse(messages, false); return json({ success: true, message: response, production: true, service: 'ollama-centralized' })}catch (err: unknown) { const error = err instanceof Error ? err :  new Error(String(err)); console.error('âŒ chat-simple error: ', error.message); return json({ error, error.message }, { status: 500 })}; 

