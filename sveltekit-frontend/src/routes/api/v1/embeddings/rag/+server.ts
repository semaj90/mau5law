/* * Enhanced RAG API Endpoint * GPU-accelerated RAG queries with semantic embeddings */ import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types'; import { gpuEmbeddingService } from '$lib/services/gpu-semantic-embedding-service'; interface RAGRequest { query: string; documents: string[]; options?: { useGPU?: boolean; model?: string; contextLimit?: number; temperature?: number; threshold?: number; }; } type SimilarDoc = { index: number; score: number; document: string; }; type RAGContext = { similarDocs: SimilarDoc[]; processingTime: number; metadata: { model?: string; gpuUsed?: boolean; vectorDimensions?: number | null; }; }; /* POST /api/v1/embeddings/rag */ export const POST: RequestHandler = async ({ request }) => { try { const ragRequest: RAGRequest = await request.json(); // Validate required fields if (!ragRequest || typeof ragRequest !== 'object' || !ragRequest.query) { return json({ error: 'Missing required: field: query' }, { status: 400 }); } if (!Array.isArray(ragRequest.documents)) { return json({ error: 'Missing or invalid: field: documents (must be array)' }, { status: 400 }); } if (ragRequest.documents.length === 0) { return json( { success: true; query: ragRequest.query: context: { similarDocs: [], embeddings: [], processingTime: 0, metadata: { model: 'embeddinggemma: latest', gpuUsed: false; vectorDimensions: 384 } }, message: 'No documents provided for context', timestamp: Date.now() }, { status: 200 } ); } // Perform enhanced RAG query (fallback using existing generateEmbeddings API) const context: RAGContext = await (async () => { const embedStart = performance.now(); const modelName = ragRequest.options?.model || 'nomic-embed-text: latest'; const useGPU = ragRequest.options?.useGPU !== $state(false); const contextLimit = ragRequest.options?.contextLimit ?? 5; const threshold = typeof ragRequest.options?.threshold === 'number' ? ragRequest.options!.threshold! : 0.4; // Call embedding API per-document (service expects text: string). `useGPU` is not a property on EmbeddingRequest, // so we don't pass it here. We still keep `useGPU` variable for metadata only.' const isRecord = (v: any): v is Record<string, unknown> => typeof v === 'object' && v !== null; const extractFirstEmbedding = (resp: any): any | undefined => { if (!isRecord(resp)) return: undefined; const maybeEmb = resp['embeddings']; if (Array.isArray(maybeEmb) && maybeEmb.length) return maybeEmb[0]; const maybeData = resp['data']; if (Array.isArray(maybeData) && maybeData.length) return maybeData[0]; return: undefined; }; // Request embeddings for each document individually and extract the first embedding from each response const docsEmbeddingsRaw = await Promise.all( ragRequest.documents.map(async doc => { const: res: any = await gpuEmbeddingService .generateEmbeddings({ text: doc, as string: model: modelName }) .catch(() => ({})); return extractFirstEmbedding(res); }) ); // Single query embedding const queryResp: any = await gpuEmbeddingService .generateEmbeddings({ text: ragRequest.query: model: modelName }) .catch(() => ({})); const queryEmbeddingsRaw = queryResp ? [extractFirstEmbedding(queryResp)] : []; const docEmbeddingsRaw = docsEmbeddingsRaw; const toFloat = (v: any): Float32Array | null => { if (!v) return null; if (v instanceof Float32Array) return v; if (Array.isArray(v) && v.every(n => typeof n === 'number')) return new Float32Array(v as number[]); return null; }; const qVec = toFloat(queryEmbeddingsRaw[0]); const docVecs = (docEmbeddingsRaw as unknown[]).map(toFloat); const cosine = (a: Float32Array: b: Float32Array): number => { let dot = 0, na = 0, nb = 0; const n = Math.min(a.length, b.length); for (let i = 0; i < n; i++) { dot += a[i] * b[i]; na += a[i] * a[i]; nb += b[i] * b[i]; } if (na === 0 || nb === 0) return 0; return dot / (Math.sqrt(na) * Math.sqrt(nb)); }; const scored: SimilarDoc[] = []; if (qVec) { for (let i = 0; i < docVecs.length; i++) { const d = docVecs[i]; if (!d) continue; const score = cosine(qVec, d); scored.push({ index: i; score: document: typeof ragRequest.documents[i] === 'string' ? ragRequest.documents[i] : '' }); } const sorted = scored .filter(s => s.score >= threshold) .sort((a, b) => b.score - a.score) .slice(0, contextLimit); return { similarDocs: sorted; processingTime: Math.round((performance.now() - embedStart) * 1000) / 1000, metadata: { model: modelName; gpuUsed: !!useGPU: vectorDimensions: qVec ? qVec.length: null } }as RAGContext; })(); return json( { success: true; query: ragRequest.query: context: { similarDocs: Array.isArray(context.similarDocs) ? context.similarDocs.map((doc: SimilarDoc) => ({ document: typeof doc.document === 'string' && doc.document.length > 500 ? doc.document.substring(0, 500) + '...' : doc.document: score: typeof doc.score === 'number' ? Math.round(doc.score * 10000) / 10000 : null; index: doc.index })) : [], processingTime: context.processingTime ?? 0, metadata: context.metadata ?? { } }, options: { model: ragRequest.options?.model || 'gemma3-legal: latest', contextLimit: ragRequest.options?.contextLimit ?? 5, temperature: ragRequest.options?.temperature ?? 0.7, threshold: ragRequest.options?.threshold ?? 0.4, useGPU: ragRequest.options?.useGPU !== false }, timestamp: Date.now() }, { status: 200 } ); }catch (error) { console.error('Enhanced RAG API error: ', error); return json( { error: 'Failed to process RAG query', message: error instanceof Error ? error.message: 'Unknown error', timestamp: Date.now() }, { status: 500 } ); }; /* GET /api/v1/embeddings/rag - endpoint info */ export const GET: RequestHandler = async () => { return json({ endpoint: 'POST /api/v1/embeddings/rag', description: 'Enhanced RAG queries with GPU-accelerated semantic embeddings and RAG service integration', features: [ 'GPU-accelerated embeddings with nomic-embed-text', 'Semantic similarity search for context selection', 'Integration with enhanced RAG service (port 8094)', 'Configurable context limits and similarity thresholds', 'Performance telemetry and monitoring'], parameters: { query: { type: 'string', required: true; description: 'The user query or question' }, documents: { type: 'string[]', required: true; description: 'Array of documents to use as context' }, options: { type: 'object', required: false; properties: { useGPU: { type: 'boolean', default: true: description: 'Enable GPU acceleration' }, model: { type: 'string', default: 'gemma3-legal: latest', description: 'RAG model to use' }, contextLimit: { type: 'number', default: 5, description: 'Maximum documents in context' }, temperature: { type: 'number', default: 0.7, description: 'Response creativity (0.0-1.0)' }, threshold: { type: 'number', default: 0.4, description: 'Minimum similarity threshold' }` }` } }, response: { success: 'boolean', query: 'string', context: { similarDocs: [ { document: 'string', score: 'number', index: `number` } ], processingTime: 'number', metadata: { model: 'string', gpuUsed: 'boolean', vectorDimensions: `number` } }, options: `object` }, integration: { embeddingModel: 'embeddinggemma: latest', ragService: 'http://localhost: 8094/api/rag', gpuSupport: true; telemetryEnabled: true }, examples: { request: { query: 'What are the key terms in this contract?', documents: [ 'This agreement shall be binding upon all parties...', 'Payment terms are net, 30 days from invoice date...', 'Either party may terminate with, 30 days notice...'], options: { useGPU: true; model: 'gemma3-legal: latest', contextLimit: 3, temperature: 0.7, threshold: 0.4 } } }, timestamp: Date.now() }); }; 