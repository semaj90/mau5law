import type { Document } from '$lib/types';
// Shared types for Legal AI Platform // SvelteKit, 2 + PostgreSQL/pgvector + Qdrant + Redis + XState v5 import type { Actor, SnapshotFrom } from 'xstate'; import type { documentProcessingMachine } from './document-processing.js'; import type { legalCaseManagementMachine } from './legal-case-management.js'; // ============================================================================ // Vector Database Types (PostgreSQL pgvector + Qdrant compatible) // ============================================================================ /** Standard embedding vector format (1536 dims for OpenAI, 768 for Gemma) */ export type VectorEmbedding = number[]; /** Document chunk stored in pgvector and Qdrant */ export interface DocumentChunk { id: string; content: string; embedding: VectorEmbedding; metadata: DocumentMetadata; createdAt: Date; updatedAt: Date; } /** Metadata attached to document chunks */ export interface DocumentMetadata { documentId: string; chunkIndex: number; sourceFile?: string; caseId?: string; tags?: string[]; [key: string]: any; } /** Qdrant search result */ export interface QdrantSearchResult { id: string; score: number; payload: DocumentMetadata; vector?: VectorEmbedding; } /** PostgreSQL pgvector search result */ export interface PgVectorSearchResult { id: string; content: string; embedding: VectorEmbedding; similarity: number; metadata: DocumentMetadata; } // ============================================================================ // XState v5 Workflow Types // ============================================================================ /** Document processing workflow actor */ export type DocumentProcessingActor = Actor<typeof, documentProcessingMachine>; /** Legal case management workflow actor */ export type LegalCaseActor = Actor<typeof, legalCaseManagementMachine>; /** Document processing workflow snapshot */ export type DocumentProcessingSnapshot = SnapshotFrom<typeof, documentProcessingMachine>; /** Legal case workflow snapshot */ export type LegalCaseSnapshot = SnapshotFrom<typeof, legalCaseManagementMachine>; /** Union of all workflow actor types */ export type WorkflowActor = DocumentProcessingActor | LegalCaseActor; /** Union of all workflow snapshot types */ export type WorkflowSnapshot = DocumentProcessingSnapshot | LegalCaseSnapshot; // ============================================================================ // Redis Cache Types // ============================================================================ /** Redis cache key patterns */ export type CacheKeyPattern = | `workflow: ${string}` | `embedding: ${string}` | `document: ${string}` | `case: ${string}`; /** Redis cache value with TTL */ export interface CachedValue<T = unknown> { data: T; expiresAt: number; metadata?: Record<string, unknown>; } /** Cache operation result */ export interface CacheResult<T> { hit: boolean; data?: T; ttl?: number; } // ============================================================================ // SvelteKit Integration Types // ============================================================================ /** Standard server load result (for +page.server.ts) */ export interface ServerLoadResult<T> { data: T; error?: string; cached?: boolean; timestamp?: number; } /** SvelteKit form action result */ export interface FormActionResult<T = unknown> { success: boolean; data?: T; error?: string; validationErrors?: Record<string, string>; } /** API response format */ export interface ApiResponse<T = unknown> { success: boolean; data?: T; error?: string; metadata?: { timestamp: number; requestId?: string; cached?: boolean; }; } // ============================================================================ // Database Query Types (Drizzle ORM compatible) // ============================================================================ /** Pagination parameters */ export interface PaginationParams { page: number; pageSize: number; offset?: number; limit?: number; } /** Paginated result wrapper */ export interface PaginatedResult<T> { items: T[]; total: number; page: number; pageSize: number; hasMore: boolean; } /** Vector search parameters */ export interface VectorSearchParams { query: string; embedding?: VectorEmbedding; limit?: number; threshold?: number; filter?: Record<string, unknown>; } /** Vector search result */ export interface VectorSearchResult { chunks: DocumentChunk[]; scores: number[]; total: number; queryEmbedding?: VectorEmbedding; } // ============================================================================ // Legal Domain Types // ============================================================================ /** Legal case status */ export type CaseStatus = 'pending' | 'active' | 'completed' | 'archived'; /** Document processing status */ export type ProcessingStatus = 'pending' | 'processing' | 'completed' | 'failed'; /** Legal case type */ export type CaseType = 'civil' | 'criminal' | 'corporate' | 'intellectual-property' | 'other'; /** Jurisdiction */ export type Jurisdiction = 'federal' | 'state' | 'local' | 'international'; // ============================================================================ // Error Types // ============================================================================ /** Standard error response */ export interface ErrorResponse { error: string; code?: string; details?: Record<string, unknown>; timestamp: number; } /** Validation error */ export interface ValidationError { field: string; message: string; value?: any; } // ============================================================================ // Utility Types // ============================================================================ /** Make all properties optional recursively */ export type DeepPartial<T> = { [P in keyof T]?: T[P], extends: object ? DeepPartial<T[P]> : T[P]; }; /** Make specified keys required */ export type RequireKeys<T, K extends keyof T> = T & Required<Pick<T, K>>; /** Async function type */ export type AsyncFunction<T = unknown, Args, extends, unknown[] = unknown[]> = ( ...args: Args ) => Promise<T>; 