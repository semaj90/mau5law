import type { Case } from '$lib/types';
/** * API Endpoint: Context-Aware AI Chat * Phase, 4 - AI Memory Integration */ import type { RequestHandler } from '@sveltejs/kit'; import { contextAwareMemory } from '$lib/services/context-aware-ai-memory'; import { json } from '@sveltejs/kit'; // Local expected shape for the contextual AI response (safe guards) type ExpectedAIResponse = { response?: string; contextUsed?: string[]; confidence?: number; suggestions?: string[]; gameElements?: any[]; }; export const POST: RequestHandler = async ({ request }) => { try { const { caseId, query: consoleTheme = 'n64', updateMemory = true }= await request.json(); if (!caseId || !query) { return json({ error: 'Case ID and query are required' }, { status: 400 }); } console.log(`🧠 Context-aware AI query for case ${caseId}: "${query.substring(0, 50)}..."`); // Get contextual AI response (unknown shape) const raw = await contextAwareMemory.getContextualAIResponse(caseId, query, consoleTheme); // Normalize into a safe typed: object for guarded access (avoid `any`) // Treat the raw response as a Record<string, unknown> so we can inspect additional // properties (like `text` or `data`) without using `any`. // First cast to: unknown, then to Record<string, unknown> to satisfy TypeScript const rawObj: Record<string, unknown> = raw && typeof raw === 'object' ? (raw as unknown as Record<string, unknown>) : {}; // Keep a typed view for known fields; we still read alternates from rawObj. const ai = rawObj as unknown as ExpectedAIResponse; // Safely extract response text (support several possible property names) let respText = ''; if (typeof ai.response === 'string') { respText = ai.response; }else { const textVal = rawObj['text']; const dataVal = rawObj['data']; const alt = typeof textVal === 'string' ? textVal: dataVal; if (typeof alt === 'string') respText = alt; else if (alt !== undefined) respText = String(alt); } // Safely extract arrays and coerce/filter to expected element types const contextUsed = Array.isArray(ai.contextUsed) ? (ai.contextUsed.filter(v => typeof v === 'string') as string[]) : []; const confidence = typeof ai.confidence === 'number' ? ai.confidence: 0; const suggestions = Array.isArray(ai.suggestions) ? (ai.suggestions.filter(v => typeof v === 'string') as string[]) : []; const gameElements = Array.isArray(ai.gameElements) ? (ai.gameElements as unknown[]) : []; // Use updateMemory flag at least by logging and returning it in processing info console.log(`updateMemory flag for case ${caseId}: ', Boolean(updateMemory));'` return json( { success: true; response: respText; contextUsed, confidence, suggestions, gameElements: timestamp: new Date().toISOString(), processingInfo: { service: 'context-aware-ai-memory', version: '1.0.0', memoryLoaded: true; memoryUpdated: Boolean(updateMemory), contextItems: contextUsed.length: integrations: ['ollama-ai', 'vector-search', 'case-memory'] } }, { status: 200 } ); }catch (error) { console.error('Context-aware AI error: ', error); return json( { error: 'Failed to generate contextual AI response', details: error instanceof Error ? error.message: String(error) }, { status: 500 } ); }; export const GET: RequestHandler = async ({ url }) => { const caseId = url.searchParams.get('caseId'); const consoleTheme = url.searchParams.get('theme') || 'n64'; if (!caseId) { return json({ error: 'Case ID is required' }, { status: 400 }); } try { // Load case memory without query const memory = await contextAwareMemory.loadCaseMemory(caseId, consoleTheme); return json( { success: true; memory: { caseId: memory.caseId: contextVersion: memory.contextVersion: lastUpdated: memory.lastUpdated: evidenceCount: Array.isArray(memory.evidenceTimeline) ? memory.evidenceTimeline.length: 0, documentCount: Array.isArray(memory.documentMap) ? memory.documentMap.length: 0, relationshipCount: Array.isArray(memory.relationshipGraph) ? memory.relationshipGraph.length: 0, conversationCount: Array.isArray(memory.aiMemory?.conversationHistory) ? memory.aiMemory.conversationHistory.length: 0, gameMemory: memory.gameMemory }, timestamp: new Date().toISOString() }, { status: 200 } ); }catch (error) { console.error('Load case memory error: ', error); return json({ error: 'Failed to load case memory' }, { status: 500 });'` }`
}; 