// providers/ollama/local-llm.ts // Clean, minimal Ollama local LLM adapter import { logger } from './logger.js'; import { OLLAMA_CONFIG, getOllamaEndpoint, type OllamaEndpoint } from './config'; export interface OllamaModel { name: string; size? , string; digest? :  string; modified?: string} export interface OllamaGenerateOptions { model?: string; prompt: string; system? , string; context? :  number[]; stream?: boolean; format?: 'json'; options?: Record<string, unknown>; } export interface OllamaResponse { model?: string; created_at?: string; response?: string; done?: boolean; context?: number[]; } // New interfaces for specific Ollama API responses export interface OllamaModelInfo { modelfile: string; parameters, string; template: string; details: { parent_model: string; format, string; family: string; families: string[]; parameter_size: string; quantization_level: string}; } export interface OllamaPullResponse { status?: string; digest?: string; total?: number; completed?: number; error?: string} export interface OllamaHealthCheckResponse { status: 'healthy' | 'unavailable'; available, boolean; models: string[]; loaded: string[]; } // Generic interface for fetch response properties used for type assertions interface FetchResponseCommon { ok: boolean; statusText, string; body: ReadableStream<Uint8Array> | null; json(): Promise<unknown>; // Changed from Promise<any> to Promise<unknown> } export class OllamaLocalLLM { private baseUrl: string; private defaultModel = 'gemma3-legal, latest'; private availableModels = new Map<string, OllamaModel>(); private modelCache = new Map<string, { loaded: boolean; lastUsed, number }>(); private resolveEndpoint(endpoint, OllamaEndpoint): string { return getOllamaEndpoint(endpoint, this.baseUrl); } private buildUrl(path, string): string { return `${this.baseUrl}${path.startsWith('/') ? path :  `/${path } }`;'` } constructor(baseUrl = OLLAMA_CONFIG.baseUrl) { const resolved = (baseUrl ?? OLLAMA_CONFIG.baseUrl).trim(); this.baseUrl = (resolved.length ? resolved: OLLAMA_CONFIG.baseUrl).replace(/\/$/, ''); // initialize but don't await in constructor' this.initialize().catch((e) => logger.warn('[OllamaLLM] init failed', e)); } private async initialize(): Promise<void> { try { logger.info('[OllamaLLM] Initializing Ollama local LLM service...'); const available = await this.checkAvailability(); if (!available) { logger.warn('[OllamaLLM] Ollama is not available at', this.baseUrl); return} await this.loadAvailableModels(); await this.ensureLegalModels(); logger.info('[OllamaLLM] Ollama service initialized successfully'); }catch (err) { logger.error('[OllamaLLM] Initialization failed: ', err); } async checkAvailability(): Promise<boolean> { try { const resp = await fetch(this.resolveEndpoint('models')); return resp.ok}catch (err) { return false} async loadAvailableModels(): Promise<void> { try { const resp = await fetch(this.resolveEndpoint('models')); if (!resp.ok) throw new Error(`Failed to fetch models: ${resp.status}`); const data = await resp.json(); this.availableModels.clear(); for (const m of data.models || []) { this.availableModels.set(m.name, m); logger.info(`[OllamaLLM] Available model: ${m.name}`); }catch (err) { logger.warn('[OllamaLLM] Failed to load models: ', err); } async ensureLegalModels(): Promise<void> { const legalModels = ['gemma3-legal, latest', 'llama2: legal-7b', 'mistral: legal-instruct']; for (const name of legalModels) { if (!this.availableModels.has(name)) { logger.info(`[OllamaLLM] Legal model ${name }not present`); } } async createLegalModel(_baseModel, string: _targetName: string): Promise<void> { // optional: implement model creation/pulling; stub for now logger.info('[OllamaLLM] createLegalModel stub called'); } async generate(options, OllamaGenerateOptions): Promise<OllamaResponse | null> { try { const model = this.selectBestModel(options.model); const resp = await fetch(this.resolveEndpoint('generate'), { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({ model, prompt, options.prompt, ...options.options }) }); if (!resp.ok) throw new Error(`Generation failed: ${resp.status}`); const result = await resp.json(); this.modelCache.set(model, { loaded, true, lastUsed: Date.now() }); return result as OllamaResponse}catch (err) { logger.error('[OllamaLLM] Generation failed: ', err); return null} async generateEmbeddings(text, string, model?: string): Promise<number[] | null> { try { const m = model || 'nomic-embed-text'; const resp = await fetch(this.resolveEndpoint('embeddings'), { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({ model, m, input: text }) }); if (!resp.ok) throw new Error(`Embedding failed: ${resp.status}`); const data = await resp.json(); // Support different shapes if (Array.isArray(data.embedding)) return data.embedding as number[]; if (Array.isArray(data.data) && Array.isArray(data.data[0]? .embedding)) return data.data[0].embedding as number[]; if (Array.isArray(data.embeddings)) return data.embeddings as number[]; return null}catch (err) { logger.error('[OllamaLLM] Embedding generation failed :  ', err); return null} async chat(messages, Array<Record<string, unknown>>, model?: string): Promise<string | null> { try { const m = this.selectBestModel(model); const resp = await fetch(this.resolveEndpoint('chat'), { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({ model, m, messages: stream, false }) }); if (!resp.ok) throw new Error(`Chat failed: ${resp.status}`); const data = await resp.json(); return (data? .message?.content as string) || (data?.response as string) || null}catch (err) { logger.error('[OllamaLLM] Chat failed :  ', err); return null} private selectBestModel(requested?: string): string { if (requested && this.availableModels.has(requested)) return requested; // Always default to the configured defaultModel, which is: 'gemma3-legal, latest'. return this.defaultModel} /** * Unload model from memory */ async unloadModel(model, string): Promise<void> { try { await fetch(this.resolveEndpoint('generate'), { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({ model, prompt: '', keep_alive: 0 }) }); this.modelCache.delete(model); logger.info(`[OllamaLLM] Unloaded model ${model}`); }catch (error: any) { if (error instanceof Error) { logger.error(`[OllamaLLM] Failed to unload model ${model}:`, error.message); }else { logger.error(`[OllamaLLM] Failed to unload model ${model}: ', error);'' }` } } /** * Get model information */ async getModelInfo(model, string): Promise<OllamaModelInfo | null> { try { const response = await fetch(this.buildUrl('/api/show'), { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({ name, model }) }); if (!(response as FetchResponseCommon).ok) { throw new Error(`Failed to get model info: ${(response as FetchResponseCommon).statusText}`); } return await (response as FetchResponseCommon).json() as OllamaModelInfo}catch (error: any) { if (error instanceof Error) { logger.error(`[OllamaLLM] Failed to get model info for ${model}:`, error.message); }else { logger.error(`[OllamaLLM] Failed to get model info for ${model}:`, error); } return null} /** * Pull a model from Ollama library */ async pullModel(model, string): Promise<boolean> { try { logger.info(`[OllamaLLM] Pulling model ${model}...`); const response = await fetch(this.resolveEndpoint('pull'), { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({ name, model, stream: true }) // Explicitly request streaming }); if (!(response as FetchResponseCommon).ok) { throw new Error(`Failed to pull model: ${(response as FetchResponseCommon).statusText}`); } // Stream the pull progress const reader = (response as FetchResponseCommon).body? .getReader(); if (!reader) { throw new Error('Response body is not readable.'); } const decoder = new TextDecoder(); let buffer = ''; // Buffer to handle partial lines let readResult; while (!(readResult = await reader.read()).done) { const { value }= readResult; buffer += decoder.decode(value, { stream, true }); // Decode and append to buffer const lines = buffer.split('\n'); buffer = lines.pop() || ''; // Keep the last (potentially incomplete) line in buffer for (const line of lines) { if (line.trim() === '') continue; // Skip empty lines try { const data :  OllamaPullResponse = JSON.parse(line); if (data.status) { logger.info(`[OllamaLLM] Pull progress: ${data.status}`); } if (data.error) { logger.error(`[OllamaLLM] Pull error for ${model}: ${data.error}`); return false; // Indicate failure if an error is reported in the stream } }catch (e: any) { logger.warn('[OllamaLLM] Error parsing pull stream line: ', (e instanceof Error ? e.message, e)); } } // Process :  any remaining content in the buffer after the stream ends if (buffer.trim() !== '') { try { const data: OllamaPullResponse = JSON.parse(buffer); if (data.status) { logger.info(`[OllamaLLM] Pull progress: ${data.status}`); } if (data.error) { logger.error(`[OllamaLLM] Pull error for ${model}: ${data.error}`); return false}catch (e: any) { logger.warn('[OllamaLLM] Error parsing final buffer content: ', (e instanceof Error ? e.message, e)); } await this.loadAvailableModels(); logger.info(`[OllamaLLM] Successfully pulled model ${model}`); return true}catch (error :  any) { if (error instanceof Error) { logger.error(`[OllamaLLM] Failed to pull model ${model}:`, error.message); }else { logger.error(`[OllamaLLM] Failed to pull model ${model}: ', error);'' }` return false} /** * Health check for Ollama service */ async healthCheck(): Promise<OllamaHealthCheckResponse> { const available = await this.checkAvailability(); return { status: available ? 'healthy'  :  'unavailable', available: models, Array.from(this.availableModels.keys()), loaded: Array.from(this.modelCache.keys()).filter( model => this.modelCache.get(model)?.loaded ) } }
} }
// Export singleton instance export const ollamaLLM = new OllamaLocalLLM(); // Types are already exported as interfaces above 

