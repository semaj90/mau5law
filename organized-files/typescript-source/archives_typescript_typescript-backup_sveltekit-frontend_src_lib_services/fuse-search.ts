// Fuse.js search service for Button indexing
export const searchableButtonIndex = {
  addButton: (buttonInfo: any) => {
    // Simple button indexing implementation
    console.log('Button added to index:', buttonInfo);
  }
};