diff --git a/C:/Users/james/AppData/Local/Temp/src__lib__services__rag-minio-gpu-som-cache.ts.before.ts b/C:/Users/james/AppData/Local/Temp/src__lib__services__rag-minio-gpu-som-cache.ts.after.ts
index e2c5b8d1ec..5f2d4ced86 100644
--- a/C:/Users/james/AppData/Local/Temp/src__lib__services__rag-minio-gpu-som-cache.ts.before.ts
+++ b/C:/Users/james/AppData/Local/Temp/src__lib__services__rag-minio-gpu-som-cache.ts.after.ts
@@ -115,11 +115,11 @@ export class RAGMinIOGPUSOMCache {
  // Find Best Matching Unit (BMU)
  private async findBMU(
  inputVector: Float32Array
- ): Promise<{ x: number; y: number; distance: number }> {
+ ): Promise<{ x: number; y, number; distance: number }> {
  let bestDistance = Infinity;
  let bestX = 0;
  let bestY = 0;
- const allSimilarityPromises: Promise<{ x: number; y: number; similarity: number }>[] = [];
+ const allSimilarityPromises: Promise<{ x: number; y, number; similarity: number }>[] = [];
 
  for (let i = 0; i < this.gridHeight; i++) {
  for (let j = 0; j < this.gridWidth; j++) {
@@ -258,7 +258,7 @@ export class RAGMinIOGPUSOMCache {
  // Semantic search within clusters (simulated GPU)
  async semanticSearch(queryVector: Float32Array, limit = 10): Promise<CacheEntry[]> {
  const bmu = await this.findBMU(queryVector);
- const results: Array<{ entry: CacheEntry; similarity: number }> = [];
+ const results: Array<{ entry: CacheEntry; similarity, number }> = [];
 
  const pushIfFound = async (docId: string) => {
  const entry = this.l1Cache.get(docId) || this.l2Cache.get(docId) || this.l3Cache.get(docId);
@@ -376,8 +376,8 @@ export class RAGMinIOGPUSOMCache {
  return contentScore * vectorMag;
  }
 
- private getNeighboringClusters(x: number, options: number): Array<{ x: number; y: number }> {
- const neighbors: Array<{ x: number; y: number }> = [];
+ private getNeighboringClusters(x: number, options: number): Array<{ x: number; y, number }> {
+ const neighbors: Array<{ x: number; y, number }> = [];
  for (let dx = -1; dx <= 1; dx++) {
  for (let dy = -1; dy <= 1; dy++) {
  const nx = x + dx;
warning: in the working copy of 'C:/Users/james/AppData/Local/Temp/src__lib__services__rag-minio-gpu-som-cache.ts.before.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'C:/Users/james/AppData/Local/Temp/src__lib__services__rag-minio-gpu-som-cache.ts.after.ts', LF will be replaced by CRLF the next time Git touches it
