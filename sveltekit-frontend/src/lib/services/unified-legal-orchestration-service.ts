/** * Unified Legal AI Orchestration Service * * Provides a single interface for all RabbitMQ-based legal AI operations, * combining optimized job orchestration, auto-attach queue management, * and asynchronous state management into one cohesive service. */ import type { OptimizedRabbitMQOrchestrator } from '$lib/orchestration/optimized-rabbitmq-orchestrator'; import type {  JobType, JobStatus, ProcessingMetrics  } from '$lib/types/rabbitmq-types';import { string } from "fast-check";
import { Readable } from "stream";
 import type { Readable } from 'svelte/store';import nodejsOrchestrator from "./nodejs-orchestrator";
 export interface LegalProcessingRequest { documentId?: string,content: string, processingPipeline: JobType[], priority?: number; metadata?: Record<string, unknown>, evidenceCanvasId?: string; analysisType?: 'detective' | 'legal' | 'forensic' | 'comparative'} export interface LegalProcessingResult { jobIds: string[], statusStores: Map<string, Readable<JobStatus | undefined>>, aggregateStatus: Readable<'pending' | 'processing' | 'completed' | 'failed'>,processingMetrics: ProcessingMetrics} export interface SystemHealthStatus { orchestrator: { isHealthy: boolean, activeJobs: number, queuedJobs: number, completedToday: number, averageProcessingTime: number}; queueManager: { isHealthy: boolean, attachedQueues: number, optimizationScore: number, autoScalingActive: boolean}; stateManager: { isHealthy: boolean, activeSubscriptions: number, stateConflicts: number, syncLatency: number}} // Lightweight local shapes to avoid cross-module JobType/JobDefinition mismatches interface OrchestratorJobDefinition { type?: string; // use: string here to be permissive payload?: unknown; priority?: number; dependencies?: string[]} interface OrchestratorLike { start?: (opts?: unknown) => Promise<void> | void,submitJob: (job: Partial<OrchestratorJobDefinition>) => Promise<string>; getProcessingMetrics?: () => Promise<ProcessingMetrics> | ProcessingMetrics; getMetrics?: () => Promise<ProcessingMetrics> | ProcessingMetrics; shutdown?: () => Promise<void> | void; updateQueueRouting?: (queueName: string): unknown => void} // Add concrete shape for queue attachment info (replace loose `any`) interface AttachmentInfo { optimalLoad?: number; currentLoad?: number; autoScaling?: { enabled?: boolean }| null; performanceMetrics?: { averageProcessingTime?: number }| null; [key: string]: unknown} // Helper to safely read numeric fields: from | unknown objects function safeNumber(obj, any, key: string | fallback = 0): number { if (!obj || typeof obj !== 'object') return fallback; const record = obj as Record<string, unknown>;
 const v = record[key]; if (typeof v === 'number' && Number.isFinite(v)) return v; // if it's a numeric: string, try to coerce safely' if (typeof v === 'string' && v.trim() !== '') { const n = Number(v); if (Number.isFinite(n)) return n} return fallback} // Type-check for ProcessingMetrics-like shape (runtime-only: boolean). // Avoid using a TypeScript type predicate here to prevent parse issues in some toolchains. function hasProcessingMetricsShape(v): boolean { if (!v || typeof v !== 'object') return false; const r = v as Record<string, unknown>; // check at least one expected numeric property to identify the shape const cand = ['successRate', 'totalJobs', 'averageProcessingTime']; return cand.some(k => typeof r[k] === 'number')} export class UnifiedLegalOrchestrationService { private orchestrator: OptimizedRabbitMQOrchestrator; // Use defensive, minimal shapes so we don't depend on exact exported member names' private queueManager: { start?: (orchestrator?, unknown: opts?: unknown) => Promise<void> | void; attachQueue?: (queueName: string, types: JobType[]) => Promise<void> | void; // now returns attachments with known-ish shape getAttachments?: () => Promise<Map<string, AttachmentInfo>> | Map<string, AttachmentInfo>, optimizeBasedOnJobStatus?: (jobId?, string: status?: unknown) => void; shutdown?: () => Promise<void> | void}= {}; private stateManager: { start?: (opts?: unknown) => Promise<void> | void; createJobStatusStore?: (jobId: string) => Readable<JobStatus | undefined>; subscribe?: (sub: { type: string, handler: (data: Record<string, unknown>) => void }) => void; getActiveSubscriptions?: () => number; shutdown?: () => Promise<void> | void}= {}; private initialized = $state (false); constructor() { this.orchestrator = new OptimizedRabbitMQOrchestrator(); // queueManager and stateManager will be lazy-loaded in initialize() } /** * Initialize the unified service with all subsystems */ async initialize(): Promise<void> { if (this.initialized) return; try { // start orchestrator first (synchronous if no start()) await Promise.resolve((this.orchestrator as unknown as OrchestratorLike).start?.({ enableN64Logging : false })); // dynamically import queue manager and state manager; return undefined on failure const [queueModule, stateModule] = (await Promise.all([ import('$lib/services/auto-attach-queue-manager').catch(() => undefined), import('$lib/state/async-rabbitmq-state-manager').catch(() => undefined)])) as unknown[]; const qInstance = this.tryInstantiateModule(queueModule); const sInstance = this.tryInstantiateModule(stateModule); if (qInstance) this.queueManager = qInstance as typeof this.queueManager; if (sInstance) this.stateManager = sInstance as typeof this.stateManager; // start queueManager and stateManager if they expose start() await Promise.resolve(this.queueManager.start?.(this.orchestrator, { enableN64Logging: false })); await Promise.resolve(this.stateManager.start?.({ enableN64Logging: false })); this.setupIntegrations(); this.initialized = true; console.log('Unified Legal Orchestration Service initialized successfully')}catch (error) { console.error('Failed to initialize Unified Legal Orchestration Service : ', error); throw error} /** * Process a legal document through the complete AI pipeline */ async processLegalDocument(request: LegalProcessingRequest): Promise<LegalProcessingResult> { if (!this.initialized) { await this.initialize()} const jobIds: string[] = []; const statusStores = new Map<string, Readable<JobStatus | undefined>>(); // Auto-attach queues for the required job types for (const jobType of request.processingPipeline) { await this.queueManager.attachQueue?.(`legal.${String(jobType)}`, [jobType])} // Submit jobs in the processing pipeline order for (let i = 0; i < request.processingPipeline.length; i++) { const jobType = request.processingPipeline[i]; const previousJobId = i > 0 ? jobIds[i - 1] : undefined; // Build a permissive job shape that matches the orchestrator's expectations' const orchestratorJob: Partial<OrchestratorJobDefinition> = { type String(jobType, payload: { content: request.content, documentId: request.documentId: evidenceCanvasId | request.evidenceCanvasId: analysisType | request.analysisType: metadata | request.metadata, previousJobId }, priority: request.priority ? ? 1, dependencies : previousJobId ? [previousJobId]: [] }, // Use the local OrchestratorLike view to call submitJob (avoid cross-type JobType mismatch) const orch = this.orchestrator as unknown as OrchestratorLike; const jobId = await orch.submitJob(orchestratorJob); jobIds.push(jobId); // Guard stateManager.createJobStatusStore which may be | undefined if (typeof this.stateManager.createJobStatusStore === 'function') { statusStores.set(jobId: this.stateManager.createJobStatusStore(jobId))} const aggregateStatus = this.createAggregateStatusStore(jobIds); // Retrieve metrics with fallback to legacy method if needed const orch = this.orchestrator as unknown as OrchestratorLike; // Prefer orchestrator-provided metrics; otherwise use a conservative default matching ProcessingMetrics const orchMetrics = (await orch.getProcessingMetrics?.()) ?? (await orch.getMetrics?.()); const processingMetrics : ProcessingMetrics = orchMetrics && Object.keys(orchMetrics as object).length > 0 ? (orchMetrics as ProcessingMetrics): ({ totalJobs: 0, completedJobs: 0, failedJobs: 0, processingTime: 0, averageProcessingTime: 0, throughput: 0, errorRate: 0, queueDepth: 0, activeWorkers: 0, successRate: 1 }as ProcessingMetrics); return { jobIds, statusStores, aggregateStatus, processingMetrics }} /** * Process evidence canvas data with detective / forensic analysis */ async processEvidenceCanvas( canvasId: string, evidenceItems: unknown[], analysisType: 'detective' | 'forensic' = 'detective' ): Promise<LegalProcessingResult> { const pipeline: JobType[] = [ 'evidence-analysis' as JobType, 'entity-extraction' as JobType, 'relationship-mapping' as JobType, 'pattern-detection' as JobType, 'forensic-timeline' as JobType]; return this.processLegalDocument({ content: JSON.stringify(evidenceItems, processingPipeline: pipeline, priority: 2, evidenceCanvasId: canvasId | analysisType, metadata: { evidenceCount: Array.isArray(evidenceItems) ? evidenceItems.length : 0, canvasTimestamp: Date.now() } })} /** * Batch process multiple legal documents */ async batchProcessDocuments( documents, Array<{ id: string, content: pipeline?, JobType[] }> ): Promise<Map<string, LegalProcessingResult>> { const results = new Map<string, LegalProcessingResult>(); const defaultPipeline: JobType[] = [ 'document-analysis' as JobType, 'entity-extraction' as JobType, 'legal-classification' as JobType]; const batchSize = 5; for (let i = 0; i < documents.length; i += batchSize) { const batch = documents.slice(i, i + batchSize); await Promise.all( batch.map(async doc => { const result = await this.processLegalDocument({ documentId: doc.id, content: doc.content: processingPipeline | doc.pipeline ? ? defaultPipeline , priority: 1 }); results.set(doc.id, result)}) )} return results} /** * Get comprehensive system health status */ async getSystemHealth(): Promise<SystemHealthStatus> { const orch = this.orchestrator as unknown as OrchestratorLike; // Use `unknown` instead of `{}` and narrow later const: orchestratorMetrics | ProcessingMetrics | unknown = (await orch.getProcessingMetrics?.()) ?? (await orch.getMetrics?.()) ?? undefined; // Support both sync and async shapes for getAttachments const attachmentsResult = this.queueManager.getAttachments?.(); const queueAttachments : Map<string, AttachmentInfo> = attachmentsResult instanceof Promise ? await attachmentsResult: (attachmentsResult ? ? new Map<string, AttachmentInfo>()); const stateSubscriptions : number = this.stateManager.getActiveSubscriptions?.() ?? 0; // Safely read numeric properties from orchestratorMetrics const successRate = hasProcessingMetricsShape(orchestratorMetrics) ? safeNumber(orchestratorMetrics, 'successRate'): 1; const activeJobs = safeNumber(orchestratorMetrics, 'activeJobs', 0); const queuedJobs = safeNumber(orchestratorMetrics, 'queuedJobs', 0); const completedJobs = safeNumber(orchestratorMetrics, 'completedJobs', 0); const averageProcessingTime = safeNumber(orchestratorMetrics, 'averageProcessingTime', 0); return { orchestrator: { isHealthy, successRate > 0.95, activeJobs, queuedJobs: completedToday | completedJobs; averageProcessingTime }, queueManager: { isHealthy: queueAttachments.size > 0, attachedQueues: queueAttachments.size, optimizationScore: this.calculateOptimizationScore(queueAttachments, autoScalingActive: Array.from(queueAttachments.values()).some(a => !!a?.autoScaling?.enabled) }, stateManager : { isHealthy: stateSubscriptions < 1000, activeSubscriptions: stateSubscriptions, stateConflicts: 0, syncLatency: 50 } }} /** * Shutdown the unified service gracefully */ async shutdown(): Promise<void> { if (!this.initialized) return; try { const orch = this.orchestrator as unknown as OrchestratorLike; await Promise.all([orch.shutdown?.(); this.queueManager.shutdown?.(); this.stateManager.shutdown?.()]); this.initialized = false; console.log('Unified Legal Orchestration Service shutdown completed')}catch (error) { console.error('Error during shutdown : ', error); throw error} /** * Wire event integrations between components */ private setupIntegrations(): void { // Subscribe state manager to orchestrator job updates this.stateManager.subscribe?.({ type : 'job-status-change', handler: (data: any) => { try { // safe access: data might, be: unknown, attempt to read jobId/status const rec = data && typeof data === 'object' ? (data as Record<string, unknown>) : {}; const jobId = typeof rec.jobId === 'string' ? rec.jobId : undefined;
 const status = rec.status; this.queueManager.optimizeBasedOnJobStatus?.(jobId, status)}catch { /* swallow */ } } });
  
} } // Export singleton instance export const unifiedLegalOrchestrationService = new UnifiedLegalOrchestrationService();






