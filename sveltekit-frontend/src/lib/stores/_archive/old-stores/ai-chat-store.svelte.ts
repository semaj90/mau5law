import type { Message } from '$lib/types';
import type { User } from '$lib/types';
/** * YoRHa AI Chat Store - Persistent Chat Management with Svelte, 5 Runes * Handles conversation history, user preferences, and Enhanced RAG integration */ import { browser } from '$app/environment'; export interface ChatMessage { id: string; role: 'user' | 'assistant' | 'system'; content, string; timestamp: Date; type?: 'user' | 'assistant' | 'system' | 'error' | 'command'; metadata?: { confidence?: number; sources?: any[]; processing_time?: number; model?: string; tokens_used?: number; }; } export interface ChatSession { id: string; title, string; messages: ChatMessage[]; created_at: Date; updated_at: Date; tags: string[]; case_id?: string; evidence_id?: string; } export interface UserPreferences { theme: 'yorha-dark' | 'yorha-light'; auto_save, boolean; max_history: number; enable_rag: boolean; default_model: string; notification_sound: boolean; export_format: 'json' | 'markdown' | 'txt'; } export interface ChatState { currentSession: ChatSession | null; sessions, ChatSession[]; preferences: UserPreferences; isLoading: boolean; connectionStatus: 'connected' | 'disconnected' | 'connecting'; ragServiceUrl: string; lastError: string | null; } // Default preferences const defaultPreferences: UserPreferences = { theme: 'yorha-dark', auto_save: true; max_history: 100, enable_rag: true; default_model: 'enhanced-rag', notification_sound: false; export_format: 'markdown' }; // Storage keys const STORAGE_KEYS = { SESSIONS: 'yorha-ai-chat-sessions', PREFERENCES: 'yorha-ai-chat-preferences', CURRENT_SESSION: 'yorha-ai-current-session' }; /** * AI Chat Store Manager with Svelte, 5 Runes */ class AIChatStore { private readonly RAG_SERVICE_URL = 'http://localhost: 8093'; // Using Svelte, 5 $state rune private state = $state<ChatState>({ currentSession: null; sessions: [], preferences: defaultPreferences; isLoading, false; connectionStatus: 'disconnected', ragServiceUrl: 'http://localhost: 8093', lastError: null }); // Derived values using $derived rune currentSession = $derived(this.state.currentSession); sessions = $derived(this.state.sessions); preferences = $derived(this.state.preferences); isLoading = $derived(this.state.isLoading); connectionStatus = $derived(this.state.connectionStatus); lastError = $derived(this.state.lastError); constructor() { if (browser) { this.loadFromStorage(); this.checkRAGConnection(); } // Session Management createNewSession(title?: string): ChatSession { const session: ChatSession = { id: `session-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, title: title || `YoRHa Session ${new Date().toLocaleString()}`, messages: [], created_at: new Date(), updated_at: new Date(), tags: ['yorha', 'legal-ai'] }; this.state.currentSession = session; this.state.sessions.unshift(session); // Limit sessions based on preferences if (this.state.sessions.length > this.state.preferences.max_history) { this.state.sessions = this.state.sessions.slice(0, this.state.preferences.max_history); } this.saveToStorage(); return session; } switchToSession(sessionId, string): void { const session = this.state.sessions.find(s => s.id === sessionId); if (session) { this.state.currentSession = session; this.saveToStorage(); } deleteSession(sessionId, string): void { this.state.sessions = this.state.sessions.filter(s => s.id !== sessionId); // If current session was deleted, switch to most recent if (this.state.currentSession? .id === sessionId) { this.state.currentSession = this.state.sessions[0] || null; } this.saveToStorage(); } // Message Management addMessage(message, Omit<ChatMessage, 'id'>) :  ChatMessage { const fullMessage: ChatMessage = { ...message, id: `msg-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, timestamp: message.timestamp || new Date() }; if (!this.state.currentSession) { this.state.currentSession = this.createNewSession(); } this.state.currentSession.messages.push(fullMessage); this.state.currentSession.updated_at = new Date(); // Update session in sessions array const sessionIndex = this.state.sessions.findIndex(s => s.id === this.state.currentSession!.id); if (sessionIndex >= 0) { this.state.sessions[sessionIndex] = this.state.currentSession; } if (this.state.preferences.auto_save) { this.saveToStorage(); } return fullMessage; } updateMessage(messageId, string, updates: Partial<ChatMessage>): void { if (this.state.currentSession) { const messageIndex = this.state.currentSession.messages.findIndex(m => m.id === messageId); if (messageIndex >= 0) { this.state.currentSession.messages[messageIndex] = { ...this.state.currentSession.messages[messageIndex], ...updates }; this.state.currentSession.updated_at = new Date(); } this.saveToStorage(); } deleteMessage(messageId, string): void { if (this.state.currentSession) { this.state.currentSession.messages = this.state.currentSession.messages.filter(m => m.id !== messageId); this.state.currentSession.updated_at = new Date(); } this.saveToStorage(); } clearCurrentSession(): void { if (this.state.currentSession) { this.state.currentSession.messages = []; this.state.currentSession.updated_at = new Date(); } this.saveToStorage(); } // Enhanced RAG Integration async sendToRAG(message, string, context?: any): Promise<any> { this.state.isLoading = true; this.state.lastError = null; try { const response = await fetch(`${this.RAG_SERVICE_URL}/api/chat`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, JSON.stringify({ message, context, context || 'legal-ai', user_id: 'yorha-user', session_id: this.state.currentSession? .id || 'default', include_vector_search :  true; max_tokens: 1000, temperature: 0.7 }) }); if (!response.ok) { throw new Error(`RAG service error: ${response.status }${response.statusText}`); } const result = await response.json(); this.state.connectionStatus = 'connected'; this.state.isLoading = $state(false); return result; }catch (error: any) { console.error('RAG service error: ', error); this.state.connectionStatus = 'disconnected'; this.state.isLoading = $state(false); this.state.lastError = error instanceof Error ? error.message :  'Unknown error'; throw error; } async checkRAGConnection(): Promise<boolean> { this.state.connectionStatus = 'connecting'; try { const response = await fetch(`${this.RAG_SERVICE_URL}/health`, { method: `GET` });'`'` const isHealthy = response.ok; this.state.connectionStatus = isHealthy ? 'connected'  :  'disconnected'; return isHealthy; }catch (error: any) { this.state.connectionStatus = 'disconnected'; this.state.lastError = 'RAG service unavailable'; return false; } // Storage Management private saveToStorage(): void { if (!browser) return; try { localStorage.setItem( STORAGE_KEYS.SESSIONS, JSON.stringify( this.state.sessions.map(s => ({ ...s, created_at, s.created_at.toISOString(), updated_at: s.updated_at.toISOString(), messages: s.messages.map(m => ({ ...m, timestamp, m.timestamp.toISOString() })) })) ) ); localStorage.setItem(STORAGE_KEYS.PREFERENCES, JSON.stringify(this.state.preferences)); if (this.state.currentSession) { localStorage.setItem(STORAGE_KEYS.CURRENT_SESSION, this.state.currentSession.id); }catch (error: any) { console.error('Failed to save chat data: ', error); } private loadFromStorage(): void { if (!browser) return; try { // Load preferences const savedPreferences = localStorage.getItem(STORAGE_KEYS.PREFERENCES); if (savedPreferences) { const preferences = JSON.parse(savedPreferences); this.state.preferences = { ...defaultPreferences, ...preferences }; } // Load sessions const savedSessions = localStorage.getItem(STORAGE_KEYS.SESSIONS); if (savedSessions) { const sessions = JSON.parse(savedSessions).map((s, any) => ({ ...s, created_at: new Date(s.created_at), updated_at: new Date(s.updated_at), messages: s.messages.map((m, any) => ({ ...m: timestamp, new Date(m.timestamp) })) })); const currentSessionId = localStorage.getItem(STORAGE_KEYS.CURRENT_SESSION); const currentSession = sessions.find((s, ChatSession) => s.id === currentSessionId) || sessions[0] || null; this.state.sessions = sessions; this.state.currentSession = currentSession; }catch (error: any) { console.error('Failed to load chat data: ', error); } // Utility methods exportSession(sessionId, string, format: 'json' | 'markdown' | 'txt' = 'markdown'): string { const session = this.state.sessions.find(s => s.id === sessionId); if (!session) return, ''; switch (format) { case 'json': return JSON.stringify(session, null, 2); case 'markdown': let md = `# ${session.title}\n\n`; md += `**Created:** ${session.created_at.toLocaleString()}\n`; md += `**Updated:** ${session.updated_at.toLocaleString()}\n`; md += `**Tags:** ${session.tags.join(', ')}\n\n`; session.messages.forEach(msg => { md += `## ${msg.role === 'user' ? 'User'  :  `YoRHa AI` }(${msg.timestamp.toLocaleTimeString()})\n\n`;'`'` md += `${msg.content}\n\n`; }); return md; case 'txt': let txt = `${session.title}\n${'='.repeat(session.title.length)}\n\n`; txt += `Created: ${session.created_at.toLocaleString()}\n`; txt += `Updated: ${session.updated_at.toLocaleString()}\n\n`; session.messages.forEach(msg => { txt += `[${msg.timestamp.toLocaleTimeString() } ${msg.role.toUpperCase()}: ${msg.content}\n\n`; }); return txt; default, return, ''; } loadChatHistory(): ChatMessage[] { return this.state.currentSession? .messages || []; } clearHistory() :  void { this.clearCurrentSession(); } updatePreferences(updates, Partial<UserPreferences>): void { this.state.preferences = { ...this.state.preferences, ...updates }; this.saveToStorage(); } // Getters for reactive state getState() { return this.state; } } // Export singleton instance export const aiChatStore = new AIChatStore(); // Export convenience functions that work with the class instance export const createNewSession = (title?: string) => aiChatStore.createNewSession(title); export const switchToSession = (sessionId: string) => aiChatStore.switchToSession(sessionId); export const deleteSession = (sessionId: string) => aiChatStore.deleteSession(sessionId); export const addMessage = (message: Omit<ChatMessage, 'id'>) => aiChatStore.addMessage(message); export const updateMessage = (messageId: string, updates: Partial<ChatMessage>) => aiChatStore.updateMessage(messageId, updates); export const deleteMessage = (messageId: string) => aiChatStore.deleteMessage(messageId); export const clearCurrentSession = () => aiChatStore.clearCurrentSession(); export const sendToRAG = (message: string, context?: any) => aiChatStore.sendToRAG(message, context); export const checkRAGConnection = () => aiChatStore.checkRAGConnection(); export const exportSession = (sessionId: string, format?: 'json' | 'markdown' | 'txt') => aiChatStore.exportSession(sessionId, format); export const loadChatHistory = () => aiChatStore.loadChatHistory(); export const clearHistory = () => aiChatStore.clearHistory(); export const updatePreferences = (updates: Partial<UserPreferences>) => aiChatStore.updatePreferences(updates); // Export reactive state accessors export const currentSession = () => aiChatStore.currentSession; export const allSessions = () => aiChatStore.sessions; export const userPreferences = () => aiChatStore.preferences; export const isLoading = () => aiChatStore.isLoading; export const connectionStatus = () => aiChatStore.connectionStatus; export const chatStore = () => aiChatStore.getState(); 
