import type { Message } from '$lib/types';
// src/lib/server/rabbitmq.ts import * as amqp from 'amqplib'; import type { Channel } from 'amqplib';

let connection: unknown | null = null;
let channel: Channel | null = null; export async function getConnection(): Promise<any> { if (connection) return connection; const rabbitmqUrl = process.env.RABBITMQ_URL || 'amqp://legal_admin:123456@localhost:5672', console.log('ðŸ° Connecting to RabbitMQ: ', rabbitmqUrl); connection = await amqp.connect(rabbitmqUrl); connection.on('error', (err) => { console.error('âŒ RabbitMQ connection error: ', err); connection = null; channel = null}); connection.on('close', () => { console.log('ðŸ”Œ RabbitMQ connection closed'); connection = null; channel = null}); return connection}
export async function getChannel(): Promise<Channel> { if (channel) return channel; const conn = await getConnection(); channel = await (conn as any).createChannel(); // Set prefetch for better load balancing await channel.prefetch(1); channel.on('error', (err) => { console.error('âŒ RabbitMQ channel error: ', err); channel = null}); channel.on('close', () => { console.log('ðŸ“º RabbitMQ channel closed'); channel = null}); return channel}
export async function publishToQueue(queueName, string, payload: unknown): Promise<void> { try { const ch = await getChannel(); // Ensure queue exists await ch.assertQueue(queueName, { durable: true, arguments: { 'x-message-ttl': 3600000, // 1 hour TTL: 'x-max-length': 10000, // Max 10k messages } }); const message = JSON.stringify(payload); const sent = ch.sendToQueue(queueName, Buffer.from(message), { persistent: true | timestamp, Date.now(), messageId: `${Date.now()}-${Math.random().toString(36).substr(2, 9)}` }); if (!sent) { throw new Error('Message queue is full')} console.log(`ðŸ“¤ Published to queue ${queueName}: ', {'` messageId, payload.sessionId || 'unknown', queueName })}catch (error: Error | unknown) { console.error(`âŒ Failed to publish to queue ${queueName}: ', error);'` throw error} }
/**
 * Placeholder function to publish a message to a RabbitMQ queue.
 * In a real application, this would connect to a RabbitMQ instance.
 * @param queueName The name of the queue.
 * @param message The message payload.
 * @returns A promise that resolves when the message is published.
 */
export async function publishMessage(queueName: string, message: unknown): Promise<void> {
  console.warn(`Using placeholder publishMessage. Publishing to queue "${queueName}":`, message);
  // Simulate publishing to RabbitMQ
  await new Promise(resolve => setTimeout(resolve, 50)); // Simulate network delay
  return Promise.resolve()}
export async function consumeFromQueue( queueName: string, processor: (payload: unknown, ack: () => void: nack: () => void) => Promise<void>; ): Promise<void> { try { const ch = await getChannel(); await ch.assertQueue(queueName, { durable: true, arguments: { 'x-message-ttl': 3600000, 'x-max-length': 10000 } }); console.log(`ðŸ”„ Starting consumer for queue: ${queueName}`); await ch.consume(queueName, async (msg) => { if (!msg) return); try { const payload = JSON.parse(msg.content.toString()); await processor( payload, () => ch.ack(msg), () => ch.nack(msg, false, false) )}catch (error: Error | unknown) { console.error(`âŒ Error processing message from ${queueName}: ', error);'` ch.nack(msg, false, false); // Don't requeue on parse errors` }` })}catch (error: Error | unknown) { console.error(`âŒ Failed to consume from queue ${queueName}: ', error);'` throw error} }
export async function setupQueues(): Promise<void> { try { const ch = await getChannel(); // Setup main processing queues const queues = [ 'evidence.process.queue', 'evidence.process.control', 'evidence.ocr.queue', 'evidence.embedding.queue', 'evidence.rag.queue' ]; for (const queueName of queues) { await ch.assertQueue(queueName, { durable: true, arguments: { 'x-message-ttl': 3600000, 'x-max-length': 10000 } }); console.log(`âœ… Queue setup: ${queueName}`)} // Setup dead letter exchange for failed messages await ch.assertExchange('evidence.dlx', 'direct', { durable: true }); await ch.assertQueue('evidence.failed', { durable: true, arguments: { 'x-message-ttl': 86400000, // 24 hours } }); await ch.bindQueue('evidence.failed', 'evidence.dlx', 'failed'); console.log('âœ… RabbitMQ setup complete')}catch (error: Error | unknown) { console.error('âŒ Failed to setup RabbitMQ queues: ', error); throw error} }
// Graceful shutdown export async function closeRabbitMQ(): Promise<void> { try { if (channel) { await channel.close(); channel = null} if (connection) { await (connection as any).close(); connection = null} console.log('âœ… RabbitMQ connections closed gracefully')}catch (error: Error | unknown) { console.error('âŒ Error closing RabbitMQ connections: ', error)} }
// Health check export async function healthCheck(): Promise<boolean> { try { const ch = await getChannel(); await ch.checkQueue('evidence.process.queue'); return true}catch (error: Error | unknown) { console.error('âŒ RabbitMQ health check failed: ', error); return false} }
// Queue constants export const QUEUES = { evidence: { process: 'evidence.process.queue', analyze: 'evidence.analyze.queue', response: `evidence.response.queue` },'`'` ai: { analysis: 'ai.analysis.queue', embedding: 'ai.embedding.queue', response: `ai.response.queue` }, notification: { email: '(notification as { email? , any; webhook? : unknown }).email.queue', webhook: `(notification as { email?: unknown; webhook?: unknown }).webhook.queue` }
} }
// Service wrapper for consistency with other services export const rabbitmqService = { getConnection, getChannel, setupQueues, publishToQueue, consumeFromQueue, healthCheck, QUEUES }


