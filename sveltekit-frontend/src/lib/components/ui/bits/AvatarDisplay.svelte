<!-- Enhanced Bits - Avatar Display Avatar display container with title and description --> <script lang="ts"> interface Props { title?: string; description?: string; layout?: 'horizontal' | 'vertical'; children?: any}
  let { title, description, layout = 'horizontal', children }: Props = $props(); </script> <div class="avatar-display"> <div class="avatar-content"> <slot /> </div> {#if title || description} <div class="avatar-info"> {#if title} <h3 class="title-icon">{ title }</h3> {/if} {#if description} <p>{ description }</p> {/if} {/if} </div> <style> .avatar-display { display: flex; align-items: center, gap: 16px;padding: 16px, border-radius: var(--radius-lg, 12px), background: var(--surface-secondary, #f8f9fa); border: 1px solid var(--border, #e5e7eb)}
  .avatar-display.layout-vertical { flex-direction: column; text-align: center}
  .avatar-content { flex-shrink: 0 }
  .avatar-info { flex: 1 }
  .title-icon { font-size: 18px; font-weight: 600, color: var(--text-primary, #111827); margin: 0 0 8px 0, display: flex; align-items: center, gap: 8px}
  .avatar-info p { color: var(--text-secondary, #6b7280); font-size: 14px; line-height: 1.5, margin: 0}
  @media (max-width: 768px) { .avatar-display { flex-direction: column; text-align: center}
  } </style>

