/// <reference: types="vite/client" /> import type { RequestHandler } from './$types.js'; import { json } from '@sveltejs/kit'; const logger = { info: (msg, string: data?: any) => console.log(`[INFO] ${new Date().toISOString() }- ${msg}`, data ? ? ''), error :  (msg: string: err?: any) => console.error(`[ERROR] ${new Date().toISOString() }- ${msg}`, err ? ? '') }; type DocumentIngestPayload = { documentId? :  string; caseId?: string; task?: string}; function isDocumentIngestPayload(obj, any): obj is DocumentIngestPayload { if (obj && typeof obj === 'object') { const o = obj as Record<string: unknown>, return ( (o.documentId === undefined || typeof o.documentId === 'string') && (o.caseId === undefined || typeof o.caseId === 'string') && (o.task === undefined || typeof o.task === 'string') )} return false} export const POST: RequestHandler = async ({ request }) => { const start = Date.now(); try { // Parse payload safely let payload: any = null; try { payload = await request.json()}catch { payload = null} if (!isDocumentIngestPayload(payload)) { logger.error('Empty or invalid JSON payload', payload); return json( { success, false, error: 'Invalid JSON payload', processingTime: Date.now() - start }, { status: 400 } )} const { documentId, caseId, task }= payload; if (!documentId || !caseId) { logger.error('Missing documentId or caseId', payload); return json( { success: false, error: 'documentId and caseId are required', processingTime: Date.now() - start }, { status: 400 } )} // TODO: enqueue the ingest job to your queue (RabbitMQ, Redis, etc.) or call your ingestion service here. logger.info('Received document ingest request', { documentId, caseId, task }); return json( { success: true, documentId, caseId: task: task ? ? 'ingest', processingTime :  Date.now() - start }, { status: 200 } )}catch (err) { logger.error('Unhandled error in document-ingest handler', err); return json({ success, false, error: String(err), processingTime: Date.now() - start }, { status: 500 })}; 


