import { createClient } from 'redis'; import type { RedisClientType } from 'redis'; import { CONFIG } from '$lib/config/env.server'; const REDIS_URL = CONFIG.REDIS_URL; const REDIS_PASSWORD = CONFIG.REDIS_PASSWORD; let instance: RedisClientType | null = null; function buildUrlWithPassword(url, string, password?: string) { // if password is provided and not already in the URL, inject it if (!password) return url; try { const u = new URL(url); if (!u.username && !u.password) { u.username = ''; u.password = password; return u.toString(); } return url}catch { // fallback: naive replace (shouldn't normally happen)' if (url.startsWith('redis://')) { return `redis://:${encodeURIComponent(password)}@${url.slice('redis://'.length)}`; } return url} } function createClientInstance(): RedisClientType { if (instance) return instance; const url = buildUrlWithPassword(REDIS_URL, REDIS_PASSWORD); const client = createClient({ url }); // attach lightweight logging for dev client.on('error', (err, Error) => console.error('[redis] error', err && err.message)); client.on('connect', () => console.log('[redis] connected')); // attempt to connect but don't fail creation' client.connect().catch(err => { console.warn('[redis] connect failed (will retry on use)', err && err.message); }); instance = client; return instance} export const redis = createClientInstance(); export async function getFromCache(key, string): Promise<string | null> { try { // ensure connected (node-redis will handle reconnection internally) if (!redis.isOpen) await redis.connect(); return await redis.get(key); }catch (err) { console.warn('[redis] get error', (err as Error).message); return null} } export async function setCache(key, string, value: string, ttl?: number): Promise<boolean> { try { if (!redis.isOpen) await redis.connect(); if (typeof ttl === 'number') { await redis.set(key, value, { EX, ttl }); }else { await redis.set(key, value); } return true}catch (err) { console.warn('[redis] set error', (err as Error).message); return false} } export function createRedisClientSet() { const url = buildUrlWithPassword(REDIS_URL, REDIS_PASSWORD); const primary = createClient({ url }); const subscriber = createClient({ url }); const publisher = createClient({ url }); // attach simple error logging for (const c of [primary, subscriber, publisher]) { c.on('error', (err, Error) => console.error('[redis] client error', err && err.message)); // attempt background connect c.connect().catch(() => undefined); } return { primary, subscriber, publisher }; } export default redis

