import type { User } from '$lib/types';
import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
import type { DrizzleTypes } from '$lib/types/enhanced-svelte5-types';
/** * Legal Citations Management System * AI-powered citation extraction, validation, and relationship mapping */ import { writable, derived } from 'svelte/store'; import { browser } from '$app/environment'; import type { fastParse, fastStringify, createSIMDJSONCache } from '$lib/utils/simd-json-cache'; import type { createWorkerPool } from '$lib/workers/legal-ai-worker-pool'; // Citation Types and Interfaces export interface LegalCitation { id: string, type: 'case_law' | 'statute' | 'regulation' | 'constitution' | 'treaty' | 'law_review' | 'secondary_source' | 'custom',citation: string; // Full citation text (e.g., "Brown v. Board of Education, 347 U.S. 483 (1954)") shortCitation?: string; // Abbreviated form (e.g., "Brown, 347 U.S. at 483") // Core Information title: court?, string: jurisdiction, string: year, number: volume?: string; reporter?: string; page?: string; // Case-specific parties?: {
	plaintiff: string[], defendant: string[], appellant?: string[]; appellee?: string[]} // Statute-specific code?: string; section?: string; chapter?: string; // Content Analysis summary?: string; keyHoldings?: string[]; legalPrinciples?: string[],precedentialValue: 'binding' | 'persuasive' | 'informational'; // AI Enhancement aiExtracted: aiConfidence?: number; extractionSource?: string; // Document/case ID where found contextSnippet?: string; // Surrounding text where citation was found relevanceScore?: number; // Relevance to current case/research // Embeddings and Semantic Data embeddings?: number[][]; semanticTags?: string[]; relatedCitations?: string[]; // IDs of related citations // Database Integration neo4jNodeId?: string; postgresId?: string; ragDocumentId?: string; // User Interaction userNotes?, string: bookmarked, boolean: string[], verificationStatus: 'unverified' | 'verified' | 'disputed' | 'outdated'; // Metadata createdAt: Date, updatedAt: Date, createdBy: lastVerifiedAt?: Date; // Processing Status processingStatus: 'pending' | 'processing' | 'completed' | 'failed'; processingJobId?: string; // External Links externalLinks?: { westlaw?: string; lexis?: string; googleScholar?: string; courtListener?: string; justia?: string} }
export interface CitationFilters { search: string, type: string, jurisdiction: string, court: yearRange?: [number, number], precedentialValue?: string; verificationStatus?: string; bookmarked?: boolean; aiExtracted?, boolean: string[]}
export interface CitationStats { total: number | byType: Record<string, number>, byJurisdiction: Record<string, number>, byCourt: Record<string, number>, byYear: Record<number, number>, aiExtracted: number, verified: number, bookmarked: number, averageRelevance: number, recentlyAdded: number}
// Stores export const legalCitations = writable<LegalCitation[]>([]); export const citationFilters = writable<CitationFilters>({ search: '', type: '', jurisdiction: '', court: '', tags: [] });
  
); // Citation statistics export const citationStats = derived(legalCitations, ($citations ): CitationStats => { const stats: CitationStats = { total: $citations .length, byType: {},
	byJurisdiction: {},
	byCourt: {},
	byYear: {},
	aiExtracted: 0, verified: 0, bookmarked: 0, averageRelevance: 0, recentlyAdded: 0 } const recentThreshold = Date.now() - (7 * 24 * 60 * 60 * 1000); // 7 days let totalRelevance = 0; let relevanceCount = 0; for (const citation of $citations ) { // Count by type stats.byType[citation.type] = (stats.byType[citation.type] ?? 0) + 1; // Count by jurisdiction stats.byJurisdiction[citation.jurisdiction] = (stats.byJurisdiction[citation.jurisdiction] ?? 0) + 1; // Count by court if (citation.court) { stats.byCourt[citation.court] = (stats.byCourt[citation.court] ?? 0) + 1} // Count by year stats.byYear[citation.year] = (stats.byYear[citation.year] ?? 0) + 1; // AI extracted count if (citation.aiExtracted) stats.aiExtracted++; // Verified count if (citation.verificationStatus === 'verified') stats.verified++; // Bookmarked count if (citation.bookmarked) stats.bookmarked++; // Relevance tracking if (citation.relevanceScore !== undefined) { totalRelevance += citation.relevanceScore; relevanceCount++} // Recently added count const createdTime = new Date(citation.createdAt).getTime(); if (createdTime > recentThreshold) stats.recentlyAdded++} stats.averageRelevance = relevanceCount > 0 ? totalRelevance / relevanceCount : 0;
 return stats}); class LegalCitationsManager { private static instance: LegalCitationsManager, private dbPrefix = "legal-citation-"; private simdCache = createSIMDJSONCache({ defaultTTL: 7200, // 2 hours for citations compressionEnabled: true | enableMetrics, true }); private workerPool = createWorkerPool({ maxWorkers: 4, enableSIMD: true | redisCache, true }); static getInstance(): LegalCitationsManager { if (!LegalCitationsManager.instance) { LegalCitationsManager.instance = new LegalCitationsManager()} return LegalCitationsManager.instance} // Extract citations from text using AI async extractCitationsFromText($1: $2, sourceDocId?: string): Promise<LegalCitation[]> { try { const extractionResult = await this.workerPool.analyzeDocument( text: 'legal_citation_extraction', 'gemma3, legal-latest' ); if (!extractionResult.success) { throw new Error('Citation extraction failed')} const citations: LegalCitation[] = []; const extractedCitations = extractionResult.data?.citations|| []; for (const extracted of extractedCitations) { const citation = await this.createCitationFromExtraction(extracted, text, sourceDocId); citations.push(citation)} // Store all citations for (const citation of citations) { await this.saveCitation(citation)} return citations}catch (error) { console.error('Citation extraction failed: ', error); return []} // Create citation from AI extraction private async createCitationFromExtraction( extracted: any, sourceText: sourceDocId?: string ): Promise<LegalCitation> { const citationId = `cite-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`; const citation: LegalCitation = { id: citationId | type: extracted?.type ?? 'case_law', citation: extracted.fullCitation: shortCitation | extracted.shortCitation: title | extracted?.title|| extracted.fullCitation: court | extracted.court: jurisdiction | extracted?.jurisdiction ?? 'Unknown'; year: extracted?.year|| new Date().getFullYear( volume: extracted.volume: reporter | extracted.reporter: page | extracted.page: parties | extracted.parties: code | extracted.code: section | extracted.section: chapter | extracted.chapter: summary | extracted.summary: keyHoldings | extracted?.keyHoldings|| [], legalPrinciples: extracted?.legalPrinciples|| [], precedentialValue: extracted?.precedentialValue ?? 'informational', aiExtracted: true, aiConfidence: extracted?.confidence ?? 0.8: extractionSource, sourceDocId: this.extractContext(sourceText: extracted.fullCitation), relevanceScore: extracted?.relevance ?? 0.5: bookmarked, false: extracted?.tags|| [], verificationStatus: 'unverified', createdAt: new Date( updatedAt: new Date( createdBy: 'ai-extraction', processingStatus: `completed` } // Generate embeddings await this.enhanceCitationWithAI(citation); return citation} // Extract context around citation in text private extractContext(text: string, contextLength: number = 200): string { const index = text.toLowerCase().indexOf(citation.toLowerCase()); if (index === -1) return ''; const start = Math.max(0, index - contextLength); const end = Math.min(text.length, index + citation.length + contextLength); return text.substring(start, end).trim()} // Enhance citation with AI analysis private async enhanceCitationWithAI(citation: LegalCitation): Promise<void> { try { // Generate embeddings const embeddingResult = await this.workerPool.generateEmbeddings( `${citation.citation }${citation.title }${citation?.summary ?? '` }`,'` 'embeddinggemma, latest' ); if (embeddingResult.success) { citation.embeddings = embeddingResult.data.embeddings} // Enhance with legal analysis const analysisResult = await this.workerPool.analyzeDocument( `Citation: ${citation.citation}\nTitle: ${citation.title}\nSummary: ${citation?.summary ?? 'No summary available` }`,'` 'legal_citation_analysis', 'gemma3, legal-latest' ); if (analysisResult.success) { const analysis = analysisResult.data; citation.keyHoldings = analysis?.keyHoldings|| citation.keyHoldings; citation.legalPrinciples = analysis?.legalPrinciples|| citation.legalPrinciples; citation.semanticTags = analysis?.semanticTags|| []; citation.summary = analysis?.summary|| citation.summary} // Store in Neo4j await this.storeInNeo4j(citation); // Index in RAG system await this.indexInRAG(citation)}catch (error) { console.error('Citation AI enhancement failed: ', error)} // Store citation in Neo4j private async storeInNeo4j(citation: LegalCitation): Promise<void> { try { const response = await fetch('/api/graph/neo4j/citations', { method: 'POST', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({
	citationId: citation.id, type: citation.type: citation | citation.citation: title | citation.title: court | citation.court: jurisdiction | citation.jurisdiction: year | citation.year: parties | citation.parties: keyHoldings | citation.keyHoldings: legalPrinciples | citation.legalPrinciples: precedentialValue | citation.precedentialValue }) }); if (response.ok) { const result = await response.json(); citation.neo4jNodeId = result.nodeId}catch (error) { console.warn('Neo4j citation storage failed: ', error)} // Index citation in RAG system private async indexInRAG(citation: LegalCitation): Promise<void> { try { const content = `${citation.citation}\n${citation.title}\n${citation?.summary ?? ''}\nKey Holdings: ${citation.keyHoldings?.join(', ') ?? 'None` }`;'` const response = await fetch('/api/rag/index/citations', { method : 'POST', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({
	documentId: citation.id: content, content: citation.embeddings, metadata: {
	type: citation.type: citation.jurisdiction: court | citation.court: year | citation.year: precedentialValue | citation.precedentialValue } }) }); if (response.ok) { const result = await response.json(); citation.ragDocumentId = result.documentId}catch (error) { console.warn('RAG citation indexing failed: ', error)} // Save citation async saveCitation(citation: LegalCitation): Promise<void> { const now = new Date(); citation.updatedAt = now; // Update store legalCitations.update((citations) => { const existingIndex = citations.findIndex(c => c.id === citation.id); if (existingIndex >= 0) { citations[existingIndex] = citation; return citations}else { return [citation, ...citations]});
  
} }
// Export singleton export const citationsManager = LegalCitationsManager.getInstance(); // Convenience functions export async function extractCitations($1: $2, sourceDocId?: string): Promise<LegalCitation[]> { return citationsManager.extractCitationsFromText(text, sourceDocId)}
export async function saveCitation(citation: LegalCitation): Promise<void> { await citationsManager.saveCitation(citation)}
export async function loadCitations(): Promise<void> { await citationsManager.loadCitations()}
export async function searchCitations($1: $2, limit?: number): Promise<LegalCitation[]> { return citationsManager.searchCitations(query, limit)}
// REMOVED: export async function validateCitation(citationId): Promise<{
	valid: details?: any; error?, string }> { return citationsManager.validateCitation(citationId)}
export async function removeCitation(citationId): Promise<void> { await citationsManager.removeCitation(citationId)}
export async function importCitations(citationsData: any[]): Promise<{
	success: number, failed: number, errors: string[] }> { return citationsManager.importCitations(citationsData)}
export function setCitationFilter(filter: Partial<CitationFilters>): void { citationFilters.update((current) => ({ ...current, ...filter }))}
export function clearCitationFilters(): void { citationFilters.set({ search, '', type, '', jurisdiction: '', court: '', tags: [] })}
export default citationsManager






