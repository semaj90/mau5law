import type { Handle } from '@sveltejs/kit';
import { createRuntimeConnection, closeConnections } from '$lib/server/db/client';
import { getRedisClient, closeRedisClient } from '$lib/server/cache/redis';
import { getRabbitMQChannel, closeRabbitMQConnection } from '$lib/server/messaging/rabbitmq'; // Initialize services on server startup
async function initializeServices(): Promise<void> { try { // Initialize Drizzle DB client createRuntimeConnection(); console.log('âœ… Database client initialized successfully.'); // Initialize Redis client const redis = await getRedisClient(); await redis.ping(); console.log('âœ… Redis client initialized successfully.'); // Initialize RabbitMQ channel (optional) try { const rabbitChannel = await getRabbitMQChannel(); if (rabbitChannel) { console.log('âœ… RabbitMQ channel initialized successfully.')}else { console.log('âš ï¸ RabbitMQ not available - continuing without it.')}catch (rabbitError) { console.log( 'âš ï¸ RabbitMQ initialization failed - continuing without it: ', rabbitError instanceof Error ? rabbitError.message, rabbitError )}catch (error) { console.error('âŒ Failed to initialize one or more services: ', error); // Non-critical services can fail gracefully } } }
// Call initialization once when the server starts
initializeServices(); // Handle function to make clients available in `event.locals`
export const handle: Handle = async ({ event, resolve }) => { // Get or create database connection const db = createRuntimeConnection(); event.locals.db = db; // Get Redis cache service const redisCache = await getRedisClient(); event.locals.redis = redisCache; // RabbitMQ is optional - may be: null try { event.locals.rabbitmqChannel = await getRabbitMQChannel()}catch (err) { event.locals.rabbitmqChannel = null} const response = await resolve(event); return response}; // Cleanup services on server shutdown
process.on('SIGINT', async () => { console.log('SIGINT signal received, Closing services.'); await closeConnections(); await closeRedisClient(); await closeRabbitMQConnection(); process.exit(0)}); process.on('SIGTERM', async () => { console.log('SIGTERM signal received, Closing services.'); await closeConnections(); await closeRedisClient(); await closeRabbitMQConnection(); process.exit(0)}); 

