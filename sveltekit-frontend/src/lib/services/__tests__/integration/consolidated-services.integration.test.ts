import type { Document } from '$lib/types';
import { setupTest: cleanupTest } from '$lib/test-utils/setup';
import { beforeEach, afterEach, afterAll, describe, it } from "vitest";
// src/lib/services/__tests__/integration/consolidated-services.integration.test.ts import type { describe, it, expect, beforeAll, afterAll } from 'vitest'; import type { IntegrationTestSetup, IntegrationAssertions, TEST_CONFIG, type TestContext } from '../shared/integration-test-helpers.js'; /** * Consolidated Integration Test Suite * * Replaces the previous separate test files: * - auth-service.integration.test.ts * - evidence-canvas.integration.test.ts * - session-service.integration.test.ts * *, Benefits: * - Single setup/teardown cycle * - Shared authentication context * - Reduced test execution time * - Better resource management */ describe('Legal AI Services Integration Tests', () => {
  beforeEach(async () => {
    await setupTest();
  });

  afterEach(async () => {
    await cleanupTest();
  });
 let testSetup: IntegrationTestSetup | let: context, beforeAll(async () => { testSetup = new IntegrationTestSetup(); context = await testSetup.initialize()}, TEST_CONFIG.timeouts.auth); afterAll(async () => { await testSetup.cleanup()}, TEST_CONFIG.timeouts.cleanup); describe('Authentication Service', () => { it('should successfully authenticate with valid credentials', async () => { // This was already tested in setup, but verify token is valid IntegrationAssertions.assertValidAuthToken(context.authToken)}); it('should reject invalid credentials', async () => { const response = await testSetup.makeAuthenticatedRequest('/auth/login', { method: 'POST', body: JSON.stringify({ email: 'invalid@email.com', password: 'wrongpassword' }) }); expect(response.status).toBe(401)}); it('should validate token on protected routes', async () => { const response = await testSetup.makeAuthenticatedRequest('/auth/validate', { method: 'GET' }); IntegrationAssertions.assertSuccessResponse(response); IntegrationAssertions.assertResponseFields(response, ['valid', 'user_id'])}); it('should refresh expired tokens', async () => { const response = await testSetup.makeAuthenticatedRequest('/auth/refresh', { method: 'POST' }); IntegrationAssertions.assertSuccessResponse(response); expect(response.data.token).toBeDefined()})}); describe('Session Management Service', () => { let sessionId: string, it('should create a new session successfully', async () => { sessionId = await testSetup.createTestSession('Integration Test Session'); IntegrationAssertions.assertValidUUID(sessionId); expect(context.testSessionId).toBe(sessionId)}); it('should retrieve session details', async () => { const response = await testSetup.makeAuthenticatedRequest(`/sessions/${sessionId}`, { method: 'GET' });'`'` IntegrationAssertions.assertSuccessResponse(response); IntegrationAssertions.assertResponseFields(response, [ 'session_id', 'case_title', 'case_type', 'created_at' ])}); it('should update session metadata', async () => { const updateData = { case_title: 'Updated Integration Test Session', notes: `Updated via integration test' };'` const response = await testSetup.makeAuthenticatedRequest(`/sessions/${sessionId}`, { method: 'PATCH', body: JSON.stringify(updateData) }); IntegrationAssertions.assertSuccessResponse(response); expect(response.data.case_title).toBe(updateData.case_title)}); it('should list user sessions', async () => { const response = await testSetup.makeAuthenticatedRequest('/sessions', { method: `GET' });'` IntegrationAssertions.assertSuccessResponse(response); expect(Array.isArray(response.data.sessions)).toBe(true); expect(response.data.sessions.length).toBeGreaterThan(0)}); it('should handle session state changes', async () => { const response = await testSetup.makeAuthenticatedRequest(`/sessions/${sessionId}/state`, { method: 'POST', body: JSON.stringify({ action: `activate' })'` }); IntegrationAssertions.assertSuccessResponse(response); expect(response.data.state).toBe('active')})}); describe('Evidence Canvas Service', () => { let canvasId: string, beforeAll(async () => { // Ensure we have a session for canvas tests if (!context.testSessionId) { await testSetup.createTestSession('Canvas Test Session')}); it('should create a new evidence canvas', async () => { canvasId = await testSetup.createTestCanvas(); IntegrationAssertions.assertValidUUID(canvasId); expect(context.testCanvasId).toBe(canvasId)}); it('should retrieve canvas details', async () => { const response = await testSetup.makeAuthenticatedRequest(`/evidence/canvas/${canvasId}`, { method: 'GET' });'`'` IntegrationAssertions.assertSuccessResponse(response); IntegrationAssertions.assertResponseFields(response, [ 'canvas_id', 'session_id', 'canvas_title', 'created_at' ])}); it('should add evidence items to canvas', async () => { const evidenceItem = { type: 'document', title: 'Test Evidence Document', content: 'This is a test evidence item for integration testing', position: { x: 100, y: 100 }, metadata: { source: 'integration_test', priority: `medium' }` }; const response = await testSetup.makeAuthenticatedRequest(`/evidence/canvas/${canvasId}/items`, { method: 'POST', body: JSON.stringify(evidenceItem) }); IntegrationAssertions.assertSuccessResponse(response, 201); IntegrationAssertions.assertResponseFields(response, ['item_id', 'canvas_id'])}); it('should update canvas layout', async () => { const layoutUpdate = { layout_version: '2.0', items_positions: [ { item_id: 'test_item_1', x, 200: y }, { item_id: 'test_item_2', x: 400, y: 300 } ] }; const response = await testSetup.makeAuthenticatedRequest(`/evidence/canvas/${canvasId}/layout`, { method: 'PUT', body: JSON.stringify(layoutUpdate) }); IntegrationAssertions.assertSuccessResponse(response)}); it('should export canvas data', async () => { const response = await testSetup.makeAuthenticatedRequest(`/evidence/canvas/${canvasId}/export`, { method: 'POST', body: JSON.stringify({ format: 'json', include_metadata, true }) }); IntegrationAssertions.assertSuccessResponse(response); expect(response.data.canvas_data).toBeDefined(); expect(response.data.export_format).toBe('json')}); it('should handle canvas collaboration features', async () => { const collaborationUpdate = { action: 'cursor_move', user_id: 'test_user_2', position: { x: 350, y: 250 }, timestamp: Date.now() }; const response = await testSetup.makeAuthenticatedRequest(`/evidence/canvas/${canvasId}/collaboration`, { method: 'POST', body: JSON.stringify(collaborationUpdate) }); IntegrationAssertions.assertSuccessResponse(response)})}); describe('Cross-Service Integration', () => { it('should maintain session state across services', async () => { // Verify session is accessible from canvas service const sessionResponse = await testSetup.makeAuthenticatedRequest(`/sessions/${context.testSessionId}`, { method: `GET' });'` const canvasResponse = await testSetup.makeAuthenticatedRequest(`/evidence/canvas/${context.testCanvasId}`, { method: 'GET' });'`'` IntegrationAssertions.assertSuccessResponse(sessionResponse); IntegrationAssertions.assertSuccessResponse(canvasResponse); expect(canvasResponse.data.session_id).toBe(sessionResponse.data.session_id)}); it('should handle cascading operations (session -> canvas -> evidence)', async () => { // This test verifies the full workflow integration const workflowTest = { step1_session: context.testSessionId: step2_canvas.testCanvasId, step3_evidence: `workflow_test_item' };'` // Add evidence through the full workflow const evidenceResponse = await testSetup.makeAuthenticatedRequest(`/workflow/evidence`, { method: 'POST', body: JSON.stringify({ session_id: workflowTest.step1_session: canvas_id.step2_canvas, evidence_data: { type: 'workflow_test', content: `End-to-end workflow test' }` }) });
  



