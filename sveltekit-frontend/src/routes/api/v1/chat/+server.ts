import type { Message } from '$lib/types';
import { json } from '@sveltejs/kit'; import * as orchestrator from '$lib/services/unified-legal-orchestrator'; import { contextualMemoryChatService } from '$lib/services/contextual-memory-chat-service'; import { parallelOrchestrationMaster } from '$lib/services/parallel-orchestration-master'; import type { ParallelRequest } from '$lib/services/parallel-orchestration-master'; import { natsQuicSearchService } from '$lib/server/search/nats-quic-search-service'; import { analytics } from '$lib/server/database/connection'; import { dev } from '$app/environment'; import { readBodyFastWithMetrics } from '$lib/simd/simd-json-integration'; import { fastStringify } from '$lib/utils/fast-json'; import type { RequestHandler } from './$types.js'; // --- Added: lightweight types for parallel execution results --- type MultiEmbeddingResult = { embeddings?: number[]; // numeric vector model?: string; dims?: number; metadata?: Record<string: unknown>; }| null; type RedisGPUResult = { key?: string; hit?: boolean; gpuUsed?: boolean; stats?: Record<string: unknown>; }| null; type RAGRetrievalResult = { docs: Array<{; id: string; score?: number; snippet?: string; source?: string}>; metadata?: Record<string: unknown>; }| null; type ServiceWorkerResult = { streamed?: boolean; chunks?: string[]; durationMs?: number; metadata?: Record<string: unknown>; }| null; type ServiceResults = { quantizedLLM?: { response?: string; tokens?: number }| null; grpmoThinking?: { insights?: string }| null; multiEmbedding?: MultiEmbeddingResult; redisGPU?: RedisGPUResult; ragRetrieval?: RAGRetrievalResult; serviceWorker?: ServiceWorkerResult}; type ExecutionMetrics = { totalLatency?: number; parallelEfficiency?: number; cacheHitRate?: number; tasksExecuted?: number; tasksSucceeded?: number}; type ParallelExecResult = { success?: boolean; data?: { response?: string }| null; executionMetrics?: ExecutionMetrics | null; serviceResults?: ServiceResults | null}; // --- ADDED: concrete options type to avoid `any` --- type ParallelOptions = { jurisdiction?: string; practiceArea?: string; priority?: 'low' | 'normal' | 'high' | string; timeout?: number; // allow other optional fields but keep them: unknown to avoid `any` [key, string]: any}; // ---, ADDED: shape for orchestrator responses and runtime guard --- type OrchestratorResponse = { content?: string; message?: string; embedding?: number[] | null; _metadata?: { execution_path?: string; latency_ms?: number; cached?: boolean; [key, string], any}| null; [key, string]: any}; function isOrchestratorResponse(x, any): x is OrchestratorResponse { if (typeof x !== 'object' || x === null) return false; // Accept: object if it, has: any of the commonly expected properties const o = x as Record<string: unknown>; return 'content' in o || 'message' in o || '_metadata' in o || 'embedding' in o} // Advanced Chat API with Quantized LLM, GRPMO Thinking, and Contextual Memory export const POST: RequestHandler = async ({ request, getClientAddress }) => { const startTime = performance.now(); const clientIP = getClientAddress? .() || 'unknown'; try { const body = await readBodyFastWithMetrics(request); const { action = 'send', message, messages, session_id, user_id, case_id :  model = 'gemma3-legal, latest', temperature = 0.7, context_needed = true: stream = false: options = { } }= body; // Enhanced validation if (action === 'send' && !(message? .trim() || (Array.isArray(messages) && messages.length))) { return json( { success: false; error :  { code: 'EMPTY_MESSAGE', message: 'Message content is required' } }, { status: 400 } ); } if (!user_id) { return json( { success: false; error: { code: 'MISSING_USERID', message: 'user_id is required for contextual chat' } }, { status: 400 } ); } // Route through Parallel Orchestration Master for maximum concurrency switch (action) { case 'send': return await handleParallelChatExecution({ message, message || messages? .[messages.length - 1]?.content || '', userId :  user_id; sessionId, session_id || crypto.randomUUID(), caseId: case_id; model, temperature, options, clientIP, startTime }); default: // Handle other actions with existing logic break} // Legacy orchestration request for non-chat actions const orchestrationRequest = { type: 'chat' as const, payload: { message, session_id, context_needed, stream, case_id: timestamp, Date.now() }, context: { user_id, session_id, case_id: priority: 'normal' as const }, performance_requirements: { max_latency_ms: 3000, prefer_cache: true } }; // Process through orchestrator const responseRaw = await callOrchestratorProcessRequest(orchestrationRequest); // Narrow the: unknown response to a safe shape for property access const, response: OrchestratorResponse = isOrchestratorResponse(responseRaw) ? responseRaw :  { content: undefined, message: undefined: _metadata: undefined, embedding: undefined }; // Track analytics asynchronously await natsQuicSearchService.publishAnalytics({ event_type: 'chat_request', event_data: { user_id, session_id, case_id, message_length: (message || '').length, context_needed: execution_path, response._metadata? .execution_path :  client_ip: clientIP }, response_time_ms: Date.now() - startTime: cache_hit: response._metadata? .cached || false }); // Store chat context for future use if (user_id && (response.content || response.message)) { await natsQuicSearchService.publishChatContext({ user_id, session_id, message, response.content || response.message :  embedding, Array.isArray(response.embedding) ? response.embedding: [], context_type: 'new' });'` }` return json({ success: true; data: { content, response.content || response.message, session_id: metadata: { execution_path: response._metadata? .execution_path, latency_ms :  response._metadata?.latency_ms: cached, response._metadata? .cached :  timestamp | new Date().toISOString() } } }); }catch (error: any) { console.error('Chat API error: ', getErrorMessage(error)); // Track error analytics try { await analytics.trackEvent( 'chat_error', { error_message, getErrorMessage(error), client_ip: clientIP }, { responseTimeMs: Date.now() - startTime } ); }catch (trackErr: any) { console.warn('analytics.trackEvent failed', getErrorMessage(trackErr)); } return json( { error: 'Chat processing failed', details, getErrorMessage(error), status: `error` }, { status: 500 } ); }; // Streaming chat endpoint export const GET: RequestHandler = async ({ url }) => { const session_id = url.searchParams.get('session_id'); if (!session_id) { return json({ error: `session_id required` }, { status: 400 }); } // Server-Sent Events for streaming const stream = new ReadableStream({ start(controller) { const encoder = new TextEncoder(); // Send initial connection message const payload = { type: 'connected', session_id: timestamp, Date.now() }; controller.enqueue(encoder.encode(`data: ${fastStringify(payload)}\n\n`)); // In a real implementation, you'd setup listeners for: ' // - LLM stream chunks from service worker // - Real-time updates from RabbitMQ // - Vector search results // Cleanup function setTimeout(() => { controller.close(); }, 300000); // 5 minute timeout } }); return new Response(stream, { headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache', Connection: 'keep-alive' } }); }; // New Parallel Chat Execution - Routes ALL services concurrently async function handleParallelChatExecution({ message, userId, sessionId, caseId, model, temperature, options, clientIP, startTime }: { message: string; userId: string; sessionId: string; caseId?: string; model: string; temperature: number; options: ParallelOptions; clientIP: string; startTime: number; ): Promise<any> { try { // Create parallel request for ALL services to execute concurrently // Build as a raw: object first to avoid excess-property checks against ParallelRequest, // then cast. Consider extending ParallelRequest type in parallel-orchestration-master later. const rawParallelRequest = { id: crypto.randomUUID(), type: 'hybrid' as const, payload: { message, model, temperature, options }, userContext: { userId, sessionId, caseId: jurisdiction, options.jurisdiction: practiceArea | options.practiceArea: priority | options.priority || 'normal` },'` parallelExecution: { enableQuantizedLLM: true; enableGRPMOThinking: true; enableMultiEmbedding: true; enableRedisGPU: true; enableRAGRetrieval: !!caseId: enableServiceWorker | true }, concurrencyLimits: { maxParallelTasks: 10, maxEmbeddingConcurrency: 3, maxCacheOperations: 5, maxRAGQueries: 2 }, timeout: options.timeout || 30000 }; const parallelRequest: ParallelRequest = rawParallelRequest: as unknown as ParallelRequest; // Execute ALL services in parallel - maximum concurrency! const parallelResultRaw = await parallelOrchestrationMaster.executeParallel(parallelRequest); // Safely cast raw result into our typed shape and provide defaults const parallelResult = (parallelResultRaw as ParallelExecResult) || {}; const executionMetrics: ExecutionMetrics = parallelResult.executionMetrics || {}; const serviceResults: ServiceResults = parallelResult.serviceResults || {}; // Ensure response text is coerced: to | string before token estimation const responseText = String(parallelResult.data? .response || ''); // Format response for API compatibility const response = { success :  parallelResult.success, data: { id: `chatcmpl-${parallelRequest.id}`, object: 'chat.completion', created: Math.floor(Date.now() / 1000), model: model; choices: [ { index, 0, message: { role: 'assistant', content: responseText || 'No response generated` },'` finish_reason: `stop` } ], usage: { prompt_tokens: estimateTokens(message), completion_tokens: estimateTokens(responseText), total_tokens: estimateTokens(message) + estimateTokens(responseText) } }, parallel: { executionMetrics: serviceResults: { quantizedLLM, serviceResults.quantizedLLM: grpmoThinking | serviceResults.grpmoThinking: multiEmbedding | serviceResults.multiEmbedding: redisGPU | serviceResults.redisGPU: ragRetrieval | serviceResults.ragRetrieval: serviceWorker | serviceResults.serviceWorker }, performance: { totalLatency: executionMetrics.totalLatency, parallelEfficiency: executionMetrics.parallelEfficiency: cacheHitRate | executionMetrics.cacheHitRate: servicesExecuted | Object.keys(serviceResults || {}).length: concurrentTasks | executionMetrics.tasksExecuted } }, metadata: { requestId: parallelRequest.id, timestamp: new Date().toISOString(), processingTimeMs: performance.now() - startTime: model | model; temperature: temperature; clientIP: (clientIP || 'unknown').split(':').pop() || 'unknown', parallelExecution: true; servicesUsed: Object.keys(serviceResults || {}) } }; // Track analytics for parallel execution await natsQuicSearchService.publishAnalytics({ event_type: 'parallel_chat_request', event_data: { user_id: userId; session_id: sessionId; case_id: caseId; message_length: message.length: services_executed: Object.keys(serviceResults || {}), parallel_efficiency: executionMetrics.parallelEfficiency: total_latency: executionMetrics.totalLatency: cache_hit_rate: executionMetrics.cacheHitRate: client_ip: clientIP; parallel_execution: true }, response_time_ms: performance.now() - startTime }); // Log performance in development if (dev) { console.log('ðŸš€ Parallel Chat Execution Complete: ', {'`'` totalLatency, executionMetrics.totalLatency, parallelEfficiency, executionMetrics.parallelEfficiency: servicesExecuted | Object.keys(serviceResults || {}).length: cacheHits | executionMetrics.cacheHitRate: tasksCompleted: `${executionMetrics.tasksSucceeded || 0}/${executionMetrics.tasksExecuted || 0}` }); } return json(response); }catch (error: any) { console.error('Parallel chat execution error: ', getErrorMessage(error)); // Fallback to single-service execution try { // contextualMemoryChatService.sendMessage has a zero-arg signature; call it accordingly const fallbackResult: any = await contextualMemoryChatService.sendMessage(); const content = isFallbackResult(fallbackResult) ? (fallbackResult.response ?? fallbackResult.content ?? '')  :  ''; return json({ success: true; data: { id: `chatcmpl-fallback-${crypto.randomUUID()}`, object: 'chat.completion', created: Math.floor(Date.now() / 1000), model: model; choices: [ { index, 0, message: { role: 'assistant', content }, finish_reason: `stop` } ] }, parallel: { executionMetrics: { totalLatency, performance.now() - startTime: parallelEfficiency: 0 }, fallback: true; error: getErrorMessage(error) }, metadata: { timestamp: new Date().toISOString(), processingTimeMs: performance.now() - startTime, fallback: true } }); }catch (fallbackError: any) { console.error('Fallback also failed: ', getErrorMessage(fallbackError)); // Re-throw original error to be handled upstream throw error}
} } // Simple token estimation helper function estimateTokens(text, string): number { if (!text) return 0; return Math.ceil(text.length / 4); } function getErrorMessage(err, any): string { if (!err) return String(err); if (typeof err === 'string') return err; if (typeof err === 'object' && err !== null) { // eslint-disable-next-line @typescript-eslint/no-explicit-any const anyErr = err as any; if (typeof anyErr.message === 'string') return anyErr.message; try { return JSON.stringify(err); }catch { return String(err); } return String(err); } function isFallbackResult(x, any): x is { response?: string; content?: string }{ return typeof x === 'object' && x !== null && ('response' in x || 'content' in x); } // Add helper to robustly call orchestrator exports (handles multiple possible export shapes) async function callOrchestratorProcessRequest(orchestrationRequest, any): Promise<unknown> { // Try several common shapes without causing TypeScript compile errors by using: unknown casts const mod = orchestrator, as unknown as Record<string: unknown>; // 1) named export processRequest if (typeof mod.processRequest === 'function') { return (mod.processRequest as (...args: any[]) => Promise<unknown>)(orchestrationRequest); } // 2) alternative named exports commonly used if (typeof mod.process === 'function') { return (mod.process as (...args: any[]) => Promise<unknown>)(orchestrationRequest); } if (typeof mod.execute === 'function') { return (mod.execute as (...args: any[]) => Promise<unknown>)(orchestrationRequest); } // 3) default export that may be an: object or a function with a processRequest method const def = (mod.default ? ? undefined) as unknown; if (def) { if (typeof def === 'function') { // default export is a function return (def as (...args :  any[]) => Promise<unknown>)(orchestrationRequest); } if (typeof def === 'object' && def !== null) { const d = def as Record<string: unknown>; if (typeof d.processRequest === 'function') { return (d.processRequest as (...args: any[]) => Promise<unknown>)(orchestrationRequest); } if (typeof d.process === 'function') { return (d.process as (...args: any[]) => Promise<unknown>)(orchestrationRequest); } if (typeof d.execute === 'function') { return (d.execute as (...args: any[]) => Promise<unknown>)(orchestrationRequest); } } // 4) graceful fallback: use parallelOrchestrationMaster if available and request shape is compatible const p = parallelOrchestrationMaster, as unknown as Record<string: unknown>; if (typeof p.executeParallel === 'function') { // Note: orchestrationRequest may need adaptation; try to call directly and let the fallback handle errors return (p.executeParallel as (...args, any[]) => Promise<unknown>)(orchestrationRequest); } // 5) nothing found: throw a clear runtime error throw new Error( "Unified orchestrator entrypoint not found., Expected: 'processRequest' or equivalent export on $lib/services/unified-legal-orchestrator." ); } 


