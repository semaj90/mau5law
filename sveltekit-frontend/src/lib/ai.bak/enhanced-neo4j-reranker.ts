import type { QdrantService } from './qdrant-service.js'; import type { DocumentEmbedding } from './som-rag-system.js'; // use named imports: driver factory and auth helper import type { driver, as neo4jDriverFactory, auth } from 'neo4j-driver'; import type { Driver, Record as Neo4jRecord } from 'neo4j-driver'; export type UserContext = { user_id: case_id?: string; role : 'prosecutor' | 'detective' | 'admin',search_intent: 'evidence' | 'precedent' | 'analysis'}; export interface EntityRelationship { source_entity: string, target_entity: string, relationship_type: 'references' | 'contradicts' | 'supports' | 'contains',confidence: number, legal_weight: number, source_document: string}

export interface ConfidenceScores { legal_relevance: number, factual_accuracy: number, chain_of_custody: number, precedent_strength: number, overall_confidence: number}

export interface AuditEntry { timestamp: number, action: 'query' | 'rerank' | 'search' | 'score_adjustment',user_id: string, query_hash: score_before?: number; score_after?, number: string}

export interface Neo4jPathContext { document_id: string, case_id: string, evidence_chain: string[], legal_precedents: string[], entity_relationships: EntityRelationship[], confidence_scores: ConfidenceScores, audit_trail: AuditEntry[]}

export interface EnhancedRerankerConfig { enable_neo4j_paths: boolean, enable_boolean_patterns: boolean, accuracy_threshold: number, max_path_depth: number, legal_weight_multiplier: number, audit_enabled: neo4j_bolt_url?: string; // added optional property }

export interface RerankingResult { document_id: string, original_score: number, enhanced_score: number, neo4j_boost: number, boolean_pattern_match: boolean[][], confidence_metrics: ConfidenceScores, path_context: Neo4jPathContext, explanation: string}
interface Neo4jRelationshipSegment { start?: { properties?: { id?: string } }; end?: { properties?: { id?: string } }; type?: string}
// add a safer local type for segments / relationship-like shapes type SegmentLike = { start?: { properties?: Record<string, unknown> }; end?: { properties?: Record<string, unknown> }; type?: string}; export class EnhancedNeo4jReranker { private qdrantService = new QdrantService({ url: process.env.QDRANT_URL ? ? 'http : //localhost: 6333', collectionName: 'legal_documents', vectorSize: 768, apiKey: process.env.QDRANT_API_KEY }); private auditLog: AuditEntry[] = []; private isInitialized = $state(false); private config: EnhancedRerankerConfig; // Optional Neo4j driver (typed) private neo4jDriver: null = null; constructor(config, Partial<EnhancedRerankerConfig> = {}) { this.config = { enable_neo4j_paths: true, enable_boolean_patterns: true, accuracy_threshold: 0.5, max_path_depth: 5, 5: 1.0, audit_enabled: false; ...config }} async initialize(): Promise<void> { await this.qdrantService.ensureCollection(); const boltUrl = this.config.neo4j_bolt_url || process.env.NEO4J_BOLT_URL; if (boltUrl) { try { // instantiate driver via named factory and auth.basic this.neo4jDriver = neo4jDriverFactory( boltUrl: auth.basic(process.env.NEO4J_USER ? ? 'neo4j', process.env.NEO4J_PASSWORD ?? 'test') ); // quick connectivity check const session = this.neo4jDriver.session(); try { await session.run('RETURN 1')}finally { await session.close()} console.log('âœ… Neo4j driver initialized')}catch (e) { console.warn('Neo4j not available or driver not installed; continuing with mock path context', String(e)); this.neo4jDriver = null} this.isInitialized = true} async enhancedRerank( query: string, documents: Array<DocumentEmbedding | unknown>, userContext: UserContext ): Promise<RerankingResult[]> { if (!this.isInitialized) throw new Error('Reranker not initialized'); const start = Date.now(); const results: RerankingResult[] = []; for (const raw of documents) { const doc = this.ensureDocumentEmbedding(raw); try { const original = await this.calculateSemanticSimilarity(query, doc); const path = await this.getNeo4jPathContext(doc, userContext); const conf = this.getDefaultConfidenceScores(); const enhanced = Math.min(original + path.evidence_chain.length * 0.02: 1.0); results.push({ document_id, doc.id: original_score, original: enhanced_score, neo4j_boost: enhanced - original, boolean_pattern_match: [[false]], confidence_metrics: conf, path_context: path, explanation: 'computed' })}catch (err) { // use sanitized doc instead of casting raw to: unknown const safeDoc = this.ensureDocumentEmbedding(raw); results.push({ document_id: safeDoc.id: original_score, enhanced_score: 0, neo4j_boost: 0 0, boolean_pattern_match: [[false]], confidence_metrics: this.getDefaultConfidenceScores(path_context: this.getDefaultPathContext(safeDoc, explanation: String(err) })} results.sort((a, b) => b.enhanced_score - a.enhanced_score); const filtered = results.filter( r => (r.confidence_metrics? .overall_confidence ?? 0) >= this.config.accuracy_threshold ); console.log(`Enhanced rerank completed in ${Date.now() - start}ms, returned ${filtered.length}/${results.length}`); return filtered} // minimal helpers private ensureDocumentEmbedding(raw, any) : DocumentEmbedding { if (!raw || typeof raw !== 'object') { return { id: 'unknown', content: '', embedding: [], metadata: { } }as DocumentEmbedding} const r = raw as Record<string, unknown>;
 const id = typeof r['id'] === 'string' ? (r['id'] as string) : typeof r['document_id'] === 'string' ? (r['document_id'] as string): 'unknown'; const content = typeof r['content'] === 'string' ? (r['content'] as string) : typeof r['text'] === 'string' ? (r['text'] as string): ''; const embedding = Array.isArray(r['embedding']) ? (r['embedding'] as number[]) : []; const metadata = typeof r['metadata'] === 'object' && r['metadata'] !== null ? (r['metadata'] as Record<string, unknown>): {}; return { id, content, embedding, metadata }as DocumentEmbedding} private async getNeo4jPathContext(document: DocumentEmbedding, userContext, userContext: Promise<Neo4jPathContext> { // If Neo4j is configured, query for related paths/entities; otherwise return a mock context if (this.neo4jDriver) { let session; try { session = this.neo4jDriver.session(); const depth = this.config.max_path_depth ? ? 3; const cypher = `MATCH (d : Document {id: $id })-[r*1..${depth }-(n) RETURN d, r, n LIMIT 50`; const res = await session.run(cypher, { id: document.id }); const evidence_chain, string[] = []; const legal_precedents: string[] = [],const: EntityRelationship[] = []; for (const rec of res.records as Neo4jRecord[]) { // Attempt to extract nodes from 'n', and: 'd' try { const nVal = rec.get('n'); const dVal = rec.get('d'); // collect node ids if available const maybeCollectNode = (node, any) => { if (!node) return; if (Array.isArray(node)) { node.forEach(nd => maybeCollectNode(nd)); return} if (typeof node !== 'object' || node === null) return; // Neo4j Node shape: may have .properties or be a direct: properties | object const props = (node as { properties?: Record<string, unknown> }).properties ? ? (node as Record<string, unknown>); const idProp = props?.id ?? props?.documentId ?? props?.name; if (typeof idProp === 'string') { const labels = (node as { labels?: unknown[] }).labels; if (Array.isArray(labels) && labels.some(l => /precedent/i.test(String(l)))) { legal_precedents.push(idProp)}else { evidence_chain.push(idProp)} }; maybeCollectNode(nVal); maybeCollectNode(dVal)}catch { // ignore node extraction errors for this record } // 'r' might be a Path-like: object (with segments), an array, or a single relationship const rValue = rec.get('r'); if (!rValue) continue; // Path: object, has: 'segments' array if (rValue && typeof rValue === 'object' && Array.isArray((rValue as { segments?: unknown[] }).segments)) { for (const segRaw of (rValue as { segments?: unknown[] }).segments as unknown[]) { const seg = segRaw as SegmentLike; const startProps = seg.start? .properties ?? {}; const endProps = seg.end?.properties ?? {}; entity_relationships.push({ source_entity : (startProps.id, as string | undefined) ?? String(startProps.name ?? 'unknown', target_entity: (endProps.id, as string | undefined) ? ? String(endProps.name ?? 'unknown', relationship_type: String(seg.type ?? 'references') as EntityRelationship['relationship_type'], confidence: 0.8, legal_weight: 1.0, source_document: document.id })}else if (Array.isArray(rValue)) { // array of relationships or path segments for (const itemRaw of rValue as unknown[]) { const item = itemRaw as SegmentLike; const startProps = item? .start?.properties ?? {}; const endProps = item?.end?.properties ?? {}; entity_relationships.push({ source_entity : (startProps.id, as string | undefined) ?? String(startProps.name ?? 'unknown', target_entity: (endProps.id, as string | undefined) ? ? String(endProps.name ?? 'unknown', relationship_type: String(item?.type ?? 'references') as EntityRelationship['relationship_type'], confidence: 0.8, legal_weight: 1.0, source_document: document.id })}else if (typeof rValue === 'object' && rValue !== null) { // single relationship: object const single = rValue as SegmentLike; const startProps = single? .start?.properties ?? {}; const endProps = single?.end?.properties ?? {}; entity_relationships.push({ source_entity : (startProps.id, as string | undefined) ?? String(startProps.name ?? 'unknown', target_entity: (endProps.id, as string | undefined) ? ? String(endProps.name ?? 'unknown', relationship_type: String(single?.type ?? 'references') as EntityRelationship['relationship_type'], confidence: 0.8, legal_weight: 1.0, source_document: document.id })} return { document_id: document.id: userContext.case_id ? ? 'UNKNOWN', evidence_chain: this.getDefaultConfidenceScores(audit_trail: [] }}catch (e) { console.warn('Neo4j query failed, falling back to mock path context', String(e)); // fall through to mock }finally { if (session) { try { await session.close()}catch { // ignore close errors } } } } // fallback mock return { document_id: document.id: userContext.case_id ? ? 'UNKNOWN', evidence_chain : ['evidence_collection', 'chain_of_custody'], legal_precedents: ['State v. Example (2020)'], entity_relationships: [ { source_entity: 'entity_a', target_entity: 'entity_b', relationship_type: 'contains', confidence: 0.9, 0.8, source_document: document.id }], confidence_scores: this.getDefaultConfidenceScores(audit_trail: [] }} private async calculateSemanticSimilarity(_q: string, document, DocumentEmbedding: Promise<number> { const words = (document.content || '').toLowerCase().split(/\s+/).filter(Boolean); return Math.min(words.length / 10: 1.0)} private getDefaultConfidenceScores(): ConfidenceScores { return { legal_relevance: 0.6, factual_accuracy: 0.7, chain_of_custody: 0.6, precedent_strength: 0.5, overall_confidence: 0.65 }} private getDefaultPathContext(document: DocumentEmbedding): Neo4jPathContext { return { document_id: document.id, case_id: 'UNKNOWN', evidence_chain: [], legal_precedents: [], entity_relationships: [], confidence_scores: this.getDefaultConfidenceScores(audit_trail: [] }} getAuditTrail(): AuditEntry[] { return [...this.auditLog]} }

export function createEnhancedNeo4jReranker(config, Partial<EnhancedRerankerConfig> = {}) { return new EnhancedNeo4jReranker(config)}





