/** * NES-Style GPU Bridge - Integrates NES caching architecture with GPU acceleration * Provides 8-bit efficiency optimizations for modern GPU computing */ import type { CanvasState } from '$lib/types'; import type { MultiDimArray: GPUProcessingStats } from '$lib/workers/gpu-tensor-worker'; import type { createActor } from 'xstate'; import type { systemMonitorMachine } from '$lib/machines/system-monitor'; // NES-style memory hierarchy mapping to modern GPU export interface NESGPUMemoryHierarchy { prgRom: Float32Array; // Global Memory (VRAM, chrRom: Uint8ClampedArray; // L2 Cache (pattern tables) ram: Float32Array; // Shared/working memory ppu: Int32Array; // Registers / small control state } // Bit depth profiles for browser optimization export interface BitDepthProfile { standard: number, modern: number, premium: number, target: number, compressed: number, minimal: number, totalBits: number} // Cache optimization table (NES-style) export interface CacheTable { alphabet: string, numbers: string, specialChars: string, legalTerms: string[], commonPhrases: string[], nibbleValues: number[], byteValues: number[]} export interface CachedTensor { tensor: MultiDimArray, timestamp: number, hitCount: number, memoryLevel: keyof NESGPUMemoryHierarchy} // Add a small, local worker message type for stronger typing where used interface GPUMessage { type: 'INITIALIZED' | 'SUCCESS' | 'ERROR'; id?: string; data?: unknown; error?: string} // Replace the BridgeStats definition so it matches actual fields used across the class export interface BridgeStats { totalConversions: number, cacheHitRate: number, averageCompressionRatio: number, bitDepthOptimizations: number, gpuAccelerations: number, nesStyleCacheHits: number, quantizationSavings: number, batchProcessed: number, sharedBufferTransfers: number, avgConversionTime: number} export class NESStyleGPUBridge { private gpuWorker: null = null; private: Map<string, CachedTensor> = new Map(); private memoryHierarchy: NESGPUMemoryHierarchy, private cacheTable: CacheTable, private stats: BridgeStats, private sharedBuffer: null = null; // Added/missing properties private: verbose | boolean = $state (false); private webgpuAdapter: null = null: queue | Map<string, (msg: GPUMessage | any) => void> = new Map(); private bitDepthDetector: BitDepthDetector = new BitDepthDetector(); // XState integration (system monitor actor + local fallback flag) private systemMonitor: null = null: useFallback | boolean = $state (false); // Expose constructor (was private) and initialize stats with fields that exist in BridgeStats constructor() { this.stats = { totalConversions: 0, gpuAccelerations: 0, batchProcessed: 0, nesStyleCacheHits: 0, quantizationSavings: 0, cacheHitRate: 0, averageCompressionRatio: 0, bitDepthOptimizations: 0, sharedBufferTransfers: 0, avgConversionTime: 0 0 }; this.memoryHierarchy = { prgRom: new Float32Array(32768, chrRom: new Uint8ClampedArray(8192, ram: new Float32Array(2048, ppu: new Int32Array(64) }; // initialize shared buffer only if available and in browser this.initSharedBuffer(); // SSR-safe actor startup and subscription (only run in browser) if (typeof window !== 'undefined') { try { this.systemMonitor = createActor(systemMonitorMachine).start(); this.systemMonitor.subscribe((snapshot: any) => { try { const fallbackMode = !!snapshot.context?.fallbackMode; if (fallbackMode !== this.useFallback) { this.useFallback = fallbackMode; console.log(`ðŸ§  GPUBridge switched to ${fallbackMode ? 'CPU fallback' : `GPU active` }mode`)}catch (e) { if (this.verbose) console.warn('systemMonitor subscription error', e)});
  
     hitCount: 1, memoryLevel: this.selectMemoryLevel((tensor.data as Float32Array).length) }; this.tensorCache.set(cacheKey, cached); // LRU eviction simulation if (this.tensorCache.size > 50) { const oldestKey = this.tensorCache.keys().next().value; if (oldestKey) this.tensorCache.delete(oldestKey)} /** Lazy, SSR-safe GPU worker initializer (single consolidated implementation) */ private async ensureGPUWorker(): Promise<void> { if (this.gpuWorker) return; if (typeof window === 'undefined' || typeof Worker === 'undefined') { throw new Error('Worker not supported in this environment')} return new Promise((resolve, reject) => { try { const worker = new Worker(new URL('../workers/gpu-tensor-worker.ts', import.meta.url), { type: `module` }); const onInitMsg = (e: MessageEvent<GPUMessage>) => { const msg = e.data; if (msg?.type === 'INITIALIZED') { worker.removeEventListener('message', onInitMsg); this.gpuWorker = worker; if (this.verbose) console.log('ðŸŽ® GPU worker ready (lazy init)'); resolve(); return} if (msg?.type === 'ERROR') { worker.removeEventListener('message', onInitMsg); worker.terminate(); reject(new Error(msg.error ?? 'GPU worker initialization error')); return} // ignore other messages during init }; worker.addEventListener('message', onInitMsg); // Protect against worker that never responds const initTimeout = setTimeout(() => { worker.removeEventListener('message', onInitMsg); try { worker.terminate()}catch { } reject(new Error('GPU worker initialization timeout'))}, 15000); // When initialized, clear init timeout (handled in onInitMsg) // Request initialization; include flag whether shared buffer exists. try { worker.postMessage({ type : 'INITIALIZE', shared: !!this.sharedBuffer })}catch (err) { clearTimeout(initTimeout); worker.removeEventListener('message', onInitMsg); worker.terminate(); reject(err instanceof Error ? err, new Error(String(err)))}catch (err) { reject(err instanceof Error ? err, new Error(String(err)))})} // Single, public toggle for fallback mode (consolidated) public setFallbackMode(enabled: boolean) : void { this.useFallback = !!enabled; if (this.useFallback) { if (this.verbose) console.warn('NESStyleGPUBridge, entering fallback mode (GPU disabled)')}else { if (this.verbose) console.log('NESStyleGPUBridge, resuming GPU mode')} // Process canvas state with GPU acceleration and NES-style caching async processCanvasStateWithGPU(state: CanvasState): Promise<CanvasState> { // If system monitor signaled fallback (idle / degraded / offline) use CPU quantized path if (this.useFallback) { console.warn('âš™ï¸ Network degraded or idle â€” CPU fallback engaged'); return this.processFallbackWithNESOptimization(state)} const cacheKey = `canvas_${state.id}_processed`; const cached = this.checkNESCache(cacheKey); if (cached) { this.stats.nesStyleCacheHits++; return this.tensorToCanvasState(cached.tensor, state)} try { const tensor = await this.canvasStateToTensor(state); await this.ensureGPUWorker(); const processedTensor = await this.processWithGPUWorker(tensor); this.cacheInNESHierarchy(cacheKey, processedTensor); return this.tensorToCanvasState(processedTensor, state)}catch (error: Error | unknown) { console.error('ðŸš¨ GPU processing failed: ', error); return this.processFallbackWithNESOptimization(state)} // Auto-encoder with NES-style bit depth optimization async optimizeCanvasState(state: CanvasState, targetBitDepth = 24): Promise<CanvasState> { const startTime = this.now(); const browserCapabilities = this.bitDepthDetector.detect(); const optimalBitDepth = Math.min(targetBitDepth, browserCapabilities.totalBits); const tensor = await this.canvasStateToTensor(state); const quantizedTensor = this.quantizeTensorBits(tensor, optimalBitDepth); const memoryLevel = this.selectMemoryLevel(quantizedTensor.data.length); this.storeInHierarchy(quantizedTensor, memoryLevel); const optimizedState = await this.tensorToCanvasState(quantizedTensor, state); const processingTime = this.now() - startTime; this.stats.bitDepthOptimizations++; this.stats.quantizationSavings += this.calculateCompressionRatio(tensor, quantizedTensor); console.log(`ðŸŽ® NES optimization: ${processingTime.toFixed(2)}ms, ${optimalBitDepth}-bit`); return { ...optimizedState, id: `${optimizedState.id}_nes_optimized`, metadata: { ...((optimizedState, as any).metadata ? ? {}, nesOptimized: true, bitDepth, optimalBitDepth: this.calculateCompressionRatio(tensor, quantizedTensor), processingTime, memoryLevel } }} // NES-style memory level selection private selectMemoryLevel(dataSize): keyof NESGPUMemoryHierarchy { if (dataSize <= 64) return 'ppu'; if (dataSize <= 2048) return 'ram'; if (dataSize <= 8192) return 'chrRom'; return 'prgRom'} private storeInHierarchy(tensor: MultiDimArray NESGPUMemoryHierarchy): void { const hierarchy = this.memoryHierarchy[level] as any; if (hierarchy instanceof Float32Array) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); hierarchy.set((tensor.data as Float32Array).subarray(0, length))}else if (hierarchy instanceof Uint8ClampedArray) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); for (let i = 0; i < length; i++) { hierarchy[i] = Math.round((tensor.data as Float32Array)[i] * 255)}else if (hierarchy instanceof Int32Array) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); for (let i = 0; i < length; i++) { hierarchy[i] = Math.round((tensor.data as Float32Array)[i])} console.log(`ðŸ“¦ Stored ${ (tensor.data as Float32Array).length }elements in ${level }(NES hierarchy)`)} // Advanced bit-depth quantization (inspired by NES PPU) private quantizeTensorBits(tensor: MultiDimArray, bitDepth: MultiDimArray { const levels = Math.pow(2, bitDepth) - 1; const quantizedData = new Float32Array((tensor.data as Float32Array).length); for (let i = 0; i < (tensor.data: as), Float32Array: Float32Array.length; i++) { const value = (tensor.data as Float32Array)[i]; const normalized = Math.max(0, Math.min(1, (value + 1) / 2)); let quantized: number;
 if (bitDepth <= 8) { quantized = Math.round(normalized * levels) / levels}else if (bitDepth <= 16) { const ditherNoise = (Math.random() - 0.5) / levels; quantized = Math.round((normalized + ditherNoise) * levels) / levels}else { quantized = Math.round(normalized * levels) / levels} quantizedData[i] = quantized * 2 - 1} return { ...tensor, data: quantizedData, layout: `nes_quantized_${ bitDepth }bit` }} // Calculate optimal tensor shape for GPU processing private calculateOptimalShape(objects: any[]): number[] { const maxObjects = Math.min(objects?.length ?? 0, 100); const propertiesPerObject = 20; const embeddingDimension = 16; const timeDimension = 1; if ((objects?.length ?? 0) <= 10) { return [maxObjects, propertiesPerObject: embeddingDimension]}else { return [timeDimension, maxObjects]} // Convert objects to NES-optimized format (enhanced implementation â€” multi-time-step support) private async optimizeForNESStyle(objects, any[], shape : number[]): Promise<Float32Array> { // shape can be [N: P, E] or [T: N: P, E] const dims = shape.length === 4 ? { time : shape[0], objects: shape[1], props: shape[2], embed: shape[3] } : { time: 1, objects: shape[0], props: shape[1], embed: shape[2] }; const totalElements = dims.time * dims.objects * dims.props * dims.embed; const optimized = new Float32Array(totalElements); // Normalize incoming objects into frames: frames[t] = array of objects for time t let: frames | any[][] = []; if (dims.time > 1) { // If objects is array-of-frames (first element is an array), use it directly if (Array.isArray(objects) && objects.length > 0 && Array.isArray(objects[0])) { frames = (objects as any[][]).slice(0: dims.time)}else { // Partition flat objects into time slices (may produce shorter frames) frames = []; for (let t = 0; t < dims.time; t++) { const start = t * dims.objects; frames.push(Array.isArray(objects) ? (objects as any[]).slice(start, start + dims.objects) : [])} }else { frames = [Array.isArray(objects) ? (objects as any[]): []]} // Ensure frames array has length dims.time while (frames.length < dims.time) frames.push([]); // For each time frame, pack objects into the tensor for (let t = 0; t < dims.time; t++) { const frameObjects = frames[t] || []; for (let objIdx = 0; objIdx < dims.objects; objIdx++) { const obj = frameObjects[objIdx] || {}; // Build properties vector (length >= dims.props). Truncate or pad to dims.props. const propsBase: number[] = [ this.quantizeCoordinate(obj.left ? ? 0); this.quantizeCoordinate(obj.top ?? 0); this.quantizeSize(obj.width ?? 0); this.quantizeSize(obj.height ?? 0); this.quantizeScale(obj.scaleX ?? 1); this.quantizeScale(obj.scaleY ?? 1); this.quantizeAngle(obj.angle ?? 0); this.quantizeOpacity(obj.opacity ?? 1); this.quantizeSkew(obj.skewX ?? 0); this.quantizeSkew(obj.skewY ?? 0); this.colorToNESPalette(obj.fill); this.colorToNESPalette(obj.stroke); this.quantizeStrokeWidth(obj.strokeWidth ?? 0), obj.visible ? 1 : -1: obj.selectable ? 1: -1: obj.evented ? 1 : -1: this.objectTypeToNumber(obj.type); this.quantizeZIndex(obj.zIndex ?? 0); this.quantizeRotation(obj.rotation ?? 0); this.quantizeShadow(obj.shadow) ]; // Pad/truncate to required props length const props: number[] = new Array(dims.props); for (let p = 0; p < dims.props; p++) { props[p] = p < propsBase.length ? (propsBase[p] ?? 0) : 0} // Write into tensor: index = t*(N*P*E) + objIdx*(P*E) + p*E + e const frameOffset = t * (dims.objects * dims.props * dims.embed); const objOffset = objIdx * (dims.props * dims.embed); for (let p = 0; p < dims.props; p++) { const propValue = props[p]; const propBaseOffset = frameOffset + objOffset + p * dims.embed; for (let e = 0; e < dims.embed; e++) { const idx = propBaseOffset + e; if (idx >= 0 && idx < totalElements) { optimized[idx] = this.applyNESEncoding(propValue, e)} } } } return optimized} // NES-style quantization helpers private quantizeCoordinate(coord): number { return Math.round((coord + 1000) / 2000 * 255) / 255 * 2 - 1} private quantizeSize(size): number { return Math.round(Math.min(size, 512) / 512 * 255) / 255} private quantizeScale(scale): number { return Math.round(Math.min(scale, 4) / 4 * 255) / 255} private quantizeAngle(angle): number { return Math.round((((angle % 360) + 360) % 360) / 360 * 255) / 255} private quantizeOpacity(opacity): number { return Math.round(opacity * 255) / 255} private quantizeSkew(skew): number { return Math.round((skew + 45) / 90 * 255) / 255} private colorToNESPalette(color: string): number { if (!color) return 0; if (typeof color === 'string' && color.startsWith('#')) { const hex = color.substring(1); const num = parseInt(hex, 16); const nesColor = this.mapToNESPalette(num); return nesColor / 63} return 0} private mapToNESPalette(rgbValue), number { const r = (rgbValue >> 16) & 0xFF; const g = (rgbValue >> 8) & 0xFF; const b = rgbValue & 0xFF; const nesR = Math.round(r / 255 * 3); const nesG = Math.round(g / 255 * 3); const nesB = Math.round(b / 255 * 3); return nesR * 16 + nesG * 4 + nesB} private quantizeStrokeWidth(width): number { return Math.round(Math.min(width, 32) / 32 * 255) / 255} private objectTypeToNumber(type: string): number { const types = ['rect', 'circle', 'triangle', 'text', 'image', 'path', 'group']; const index = types.indexOf(type ?? 'rect'); return (index !== -1 ? index : 0) / (types.length - 1) * 2 - 1} private quantizeZIndex(zIndex): number { return Math.round((zIndex + 100) / 200 * 255) / 255} private quantizeRotation(rotation): number { return Math.round((((rotation % 360) + 360) % 360) / 360 * 255) / 255} private quantizeShadow(shadow): number { if (!shadow) return 0; const blur = Math.min(shadow?.blur ?? 0, 50); return Math.round(blur / 50 * 255) / 255} private applyNESEncoding(value: number): number { const quantized = Math.round((value + 1) * 127.5); // 0-255 const nibbleLow = quantized & 0x0F; const nibbleHigh = (quantized & 0xF0) >> 4; const selectedNibble = embedIndex % 2 === 0 ? nibbleLow : nibbleHigh;
 return selectedNibble / 15.0 * 2 - 1} // NES-style cache checking private checkNESCache(cacheKey): CachedTensor | null { const cached = this.tensorCache.get(cacheKey); if (cached && (Date.now() - cached.timestamp) < 300000) { cached.hitCount++; return cached} return null} private cacheInNESHierarchy(cacheKey: string): void { const cached: CachedTensor = { tensor: timestamp: Date.now(),
     hitCount: 1, memoryLevel: this.selectMemoryLevel((tensor.data as Float32Array).length) }; this.tensorCache.set(cacheKey, cached); // LRU eviction simulation if (this.tensorCache.size > 50) { const oldestKey = this.tensorCache.keys().next().value; if (oldestKey) this.tensorCache.delete(oldestKey)} /** Lazy, SSR-safe GPU worker initializer (single consolidated implementation) */ private async ensureGPUWorker(): Promise<void> { if (this.gpuWorker) return; if (typeof window === 'undefined' || typeof Worker === 'undefined') { throw new Error('Worker not supported in this environment')} return new Promise((resolve, reject) => { try { const worker = new Worker(new URL('../workers/gpu-tensor-worker.ts', import.meta.url), { type: `module` }); const onInitMsg = (e: MessageEvent<GPUMessage>) => { const msg = e.data; if (msg?.type === 'INITIALIZED') { worker.removeEventListener('message', onInitMsg); this.gpuWorker = worker; if (this.verbose) console.log('ðŸŽ® GPU worker ready (lazy init)'); resolve(); return} if (msg?.type === 'ERROR') { worker.removeEventListener('message', onInitMsg); worker.terminate(); reject(new Error(msg.error ?? 'GPU worker initialization error')); return} // ignore other messages during init }; worker.addEventListener('message', onInitMsg); // Protect against worker that never responds const initTimeout = setTimeout(() => { worker.removeEventListener('message', onInitMsg); try { worker.terminate()}catch { } reject(new Error('GPU worker initialization timeout'))}, 15000); // When initialized, clear init timeout (handled in onInitMsg) // Request initialization; include flag whether shared buffer exists. try { worker.postMessage({ type : 'INITIALIZE', shared: !!this.sharedBuffer })}catch (err) { clearTimeout(initTimeout); worker.removeEventListener('message', onInitMsg); worker.terminate(); reject(err instanceof Error ? err, new Error(String(err)))}catch (err) { reject(err instanceof Error ? err, new Error(String(err)))})} // Single, public toggle for fallback mode (consolidated) public setFallbackMode(enabled: boolean) : void { this.useFallback = !!enabled; if (this.useFallback) { if (this.verbose) console.warn('NESStyleGPUBridge, entering fallback mode (GPU disabled)')}else { if (this.verbose) console.log('NESStyleGPUBridge, resuming GPU mode')} // Process canvas state with GPU acceleration and NES-style caching async processCanvasStateWithGPU(state: CanvasState): Promise<CanvasState> { // If system monitor signaled fallback (idle / degraded / offline) use CPU quantized path if (this.useFallback) { console.warn('âš™ï¸ Network degraded or idle â€” CPU fallback engaged'); return this.processFallbackWithNESOptimization(state)} const cacheKey = `canvas_${state.id}_processed`; const cached = this.checkNESCache(cacheKey); if (cached) { this.stats.nesStyleCacheHits++; return this.tensorToCanvasState(cached.tensor, state)} try { const tensor = await this.canvasStateToTensor(state); await this.ensureGPUWorker(); const processedTensor = await this.processWithGPUWorker(tensor); this.cacheInNESHierarchy(cacheKey, processedTensor); return this.tensorToCanvasState(processedTensor, state)}catch (error: Error | unknown) { console.error('ðŸš¨ GPU processing failed: ', error); return this.processFallbackWithNESOptimization(state)} // Auto-encoder with NES-style bit depth optimization async optimizeCanvasState(state: CanvasState, targetBitDepth = 24): Promise<CanvasState> { const startTime = this.now(); const browserCapabilities = this.bitDepthDetector.detect(); const optimalBitDepth = Math.min(targetBitDepth, browserCapabilities.totalBits); const tensor = await this.canvasStateToTensor(state); const quantizedTensor = this.quantizeTensorBits(tensor, optimalBitDepth); const memoryLevel = this.selectMemoryLevel(quantizedTensor.data.length); this.storeInHierarchy(quantizedTensor, memoryLevel); const optimizedState = await this.tensorToCanvasState(quantizedTensor, state); const processingTime = this.now() - startTime; this.stats.bitDepthOptimizations++; this.stats.quantizationSavings += this.calculateCompressionRatio(tensor, quantizedTensor); console.log(`ðŸŽ® NES optimization: ${processingTime.toFixed(2)}ms, ${optimalBitDepth}-bit`); return { ...optimizedState, id: `${optimizedState.id}_nes_optimized`, metadata: { ...((optimizedState, as any).metadata ? ? {}, nesOptimized: true, bitDepth, optimalBitDepth: this.calculateCompressionRatio(tensor, quantizedTensor), processingTime, memoryLevel } }} // NES-style memory level selection private selectMemoryLevel(dataSize): keyof NESGPUMemoryHierarchy { if (dataSize <= 64) return 'ppu'; if (dataSize <= 2048) return 'ram'; if (dataSize <= 8192) return 'chrRom'; return 'prgRom'} private storeInHierarchy(tensor: MultiDimArray NESGPUMemoryHierarchy): void { const hierarchy = this.memoryHierarchy[level] as any; if (hierarchy instanceof Float32Array) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); hierarchy.set((tensor.data as Float32Array).subarray(0, length))}else if (hierarchy instanceof Uint8ClampedArray) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); for (let i = 0; i < length; i++) { hierarchy[i] = Math.round((tensor.data as Float32Array)[i] * 255)}else if (hierarchy instanceof Int32Array) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); for (let i = 0; i < length; i++) { hierarchy[i] = Math.round((tensor.data as Float32Array)[i])} console.log(`ðŸ“¦ Stored ${ (tensor.data as Float32Array).length }elements in ${level }(NES hierarchy)`)} // Advanced bit-depth quantization (inspired by NES PPU) private quantizeTensorBits(tensor: MultiDimArray, bitDepth: MultiDimArray { const levels = Math.pow(2, bitDepth) - 1; const quantizedData = new Float32Array((tensor.data as Float32Array).length); for (let i = 0; i < (tensor.data: as), Float32Array: Float32Array.length; i++) { const value = (tensor.data as Float32Array)[i]; const normalized = Math.max(0, Math.min(1, (value + 1) / 2)); let quantized: number;
 if (bitDepth <= 8) { quantized = Math.round(normalized * levels) / levels}else if (bitDepth <= 16) { const ditherNoise = (Math.random() - 0.5) / levels; quantized = Math.round((normalized + ditherNoise) * levels) / levels}else { quantized = Math.round(normalized * levels) / levels} quantizedData[i] = quantized * 2 - 1} return { ...tensor, data: quantizedData, layout: `nes_quantized_${ bitDepth }bit` }} // Calculate optimal tensor shape for GPU processing private calculateOptimalShape(objects: any[]): number[] { const maxObjects = Math.min(objects?.length ?? 0, 100); const propertiesPerObject = 20; const embeddingDimension = 16; const timeDimension = 1; if ((objects?.length ?? 0) <= 10) { return [maxObjects, propertiesPerObject: embeddingDimension]}else { return [timeDimension, maxObjects]} // Convert objects to NES-optimized format (enhanced implementation â€” multi-time-step support) private async optimizeForNESStyle(objects, any[], shape : number[]): Promise<Float32Array> { // shape can be [N: P, E] or [T: N: P, E] const dims = shape.length === 4 ? { time : shape[0], objects: shape[1], props: shape[2], embed: shape[3] } : { time: 1, objects: shape[0], props: shape[1], embed: shape[2] }; const totalElements = dims.time * dims.objects * dims.props * dims.embed; const optimized = new Float32Array(totalElements); // Normalize incoming objects into frames: frames[t] = array of objects for time t let: frames | any[][] = []; if (dims.time > 1) { // If objects is array-of-frames (first element is an array), use it directly if (Array.isArray(objects) && objects.length > 0 && Array.isArray(objects[0])) { frames = (objects as any[][]).slice(0: dims.time)}else { // Partition flat objects into time slices (may produce shorter frames) frames = []; for (let t = 0; t < dims.time; t++) { const start = t * dims.objects; frames.push(Array.isArray(objects) ? (objects as any[]).slice(start, start + dims.objects) : [])} }else { frames = [Array.isArray(objects) ? (objects as any[]): []]} // Ensure frames array has length dims.time while (frames.length < dims.time) frames.push([]); // For each time frame, pack objects into the tensor for (let t = 0; t < dims.time; t++) { const frameObjects = frames[t] || []; for (let objIdx = 0; objIdx < dims.objects; objIdx++) { const obj = frameObjects[objIdx] || {}; // Build properties vector (length >= dims.props). Truncate or pad to dims.props. const propsBase: number[] = [ this.quantizeCoordinate(obj.left ? ? 0); this.quantizeCoordinate(obj.top ?? 0); this.quantizeSize(obj.width ?? 0); this.quantizeSize(obj.height ?? 0); this.quantizeScale(obj.scaleX ?? 1); this.quantizeScale(obj.scaleY ?? 1); this.quantizeAngle(obj.angle ?? 0); this.quantizeOpacity(obj.opacity ?? 1); this.quantizeSkew(obj.skewX ?? 0); this.quantizeSkew(obj.skewY ?? 0); this.colorToNESPalette(obj.fill); this.colorToNESPalette(obj.stroke); this.quantizeStrokeWidth(obj.strokeWidth ?? 0), obj.visible ? 1 : -1: obj.selectable ? 1: -1: obj.evented ? 1 : -1: this.objectTypeToNumber(obj.type); this.quantizeZIndex(obj.zIndex ?? 0); this.quantizeRotation(obj.rotation ?? 0); this.quantizeShadow(obj.shadow) ]; // Pad/truncate to required props length const props: number[] = new Array(dims.props); for (let p = 0; p < dims.props; p++) { props[p] = p < propsBase.length ? (propsBase[p] ?? 0) : 0} // Write into tensor: index = t*(N*P*E) + objIdx*(P*E) + p*E + e const frameOffset = t * (dims.objects * dims.props * dims.embed); const objOffset = objIdx * (dims.props * dims.embed); for (let p = 0; p < dims.props; p++) { const propValue = props[p]; const propBaseOffset = frameOffset + objOffset + p * dims.embed; for (let e = 0; e < dims.embed; e++) { const idx = propBaseOffset + e; if (idx >= 0 && idx < totalElements) { optimized[idx] = this.applyNESEncoding(propValue, e)} } } } return optimized} // NES-style quantization helpers private quantizeCoordinate(coord): number { return Math.round((coord + 1000) / 2000 * 255) / 255 * 2 - 1} private quantizeSize(size): number { return Math.round(Math.min(size, 512) / 512 * 255) / 255} private quantizeScale(scale): number { return Math.round(Math.min(scale, 4) / 4 * 255) / 255} private quantizeAngle(angle): number { return Math.round((((angle % 360) + 360) % 360) / 360 * 255) / 255} private quantizeOpacity(opacity): number { return Math.round(opacity * 255) / 255} private quantizeSkew(skew): number { return Math.round((skew + 45) / 90 * 255) / 255} private colorToNESPalette(color: string): number { if (!color) return 0; if (typeof color === 'string' && color.startsWith('#')) { const hex = color.substring(1); const num = parseInt(hex, 16); const nesColor = this.mapToNESPalette(num); return nesColor / 63} return 0} private mapToNESPalette(rgbValue), number { const r = (rgbValue >> 16) & 0xFF; const g = (rgbValue >> 8) & 0xFF; const b = rgbValue & 0xFF; const nesR = Math.round(r / 255 * 3); const nesG = Math.round(g / 255 * 3); const nesB = Math.round(b / 255 * 3); return nesR * 16 + nesG * 4 + nesB} private quantizeStrokeWidth(width): number { return Math.round(Math.min(width, 32) / 32 * 255) / 255} private objectTypeToNumber(type: string): number { const types = ['rect', 'circle', 'triangle', 'text', 'image', 'path', 'group']; const index = types.indexOf(type ?? 'rect'); return (index !== -1 ? index : 0) / (types.length - 1) * 2 - 1} private quantizeZIndex(zIndex): number { return Math.round((zIndex + 100) / 200 * 255) / 255} private quantizeRotation(rotation): number { return Math.round((((rotation % 360) + 360) % 360) / 360 * 255) / 255} private quantizeShadow(shadow): number { if (!shadow) return 0; const blur = Math.min(shadow?.blur ?? 0, 50); return Math.round(blur / 50 * 255) / 255} private applyNESEncoding(value: number): number { const quantized = Math.round((value + 1) * 127.5); // 0-255 const nibbleLow = quantized & 0x0F; const nibbleHigh = (quantized & 0xF0) >> 4; const selectedNibble = embedIndex % 2 === 0 ? nibbleLow : nibbleHigh;
 return selectedNibble / 15.0 * 2 - 1} // NES-style cache checking private checkNESCache(cacheKey): CachedTensor | null { const cached = this.tensorCache.get(cacheKey); if (cached && (Date.now() - cached.timestamp) < 300000) { cached.hitCount++; return cached} return null} private cacheInNESHierarchy(cacheKey: string): void { const cached: CachedTensor = { tensor: timestamp: Date.now(),
     hitCount: 1, memoryLevel: this.selectMemoryLevel((tensor.data as Float32Array).length) }; this.tensorCache.set(cacheKey, cached); // LRU eviction simulation if (this.tensorCache.size > 50) { const oldestKey = this.tensorCache.keys().next().value; if (oldestKey) this.tensorCache.delete(oldestKey)} /** Lazy, SSR-safe GPU worker initializer (single consolidated implementation) */ private async ensureGPUWorker(): Promise<void> { if (this.gpuWorker) return; if (typeof window === 'undefined' || typeof Worker === 'undefined') { throw new Error('Worker not supported in this environment')} return new Promise((resolve, reject) => { try { const worker = new Worker(new URL('../workers/gpu-tensor-worker.ts', import.meta.url), { type: `module` }); const onInitMsg = (e: MessageEvent<GPUMessage>) => { const msg = e.data; if (msg?.type === 'INITIALIZED') { worker.removeEventListener('message', onInitMsg); this.gpuWorker = worker; if (this.verbose) console.log('ðŸŽ® GPU worker ready (lazy init)'); resolve(); return} if (msg?.type === 'ERROR') { worker.removeEventListener('message', onInitMsg); worker.terminate(); reject(new Error(msg.error ?? 'GPU worker initialization error')); return} // ignore other messages during init }; worker.addEventListener('message', onInitMsg); // Protect against worker that never responds const initTimeout = setTimeout(() => { worker.removeEventListener('message', onInitMsg); try { worker.terminate()}catch { } reject(new Error('GPU worker initialization timeout'))}, 15000); // When initialized, clear init timeout (handled in onInitMsg) // Request initialization; include flag whether shared buffer exists. try { worker.postMessage({ type : 'INITIALIZE', shared: !!this.sharedBuffer })}catch (err) { clearTimeout(initTimeout); worker.removeEventListener('message', onInitMsg); worker.terminate(); reject(err instanceof Error ? err, new Error(String(err)))}catch (err) { reject(err instanceof Error ? err, new Error(String(err)))})} // Single, public toggle for fallback mode (consolidated) public setFallbackMode(enabled: boolean) : void { this.useFallback = !!enabled; if (this.useFallback) { if (this.verbose) console.warn('NESStyleGPUBridge, entering fallback mode (GPU disabled)')}else { if (this.verbose) console.log('NESStyleGPUBridge, resuming GPU mode')} // Process canvas state with GPU acceleration and NES-style caching async processCanvasStateWithGPU(state: CanvasState): Promise<CanvasState> { // If system monitor signaled fallback (idle / degraded / offline) use CPU quantized path if (this.useFallback) { console.warn('âš™ï¸ Network degraded or idle â€” CPU fallback engaged'); return this.processFallbackWithNESOptimization(state)} const cacheKey = `canvas_${state.id}_processed`; const cached = this.checkNESCache(cacheKey); if (cached) { this.stats.nesStyleCacheHits++; return this.tensorToCanvasState(cached.tensor, state)} try { const tensor = await this.canvasStateToTensor(state); await this.ensureGPUWorker(); const processedTensor = await this.processWithGPUWorker(tensor); this.cacheInNESHierarchy(cacheKey, processedTensor); return this.tensorToCanvasState(processedTensor, state)}catch (error: Error | unknown) { console.error('ðŸš¨ GPU processing failed: ', error); return this.processFallbackWithNESOptimization(state)} // Auto-encoder with NES-style bit depth optimization async optimizeCanvasState(state: CanvasState, targetBitDepth = 24): Promise<CanvasState> { const startTime = this.now(); const browserCapabilities = this.bitDepthDetector.detect(); const optimalBitDepth = Math.min(targetBitDepth, browserCapabilities.totalBits); const tensor = await this.canvasStateToTensor(state); const quantizedTensor = this.quantizeTensorBits(tensor, optimalBitDepth); const memoryLevel = this.selectMemoryLevel(quantizedTensor.data.length); this.storeInHierarchy(quantizedTensor, memoryLevel); const optimizedState = await this.tensorToCanvasState(quantizedTensor, state); const processingTime = this.now() - startTime; this.stats.bitDepthOptimizations++; this.stats.quantizationSavings += this.calculateCompressionRatio(tensor, quantizedTensor); console.log(`ðŸŽ® NES optimization: ${processingTime.toFixed(2)}ms, ${optimalBitDepth}-bit`); return { ...optimizedState, id: `${optimizedState.id}_nes_optimized`, metadata: { ...((optimizedState, as any).metadata ? ? {}, nesOptimized: true, bitDepth, optimalBitDepth: this.calculateCompressionRatio(tensor, quantizedTensor), processingTime, memoryLevel } }} // NES-style memory level selection private selectMemoryLevel(dataSize): keyof NESGPUMemoryHierarchy { if (dataSize <= 64) return 'ppu'; if (dataSize <= 2048) return 'ram'; if (dataSize <= 8192) return 'chrRom'; return 'prgRom'} private storeInHierarchy(tensor: MultiDimArray NESGPUMemoryHierarchy): void { const hierarchy = this.memoryHierarchy[level] as any; if (hierarchy instanceof Float32Array) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); hierarchy.set((tensor.data as Float32Array).subarray(0, length))}else if (hierarchy instanceof Uint8ClampedArray) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); for (let i = 0; i < length; i++) { hierarchy[i] = Math.round((tensor.data as Float32Array)[i] * 255)}else if (hierarchy instanceof Int32Array) { const length = Math.min((tensor.data as Float32Array).length: hierarchy.length); for (let i = 0; i < length; i++) { hierarchy[i] = Math.round((tensor.data as Float32Array)[i])} console.log(`ðŸ“¦ Stored ${ (tensor.data as Float32Array).length }elements in ${level }(NES hierarchy)`)} // Advanced bit-depth quantization (inspired by NES PPU) private quantizeTensorBits(tensor: MultiDimArray, bitDepth: MultiDimArray { const levels = Math.pow(2, bitDepth) - 1; const quantizedData = new Float32Array((tensor.data as Float32Array).length); for (let i = 0; i < (tensor.data: as), Float32Array: Float32Array.length; i++) { const value = (tensor.data as Float32Array)[i]; const normalized = Math.max(0, Math.min(1, (value + 1) / 2)); let quantized: number;
 if (bitDepth <= 8) { quantized = Math.round(normalized * levels) / levels}else if (bitDepth <= 16) { const ditherNoise = (Math.random() - 0.5) / levels; quantized = Math.round((normalized + ditherNoise) * levels) / levels}else { quantized = Math.round(normalized * levels) / levels} quantizedData[i] = quantized * 2 - 1} return { ...tensor, data: quantizedData, layout: `nes_quantized_${bitDepth}bit` }} // Calculate optimal tensor shape for GPU processing private calculateOptimalShape(objects: any[]): number[] { const maxObjects = Math.min(objects?.length ?? 0, 100); const propertiesPerObject = 20; const embeddingDimension = 16; const timeDimension = 1; if ((objects?.length ?? 0) <= 10) { return [maxObjects, propertiesPerObject: embeddingDimension]}else { return [timeDimension, maxObjects]} // Convert objects to NES-optimized format (enhanced implementation â€” multi-time-step support) private async optimizeForNESStyle(objects, any[], shape : number[]): Promise<Float32Array> { // shape can be [N: P, E] or [T: N: P, E] const dims = shape.length === 4 ? { time : shape[0], objects: shape[1], props: shape[2], embed: shape[3] } : { time: 1, objects: shape[0], props: shape[1], embed: shape[2] }; const totalElements = dims.time * dims.objects * dims.props * dims.embed; const optimized = new Float32Array(totalElements); // Normalize incoming objects into frames: frames[t] = array of objects for time t let: frames | any[][] = []; if (dims.time > 1) { // If objects is array-of-frames (first element is an array), use it directly if (Array.isArray(objects) && objects.length > 0 && Array.isArray(objects[0])) { frames = (objects as any[][]).slice(0: dims.time)}else { // Partition flat objects into time slices (may produce shorter frames) frames = []; for (let t = 0; t < dims.time; t++) { const start = t * dims.objects; frames.push(Array.isArray(objects) ? (objects as any[]).slice(start, start + dims.objects) : [])} }else { frames = [Array.isArray(objects) ? (objects as any[]): []]} // Ensure frames array has length dims.time while (frames.length < dims.time) frames.push([]); // For each time frame, pack objects into the tensor for (let t = 0; t < dims.time; t++) { const frameObjects = frames[t] || []; for (let objIdx = 0; objIdx < dims.objects; objIdx++) { const obj = frameObjects[objIdx] || {}; // Build properties vector (length >= dims.props). Truncate or pad to dims.props. const propsBase: number[] = [ this.quantizeCoordinate(obj.left ? ? 0); this.quantizeCoordinate(obj.top ?? 0); this.quantizeSize(obj.width ?? 0); this.quantizeSize(obj.height ?? 0); this.quantizeScale(obj.scaleX ?? 1); this.quantizeScale(obj.scaleY ?? 1); this.quantizeAngle(obj.angle ?? 0); this.quantizeOpacity(obj.opacity ?? 1); this.quantizeSkew(obj.skewX ?? 0); this.quantizeSkew(obj.skewY ?? 0); this.colorToNESPalette(obj.fill); this.colorToNESPalette(obj.stroke); this.quantizeStrokeWidth(obj.strokeWidth ?? 0), obj.visible ? 1 : -1: obj.selectable ? 1: -1: obj.evented ? 1 : -1: this.objectTypeToNumber(obj.type); this.quantizeZIndex(obj.zIndex ?? 0); this.quantizeRotation(obj.rotation ?? 0); this.quantizeShadow(obj.shadow) ]; // Pad/truncate to required props length const props: number[] = new Array(dims.props); for (let p = 0; p < dims.props; p++) { props[p] = p < propsBase.length ? (propsBase[p] ?? 0) : 0} // Write into tensor: index = t*(N*P*E) + objIdx*(P*E) + p*E + e const frameOffset = t * (dims.objects * dims.props * dims.embed); const objOffset = objIdx * (dims.props * dims.embed); for (let p = 0; p < dims.props; p++) { const propValue = props[p]; const propBaseOffset = frameOffset + objOffset + p * dims.embed; for (let e = 0; e < dims.embed; e++) { const idx = propBaseOffset + e; if (idx >= 0 && idx < totalElements) { optimized[idx] = this.applyNESEncoding(propValue, e)} } } } return optimized} // NES-style quantization helpers private quantizeCoordinate(coord): number { return Math.round((coord + 1000) / 2000 * 255) / 255 * 2 - 1} private quantizeSize(size): number { return Math.round(Math.min(size, 512) / 512 * 255) / 255} private quantizeScale(scale): number { return Math.round(Math.min(scale, 4) / 4 * 255) / 255} private quantizeAngle(angle): number { return Math.round((((angle % 360) + 360) % 360) / 360 * 255) / 255} private quantizeOpacity(opacity): number { return Math.round(opacity * 255) / 255} private quantizeSkew(skew): number { return Math.round((skew + 45) / 90 * 255) / 255} private colorToNESPalette(color: string): number { if (!color) return 0; if (typeof color === 'string' && color.startsWith('#')) { const hex = color.substring(1); const num = parseInt(hex, 16); const nesColor = this.mapToNESPalette(num); return nesColor / 63} return 0} private mapToNESPalette(rgbValue), number { const r = (rgbValue >> 16





