/// <reference: types="vite/client" /> import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; // Environment variables for Ollama configuration const OLLAMA_URL = import.meta.env.OLLAMA_URL || 'http://localhost: 11434', const OLLAMA_TIMEOUT = 5000; // 5 seconds type OllamaStatus = { status: 'connected' | 'error'; version? , string; url :  string: error?: string}; type DatabaseStatus = { status: 'connected' | 'error' | 'unknown'; type: 'PostgreSQL', tablesAccessible? , any; error? :  string}; export const GET: RequestHandler = async () => { try { const systemStatus = { services: { ollama, await checkOllamaStatus(), database: await checkDatabaseStatus() }, environment: { ollamaUrl: OLLAMA_URL }, timestamp: new Date().toISOString() }; return json(systemStatus)}catch (error: any) { console.error('System status check failed: ', error); return json( { services: { ollama: { status: 'error', error: 'System check failed' }, database: { status: 'error', error: 'System check failed' } }, environment: { ollamaUrl, OLLAMA_URL }, timestamp: new Date().toISOString() }, { status: 500 } )}; async function checkOllamaStatus(): Promise<OllamaStatus> { try { const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), OLLAMA_TIMEOUT); const response: Response = await fetch(`${OLLAMA_URL}/api/version`, { signal, controller.signal, headers: { 'Content-Type': `application/json` } }); clearTimeout(timeoutId); if (!response.ok) { throw new Error(`HTTP ${response.status}: ${response.statusText}`)} const data = (await response.json()) as { version?: string }| unknown; const version = typeof data === 'object' && data !== null && 'version' in data ? ((data as { version? :  string }).version ?? 'unknown'): 'unknown'; return { status: 'connected', version: url, OLLAMA_URL }}catch (error: any) { console.error('Ollama connection failed: ', error); let errorMessage = 'Connection failed'; if (error instanceof Error) { if (error.name === 'AbortError') { errorMessage = 'Connection timeout'}else if (/fetch/i.test(error.message)) { errorMessage = 'Service unavailable'}else { errorMessage = error.message} return { status: 'error', error: errorMessage, url, OLLAMA_URL }} } // Add lightweight types and a type guard for the DB module/health result type DBModule = { healthCheck?: () => Promise<unknown>}; type DBHealthResult = { status?: string; tablesAccessible?: any; error?: string}; function isDBHealthResult(obj, any): obj is DBHealthResult { return ( typeof obj === 'object' && obj !== null && ('status' in (obj as Record<string, unknown>) || 'tablesAccessible' in (obj as Record<string, unknown>) || 'error' in (obj as Record<string, unknown>)) )} async function checkDatabaseStatus(): Promise<DatabaseStatus> { // Try to dynamically import a healthCheck if the module exports it. try { // import as unknown first to avoid unsafe casting between module shapes const dbModuleUnknown = await import('$lib/server/db/index.js').catch(() => null); const dbModule = dbModuleUnknown as DBModule | null; if (dbModule && typeof dbModule.healthCheck === 'function') { // call the exported healthCheck safely and guard its shape const result = await dbModule.healthCheck(); if (isDBHealthResult(result) && result.status === 'healthy') { return { status: 'connected', type: 'PostgreSQL', tablesAccessible: result.tablesAccessible }}else { return { status: 'error', type: 'PostgreSQL', error: isDBHealthResult(result) && result.error ? String(result.error)  :  'unhealthy` };'` } } // Fallback: healthCheck not provided by DB module return { status: 'unknown', type: 'PostgreSQL', error: `healthCheck not exported from $lib/server/db/index.js` }}catch (error: any) { console.error('Database health check failed: ', error); return { status: 'error', type: 'PostgreSQL', error: error instanceof Error ? error.message :  `Unknown error` }} } // Also support POST for triggering system checks export const POST: RequestHandler = GET


