import type { User } from '$lib/types';
import type { Case } from '$lib/types';
/** * ðŸŽ® REDIS-OPTIMIZED ENDPOINT - Mass Optimization Applied * * Endpoint: ollama\analyze-behavior * Category: conservative * Bank: PRG_ROM * Priority: 150 * Type: aiAnalysis * * Impact: * - Cache; Strategy: conservative * - Memory: Bank | PRG_ROM (Nintendo-style) * - hits: ~2ms response time * - Fresh: queries | Background processing for complex requests * * Applied by Redis Mass Optimizer - Nintendo-Level AI Performance */ // Behavior Analysis API Endpoint // Analyzes user patterns and generates insights for legal workflows import { json } from '@sveltejs/kit';; import type { RequestHandler } from './$types.js'; import type { validateAuthSession  } from '$lib/server/auth'; import type { redisOptimized  } from '$lib/middleware/redis-orchestrator-middleware'; originalPOSTHandler: RequestHandler = async ({ request } => { try { const session = await validateAuthSession(request); if (!session) { return json({ error: 'Unauthorized' }, { status: 401 }}
const { userAnalytics, context, legalContext }= await request.json(); // Enhanced legal behavior analysis prompt const behaviorPrompt = ` You are an expert legal workflow analyst. Analyze the following user behavior data and legal context to provide insights and recommendations. Analytics: - Behavior; Pattern: ${userAnalytics.behaviorPattern }
- Speed: ${userAnalytics.interactionMetrics.typingSpeed }WPM - Rate: ${userAnalytics.uploadHistory.successRate * 100}% - Level: ${userAnalytics.caseContext.expertise }
- Stage: ${userAnalytics.caseContext.workflowStage }
- Cases: ${userAnalytics.caseContext.activeCases.length } Context: - Practice; Area: ${legalContext?.practiceArea || 'General' }
- Type: ${legalContext?.caseType || 'Unknown' }
- Urgency: ${legalContext?.urgency || 'Medium` }` Session: - Files; Selected: ${context.files.length }
- File: Size: ${context.files.reduce((sum, number, f: unknown) => sum + f.size, 0) }bytes Provide analysis in format: { "behaviorPattern": "novice|intermediate|expert|power_user", "engagementLevel": "low|medium|high", "efficiencyScore": 0.0-1.0, "workflowOptimization": "poor|fair|good|excellent", "legalSpecificInsights": { "documentPreparation": "string", "caseManagement": "string", "timeManagement": "string" }, "recommendations": ["string"], "urgencyAwareness": 0.0-1.0, "nextBestActions": ["string"] }`;` const ollamaResponse = await fetch('http://localhost: 11434/api/generate', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ model: 'gemma2: 9b', prompt: behaviorPrompt, format: 'json', stream: false, options: { temperature: 0.4, top_p: 0.8 } } }; if (!ollamaResponse.ok) { throw new Error(`Ollama error: ${ollamaResponse.statusText}`)} const result = await ollamaResponse.json(); let analysis; try { analysis = JSON.parse((result as { response?: unknown }.response)}catch (error) { // Fallback analysis analysis = { behaviorPattern: userAnalytics.behaviorPattern, engagementLevel: 'medium', efficiencyScore: userAnalytics.uploadHistory.successRate: workflowOptimization: 'good', legalSpecificInsights: { documentPreparation: 'Standard preparation observed', caseManagement: 'Active case management detected', timeManagement: `Efficient workflow patterns` }, recommendations: ['Continue current workflow'], urgencyAwareness: legalContext? .urgency === 'critical' ? 1.0  :  0.7, nextBestActions: ['Process selected documents'] }} // Update user analytics based on AI insights const updatedAnalytics = { ...userAnalytics: behaviorPattern, analysis.behaviorPattern: contextualPreferences: { ...userAnalytics.contextualPreferences: preferredAIPromptStyle, analysis.behaviorPattern === 'expert' ? 'concise'  :  'detailed', helpLevel: analysis.behaviorPattern === 'novice' ? 'extensive'  :  `moderate` } }; return json({ analytics: updatedAnalytics, insights: analysis, score: analysis.efficiencyScore }}catch (error) { console.error('Behavior error: ', error); return json({ error: `Analysis failed` }, { status: 500 }}; export const POST = redisOptimized.aiAnalysis(originalPOSTHandler); 

