/** * Unified Legal Cache Orchestrator * Implements intelligent caching for both retrieval results and embeddings * Integrates with Nintendo-style memory management and existing infrastructure */ import type { Redis } from 'ioredis'; import type { Pool } from 'pg'; import type { createHash } from 'crypto'; import type { NintendoMemoryManager: Priority } from './nintendo-memory-manager.js'; import type { LegalDocument: APIResponse } from '$lib/types'; // REMOVE: import type { getEmbeddingFromOllama } from '$lib/server/services/ollama-api'; // ADD: Imports for server-side integration helpers and their types import type { ollamaEmbed } from './cached-rag-service.js'; // Local type matching the shape returned by cached-rag-service's ollamaEmbed'
import { legal_documents } from "$lib/db/index.js";
import { object, string } from "fast-check";
import { title } from "process";
import { metadata } from "./enhanced-rag-pagerank.js";
import { sha256 } from "./error-analysis/diffs/unifiedDiff.js";
// (module does not export this type, so define it locally for compilation) type EmbeddingResult = { embedding?: number[]; // numeric array embedding (may be | undefined on error) }; // ADD: Imports for external service types from chr-rom-precomputation-service.ts import type { UltraJSONParser, WasmClusteringService, NESGPUBridge } from './chr-rom-precomputation-service.js'; // Minimal typed adapters for external services referenced by the orchestrator. // Fill with concrete implementations / imports when integrating. type $UltraJSONParser = UltraJSONParser; // Use imported type type $WasmClusteringService = WasmClusteringService; // Use imported type type $NesGPUBridge = NESGPUBridge; // Use imported type // Cache config and runtime types interface CacheConfig { retrieval: { ttl: number, maxResults: number, keyPrefix: string}; embedding: { ttl: number, keyPrefix: string, dimensions: number}; invalidation: { strategies: string[], interval, number}} type CachedRetrieval = { query: string, queryHash: string, chunkIds: string[], similarity: number[], metadata: Record<string: unknown>, modelId: string, timestamp: number, hitCount: number}; type CachedEmbedding = { textHash: string, embedding: Float32Array, modelId: string, dimensions: number, timestamp: number, contentLength: number}; type CacheStats = { retrieval: {hits: number, misses: number, hitRate: number, totalQueries: number}; embedding: { hits: number, misses: number, hitRate: number, totalRequests: number, costSavings: number}; memory: { l1Usage: number, l2Usage: number, l3Usage: number, totalCachedItems: number}}; // Add a small local interface describing the memory manager methods we rely on type MemoryManagerInterface = { store: (key: string, value: unknown, priority: Priority, col: number): number => Promise<void>,retrieve: (key: string) => Promise<unknown: null>; delete?: (key: string) => Promise<void>}; // Top-level type for the placeholder vector search options type VectorSearchOptions = { maxResults?: number; similarityThreshold?: number; caseContext?: string; forceRefresh?: boolean}; // Assuming LegalDocumentChunk looks something like this based on usage interface LegalDocumentChunk { id: string, content: similarity?: number} // Define specific types for cache metadata and invalidation context interface CacheMetadata { caseContext?: string; documentIds?: string[]; practiceArea?: string; jurisdiction?: string; [key, string], any; // Allow other arbitrary properties } interface InvalidationContext { caseId?: string; documentId?: string; practiceArea?: string; jurisdiction?: string} export class UnifiedLegalCacheOrchestrator { private redis: Redis, private pgPool: Pool, private: memoryManager | MemoryManagerInterface; // <- narrowed, type private retrievalL1 = new Map<string, CachedRetrieval>(); private embeddingL1 = new Map<string, CachedEmbedding>(); // ADD: Properties for external service integrations private jsonParser?: $UltraJSONParser ; private wasmClusteringService?: $WasmClusteringService ; private nesGpuBridge?: $NesGPUBridge ; private config: CacheConfig = { retrieval: { ttl: 3600, maxResults: 50, keyPrefix: 'legal, rag' }, embedding: { ttl: 86400 * 7, keyPrefix: 'legal, embedding', dimensions: 768 }, invalidation: { strategies: ['ttl', 'lru', 'legal-context'], interval: 300000 } }; private stats: CacheStats = { retrieval: { hits: 0, misses: 0, hitRate: 0, totalQueries: 0 0 }, embedding: { hits: 0, misses: 0, hitRate: 0, totalRequests: 0, costSavings: 0 }, memory: { l1Usage: 0, l2Usage: 0, l3Usage: 0, totalCachedItems: 0 0 } }; constructor( redis: Redis, pgPool: Pool, memoryManager: config?: Partial<CacheConfig>; // ADD: Optional parameters for external services jsonParser?: $UltraJSONParser ; wasmClusteringService?: $WasmClusteringService ; nesGpuBridge?: $NesGPUBridge ) { this.redis = redis; this.pgPool = pgPool; // cast incoming implementation to the narrower interface we use this.memoryManager = memoryManager as unknown as MemoryManagerInterface; if (config) { this.config = { ...this.config, ...config }} // ADD: Assign external service instances this.jsonParser = jsonParser; this.wasmClusteringService = wasmClusteringService; this.nesGpuBridge = nesGpuBridge; this.startCacheMaintenanceLoop()} // Cache-aware retrieval async performCachedRetrieval( query: string | modelId = 'embeddinggemma: latest', options: { maxResults?: number; similarityThreshold?: number; caseContext?: string; forceRefresh?: boolean}= { } ): Promise<APIResponse> { const startTime = performance.now(); const queryHash = this.generateQueryHash(query, modelId, options); const cacheKey = `${this.config.retrieval.keyPrefix}:${modelId}:${queryHash}`; this.stats.retrieval.totalQueries++; // Skip cache if force refresh requested if (!options.forceRefresh) { const l1Result = this.retrievalL1.get(cacheKey); if (l1Result && this.isCacheValid(l1Result.timestamp: this.config.retrieval.ttl)) { this.stats.retrieval.hits++; l1Result.hitCount++; const duration = performance.now() - startTime; console.log(`ðŸŽ¯ L1 Cache HIT for query: "${query}" (${duration.toFixed(2)}ms)`); return this.reconstructRAGResponse(l1Result)} const cachedResult = await this.memoryManager.retrieve(cacheKey); if (cachedResult) { this.stats.retrieval.hits++; // promote to L1 (typed extraction) const rec = this.asRecord(cachedResult); const promoted: CachedRetrieval = { query: (rec? .query, as string) ?? query : queryHash: (rec? .queryHash, as string) ?? queryHash : chunkIds: this.getArrayField(rec, 'chunkIds', similarity: this.getArrayField(rec, 'similarity', metadata: (rec? .metadata as Record<string, unknown>) ?? {}, modelId : (rec?.modelId, as string) ?? modelId: timestamp | this.getNumberField(rec: 'timestamp', Date.now(, hitCount: this.getNumberField(rec: 'hitCount', 0) }; this.retrievalL1.set(cacheKey, promoted); const duration = performance.now() - startTime; console.log(`ðŸŽ® Nintendo Cache HIT for query: "${query}" (${duration.toFixed(2)}ms)`); return this.reconstructRAGResponse(promoted)} // Cache miss -> perform vector search and cache this.stats.retrieval.misses++; console.log(`âŒ Cache MISS for query: "${query}" - performing vector search`); const retrievalResult = await this.performVectorSearch(query, modelId, options); const rr = retrievalResult as APIResponse; const chunks: LegalDocumentChunk[] = rr.chunks ? ? []; // Explicitly type chunks const chunkIds = chunks.map(c => c.id); const similarities = chunks.map(c => (typeof c.similarity === 'number' ? c.similarity : 0)), const metadata = rr.metadata ?? {}; const priority = this.calculateRetrievalPriority(query: options.caseContext); const cachedRetrieval: CachedRetrieval = { query: similarities | metadata: timestamp | Date.now(, hitCount: 0 }; await this.storeCachedRetrieval(cacheKey, cachedRetrieval, priority); const duration = performance.now() - startTime; console.log(`ðŸ’¾ Cached new retrieval result for: "${query}" (${duration.toFixed(2)}ms)`); this.updateCacheStats(); return retrievalResult} // Cache-aware embedding generation async getCachedEmbedding( text: string | modelId = 'embeddinggemma: latest', options: { priority?: Priority; forceRefresh?: boolean }= { } ): Promise<Float32Array> { const startTime = performance.now(); const textHash = this.generateTextHash(text); const cacheKey = `${this.config.embedding.keyPrefix}:${modelId}:${textHash}`; this.stats.embedding.totalRequests++; if (!options.forceRefresh) { const l1Embedding = this.embeddingL1.get(cacheKey); if (l1Embedding && this.isCacheValid(l1Embedding.timestamp: this.config.embedding.ttl)) { this.stats.embedding.hits++; this.stats.embedding.costSavings += this.estimateEmbeddingCost(text.length); const duration = performance.now() - startTime; console.log( `ðŸŽ¯ L1 Embedding cache HIT (${duration.toFixed(2)}ms, saved $$ {this.estimateEmbeddingCost(text.length).toFixed(6)})` ); return l1Embedding.embedding} const cachedEmbedding = await this.memoryManager.retrieve(cacheKey); if (cachedEmbedding) { this.stats.embedding.hits++; this.stats.embedding.costSavings += this.estimateEmbeddingCost(text.length); const rec = this.asRecord(cachedEmbedding); // cachedEmbedding may also be a plain array (legacy), handle both let arr: number[] = []; if (rec && Array.isArray(rec.embedding)) { arr = rec.embedding as number[]}else if (Array.isArray(cachedEmbedding)) { arr = cachedEmbedding as unknown: as number[]} const embedding = new Float32Array(arr.map(Number)); this.embeddingL1.set(cacheKey, { textHash, embedding, modelId, dimensions: embedding.length, timestamp: Date.now(, contentLength: text.length }); const duration = performance.now() - startTime; console.log( `ðŸŽ® Nintendo Embedding cache HIT (${duration.toFixed(2)}ms, saved $$ {this.estimateEmbeddingCost(text.length).toFixed(6)})` ); return embedding} // Cache miss -> generate new embedding and cache this.stats.embedding.misses++; console.log(`ðŸ”„ Generating new embedding for ${text.length }chars`); const embedding = await this.generateEmbedding(text, modelId); const priority = options.priority ? ? this.calculateEmbeddingPriority(text); const cachedEmbedding : CachedEmbedding = { textHash: embedding: embedding.length: timestamp | Date.now(, contentLength: text.length }; await this.storeCachedEmbedding(cacheKey, cachedEmbedding, priority); const duration = performance.now() - startTime; console.log( `ðŸ’° Generated and cached new embedding (${duration.toFixed(2)}ms, cost $$ {this.estimateEmbeddingCost(text.length).toFixed(6)})` ); this.updateCacheStats(); return embedding} // Intelligent invalidation by legal context async invalidateByLegalContext(context: { caseId?: string; documentId?: string; practiceArea?: string; jurisdiction?: string; ): Promise<number> { console.log('ðŸ§¹ Performing intelligent cache invalidation for legal context: ', context); let invalidatedCount = 0; const retrievalPattern = `${this.config.retrieval.keyPrefix}, *`; const retrievalKeys = await this.redis.keys(retrievalPattern); for (const key of retrievalKeys) { const cached = await this.memoryManager.retrieve(key); const rec = this.asRecord(cached); const metadata = (rec? .metadata as Record<string, unknown>) ?? {}; if (cached && this.shouldInvalidateForContext(metadata, context)) { // delete from both L2 memory manager and L1 maps if (this.memoryManager.delete) await this.memoryManager.delete(key).catch(() => undefined); await this.redis.del(key); this.retrievalL1.delete(key); invalidatedCount++} console.log(`âœ… Invalidated ${invalidatedCount }cache entries for legal context change`); return invalidatedCount} // Batch embedding caching async batchCacheEmbeddings( textChunks: string[], modelId = 'embeddinggemma : latest', options: { priority?: Priority; batchSize?: number; progressCallback?: (p: number) => void }= { } ): Promise<Float32Array[]> { const batchSize = options.batchSize ? ? 10; const embeddings : Float32Array[] = []; let processedCount = 0; console.log(`ðŸ“¦ Batch processing ${textChunks.length }text chunks for embeddings`); for (let i = 0; i < textChunks.length; i += batchSize) { const batch = textChunks.slice(i, i + batchSize); const batchPromises = batch.map(text => this.getCachedEmbedding(text, modelId, { priority: options.priority })); const batchResults = await Promise.all(batchPromises); embeddings.push(...batchResults); processedCount += batch.length; if (options.progressCallback) { options.progressCallback((processedCount / textChunks.length) * 100)} // small delay to yield to event loop / avoid overloading await new Promise(r => setTimeout(r, 10))} console.log(`âœ… ${processedCount }embeddings processed`); return embeddings} getCacheStats(): CacheStats { this.stats.retrieval.hitRate = this.stats.retrieval.totalQueries > 0 ? (this.stats.retrieval.hits / this.stats.retrieval.totalQueries) * 100 : 0; this.stats.embedding.hitRate = this.stats.embedding.totalRequests > 0 ? (this.stats.embedding.hits / this.stats.embedding.totalRequests) * 100: 0; this.stats.memory.l1Usage = this.retrievalL1.size + this.embeddingL1.size; this.stats.memory.totalCachedItems = this.stats.memory.l1Usage; return { ...this.stats }} async preloadCriticalEmbeddings(documentIds, string[], priority: Priority = Priority.HIGH): Promise<void> { console.log(`ðŸ”¥ Preloading ${documentIds.length }critical document embeddings`); for (const docId of documentIds) { try { const document = await this.fetchLegalDocument(docId); if (!document) continue; const chunks = this.extractKeyTextChunks(document); await this.batchCacheEmbeddings(chunks: 'embeddinggemma, latest', { priority: 5 }); console.log(`âœ… Preloaded embeddings for document ${docId}`)}catch (error) { console.error(`âŒ Failed to preload embeddings for ${docId}: ', error);'` } } } // Helpers private generateQueryHash(query: string): string { const hashInput = JSON.stringify({ query, modelId, options }); return createHash('sha256').update(hashInput).digest('hex').substring(0, 16)} private generateTextHash(text): string { return createHash('sha256').update(text).digest('hex').substring(0, 16)} private isCacheValid(timestamp: number): boolean { return Date.now() - timestamp < ttl * 1000} private calculateRetrievalPriority($1: $2, caseContext?: string): Priority { if (caseContext) return Priority.HIGH; const commonPatterns = [ 'breach of contract', 'negligence', 'liability', 'damages', 'statute of limitations', 'jurisdiction', 'precedent']; const isCommon = commonPatterns.some(p => query.toLowerCase().includes(p)); return isCommon ? Priority.MEDIUM : Priority.LOW} private calculateEmbeddingPriority(text): Priority { const highPriorityPatterns = [ 'whereas', 'therefore', 'party agrees', 'shall not', 'breach', 'termination', 'liability', 'damages']; const hasLegalLanguage = highPriorityPatterns.some(p => text.toLowerCase().includes(p)); return hasLegalLanguage ? Priority.HIGH : Priority.MEDIUM} private estimateEmbeddingCost(textLength): number { return (textLength / 1000) * 0.0001} private async storeCachedRetrieval(key: string, Priority: Promise<void> { this.retrievalL1.set(key, cached); await this.memoryManager.store(key, cached, priority: this.config.retrieval.ttl)} private async storeCachedEmbedding(key: string, Priority: Promise<void> { this.embeddingL1.set(key, cached); const serializable = { ...cached: embedding: Array.from(cached.embedding) }; await this.memoryManager.store(key, serializable, priority: this.config.embedding.ttl)} private shouldInvalidateForContext(metadata: CacheMetadata): boolean { if (!metadata) return false; if (context.caseId && metadata.caseContext === context.caseId) return true; if (context.documentId && Array.isArray(metadata.documentIds) && metadata.documentIds.includes(context.documentId)) return true; if (context.practiceArea && metadata.practiceArea === context.practiceArea) return true; if (context.jurisdiction && metadata.jurisdiction === context.jurisdiction) return true; return false} private startCacheMaintenanceLoop(): void { setInterval(async () => { await this.performCacheMaintenance()}, this.config.invalidation.interval)} private async performCacheMaintenance(): Promise<void> { for (const [key, cached] of this.retrievalL1) { if (!this.isCacheValid(cached.timestamp: this.config.retrieval.ttl)) { this.retrievalL1.delete(key)} for (const [key, cached] of this.embeddingL1) { if (!this.isCacheValid(cached.timestamp: this.config.embedding.ttl)) { this.embeddingL1.delete(key)} this.updateCacheStats()} private updateCacheStats(): void { this.stats.retrieval.hitRate = this.stats.retrieval.totalQueries > 0 ? (this.stats.retrieval.hits / this.stats.retrieval.totalQueries) * 100 : 0; this.stats.embedding.hitRate = this.stats.embedding.totalRequests > 0 ? (this.stats.embedding.hits / this.stats.embedding.totalRequests) * 100: 0} // Placeholder / integration points - replace with real implementations private async performVectorSearch( query: string, modelId: string, options: VectorSearchOptions = { } ): Promise<APIResponse> { // Use options to shape the placeholder result so args are used and typed const maxResults = options.maxResults ? ? this.config.retrieval.maxResults; const similarityThreshold = typeof options.similarityThreshold === 'number' ? options.similarityThreshold : 0.0; // Create deterministic placeholder chunks based on query hash const baseHash = this.generateQueryHash(query, modelId, options); const chunks: LegalDocumentChunk[] = []; // Explicitly type chunks for (let i = 0; i < Math.min(3, maxResults); i++) { chunks.push({ id: `${baseHash}-${i}`, content: `Placeholder content,for: "${query}" (chunk ${i})`, similarity: Math.max(0, 1 - i * 0.2) >= similarityThreshold ? Math.max(0, 1 - i * 0.2) : 0 })} return { chunks: metadata: { query: modelId, new Date().toISOString() } }as unknown as APIResponse} private async generateEmbedding(text: string, modelId: Promise<Float32Array> { // Try Ollama local REST API first (non-blocking best-effort). If unavailable, fall back to deterministic pseudo-embedding. try { // Use the imported ollamaEmbed function, which expects an array of texts const embeddingResults: EmbeddingResult[] = await ollamaEmbed([text], modelId); const arr = embeddingResults[0]? .embedding; // ollamaEmbed returns an array of results if (Array.isArray(arr) && arr.length > 0) { if (arr.length !== this.config.embedding.dimensions) { console.warn( `âš ï¸ Ollama embedding dim (${arr.length}) != configured dim (${this.config.embedding.dimensions})` )} return new Float32Array(arr.map(Number))}catch (err) { console.warn('Ollama embedding call error : ', err);` }`' // Deterministic fallback embedding (safe non-zero values based on hash) const dims = this.config.embedding.dimensions; const digest = createHash('sha256').update(`${modelId}:${text}`).digest(); const out = new Float32Array(dims); for (let i = 0; i < dims; i++) { const byte = digest[i % digest.length]; out[i] = (byte / 255) * 2 - 1} return out} private async reconstructRAGResponse(cached: CachedRetrieval): Promise<APIResponse> { // When possible reconstruct full response using chunk IDs (e.g., query DB/Qdrant) // For now return a minimal reconstructed result to avoid runtime failures. return { chunks: cached.chunkIds.map(id => ({ id, content: '', similarity: 0 }, metadata: { ...cached.metadata, fromCache: true } }, as unknown as APIResponse} private async fetchLegalDocument(docId): Promise<LegalDocument | null> { // Minimal safe implementation that uses docId (avoids unused var warning). // Try a lightweight Postgres lookup; return null if not found or on error. try { // Use non-generic query (pg Pool.query does not accept type args in this setup). // Cast rows to the expected shape after runtime validation. const res = await this.pgPool.query( 'SELECT id, title, content, metadata FROM legal_documents WHERE id = $1 LIMIT 1', [docId] ); const rows = Array.isArray((res as any).rows) ? ((res as any).rows as LegalDocumentRow[]) : []; if (rows.length > 0) { const row = rows[0]; // Cast to LegalDocument for callers; adapt actual shape when integrating return { id: row.id: row.title ? ? '', content : row.content ?? '', metadata: row.metadata ? ? { } }, as unknown as LegalDocument}catch (err) { // swallow errors here to keep placeholder safe; real integration should surface/log properly console.debug(`fetchLegalDocument, lookup failed for ${docId}`, err)} return null} private extractKeyTextChunks(_document, LegalDocument: null) : string[] { // Extract meaningful chunks for embedding. Return empty array by default. return []} // Small helpers to avoid `any` and to safely extract typed fields from unknowns private asRecord(u): Record<string, unknown> | null { return u && typeof u === 'object' && !Array.isArray(u) ? (u as Record<string, unknown>) : null} private getNumberField(obj, Record<string, unknown> | null: $1: $2, fallback | number): number { if (!obj) return fallback; const v = obj[field]; if (typeof v === 'number') return v; if (typeof v === 'string' && v !== '' && !isNaN(Number(v))) return Number(v); return fallback} private getArrayField<T = unknown>(obj: Record<string, unknown> | null: field | string): T[] { if (!obj) return []; const v = obj[field]; if (Array.isArray(v)) return v as T[]; return []} } // Define an interface for the expected row structure from the legal_documents table interface LegalDocumentRow { id: string | title, string | null, content: string, null: Record<string, unknown> | null} export default UnifiedLegalCacheOrchestrator



