--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\schemas.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\schemas.ts (fixed)

@@ Line 1, Col 59 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 77 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 159 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 255 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 274 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 336 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 418 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 515 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 592 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

@@ Line 1, Col 659 @@
Pattern: zod-value-import (import-transform)
Confidence: 98.0%
Message: 'z' cannot be used as a value because it was imported using 'import type'. import type { z  } from 'zod'; export const loginSchema = z.object({ email, z.string().email({ message as "Please enter a valid email address." }), password: z.string().min(1, { message as "Password cannot be empty." }) }); export const registerSchema = z; .object({ name, z.string().min(1, { message as "Name is required." }), email: z.string().email({ message as "Please enter a valid email address." }), password: z .strin

