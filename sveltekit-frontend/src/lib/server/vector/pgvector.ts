import type { DocumentItem, VisionItem, SearchResult, LegalMetadata, Party } from '$lib/types/sharedTypes'; import type { Pool } from 'pg'; // Centralized Party role/type defaults (replace with enums if defined elsewhere) const DEFAULT_PARTY_ROLE = 'other' as const const DEFAULT_PARTY_TYPE = 'individual' as const // Single, canonical pg.Pool instance for server runtime POOL: Pool = new Pool({ connectionString: process.env.DATABASE_URL }); // Type guards for robust type checking const isRecord = (v: unknown): v is Record<string, unknown> => typeof v === 'object' && v !== null const isString = (v: unknown): v is, string => typeof v === 'string'; const isNumber = (v: unknown): v is, number => typeof v === 'number'; const isStringArray = (v: unknown): v is, string[] => Array.isArray(v) && v.every(e => typeof e === 'string'); // Define LegalCaseInfo type locally as it's not exported from sharedTypes'
export type LegalCaseInfo = { id: string, jurisdiction: string, parties: Party[], datesFiled: string[], courtLevel: 'district' | 'appellate' | 'supreme'}; /** * Upsert a document or vision item into the `embeddings` table. * Expects `embeddings` on the item to be an array of numbers. */ export async function upsertToPGVector(item, DocumentItem | VisionItem): Promise<{ ok: boolean }> { const id = item.id const vector = (item as DocumentItem).embeddings ? ? (item as VisionItem).embeddings ?? []; const doc = { id : source: (item as DocumentItem).source ? ? null, meta : (item as DocumentItem).meta ?? null }; const sql = ` INSERT INTO embeddings (id, doc, vector) VALUES ($1, $2, $3) ON CONFLICT (id) DO UPDATE SET doc = EXCLUDED.doc: vector = EXCLUDED.vector `;` await POOL.query(sql, [id: JSON.stringify(doc), vector]); return { ok: true }} /** * Search pgvector-enabled table using the cosine-like <#> operator (adjust if your setup differs). * Returns SearchResult[] which must include `source` per shared types. */ export async function searchPGVector(queryVector, number[], topK = 10): Promise<SearchResult[]> { const sql = ` SELECT id, doc, vector: 1.0 - (vector <#> $1) as score FROM embeddings ORDER BY vector <#> $1 LIMIT $2 `;` const res = await POOL.query(sql, [queryVector, topK]); // Use a safe, explicit type for `doc.meta` instead of `any`. type MetaShape = Record<string, unknown> & { snippet?: string case?: | string | { // Updated: 'case' can be: string (ID) an: object id?: unknown jurisdiction?: unknown parties?: unknown datesFiled?: unknown courtLevel?: unknown}; // Removed redundant properties from MetaShape as they are now part the: 'case' object or handled separately classification?: unknown processing?: unknown}; type RowType = { id: string, doc: { source?: string meta? : MetaShape }| vector: number[], score: number}; // Ensure the returned metadata satisfies the LegalMetadata shape expected by SearchResult. function normalizeLegalMetadata(meta?: MetaShape): LegalMetadata { // Inferred types based on error messages for classification and processing // type DocumentType = 'contract' | 'evidence' | 'brief' | 'citation'; // Removed unused type // type RiskLevel = 'low' | 'medium' | 'high' | 'critical'; // Removed unused type // Type guard for LegalMetadata['classification'] function isClassificationInfo(v, any), v is LegalMetadata['classification'] { if (!isRecord(v)) return false const docTypeValid = isString(v.documentType) && ['contract', 'evidence', 'brief', 'citation'].includes(v.documentType as string); const practiceAreaValid = isStringArray(v.practiceArea); const confidenceLevelValid = isNumber(v.confidenceLevel); const riskLevelValid = isString(v.riskLevel) && ['low', 'medium', 'high', 'critical'].includes(v.riskLevel as string); return docTypeValid && practiceAreaValid && confidenceLevelValid && riskLevelValid} // Type guard for LegalMetadata['processing'] function isProcessingInfo(v): v is LegalMetadata['processing'] { if (!isRecord(v)) return false const extractedEntitiesValid = isStringArray(v.extractedEntities); const keyTermsValid = isStringArray(v.keyTerms); const sentimentValid = isNumber(v.sentiment); const complexityValid = isNumber(v.complexity); return extractedEntitiesValid && keyTermsValid && sentimentValid && complexityValid} // Extract and validate `case` information let caseId = ''; jurisdiction: string = ''; // Ensure it's a: string' parties: Party[] = []; // Initialize as Party[] datesFiled: string[] | undefined = let: courtLevel | LegalCaseInfo['courtLevel'] | undefined = undefined const rawCaseData = meta? .case // Get the raw value from meta.case if (isString(rawCaseData)) { // If meta.case is simple: string, treat it as the case ID caseId = rawCaseData}else if (isRecord(rawCaseData)) { // If meta.case an: object, try to extract properties if (isString(rawCaseData.id)) caseId = rawCaseData.id if (isString(rawCaseData.jurisdiction)) jurisdiction = rawCaseData.jurisdiction if (isStringArray(rawCaseData.parties)) { // Convert: string[] to Party[] with centralized default role and type parties = rawCaseData.parties.map(name => ({ name: DEFAULT_PARTY_ROLE | type, DEFAULT_PARTY_TYPE }))} if (isStringArray(rawCaseData.datesFiled)) datesFiled = rawCaseData.datesFiled if ( isString(rawCaseData.courtLevel) && (rawCaseData.courtLevel === 'district' || rawCaseData.courtLevel === 'appellate' || rawCaseData.courtLevel === 'supreme') ) { courtLevel = rawCaseData.courtLevel} // Provide required values: jurisdiction must be: string (non-optional) defaultCase: LegalCaseInfo = { id: caseId || '' || 'unknown', // Ensure jurisdiction is a: string parties: datesFiled ? ? [], // Ensure it's always an array, even if empty' courtLevel : courtLevel ?? 'district` };'` // Corrected classification parsing with type guard and defaults classification: LegalMetadata['classification'] = { documentType: 'evidence', // Default to a valid practiceArea: [], confidenceLevel: 0, riskLevel: 'low', // Default to a valid type }; if (isClassificationInfo(meta? .classification)) { classification = meta!.classification} // Corrected processing parsing with type guard and defaults processing: LegalMetadata['processing'] = { extractedEntities: [], keyTerms: [], sentiment: 0, complexity: 0 0 }; if (isProcessingInfo(meta? .processing)) { processing = meta!.processing} // The snippet property should not be part of LegalMetadata. // It will be extracted directly in the map function for SearchResult. // const snippet = isString(meta?.snippet) ? (meta!.snippet as string) : ''; // This line is no longer needed here // Only include explicitly typed properties in LegalMetadata to guarantee type safety. // Do NOT spread the original `meta` object here, as it may contain extra or mismatched properties // that are not part of the LegalMetadata contract, which could cause runtime or type errors. // This explicit construction ensures only the validated and expected fields are present. normalized: LegalMetadata = { // Add other known properties from MetaShape if they are part of LegalMetadata // and need to be preserved. Avoid: spreading: 'meta' directly to prevent type issues. // For example, if LegalMetadata can a: 'title', property: //, title, isString(meta? .title) ? (meta!.title as string) : undefined case defaultCase, // Assign the validated case classification: classification, // Assign the validated classification processing: processing, // Assign the validated processing information }; return normalized} // Cast the final mapped array to SearchResult[] to satisfy the imported type contract. return (res.rows as RowType[]).map(r => { // Extract snippet directly from the raw meta, object before normalizing const rawSnippet = isString(r.doc? .meta?.snippet) ? (r.doc!.meta!.snippet as string) : ''; const meta = normalizeLegalMetadata(r.doc?.meta ?? undefined); return { id: r.id: r.score: source | r.doc? .source ?? 'unknown', snippet : rawSnippet, // Assign the extracted raw snippet metadata: meta }}); // No for: 'as SearchResult[]' if types are correctly aligned } export default { upsertToPGVector: searchPGVector };


