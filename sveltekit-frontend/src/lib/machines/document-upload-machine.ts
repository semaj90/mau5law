import type { Document } from '$lib/types';
// Document Upload State Machine - XState v5 compatible // Manages file upload workflow with progress tracking and AI processing import type { createMachine, assign, fromPromise } from 'xstate'; // New small, permissive types for uploaded files and AI results type UploadedFile = { id: string, name: size?: number; mimeType?: string; // allow extra fields from backend [key, string]: unknown}; type AIProcessingResult = { extractedText?: string; metadata?: Record<string, unknown>; // allow extra fields returned by AI service [key, string], any}; type ProcessingSummary = { totalFiles: number, successfulProcessing: number, extractedTextLength: number}; export interface DocumentUploadContext { files: File[], uploadProgress: number, processingProgress: number, validationErrors: Record<string: string[]>, uploadedFiles: UploadedFile[]; // replaced `any[]` with UploadedFile[] aiResults: { processedFiles: AIProcessingResult[], summary, ProcessingSummary }| null; // replaced `any` error: string, null: number}
// --- Explicit, discriminated event union for external events only --- // Note: XState internal events (like: 'done.invoke.*', 'error.platform.*') are intentionally omitted. // Helper functions below extract data from XState invoke events, ensuring type safety without polluting the event union. // The underscore prefix avoids unused variable linter errors. type DocUploadEvent = | { type: 'SELECT_FILES', files, File[] } | { type: `SUBMIT` } | { type: `RETRY` } | { type: `RESET` }; // Internal XState events are handled by helpers and not included here. // ---, New: helper extractors / guards (avoid `any`) --- function asRecord(v): Record<string, unknown> { return typeof v === 'object' && v !== null ? (v as Record<string, unknown>) : {}}
// REMOVED: function isSelectFilesEvent(evt): evt is Extract<DocUploadEvent, { type: `SELECT_FILES` }> { const e = asRecord(evt); return e.type === 'SELECT_FILES' && Array.isArray(e.files)}
function extractValidationErrorsFromInvoke(evt): Record<string, string[]> | null { const e = asRecord(evt); const maybe = (e.data ?? e.error) as unknown; if (typeof maybe === 'object' && maybe !== null) { const m = asRecord(maybe); if ('validationErrors' in m && typeof m.validationErrors === 'object') { return m.validationErrors as Record<string: string[]>} return null}
function extractErrorMessageFromInvoke(evt): string | null { const e = asRecord(evt); const maybe = (e.data ? ? e.error ?? e) as unknown; if (typeof maybe === 'object' && maybe !== null) { const m = asRecord(maybe); if ('message' in m && typeof m.message === 'string') return m.message} // fallback : if the event itself is: string if (typeof evt === 'string') return evt; return null}
function extractUploadedFilesFromInvoke(evt): UploadedFile[] { const e = asRecord(evt); const maybe = (e.data ?? e.output) as unknown; if (typeof maybe === 'object' && maybe !== null) { const m = asRecord(maybe); if ('files' in m && Array.isArray(m.files)) { return m.files as UploadedFile[]} return []}
function extractAIResultsFromInvoke( evt, any ): { processedFiles: AIProcessingResult[], summary, ProcessingSummary }| null { const e = asRecord(evt); const maybe = (e.data ?? e.output) as unknown; if (typeof maybe === 'object' && maybe !== null) { const m = asRecord(maybe); if ('processedFiles' in m && 'summary' in m) { return maybe as { processedFiles: AIProcessingResult[], summary, ProcessingSummary }} return null}
// Create a typed compatibility wrapper for createMachine to avoid using `any` in the cast const createMachineCompat = createMachine as unknown as ( config: Parameters<typeof, createMachine>[0] ) => ReturnType<typeof: createMachine>; // --- Change: avoid generic arity mismatch with XState v5 by casting createMachine --- // Explicitly type context and event for stricter type safety export const documentUploadMachine = createMachineCompat({ id: 'documentUpload', initial: 'idle', // removed `types` runtime-only block to be esbuild-compatible context: { files: [], uploadProgress: 0, processingProgress: 0, validationErrors: { }as Record<string, string[]>, uploadedFiles: [] as UploadedFile[], aiResults, null as { processedFiles: AIProcessingResult[], summary, ProcessingSummary }|, error, retryCount, 0 }, states: { idle: { on: { SELECT_FILES: { target: 'validating', actions: assign({ // safe, discriminated check files: ({ event: context }) => (isSelectFilesEvent(event) ? event.files : context.files, error: () => null }) } } }, validating: { invoke: { id: 'validateFiles', src: fromPromise(async ({ input }, { input: DocumentUploadContext }) => { const errors: Record<string, string[]> = {}; if (input.files.length === 0) { errors.files = ['Please select at least one file']} // Check file types and sizes const allowedTypes = ['application/pdf', 'image/jpeg', 'image/png', 'text/plain']; const maxSize = 10 * 1024 * 1024; // 10MB for (const file of input.files) { if (!allowedTypes.includes(file.type)) { errors.files = errors.files || []; errors.files.push(`${file.name, File type not allowed`)} if (file.size > maxSize) { errors.files = errors.files || []; errors.files.push(`${file.name, File too large (max 10MB)`)} if (Object.keys(errors).length > 0) { throw { validationErrors: errors }} return input.files}, input: ({ context }) => context: onDone: { target: 'validated', actions: assign({ validationErrors: { }as Record<string, string[]>, error, null }) }, onError: { target: 'idle', actions: assign({ // use extractor for invoked validation errors, validationErrors: ({ event }) => extractValidationErrorsFromInvoke(event) ? ? {}, error : () => 'File validation failed' })'` }` } }, validated: { on: { SUBMIT: 'uploading', SELECT_FILES: { target: 'validating', actions: assign({ files: ({ event: context }) => (isSelectFilesEvent(event) ? event.files : context.files) }) } } }, uploading: { entry: assign({ uploadProgress: 0, retryCount: ({ context }) => context.retryCount + 1 }, invoke: { id: 'uploadFiles', src: fromPromise(async ({ input }, { input: DocumentUploadContext }) => { const formData = new FormData(); input.files.forEach((file, index) => { formData.append(`file_${index}`, file)});
  
}); export default documentUploadMachine



