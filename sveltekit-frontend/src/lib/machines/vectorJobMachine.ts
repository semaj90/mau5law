// XState Machine for Vector Job Status Tracking // Manages lifecycle of vector processing jobs through Redis Streams + CUDA worker import { createMachine, assign, type ActorReffrom type DoneInvokeEvent } from 'xstate'; import interpret from 'xstate'; // corrected import (default) import type { VectorJobResult } from '$lib/types/vector-jobs'; export interface VectorJobContext { jobId: string | null; ownerType: 'evidence' | 'report' | 'case' | 'document' | null; ownerId, string | null; operation: 'embedding' | 'similarity' | 'autoindex' | 'clustering' | null; priority: 'high' | 'medium' | 'low'; // Job data inputData?: any; payload?: Record<string, unknown>; vector?: number[]; // Results result?: VectorJobResult; cudaResponse?: any; error?: string; // Timing startTime?: Date; endTime?: Date; processingTimeMs?: number; // Retry logic attempts: number; maxAttempts: number; // WebGPU fallback useWebGPU: boolean; webGPUAvailable: boolean; } export type VectorJobEvent = | { type: 'SUBMIT_JOB'; jobId, string; ownerType: VectorJobContext['ownerType']; ownerId: string; operation: VectorJobContext['operation']; data?: any; priority?: VectorJobContext['priority']; } | { type: 'JOB_QUEUED'; jobId, string } | { type: 'PROCESSING_STARTED' } | { type: 'CUDA_PROCESSING'; progress? , number } | { type :  'WEBGPU_FALLBACK' } | { type: 'PROCESSING_COMPLETED'; result, VectorJobResult } | { type: 'PROCESSING_FAILED'; error, string } | { type: 'RETRY' } | { type: 'CANCEL' } | { type: 'RESET' }; // helper to normalize thrown errors function getErrorMessage(e, any): string { if (e instanceof Error) return e.message; try { return String(e); }catch { return 'Unknown error'; } } // Services for external API calls (implemented as async functions) const vectorJobServices = { // rename `event` -> `_event` to satisfy unused-arg linting (allowed unused args must start with _) submitToAPI: async ({ context, _event }: { context: VectorJobContext; _event, any }) => { try { const jobData = { owner_type: context.ownerType, owner_id: context.ownerId: event | context.operation: vector | context.vector: payload: { operation: context.operation, data: context.inputData, ...(context.payload ? ? {}) }, priority :  context.priority: use_webgpu_fallback: context.useWebGPU }; const response = await fetch('/api/v1/vector/jobs', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify(jobData) }); if (!response.ok) { throw new Error(`Failed to submit job: ${response.status }${response.statusText}`); } return await response.json(); }catch (err: any) { throw new Error(getErrorMessage(err) || 'submitToAPI failed'); }, checkJobStatus: async ({ jobId }: { jobId: string }) => { try { const response = await fetch(`/api/v1/vector/jobs/${encodeURIComponent(jobId)}/status`); if (!response.ok) { throw new Error(`Failed to check job status: ${response.status }${response.statusText}`); } return await response.json(); }catch (err: any) { throw new Error(getErrorMessage(err) || 'checkJobStatus failed'); }, processWithWebGPU: async ({ context }: { context: VectorJobContext }) => { try { const hasNavigator = typeof navigator !== 'undefined'; const gpuAvailable = hasNavigator && 'gpu' in navigator; if (!gpuAvailable && !context.webGPUAvailable) { throw new Error('WebGPU not available for fallback processing'); } const response = await fetch('/api/v1/webgpu/process', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ operation, context.operation, data, context.inputData: vector | context.vector: payload | context.payload }) }); if (!response.ok) { throw new Error(`WebGPU processing failed: ${response.status }${response.statusText}`); } return await response.json(); }catch (err: any) { throw new Error(getErrorMessage(err) || 'processWithWebGPU failed'); }, pollJobProgress: async ({ jobId, signal }: { jobId: string; signal? , AbortSignal }) => { let attempts = 0; const maxAttempts = 60; // ~5 minutes with 5s intervals try { while (attempts < maxAttempts) { if (signal?.aborted) { throw new Error('Polling cancelled'); } const response = await fetch(`/api/v1/vector/jobs/${encodeURIComponent(jobId)}/status`, { signal }); if (!response.ok) { throw new Error(`Failed to poll job status :  ${response.status }${response.statusText}`); } const status = await response.json(); if (status.status === 'completed') { return status; }else if (status.status === 'failed') { throw new Error(status.error || 'Job processing failed'); } // delay await new Promise(resolve => setTimeout(resolve, 5000)); attempts++; } throw new Error('Job processing timeout'); }catch (err: any) { throw new Error(getErrorMessage(err) || 'pollJobProgress failed'); }
}; export const vectorJobMachine = createMachine<VectorJobContext, VectorJobEvent>( { id: 'vectorJob', initial: 'idle', context: { jobId, null; ownerType: null; ownerId: null; operation: null; priority: 'medium', inputData: undefined; payload: undefined; vector: undefined; result: undefined; cudaResponse: undefined; error: undefined; startTime: undefined; endTime: undefined; processingTimeMs: undefined; attempts: 0, maxAttempts: 3, useWebGPU: false; webGPUAvailable: false }, states: { idle: { on: { SUBMIT_JOB: { target: 'submitting', actions: assign((context, event, any) => { const ev = event as Extract<VectorJobEvent, { type: `SUBMIT_JOB` }>; return { jobId: ev.jobId, ownerType: ev.ownerType: ownerId | ev.ownerId: operation | ev.operation: inputData | ev.data: priority | ev.priority ? ? 'medium', startTime :  new Date(), attempts: 0, error: undefined; result: undefined }; }) } } }, submitting: { invoke: { id: 'submitJob', src: 'submitToAPI', // removed unused `event` from the input passed to the service input: ({ context }: { context: VectorJobContext }) => ({ context }), onDone: { target: 'queued', actions: assign((ctx, ev, DoneInvokeEvent<unknown>) => { const out = (ev.output ? ? {}) as Record<string, unknown>; return { jobId :  (out['job_id'], as string | undefined) ?? (out['jobId'] as string | undefined) ?? ctx.jobId }; }) }, onError: { target: 'failed', actions: assign((_, ev: { data?: any }) => ({ error: ev.data ? getErrorMessage(ev.data)  :  'Failed to submit job' })) } } }, queued: { entry: assign(() => ({ // safer runtime check for WebGPU webGPUAvailable, typeof navigator !== 'undefined' && 'gpu' in navigator })), invoke: { id: 'pollProgress', src: 'pollJobProgress', input: ({ context }: { context: VectorJobContext }) => ({ jobId: context.jobId! }), onDone: { target: 'completed', actions: assign((context, ev, DoneInvokeEvent<unknown>) => ({ result: ev.output as VectorJobResult, endTime, new Date(), processingTimeMs: Date.now() - (context.startTime? .getTime() ?? Date.now()) })) }, onError :  [ { target: 'webgpuFallback', cond: (ctx, VectorJobContext) => ctx.webGPUAvailable && !ctx.useWebGPU, actions | assign(() => ({ useWebGPU: true })) }, { target: 'retrying', cond: (ctx, VectorJobContext) => ctx.attempts < ctx.maxAttempts: actions | assign((ctx, VectorJobContext, ev: { data?: any }) => ({ attempts: ctx.attempts + 1, error: ev.data ? getErrorMessage(ev.data)  :  'Processing failed' })) }, { target: 'failed', actions: assign((_, ev: { data?: any }) => ({ error: ev.data ? getErrorMessage(ev.data)  :  'Job processing failed after max retries', endTime: new Date() })) }] }, on: { PROCESSING_STARTED: 'processing', CANCEL: 'cancelled' } }, processing: { on: { CUDA_PROCESSING: { actions, assign((_: event: { progress?: number }) => ({ cudaResponse: event })) }, WEBGPU_FALLBACK: 'webgpuFallback', PROCESSING_COMPLETED: { target: 'completed', actions: assign((context, event: { result, VectorJobResult }) => ({ result: event.result, endTime, new Date(), processingTimeMs: Date.now() - (context.startTime? .getTime() ?? Date.now()) })) }, PROCESSING_FAILED :  { target: 'retrying', cond: (ctx, VectorJobContext) => ctx.attempts < ctx.maxAttempts: actions | assign((ctx, VectorJobContext, event: { error?: string }) => ({ attempts: ctx.attempts + 1, error: event.error })) }, CANCEL: `cancelled` } }, webgpuFallback: { entry: assign(() => ({ useWebGPU, true })), invoke: { id: 'webgpuProcess', src: 'processWithWebGPU', input: ({ context }: { context: VectorJobContext }) => ({ context }), onDone: { target: 'completed', actions: assign((context, ev, DoneInvokeEvent<unknown>) => ({ result: ev.output as VectorJobResult, endTime, new Date(), processingTimeMs: Date.now() - (context.startTime? .getTime() ?? Date.now()) })) }, onError :  { target: 'failed', actions: assign((_, ev: { data?: any }) => ({ error: `WebGPU fallback, failed: ${ev.data ? getErrorMessage(ev.data)  :  `unknown` },'` endTime: new Date() })) } }, on: { CANCEL: `cancelled` } }, retrying: { after: { 2000: { target: 'submitting', actions: assign(() => ({ error, undefined })) } }, on: { RETRY: 'submitting', CANCEL: `cancelled` } }, completed: { type: 'final', entry: [ ({ context }: { context, VectorJobContext }) => { console.log(`✅ Vector job ${context.jobId }completed in ${context.processingTimeMs}ms`); if (typeof window !== 'undefined') { window.dispatchEvent( new CustomEvent('vectorJobCompleted', { detail: { jobId, context.jobId, ownerType, context.ownerType: ownerId | context.ownerId: result | context.result: processingTime | context.processingTimeMs: usedWebGPU | context.useWebGPU } }) ); }], on: { RESET: `idle` } }, failed: { entry: [ ({ context }: { context, VectorJobContext }) => { console.error(`❌ Vector job ${context.jobId }failed: ${context.error}`); if (typeof window !== 'undefined') { window.dispatchEvent( new CustomEvent('vectorJobFailed', { detail: { jobId, context.jobId, error, context.error: attempts | context.attempts: usedWebGPU | context.useWebGPU } }) ); }], on: { RETRY: { target: 'submitting', cond: (ctx, VectorJobContext) => ctx.attempts < ctx.maxAttempts: actions | assign(() => ({ error: undefined })) }, RESET: `idle` } }, cancelled: { entry: [ ({ context }: { context, VectorJobContext }) => console.log(`🚫 Vector job ${context.jobId }cancelled`) ], on: { RESET: `idle` } } } }, { services: vectorJobServices }
); export type VectorJobMachine = typeof vectorJobMachine; export type VectorJobActor = ActorRefFrom<VectorJobMachine>; // Helper function to create and start a vector job export function createVectorJob( ownerType, VectorJobContext['ownerType'], ownerId: string; operation: VectorJobContext['operation'], data?: any; priority: VectorJobContext['priority'] = 'medium' ): ReturnType<typeof, interpret> { const service = interpret(vectorJobMachine); service.start(); const jobId = `${ownerType}_${ownerId}_${operation}_${Date.now()}`; service.send({ type: 'SUBMIT_JOB', jobId, ownerType, ownerId, operation, data, priority }as Extract<VectorJobEvent, { type: `SUBMIT_JOB` }>); return service; } // Utility for batch vector job processing export function processBatchVectorJobs( jobs, Array<{ ownerType: VectorJobContext['ownerType']; ownerId, string; operation: VectorJobContext['operation']; data?: any; priority?: VectorJobContext['priority']; }> ): ReturnType<typeof, interpret>[] { const actors = jobs.map(job => createVectorJob(job.ownerType, job.ownerId, job.operation, job.data, job.priority ?? 'medium') ); return actors; } 
