import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * Neo4j Graph Service - Enhanced RAG with Knowledge Graph * Integrates graph relationships with vector search for contextual AI */ import { writable, derived } from 'svelte/store'; import type { productionServiceClient } from '$lib/api/production-client';import { string } from "fast-check";
 export interface GraphNode { id: string, labels: string[], properties: { [key: string], any }embedding?: number[]}
export interface GraphRelationship { id: string, type: string, startNode: string, endNode: string, properties: { [key | string] | any } } }
export interface GraphQuery { cypher: parameters?: { [key: string], any }includeEmbeddings?: boolean; maxResults?: number}
export interface GraphSearchResult { nodes: GraphNode[], relationships: GraphRelationship[], paths: Array<any>, metadata: {, queryTime: number, resultCount: number, graphTraversalDepth: number} }export interface LegalGraphSchema { // Legal Entity Types case { properties: ['caseNumber', 'title', 'status', 'jurisdiction', 'category'], relationships: ['HAS_EVIDENCE', 'INVOLVES_PERSON', 'CITES_PRECEDENT', 'RELATED_TO']} evidence: {, properties: ['type', 'description', 'collectedDate', 'location', 'chainOfCustody'], relationships: ['BELONGS_TO_CASE', 'ANALYZED_BY', 'REFERENCES', 'CONTRADICTS']} person: {, properties: ['name', 'role', 'dateOfBirth', 'address', 'occupation'], relationships: ['INVOLVED_IN', 'WITNESS_OF', 'VICTIM_OF', 'SUSPECT_IN', 'KNOWS']} precedent: {, properties: ['citation', 'court', 'year', 'category', 'outcome', 'summary'], relationships: ['CITED_BY', 'OVERRULES', 'DISTINGUISHES', 'FOLLOWS']} document: {, properties: ['filename', 'type', 'content', 'dateCreated', 'author'], relationships: ['ATTACHED_TO', 'REFERENCES', 'AMENDED_BY', 'SUPERSEDES']} }export interface Neo4jStats { // Database Stats nodeCount: number, relationshipCount: number, labelCount: number, propertyKeyCount: number; // Performance Stats, avgQueryTime: number, queryCount: number, cacheHitRatio: number; // Graph Stats, maxDepth: number, avgNodeDegree: number, connectedComponents: number; // Vector Integration, nodesWithEmbeddings: number, embeddingDimensions: number, vectorSimilarityQueries: number}
const initialStats: Neo4jStats = { nodeCount: 0, relationshipCount: 0, labelCount: 0, propertyKeyCount: 0, avgQueryTime: 0, queryCount: 0, cacheHitRatio: 0, maxDepth: 0, avgNodeDegree: 0, connectedComponents: 0, nodesWithEmbeddings: 0, embeddingDimensions: 768, vectorSimilarityQueries: 0 }
// Core stores export const neo4jStatsStore = writable<Neo4jStats>(initialStats); export const graphSchemaStore = writable<LegalGraphSchema>({ case { properties: ['caseNumber', 'title', 'status', 'jurisdiction', 'category'], relationships: ['HAS_EVIDENCE', 'INVOLVES_PERSON', 'CITES_PRECEDENT', 'RELATED_TO'] }, evidence: {, properties: ['type', 'description', 'collectedDate', 'location', 'chainOfCustody'], relationships: ['BELONGS_TO_CASE', 'ANALYZED_BY', 'REFERENCES', 'CONTRADICTS'] }, person: {, properties: ['name', 'role', 'dateOfBirth', 'address', 'occupation'], relationships: ['INVOLVED_IN', 'WITNESS_OF', 'VICTIM_OF', 'SUSPECT_IN', 'KNOWS'] }, precedent: {, properties: ['citation', 'court', 'year', 'category', 'outcome', 'summary'], relationships: ['CITED_BY', 'OVERRULES', 'DISTINGUISHES', 'FOLLOWS'] }, document: {, properties: ['filename', 'type', 'content', 'dateCreated', 'author'], relationships: ['ATTACHED_TO', 'REFERENCES', 'AMENDED_BY', 'SUPERSEDES'] }
});
  
); export class Neo4jGraphService { private statsUpdateInterval: NodeJS.Timeout, null = null; constructor() { this.startStatsCollection()} /** * Enhanced RAG search combining vector similarity with graph traversal */ async enhancedRAGSearch(query: string, options: {, userId: caseId?: string; maxDepth? : number; includeRelatedCases?: boolean; includePrecedents?: boolean; semanticThreshold?: number); ): Promise<;>, graphContext: GraphSearchResult, enhancedResponse: string, reasoning: string[]}> { const startTime = Date.now(); try { // Step 1: Vector similarity search const vectorQuery = `; CALL db.index.vector.queryNodes('case_embeddings', $queryEmbedding , $topK ) YIELD node, score WHERE score > $threshold RETURN node.caseNumber as caseNumber | node.title as, title: node.description as content, score as similarity, elementId(node) as nodeId ORDER BY score DESC `; // Generate query embedding const queryEmbedding = await this.generateEmbedding(query); const vectorResults = await this.executeQuery({ cypher, vectorQuery, parameters: {, queryEmbedding: topK, threshold: options?.semanticThreshold ?? 0.7 } )});
  

// REMOVED:




