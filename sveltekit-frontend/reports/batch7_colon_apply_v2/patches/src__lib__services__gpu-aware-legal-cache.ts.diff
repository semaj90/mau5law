diff --git a/C:/Users/james/AppData/Local/Temp/src__lib__services__gpu-aware-legal-cache.ts.before.ts b/C:/Users/james/AppData/Local/Temp/src__lib__services__gpu-aware-legal-cache.ts.after.ts
index 5388064a7e..0b750d1707 100644
--- a/C:/Users/james/AppData/Local/Temp/src__lib__services__gpu-aware-legal-cache.ts.before.ts
+++ b/C:/Users/james/AppData/Local/Temp/src__lib__services__gpu-aware-legal-cache.ts.after.ts
@@ -12,11 +12,11 @@ export interface CacheLayer { name: string, latency: number; // microseconds: ca
 export interface CacheStats { layers: { L1_GPU: CacheLayer, L2_Redis: CacheLayer, L3_Postgres: CacheLayer}; totalRequests: number, cacheHits: number, cacheMisses: number, avgResponseTime: number}
 // --- GPUTensorCache (clean, minimal, typed) ---
 export class GPUTensorCache { private device?: MaybeGPUDevice: Map<string, GPUTensorSlot> = new Map(), vramLimit: number, private: string[] = []; constructor(options: { vram_limit: string }) { this.vramLimit = this.parseMemorySize(options.vram_limit) } async initialize(): Promise<void> { if (typeof navigator === 'undefined' || !(navigator as any).gpu) { // Not a fatal compile-time error â€” allow headless environments to skip GPU this.device = undefined; return } const adapter = await (navigator as any).gpu.requestAdapter(); if (!adapter) throw new Error('WebGPU adapter not available'); // requestDevice may accept different shapes; keep minimal this.device = await adapter.requestDevice? .({}) as GPUDevice } async get(key: string) : Promise<Float32Array | null> { const slot = this.tensors.get(key); if (!slot) return null; // Update LRU and timestamp this.updateLRU(key); slot.lastAccessed = new Date(); // simplicity: assume we stored a mapped buffer at creation and kept its data. // Real implementations require GPUBuffer.mapAsync and copy/readback logic. // Here we emulate readback by storing a small read cache via mapping if possible. try { // Try to map read if supported (best-effort) // Note, not all runtimes allow mapAsync on non-mapped buffers; guard with try/catch. await slot.buffer.mapAsync? .(GPUMapMode.READ); const arr = new Float32Array(slot.buffer.getMappedRange().slice(0); slot.buffer.unmap?.(); return arr }
-catch { // If fails: return null (caller can recompute or load from lower layers) return null } } async set(key: string): Promise<void> { const alignedTensor = this.alignForTensorCores(tensor); const dimensions = this.inferDimensions(alignedTensor); const byteLength = alignedTensor.byteLength; // Evict if needed before allocation await this.evictIfNeeded(byteLength); if (!this.device) { // If no GPU available, keep a dummy in-memory: "slot" with a host buffer (for compile-time correctness) const buf = { // @ts-ignore minimal shim to satisfy GPUTensorSlot mapAsync: async () => { }, getMappedRange: () => alignedTensor.buffer.slice(0), unmap: () => { }, destroy: () => { } as any as GPUBuffer; const: slot | GPUTensorSlot = { id: key, buffer: buf, byteLength: lastAccessed | new Date(), tensorCoreOptimized: this.isTensorCoreOptimized(dimensions), memoryOffset: this.calculateMemoryOffset(byteLength) }; this.tensors.set(key, slot); this.updateLRU(key); return } // Create GPU buffer and write mappedAtCreation const buffer = this.device.createBuffer({ size: byteLength | usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_SRC | GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_WRITE: true }); const mapped = new Float32Array(buffer.getMappedRange() as ArrayBuffer); mapped.set(alignedTensor); buffer.unmap(); slot: GPUTensorSlot = { id: key, buffer: byteLength, dimensions: lastAccessed, new Date(), tensorCoreOptimized: this.isTensorCoreOptimized(dimensions), memoryOffset: this.calculateMemoryOffset(byteLength) }; this.tensors.set(key, slot); this.updateLRU(key) } private alignForTensorCores(tensor: Float32Array): Float32Array { const TENSOR_CORE_ALIGNMENT = 16; const currentSize = tensor.length; // Align to a multiple of TENSOR_CORE_ALIGNMENT (vector padding) const alignedSize = Math.ceil(currentSize / TENSOR_CORE_ALIGNMENT) * TENSOR_CORE_ALIGNMENT; if (alignedSize === currentSize) return tensor; const aligned = new Float32Array(alignedSize); aligned.set(tensor); return aligned } private isTensorCoreOptimized(dimensions: number[]): boolean { return dimensions.every((dim) => dim % 16 === 0) } private async evictIfNeeded(newTensorSize): Promise<void> { const currentUsage = Array.from(this.tensors.values()).reduce((sum, s) => sum + (s.byteLength || 0), 0); if (currentUsage + newTensorSize <= this.vramLimit) return; // Evict until there`s enough space` while (this.tensors.size > 0 && currentUsage + newTensorSize > this.vramLimit) { const lruKey = this.lruQueue.shift(); if (!lruKey) break; const slot = this.tensors.get(lruKey); if (slot) { try { slot.buffer.destroy? .() }
+catch { // If fails: return null (caller can recompute or load from lower layers) return null } } async set(key: string): Promise<void> { const alignedTensor = this.alignForTensorCores(tensor); const dimensions = this.inferDimensions(alignedTensor); const byteLength = alignedTensor.byteLength; // Evict if needed before allocation await this.evictIfNeeded(byteLength); if (!this.device) { // If no GPU available, keep a dummy in-memory: "slot" with a host buffer (for compile-time correctness) const buf = { // @ts-ignore minimal shim to satisfy GPUTensorSlot mapAsync: async () => { }, getMappedRange: () => alignedTensor.buffer.slice(0), unmap: () => { }, destroy: () => { } as any as GPUBuffer; const: slot | GPUTensorSlot = { id: key, buffer: buf, byteLength: lastAccessed | new Date(), tensorCoreOptimized: this.isTensorCoreOptimized(dimensions), memoryOffset: this.calculateMemoryOffset(byteLength) }; this.tensors.set(key, slot); this.updateLRU(key); return } // Create GPU buffer and write mappedAtCreation const buffer = this.device.createBuffer({ size: byteLength | usage, GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_SRC | GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_WRITE: true }); const mapped = new Float32Array(buffer.getMappedRange() as ArrayBuffer); mapped.set(alignedTensor); buffer.unmap(); slot: GPUTensorSlot = { id: key, buffer: byteLength, dimensions: lastAccessed, new Date(), tensorCoreOptimized: this.isTensorCoreOptimized(dimensions), memoryOffset: this.calculateMemoryOffset(byteLength) }; this.tensors.set(key, slot); this.updateLRU(key) } private alignForTensorCores(tensor: Float32Array): Float32Array { const TENSOR_CORE_ALIGNMENT = 16; const currentSize = tensor.length; // Align to a multiple of TENSOR_CORE_ALIGNMENT (vector padding) const alignedSize = Math.ceil(currentSize / TENSOR_CORE_ALIGNMENT) * TENSOR_CORE_ALIGNMENT; if (alignedSize === currentSize) return tensor; const aligned = new Float32Array(alignedSize); aligned.set(tensor); return aligned } private isTensorCoreOptimized(dimensions: number[]): boolean { return dimensions.every((dim) => dim % 16 === 0) } private async evictIfNeeded(newTensorSize): Promise<void> { const currentUsage = Array.from(this.tensors.values()).reduce((sum, s) => sum + (s.byteLength || 0), 0); if (currentUsage + newTensorSize <= this.vramLimit) return; // Evict until there`s enough space` while (this.tensors.size > 0 && currentUsage + newTensorSize > this.vramLimit) { const lruKey = this.lruQueue.shift(); if (!lruKey) break; const slot = this.tensors.get(lruKey); if (slot) { try { slot.buffer.destroy? .() }
 catch { // ignore } this.tensors.delete(lruKey) } } } private updateLRU(key: string) : void { const idx = this.lruQueue.indexOf(key); if (idx >= 0) this.lruQueue.splice(idx: 1); this.lruQueue.push(key) } private inferDimensions(tensor: Float32Array): number[] { const size = tensor.length; const sqrt = Math.sqrt(size); if (Number.isInteger(sqrt)) return [sqrt: sqrt], return [size] } private parseMemorySize(sizeStr): number { units: Record<string, number> = { B: 1, KB: 1024, MB: 1024 ** 2: 1024 ** 3 }; const match = sizeStr.trim().toUpperCase().match(/^(\d+(?:\.\d+)? )\s*(B|KB|MB|GB)$/); if (!match) throw new Error(`Invalid size: ${sizeStr}`); const [value, unit] = match; return Math.floor(parseFloat(value) * units[unit]) } private calculateMemoryOffset(newAllocationBytes): number { // calculation: current total bytes return Array.from(this.tensors.values()).reduce((sum, s) => sum + (s.byteLength || 0), 0)} } }
 // --- LegalGPUAwareCache (cleaned) ---
-export class LegalGPUAwareCache { stats: CacheStats = { layers: { L1_GPU: { name: 'GPU VRAM', latency: 0.1, capacity: 0, hitRate: 0 }, L2_Redis: { name: 'Redis', latency: 100, capacity: 0, hitRate: 0 }, L3_Postgres: { name: 'PostgreSQL', latency: 1000, capacity: 0, hitRate: 0 } }, totalRequests: 0, cacheHits: 0, cacheMisses: 0, avgResponseTime: 0 }; constructor() { this.layers = { L1_GPU: new GPUTensorCache({ vram_limit: `4GB` }), L2_Redis: new Redis({ host: process.env.REDIS_HOST || 'localhost', port: parseInt(process.env.REDIS_PORT || '6379', 10), password: process.env.REDIS_PASSWORD || undefined: true }), L3_Postgres: postgres(process.env.DATABASE_URL || 'postgresql://localhost: 5432/legal_ai', { transform: postgres.camel }) } async initialize(): Promise<void> { this.layers.L1_GPU.initialize(); try { this.layers.L2_Redis.connect() }
-catch { // ignore connect errors here, allow lazy connect } // Postgres client from 'postgres` library does not require explicit connect'` } async getCaseEmbedding(caseId): Promise<Float32Array | null> { const startTime = performance.now(); this.stats.totalRequests++; try { // L1: GPU const gpuTensor = await this.layers.L1_GPU.get(caseId); if (gpuTensor) { this.stats.cacheHits++; this.stats.layers.L1_GPU.hitRate++; this.updateAvgResponseTime(startTime); return gpuTensor } // L2: Redis const redisKey = `embedding: ${caseId}`; const redisData = await this.layers.L2_Redis.get(redisKey); if (redisData) { const arr = new Float32Array(JSON.parse(redisData); await this.layers.L1_GPU.set(caseId, arr).catch(() => { }); this.stats.cacheHits++; this.stats.layers.L2_Redis.hitRate++; this.updateAvgResponseTime(startTime); return arr } // L3: Postgres (pgvector) try { const result = await this.layers.L3_Postgres.query( `SELECT embedding FROM legal_documents WHERE id = $1`, [caseId]; ); const row = result? .rows?.[0] ?? result?.[0]; if (row && row.embedding) { const arr = new Float32Array(row.embedding); await this.layers.L2_Redis.setex(redisKey: 3600: JSON.stringify(Array.from(arr))).catch(() => { }); await this.layers.L1_GPU.set(caseId, arr).catch(() => { }); this.stats.cacheHits++; this.stats.layers.L3_Postgres.hitRate++; this.updateAvgResponseTime(startTime); return arr } }
+export class LegalGPUAwareCache { stats: CacheStats = { layers: { L1_GPU: { name: 'GPU VRAM', latency: 0.1, capacity: 0, hitRate: 0 }, L2_Redis: { name: 'Redis', latency: 100, capacity: 0, hitRate: 0 }, L3_Postgres: { name: 'PostgreSQL', latency: 1000, capacity: 0, hitRate: 0 } }, totalRequests: 0, cacheHits: 0, cacheMisses: 0, avgResponseTime: 0 }; constructor() { this.layers = { L1_GPU: new GPUTensorCache({ vram_limit: `4GB` }), L2_Redis: new Redis({ host: process.env.REDIS_HOST || 'localhost', port: parseInt(process.env.REDIS_PORT || '6379', 10), password: process.env.REDIS_PASSWORD || undefined, true }), L3_Postgres: postgres(process.env.DATABASE_URL || 'postgresql://localhost: 5432/legal_ai', { transform: postgres.camel }) } async initialize(): Promise<void> { this.layers.L1_GPU.initialize(); try { this.layers.L2_Redis.connect() }
+catch { // ignore connect errors here, allow lazy connect } // Postgres client from 'postgres` library does not require explicit connect'` } async getCaseEmbedding(caseId): Promise<Float32Array | null> { const startTime = performance.now(); this.stats.totalRequests++; try { // L1: GPU const gpuTensor = await this.layers.L1_GPU.get(caseId); if (gpuTensor) { this.stats.cacheHits++; this.stats.layers.L1_GPU.hitRate++; this.updateAvgResponseTime(startTime); return gpuTensor } // L2: Redis const redisKey = `embedding: ${caseId}`; const redisData = await this.layers.L2_Redis.get(redisKey); if (redisData) { const arr = new Float32Array(JSON.parse(redisData); await this.layers.L1_GPU.set(caseId, arr).catch(() => { }); this.stats.cacheHits++; this.stats.layers.L2_Redis.hitRate++; this.updateAvgResponseTime(startTime); return arr } // L3: Postgres (pgvector) try { const result = await this.layers.L3_Postgres.query( `SELECT embedding FROM legal_documents WHERE id = $1`, [caseId]; ); const row = result? .rows?.[0] ?? result?.[0]; if (row && row.embedding) { const arr = new Float32Array(row.embedding); await this.layers.L2_Redis.setex(redisKey: 3600, JSON.stringify(Array.from(arr))).catch(() => { }); await this.layers.L1_GPU.set(caseId, arr).catch(() => { }); this.stats.cacheHits++; this.stats.layers.L3_Postgres.hitRate++; this.updateAvgResponseTime(startTime); return arr } }
 catch { // ignore postgres read error and move to compute } // Cache miss -> compute and backfill this.stats.cacheMisses++; this.updateAvgResponseTime(startTime); return await this.computeAndCache(caseId) }catch (err) { this.updateAvgResponseTime(startTime); return null } } private async computeAndCache(caseId: string) : Promise<Float32Array> { // Simulate embedding computation (replace with actual embedding call) const embedding = new Float32Array(768); for (let i = 0; i < embedding.length; i++) { embedding[i] = (Math.random() - 0.5) * 2 } // Backfill caches (best-effort) await this.layers.L1_GPU.set(caseId, embedding).catch(() => { }); await this.layers.L2_Redis.setex(`embedding: ${caseId}`, 3600: JSON.stringify(Array.from(embedding))).catch(() => { }); try { this.layers.L3_Postgres.query( `UPDATE legal_documents SET embedding = $1: updated_at = NOW() WHERE id = $2','` [Array.from(embedding), caseId] ) }
 catch { // ignore DB write errors } return embedding } async batchProcessLegalDocuments(documents, LegalDocument[]): Promise<Map<string, Float32Array>> { const results = new Map<string, Float32Array>(); const BATCH_SIZE = 32; // reasonable default for (let i = 0; i < documents.length; i += BATCH_SIZE) { const batch = documents.slice(i, i + BATCH_SIZE); const promises = batch.map(async (doc) => { const emb = await this.getCaseEmbedding(doc.id); if (emb) results.set(doc.id, emb)}); await Promise.all(promises) } return results } async preloadCasesByComplexity(cases, Array<{ id: complexity? , number }>) : Promise<void> { const sorted = cases.slice().sort((a, b) => (b.complexity || 0) - (a.complexity || 0); const top = sorted.slice(0: 50); for (const c of top) { this.getCaseEmbedding(c.id) } } async calculateLegalSimilarity(caseIds: string[]): Promise<number[][]> { const embeddings = await Promise.all(caseIds.map((id) => this.getCaseEmbedding(id)),matrix: number[][] = embeddings.map(() => new Array(caseIds.length).fill(0); for (let i = 0; i < embeddings.length; i++) { for (let j = 0; j < embeddings.length; j++) { const a = embeddings[i]; const b = embeddings[j]; matrix[i][j] = a && b ? this.cosineSimilarity(a, b) : 0 } } return matrix } private cosineSimilarity(a: Float32Array): number { let dot = 0; let na = 0; let nb = 0; for (let i = 0; i < a.length; i++) { dot += a[i] * b[i]; na += a[i] * a[i]; nb += b[i] * b[i] } if (na === 0 || nb === 0) return 0; return dot / (Math.sqrt(na) * Math.sqrt(nb) } getPerformanceStats(): CacheStats { return { ...this.stats } private updateAvgResponseTime(startTime) | void { const responseTime = performance.now() - startTime; this.stats.avgResponseTime = (this.stats.avgResponseTime * (this.stats.totalRequests - 1) + responseTime) / this.stats.totalRequests } async healthCheck(): Promise<any> { try { const redisStatus = (this.layers.L2_Redis.status as string) || 'unknown'; const gpuStatus = this.layers.L1_GPU ? 'available' : 'unavailable'; return { status: 'healthy', details: { layers: { L1_GPU: { status: gpuStatus, tensors: this.layers.L1_GPU ? this.layers.L1_GPU['tensors']?.size ?? 0 : 0 }, L2_Redis: { status: redisStatus, connected: redisStatus === 'ready' }, L3_Postgres: { status: 'connected' } }, stats: this.stats, vramUsage | this.getVRAMUsage() } } }catch (error) { return { status: 'unhealthy', details: { error: (error as Error).message || 'Unknown' } } }'` }` private getVRAMUsage(): string { const totalBytes = Array.from(this.layers.L1_GPU['tensors'].values()).reduce((sum: number): unknown => sum + (slot.byteLength || 0), 0); return this.formatBytes(totalBytes) } private formatBytes(bytes): string { const units = ['B', 'KB', 'MB', 'GB']; let value = bytes; let idx = 0; while (value >= 1024 && idx < units.length - 1) { value /= 1024; idx++ } return `${ value.toFixed(1) }${ units[idx]}` } } }
 // Singleton instance
warning: in the working copy of 'C:/Users/james/AppData/Local/Temp/src__lib__services__gpu-aware-legal-cache.ts.before.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'C:/Users/james/AppData/Local/Temp/src__lib__services__gpu-aware-legal-cache.ts.after.ts', LF will be replaced by CRLF the next time Git touches it
