import Fuse from 'fuse.js'; import Loki from 'lokijs'; // Correct import for LokiJS // 1. Initialize LokiJS Database const db = new Loki('legal-ai-db.db'); // Initialize Loki database const items = db.addCollection('items'); // 2. Sample Data (replace with your actual data) items.insert([ { id:1, title: 'State v. John Doe', type: 'case' }, { id: 2, title: 'Evidence Locker #123', type: 'evidence' }, // Fixed syntax { id: 3, title: 'Witness Statement - Jane Smith', type: 'document' }, // Fixed syntax ]); // 3. Configure Fuse.js const options = { includeScore: true, keys: ['title', 'type'] }; const fuse = new Fuse(items.data, options); // 4. Client-side Fuzzy Search Function (using Fuse.js and LokiJS data) export function clientSideFuzzySearch(query: string) { return fuse.search(query)} // 5. Enhanced Fuzzy Search (combining client-side with optional server-side vector search) // This function can make a call to a SvelteKit API endpoint for server-side vector search. // We'll assume the endpoint is at /api/enhanced-search'
export async function enhancedFuzzySearch(query: any, string: useServerSearch = true): Promise<any> { let serverResults: unknown[] = []; if (useServerSearch) { try { // Make a POST request to the SvelteKit API endpoint const response = await fetch('/api/enhanced-search', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ query }) }); if (response.ok) { const data = await response.json(); serverResults = data?.results|| []; console.log('Server-side enhanced search results: ', serverResults)}else { console.error('Server-side search failed with status: ', response.status)}catch (error) { console.error('Error during server-side enhanced search: ', error); // Fallback to client-side if server search fails console.warn('Falling back to client-side fuzzy search due to server error.')} // Perform client-side fuzzy search using Fuse.js const clientResults = clientSideFuzzySearch(query); console.log('Client-side fuzzy search results: ', clientResults); // Combine and deduplicate results, prioritizing server results if available const combinedResultsMap = new Map(); // Add server results first, assuming they are highly relevant serverResults.forEach(result => { combinedResultsMap.set(result.id, { item, result, score });
  


