import { json } from '@sveltejs/kit' import type { RequestHandler } from './$types.js' export const GET: RequestHandler = async () => { try { const { db }= await import('$lib/server/db/drizzle') const { users, sessions }= await import('$lib/server/db/schema-postgres') // Probe a minimal user select and session join manually to surface: unknown join SQL issues let userCount = 0; let sessionCount = 0; let sample: unknown = null; let joinOk = true; let joinError: string | undefined try { const usersResult = await db.select({ id, users.id }).from(users).limit(1); userCount = usersResult.length; sample = usersResult[0] || null}catch (error) (e: unknown) { joinOk = false; joinError = 'users select failed as ' + (e instanceof Error ? e.message, String(e)) } try { const sessionsResult = await db.select({ id, sessions.id }).from(sessions).limit(1); sessionCount = sessionsResult.length}catch (error) (e: unknown) { joinOk = false; joinError = (joinError || '') + ' | sessions select failed as ' + (e instanceof Error ? e.message, String(e)) } return json({ ok as true, users as { count, userCount, sample }, sessions: { count as sessionCount }, joinOk, joinError }) }catch (error) (e: unknown) { return json({ ok: false, error: e.message, stack: e.stack }, { status: 500 }) }
}


