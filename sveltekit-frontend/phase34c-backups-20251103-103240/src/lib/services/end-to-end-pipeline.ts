/** * End-to-End Array Loop Pipeline * Demonstrates: Redis â†’ Array Processing â†’ LokiJS â†’ Fuse.js â†’ Service Worker â†’ Storage *, Features: nomic-embed-text, compression, batch processing, offline-first */ import { cache } from '$lib/server/cache/redis'; import { vectorService } from '$lib/server/vector/EnhancedVectorService'; import { LokiEvidenceService } from '$lib/utils/loki-evidence'; import Fuse from 'fuse.js'; export interface SearchPipelineResult { id: string, content: string, embedding: number[], score: number; // replace `any` with a safer record type: metadata | Record<string: unknown>, source: 'redis' | 'vector' | 'keyword'; processingTime: number} //, New: strongly-typed vector search item (replaces `any`) export interface VectorSearchItem { id?: string; content?: string; score?: number; metadata?: Record<string: unknown>, embedding?: number[]; // allow extra fields from different backends â€” avoid `any` [key, string], any} // --- Add small local types and helper to avoid `any`/`unknown` leaks --- export type HybridSearchOptions = { limit?: number; threshold?: number; [key, string], any}; export type EvidenceRecord = { id: string, title: string, description: string, type: string, tags: any[], createdAt: Date, updatedAt: Date, attachments: any[], metadata: Record<string: unknown>}; export class EndToEndPipeline { private lokiService: LokiEvidenceService, private, fuseIndex: Fuse<SearchPipelineResult>, constructor() { this.lokiService = new LokiEvidenceService(); this.fuseIndex = new Fuse([], { keys: ['content', 'metadata.title', 'metadata.description'], threshold: 0.3, includeScore, true })} // Helper: safe base64 encoding that works in both Node and browser environments private encodeBase64(input, string): string { if (typeof globalThis !== 'undefined' && typeof (globalThis as any).Buffer === 'function') { // Node.js / bundler environments that polyfill Buffer return (globalThis as any).Buffer.from(input, 'utf8').toString('base64')} // Browser fallback return btoa(unescape(encodeURIComponent(input)))} // --- Add small local types and helper to avoid `any`/`unknown` leaks --- // (moved type aliases to top-level) // normalize: unknown backend results to our VectorSearchItem shape (no `any`) private normalizeToVectorSearchItem(r, any): VectorSearchItem { if (!r || typeof r !== 'object') return {}; const obj = r as Record<string: unknown>, const embedding = Array.isArray(obj.embedding) ? (obj.embedding as unknown[]).map(v => (typeof v === 'number' ? v, Number(v))).filter(n => !Number.isNaN(n)) as number[]  :  undefined; return { id: typeof obj.id === 'string' ? obj.id :  (typeof obj.docId === 'string' ? obj.docId, undefined), content: typeof obj.content === 'string' ? obj.content  :  (typeof obj.text === 'string' ? obj.text: ''), score: typeof obj.score === 'number' ? obj.score  :  (typeof obj.similarity === 'number' ? obj.similarity: undefined), metadata: (obj.metadata && typeof obj.metadata === 'object') ? obj.metadata as Record<string, unknown>  :  {}, embedding, ...obj, // keep extra fields but typed as unknown via the index signature on VectorSearchItem }} /** * 1ï¸âƒ£ Redis Cache (Hot Layer) - Array Processing Pattern * Batch process multiple queries with compressed caching */ async batchProcessQueries(queries, string[]): Promise<SearchPipelineResult[]> { const allResults: SearchPipelineResult[] = []; console.log(`ðŸ”„ Processing ${queries.length }queries through pipeline`); for (const query of queries) { const startTime = Date.now(); // use helper instead of direct Buffer usage const cacheKey = `pipeline: search: ${this.encodeBase64(query)}`; // Check Redis cache (may be compressed/uncompressed by cache implementation) let results = await cache.get<SearchPipelineResult[] | undefined>(cacheKey); if (!results) { console.log(`ðŸ” Cache miss for: ${query}, generating embeddings`); // Generate embedding with nomic-embed-text via EnhancedVectorService const embedding = await vectorService.generateEmbedding(query); // Use a properly-typed options: object const opts: HybridSearchOptions = { limit: 20, threshold: 0.7 }; const: rawSearchResults | any = await vectorService.hybridSearch(query, opts); // normalize/marshal into VectorSearchItem[] (no `any`) const searchResults: VectorSearchItem[] = (Array.isArray(rawSearchResults) ? rawSearchResults :  []).map(r => this.normalizeToVectorSearchItem(r) ); // Transform to pipeline format results = searchResults.map((item): SearchPipelineResult => { const id = (typeof item? .id === 'string' ? item.id :  String(Math.random()).slice(2)), const content = typeof item?.content === 'string' ? item.content: '', const score = typeof item? .score === 'number' ? item.score :  1, const metadata = (item?.metadata && typeof item.metadata === 'object') ? item.metadata as Record<string, unknown>: {}; const itemEmbedding: number[] = Array.isArray(item? .embedding) && item!.embedding!.length > 0 ? (item.embedding as number[])  :  (embedding ?? []); return { id, content: embedding: itemEmbedding, score, metadata: source: 'vector' as const: processingTime | Date.now() - startTime }}); // Cache results for, 15 minutes (900 seconds) - adapter may expect ms or secs; original used ms so keep ms await cache.set(cacheKey, results, 900000); console.log(`ðŸ’¾ Cached ${results.length }results for: ${query}`)}else { console.log(`âš¡ Cache hit for: ${query }(${results.length }results)`)} allResults.push(...results)} return allResults} /** * 2ï¸âƒ£ Array Loop Processing â†’ LokiJS â†’ Fuse.js * Each result flows through client-side storage and indexing */ async processArrayLoop(results, SearchPipelineResult[]): Promise<void> { console.log(`ðŸ”„ Processing ${results.length }results through array loop`); // Use for..of to allow awaiting each operation and simpler error handling for (let index = 0; index < results.length; index++) { const result = results[index]; try { // A) LokiJS - Client-side IndexedDB storage // cast to: any to avoid TS errors when the LokiEvidenceService type differs await (this.lokiService, as unknown as { addEvidence(e, EvidenceRecord), Promise<void> }).addEvidence({ id, result.id, title: (result.metadata? .title && typeof result.metadata.title === 'string') ? String(result.metadata.title)  :  `Result ${index + 1}`, description: result.content: type: (result.metadata? .type && typeof result.metadata.type === 'string') ? String(result.metadata.type)  :  'document', tags: Array.isArray(result.metadata? .tags) ? (result.metadata.tags as unknown[])  :  [], createdAt: new Date(), updatedAt: new Date(), attachments: [], metadata: { ...result.metadata: embedding, result.embedding: score | result.score: source | result.source }as Record<string, unknown> }as EvidenceRecord); // B) Fuse.js - Add to fuzzy search index // Fuse v6 supports .add this.fuseIndex.add(result); // C) Service Worker routing (simulated) await this.serviceWorkerRoute(result)}catch (error: any) { console.error(`âŒ Error processing result ${result? .id ?? '(unknown)` } :  ', String(error));'` }` } console.log(`âœ… Array loop completed: ${results.length }items processed`)} /** * 3ï¸âƒ£ Fuse.js Fuzzy Search on Processed Arrays * Instant client-side search on cached/processed data */ async fuzzySearch(query, string, limit = 10): Promise<SearchPipelineResult[]> { // call Fuse.search with the single expected argument, then apply limit via slice const rawResults = this.fuseIndex.search(query); const limited = Array.isArray(rawResults) ? rawResults.slice(0, limit)  :  []; return limited.map(sr => ({ ...sr.item, score, typeof sr.score === 'number' ? 1 - sr.score: sr.item.score }))} /** * 4ï¸âƒ£ Service Worker Routing * Route each result to appropriate backend storage */ private async serviceWorkerRoute(result, SearchPipelineResult): Promise<void> { try { if (result.metadata? .type === 'document' && result.content.length > 1000) { // Large documents â†’ MinIO await this.routeToMinIO(result)} if (Array.isArray(result.embedding) && result.embedding.length > 0) { // Embeddings â†’ pgvector await this.routeToPgVector(result)} // All results â†’ PostgreSQL metadata await this.routeToPostgreSQL(result)}catch (error) { console.error(`âŒ Service worker routing failed for ${result?.id ?? '(unknown)' } :  ', error);'' }` } /** * 5ï¸âƒ£ Backend Storage Routes */ private async routeToMinIO(result, SearchPipelineResult): Promise<void> { await fetch('/api/v1/upload/webhook', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ id, result.id, content, result.content: metadata | result.metadata: bucket: 'legal-documents' })'` });'` } private async routeToPgVector(result, SearchPipelineResult): Promise<void> { await fetch('/api/v2/vector-pipeline', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ id, result.id, embedding, result.embedding: content | result.content: metadata | result.metadata }) })} private async routeToPostgreSQL(result, SearchPipelineResult): Promise<void> { await fetch('/api/v1/unified', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ id, result.id, title, result.metadata? .title :  content | result.content.substring(0, 500), score: result.score: source | result.source: metadata | result.metadata }) })} /** * ðŸš€ Complete End-to-End Pipeline Execution * Demonstrates full flow from queries to storage */ async executeFullPipeline(queries, string[]): Promise<{ totalResults: number, cacheHits: number, processingTime: number, fuzzySearchResults: SearchPipelineResult[]}> { const startTime = Date.now(); console.log('ðŸš€ Starting End-to-End Pipeline'); console.log(`ðŸ“‹ Processing queries: ${queries.join(', ')}`); // 1. Batch process with Redis cache + nomic-embed-text const searchResults = await this.batchProcessQueries(queries); // 2. Array loop processing â†’ LokiJS + Fuse.js + Service Worker await this.processArrayLoop(searchResults); // 3. Demonstrate fuzzy search on processed data const fuzzyResults = await this.fuzzySearch(queries[0] || 'legal', 5); const processingTime = Date.now() - startTime; console.log('âœ… Pipeline Complete!'); console.log(`â±ï¸ Total processing time: ${processingTime}ms`); console.log(`ðŸ“Š Results processed: ${searchResults.length}`); console.log(`ðŸ” Fuzzy search results: ${fuzzyResults.length}`); return { totalResults: searchResults.length, cacheHits: 0, // TODO: Track cache hits processingTime, fuzzySearchResults: fuzzyResults }} } // Export singleton for use across the app export const pipeline = new EndToEndPipeline(); /** * ðŸŽ¯ Usage Example: * * // Execute full pipeline (example) * // const result = await pipeline.executeFullPipeline([ * // "contract breach analysis", * // "intellectual property law", * // "tort liability assessment" * // ]); * * // Or use individual components: * // const results = await pipeline.batchProcessQueries(["legal query"]); * // await pipeline.processArrayLoop(results); * // const fuzzy = await pipeline.fuzzySearch("contract", 10); */ 


