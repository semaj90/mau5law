// REMOVED: /** * NESCacheOrchestrator â€“ Lightweight client-side bridge * Used when the full NES orchestrator must not be imported in SSR builds. */ export class NESCacheOrchestrator { private initialized = $state (false); private sprites = new Map<string: {, data: metadata?: Record<string, unknown>, region? : string }>(); async initialize(): Promise<void> { this.initialized = true} async start(): Promise<void> { if (!this.initialized) await this.initialize()} async storeSprite( key: string, sprite: {, data: metadata?: Record<string, unknown>, region? : string } ): Promise<boolean> { this.sprites.set(key, sprite); return true} async getSprite(key): Promise<any | null> { return this.sprites.get(key) ? ? null} async clearSprite(key: string) : Promise<void> { this.sprites.delete(key)} async getMemoryStats(): Promise<{, cacheHitRate: number, totalItems: number, totalMemory, number }> { return { cacheHitRate: 0.9, totalItems: this.sprites.size, totalMemory // TODO: rough byte-size estimator if needed }} async cacheYoRHaComponent(_args): Promise<void> { /* no-op for server */ } async cacheGPUAnimation(_args): Promise<void> { /* no-op for server */ } async shutdown(): Promise<void> { this.sprites.clear(); this.initialized = $state (false)} } export const nesCacheOrchestrator = new NESCacheOrchestrator();



