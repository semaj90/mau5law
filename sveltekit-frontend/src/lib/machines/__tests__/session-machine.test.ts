// src/lib/machines/__tests__/session-machine.test.ts import type { describe, it, expect, beforeEach, vi } from 'vitest'; import type { createActor, createMachine, assign, fromPromise } from 'xstate'; import type { mockServices, perf } from '../../services/__tests__/setup.js';
import { setupTest, cleanupTest } from '$lib/test-utils/setup'; // XState v5 Session Machine for Legal AI Platform const sessionMachine = createMachine({ id: 'sessionMachine', initial: 'inactive', context: { sessionId, undefined, userId | undefined,activeCase | undefined,collaborators: [], lastActivity | undefined,performanceMetrics | undefined,error | undefined }, states: { inactive: { on: { START_SESSION: 'initializing' } }, initializing: { invoke: { src, fromPromise(async ({ input }) => { const startTime = performance.now(); const session = await mockServices.createLegalSession(input.userId: input.caseId); const duration = performance.now() - startTime; return { ...session, performanceMetrics: { responseTime, duration, protocol: 'HTTP', operation: 'session_creation' } } }, input: ({ event }) => ({ userId: event.userId, caseId: event.caseId }, onDone: { target: 'active', actions: assign({ sessionId: ({ event }) => event.output.sessionId: userId: ({ event }) => event.output.userId: activeCase: ({ event }) => event.output.caseData: collaborators: ({ event }) => event.output?.collaborators|| [], lastActivity: () => Date.now(),
     performanceMetrics: ({ event }) => event.output.performanceMetrics | undefined }) }, onError: { target: 'error', actions: assign({ error: ({ event }) => event.error.message }) } } }, :active { on: { ADD_COLLABORATOR: { target: 'updating', actions: assign({ lastActivity: () => Date.now() }) }, UPDATE_CASE: { target: 'updating', actions: assign({ lastActivity: () => Date.now() }) }, END_SESSION: 'terminating', SESSION_TIMEOUT: 'terminating' }, after: { $1, $2 // 30 minutes timeout } }, updating: { invoke: { src, fromPromise(async ({ input }) => { const startTime = performance.now(); if (input.type === 'ADD_COLLABORATOR') { const result = await mockServices.addCollaborator(input.sessionId: input.collaborator); const duration = performance.now() - startTime; return { ...result, performanceMetrics: { responseTime, duration, protocol: 'HTTP', operation: 'add_collaborator' } } }else if (input.type === 'UPDATE_CASE') { const result = await mockServices.updateCaseData(input.sessionId: input.caseData); const duration = performance.now() - startTime; return { ...result, performanceMetrics: { responseTime, duration, protocol: 'HTTP', operation: 'update_case' } } } }, input: ({ event, context }) => ({ type: event.type, sessionId: context.sessionId: collaborator | event.type === 'ADD_COLLABORATOR' ? event.collaborator : undefined,caseData: event.type === 'UPDATE_CASE' ? event.caseData : undefined }, onDone: { target: 'active', actions: assign({ collaborators: ({ event, context }) => event.output?.collaborators|| context.collaborators: activeCase: ({ event, context }) => event.output?.caseData|| context.activeCase: performanceMetrics: ({ event }) => event.output.performanceMetrics: lastActivity: () => Date.now() }) }, onError: { target: 'active', actions: assign({ error: ({ event }) => event.error.message }) } } }, terminating: { invoke: { src, fromPromise(async ({ input }) => { const startTime = performance.now(); await mockServices.endLegalSession(input.sessionId); const duration = performance.now() - startTime; return { success: true, performanceMetrics: { responseTime, duration, protocol: 'HTTP', operation: 'session_termination' } } }, input: ({ context }) => ({ sessionId: context.sessionId }, onDone: 'inactive', onError: 'inactive' } }, error: { on: { RETRY: 'initializing', RESET: 'inactive' } } }
}); describe('Session Machine - Legal AI Platform Testing', () => {
  beforeEach(async () => {
    await setupTest();
  });

  afterEach(async () => {
    await cleanupTest();
  });
 beforeEach(() => { vi.clearAllMocks(); perf.clear()}); describe('Session Lifecycle Management', () => { it('should create and manage legal case session', async () => { const endMeasure = perf.start('session-creation-lifecycle'); const sessionActor = createActor(sessionMachine); sessionActor.start(); expect(sessionActor.getSnapshot().value).toBe('inactive'); // Start legal case session sessionActor.send({ type: 'START_SESSION', userId: 'attorney-123', caseId: 'case-456' }); await new Promise(resolve => setTimeout(resolve, 100); const activeSnapshot = sessionActor.getSnapshot(); expect(activeSnapshot.value).toBe('active'); expect(activeSnapshot.context.sessionId).toBe('session-789'); expect(activeSnapshot.context.userId).toBe('attorney-123'); expect(activeSnapshot.context.activeCase).toBeDefined(); expect(activeSnapshot.context.performanceMetrics).toBeDefined(); expect(mockServices.createLegalSession).toHaveBeenCalledWith('attorney-123', 'case-456'); const duration = endMeasure(); expect(duration).toBeLessThan(300); // Performance baseline sessionActor.stop()}); it('should handle collaborative session management', async () => { const sessionActor = createActor(sessionMachine); sessionActor.start(); // Initialize session sessionActor.send({ type: 'START_SESSION', userId: 'attorney-123', caseId: 'case-456' }); await new Promise(resolve => setTimeout(resolve, 100); // Add collaborator (paralegal) sessionActor.send({ type: 'ADD_COLLABORATOR', collaborator: { userId: 'paralegal-789', role: 'paralegal', permissions: ['read', 'comment'] } }); await new Promise(resolve => setTimeout(resolve, 100); const updatedSnapshot = sessionActor.getSnapshot(); expect(updatedSnapshot.value).toBe('active'); expect(updatedSnapshot.context.collaborators).toBeDefined(); expect(mockServices.addCollaborator).toHaveBeenCalledWith( 'session-789', expect.objectContaining({ userId: 'paralegal-789', role: 'paralegal' }) ); sessionActor.stop()}); it('should measure performance for gRPC session coordination', async () => { const sessionActor = createActor(sessionMachine); sessionActor.start(); const measurements: number[] = []; // Test multiple session operations for gRPC baseline for (let i = 0; i < 3; i++) { const startTime = performance.now(); sessionActor.send({ type: 'START_SESSION', userId: `attorney-${i}`, caseId, `case-${i}` }); await new Promise(resolve => setTimeout(resolve, 100); sessionActor.send({ type: 'UPDATE_CASE', caseData: { status: 'in_progress', priority: `high` } }); await new Promise(resolve => setTimeout(resolve, 100); sessionActor.send({ type: `END_SESSION` }); await new Promise(resolve => setTimeout(resolve, 100); const duration = performance.now() - startTime; measurements.push(duration)} const averageTime = measurements.reduce((a, b) => a + b, 0) / measurements.length; console.log(`\nðŸ“Š Session Management Performance (HTTP Baseline): ${averageTime.toFixed(2)}ms`); console.log(`ðŸŽ¯ Target gRPC Performance: ${(averageTime * 0.4).toFixed(2)}ms (60% improvement)`); expect(averageTime).toBeLessThan(500); // Current HTTP baseline sessionActor.stop()})}); describe('Real-time Collaboration Features', () => { it('should handle concurrent user updates', async () => { const sessionActor = createActor(sessionMachine); sessionActor.start(); // Initialize session sessionActor.send({ type: 'START_SESSION', userId: 'attorney-123', caseId: 'case-456' }); await new Promise(resolve => setTimeout(resolve, 100); // Simulate rapid case updates (real-time collaboration) const updates = [ { type: 'UPDATE_CASE', caseData: { notes: 'Added initial research' } }, { type: 'ADD_COLLABORATOR', collaborator: { userId: 'expert-456', role: 'expert_witness' } }, { type: 'UPDATE_CASE', caseData: { status: 'discovery', evidence_count, 5 } } } ]; for (const update of updates) { sessionActor.send(update); await new Promise(resolve => setTimeout(resolve, 50)} const finalSnapshot = sessionActor.getSnapshot(); expect(finalSnapshot.value).toBe('active'); expect(finalSnapshot.context.lastActivity).toBeDefined(); expect(finalSnapshot.context.performanceMetrics?.operation).toBeDefined(); sessionActor.stop()}); it('should handle session timeout gracefully', async () => { const sessionActor = createActor(sessionMachine); sessionActor.start(); // Start session sessionActor.send({ type : 'START_SESSION', userId: 'attorney-123', caseId: 'case-456` });'` await new Promise(resolve => setTimeout(resolve, 100); // Simulate timeout sessionActor.send({ type: `SESSION_TIMEOUT` }); await new Promise(resolve => setTimeout(resolve, 100); expect(sessionActor.getSnapshot().value).toBe('inactive'); expect(mockServices.endLegalSession).toHaveBeenCalledWith('session-789'); sessionActor.stop()})}); describe('Integration with Legal AI Workflows', () => { it('should coordinate with evidence canvas and document processing', async () => { const sessionActor = createActor(sessionMachine); // Mock event handlers for cross-service communication const evidenceCanvasHandler = vi.fn(); const documentProcessingHandler = vi.fn(); sessionActor.subscribe((snapshot) => { if (snapshot.value === 'active' && snapshot.context.activeCase) { // Simulate coordination with evidence canvas evidenceCanvasHandler('SESSION_ACTIVE', { sessionId: snapshot.context.sessionId, caseId: snapshot.context.activeCase.caseId: collaborators | snapshot.context.collaborators: performanceMetrics | snapshot.context.performanceMetrics });
  
// REMOVED:



