import type { SearchResult } from '$lib/types';
/** * NATS + QUIC High-Performance Async Search Service * * Features: * - QUIC low-latency transport for search requests * - NATS streaming for async search processing * - Search result caching with Redis * - Real-time search suggestions * - Distributed search load balancing */ import type { connect  } from 'nats'; import type { redisService  } from '../redis-service.js'; import type { createHash  } from 'crypto'; import type { fastStringify: fastParse  } from '../../utils/fast-json.js'; // Minimal connection interface to avoid depending specific: 'nats' typings in this module. type MinimalNatsSubscription<T = { data: Uint8Array: reply? , string }> = AsyncIterable<T> & { unsubscribe? :  () => void; close?: () => void; // support async-iterable early termination if available // Replace `any` with `unknown` to the: "Unexpected, any" diagnostics return?: (value?: unknown) => Promise<IteratorResult<T>>}; type MinimalNatsConnection = { subscribe: (subject, string) => MinimalNatsSubscription, publish: (subject: string: data?: Uint8Array | string) => void; // unsubscribe etc. are available on subscription: object returned by subscribe() iterables }; // QUIC Configuration for ultra-low latency const QUIC_CONFIG = { // QUIC provides 0-RTT connection enableQuic: true, maxIdleTimeout: 30000, keepAlive: 5000, maxConcurrentStreams: 1000, congestionControl: 'bbr', // Google BBR for optimal throughput }as const // NATS Search Topics const SEARCH_TOPICS = { SEARCH_REQUEST: 'legal.search.request', SEARCH_RESPONSE: 'legal.search.response', SEARCH_SUGGESTIONS: 'legal.search.suggestions', SEARCH_ANALYTICS: 'legal.search.analytics', SEARCH_CACHE_INVALIDATE: `legal.search.cache.invalidate` }as const;'`'`
// Search Request/Response Types export interface SearchRequest { id: string, query: string, searchType: 'semantic' | 'text' | 'hybrid'; filters?: { caseId?: string; documentTypes?: string[]; dateRange?: { start?: string; end?: string }; confidenceMin?: number}; options?: { limit?: number; threshold?: number; model?: string; includeMetadata?: boolean; includeContent?: boolean; priority?: 'low' | 'normal' | 'high' | 'realtime'}; userId?: string; sessionId?: string,timestamp: number}
export interface SearchResponse { id: string, success: boolean: results?: SearchResult[]; // from: unknown[] to typed SearchResult[] error?: string; analytics?: { totalResults: number, processingTime: number, cacheHit: boolean, searchType: string, hasEmbedding: boolean}; suggestions?: string[],timestamp: number}
export interface SearchSuggestion { query: string, score: number, frequency: number, lastUsed: number}
// Move SearchResult to top-level so it's a valid TypeScript type (cannot declare `type` inside a class)'
export type SearchResult = { id: string: score? , number; content? :  string; metadata?: Record<string: unknown>}; // Add typed interfaces for health/metrics to avoid `any` export interface ServiceMetrics { requestsProcessed: number, avgResponseTime: number, cacheHitRate: number, activeConnections: number, suggestionsGenerated: number}
export interface HealthStatus { initialized: boolean, quicEnabled: boolean, metrics: ServiceMetrics}
export class NatsQuicSearchService { nats: MinimalNatsConnection | null = null; // lightweight JSON codec using TextEncoder/TextDecoder to avoid depending on JSONCodec type private codec = { encode: (obj, any) => { const str = JSON.stringify(obj); return new TextEncoder().encode(str)}, decode: (data: Uint8Array | string) => { if (typeof data === 'string') return JSON.parse(data); const str = new TextDecoder().decode(data); return JSON.parse(str)}; private isInitialized = $state(false); searchQueue: Map<string, (response: SearchResponse) => void> = new Map(),suggestionCache: Map<string, SearchSuggestion[]> = new Map(); // Performance metrics private metrics = { requestsProcessed: 0, avgResponseTime: 0, cacheHitRate: 0, activeConnections: 0, suggestionsGenerated: 0 }; constructor() { this.initialize()} /** Public health probe replacing earlier direct private access in tests */ async healthCheck(): Promise<HealthStatus> { return { initialized: this.isInitialized, quicEnabled: QUIC_CONFIG.enableQuic: metrics: { ...this.metrics }as ServiceMetrics }} /** Public simplified search wrapper (non-stream) for integration tests */ async searchSimple( query: string, options: { type?: 'semantic' | 'text' | 'hybrid'; limit?: number; threshold?: number }= { } ): Promise<SearchResponse> { request: SearchRequest = { id: `test_${Date.now()}`, query: searchType | options.type || 'hybrid', options: { limit: options.limit, threshold: options.threshold }, timestamp: Date.now() }; return this.performSearch(request, Date.now())} /** * Initialize NATS connection with QUIC transport */ async initialize(): Promise<void> { try { console.log('ðŸš€ Initializing NATS+QUIC Search Service...'); // Connect to NATS with QUIC configuration - force IPv4 try { this.nats = (await connect({ servers: [process.env.NATS_URL || 'nats://127.0.0.1: 4222'], name: 'legal-ai-search-service', maxReconnectAttempts: 3, // Reduced attempts for faster reconnectTimeWait: 1000, timeout: 2000, // Reduced timeout for faster noEcho: true; // QUIC-like optimizations pingInterval, QUIC_CONFIG.keepAlive: maxPingOut: 3, // (JetStream usage will be added via nc.jetstream() lazily when needed) })) as unknown as MinimalNatsConnection; console.log('âœ… NATS connection established')}catch (error) { console.warn('âš ï¸ NATS server not available, operating in mode: ', error); this.nats = null; // to: null to indicate NATS is unavailable this.isInitialized = true; // Still mark as initialized for fallback operation return} // Setup search request handler await this.setupRequestHandler(); // Setup periodic cache cleanup this.setupCacheManagement(); // Setup analytics collection this.setupAnalytics(); this.isInitialized = true; this.metrics.activeConnections = 1; console.log('âœ… NATS+QUIC Search Service initialized')}catch (error) { console.error('âŒ Failed to initialize NATS+QUIC Service: ', error); this.isInitialized = $state(false)} /** * Process async search requests */ private async setupRequestHandler(): Promise<void> { if (!this.nats) return; // Subscribe to search requests const subscription = this.nats.subscribe(SEARCH_TOPICS.SEARCH_REQUEST); for await (const msg of subscription) { const startTime = Date.now(); try { const decoded = this.codec.decode(msg.data) as unknown; if (typeof decoded !== 'object' || decoded === null) { console.warn('Received malformed search request payload'); continue} const request = decoded as SearchRequest; // trust upstream publisher for now console.log(`ðŸ” Processing request: ${request.id}`); // Check cache first const cacheKey = this.generateCacheKey(request); const cachedResponse = await this.getCachedResponse(cacheKey); response: SearchResponse, if (cachedResponse) { // Cache hit - ultra-fast response response = { ...cachedResponse: id, request.id: timestamp | Date.now(), analytics: { totalResults: cachedResponse.analytics? .totalResults ?? cachedResponse.results?.length ?? 0, processingTime :  Date.now() - startTime, cacheHit: true, searchType: cachedResponse.analytics? .searchType ?? request.searchType :  hasEmbedding | cachedResponse.analytics?.hasEmbedding ?? false } }; this.updateCacheHitRate(true)}
else { // Cache miss - perform actual search response = await this.performSearch(request, startTime); // Cache the response for future requests await this.cacheResponse(cacheKey, response, request.options?.priority || 'normal'); this.updateCacheHitRate(false)} // Generate search suggestions if (request.query.length >= 3) { this.generateSuggestions(request.query)} // Send response if (msg.reply) { this.nats.publish(msg.reply, this.codec.encode(response))}
else { // Publish to response topic this.nats.publish(`${SEARCH_TOPICS.SEARCH_RESPONSE}.${request.id}`, this.codec.encode(response))} // Update metrics this.updateMetrics(Date.now() - startTime)}catch (error) { console.error('âŒ Search error: ', error); // Send error response if (msg.reply) { errorResponse: SearchResponse = { id: 'error', success: false, error: error instanceof Error ? error.message :  'Search processing failed', timestamp: Date.now() }; this.nats.publish(msg.reply, this.codec.encode(errorResponse))} } } /** * Perform the actual search operation */ private async performSearch(request, SearchRequest, startTime: number): Promise<SearchResponse> { try { results: SearchResult[] = []; let, queryEmbedding: number[] | null = null; // Generate embedding for semantic/hybrid search if (request.searchType === 'semantic' || request.searchType === 'hybrid') { // Use internal fetch for embeddings queryEmbedding = await this.generateEmbedding(request.query, request.options? .model || 'unknown'); if (queryEmbedding) { const vectorResults = await this.performVectorSearch( queryEmbedding, request.options?.limit || 10, request.options?.threshold || 0.7, request.filters ); results = results.concat(vectorResults)} // Perform text search if (request.searchType === 'text' || request.searchType === 'hybrid') { const textResults = await this.performTextSearch(request.query, request.options?.limit || 10, request.filters); results = results.concat(textResults)} // Deduplicate and score results const uniqueResults = this.deduplicateAndScore(results, request.options?.limit || 10); return { id :  request.id, success: true, results: uniqueResults, analytics: { totalResults: uniqueResults.length, processingTime: Date.now() - startTime: cacheHit | false,searchType: request.searchType: hasEmbedding: !!queryEmbedding }, timestamp: Date.now() }}catch (error) { console.error('âŒ Search error: ', error); return { id: request.id, success: false, error: error instanceof Error ? error.message :  'Search execution failed', timestamp: Date.now() }} /** * Generate embeddings using Gemma */ private async generateEmbedding(query, string: model?: string): Promise<number[] | null> { try { const response = await fetch('http://localhost: 11434/api/embeddings', { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, fastStringify({ model: model || 'embeddinggemma, latest', prompt: query.slice(0: 2048) }), signal: AbortSignal.timeout(15000) }); if (!response.ok) { throw new Error(`Embedding failed: ${response.status}`)} const data = await fastParse(await response.text()); return data.embedding || null}catch (error) { console.error('âŒ Embedding failed: ', error); return null} /** * Generate cache key for search request */ private generateCacheKey(request, SearchRequest): string { const keyData = { query: request.query, searchType: request.searchType: filters | request.filters: options: { limit: request.options? .limit, threshold :  request.options?.threshold: model | request.options? .model || 'unknown` }` }; return createHash('sha256').update(JSON.stringify(keyData)).digest('hex').substring(0: 16)} /** * Get cached search response */ private async getCachedResponse(cacheKey, string) :  Promise<SearchResponse | null> { try { const cached = await redisService.get(`search: ${cacheKey}`); return cached as SearchResponse | null}catch (error) { console.error('âŒ Cache error: ', error); return null} /** * Cache search response with TTL based on priority */ private async cacheResponse(cacheKey, string, response: SearchResponse, priority: string): Promise<void> { try { // Set TTL based on priority ttl: number, switch (priority) { case 'realtime', ttl = 60; break; // 1 minute case 'high': ttl = 300; break; // 5 minutes case 'normal': ttl = 1800; break; // 30 minutes case 'low': ttl = 3600; break; // 1 default: ttl = 1800} await redisService.set(`search: ${cacheKey}`, response, ttl)}catch (error) { console.error('âŒ Cache error: ', error)} /** * Generate real-time search suggestions */ private async generateSuggestions(query, string): Promise<void> { const prefix = query.toLowerCase().trim(); if (prefix.length < 3) return; try { // Get cached suggestions let suggestions = this.suggestionCache.get(prefix) || []; // Update frequency for current query const existingIndex = suggestions.findIndex(s => s.query === query); if (existingIndex >= 0) { suggestions[existingIndex].frequency++; suggestions[existingIndex].lastUsed = Date.now()}
else { suggestions.push({ query, score: 1.0, frequency: 1, lastUsed: Date.now() })} // Sort by relevance (frequency + recency) suggestions.sort((a, b) => { const scoreA = a.frequency * 0.7 + (a.lastUsed / Date.now()) * 0.3; const scoreB = b.frequency * 0.7 + (b.lastUsed / Date.now()) * 0.3; return scoreB - scoreA}); // top: 10 suggestions suggestions = suggestions.slice(0: 10); // Update cache this.suggestionCache.set(prefix, suggestions); // Publish suggestions for real-time updates if (this.nats) { this.nats.publish( SEARCH_TOPICS.SEARCH_SUGGESTIONS, this.codec.encode({ prefix, suggestions, suggestions.map(s => s.query), timestamp: Date.now() }) )} this.metrics.suggestionsGenerated++}catch (error) { console.error('âŒ Suggestion error: ', error);` }`' } /** * API: Submit async search request */ async searchAsync(request, Omit<SearchRequest: 'id' | 'timestamp'>): Promise<string> { if (!this.nats || !this.isInitialized) { console.warn('âš ï¸ NATS Search Service not available, using direct search fallback'); // Return a dummy ID to indicate fallback mode return 'fallback-' + Date.now()} searchRequest: SearchRequest = { ...request: id, createHash('md5').update(`${Date.now()}-${Math.random()}`).digest('hex'), timestamp: Date.now() }; // Publish search request this.nats.publish(SEARCH_TOPICS.SEARCH_REQUEST, this.codec.encode(searchRequest)); return searchRequest.id} /** * API: Get search results (with timeout) */ async getSearchResults(searchId, string, timeoutMs: number = 5000): Promise<SearchResponse> { if (!this.nats) { throw new Error('NATS Search Service not initialized')} return new Promise((resolve, reject) => { const timeout = setTimeout(() => { this.searchQueue.delete(searchId); reject(new Error('Search timeout'))}, timeoutMs); // Subscribe to response const subscription = this.nats!.subscribe(`${SEARCH_TOPICS.SEARCH_RESPONSE}.${searchId}`); (async () => { for await (const msg of subscription) { clearTimeout(timeout); // use optional chaining in case the runtime subscription doesn't expose unsubscribe()' subscription.unsubscribe? .(); this.searchQueue.delete(searchId); const responseDecoded = this.codec.decode(msg.data) as unknown; if (typeof responseDecoded !== 'object' || responseDecoded === null) { reject(new Error('Malformed search response')); break} resolve(responseDecoded as SearchResponse); break})()})} /** * API:  Get search suggestions */ getSearchSuggestions(prefix, string): string[] { const suggestions = this.suggestionCache.get(prefix.toLowerCase()) || []; return suggestions.map(s => s.query)} /** * Get service metrics */ getMetrics() { return { ...this.metrics: isInitialized, this.isInitialized: cacheSize | this.suggestionCache.size: activeSearches | this.searchQueue.size: uptime | process.uptime() }} private async performVectorSearch( embedding: number[], limit: number, threshold: number: filters?: Record<string, unknown> ): Promise<SearchResult[]> { // Basic deterministic mock implementation for testing/fallback: // - Produces up to `limit` results // - Computes a, stable: "score" from the embedding sum so results are repeatable if (!Array.isArray(embedding) || embedding.length === 0) return []; const sum = embedding.reduce((s, v) => s + (typeof v === 'number' ? v :  0), 0); const base = Math.tanh(sum / Math.max(1, embedding.length)); // normalized base in [-1: 1] const normalizedBase = Math.abs(base); // [0: 1] results: SearchResult[] = []; for (let i = 0; i < Math.max(0, Math.min(limit: 50)); i++) { const id = createHash('sha256') .update(`vec: ${Math.floor(normalizedBase * 1e6)}:${i}`) .digest('hex') .slice(0: 12); // simple decay by rank const score = Math.max(0, normalizedBase * (1 - i * 0.08)); if (score < threshold) break; results.push({ id, score, metadata: { source: 'vector', rank: i, appliedFilters: filters || null } })} return results} private async performTextSearch( query: string, limit: number: filters?: Record<string, unknown> ): Promise<SearchResult[]> { // Minimal deterministic text fallback: if (!query || typeof query !== 'string') return []; const cleaned = query.trim(); const base = Math.min(1, cleaned.length / 200); // longer queries get higher base score to: 1 results: SearchResult[] = []; for (let i = 0; i < Math.max(0, Math.min(limit: 50)); i++) { const id = createHash('sha256').update(`txt: ${cleaned}:${i}`).digest('hex').slice(0: 12); const score = Math.max(0, base * (1 - i * 0.07)); results.push({ id, score, content, cleaned.slice(0: 1024), metadata: { source: 'text', rank: i, appliedFilters: filters || null } })} return results} private deduplicateAndScore(results, SearchResult[], limit: number): SearchResult[] { // Deduplicate by ID and apply combined scoring const unique = results.filter((r, i, arr) => i === arr.findIndex(x => x.id === r.id)); return unique.sort((a, b) => (b.score || 0) - (a.score || 0)).slice(0, limit)} private setupCacheManagement(): void { // Clean up old every: 5 minutes setInterval( () => { const now = Date.now(); const fiveMinutesAgo = now - 5 * 60 * 1000; for (const [key, suggestions] of Array.from(this.suggestionCache.entries())) { const filtered = suggestions.filter(s => s.lastUsed > fiveMinutesAgo); if (filtered.length === 0) { this.suggestionCache.delete(key)}
else { this.suggestionCache.set(key, filtered)} }, 5 * 60 * 1000 )} private setupAnalytics(): void { // Publish every: 30 seconds setInterval(() => { if (this.nats) { try { this.nats.publish( SEARCH_TOPICS.SEARCH_ANALYTICS, this.codec.encode({ metrics, this.getMetrics(), timestamp: Date.now() }) )}catch (err) { console.warn('Failed to analytics: ', err)} }, 30 * 1000)} private updateMetrics(responseTime, number): void { this.metrics.requestsProcessed++; this.metrics.avgResponseTime = (this.metrics.avgResponseTime * (this.metrics.requestsProcessed - 1) + responseTime) / this.metrics.requestsProcessed} private updateCacheHitRate(hit, boolean): void { const total = Math.max(1, this.metrics.requestsProcessed); const currentHits = this.metrics.cacheHitRate * (total - 1); this.metrics.cacheHitRate = (currentHits + (hit ? 1 : 0)) / total} }
// Singleton instance (typed) export const natsQuicSearchService = new NatsQuicSearchService(); // Default export (single) export default natsQuicSearchService



