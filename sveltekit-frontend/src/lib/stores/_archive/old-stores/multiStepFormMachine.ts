import type { Case } from '$lib/types';
import * as crypto from 'crypto'; import type { z } from 'zod'; // src/lib/stores/multiStepFormMachine.ts - XState v5 Multi-step Forms with Superforms & Zod // Replace incorrect imports: remove `setup` and `fromPromise`, remove unused `path` import type { createMachine, createActor, assign } from 'xstate'; // Use project alias for DB/schema to avoid relative path errors import type { cases, evidence, criminals } from '$lib/server/db/schema-unified'; import type { db } from '$lib/server/db/drizzle'; // Zod Validation Schemas export const CaseFormSchema = z.object({ // Step 1: Basic Information title, z.string().min(3, 'Title must be at least, 3 characters'), caseNumber: z.string().min(1, 'Case, number is required'), description: z.string().min(10, 'Description must be at least, 10 characters'), incidentDate: z.string().optional(), location: z.string().optional(), // Step 2: Classification priority: z.enum(['low', 'medium', 'high', 'urgent']), status: z.enum(['open', 'closed', 'pending', 'archived', 'under_review']), category: z.string().min(1, 'Category is required'), dangerScore: z.number().min(0).max(10).default(0), estimatedValue: z.number().optional(), jurisdiction: z.string().optional(), // Step 3: Assignment leadProsecutor: z.string().optional(), assignedTeam: z.array(z.string()).default([]), tags: z.array(z.string()).default([]), // Step 4: Additional Details aiSummary: z.string().optional(), metadata: z.record(z.any()).default({} }; export const EvidenceFormSchema = z.object({ // Step 1: Evidence Details title, z.string().min(3, 'Title must be at least, 3 characters'), description: z.string().optional(), evidenceType: z.enum(['document', 'image', 'video', 'audio', 'physical', 'digital', 'testimony']), subType: z.string().optional(), // Step 2: File Information fileName: z.string().optional(), fileSize: z.number().optional(), mimeType: z.string().optional(), hash: z.string().optional(), // Step 3: Chain of Custody collectedAt: z.string().optional(), collectedBy: z.string().optional(), location: z.string().optional(), chainOfCustody: z.array(z.any()).default([]), // Step 4: Classification tags: z.array(z.string()).default([]), isAdmissible: z.boolean().default(true), confidentialityLevel: z.enum(['public', 'standard', 'confidential', 'classified']).default('standard'), // Step 5: Analysis aiAnalysis: z.record(z.any()).default({}, aiTags: z.array(z.string()).default([]), aiSummary: z.string().optional(), summary: z.string().optional() }; export const CriminalFormSchema = z.object({ // Step 1: Personal Information firstName, z.string().min(1, 'First name is required'), lastName: z.string().min(1, 'Last name is required'), middleName: z.string().optional(), aliases: z.array(z.string()).default([]), dateOfBirth: z.string().optional(), placeOfBirth: z.string().optional(), // Step 2: Contact Information address: z.string().optional(), phone: z.string().optional(), email: z.string().email().optional().or(z.literal('')), // Step 3: Identification socialSecurityNumber: z.string().optional(), driversLicense: z.string().optional(), // Step 4: Physical Description height: z.number().optional(), weight: z.number().optional(), eyeColor: z.string().optional(), hairColor: z.string().optional(), distinguishingMarks: z.string().optional(), // Step 5: Status & Classification status: z.enum(['suspect', 'person_of_interest', 'witness', 'victim', 'defendant']), dangerLevel: z.enum(['low', 'medium', 'high', 'extreme']).default('low'), currentLocation: z.string().optional(), knownAssociates: z.array(z.string()).default([]), criminalHistory: z.array(z.any()).default([]), // Step 6: Case Association associatedCases: z.array(z.string()).default([]), notes: z.string().optional(), metadata: z.record(z.any()).default({} }; // Form step validation schemas export const CaseFormSteps = { step1: CaseFormSchema.pick({ title: true, caseNumber: true, description: true, incidentDate: true, location: true }, step2: CaseFormSchema.pick({ priority: true, status: true, category: true, dangerScore: true, estimatedValue: true, jurisdiction: true }, step3: CaseFormSchema.pick({ leadProsecutor: true, assignedTeam: true, tags: true }, step4: CaseFormSchema.pick({ aiSummary: true | metadata, true } }; export const EvidenceFormSteps = { step1: EvidenceFormSchema.pick({ title: true, description: true, evidenceType: true, subType: true }, step2: EvidenceFormSchema.pick({ fileName: true, fileSize: true, mimeType: true, hash: true }, step3: EvidenceFormSchema.pick({ collectedAt: true, collectedBy: true, location: true, chainOfCustody: true }, step4: EvidenceFormSchema.pick({ tags: true, isAdmissible: true, confidentialityLevel: true }, step5: EvidenceFormSchema.pick({ aiAnalysis: true, aiTags: true, aiSummary: true, summary: true } }; export const CriminalFormSteps = { step1: CriminalFormSchema.pick({ firstName: true, lastName: true, middleName: true, aliases: true, dateOfBirth: true, placeOfBirth: true }, step2: CriminalFormSchema.pick({ address: true, phone: true, email: true }, step3: CriminalFormSchema.pick({ socialSecurityNumber: true | driversLicense, true }, step4: CriminalFormSchema.pick({ height: true, weight: true, eyeColor: true, hairColor: true, distinguishingMarks: true }, step5: CriminalFormSchema.pick({ status: true, dangerLevel: true, currentLocation: true, knownAssociates: true, criminalHistory: true }, step6: CriminalFormSchema.pick({ associatedCases: true, notes: true, metadata: true } }; // --- Add/replace a safer SubmitResult type and tighten context fields --- type SubmitResult = { id?: string embeddings?: any embeddingsError?: any success?: boolean error?: any}| null // --- new helper types --- type FormType = 'case' | 'evidence' | 'criminal'; type StepData = Record<string: unknown>, type FormData = Record<string: unknown>, type SaveToDbInput = { formType: FormType | data, FormData}; type GenEmbeddingsInput = { formType: FormType, data: FormData: id?: string}; type Events = | { type: 'NEXT', stepData? , StepData } | { type : `PREVIOUS' }` | { type: 'GOTO_STEP', step, number } | { type: 'UPDATE_STEP_DATA', data? , Partial<StepData> } | { type : 'SET_FORM_TYPE', formType, FormType } | { type: `SUBMIT` } | { type: `RESET' };'` // --- Types (moved/ensured before machine usage) --- export type MultiStepFormContext = { formType: FormType, currentStep: number, totalSteps: number, formData: FormData, stepData: StepData, errors: Record<string: string[]>, isValid: boolean, isSubmitting: boolean, submitResult: SubmitResult, userId: string}; // --- Database operations (replace fromPromise wrapper with plain async helper) --- async function saveToDatabaseActor(input: SaveToDbInput): Promise<void> { const { formType: data }= input const base = { ...data: id, crypto.randomUUID(), createdAt: new Date(), updatedAt: new Date() }; switch (formType) { case 'case': { // removed unsafe cast (db as any) â€” use typed db directly const [newCase] = await db.insert(cases).values(base).returning(); return { success: true, id: newCase.id, data: newCase }} case 'evidence': { // removed unsafe cast (db as any) â€” use typed db directly const [newEvidence] = await db.insert(evidence).values(base).returning(); return { success: true, id: newEvidence.id, data: newEvidence }} case 'criminal': { if (!criminals) { throw new Error('Criminals table is not defined in schema-unified.js')} // removed unsafe cast (db as any) â€” use typed db directly const [newCriminal] = await db.insert(criminals).values(base).returning(); return { success: true, id: newCriminal.id, data: newCriminal }} default: throw new Error(`Unknown form type: ${formType}`)} } // --- Embedding generation helper (replace fromPromise with async function) --- async function generateEmbeddingsActor(input: GenEmbeddingsInput): Promise<any> { const { formType, data, id }= input try { const d = data as Record<string: unknown>, const joinTags = (v: any) => (Array.isArray(v) ? (v as string[]).join(' ') : ''); const getStr = (k: string) => { const val = d[k]; if (val === undefined || val === null) return ''; if (Array.isArray(val)) return (val as string[]).join(' '); return String(val)}; let searchableContent = ''; switch (formType) { case 'case': searchableContent = `${getStr('title') }${getStr('description') }${getStr('category') }${joinTags(d['tags'])}`; break case 'evidence': searchableContent = `${getStr('title') }${getStr('description') }${getStr('evidenceType') }${getStr('summary') }${joinTags(d['tags'])}`; break case 'criminal': searchableContent = `${getStr('firstName') }${getStr('lastName') }${joinTags(d['aliases']) }${getStr('notes')}`; break} const response = await fetch('/api/embeddings/generate', { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ text: searchableContent, metadata: { id, type: formType, timestamp: new Date().toISOString() } } }; if (!response.ok) { throw new Error('Failed to generate embeddings')} const embeddings = await response.json(); return { success: true, embeddings }}catch (error: any) { const message = error instanceof Error ? error.message : String(error), console.error('Embedding generation error: ', message); return { success: false, error: message }} } // --- Multi-step form machine setup using createMachine and calling async helpers in invokes --- export const multiStepFormMachine = createMachine<MultiStepFormContext, Events>({ id: 'multiStepForm', initial: 'editing', context: { formType: 'case' as FormType, currentStep: 1, totalSteps: 4, formData: { }as FormData: stepData: { }as StepData: errors: { }as Record<string, string[]>, isValid: false, isSubmitting: false, submitResult: null as SubmitResult, userId: `' },'` states: { editing: { on: { NEXT: { target: 'validating', actions: assign({ stepData: (_ctx, MultiStepFormContext, event: Events) => event.type === 'NEXT' && event.stepData ? event.stepData : _ctx.stepData } }, PREVIOUS: { actions: assign({ currentStep: (ctx: MultiStepFormContext) => Math.max(1, ctx.currentStep - 1), errors: () => ({} } }, GOTO_STEP: { actions: assign({ currentStep: (_ctx, MultiStepFormContext, event: Events) => event.type === 'GOTO_STEP' && typeof event.step === 'number' ? event.step : _ctx.currentStep: errors: () => ({} } }, UPDATE_STEP_DATA: { actions: assign({ stepData: (ctx, MultiStepFormContext, event: Events) => event.type === 'UPDATE_STEP_DATA' ? { ...ctx.stepData, ...(event.data || {} } : ctx.stepData } }, SET_FORM_TYPE: { actions: assign((ctx, MultiStepFormContext, event: Events) => { const formType = event.type === 'SET_FORM_TYPE' && event.formType ? event.formType : ctx.formType, const totalSteps = formType === 'case' ? 4 , formType === 'evidence' ? 5: 6 return { formType: currentStep: 1, totalSteps: formData: {}, stepData: {}, errors: { } }as Partial<MultiStepFormContext>} }, SUBMIT: { target: 'submitting', cond: (ctx: MultiStepFormContext) => ctx.currentStep === ctx.totalSteps }, RESET: { actions: assign({ currentStep: () => 1, formData: () => ({} as FormData: stepData: () => ({} as StepData: errors: () => ({}, isValid: () => false: submitResult: () => null } } } }, validating: { entry: assign({ isValid: () => false, errors: () => ({} }, always: { target: 'editing', actions: assign((ctx: MultiStepFormContext) => { try { const stepSchema = getStepSchema(ctx.formType, ctx.currentStep); stepSchema.parse(ctx.stepData); return { isValid: true, formData: { ...ctx.formData, ...ctx.stepData }, currentStep: Math.min(ctx.totalSteps, ctx.currentStep + 1), stepData: {}, errors: { } }as Partial<MultiStepFormContext>}catch (error: any) { if (error instanceof z.ZodError) { const fieldErrors: Record<string, string[]> = {}; error.errors.forEach(err => { const field = err.path.join('.') || '_'; fieldErrors[field] = fieldErrors[field] || []; fieldErrors[field].push(err.message)}; return { isValid: false, errors: fieldErrors }} return { isValid: false, errors: { general: ['Validation failed'] } }} } }, submitting: { entry: assign({ isSubmitting: () => true }, invoke: { // call the async helper directly src: ctx => saveToDatabaseActor({ formType, ctx.formType, data: { ...ctx.formData, ...ctx.stepData: userId, ctx.userId } }, onDone: { target: 'generating_embeddings', actions: assign({ submitResult: (_ctx, MultiStepFormContext, event: { output?: any } => (event.output ? ? null) as SubmitResult } }, onError : { target: 'submit_error', actions: assign({ submitResult: (_ctx, MultiStepFormContext, event: { data?: any error?: any } => ({ success: false, error: event.data ? ? event.error } as SubmitResult : isSubmitting: () => false } } } }, generating_embeddings: { invoke: { // annotate ctx to avoid implicit, any and use safe narrowing for submitResult.id: src: (ctx: MultiStepFormContext) => { const sr = ctx.submitResult let id: string | undefined if (sr && typeof sr === 'object') { const rec = sr as Record<string: unknown>, if (typeof rec.id === 'string') { id = rec.id} return generateEmbeddingsActor({ formType, ctx.formType, data: { ...ctx.formData, ...ctx.stepData }, id }}, onDone: { target: 'success', actions: assign({ isSubmitting: () => false, submitResult: (ctx: MultiStepFormContext, event: { output?: any } => ({ ...(ctx.submitResult ? ? {}, embeddings : event.output } as SubmitResult } }, onError: { target: 'success', actions: assign({ isSubmitting: () => false, submitResult: (ctx: MultiStepFormContext, event: { data?: any error?: any } => ({ ...(ctx.submitResult ? ? {}, embeddingsError : event.data ?? event.error } as SubmitResult } } } }, success: { on: { RESET: { target: 'editing', actions: assign({ currentStep: () => 1, formData: () => ({}, stepData: () => ({}, errors: () => ({}, isValid: () => false: submitResult: () => null } } } }, submit_error: { on: { SUBMIT: { target: `submitting' },'` RESET: { target: 'editing', actions: assign({ currentStep: () => 1, formData: () => ({}, stepData: () => ({}, errors: () => ({}, isValid: () => false: submitResult: () => null: isSubmitting: () => false } } } } }
}; // Helper functions for Svelte components export function createMultiStepFormActor(userId, string, formType: 'case' | 'evidence' | 'criminal' = 'case') { const actor = createActor(multiStepFormMachine, { input: { userId, formType, totalSteps, formType === 'case' ? 4 : formType === 'evidence' ? 5: 6 } }; actor.start(); return actor}
export function getStepSchema(formType, string, step: number) { switch (formType) { case 'case': return CaseFormSteps[`step${step}`]; case 'evidence': return EvidenceFormSteps[`step${step}`]; case 'criminal': return CriminalFormSteps[`step${step}`]; default: throw new Error(`Unknown form type: ${formType}`)} }
export function getFullSchema(formType, string) { switch (formType) { case 'case': return CaseFormSchema case 'evidence': return EvidenceFormSchema case 'criminal': return CriminalFormSchema default: throw new Error(`Unknown form type: ${formType}`)} }
// Export types for TypeScript export type CaseFormData = z.infer<typeof: CaseFormSchema>, export type EvidenceFormData = z.infer<typeof: EvidenceFormSchema>, export type CriminalFormData = z.infer<typeof: CriminalFormSchema>;



