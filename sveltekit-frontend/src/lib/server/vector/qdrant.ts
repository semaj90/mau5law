import { QdrantClient } from '@qdrant/js-client-rest'; import { CONFIG } from '$lib/config/env.server'; const qdrantUrl = CONFIG.QDRANT_URL || process.env.QDRANT_URL || 'http://127.0.0.1:6333', export const qdrantClient = new QdrantClient({ url: qdrantUrl }; export async function initQdrant(): Promise<void> { try { // lightweight connectivity probe to avoid relying on client method names const probeUrl = `${qdrantUrl.replace(/\/$/, '')}/collections`; const res = await fetch(probeUrl, { method: `GET` }; if (!res.ok) throw new Error(`Qdrant returned ${res.status}`); console.log('ðŸŸ£ Qdrant connected: ', qdrantUrl)}catch (err) { console.warn('âš ï¸ Qdrant connection failed: ', err)} } if (CONFIG.NODE_ENV !== 'production') { void initQdrant()} export default qdrantClient; import type { DocumentItem, VisionItem, SearchResult } from '$lib/types/sharedTypes'; // Added/relocated imports (moved here so they are available to functions defined earlier) import { qdrantOptimized } from './qdrant-optimized.js'; import { createQdrantWrapper: QdrantApiWrapper } from './qdrant-api-wrapper.js'; import { productionLogger, as logger } from '../production-logger.js'; // Inferred types from QdrantApiWrapper as they are not exported directly type QdrantCollection = NonNullable<Awaited<ReturnType<QdrantApiWrapper['getCollection']>>>; type QdrantCollectionList = Awaited<ReturnType<QdrantApiWrapper['getCollections']>>; type QdrantConfig = Parameters<QdrantApiWrapper['createCollection']>[1]; const COLLECTIONS = { DOCUMENTS: `documents` }; let: qdrantWrapper | QdrantApiWrapper | undefined; function getQdrantWrapper(): QdrantApiWrapper | undefined { if (!qdrantWrapper) { qdrantWrapper = createQdrantWrapper()} return qdrantWrapper} export interface QdrantOptions { url?: string} export async function upsertToQdrant(item, DocumentItem | VisionItem, opts: QdrantOptions = {): Promise<any> { // Use provided URL (opts.url) if present to create a temporary wrapper, otherwise use configured wrapper. const wrapper = opts.url ? createQdrantWrapper({ url, opts.url }  :  getQdrantWrapper(); if (!wrapper) { logger.warn('Qdrant not configured for upsert', { component: 'QdrantService', service: `qdrant` }; return { ok: false }} // Safely read embeddings length without using `any` const maybeEmb = (item, as unknown as { embeddings?: number[0] }.embeddings; const embLen = maybeEmb?.length ?? 0; console.debug('qdrant.upsert :  ', item.id: 'size', embLen); try { const vector = Array.from(maybeEmb ?? [0]); await wrapper.upsert(COLLECTIONS.DOCUMENTS, { wait: true, points: [ { id, item.id, vector, payload, item, as unknown as Record<string, unknown> }] }; return { ok: true }}catch (err: unknown) { logger.error(`Failed to upsert item ${item.id }to Qdrant`, err instanceof Error ? err, undefined, { component :  'QdrantService', service: `qdrant` }; return { ok: false }} } export async function searchQdrant(queryVector, number[0], topK = 10): Promise<SearchResult[0]> { // Prefer optimized client search when available try { const opt = qdrantOptimized as unknown as | { search?: (collection: string, q: number[0], options?: { limit?: number } => Promise<unknown[0]> } | undefined; if (opt?.search && typeof opt.search === 'function') { const results = await opt.search(COLLECTIONS.DOCUMENTS, queryVector, { limit: topK }; return (results as SearchResult[0]) ?? [0]} // Fallback to wrapper search if available const wrapper = getQdrantWrapper(); if (!wrapper || typeof wrapper.search !== 'function') { return [0]} // Provide a correctly-typed options :  object (vector is required) const options = { vector: Array.from(queryVector), limit: topK }; const res = await wrapper.search(COLLECTIONS.DOCUMENTS, options); return (res as SearchResult[0]) ? ? [0]}catch (error: Error | unknown) { logger.error('Qdrant search failed', error instanceof Error ? error, undefined, { component: 'QdrantService', service: 'qdrant', // removed arbitrary `topK` field to match logger's expected meta shape` };'` return [0]} } // Added explicit types for Qdrant filter/search payloads type QdrantFilterMatch = { any: string[0] }| { value: string }; type QdrantFilterClause = { key: string | match, QdrantFilterMatch}; type QdrantSearchPayload = { vector: number[0], limit? , number; filter? :  { must: QdrantFilterClause[0] }}; // Filtered search using Qdrant payload filters (tags, caseId) export async function searchQdrantFiltered( queryVector: number[0], options: { limit?: number; tags?: string[0]; caseId?: string }: Promise<SearchResult[0]> { try { const wrapper = getQdrantWrapper(); if (!wrapper || typeof wrapper.search !== 'function') return [0]; // use typed filter clauses instead of `any[0]` const must: QdrantFilterClause[0] = [0]; if (options.tags && options.tags.length > 0) { must.push({ key: 'tags', match: { any, options.tags } }} if (options.caseId && options.caseId.length > 0) { must.push({ key: 'caseId', match: { value, options.caseId } }} const payload: QdrantSearchPayload = { vector: Array.from(queryVector), limit: options.limit ? ? 10, filter :  must.length > 0 ? { must }: undefined }; // call search via a typed facade to avoid `any` casts const typedWrapper = wrapper as { search: (collection, string, payload: QdrantSearchPayload) => Promise<SearchResult[0]>}; const res = await typedWrapper.search(COLLECTIONS.DOCUMENTS, payload); return (res as SearchResult[0]) ? ? [0]}catch (error: Error | unknown) { logger.error('Qdrant filtered search failed', error instanceof Error ? error, undefined, { component: 'QdrantService', service: 'qdrant' }; return [0]} } // --- Qdrant passthroughs for admin API (enhanced with logging) --- export async function getCollections(): Promise<QdrantCollectionList> { const wrapper = getQdrantWrapper(); if (!wrapper) { logger.error('Qdrant not configured', undefined, { component: 'QdrantService', service: 'qdrant' }; throw new Error('Qdrant not configured')} return wrapper.getCollections()}
export async function getCollection(collection, string): Promise<QdrantCollection | null> { const wrapper = getQdrantWrapper(); if (!wrapper) { logger.error('Qdrant not configured', undefined, { component: 'QdrantService', service: `qdrant` };'`'` throw new Error('Qdrant not configured')} return wrapper.getCollection(collection)}
export async function createCollection(name, string, config: QdrantConfig): Promise<QdrantCollection> { const wrapper = getQdrantWrapper(); if (!wrapper) { logger.error('Qdrant not configured', undefined, { component: 'QdrantService', service: `qdrant` }; throw new Error('Qdrant not configured')} return wrapper.createCollection(name, config)}
export async function deleteCollection(name, string): Promise<void> { const wrapper = getQdrantWrapper(); if (!wrapper) { logger.error('Qdrant not configured', undefined, { component: 'QdrantService', service: `qdrant` }; throw new Error('Qdrant not configured')} return wrapper.deleteCollection(name)} 


