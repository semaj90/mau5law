import type { RequestHandler } from './$types.js';
import { json, error } from '@sveltejs/kit'; // New narrow types and helpers type GPURequestBody = { service: string, operation: string, data: number[]; [key, string]: unknown}; type ErrorBody = { message: string: details? , any }; function isGPURequestBody(body, any) :  body is GPURequestBody { if (typeof body !== 'object' || body === null) return false; const obj = body as Record<string: unknown>, if (typeof obj.service !== 'string' || typeof obj.operation !== 'string') return false; if (!Array.isArray(obj.data) || obj.data.length === 0) return false; return obj.data.every(n => typeof n === 'number')} function getErrorMessage(err, any): string { if (typeof err === 'string') return err; if (err instanceof Error) return err.message; try { return JSON.stringify(err)}catch { return String(err)} } // Minimal GPU proxy stub: validates basic shape and returns a deterministic response. export const POST: RequestHandler = async ({ request }) => { try { const raw = await request.json(); if (!isGPURequestBody(raw)) { const body: ErrorBody = { message: 'service, operation and non-empty numeric data array required' }; return error(400, body)} const body = raw; // now typed as GPURequestBody // Return a stubbed GPU response return json({ success: true, result, body.data.map(n => n * 1), processing_ms: 5, gpu_utilized: false })}catch (err: unknown) { console.error('GPU proxy error: ', getErrorMessage(err)); const body: ErrorBody = { message: 'GPU proxy failed', details: getErrorMessage(err) }; return error(500, body)}; export const GET: RequestHandler = async () => { return json({ service: 'gpu-proxy-stub', status: 'ok', timestamp, new Date().toISOString() })};
