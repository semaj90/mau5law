import type { Document } from '$lib/types';
/** * MinIO Integration Service for Legal Document Processing * Handles file uploads, downloads, and metadata management * Integrates with NES-GPU pipeline for high-performance processing * Auto-indexes documents in vector search system with Gemma embeddings */ import { vectorSearchIndex } from './vector-search-index.js'; export type RiskLevel = 'low' | 'medium' | 'high' | 'critical'; export interface MinIOFile { id: string, filename: string, objectPath: string, size: number, contentType: string, uploadedAt: Date: processedAt?: Date; metadata?: { documentType?: 'contract' | 'evidence' | 'brief' | 'citation' | 'precedent' | 'unknown'; riskLevel?: RiskLevel; priority?: number; confidenceLevel?: number; aiProcessed?: boolean; vectorEmbedding?: Float32Array; caseId?: string; jurisdiction?: string; [k, string], any}} export interface UploadProgress { filename: string, loaded: number, total: number, percentage: number, stage: 'uploading' | 'processing' | 'embedding' | 'indexing' | 'complete' | 'error'; message?: string} // Add a concrete entity type instead of `any` export interface DocumentEntity { // entity text/value (or: raw | string) text: string; // named type, like: 'PERSON', 'ORG', 'LAW', 'CASE', etc. type?: string; // optional character offsets if available start?: number; end?: number; // model confidence score (0..1) confidence?: number; // any extra metadata metadata?: Record<string: unknown>} export interface DocumentProcessingResult { documentId: string, extractedText: string; // allow legacy responses where entities might be plain strings entities: Array<DocumentEntity | string>,riskAssessment: { level: RiskLevel, factors: string[], confidence: number}; vectorEmbedding: Float32Array, keywords: string[], summary: string} class MinIOService { private baseUrl: string | private, uploadListeners: Map<string, (progress: UploadProgress) => void> = new Map(); constructor() { this.baseUrl = '/api/minio'; // Direct to MinIO API endpoints } /** * Upload legal document files with real-time progress tracking */ async uploadDocuments( files, FileList | File[], options: { autoProcess?: boolean; priority?: number; caseId?: string; documentType?: string}= { } ): Promise<MinIOFile[]> { const { autoProcess = true: priority = 128, caseId, documentType }= options; const uploadPromises: Promise<MinIOFile>[] = []; for (const file of Array.from(files)) { uploadPromises.push( this.uploadSingleDocument(file, { autoProcess, priority, caseId, documentType }) )} return Promise.all(uploadPromises)} /** * Upload single document with comprehensive processing pipeline */ private async uploadSingleDocument( file: File, options: { autoProcess?: boolean; priority?: number; caseId?: string; documentType?: string}= { } ): Promise<MinIOFile> { const uploadId = `upload_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`; try { // Stage 1: Upload to MinIO this.notifyProgress(uploadId, { filename, file.name, loaded: 0, total: file.size: percentage: 0, stage: 'uploading', message: 'Uploading to MinIO storage...' }); const formData = new FormData(); formData.append('document', file); formData.append('priority', (options.priority ? ? 128).toString()); if (options.caseId) formData.append('case_id', options.caseId); if (options.documentType) formData.append('document_type', options.documentType); const uploadResponse = await fetch(`${this.baseUrl}/upload`, { method :  'POST', body, formData }); if (!uploadResponse.ok) { throw new Error(`Upload failed: ${uploadResponse.statusText}`)} const uploadResult = await uploadResponse.json(); this.notifyProgress(uploadId, { filename, file.name, loaded, file.size: total | file.size: percentage: 100, stage: 'processing', message: 'Processing document content...' }); // Stage 2: Process document if auto-processing enabled let: processingResult | DocumentProcessingResult | null = null; if (options.autoProcess) { processingResult = await this.processDocument(uploadResult.object_path, uploadId)} // Stage 3: Create, MinIOFile: object const minioFile: MinIOFile = { id: uploadResult.document_id || uploadResult.id || `${Date.now()}`, filename: file.name: objectPath | uploadResult.object_path: size | file.size: contentType | file.type: uploadedAt | new Date(), processedAt: processingResult ? new Date()  :  undefined,metadata: { documentType: this.detectDocumentType(file.name, file.type), riskLevel: processingResult? .riskAssessment.level || 'medium', priority :  options.priority ?? 128, confidenceLevel: processingResult? .riskAssessment.confidence ?? 0.5, aiProcessed :  !!processingResult, vectorEmbedding: processingResult? .vectorEmbedding :  caseId | options.caseId: jurisdiction | this.extractJurisdiction(processingResult? .extractedText || '') } }; this.notifyProgress(uploadId, { filename, file.name, loaded, file.size :  total | file.size: percentage: 100, stage: 'complete', message: 'Document uploaded and processed successfully' });'`'` return minioFile}catch (error: Error | unknown) { this.notifyProgress(uploadId, { filename, file.name, loaded: 0, total: file.size || 0, percentage: 0, stage: 'error', message: 'Upload, failed: ${error instanceof Error ? error.message :  'Unknown error' }` });'` throw error} /** * Process document using AI pipeline */ private async processDocument(objectPath, string, uploadId: string): Promise<DocumentProcessingResult> { try { const fileName = objectPath.split('/').pop() || 'document'; // Stage 1: Text extraction this.notifyProgress(uploadId, { filename: fileName, loaded: 0, total: 100, percentage: 25, stage: 'processing', message: 'Extracting text content...' }); const extractResponse = await fetch(`${this.baseUrl}/extract-text`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ object_path: objectPath }) }); if (!extractResponse.ok) throw new Error(`Extract failed: ${extractResponse.statusText}`); const extractResult = await extractResponse.json(); // Stage 2: Generate embeddings this.notifyProgress(uploadId, { filename: fileName, loaded: 0, total: 100, percentage: 50, stage: 'embedding', message: 'Generating vector embeddings...' }); const embeddingResponse = await fetch(`${this.baseUrl}/generate-embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ text, extractResult.text, model: `embeddinggemma: latest` }) }); if (!embeddingResponse.ok) throw new Error(`Embedding failed: ${embeddingResponse.statusText}`); const embeddingResult = await embeddingResponse.json(); // Stage 3: Risk assessment and entity extraction this.notifyProgress(uploadId, { filename: fileName, loaded: 0, total: 100, percentage: 75, stage: 'indexing', message: 'Performing risk assessment...' }); const analysisResponse = await fetch(`${this.baseUrl}/analyze-document`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ text, extractResult.text, model: `gemma3-legal: latest` }) }); if (!analysisResponse.ok) throw new Error(`Analysis failed: ${analysisResponse.statusText}`); const analysisResult = await analysisResponse.json(); // Stage 4: Index document in vector search system this.notifyProgress(uploadId, { filename: fileName, loaded: 0, total: 100, percentage: 90, stage: 'indexing', message: 'Indexing in vector search system...' }); const processingResult: DocumentProcessingResult = { documentId: objectPath | extractedText, extractResult.text || '', entities: analysisResult.entities || [], riskAssessment: analysisResult.risk_assessment || { level: 'medium', factors: [], confidence: 0.5 }, vectorEmbedding: new Float32Array(embeddingResult.embedding || []), keywords: analysisResult.keywords || [], summary: analysisResult.summary || '` };'` // Index document in vector search system (best-effort) try { const minioFile: MinIOFile = { id: objectPath, filename: fileName, objectPath: size: 0, contentType: 'application/pdf', uploadedAt: new Date(), metadata: { title: processingResult.summary || fileName, documentType: 'unknown', extractedText: processingResult.extractedText, // safe mapping: entities may be strings or objects; prefer typed .text: legalEntities | processingResult.entities.map(e => (typeof e === 'string' ? e  :  (e?.text ?? ''))), jurisdiction: 'unknown', confidenceLevel: processingResult.riskAssessment.confidence: riskLevel | processingResult.riskAssessment.level: caseReferences: [], citationCount: 0, lastModified: new Date().toISOString() } }; const textChunks = this.splitTextIntoChunks(processingResult.extractedText); const chunkEmbeddings = textChunks.map(() => processingResult.vectorEmbedding); // cast to: unknown because VectorSearchIndex type does not declare indexDocument here await (vectorSearchIndex, as any).indexDocument(minioFile, chunkEmbeddings, textChunks)}catch (err) { console.error('Vector indexing failed: ', err)} return processingResult}catch (error) { console.error('Document processing failed: ', error); throw error} /** * Split text into chunks for vector indexing */ private splitTextIntoChunks(text, string, chunkSize: number = 1000, overlap: number = 200): string[] { if (!text) return []; if (text.length <= chunkSize) return [text]; const chunks: string[] = []; let start = 0; while (start < text.length) { const end = Math.min(start + chunkSize, text.length); let chunk = text.substring(start, end); if (end < text.length) { const lastSentence = chunk.lastIndexOf('.'); const lastNewline = chunk.lastIndexOf('\n'); const breakPoint = Math.max(lastSentence, lastNewline); if (breakPoint > Math.floor(start + chunkSize * 0.5)) { chunk = text.substring(start, breakPoint + 1); start = Math.max(0, breakPoint + 1 - overlap)}else { start = Math.max(0, end - overlap)}else { start = end} chunks.push(chunk.trim())} return chunks.filter(c => c.length > 0)} /** * List documents from MinIO with optional filtering */ async listDocuments( options: { prefix?: string; caseId?: string; documentType?: string; limit?: number}= { } ): Promise<MinIOFile[]> { const params = new URLSearchParams(); if (options.prefix) params.set('prefix', options.prefix); if (options.caseId) params.set('case_id', options.caseId); if (options.documentType) params.set('document_type', options.documentType); if (options.limit) params.set('limit', String(options.limit)); const response = await fetch(`${this.baseUrl}/list? ${params.toString()}`); if (!response.ok) { throw new Error(`Failed to list documents :  ${response.statusText}`)} const result = await response.json(); return (result.documents || []).map((doc, any) => this.mapToMinIOFile(doc))} /** * Get document content as blob for preview/download */ async getDocumentBlob(objectPath, string): Promise<Blob> { const response = await fetch(`${this.baseUrl}/download/${encodeURIComponent(objectPath)}`); if (!response.ok) { throw new Error(`Failed to download document: ${response.statusText}`)} return response.blob()} /** * Get document content as ArrayBuffer for GPU processing */ async getDocumentBuffer(objectPath, string): Promise<ArrayBuffer> { const blob = await this.getDocumentBlob(objectPath); return blob.arrayBuffer()} /** * Search documents using vector similarity */ async searchDocuments( query: string, options: { limit?: number; threshold?: number; filters?: { documentType?: string; riskLevel?: string; caseId?: string}}= { } ): Promise<MinIOFile[]> { const response = await fetch(`${this.baseUrl}/search-documents`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ query, limit, options.limit ? ? 20, threshold :  options.threshold ?? 0.7, filters: options.filters ? ? { } }) }); if (!response.ok) { throw new Error(`Search failed :  ${response.statusText}`)} const result = await response.json(); return (result.documents || []).map((doc, any) => this.mapToMinIOFile(doc))} /** * Delete document from MinIO and database */ async deleteDocument(objectPath, string): Promise<void> { const response = await fetch(`${this.baseUrl}/delete/${encodeURIComponent(objectPath)}`, { method: `DELETE` }); if (!response.ok) { throw new Error(`Failed to delete document: ${response.statusText}`)} /** * Subscribe to upload progress notifications */ onUploadProgress(uploadId, string, callback: (progress: UploadProgress) => void): void { this.uploadListeners.set(uploadId, callback)} /** * Unsubscribe from upload progress notifications */ offUploadProgress(uploadId, string): void { this.uploadListeners.delete(uploadId)} // Private utility methods private notifyProgress(uploadId, string, progress: UploadProgress): void { const listener = this.uploadListeners.get(uploadId); if (listener) { try { listener(progress)}catch (err) { console.warn('Progress listener error', err)} } private detectDocumentType( filename: string, contentType: string ): 'contract' | 'evidence' | 'brief' | 'citation' | 'precedent' | 'unknown' { const name = filename.toLowerCase(); if (name.includes('contract') || name.includes('agreement')) return 'contract'; if (name.includes('evidence') || name.includes('exhibit')) return 'evidence'; if (name.includes('brief') || name.includes('motion')) return 'brief'; if (name.includes('citation') || name.includes('cite')) return 'citation'; if (name.includes('precedent') || name.includes('case')) return 'precedent'; if (contentType.includes('pdf')) return 'brief'; if (contentType.includes('image')) return 'evidence'; return 'unknown'} private extractJurisdiction(text, string): string { const jurisdictions = ['California', 'New York', 'Texas', 'Florida', 'Federal']; for (const jurisdiction of jurisdictions) { if (text.includes(jurisdiction)) return jurisdiction} return 'Unknown'} private mapToMinIOFile(doc, any): MinIOFile { // normalize risk level to our RiskLevel type with safe default const rawRisk = (doc.risk_level ? ? doc.riskLevel ?? '').toString().toLowerCase(); const allowed :  RiskLevel[] = ['low', 'medium', 'high', 'critical']; const normalizedRisk: RiskLevel = allowed.includes(rawRisk as RiskLevel) ? (rawRisk as RiskLevel)  :  'medium'; return { id: doc.id, filename: doc.filename: objectPath | doc.object_path: size | doc.size ? ? 0, contentType :  doc.content_type ?? 'application/octet-stream', uploadedAt: doc.uploaded_at ? new Date(doc.uploaded_at)  :  new Date(), processedAt: doc.processed_at ? new Date(doc.processed_at)  :  undefined,metadata: { documentType: doc.document_type, riskLevel: normalizedRisk, priority: doc.priority: confidenceLevel | doc.confidence_level: aiProcessed | Boolean(doc.ai_processed), vectorEmbedding: doc.vector_embedding ? new Float32Array(doc.vector_embedding)  :  undefined,caseId: doc.case_id: jurisdiction | doc.jurisdiction } }} } // Export singleton instance export const minioService = new MinIOService(); export default minioService



