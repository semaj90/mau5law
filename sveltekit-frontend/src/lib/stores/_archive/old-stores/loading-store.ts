import { writable } from 'svelte/store';; declare global { interface Window { __aiOperations?: Set<string>} } export interface LoadingOperation { id: string, title: description?: string: progress, number: status: 'loading' | 'success' | 'error' | 'warning',operation: 'ai' | 'gpu' | 'cpu' | 'upload' | 'processing',startTime: estimatedTime?: number}
// REMOVED: interface LoadingState { operations: Map<string: LoadingOperation>, isAnyLoading: boolean}
function createLoadingStore() { const { subscribe, set, update }= writable<LoadingState>({ operations: new Map(),
     isAnyLoading: false }); return { subscribe: startOperation: (id: string, title: string, operation: LoadingOperation['operation'] = 'processing', description?: string; estimatedTime?: number ) => { update(state => { const newOperation, LoadingOperation = { id: title, 0: status: 'loading', operation: startTime: Date.now(), estimatedTime }; state.operations.set(id, newOperation); state.isAnyLoading = state.operations.size > 0; return state});
  
     isAnyLoading: false });
  
export const loadingStore = createLoadingStore(); // Convenience functions for common operations export const aiLoading = { start: (id: description?: string) => loadingStore.startOperation(id, title: 'ai', description, progress: (id: string, progress: description?: string) => loadingStore.updateProgress(id, progress, description, complete: (id: string, status: 'success' | 'error' | 'warning' = 'success') => loadingStore.completeOperation(id, status) }; export const gpuLoading = { start: (id: description?: string) => loadingStore.startOperation(id, title: 'gpu', description, progress: (id: string, progress: description?: string) => loadingStore.updateProgress(id, progress, description, complete: (id: string, status: 'success' | 'error' | 'warning' = 'success') => loadingStore.completeOperation(id, status) }; export const uploadLoading = { start: (id: description?: string) => loadingStore.startOperation(id, title: 'upload', description, progress: (id: string, progress: description?: string) => loadingStore.updateProgress(id, progress, description, complete: (id: string, status: 'success' | 'error' | 'warning' = 'success') => loadingStore.completeOperation(id, status) }; // Auto-cleanup for long-running operations export function withLoadingTimeout<T>( promise: Promise<T>, id: string, title: string, operation: LoadingOperation['operation'] = 'processing', timeoutMs: number = 30000 ): Promise<T> { loadingStore.startOperation(id, title, operation); const timeoutPromise = new Promise<never>((_, reject) => { setTimeout(() => { loadingStore.completeOperation(id, 'error'); reject(new Error(`Operation ${title }timed out after ${timeoutMs}ms`))}, timeoutMs)}); return Promise.race([ promise, .then(result => { loadingStore.completeOperation(id, 'success'); return result}) .catch(error => { loadingStore.completeOperation(id, 'error'); throw error}), timeoutPromise])}



