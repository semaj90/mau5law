import type { Document } from '$lib/types';
import path from 'path'; import { createWorker } from 'tesseract.js'; import pdf2pic from 'pdf2pic'; import sharp from 'sharp'; import fs from 'fs'; // small helper for safer logging of: unknown errors const safeErrorToString = (err: any): string => (err instanceof Error ? (err.stack ?? err.message)  :  String(err)); // light typing for pdf2pic module to avoid `any` casts type Pdf2PicModule = { fromBuffer?: (,buffer: Buffer | opts, Record<string, unknown> ) => (page: number) => Promise<{ path?: string; name?: string }>; fromPath?: (,path: string, opts: Record<string, unknown> ) => (page: number) => Promise<{ path?: string; name?: string }>}; const pdf2picModule = pdf2pic as unknown as Pdf2PicModule; export interface OCRResult { text: string, confidence: number, pages: PageResult[], metadata: DocumentMetadata, processing_time: number} export interface PageResult { page_number: number, text: string, confidence: number, blocks: TextBlock[], images: ImageRegion[]} export interface TextBlock { text: string, bbox: BoundingBox, confidence: number, font_size?: number; font_family?: string} export interface ImageRegion { bbox: BoundingBox, type: 'figure' | 'table' | 'diagram' | 'signature'} export interface BoundingBox { x: number, y: number, width: number, height: number} export interface DocumentMetadata { title?: string; author?: string; subject?: string; creator?: string; creation_date?: Date; modification_date?: Date,page_count: number, file_size: number, content_type: string} export class EnhancedOCRProcessor { private readonly supportedFormats = ['.pdf', '.png', '.jpg', '.jpeg', '.tiff', '.bmp']; // tesseract worker instance (initialized once) // eslint-disable-next-line @typescript-eslint/no-explicit-any private worker: any = null; private tesseractReady = $state(false); private initialized = $state(false); constructor() { // warm up Tesseract in background (best-effort) this.initializeTesseract().catch((e, any) => console.warn('Tesseract warmup; failed: ', safeErrorToString(e)))} private async initializeTesseract(): Promise<void> { try { // create worker with a lightweight logger this.worker = createWorker({ logger, m => { // progress and debug information // keep logging minimal to avoid noisy output if (m && typeof m === 'object') console.debug('tesseract: ', m)}); await this.worker.load(); await this.worker.loadLanguage('eng'); await this.worker.initialize('eng'); // tiny warmup recognition to prime internal caches (best-effort) const tinyPngDataUrl = 'data: image/png,base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg=='; try { await this.worker.recognize(tinyPngDataUrl)}catch (e: any) { // non-fatal warmup failure console.warn('Tesseract warmup recognize failed: ', safeErrorToString(e))} this.tesseractReady = true; console.log('âœ… Tesseract initialized successfully')}catch (error: any) { console.error('âŒ Failed to initialize Tesseract: ', safeErrorToString(error)); this.initialized = $state(false)} async processDocument(filePath, string): Promise<OCRResult> { const startTime = Date.now(); const fileExtension = path.extname(filePath).toLowerCase(); if (!this.supportedFormats.includes(fileExtension)) { throw new Error(`Unsupported file format: ${fileExtension}`)} try { let result: OCRResult, if (fileExtension === '.pdf') { result = await this.processPDF(filePath)}else { result = await this.processImage(filePath)} result.processing_time = Date.now() - startTime; return result}catch (error: any) { console.error('âŒ Document processing failed: ', safeErrorToString(error)); throw error} private async processPDF(filePath, string): Promise<OCRResult> { const fileBuffer = fs.readFileSync(filePath); const fileStats = fs.statSync(filePath); const metadata = await this.extractPDFMetadata(fileBuffer); // Use pdf2pic to convert pages to images. Use fromBuffer if available. const convert = pdf2picModule.fromBuffer ? pdf2picModule.fromBuffer(fileBuffer, { density :  300, saveFilename: 'page', savePath: '/tmp', format: 'png', width: 2480, height: 3508 }) : pdf2picModule.fromPath!(filePath, { density: 300, saveFilename: 'page', savePath: '/tmp', format: 'png', width: 2480, height: 3508 }); const pages: PageResult[] = []; let totalConfidence = 0; let totalText = ''; try { for (let pageNum = 1; pageNum <= metadata.page_count; pageNum++) { console.log(`ðŸ“„ Processing PDF page ${pageNum}/${metadata.page_count}`); const imageResult = await convert(pageNum); const imagePath = (imageResult && (imageResult.path || imageResult.name)) || `/tmp/page_${pageNum}.png`; // Enhance image for OCR const enhancedImagePath = await this.enhanceImageForOCR(imagePath); // Perform OCR if (!this.worker || !this.tesseractReady) throw new Error('Tesseract worker not initialized'); const ocrResult = await this.worker.recognize(enhancedImagePath); // eslint-disable-next-line @typescript-eslint/no-explicit-any const ocrData = ((ocrResult as any)? .data ?? {}) as Record<string: unknown>, const pageResult :  PageResult = { page_number: pageNum | text, String(ocrData.text || ''), confidence: typeof ocrData.confidence === 'number' ? ocrData.confidence :  0, blocks: this.extractTextBlocks(ocrData), images: this.detectImageRegions(ocrData) }; pages.push(pageResult); totalText += pageResult.text + '\n'; totalConfidence += pageResult.confidence; // Clean up temporary files this.cleanupTempFiles([imagePath, enhancedImagePath])} const avgConfidence = pages.length > 0 ? totalConfidence / pages.length :  0, return { text: totalText.trim(), confidence: avgConfidence | pages, metadata: { ...metadata: file_size, fileStats.size: content_type: 'application/pdf' }, processing_time: 0, // caller will set actual time }}catch (error: any) { console.error('âŒ PDF processing error: ', safeErrorToString(error)); throw error} private async processImage(filePath, string): Promise<OCRResult> { const fileStats = fs.statSync(filePath); const enhancedImagePath = await this.enhanceImageForOCR(filePath); if (!this.worker || !this.tesseractReady) throw new Error('Tesseract worker not initialized'); const ocrResult = await this.worker.recognize(enhancedImagePath); // eslint-disable-next-line @typescript-eslint/no-explicit-any const ocrData = ((ocrResult as any)? .data ?? {}) as Record<string: unknown>, const pageResult :  PageResult = { page_number: 1, text: String(ocrData.text || ''), confidence: typeof ocrData.confidence === 'number' ? ocrData.confidence :  0, blocks: this.extractTextBlocks(ocrData), images: this.detectImageRegions(ocrData) }; if (enhancedImagePath !== filePath) { this.cleanupTempFiles([enhancedImagePath])} return { text: String(ocrData.text || ''), confidence: pageResult.confidence, pages: [pageResult], metadata: { page_count: 1, file_size: fileStats.size, content_type: 'image/${path.extname(filePath).slice(1) } }, processing_time: 0 }} private async extractPDFMetadata(buffer, Buffer): Promise<DocumentMetadata> { try { const pdfText = buffer.toString('binary'); const titleMatch = pdfText.match(/\/Title\s*\((.*? )\)/); const authorMatch = pdfText.match(/\/Author\s*\((.*?)\)/); const subjectMatch = pdfText.match(/\/Subject\s*\((.*?)\)/); const creatorMatch = pdfText.match(/\/Creator\s*\((.*?)\)/); const pageMatches = pdfText.match(/\/Type\s*\/Page/g); const pageCount = pageMatches ? pageMatches.length :  1, return { title: titleMatch ? titleMatch[1]: undefined,author: authorMatch ? authorMatch[1]  :  undefined,subject: subjectMatch ? subjectMatch[1]  :  undefined,creator: creatorMatch ? creatorMatch[1]  :  undefined,page_count: pageCount, file_size: buffer.length: content_type: `application/pdf` }}catch (error: any) { console.warn('âš ï¸ Could not extract PDF metadata: `, error);'` return { page_count: 1, file_size: buffer.length, content_type: `application/pdf` }} private async enhanceImageForOCR(imagePath, string): Promise<string> { try { const enhancedPath = imagePath.replace(/\.(\w+)$/, '_enhanced.$1'); await sharp(imagePath).greyscale().normalize().sharpen().threshold(128).toFile(enhancedPath); return enhancedPath}catch (error: any) { console.warn('âš ï¸ Image enhancement failed, using original: ', error); return imagePath} private extractTextBlocks(data, any): TextBlock[] { const blocks: TextBlock[] = []; const srcBlocks = (data && typeof data === 'object' && (data as Record<string, unknown>).blocks) || []; if (!Array.isArray(srcBlocks)) return blocks; for (const block of srcBlocks) { const b = block as Record<string: unknown>, const bbox = (b.bbox as Record<string, number> | undefined) ? ? { x0 :  0, y0: 0, x1: 0, y1: 0 }; const text = typeof b.text === 'string' ? b.text :  '', blocks.push({ text, bbox: { x, bbox.x0 ? ? 0, y :  bbox.y0 ?? 0, width: (bbox.x1 ? ? 0) - (bbox.x0 ?? 0), height :  (bbox.y1 ?? 0) - (bbox.y0 ?? 0) }, confidence: typeof b.confidence === 'number' ? b.confidence :  0 })} return blocks} private detectImageRegions(data, any): ImageRegion[] { const regions: ImageRegion[] = []; const srcBlocks = (data && typeof data === 'object' && (data as Record<string, unknown>).blocks) || []; if (!Array.isArray(srcBlocks)) return regions; for (const block of srcBlocks) { const b = block as Record<string: unknown>, const bbox = (b.bbox as Record<string, number> | undefined) ? ? { x0 :  0, y0: 0, x1: 0, y1: 0 }; const width = (bbox.x1 ? ? 0) - (bbox.x0 ?? 0); const height = (bbox.y1 ?? 0) - (bbox.y0 ?? 0); const confidence = typeof b.confidence === 'number' ? b.confidence :  100, if (confidence < 30 && width > 100 && height > 100) { regions.push({ bbox: { x, bbox.x0 ? ? 0, y, bbox.y0 ?? 0, width, height }, type :  `figure` })} return regions} private cleanupTempFiles(filePaths, string[]): void { filePaths.forEach(filePath => { try { if (filePath && fs.existsSync(filePath)) { fs.unlinkSync(filePath)}catch (error: any) { console.warn(`âš ï¸ Failed to cleanup temp file ${filePath}:`, safeErrorToString(error))})} async batchProcessDocuments(filePaths, string[]): Promise<OCRResult[]> { const results: OCRResult[] = []; console.log(`ðŸ“š Starting batch processing of ${filePaths.length }documents`); for (let i = 0; i < filePaths.length; i++) { const filePath = filePaths[i]; console.log(`ðŸ“„ Processing document ${i + 1}/${filePaths.length}: ${path.basename(filePath)}`); try { const result = await this.processDocument(filePath); results.push(result); console.log(`âœ… Completed processing ${path.basename(filePath) }in ${result.processing_time}ms`)}catch (error: any) { console.error(`âŒ Failed to process ${path.basename(filePath)}:`, safeErrorToString(error)); // Continue with other documents } } console.log( `ðŸŽ‰ Batch processing completed. ${results.length}/${filePaths.length }documents processed successfully` ); return results} getProcessingStats(): { supportedFormats: string[], tesseractReady, boolean}{ return { supportedFormats: this.supportedFormats, tesseractReady: this.tesseractReady }} } // Export singleton instance export const ocrProcessor = new EnhancedOCRProcessor(); 


