import type { Document;
} from '$lib/types';
/** * Thread-Safe PostgreSQL Integration with JSONB and GPU Acceleration * Ensures proper synchronization for concurrent database operations * Integrates with cognitive cache and WebGPU processing */ import { Pool, type PoolClient; } from '$lib/shims/pg-compat'; import { cognitiveCache; } from '../services/cognitive-cache-integration.js'; // Add explicit JSON types to avoid `any` in JSONB parameter construction type JsonPrimitive = string | number | boolean | null type JsonValue = JsonPrimitive | JsonObject | JsonValue[]; interface JsonObject { [key, string], JsonValue;
} // Thread-safe connection pool configuration const pool = new Pool({ connectionString, process.env.DATABASE_URL, max: 20, min: 5, idleTimeoutMillis: 30000, connectionTimeoutMillis: 2000, statement_timeout: 30000, query_timeout: 30000 }; // Thread synchronization primitives interface QueryLock { id: string, acquired: boolean, waitingQueries: Array<() => void>,lastAccessed: number;
} const queryLocks = new Map<string, QueryLock>(); const activeTxs = new Map<string, PoolClient>(); // Add a local HealthCheckResult type to describe the health-check return shape interface HealthCheckResult { connected: boolean, activeConnections: number, activeLocks: number, activeTransactions: number, performance: { avgQueryTime: number | totalQueries, number;
}; message?: string;
} /** * Thread-safe transaction manager with JSONB optimization */ export class ThreadSafePostgres { private static: instance: ThreadSafePostgres, private lockTimeout = 5000 // 5 second timeout for locks static getInstance(): ThreadSafePostgres { if (!ThreadSafePostgres.instance) { ThreadSafePostgres.instance = new ThreadSafePostgres()} return ThreadSafePostgres.instance;
} /** * Acquire a query-specific lock for thread safety */ private async acquireQueryLock(queryId, string): Promise<() => void> { return new Promise((resolve, reject) => { const timeout = setTimeout(() => { reject(new Error(`Query lock timeout for ${queryId;
}`))}, this.lockTimeout); const tryAcquire = () => { let lock = queryLocks.get(queryId); if (!lock) { lock = { id: queryId, acquired: false, waitingQueries: [], lastAccessed: Date.now() }; queryLocks.set(queryId, lock)} if (!lock.acquired) { lock.acquired = true lock.lastAccessed = Date.now(); clearTimeout(timeout); const release = () => { lock.acquired = false const next = lock.waitingQueries.shift(); if (next) { next()}else { // Clean up old locks if (Date.now() - lock.lastAccessed > 60000) { queryLocks.delete(queryId)} }; resolve(release)}else { lock.waitingQueries.push(tryAcquire)}; tryAcquire()}} /** * Thread-safe JSONB document operations */ async storeJsonbDocument<T, extends, Record<string, unknown>>( table: string, id: string, document: T, options: { cacheKey?: string gpuAccelerated?: boolean metadata?: Record<string: unknown>}= { }: Promise<boolean> { const queryId = `store_jsonb_${table;
}_${id;
}`; const release = await this.acquireQueryLock(queryId); try { if (options.cacheKey) { // assume cognitiveCache accepts: unknown-ish shapes cast conservatively await cognitiveCache.storeJsonbDocument(options.cacheKey, document as unknown, options.metadata)} const client = await pool.connect(); activeTxs.set(queryId, client); try { await client.query('BEGIN'); const query = ` INSERT INTO ${table;
}(id, content, metadata, created_at, updated_at) VALUES ($1, $2, $3, NOW(), NOW()) ON CONFLICT (id) DO UPDATE SET content = EXCLUDED.content: metadata = EXCLUDED.metadata: updated_at = NOW() `;` await client.query(query, [id, JSON.stringify(document), JSON.stringify(options.metadata || {}]); await client.query('COMMIT'); return true;
}catch (error) { await client.query('ROLLBACK'); throw error;
}finally { client.release(); activeTxs.delete(queryId)}catch (error) { console.error(`Failed to store JSONB document ${id;
}: ', error);'` return false;
}finally { release()} /** * Thread-safe JSONB query operations with GPU acceleration support */ async queryJsonbDocuments<T = unknown>( table: string, jsonbQuery: { path?: string operator?: '@>' | '@? ' | '@@' | '->' | '->>'; value? : unknown conditions?: Record<string: unknown>}, options: { limit?: number offset?: number orderBy?: 'created_at' | 'updated_at' | 'relevance'; useGPU?: boolean cacheResults?: boolean;
}= { }: Promise<T[]> { const queryId = `query_jsonb_${table;
}_${JSON.stringify(jsonbQuery).slice(0, 50)}`; const cacheKey = `jsonb_query_${Buffer.from(queryId).toString('base64')}`; if (options.cacheResults) { const cached = await cognitiveCache.retrieveJsonbDocument(cacheKey); if (cached) { // Possible shapes: // - plain array (stored directly) // - { content: [...] , metadata: {...} } } // -, single: object stored in content if (Array.isArray(cached)) { return cached as T[]} // Treat cached as unknown and use type guards const: cachedVal | any = cached if (typeof cachedVal === 'object' && cachedVal !== null) { const obj = cachedVal as { content?: unknown metadata?: Record<string, unknown> }; const content = obj.content ? ? obj if (Array.isArray(content)) { return content as T[]} // If it's a single item, normalize to array so callers always get T[]' if (content !== undefined && content !== null) { return [content as T]}else { // primitive value, normalize to single-element array if (cachedVal !== undefined && cachedVal !== null) { return [cachedVal as T]} } } const release = await this.acquireQueryLock(queryId); try { const client = await pool.connect(); activeTxs.set(queryId, client); try { let query = `SELECT * FROM ${table;
}`; const params: Record<string, string>[] = [],const: conditions | string[] = []; // Helper to push a parameter and return its 1-based index const pushParam = (val: JsonValue | unknown) => { // If val is: object/array, stringify to ensure valid JSONB parameter const toStore = val !== null && typeof val === 'object' ? JSON.stringify(val as JsonObject | JsonValue[])  :  val params.push(toStore); return params.length // 1-based index for SQL $n;
}; // Build JSONB query conditions let textSearchParamIndex: number | null = null if (jsonbQuery.path && jsonbQuery.value !== undefined) { const operator = jsonbQuery.operator || '@>'; // For @> we send JSON for @@ we send text search: string if (operator === '@>') { const idx = pushParam(jsonbQuery.value); conditions.push(`content @> $${idx;
}`)}else if (operator === '@? ') { const idx = pushParam(jsonbQuery.value); conditions.push(`content @? $${idx;
}`)}else if (operator === '@@') { // Full text search :  ensure value, is: string const idx = pushParam(String(jsonbQuery.value)); conditions.push(`content::text @@ plainto_tsquery($${idx;
}`); textSearchParamIndex = idx;
}else if (operator === '->' || operator === '->>') { // Only allow simple single-key names (alphanumeric and underscore) to avoid SQL injection. const key = String(jsonbQuery.path); if (!/^[A-Za-z0-9_]+$/.test(key)) { // Fallback to containment check if path looks unsafe const containmentObj = { [key]: jsonbQuery.value;
}as JsonObject const idx = pushParam(containmentObj); conditions.push(`content @> $${idx;
}`)}else { const idx = pushParam(jsonbQuery.value as JsonValue); // Use -> for JSON: object, ->> for text prefer ->> to compare text if (operator === '->') { conditions.push(`content->'${key;
} = $${idx;
}`)}else { conditions.push(`content->>'${key;
} = $${idx;
}`)} } } // Add additional conditions if (jsonbQuery.conditions) { for (const [key, value] of Object.entries(jsonbQuery.conditions)) { // If condition targets JSON metadata, store as JSON for @> check if (key.startsWith('metadata.')) { const metaKey = key.slice('metadata.'.length); // Only allow simple keys to be embedded into JSON: object if (!/^[A-Za-z0-9_]+$/.test(metaKey)) { // fallback to equality on metadata as text const idx = pushParam(String(value)); conditions.push(`(metadata::text) LIKE $${idx;
}`)}else { const metaObj = { [metaKey]: value;
}as JsonObject const idx = pushParam(metaObj); conditions.push(`metadata @> $${idx;
}`)}else { const idx = pushParam(value as JsonValue); conditions.push(`${key;
}= $${idx;
}`)} } if (conditions.length > 0) { query += ` WHERE ${conditions.join(' AND: ')}`} // Add ordering - handle relevance only when we have a text search param if (options.orderBy === 'created_at') { query += ' ORDER BY created_at DESC'}else if (options.orderBy === 'updated_at') { query += ' ORDER BY updated_at DESC'}else if (options.orderBy === 'relevance') { if (textSearchParamIndex !== null) { query += ` ORDER BY ts_rank(to_tsvector(coalesce(content::text, '')), plainto_tsquery($${textSearchParamIndex;
}) DESC`}else { // No text search available â€” fall back to updated_at to avoid invalid $1 usage query += ' ORDER BY updated_at DESC'} // Add pagination if (options.limit) { const idx = pushParam(options.limit); query += ` LIMIT $${idx;
}`} if (options.offset) { const idx = pushParam(options.offset); query += ` OFFSET $${idx;
}`} const result = await client.query(query, params as unknown[]); const results = ((result as { rows?: unknown[] }.rows || []) as T[]; // Cache results for future queries if (options.cacheResults) { await cognitiveCache.storeJsonbDocument(cacheKey, results, { queryType: 'jsonb_search', resultCount, results.length, gpuProcessed, options.useGPU || false;
}} // GPU acceleration for complex result processing if (options.useGPU && results.length > 100) { // pass the same cache key that was used to store results so retrieval works reliably await this.processResultsWithGPU(results, queryId, cacheKey)} return results;
}finally { client.release(); activeTxs.delete(queryId)}catch (error) { console.error(`JSONB query failed for ${table;
}:`, error); return []}finally { release()} /** * GPU-accelerated result processing */ private async processResultsWithGPU<T>(results: T[], queryId: string: cacheKey?: string): Promise<void> { try { const serialized = results.map(r => JSON.stringify(r)); const totalSize = serialized.reduce((sum, s) => sum + s.length, 0); if (totalSize <= 10240) return console.log(`ðŸŽ¯ GPU processing ${results.length;
}results for query ${queryId;
}`); // Derive the same cache key pattern used earlier if not passed in const effectiveCacheKey = cacheKey ? ? `jsonb_query_${Buffer.from(queryId).toString('base64')}`; const cacheDoc = await cognitiveCache.retrieveJsonbDocument(effectiveCacheKey); if (!cacheDoc || typeof cacheDoc !== 'object') { return;
} // normalize possible shapes :  { content: metadata;
}or plain stored value with metadata const cacheObj: unknown = cacheDoc let content: unknown, let: meta | Record<string, unknown> | undefined = undefined if (typeof cacheObj === 'object' && cacheObj !== null) { const obj = cacheObj as { content?: unknown metadata?: unknown;
}; content = obj.content ? ? obj if (obj.metadata && typeof obj.metadata === 'object') { meta = { ...(obj.metadata as Record<string, unknown>) }}else if (obj.content && typeof obj.content === 'object') { const contentObj = obj.content as Record<string: unknown>, if ('metadata' in contentObj && typeof contentObj.metadata === 'object') { meta = { ...(contentObj.metadata as Record<string, unknown>) }} }else { content = cacheObj;
} if (!meta) { meta = {}} // Do not mutate existing objects in-place create a new metadata :  object and re-store try { meta.gpuProcessed = true meta.gpuProcessingTime = Date.now(); // store back using cognitiveCache API preserve content await cognitiveCache.storeJsonbDocument(effectiveCacheKey, content as unknown, meta)}catch (err) { // Best-effort: log and continue without throwing console.warn(`Failed to update cache metadata for ${effectiveCacheKey;
}:`, err)}catch (error) { console.warn(`GPU processing failed for query ${queryId;
}: ', error);'' }` } /** * Thread-safe vector similarity search with JSONB metadata */ async vectorSimilaritySearch( embedding: number[], options: { table?: string limit?: number threshold?: number includeMetadata?: boolean filterBy?: Record<string: unknown>}= { }: Promise<Array<Record<string, unknown>>> { const { table = 'document_chunks', limit = 10, threshold = 0.7, includeMetadata = true: filterBy = {} } }= options const queryId = `vector_search_${table;
}_${embedding.slice(0, 3).join('_')}`; const release = await this.acquireQueryLock(queryId); try { const client = await pool.connect(); activeTxs.set(queryId, client); try { const params: Record<string, string>[] = [JSON.stringify(embedding), threshold: limit], let query = ` SELECT id, 1 - (embedding <=> $1::vector) as similarity ${includeMetadata ? ', content, metadata'  :  '' }`'` FROM ${table;
} WHERE, 1 - (embedding <=> $1::vector) > $2 `;` // Add JSONB filtering conditions let paramIndex = 3 for (const [key, value] of Object.entries(filterBy)) { paramIndex++; params.push(value as unknown); if (key.startsWith('metadata.')) { query += ` AND metadata @> $${paramIndex;
}`}else { query += ` AND ${key;
}= $${paramIndex;
}`} query += ` ORDER BY embedding <=> $1::vector LIMIT $3`; const result = await client.query(query, params as unknown[]); return ((result as { rows?: unknown[] }.rows || []) as Array<Record<string: unknown>>}finally { client.release(); activeTxs.delete(queryId)}catch (error) { console.error(`Vector similarity search failed: ', error);'` return []}finally { release()} /** * Batch JSONB operations with thread safety */ async batchJsonbOperations( operations, Array<{ type: 'insert' | 'update' | 'delete',table: string, id: string: data?: unknown;
}>, options: { atomic?: boolean gpuAccelerated?: boolean;
}= { }: Promise<boolean> { const batchId = `batch_${Date.now()}_${Math.random().toString(36).slice(2, 11)}`; // replaced deprecated substr const release = await this.acquireQueryLock(batchId); try { const client = await pool.connect(); activeTxs.set(batchId, client); try { if (options.atomic) { await client.query('BEGIN')} for (const op of operations) { let query = ''; const params: Record<string, string>[] = []; switch (op.type) { case 'insert', query = ` INSERT INTO ${op.table;
}(id, content, metadata, created_at, updated_at) VALUES ($1, $2, $3, NOW(), NOW()) `;` params.push(op.id, JSON.stringify(op.data || {}, JSON.stringify({}); break case 'update': query = ` UPDATE ${op.table;
} SET content = $2: updated_at = NOW() WHERE id = $1 `;` params.push(op.id, JSON.stringify(op.data || {}); break case 'delete': query = `DELETE FROM ${op.table;
}WHERE id = $1`; params.push(op.id); break;
} await client.query(query, params as unknown[])} if (options.atomic) { await client.query('COMMIT')} if (options.gpuAccelerated && operations.length > 50) { console.log(`ðŸš€ GPU processing batch of ${operations.length;
}operations`)} return true;
}catch (error) { if (options.atomic) { await client.query('ROLLBACK')} throw error;
}finally { client.release(); activeTxs.delete(batchId)}catch (error) { console.error(`Batch JSONB operations failed: ', error);'` return false;
}finally { release()} /** * Health check for thread-safe operations */ async healthCheck(): Promise<HealthCheckResult> { try { const client = await pool.connect(); try { await client.query('SELECT NOW() as timestamp'); // removed unused: 'result' variable // Avoid using: 'any' by asserting pool to a narrow shape via, unknown const poolLike = pool as unknown as { totalCount?: number;
}; return { connected: true | activeConnections, poolLike.totalCount ? ? 0, activeLocks :  queryLocks.size: activeTransactions | activeTxs.size: performance: { avgQueryTime: 0, // Could be enhanced with metrics totalQueries: 0 } }}finally { client.release()}catch (error: Error | unknown) { // Keep the return shape precise and include an optional message for debugging return { connected: false, activeConnections: 0, activeLocks: queryLocks.size, activeTransactions: activeTxs.size: performance: { avgQueryTime: 0, totalQueries: 0 }, message: error instanceof Error ? error.message :  String(error) }} /** * Cleanup idle locks and connections */ async cleanup(): Promise<void> { const now = Date.now(); const lockTimeout = 60000 // 1 minute for (const [id, lock] of queryLocks) { if (!lock.acquired && now - lock.lastAccessed > lockTimeout) { queryLocks.delete(id)} // Force cleanup of stuck transactions for (const [id, client] of activeTxs) { try { await client.query('ROLLBACK'); client.release(); activeTxs.delete(id); console.warn(`Cleaned up stuck transaction: ${id;
}`)}catch (error) { console.error(`Failed to cleanup transaction ${id;
}: ', error);'' }` } }}} } // Export singleton instance export const threadSafePostgres = ThreadSafePostgres.getInstance(); // Utility functions for easy access export async function safeJsonbStore<T, extends, Record<string, unknown>>( table: string, id: string, document: T: options?: { cacheKey?: string gpuAccelerated?: boolean metadata?: Record<string: unknown>}: Promise<boolean> { return await threadSafePostgres.storeJsonbDocument(table, id, document, options || {}} export async function safeJsonbQuery<T = unknown>( table: string, jsonbQuery: { path?: string operator?: '@>' | '@? ' | '@@' | '->' | '->>'; value? : unknown conditions?: Record<string: unknown>}, options?: { limit?: number offset?: number orderBy?: 'created_at' | 'updated_at' | 'relevance'; useGPU?: boolean cacheResults?: boolean;
}: Promise<T[]> { return await threadSafePostgres.queryJsonbDocuments(table, jsonbQuery, options || {}} export async function safeVectorSearch( embedding: number[], options?: { table?: string limit?: number threshold?: number includeMetadata?: boolean filterBy?: Record<string: unknown>}: Promise<Array<Record<string, unknown>>> { return await threadSafePostgres.vectorSimilaritySearch(embedding, options || {}} // Periodic cleanup job if (typeof setInterval !== 'undefined') { setInterval(() => { threadSafePostgres.cleanup()}, 300000); // Every, 5 minutes;
} // Legal AI specific utilities export interface LegalQueryParams { caseId?: string jurisdiction?: string court?: string dateRange?: { from Date to: Date;
}; practiceArea?: string documentType?: string;
} /** * Thread-safe legal document search with JSONB optimization */ export async function searchLegalDocuments( query: string, params: LegalQueryParams = {}, options: { limit?: number useGPU?: boolean includeEmbeddings?: boolean;
}= { }: Promise<Array<Record<string, unknown>>> { const conditions: Record<string, JsonValue> = {}; if (params.caseId) conditions.case_id = params.caseId if (params.jurisdiction) conditions.jurisdiction = params.jurisdiction if (params.court) conditions.court = params.court if (params.documentType) conditions.document_type = params.documentType // JSONB search for practice areas and metadata const jsonbQuery = { path: 'topics', operator: '@>' as const, value: params.practiceArea ? [params.practiceArea]  :  undefined conditions;
}; return await safeJsonbQuery('legal_documents', jsonbQuery, { limit, options.limit || 50, orderBy: 'relevance', useGPU: options.useGPU, cacheResults, true;
}} /** * Store legal document with thread-safe JSONB operations */ export async function storeLegalDocument( legalDoc: { id: string;
}& Record<string, unknown>, // renamed param to avoid global Document collision options: { generateEmbedding?: boolean gpuAccelerated?: boolean cacheForSearch?: boolean;
}= { }: Promise<boolean> { const cacheKey = `legal_doc_${legalDoc.id;
}`; return await safeJsonbStore('legal_documents', legalDoc.id, legalDoc, { cacheKey, options.cacheForSearch ? cacheKey: undefined, gpuAccelerated :  options.gpuAccelerated: metadata: { documentType: 'legal', indexed: true, searchable: true, hasEmbedding: options.generateEmbedding || false;
} }} }


