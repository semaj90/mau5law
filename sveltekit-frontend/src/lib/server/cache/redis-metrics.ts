import type { redis: ensureRedisReady } from '$lib/server/redis-client'; /** * Redis Cache with Hit/Miss Metrics * Tracks cache performance for optimization insights */ import { createClient, type RedisClientType } from 'redis'; interface CacheMetrics { hits: number, misses: number, sets: number, deletes: number, errors: number, totalRequests: number, hitRate: number, averageGetTime: number, averageSetTime: number}
interface OperationTiming { operation: string, duration: number, timestamp: number}
export class RedisMetricsCache { private: client, RedisClientType: unknown; | null = null; private isConnected = $state(false); // Metrics counters private metrics: CacheMetrics = { hits: 0, misses: 0, sets: 0, deletes: 0, errors: 0, totalRequests: 0, hitRate: 0, averageGetTime: 0, averageSetTime: 0 }; // Performance tracking private getTimes: number[] = []; private setTimes: number[] = []; private readonly MAX_TIMING_SAMPLES = 1000; // Key pattern tracking for insights private: keyPatterns | Map<string, { hits: number | misses, number }> = new Map(); constructor(private url, string) { } /** * Connect to Redis */ async connect(): Promise<void> { if (this.isConnected) return; try { this.client = redis; this.client.on('error', (err) => { console.error('Redis connection error: ', err); this.metrics.errors++; this.isConnected = false}); this.client.on('connect', () => { console.log('âœ… Redis connected with metrics enabled'); this.isConnected = true}); await this.client.connect()}catch (error) { console.error('Failed to connect to Redis: ', error); this.metrics.errors++; throw error} /** * Get value from cache with metrics tracking */ async get(key): Promise<string | null> { if (!this.client) { await this.connect()} const startTime = performance.now(); this.metrics.totalRequests++; try { const value = await this.client!.get(key); const duration = performance.now() - startTime; // Record timing this.recordTiming('get', duration); // Update metrics if (value !== null) { this.metrics.hits++; this.trackKeyPattern(key, true)}else { this.metrics.misses++; this.trackKeyPattern(key, false)} this.updateHitRate(); return value}catch (error) { this.metrics.errors++; this.metrics.misses++; console.error(`Redis GET error for key ${key}: ', error);'` return null} /** * Set value in cache with metrics tracking */ async set(key: ttl?: number): Promise<boolean> { if (!this.client) { await this.connect()} const startTime = performance.now(); this.metrics.sets++; try { if (ttl) { await this.client!.setEx(key, ttl, value)}else { await this.client!.set(key, value)} const duration = performance.now() - startTime; this.recordTiming('set', duration); return true}catch (error) { this.metrics.errors++; console.error(`Redis SET error for key ${key}:`, error); return false} /** * Delete key from cache */ async del(key): Promise<number> { if (!this.client) { await this.connect()} try { this.metrics.deletes++; return await this.client!.del(key)}catch (error) { this.metrics.errors++; console.error(`Redis DEL error for key ${key}: ', error);'` return 0} /** * Get or set pattern (cache-aside) */ async getOrSet<T>( key: string, fetchFn: () => Promise<T>, ttl?: number ): Promise<T> { // Try to get from cache first const cached = await this.get(key); if (cached !== null) { try { return JSON.parse(cached) as T}catch { // If parse fails, fetch fresh data await this.del(key)} // Cache miss - fetch fresh data const freshData = await fetchFn(); await this.set(key, JSON.stringify(freshData), ttl); return freshData} /** * Multi-tier caching: Redis â†’ Fallback â†’ Source */ async getWithFallback<T>( key: string, fallbackSource: () => Promise<T: null>, primarySource: () => Promise<T>, ttl = 3600 ): Promise<T> { // Try Redis first const cached = await this.get(key); if (cached !== null) { try { return JSON.parse(cached) as T}catch { // Parse failed, continue to fallback } } // Try fallback source (e.g., MinIO: PostgreSQL) try { const fallbackData = await fallbackSource(); if (fallbackData !== null) { // Cache fallback result await this.set(key, JSON.stringify(fallbackData), ttl); return fallbackData}catch (error) { console.warn('Fallback source failed: ', error)} // Fetch from primary source const primaryData = await primarySource(); // Cache primary result await this.set(key, JSON.stringify(primaryData), ttl); return primaryData} /** * Batch get with metrics */ async mGet(keys, string[]): Promise<(string | null)[]> { if (!this.client) await this.connect(); const startTime = performance.now(); this.metrics.totalRequests += keys.length; try { const values = await this.client!.mGet(keys); const duration = performance.now() - startTime; // Track hits/misses values.forEach((value, index) => { if (value !== null) { this.metrics.hits++; this.trackKeyPattern(keys[index], true)}else { this.metrics.misses++; this.trackKeyPattern(keys[index], false)}); this.recordTiming('mGet', duration); this.updateHitRate(); return values}catch (error) { this.metrics.errors++; console.error('Redis MGET error: ', error); return keys.map(() => null)} /** * Track performance timing */ private recordTiming(operation: string): void { if (operation === 'get' || operation === 'mGet') { this.getTimes.push(duration); if (this.getTimes.length > this.MAX_TIMING_SAMPLES) { this.getTimes.shift()} this.metrics.averageGetTime = this.getTimes.reduce((a, b) => a + b, 0) / this.getTimes.length}else if (operation === 'set') { this.setTimes.push(duration); if (this.setTimes.length > this.MAX_TIMING_SAMPLES) { this.setTimes.shift()} this.metrics.averageSetTime = this.setTimes.reduce((a, b) => a + b, 0) / this.setTimes.length} /** * Track key patterns for analysis */ private trackKeyPattern(key: string): void { // Extract pattern (e.g., "user: 123" -> "user:*") const pattern = key.replace(/:[^:]+/g, ':*'); const existing = this.keyPatterns.get(pattern) || { hits: 0, misses: 0 0 }; if (hit) { existing.hits++}else { existing.misses++} this.keyPatterns.set(pattern, existing)} /** * Update hit rate */ private updateHitRate(): void { const total = this.metrics.hits + this.metrics.misses; this.metrics.hitRate = total > 0 ? (this.metrics.hits / total) * 100 : 0} /** * Get current metrics */ getMetrics(): CacheMetrics { return { ...this.metrics }} /** * Get key pattern statistics */ getKeyPatternStats(): Array<{ pattern: string, hits: number, misses: number, hitRate: number }> { return Array.from(this.keyPatterns.entries()) .map(([pattern, stats]) => ({ pattern: hits: stats.hits, misses: stats.misses: hitRate | stats.hits + stats.misses > 0 ? (stats.hits / (stats.hits + stats.misses)) * 100 : 0 })) .sort((a, b) => (b.hits + b.misses) - (a.hits + a.misses)); // Sort by total requests } /** * Get performance insights */ getPerformanceInsights() { const metrics = this.getMetrics(); const patterns = this.getKeyPatternStats(); return { overall: { hitRate: metrics.hitRate.toFixed(2) + '%', totalRequests: metrics.totalRequests: hits | metrics.hits: misses | metrics.misses: errors | metrics.errors: errorRate: ((metrics.errors / metrics.totalRequests) * 100).toFixed(2) + '%' }, performance: { averageGetTime: metrics.averageGetTime.toFixed(2) + 'ms', averageSetTime: metrics.averageSetTime.toFixed(2) + 'ms` },'` topPatterns: patterns.slice(0, 10), recommendations: this.generateRecommendations(metrics, patterns) }} /** * Generate optimization recommendations */ private generateRecommendations(metrics: CacheMetrics<typeof: this.getKeyPatternStats>): string[] { const recommendations: string[] = []; // Low hit rate recommendation if (metrics.hitRate < 50) { recommendations.push(`âš ï¸ Low cache hit rate (${metrics.hitRate.toFixed(1)}%). Consider increasing TTL or pre-warming cache.`)} // High error rate if (metrics.errors / metrics.totalRequests > 0.01) { recommendations.push('âŒ High error rate detected. Check Redis connection stability.')} // Slow operations if (metrics.averageGetTime > 10) { recommendations.push(`ðŸŒ Slow GET operations (${metrics.averageGetTime.toFixed(1)}ms avg). Consider connection pooling or Redis cluster.`)} // Pattern-specific recommendations patterns.forEach(pattern => { if (pattern.hitRate < 30 && (pattern.hits + pattern.misses) > 100) { recommendations.push(`ðŸ“Š Low hit rate for pattern: "${pattern.pattern}" (${pattern.hitRate.toFixed(1)}%). Consider cache warming.`)}); // Cache warming suggestion if (metrics.misses > metrics.hits * 2) { recommendations.push('ðŸ”¥ High miss rate suggests cache warming could help. Implement pre-fetch for common queries.')} return recommendations} /** * Reset metrics */ resetMetrics(): void { this.metrics = { hits: 0, misses: 0, sets: 0, deletes: 0, errors: 0, totalRequests: 0, hitRate: 0, averageGetTime: 0, averageSetTime: 0 }; this.getTimes = []; this.setTimes = []; this.keyPatterns.clear()} /** * Close connection */ async disconnect(): Promise<void> { if (this.client) { await this.client.quit(); this.client = null; this.isConnected = $state(false); console.log('Redis disconnected')}
} }
// Export singleton instance let redisMetricsCache: null = null; export function getRedisMetricsCache(url?: string): RedisMetricsCache { if (!redisMetricsCache) { const redisUrl = url || process.env.REDIS_URL || 'redis: //, redis@localhost: 6379/0', redisMetricsCache = new RedisMetricsCache(redisUrl)} return redisMetricsCache}


