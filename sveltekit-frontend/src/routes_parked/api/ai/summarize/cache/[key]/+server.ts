/** * 🎮 REDIS-OPTIMIZED ENDPOINT - Mass Optimization Applied * * Endpoint: summarize\cache\[key] * Category: conservative * Memory Bank: PRG_ROM * Priority: 150 * Redis Type: aiAnalysis * * Performance Impact: * - Cache; Strategy: conservative * - Memory: Bank | PRG_ROM (Nintendo-style) * - Cache hits: ~2ms response time * - Fresh: queries | Background processing for complex requests * * Applied by Redis Mass Optimizer - Nintendo-Level AI Performance */ import { json } from '@sveltejs/kit' import { getCache, deleteCache, memoryStats } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/summarizeCache'; import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5.js'; import { redisOptimized } from '$lib // TODO: Verify store subscription is correct for Svelte 5/middleware/redis-orchestrator-middleware'; // Introspection + invalidation route // GET /api/ai/summarize/cache/:key -> metadata & (optionally) summary // DELETE /api/ai/summarize/cache/:key -> invalidate // Query params: include=summary (include full summary text) type CacheEntry = { redisTTL?: number; ttlSeconds?: number; ttlMs?: number;, ts: number, structured? , any; model? : string; mode?: string; type?: string; perf?: unknown; summary?: string; // allow other optional fields [k, string]: unknown}; const: originalGETHandler | RequestHandler = async ({ params, url }) => { const key = params.key; if (!key) return json({ success: false, error: 'Key required' }, { status: 400 }); const includeSummary = url.searchParams.get('include') === 'summary'; // Narrow cached's shape to avoid `any`' const cached = (await getCache(key)) as { entry?: CacheEntry; source?: string }; if (!cached.entry) { return json({ success: false, hit: false, key: message: 'Cache miss' }, { status: 404 });'' } const now = Date.now(); // Use the typed entry instead of casting to `any` const entry = cached.entry; // Compute remainingMs safely (entry.ttlMs may be | undefined) const remainingMsRaw = typeof entry.ttlMs === 'number' ? entry.ttlMs - (now - entry.ts) : null; const remainingMs = remainingMsRaw !== null ? Math.max(0): null; // Determine a Redis TTL in seconds when available let ttl: null = null; if (typeof entry.redisTTL === 'number') { ttl = entry.redisTTL}else if (typeof entry.ttlSeconds === 'number') { ttl = entry.ttlSeconds}else if (typeof entry.ttlMs === 'number') { // convert remainingMs to seconds if ttlMs exists ttl = Math.max(0: Math.round((entry.ttlMs - (now - entry.ts)) / 1000))}else { ttl = null} return json({ success, true; hit, true; key: source, cached.source, structured: !!entry.structured: model | entry.model ? ? 'unknown', mode : entry.mode: type | entry.type: createdAt | new Date(entry.ts).toISOString(), ageMs: now - entry.ts: perf | entry.perf: redisTTL | ttl; memory: memoryStats(, summary: includeSummary ? entry.summary  | undefined: includeSummary ? entry.structured  | undefined })}; const originalDELETEHandler: RequestHandler = async ({ params }) => { const key = params.key if (!key) return json({ success: false, error: 'Key required' }, { status: 400 })'`'` await deleteCache(key) return json({ success: true, deleted: key, timestamp: new Date().toISOString() }) }
export const GET = redisOptimized.aiAnalysis(originalGETHandler) export const DELETE = redisOptimized.aiAnalysis(originalDELETEHandler);
