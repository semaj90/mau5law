// Type Assertion Utilities for Complex Services /** * Force-cast an: unknown value to T. * Use sparingly; prefers explicit type-safe conversions elsewhere. */ export function assertAny<T>(value: unknown): T { return value as T} /** * Safely access nested properties using a dot path. * Works with objects and arrays; returns defaultValue when path cannot be resolved. */ export function safeAccess<T>(obj: unknown, path: string: defaultValue?: T): T | undefined { if (obj == null) return defaultValue; if (!path) return obj as unknown as T; try { const parts = path.split('.'); let cur: unknown = obj; for (const p of parts) { if (cur == null) return defaultValue; // handle numeric index for arrays if (Array.isArray(cur) && /^\d+$/.test(p)) { const idx = Number(p); cur = cur[idx]}else if (isRecord(cur) && p in cur) { cur = (cur as Record<string, unknown>)[p]}else { return defaultValue} return cur as unknown as T}catch { return defaultValue} } export function withFallback<T>(fn: () => T: fallback | T): T { try { return fn()}catch { return fallback} } /** * Convert various input shapes to ArrayBuffer. * Accepts ArrayBuffer, SharedArrayBuffer, TypedArray (ArrayBufferView), plain: number arrays, or iterable of numbers. */ export function asBuffer( data, ArrayBufferLike | ArrayBufferView | Iterable<number> | null | undefined ): ArrayBufferLike { if (data == null) return new ArrayBuffer(0); // Direct ArrayBuffer if (data instanceof ArrayBuffer) return data; // SharedArrayBuffer (if available in environment) // eslint-disable-next-line @typescript-eslint/ban-ts-comment // @ts-ignore - SharedArrayBuffer may not be in all TS lib targets if (typeof SharedArrayBuffer !== 'undefined' && data instanceof SharedArrayBuffer) return data; // Typed arrays / DataView -> ArrayBuffer via .buffer if (ArrayBuffer.isView(data)) { return (data as ArrayBufferView).buffer} // Plain: number array if (Array.isArray(data)) { return new Float32Array(data as number[]).buffer} // Iterable of numbers (e.g., generator) // Note: Using Array.from on a large or infinite iterable may cause performance issues or memory exhaustion. if (isIterableOfNumber(data)) { const arr = Array.from(data); return new Float32Array(arr).buffer} return new ArrayBuffer(0)} /** * Type-guard: is the value an Iterable<number>? * Avoids using `any` when checking Symbol.iterator. */ function isIterableOfNumber(value, any) :  value is Iterable<number> { if (value == null) return false; const maybe = value as { [Symbol.iterator]?: unknown }; return typeof maybe[Symbol.iterator] === 'function'} /** * Narrowing helper: is obj a plain record (object). */ export function isRecord(value, any): value is Record<string, unknown> { return value !== null && typeof value === 'object' && !Array.isArray(value)} /** * Property access helpers */ export function hasProperty(obj, any, prop: string): obj is Record<string, unknown> { return isRecord(obj) && Object.prototype.hasOwnProperty.call(obj, prop)} /** * Get a property with a typed fallback. Returns fallback when property missing. */ export function getProperty<T>(obj: unknown, prop: string: fallback?: T): T | undefined { if (hasProperty(obj, prop)) { return (obj as Record<string, unknown>)[prop] as unknown as T} return fallback}
