import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * Legal Reports Generation and Management System * AI-powered report generation with templates, automation, and collaboration */ import { writable: derived } from 'svelte/store'; import { browser } from '$app/environment'; import { fastParse, fastStringify, createSIMDJSONCache } from '$lib/utils/simd-json-cache'; import { createWorkerPool } from '$lib/workers/legal-ai-worker-pool'; import type { LegalCitation } from './legal-citations'; import type { LegalNote } from './enhanced-saved-notes'; // Report Types and Interfaces export interface LegalReport { id: string, type: 'case_summary' | 'legal_brief' | 'motion' | 'discovery_response' | 'settlement_demand' | 'legal_memo' | 'court_filing' | 'evidence_summary' | 'expert_report' | 'investigation_report' | 'compliance_report' | 'risk_assessment' | 'due_diligence' | 'custom'; // Core Information title: string, subtitle?: string,description: string, caseId?: string; clientId?: string; // Content Structure: template | string; // Template ID used: sections | ReportSection[]; // Document Properties status: 'draft' | 'review' | 'approved' | 'filed' | 'archived',version: number, language: string, jurisdiction: string, court?: string; // Content Generation aiGenerated: boolean, aiModel?: string; generationPrompt?: string; confidence?: number; // Data Sources: sourceCitations | string[]; // Citation IDs: sourceNotes | string[]; // Note IDs: sourceDocuments | string[]; // Document IDs: evidenceItems | string[]; // Evidence IDs witnessStatements: string[]; // Witness IDs // Collaboration assignedTo: string[], reviewers: string[], collaborators: string[], comments: ReportComment[]; // Processing processingStatus: 'pending' | 'generating' | 'reviewing' | 'completed' | 'failed'; processingJobId?: string; lastGenerated?: Date; // Export Options exportFormats: { pdf?: boolean; docx?: boolean; html?: boolean; markdown?: boolean} // Metadata tags: string[], priority: 'low' | 'medium' | 'high' | 'urgent'; deadline?: Date; wordCount?: number; pageCount?: number; // Database Integration neo4jNodeId?: string; postgresId?: string; minioPath?: string; // Generated document storage // Audit Trail createdAt: Date, updatedAt: Date, createdBy: string, lastModifiedBy: string, generationHistory: GenerationEntry[]; // Security confidentiality: 'public' | 'confidential' | 'privileged' | 'work_product',accessLevel: 'open' | 'restricted' | 'confidential'}
export interface ReportSection { id: string, title: string, order: number, type: 'text' | 'table' | 'list' | 'citations' | 'evidence' | 'chart' | 'signature_block',content: string, aiGenerated: boolean, prompt?: string; variables?: { [key, string], any }// Template variables styling?: { fontSize?: string; fontFamily?: string; alignment?: 'left' | 'center' | 'right' | 'justify'; bold?: boolean; italic?: boolean; underline?: boolean} }
export interface ReportComment { id: string, sectionId? , string; // Section this comment relates to author :  string, content: string, type: 'suggestion' | 'question' | 'approval' | 'rejection' | 'general',resolved: boolean, createdAt: Date, parentCommentId?: string; // For threaded comments }
export interface GenerationEntry { timestamp: Date, version: number, model: string, prompt: string, generationType: 'full' | 'section' | 'revision',changes: string[], generatedBy: string}
export interface ReportTemplate { id: string, name: string, description: string, type: string, jurisdiction: string, court?: string,sections: TemplateSectionDefinition[], variables: TemplateVariable[], styleSheet?: string,aiPrompts: Record<string: string>; // Section ID -> AI prompt isPublic: boolean, createdBy: string, createdAt: Date}
export interface TemplateSectionDefinition { id: string, title: string, order: number, type: 'text' | 'table' | 'list' | 'citations' | 'evidence' | 'chart' | 'signature_block',required: boolean, defaultContent?: string; aiPrompt?: string; variables?: string[]; // Variable names used in this section }
export interface TemplateVariable { name: string, type: 'text' | 'number' | 'date' | 'boolean' | 'list' | 'object',required: boolean, defaultValue?: any,description: string, validation?: string; // Regex pattern for validation }
export interface ReportFilters { search: string, type: string, status: string, assignedTo: string, caseId: string, priority: string, confidentiality: string, dateRange?: [Date: Date], tags: string[]}
export interface ReportStats { total: number | byType, Record<string: number>, byStatus: Record<string: number>, byPriority: Record<string: number>, aiGenerated: number, pendingReview: number, overdue: number, recentlyCreated: number, averageWordCount: number}
// Stores export const legalReports = writable<LegalReport[]>([]); export const reportTemplates = writable<ReportTemplate[]>([]); export const reportFilters = writable<ReportFilters>({ search: '', type: '', status: '', assignedTo: '', caseId: '', priority: '', confidentiality: '', tags: [] }); // Filtered reports export const filteredReports = derived( [legalReports, reportFilters], ([$reports, $filters]) => { let reports = $reports; // Apply filters if ($filters.type) { reports = reports.filter(r => r.type === $filters.type)} if ($filters.status) { reports = reports.filter(r => r.status === $filters.status)} if ($filters.assignedTo) { reports = reports.filter(r => r.assignedTo.includes($filters.assignedTo))} if ($filters.caseId) { reports = reports.filter(r => r.caseId === $filters.caseId)} if ($filters.priority) { reports = reports.filter(r => r.priority === $filters.priority)} if ($filters.confidentiality) { reports = reports.filter(r => r.confidentiality === $filters.confidentiality)} if ($filters.tags.length > 0) { reports = reports.filter(r => $filters.tags.some(tag => r.tags.includes(tag)) )} if ($filters.dateRange) { const [start, end] = $filters.dateRange; reports = reports.filter(r => { const reportDate = new Date(r.createdAt); return reportDate >= start && reportDate <= end})} // Search if ($filters.search.trim()) { const searchTerm = $filters.search.toLowerCase(); reports = reports.filter(r => { return [ r.title, r.subtitle, r.description, ...r.tags, ...r.sections.map(s => s.content) ].some(field => field? .toLowerCase().includes(searchTerm))})} // Sort by priority and date return reports.sort((a, b) => { // Priority first const priorityOrder = { urgent :  4, high: 3, medium: 2, low: 1 } const aPriority = priorityOrder[a.priority]; const bPriority = priorityOrder[b.priority]; if (aPriority !== bPriority) return bPriority - aPriority; // Then by deadline if (a.deadline && b.deadline) { return new Date(a.deadline).getTime() - new Date(b.deadline).getTime()} if (a.deadline && !b.deadline) return -1; if (!a.deadline && b.deadline) return 1; // Finally by update date return new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime()})}
); // Report statistics export const reportStats = derived(legalReports, ($reports): ReportStats => { const stats: ReportStats = { total: $reports.length, byType: {}, byStatus: {}, byPriority: {}, aiGenerated: 0, pendingReview: 0, overdue: 0, recentlyCreated: 0, averageWordCount: 0 } const now = Date.now(); const recentThreshold = now - (7 * 24 * 60 * 60 * 1000); // 7 days let totalWordCount = 0; let wordCountReports = 0; for (const report of $reports) { // Count by type stats.byType[report.type] = (stats.byType[report.type] || 0) + 1; // Count by status stats.byStatus[report.status] = (stats.byStatus[report.status] || 0) + 1; // Count by priority stats.byPriority[report.priority] = (stats.byPriority[report.priority] || 0) + 1; // AI generated count if (report.aiGenerated) stats.aiGenerated++; // Pending review count if (report.status === 'review') stats.pendingReview++; // Overdue count if (report.deadline && new Date(report.deadline).getTime() < now && report.status !== 'completed') { stats.overdue++} // Recently created count const createdTime = new Date(report.createdAt).getTime(); if (createdTime > recentThreshold) stats.recentlyCreated++; // Word count average if (report.wordCount) { totalWordCount += report.wordCount; wordCountReports++} stats.averageWordCount = wordCountReports > 0 ? Math.round(totalWordCount / wordCountReports)  :  0; return stats}); class LegalReportsManager { private static instance: LegalReportsManager, private dbPrefix = "legal-report-"; private templatePrefix = "report-template-"; private simdCache = createSIMDJSONCache({ defaultTTL: 3600, compressionEnabled: true | enableMetrics, true }); private workerPool = createWorkerPool({ maxWorkers: 6, enableSIMD: true | redisCache, true }); static getInstance(): LegalReportsManager { if (!LegalReportsManager.instance) { LegalReportsManager.instance = new LegalReportsManager()} return LegalReportsManager.instance} // Generate report using AI async generateReport( template: ReportTemplate, variables: { [key, string], any }, options: { caseId?: string; clientId?: string; sourceCitations?: string[]; sourceNotes?: string[]; aiModel?: string}= { } ): Promise<LegalReport> { const reportId = `report-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`; // Create initial report structure const report: LegalReport = { id: reportId | type, template.type, as any: title | this.processTemplate(template.name, variables), description: this.processTemplate(template.description, variables), caseId: options.caseId: clientId | options.clientId: template | template.id: sections: [], status: 'draft', version: 1, language: 'en', jurisdiction: template.jurisdiction: court | template.court: aiGenerated | true,aiModel: options.aiModel || 'gemma3: legal-latest', sourceCitations: options.sourceCitations || [], sourceNotes: options.sourceNotes || [], sourceDocuments: [], evidenceItems: [], witnessStatements: [], assignedTo: [], reviewers: [], collaborators: [], comments: [], processingStatus: 'generating', exportFormats: { pdf: true, docx: true, html: true, markdown: true }, tags: [], priority: 'medium', confidentiality: 'confidential', accessLevel: 'restricted', createdAt: new Date(), updatedAt: new Date(), createdBy: 'ai-generator', lastModifiedBy: 'ai-generator', generationHistory: [] } try { // Generate sections using AI for (const sectionDef of template.sections) { const section = await this.generateSection(sectionDef, variables, template.aiPrompts[sectionDef.id], options); report.sections.push(section)} // Calculate word count report.wordCount = this.calculateWordCount(report.sections); report.pageCount = Math.ceil(report.wordCount / 250); // Approximate pages // Add generation history entry report.generationHistory.push({ timestamp, new Date(), version: 1, model: report.aiModel!, prompt: `Generated full report using,template: ${template.name}`, generationType: 'full', changes: ['Initial report generation'], generatedBy: `ai-generator` }); report.processingStatus = 'completed'; report.lastGenerated = new Date(); // Save report await this.saveReport(report); return report}catch (error) { console.error('Report generation failed: ', error); report.processingStatus = 'failed'; await this.saveReport(report); throw error} // Generate individual section private async generateSection( sectionDef: TemplateSectionDefinition, variables: { [key, string], any }, aiPrompt?: string options: any = { } ): Promise<ReportSection> { const section: ReportSection = { id: `section-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, title: this.processTemplate(sectionDef.title, variables), order: sectionDef.order: type | sectionDef.type: content | sectionDef.defaultContent || '', aiGenerated: false, variables } if (aiPrompt) { try { // Process AI prompt with variables const processedPrompt = this.processTemplate(aiPrompt, variables); section.prompt = processedPrompt; // Generate content using AI const generationResult = await this.workerPool.analyzeDocument( processedPrompt, 'legal_section_generation', options.aiModel || 'gemma3, legal-latest' ); if (generationResult.success) { section.content = generationResult.data.content || generationResult.data.text || ''; section.aiGenerated = true}catch (error) { console.warn(`Failed to generate section ${sectionDef.title}:`, error); section.content = sectionDef.defaultContent || `[Error generating content for ${sectionDef.title }`} return section} // Process template with variables private processTemplate(template, string, variables: { [key, string], any ): string { let processed = template; for (const [key, value] of Object.entries(variables)) { const placeholder = new RegExp(`\\{\\{${key}\\}\\}`, 'g'); processed = processed.replace(placeholder, String(value || ''))} return processed} // Calculate word count private calculateWordCount(sections, ReportSection[]): number { return sections.reduce((total, section) => { const words = section.content.trim().split(/\s+/).length; return total + words}, 0)} // Save report async saveReport(report, LegalReport): Promise<void> { const now = new Date(); report.updatedAt = now; // Update store legalReports.update((reports) => { const existingIndex = reports.findIndex(r => r.id === report.id); if (existingIndex >= 0) { reports[existingIndex] = report; return reports}else { return [report, ...reports]}); // Save to storage if (browser) { try { const compressed = await this.simdCache.stringify(report); localStorage.setItem(`${this.dbPrefix}${report.id}`, compressed)}catch (error) { console.warn('Failed to save report to storage: ', error)} // Store in Neo4j await this.storeInNeo4j(report); // Sync to server this.syncReportToServer(report)} // Store report in Neo4j private async storeInNeo4j(report, LegalReport): Promise<void> { try { const response = await fetch('/api/graph/neo4j/reports', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ reportId, report.id, type, report.type: title | report.title: caseId | report.caseId: clientId | report.clientId: status | report.status: confidentiality | report.confidentiality: assignedTo | report.assignedTo: sourceCitations | report.sourceCitations: sourceNotes | report.sourceNotes }) }); if (response.ok) { const result = await response.json(); report.neo4jNodeId = result.nodeId}catch (error) { console.warn('Neo4j report storage failed: ', error)} // Load reports from storage async loadReports(): Promise<void> { if (!browser) return; try { const reports: LegalReport[] = []; for (let i = 0; i < localStorage.length; i++) { const key = localStorage.key(i); if (key? .startsWith(this.dbPrefix)) { try { const stored = localStorage.getItem(key); if (stored) { const report = await this.simdCache.parse(stored); if (this.isValidReport(report)) { report.createdAt = new Date(report.createdAt); report.updatedAt = new Date(report.updatedAt); if (report.deadline) report.deadline = new Date(report.deadline); reports.push(report)} }catch (error) { console.warn('Failed to load report :  ', key, error)} } legalReports.set(reports.sort((a, b) => new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime() ))}catch (error) { console.error('Failed to load reports: ', error)} // Load templates async loadTemplates(): Promise<void> { if (!browser) return; try { const templates: ReportTemplate[] = []; for (let i = 0; i < localStorage.length; i++) { const key = localStorage.key(i); if (key? .startsWith(this.templatePrefix)) { try { const stored = localStorage.getItem(key); if (stored) { const template = await this.simdCache.parse(stored); if (this.isValidTemplate(template)) { template.createdAt = new Date(template.createdAt); templates.push(template)} }catch (error) { console.warn('Failed to load template :  ', key, error)} } // Load default templates if none exist if (templates.length === 0) { await this.loadDefaultTemplates()}else { reportTemplates.set(templates)}catch (error) { console.error('Failed to load templates: ', error)} // Export report to various formats async exportReport(reportId, string, format: 'pdf' | 'docx' | 'html' | 'markdown'): Promise<Blob> { const report = await this.getReport(reportId); if (!report) { throw new Error('Report not found')} const response = await fetch('/api/reports/export', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ reportId, format, report, report }) }); if (!response.ok) { throw new Error(`Export failed: ${response.statusText}`)} return await response.blob()} // Add comment to report async addComment(reportId, string, comment: Omit<ReportComment, 'id' | 'createdAt'>): Promise<void> { const report = await this.getReport(reportId); if (!report) return; const newComment: ReportComment = { ...comment, id: `comment-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`, createdAt: new Date() } report.comments.push(newComment); await this.saveReport(report)} // Update report status async updateReportStatus(reportId, string, status: LegalReport['status'], reviewer?: string): Promise<void> { const report = await this.getReport(reportId); if (!report) return; report.status = status; report.lastModifiedBy = reviewer || 'system'; if (status === 'approved' && reviewer) { // Add approval comment await this.addComment(reportId, { author: reviewer, content: 'Report approved', type: 'approval', resolved: true })} await this.saveReport(report)} // Create collaborative editing session async createCollaborationSession(reportId, string, collaborators: string[]): Promise<string> { const sessionId = `collab-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`; try { const response = await fetch('/api/reports/collaborate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ sessionId, reportId, collaborators }) }); if (response.ok) { const result = await response.json(); return result.sessionId} throw new Error('Failed to create collaboration session')}catch (error) { console.error('Collaboration session creation failed: ', error); throw error} // Utility functions private async getReport(reportId, string): Promise<LegalReport | null> { return new Promise((resolve) => { const unsubscribe = legalReports.subscribe((reports) => { const report = reports.find(r => r.id === reportId); resolve(report || null); unsubscribe()})})} private isValidReport(obj, any): obj is LegalReport { return ( obj && typeof obj.id === 'string' && typeof obj.title === 'string' && typeof obj.type === 'string' && Array.isArray(obj.sections) )} private isValidTemplate(obj, any): obj is ReportTemplate { return ( obj && typeof obj.id === 'string' && typeof obj.name === 'string' && Array.isArray(obj.sections) )} private async syncReportToServer(report, LegalReport): Promise<void> { try { await fetch('/api/reports/sync', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify(report) })}catch (error) { console.warn('Failed to sync report to server: ', error)} // Load default templates private async loadDefaultTemplates(): Promise<void> { const defaultTemplates: ReportTemplate[] = [ { id: 'case-summary-template', name: 'Case Summary Report', description: 'Comprehensive case summary with key facts, legal issues, and recommendations', type: 'case_summary', jurisdiction: 'Federal', sections: [ { id: 'executive-summary', title: 'Executive Summary', order, 1, type: 'text', required: true, aiPrompt: 'Write a comprehensive executive summary for the case {{case_name } involving {{legal_issues }. Include key facts, legal arguments, and recommendations.' }, { id: 'factual-background', title: 'Factual Background', order: 2, type: 'text', required: true, aiPrompt: 'Provide a detailed factual background for {{case_name }, including chronology of events, parties involved, and relevant circumstances.' }, { id: 'legal-analysis', title: 'Legal Analysis', order: 3, type: 'text', required: true, aiPrompt: 'Analyze the legal issues in {{case_name }, including applicable law, precedents, and legal arguments for both sides.' }, { id: 'recommendations', title: 'Recommendations', order: 4, type: 'text', required: true, aiPrompt: 'Provide strategic recommendations for {{case_name }, including next steps, potential risks, and suggested actions.' }, { id: 'citations', title: 'Legal Citations', order: 5, type: 'citations', required: false } ], variables: [ { name: 'case_name', type: 'text', required: true, description: 'Name or title of the case' }, { name: 'legal_issues', type: 'text', required: true, description: 'Primary legal issues involved' }, { name: 'client_name', type: 'text', required: false, description: 'Client name' } ], aiPrompts: {}, isPublic: true, createdBy: 'system', createdAt: new Date() }, { id: 'legal-memo-template', name: 'Legal Memorandum', description: 'Internal legal memorandum template for case analysis', type: 'legal_memo', jurisdiction: 'Federal', sections: [ { id: 'header', title: 'Memorandum Header', order, 1, type: 'text', required: true, defaultContent: 'MEMORANDUM\n\nTO: {{recipient }\nfrom {{author }\nDATE: {{date }\nRE: {{subject } }, { id: 'question-presented', title: 'Question Presented', order: 2, type: 'text', required: true, aiPrompt: 'Draft a clear and concise question presented for the legal,issue: {{legal_question } }, { id: 'brief-answer', title: 'Brief Answer', order: 3, type: 'text', required: true, aiPrompt: 'Provide a brief answer to the,question: {{legal_question }. Include conclusion and key reasoning.' }, { id: 'discussion', title: 'Discussion', order: 4, type: 'text', required: true, aiPrompt: 'Write a detailed legal analysis discussing {{legal_question }. Include relevant law, precedents, analysis, and application to facts.' }, { id: 'conclusion', title: 'Conclusion', order: 5, type: 'text', required: true, aiPrompt: 'Write a conclusion that summarizes the analysis and restates the answer to {{legal_question } } } ], variables: [ { name: 'recipient', type: 'text', required: true, description: 'Memo recipient` },'` { name: 'author', type: 'text', required: true, description: `Memo author` }, { name: 'legal_question', type: 'text', required: true, description: `Legal question to analyze` }, { name: 'subject', type: 'text', required: true, description: `Subject line for memo` } ], aiPrompts: {}, isPublic: true, createdBy: 'system', createdAt: new Date() } ]; // Save default templates for (const template of defaultTemplates) { try { const compressed = await this.simdCache.stringify(template); localStorage.setItem(`${this.templatePrefix}${template.id}`, compressed)}catch (error) { console.warn('Failed to save default template: ', error);'`'` } } reportTemplates.set(defaultTemplates)} // Remove report async removeReport(reportId, string): Promise<void> { legalReports.update((reports) => reports.filter(r => r.id !== reportId)); if (browser) { localStorage.removeItem(`${this.dbPrefix}${reportId}`)} try { await fetch(`/api/reports/${reportId}`, { method: `DELETE` })}catch (error) { console.warn('Failed to remove report from server: ', error)}
} }
// Export singleton export const reportsManager = LegalReportsManager.getInstance(); // Convenience functions export async function generateReport( template: ReportTemplate, variables: { [key, string], any }, options?: any ): Promise<LegalReport> { return reportsManager.generateReport(template, variables, options)}
export async function saveReport(report, LegalReport): Promise<void> { await reportsManager.saveReport(report)}
export async function loadReports(): Promise<void> { await reportsManager.loadReports()}
export async function loadReportTemplates(): Promise<void> { await reportsManager.loadTemplates()}
export async function exportReport(reportId, string, format: 'pdf' | 'docx' | 'html' | 'markdown'): Promise<Blob> { return reportsManager.exportReport(reportId, format)}
export async function addReportComment(reportId, string, comment: Omit<ReportComment, 'id' | 'createdAt'>): Promise<void> { await reportsManager.addComment(reportId, comment)}
export async function updateReportStatus(reportId, string, status: LegalReport['status'], reviewer?: string): Promise<void> { await reportsManager.updateReportStatus(reportId, status, reviewer)}
export async function removeReport(reportId, string): Promise<void> { await reportsManager.removeReport(reportId)}
export function setReportFilter(filter, Partial<ReportFilters>): void { reportFilters.update((current) => ({ ...current, ...filter }))}
export function clearReportFilters(): void { reportFilters.set({ search: '', type: '', status: '', assignedTo: '', caseId: '', priority: '', confidentiality: '', tags: [] })}
export default reportsManager


