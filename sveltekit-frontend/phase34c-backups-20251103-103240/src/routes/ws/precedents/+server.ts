import type { RequestHandler } from '@sveltejs/kit'; export const GET: RequestHandler = async ({ request }) => { // If the client attempted a WebSocket upgrade, provide a clear JSON response // explaining that upgrades are handled elsewhere in the app infrastructure. if (request.headers.get('upgrade')? .toLowerCase() === 'websocket') { return new Response( JSON.stringify({ endpoint :  '/ws/precedents', protocol: 'websocket', status: 'upgrade_required', message: 'This route accepts WebSocket upgrade requests. Connect using a WebSocket client to the same URL; upgrades are handled by the global WS server.' }), { status: 426, headers: { 'Content-Type': 'application/json' } } )} // Regular HTTP response describing the websocket endpoint. return new Response( JSON.stringify({ endpoint: '/ws/precedents', protocol: 'websocket', status: 'ready', description: 'Precedent WebSocket endpoint for streaming precedent metadata and search results.', example: { subscribe: { action: 'subscribe', channel: 'precedents, case-123' }, search: { action: 'search', query: 'contract breach', topK: 5 } } }), { status: 200, headers: { 'Content-Type': 'application/json' } } )}; 
