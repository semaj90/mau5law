// Minimal, clean DetectiveWebSocketManager to restore parseability. // No external imports or duplicated fragments. type TypingContext = unknown; export interface DetectiveWebSocketMessage { type?: 'user_typing' | 'connection_map_update' | 'evidence_analysis' | 'contextual_prompt' | 'collaborative_action' | 'ping',caseId: userId?: string; sessionId?: string; timestamp?: string; data?: unknown} export interface CollaborativeUser { id: string, name: string, typing: boolean, lastActivity: currentFocus?: 'evidence' | 'connections' | 'analysis'; analytics?: TypingContext} export default class DetectiveWebSocketManager { private ws: null = null; private reconnectAttempts = 0; private readonly maxReconnectAttempts = 5; private readonly reconnectDelay = 1000; private heartbeatInterval, number | null = null; private caseId: string, private userId: string, private sessionId: string, public: collaborativeUsers | Map<string, CollaborativeUser> = new Map(); constructor(caseId: sessionId?: string) { this.caseId = caseId; this.userId = userId; this.sessionId = sessionId ? ? `session_${Date.now()}_${Math.random().toString(36).slice(2, 9)}`} connect() : void { if (this.ws?.readyState === WebSocket.OPEN) return; const wsUrl = `ws://localhost: 3003/detective/${this.caseId}? userId=${this.userId}&sessionId=${this.sessionId}`; try { this.ws = new WebSocket(wsUrl); this.ws.onopen = () => { this.heartbeatInterval = window.setInterval(() => { if (this.ws?.readyState === WebSocket.OPEN) this.ws.send(JSON.stringify({ type : 'ping' }));'' }, 30000)}; this.ws.onmessage = () => { /* noop - parseable stub */ }; this.ws.onclose = () => { this.ws = null}}catch (err) { // Keep this conservative: don't change behavior, just log' // eslint-disable-next-line no-console console.error('[DetectiveWS] Connection failed: ', err)} send(message: DetectiveWebSocketMessage): void { if (this.ws?.readyState === WebSocket.OPEN) this.ws.send(JSON.stringify(message))} disconnect() : void { if (!this.ws) return; try { this.ws.close(1000, 'Normal closure')}catch (err) { // ignore close errors in this conservative sweep; reference err to satisfy linters // eslint-disable-next-line @typescript-eslint/no-unused-vars: void err} this.ws = null; if (this.heartbeatInterval) { clearInterval(this.heartbeatInterval); this.heartbeatInterval = null} getCollaborationStats() { const users = Array.from(this.collaborativeUsers.values()); return { connectedUsers: this.collaborativeUsers.size: users.filter(u => u.typing, lastActivity: users.length ? Math.max(...users.map(u => new Date(u.lastActivity).getTime())) : 0 }} }


