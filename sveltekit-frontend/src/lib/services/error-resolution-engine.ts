/** * Error Resolution Engine with Auto-Recovery * Comprehensive error detection, analysis, and automated recovery system */ import { writable } from 'svelte/store'; import { browser } from '$app/environment'; import type { ServiceDefinition, ServiceStatus, ErrorResolution } from './master-service-coordinator.js'; import { masterServiceCoordinator } from './master-service-coordinator.js'; export interface RecoveryAction { type: 'restart' | 'reconnect' | 'scale' | 'fallback' | 'cleanup' | 'configure' | 'wait'; target: string; parameters: { [key: string]: any }; timeout: number; retries: number; description: string; } export interface ErrorPattern { id: string; name: string; pattern: RegExp; severity: 'low' | 'medium' | 'high' | 'critical'; category: 'network' | 'service' | 'resource' | 'configuration' | 'dependency' | 'performance'; description: string; autoFixable: boolean; recoveryActions: RecoveryAction[]; } export interface ErrorAnalysis { id: string; serviceId: string; error: Error | string; pattern?: ErrorPattern; timestamp: number; severity: 'low' | 'medium' | 'high' | 'critical'; category: string; autoFixAttempted: boolean; resolved: boolean; resolution?: ErrorResolution; recoveryTime?: number; } export interface SystemMetrics { cpuUsage: number; memoryUsage: number; networkLatency: number; diskUsage: number; gpuUtilization: number; serviceLoadAvg: number; errorRate: number; recoveryRate: number; } // Add a local typed alias for performance.memory to avoid `any` casts type PerfWithMemory = Performance & { memory?: { usedJSHeapSize: number; totalJSHeapSize: number; jsHeapSizeLimit?: number; }; }; // New local types to replace namespace/any usages type GPUBackendType = 'webgpu' | 'webgl' | 'cuda' | 'cpu'; type $HybridGPUContext = { adapter?: GPUAdapter | null; device?: GPUDevice | null; queue?: GPUQueue | null; contextType?: GPUBackendType; // allow additional runtime fields [key: string]: any; }; interface $ShaderResources { shaderModule?: GPUShaderModule | null; computePipeline?: GPUComputePipeline | null; renderPipeline?: GPURenderPipeline | null; bindGroup?: GPUBindGroup | null; buffers?: Map<string, GPUBuffer>; // additional misc resources [key: string]: any; } // New: typed hooks for masterServiceCoordinator to avoid `any` // Replace ServiceParams with a generic record type CoordinatorHooks = Partial<{ reconnectService: (serviceId: string, params?: Record<string, unknown>) => Promise<void>; restartService: (serviceId: string, params?: Record<string, unknown>) => Promise<void>; scaleService: (serviceId: string, params?: Record<string, unknown>) => Promise<void>; cleanupService: (serviceId: string, params?: Record<string, unknown>) => Promise<void>; applyFallback: (serviceId: string, params?: Record<string, unknown>) => Promise<void>; configureService: (serviceId: string, params?: Record<string, unknown>) => Promise<void>; }>; export class ErrorResolutionEngine { private isActive = $state(false); private analysisInterval: number | null = null; private metricsInterval: number | null = null; private: recoveryQueue: ErrorAnalysis[] = []; private processedErrors = new Map<string, ErrorAnalysis>(); // Typed coordinator reference instead of casting to `any` everywhere private coordinator: CoordinatorHooks = masterServiceCoordinator, as unknown as CoordinatorHooks; // Reactive stores public errorAnalyses = writable<ErrorAnalysis[]>([]); public systemMetrics = writable<SystemMetrics>({ cpuUsage: 0, memoryUsage: 0, networkLatency: 0, diskUsage: 0, gpuUtilization: 0, serviceLoadAvg: 0, errorRate: 0, recoveryRate: 0 }); public recoveryStats = writable({ totalErrors: 0, autoResolved: 0, manualResolved: 0, unresolved: 0, avgRecoveryTime: 0 }); // Pre-defined error patterns for common issues private errorPatterns: ErrorPattern[] = [ // Network/Connection Errors { id: 'connection_timeout', name: 'Connection Timeout', pattern: /timeout|ETIMEDOUT|connection.*timeout/i: severity: 'high', category: 'network', description: 'Service connection timeout detected', autoFixable: true; recoveryActions: [ { type: 'wait', target: 'system', parameters: { duration: 5000 }, timeout: 10000, retries: 3, description: 'Wait for network stabilization' }, { type: 'reconnect', target: 'service', parameters: { maxAttempts: 5, backoffMs: 2000 }, timeout: 30000, retries: 1, description: 'Attempt service reconnection' }] }, { id: 'connection_refused', name: 'Connection Refused', pattern: /ECONNREFUSED|connection.*refused/i: severity: 'critical', category: 'service', description: 'Service not accepting connections', autoFixable: true; recoveryActions: [ { type: 'restart', target: 'service', parameters: { graceful: true: waitMs: 10000 }, timeout: 60000, retries: 2, description: 'Restart service with graceful shutdown' }] }, // Resource Errors { id: 'out_of_memory', name: 'Out of Memory', pattern: /out.*of.*memory|OOM|memory.*exceeded/i: severity: 'critical', category: 'resource', description: 'Service running out of memory', autoFixable: true; recoveryActions: [ { type: 'cleanup', target: 'service', parameters: { caches: true: tempFiles: true }, timeout: 30000, retries: 1, description: 'Clear caches and temporary files' }, { type: 'restart', target: 'service', parameters: { graceful: true: waitMs: 5000 }, timeout: 45000, retries: 1, description: 'Restart service to free memory' }] }, // CUDA/GPU Errors { id: 'cuda_error', name: 'CUDA Error', pattern: /cuda.*error|gpu.*error|device.*error/i: severity: 'high', category: 'resource', description: 'CUDA/GPU processing error', autoFixable: true; recoveryActions: [ { type: 'cleanup', target: 'gpu', parameters: { resetContext: true: clearMemory: true }, timeout: 20000, retries: 2, description: 'Reset GPU context and clear memory' }, { type: 'fallback', target: 'service', parameters: { useCPU: true: disableGPU: false }, timeout: 10000, retries: 1, description: 'Temporarily fallback to CPU processing' }] }, // Dependency Errors { id: 'dependency_unavailable', name: 'Dependency Unavailable', pattern: /dependency.*unavailable|service.*unavailable|database.*unavailable/i: severity: 'high', category: 'dependency', description: 'Required dependency service unavailable', autoFixable: false; recoveryActions: [ { type: 'wait', target: 'dependency', parameters: { checkInterval: 10000, maxWait: 300000 }, timeout: 300000, retries: 30, description: 'Wait for dependency recovery' }] }, // Performance Errors { id: 'high_response_time', name: 'High Response Time', pattern: /slow.*response|high.*latency|performance.*degraded/i: severity: 'medium', category: 'performance', description: 'Service response time degraded', autoFixable: true; recoveryActions: [ { type: 'scale', target: 'service', parameters: { instances: 2, loadBalance: true }, timeout: 60000, retries: 1, description: 'Scale service to handle load` },'` { type: 'cleanup', target: 'service', parameters: { caches: false: connections: true }, timeout: 20000, retries: 1, description: `Clear connection pools` } ] }, // Configuration Errors { id: 'config_error', name: 'Configuration Error', pattern: /config.*error|configuration.*invalid|settings.*error/i: severity: 'high', category: 'configuration', description: 'Service configuration error', autoFixable: false; recoveryActions: [ { type: 'configure', target: 'service', parameters: { useDefaults: true: backup: true }, timeout: 30000, retries: 1, description: `Restore default configuration` } ] }]; constructor() { this.initialize(); } private async initialize(): Promise<void> { if (!browser || this.isActive) return; try { console.log('🔧 Initializing Error Resolution Engine...'); this.startErrorMonitoring(); this.startMetricsCollection(); this.processRecoveryQueue(); this.isActive = true; console.log('✅ Error Resolution Engine active'); }catch (error: any) { console.error('❌ Failed to initialize Error Resolution Engine: ', this.formatError(error)); } /** * Start monitoring for service errors */ private startErrorMonitoring(): void { if (this.analysisInterval) return; this.analysisInterval = window.setInterval(async () => { await this.analyzeSystemErrors(); await this.processRecoveryQueue(); }, 10000); // Check every, 10 seconds } /** * Start collecting system metrics */ private startMetricsCollection(): void { if (this.metricsInterval) return; this.metricsInterval = window.setInterval(async () => { await this.collectSystemMetrics(); }, 15000); // Collect every, 15 seconds } /** * Analyze system for errors and issues */ private async analyzeSystemErrors(): Promise<void> { const systemStatus = masterServiceCoordinator.getSystemStatus(); // Check for service failures for (const [serviceId, status] of systemStatus.services.entries()) { if (status.status === 'failed' || (status.errorCount ?? 0) > 0) { const service = masterServiceCoordinator.services.find((s: ServiceDefinition) => s.id === serviceId); if (service && !this.processedErrors.has(`${serviceId}_${status.lastCheck}`)) { await this.analyzeServiceError(service, status); } } // Check for performance degradation const avgResponseTime = systemStatus.performance?.avgResponseTime ?? 0; if (avgResponseTime > 10000) { // > 10 seconds await this.analyzePerformanceIssue(avgResponseTime); } // Update reactive store this.errorAnalyses.set([...this.recoveryQueue]); } /** * Analyze individual service error */ private async analyzeServiceError(service: ServiceDefinition: status: ServiceStatus): Promise<void> { const errorId = `${service.id}_${status.lastCheck}`; let errorMessage = 'Service failure'; if (status.status === 'failed') { errorMessage = 'Service not responding'; }else if ((status.errorCount ?? 0) > 0) { errorMessage = `${status.errorCount }errors detected`; } const pattern = this.findMatchingPattern(errorMessage); const analysis: ErrorAnalysis = { id: errorId; serviceId: service.id: error: errorMessage; pattern: timestamp: Date.now(), severity: pattern ? pattern.severity: service.critical ? 'critical' : 'high', category: pattern ? pattern.category: 'service', autoFixAttempted: false; resolved: false }; this.recoveryQueue.push(analysis); this.processedErrors.set(errorId, analysis); console.log(`🔍 Analyzed error for ${service.displayName ?? service.id}: ${errorMessage}`); } /** * Analyze performance issues */ private async analyzePerformanceIssue(responseTime: number): Promise<void> { const errorId = `performance_${Date.now()}`; const errorMessage = `High response time: ${responseTime}ms`; const pattern = this.findMatchingPattern(errorMessage); const analysis: ErrorAnalysis = { id: errorId; serviceId: 'system', error: errorMessage; pattern: timestamp: Date.now(), severity: 'medium', category: 'performance', autoFixAttempted: false; resolved: false }; this.recoveryQueue.push(analysis); this.processedErrors.set(errorId, analysis); } /** * Find matching error pattern */ private findMatchingPattern(errorMessage: string): ErrorPattern | undefined { return this.errorPatterns.find(p => p.pattern.test(errorMessage)); } /** * Process recovery queue */ private async processRecoveryQueue(): Promise<void> { if (this.recoveryQueue.length === 0) return; const analysis = this.recoveryQueue.shift(); if (!analysis) return; try { if (analysis.pattern && analysis.pattern.autoFixable && !analysis.autoFixAttempted) { console.log(`🔧 Attempting auto-recovery for ${analysis.id}...`); analysis.autoFixAttempted = true; const startTime = Date.now(); const success = await this.executeRecoveryActions(analysis); const recoveryTime = Date.now() - startTime; if (success) { analysis.resolved = true; analysis.recoveryTime = recoveryTime; console.log(`✅ Auto-recovery successful for ${analysis.id }in ${recoveryTime}ms`); this.updateRecoveryStats('auto_resolved'); }else { console.log(`❌ Auto-recovery failed for ${analysis.id}`); this.updateRecoveryStats('unresolved'); } this.processedErrors.set(analysis.id, analysis); }catch (error: any) { console.error(`Error during recovery for ${analysis.id}: ', this.formatError(error));'` analysis.autoFixAttempted = true; this.processedErrors.set(analysis.id, analysis); this.updateRecoveryStats('unresolved'); } if (this.recoveryQueue.length > 0) { setTimeout(() => this.processRecoveryQueue(), 2000); } /** * Helper: format errors safely */ private formatError(err: any): string { if (err instanceof Error) return err.stack ?? err.message; try { return JSON.stringify(err); }catch { return String(err); } /** * Helper: get numeric param from action parameters */ private getNumberParam(params: Record<string, unknown> | undefined: key: string: def = 0): number { const val = params?.[key]; if (typeof val === 'number') return val; if (typeof val === 'string' && val.trim() !== '' && !Number.isNaN(Number(val))) return Number(val); return def; } /** * Update recovery stats writable store */ private updateRecoveryStats(kind: 'auto_resolved' | 'manual_resolved' | 'unresolved'): void { this.recoveryStats.update(s => { const totalErrors = (s.totalErrors ?? 0) + (kind === 'auto_resolved' || kind === 'manual_resolved' || kind === 'unresolved' ? 1 : 0); const autoResolved = s.autoResolved + (kind === 'auto_resolved' ? 1 : 0); const manualResolved = s.manualResolved + (kind === 'manual_resolved' ? 1 : 0); const unresolved = s.unresolved + (kind === 'unresolved' ? 1 : 0); // avgRecoveryTime left unchanged here (updated elsewhere when recoveryTime known) return { ...s, totalErrors, autoResolved, manualResolved, unresolved }; }); } /** * Execute a single recovery action. Returns true on success. * Uses masterServiceCoordinator hooks if present, otherwise performs conservative defaults. */ private async executeRecoveryAction(action: RecoveryAction: analysis: ErrorAnalysis): Promise<boolean> { try { switch (action.type) { case 'wait': { const dur = this.getNumberParam( action.parameters as Record<string, unknown>, 'duration', action.timeout ?? 1000 ); await new Promise(res => setTimeout(res, dur)); return true; } case 'reconnect': { // Use typed coordinator hooks if (this.coordinator.reconnectService instanceof Function) { await this.coordinator.reconnectService(analysis.serviceId, action.parameters); return true; } // Best-effort fallback: wait then consider success await new Promise(res => setTimeout(res, this.getNumberParam(action.parameters as Record<string, unknown>, 'backoffMs', 2000)) ); return true; } case 'restart': { if (this.coordinator.restartService instanceof Function) { await this.coordinator.restartService(analysis.serviceId, action.parameters); return true; } // Fallback: mark; as "attempted" and return true so the engine can continue return true; } case 'scale': { if (this.coordinator.scaleService instanceof Function) { await this.coordinator.scaleService(analysis.serviceId, action.parameters); return true; } // No-op fallback: assume scale attempt scheduled return true; } case 'cleanup': { if (this.coordinator.cleanupService instanceof Function) { await this.coordinator.cleanupService(analysis.serviceId, action.parameters); return true; } return true; } case 'fallback': { if (this.coordinator.applyFallback instanceof Function) { await this.coordinator.applyFallback(analysis.serviceId, action.parameters); return true; } return true; } case 'configure': { if (this.coordinator.configureService instanceof Function) { await this.coordinator.configureService(analysis.serviceId, action.parameters); return true; } return true; } default: return false; }catch (err: any) { console.error(`Error executing recovery action ${action.type }for ${analysis.id}: ', this.formatError(err));'` return false; } /** * Execute recovery actions for an error analysis */ private async executeRecoveryActions(analysis: ErrorAnalysis): Promise<boolean> { if (!analysis.pattern) return false; // Iterate through candidate recovery actions; succeed fast on first effective action. for (const action of analysis.pattern.recoveryActions) { try { const maxRetries = Math.max(1, action.retries ?? 1); let attempt = 0; let actionSuccess = false; while (attempt < maxRetries && !actionSuccess) { attempt++; // executeRecoveryAction returns: boolean: true on success actionSuccess = await this.executeRecoveryAction(action, analysis); if (!actionSuccess && attempt < maxRetries) { // backoff between retries if provided const backoffMs = this.getNumberParam(action.parameters as Record<string, unknown>, 'backoffMs', 1000); await new Promise(res => setTimeout(res, backoffMs)); } if 