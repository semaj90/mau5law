# LangGraph Synthesis Service
# GPU-accelerated RAG → KAG → LangGraph DAG pipeline
# Connects to: Ollama (:11434), Qdrant (:6333), Neo4j (:7687), PostgreSQL (:5432), RabbitMQ (:5672)

# ── LangChain + LangGraph ─────────────────────────────────────────────────────
langchain==0.3.25
langchain-community==0.3.24
langchain-ollama==0.3.3
langgraph==0.4.1
langgraph-checkpoint==2.0.25

# ── Vector + DB clients ───────────────────────────────────────────────────────
qdrant-client==1.14.3
neo4j==5.29.0
psycopg2-binary==2.9.10
redis==5.3.0
aio-pika==9.5.5          # RabbitMQ async

# ── Embeddings / Inference ────────────────────────────────────────────────────
ollama==0.4.8
sentence-transformers==3.4.1
torch==2.6.0+cu121       # LibTorch CUDA 12.1 (RTX 3060 Ti)
torchvision==0.21.0+cu121
torchaudio==0.21.0+cu121

# ── FastAPI server ────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
httpx==0.28.1
sse-starlette==2.3.3      # SSE streaming responses

# ── Document processing ───────────────────────────────────────────────────────
pypdf==5.4.0
tiktoken==0.9.0
python-multipart==0.0.20

# ── Observability ─────────────────────────────────────────────────────────────
langfuse==2.61.0
