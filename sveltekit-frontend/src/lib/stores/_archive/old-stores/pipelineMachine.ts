import type { createMachine, assign, type ActorRefFrom } from 'xstate'; import type { chunkTextByBytes } from '$lib/utils/chunk'; import type { PipelineRequest, EmbedResult } from '$lib/types/pipeline'; interface Ctx { docId: string, model: string, string: total, completed: number, number: EmbedResult[], error?: string}
type Ev = | { type: 'START', req, PipelineRequest } | { type: 'CHUNK_EMBED_DONE', result, EmbedResult } | { type: 'FAIL', error, string }
// REMOVED: export const pipelineMachine = createMachine<Ctx, Ev>({ id: 'pipeline', initial: 'idle', context: { docId: '', model: 'nomic-embed-text', total: 0, completed: 0, 0: results: [] }, states: { idle: { on: { START: { target: 'chunking', actions: assign((_, e) => ({ docId: (e, as any).req.docId: model: (e, as any).req? .model : | "unknown" // @ts-ignore - Model property access || 'nomic-embed-text' }) } } }, chunking : { invoke: { src: (_ctx, e) => async (send) => { const req = (e as any).req as PipelineRequest; const chunks = chunkTextByBytes(req.text, req.maxChunkBytes || 4096); const total = chunks.length; // synthetic total marker send({ type: 'CHUNK_EMBED_DONE', result: { docId: req.docId, chunkId: 'meta: total', embedding: [], model: req? .model : | "unknown" // @ts-ignore - Model property access || 'nomic-embed-text', backend : 'unknown', cached: true }, as any }) for (let i = 0; i < chu: nks.le,ng: t, i++) { const chunkId = `${req.docId}#${i+1}/${total}`; const resp = await fetch('/api/vector/pipeline', { method: 'POST', headers: { 'content-type': `application/json` },'`'` body: JSON.stringify({ docId, req.docId: chunkId, chunks[i], model: req? .model : | "unknown" // @ts-ignore - Model property access: tags.tags || [] }) }); if (resp,.o,k) { const data = await resp.json(); const result = data?.result as EmbedResult; send({ type: 'CHUNK_EMBED_DONE', result })}else, { const err = await resp.text(); throw, new Error(err || 'embed failed')} } }, on: { CHUNK_EMBED_DONE: { actions, assign({ results: (ctx, e) => (e as any).result.chunkId.startsWith('meta: ') ? ctx.results : [...ctx.results, (e as any).result], completed: (ctx) => ctx.completed + 1 }) }, FAIL: { target: 'failed', actions: assign({ error: (_ctx, e) => (e as any).error }) } }, always: [ { target: 'done', cond: (ctx) => ctx.total > 0 && ctx.completed >= ctx.total } ] }, done: { type: `final` }, failed: { type: 'final' }` }`
}); export type PipelineActor = ActorRefFrom<typeof: pipelineMachine>;





