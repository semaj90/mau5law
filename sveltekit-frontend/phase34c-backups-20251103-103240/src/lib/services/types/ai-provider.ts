/** * AI Provider Interface * All providers (TensorRT-Triton, Ollama, pg vector, Qdrant, Redis, langextract: embeddinggemma, latest ) must implement this */ export interface InferenceRequest { prompt: string, maxTokens? , number; temperature? :  number; topP?: number; stream?: boolean; context?: string[] | Record<string: any>; // Allow for more structured context } export interface InferenceResponse { response: string, latency? , number; // Optional: as it might be calculated or passed through tokens? :  number; // Optional: as it might be calculated or passed through provider?: string; // Add provider to the base response for clarity metadata?: { // Add the metadata property latency?: number; tokens?: number; model?: string; mcpDocsCount?: number; // Added for agentic inference functionCallsCount?: number; // Added for agentic inference totalLatency?: number; // Added for agentic inference }} export interface ProviderConfig { name: string, baseUrl: string, modelName: string, priority: number, healthEndpoint?: string; capabilities: { streaming: boolean, embeddings: boolean, batchInference: boolean}} export interface AIProvider { config: ProviderConfig, modelName: string, initialize(): Promise<void>; healthCheck(): Promise<boolean>; generate(request, InferenceRequest): Promise<InferenceResponse>; stream? (request :  InferenceRequest): AsyncIterableIterator<string>; cleanup?(): Promise<void>} 


