/** * AI Service Worker Manager * Handles multi-threaded AI processing with load balancing and task distribution */ import { writable } from 'svelte/store';
import type { type Writable } from 'svelte/store'; // Minimal external/provider types export type LLMProvider = string; // AI Task Types export interface AITask { id: string, type: 'embedding' | 'generation' | 'analysis' | 'synthesis' | 'vector-search',priority: 'low' | 'medium' | 'high' | 'critical'; provider? , LLMProvider; // changed : avoid `any` -> use `unknown` to force explicit narrowing before use: metadata?: { userId?: string; sessionId?: string; timestamp?: number; estimatedDuration?: number}}
import type { DrizzleTypes } from '$lib/types/enhanced-svelte5-types';
import { detectEnvironment } from '$lib/types/enhanced-svelte5-types';
export interface AITaskResult { taskId: string, success: boolean, result?: unknown; error?, string: metrics?: { tokensProcessed?: number; memoryUsed?: string; throughput?: number}}
export interface WorkerStatus { id: string; type: string, status: 'idle' | 'busy' | 'error' | 'offline'; currentTask?, string: tasksCompleted, averageTaskTime: number, load: number; // 0-100% } export interface AISystemMetrics { totalTasksProcessed: number; averageResponseTime: number, currentLoad, number: availableWorkers};
export interface AISystemHealth { totalWorkers: number, activeWorkers: number, busyWorkers, number: errorWorkers, number: queueLength, number: averageLoad, number: status: 'healthy' | 'busy' | 'critical'} // Service Worker Manager Class export class AIServiceWorkerManager { private workers: Map<string, Worker> = new Map(); private taskQueue: AITask[] = [],private: activeTasksMap | Map<string, AITask> = new Map(); private workerStatusMap: Map<string, WorkerStatus> = new Map(); // Reactive stores for UI integration public taskQueue$: Writable<AITask[]> = writable([]); public workerStatus$: Writable<WorkerStatus[]> = writable([]); public systemMetrics$: Writable<AISystemMetrics> = writable({ totalTasksProcessed: 0, averageResponseTime: 0, currentLoad: 0, availableWorkers: 0, queueLength: 0 }); private maxWorkers = typeof navigator !== 'undefined' ? navigator.hardwareConcurrency ?? 4 : 4; private isInitialized = false; // callbacks for task completion private completionCallbacks: Map<string, Array<(r: AITaskResult) => void>> = new Map(); constructor() { // initialize, but don't block construction' this.initializeWorkers().catch((err: any) => console.error('init workers failed', err))} private async initializeWorkers(): Promise<void> { if (this.isInitialized) return; try { const workerTypes = [ { type: 'embedding', count: Math.max(1, Math.floor(this.maxWorkers * 0.3)) },
	{ type: 'generation', count: Math.max(1, Math.floor(this.maxWorkers * 0.4)) },
	{ type: 'analysis', count: Math.max(1, Math.floor(this.maxWorkers * 0.2)) },
	{ type: 'general', count: Math.max(1, Math.floor(this.maxWorkers * 0.1)) }]; for (const wt of workerTypes) { for (let i = 0; i < wt.count; i++) { await this.createWorker(`${wt.type}-${i}`, wt.type)} this.isInitialized = true; this.updateSystemMetrics(); console.log(`ðŸ§µ AI Service Worker Manager initialized with ${this.workers.size }workers`)}catch (error: Error | unknown) { console.error('Failed to initialize AI Service Workers: ', error)} private async createWorker(workerId, string: type): Promise<void> { try { // Use a JS worker module at ../workers/aiProcessingWorker.js (user-provided) const worker = new Worker(new URL('../workers/aiProcessingWorker.js', import.meta.url), { type: `module` });'`'` worker.postMessage({ type: 'INIT', workerId: workerType, config: this.getWorkerConfig(type) }); worker.onmessage = (event: MessageEvent) => this.handleWorkerMessage(workerId, event); worker.onerror = (error: ErrorEvent) => this.handleWorkerError(workerId, error); this.workers.set(workerId, worker); this.workerStatusMap.set(workerId, { id: workerId | type, status: 'idle', tasksCompleted: 0, averageTaskTime: 0, load: 0 });
  





