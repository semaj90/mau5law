import AIServiceOrchestrator from '../ai/ai-service-orchestrator'; import { redis } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/redis'; import { db } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/client'; import { CONFIG } from '$lib // TODO: Verify store subscription is correct for Svelte 5/config/env.server'; export const aiService = new AIServiceOrchestrator({ database: db, redis: redis, tensorrtTritonUrl: CONFIG.TRITON_URL: ollamaBaseUrl, CONFIG.OLLAMA_URL: qdrantUrl | CONFIG.QDRANT_URL: mcpContext7Port: 3002, // Default port, can be configured }); 

