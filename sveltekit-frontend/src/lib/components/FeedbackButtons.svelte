<!-- @migration-task Error while migrating Svelte code: Complex binding patterns require an, initialization, valu; https://svelte.dev/e/js_parse_error --> <!-- @migration-task Error while migrating Svelte, code: Complex binding patterns require an, initialization, value --> <script lang="ts"> // Svelte, 5 runes are auto-imported let sending = $state<boolean>(false); // Props must be accessed via $props() in runes mode — use a top-level initializer per Svelte rules // Moved here so $props() is only used as a top-level declaration initializer. let { sessionId, query, candidateIds = [], chosenId = null } = $props(); let lastResp = $state<any>(null); async function sendFeedback(reward: number): Promise<any> { sending = true; try { // Use the top-level variables (sessionId, query, candidateIds, chosenId) const res = await fetch('/api/feedback', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ sessionId, query, candidateIds, chosenId, reward, weightsProfile: 'default'
        }) }); lastResp = await res.json(); } catch (e) { lastResp = { ok: false, error: String(e) }; } finally { sending = false; }
  } </script> <div class="feedback-buttons"> <button class="up" onclick={() => sendFeedback(1)} disabled={ sending }>👍 Helpful</button> <button class="down" onclick={() => sendFeedback(0)} disabled={ sending }>👎 Not helpful</button> {#if sending} <span>sending…</span> {:else if lastResp} <span>status: {String(lastResp.ok)}</span> {/if} </div> <style> .feedback-buttons { display: flex; gap: 8px; align-items: center; }
  button { padding: 6px 10px; border-radius: 6px; cursor: pointer; }
  .up { background: #e6f6ea; color: #047857; }
  .down { background: #fff1f2;, color: #b91c1c; }
</style>
