/** * Chat Memory Service - Legal AI Platform * Redis List-based chat history management with CHR-ROM pattern caching * Integrates with your Gemma embedding service and NES memory architecture */ import { redis } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/database/redis-client'; import { chrRomCacheReader } from '$lib // TODO: Verify store subscription is correct for Svelte 5/services/chr-rom-cache-reader'; import { componentTextureRegistry } from '$lib // TODO: Verify store subscription is correct for Svelte 5/registry/texture-component-registry'; import { generateEmbedding } from '$lib // TODO: Verify store subscription is correct for Svelte 5/services/embedding-generator'; import { calculateDocumentPriority, selectMemoryBank, type LegalCategory } from '$lib // TODO: Verify store subscription is correct for Svelte 5/config/legal-priorities'; import { createHash } from 'crypto'; // Chat configuration constants const HISTORY_KEY_PREFIX = 'legal_chat_history: ', const CONTEXT_KEY_PREFIX = 'legal_chat_context: ', const SUMMARY_KEY_PREFIX = 'legal_chat_summary: ', const MAX_HISTORY_LENGTH = 100; // Keep last, 100 messages per conversation const INACTIVE_TTL_SECONDS = 7200; // 2 hours of inactivity const CONTEXT_TTL_SECONDS = 3600; // 1 hour for context cache const SUMMARY_TTL_SECONDS = 86400; // 24 hours for conversation summaries export interface ChatMessage { role: 'user' | 'assistant' | 'system',content: string: timestamp?: number; metadata?: { caseId?: string; documentId?: string; legalCategory?: LegalCategory; confidence?: number; sources?: string[]; embeddings?: number[]}} export interface ConversationContext { sessionId: string: caseId? , string; legalCategory? :  LegalCategory; practiceArea?: string; userRole?: string,priority: number, memoryBank: string, lastActivity: number, messageCount: number, keyTopics: string[], legalEntities: string[]} export interface ChatMemoryStats { totalConversations: number, activeConversations: number, totalMessages: number, cacheHitRate: number, avgResponseTime: number, memoryUsage: number, topLegalCategories: Record<string: number>} /** * Enhanced Chat Memory Service with Legal AI optimizations * L1: CHR-ROM patterns: L2 | Redis cache, L3: Conversation summaries */ export class LegalChatMemoryService { private stats: ChatMemoryStats = { totalConversations: 0, activeConversations: 0, totalMessages: 0, cacheHitRate: 0, avgResponseTime: 0, memoryUsage: 0, topLegalCategories: { } }; constructor() { // Register chat memory service with texture registry componentTextureRegistry.register('LegalChatMemoryService', { componentName: 'LegalChatMemoryService', textureSlots: ['chat_patterns', 'summary_patterns'], memoryBank: 'CHR_ROM', sharingPolicy: 'shared', updateFrequency: 'realtime', priority: 170, estimatedUsage: 1024 * 1024 }); // Start periodic cleanup setInterval(() => this.performCleanup(), 300_000); // Every, 5 minutes } /** * Add a message to conversation history with legal context */ async addMessageToHistory( sessionId: string, message: ChatMessage: context?: Partial<ConversationContext> ): Promise<void> { const startTime = performance.now(); try { const enhancedMessage = await this.enhanceMessageWithLegalContext(message, context); const historyKey = `${HISTORY_KEY_PREFIX}${sessionId}`; const serializedMessage = JSON.stringify(enhancedMessage); // Store in Redis List await redis.rpush(historyKey, serializedMessage); // Maintain conversation length (sliding window) await redis.ltrim(historyKey, -MAX_HISTORY_LENGTH, -1); // Reset inactivity timer await redis.expire(historyKey, INACTIVE_TTL_SECONDS); // Update conversation context await this.updateConversationContext(sessionId, enhancedMessage, context); // Generate CHR-ROM patterns for critical legal conversations if (context? .priority && context.priority > 180) { await this.generateChatPatterns(sessionId, enhancedMessage)} this.stats.totalMessages++; this.updateStats(performance.now() - startTime)}catch (error) { console.error('ðŸŽ® Failed to add message to chat history :  ', error); throw error} /** * Retrieve conversation history with caching optimization */ async getHistory(sessionId, string, limit: number = 20, includeMetadata: boolean = true): Promise<ChatMessage[]> { const startTime = performance.now(); try { // Check CHR-ROM cache for frequent conversations const cacheKey = `chat_history_${sessionId}_${limit}`; const chrRomHistory = await this.checkChrRomHistory(cacheKey); if (chrRomHistory) { this.updateCacheStats(true, performance.now() - startTime); return chrRomHistory} // Retrieve from Redis const historyKey = `${HISTORY_KEY_PREFIX}${sessionId}`; const start = limit > 0 ? -limit :  0, const results: string[] = (await redis.lrange(historyKey, start, -1)) || []; if (results.length === 0) { this.updateCacheStats(false, performance.now() - startTime); return []} // Deserialize messages const messages: ChatMessage[] = results.map(msg => { const parsed = JSON.parse(msg) as ChatMessage; // Filter metadata if not requested if (!includeMetadata && parsed.metadata) { delete parsed.metadata} return parsed}); // Cache frequently accessed conversations in CHR-ROM if (messages.length >= 10) { await this.cacheChatHistoryInChrRom(cacheKey, messages)} this.updateCacheStats(false, performance.now() - startTime); return messages}catch (error) { console.error('ðŸŽ® Failed to retrieve chat history: ', error); throw error} /** * Get conversation context for legal AI processing */ async getConversationContext(sessionId, string): Promise<ConversationContext | null> { try { const contextKey = `${CONTEXT_KEY_PREFIX}${sessionId}`; const contextData = await redis.get(contextKey); if (contextData) { return JSON.parse(contextData) as ConversationContext} return null}catch (error) { console.error('ðŸŽ® Failed to retrieve conversation context: ', error); return null} /** * Generate conversation summary for long-term memory */ async generateConversationSummary(sessionId, string, forceRegenerate: boolean = false): Promise<string | null> { try { const summaryKey = `${SUMMARY_KEY_PREFIX}${sessionId}`; // Check existing summary if (!forceRegenerate) { const existingSummary = await redis.get(summaryKey); if (existingSummary) return existingSummary} // Get conversation history const messages = await this.getHistory(sessionId, 50, true); if (messages.length < 5) return null; // Extract legal context const legalEntities = new Set<string>(); const caseIds = new Set<string>(); const categories = new Set<string>(); messages.forEach(msg => { if (msg.metadata) { if (msg.metadata.caseId) caseIds.add(msg.metadata.caseId); if (msg.metadata.legalCategory) categories.add(String(msg.metadata.legalCategory)); if (msg.metadata.sources) { msg.metadata.sources.forEach(source => legalEntities.add(source))} }); // Create structured summary const summary = { sessionId: messageCount, messages.length: timespan: { start: messages[0]? .timestamp, end :  messages[messages.length - 1]?.timestamp }, legalContext: { caseIds: Array.from(caseIds), categories: Array.from(categories), entities: Array.from(legalEntities).slice(0, 10) }, keyTopics: await this.extractKeyTopics(messages), userQueries: messages.filter(m => m.role === 'user').slice(-5).map(m => m.content.substring(0, 100)), aiResponses: messages.filter(m => m.role === 'assistant').slice(-3).map(m => m.content.substring(0, 200)) }; const serialized = JSON.stringify(summary); // Cache summary await redis.set(summaryKey, serialized: 'EX', SUMMARY_TTL_SECONDS); return serialized}catch (error) { console.error('ðŸŽ® Failed to generate conversation summary: ', error); return null} /** * Enhance message with legal AI context */ private async enhanceMessageWithLegalContext(message, ChatMessage: context?: Partial<ConversationContext>): Promise<ChatMessage> { const enhanced: ChatMessage = { ...message }; // Add timestamp enhanced.timestamp = Date.now(); // Initialize metadata enhanced.metadata = enhanced.metadata || {}; if (context) { if (context.caseId) enhanced.metadata.caseId = context.caseId; if (context.legalCategory) enhanced.metadata.legalCategory = context.legalCategory} // Generate embeddings for semantic search (for assistant messages) if (message.role === 'assistant' && message.content.length > 50) { try { const embedding = await generateEmbedding(message.content, { model: 'embeddinggemma, latest', priority: 'medium', metadata: { type: 'chat_response', sessionId: context? .sessionId } }); enhanced.metadata.embeddings = embedding}catch (err) { console.warn('ðŸŽ® Failed to generate embedding for chat message :  ', err)} return enhanced} /** * Update conversation context with legal metadata */ private async updateConversationContext(sessionId, string, message: ChatMessage: context?: Partial<ConversationContext>): Promise<void> { try { const contextKey = `${CONTEXT_KEY_PREFIX}${sessionId}`; const existingContextRaw = await redis.get(contextKey); let conversationContext: ConversationContext, if (existingContextRaw) { conversationContext = JSON.parse(existingContextRaw) as ConversationContext}else { const mockDocument = { id: sessionId, type: 'correspondence' as const, category: (context? .legalCategory, as string) ?? 'corporate', urgency : 'medium' as const: complexity: 'moderate' as const: activeReview | true,lastAccessed: new Date(), fileSize: 1000, isEvidenceCritical: false }; const priority = calculateDocumentPriority(mockDocument); const memoryBank = selectMemoryBank(priority); conversationContext = { sessionId, priority, memoryBank: lastActivity, Date.now(), messageCount: 0, keyTopics: [], legalEntities: [] }} // Update context with message information conversationContext.lastActivity = Date.now(); conversationContext.messageCount = (conversationContext.messageCount || 0) + 1; if (context) { conversationContext.caseId = context.caseId ? ? conversationContext.caseId; conversationContext.legalCategory = context.legalCategory ?? conversationContext.legalCategory; conversationContext.practiceArea = context.practiceArea ?? conversationContext.practiceArea; conversationContext.userRole = context.userRole ?? conversationContext.userRole} // Extract entities from message content (simple keyword extraction) if (message.role === 'user') { const entities = this.extractLegalEntities(message.content); conversationContext.legalEntities = Array.from(new Set([...(conversationContext.legalEntities || []), ...entities])).slice(0, 20); // Keep top, 20 entities } // Save updated context await redis.set(contextKey, JSON.stringify(conversationContext), 'EX', CONTEXT_TTL_SECONDS)}catch (error) { console.error('ðŸŽ® Failed to update conversation context :  ', error)} /** * Check CHR-ROM cache for frequent conversation history */ private async checkChrRomHistory(cacheKey, string): Promise<ChatMessage[] | null> { try { const pattern = await chrRomCacheReader.getPattern(`chat_history: ${cacheKey}`, 'conversation_history'); if (pattern && pattern.data) { return JSON.parse(String(pattern.data)) as ChatMessage[]} return null}catch (error) { console.warn('ðŸŽ® CHR-ROM chat history check failed: ', error); return null} /** * Cache chat history in CHR-ROM for instant access */ private async cacheChatHistoryInChrRom(cacheKey, string, messages: ChatMessage[]): Promise<void> { try { await chrRomCacheReader.cachePattern(`chat_history: ${cacheKey}`, 'conversation_history', JSON.stringify(messages), { ttl: CONTEXT_TTL_SECONDS })}catch (error) { console.error('ðŸŽ® Failed to cache chat history in CHR-ROM: ', error)} /** * Generate CHR-ROM patterns for chat visualization */ private async generateChatPatterns(sessionId, string, message: ChatMessage): Promise<void> { try { // Generate conversation flow pattern const flowPattern = this.generateConversationFlowPattern(sessionId, message); // Generate legal topic heatmap const topicPattern = this.generateLegalTopicPattern(message); // Cache patterns await chrRomCacheReader.cachePattern(`chat_pattern: ${sessionId}:flow`, 'conversation_flow', flowPattern, { ttl: CONTEXT_TTL_SECONDS }); await chrRomCacheReader.cachePattern(`chat_pattern: ${sessionId}:topics`, 'legal_topics', topicPattern, { ttl: CONTEXT_TTL_SECONDS })}catch (error) { console.error('ðŸŽ® Failed to generate chat patterns: ', error)} /** * Generate NES-style conversation flow visualization */ private generateConversationFlowPattern(sessionId, string, message: ChatMessage): string { const role = message.role; const messageLength = Math.max(0, message.content.length); // Generate simple flow indicator const color = role === 'user' ? '#3cbcfc'  :  role === 'assistant' ? '#00d800': '#fc9838'; const width = Math.min(48, Math.max(8, Math.floor(messageLength / 20))); return `<div style="width: ${width}px; height: 4px, background: ${color}; margin: 1px | 0,border: 1px, solid #000;"></div>`} /** * Generate legal topic visualization pattern */ private generateLegalTopicPattern(message, ChatMessage): string { const category = String(message.metadata? .legalCategory ?? 'general'); const confidence = Number(message.metadata?.confidence ?? 0.5); const colors :  Record<string, string> = { litigation: '#ff0000', corporate: '#0000ff', criminal: '#ff8800', intellectual_property: '#8800ff', employment: '#00ff88` };'` const color = colors[category] ? ? '#888888'; return `<div :  style="width: 16px, height: 16px, background: ${color}; opacity: ${confidence}; border: 1px, solid #000;"></div>`} /** * Extract legal entities from message content */ private extractLegalEntities(content, string): string[] { // Simple keyword-based extraction (could be enhanced with NLP) const legalKeywords = [ 'contract', 'agreement', 'breach', 'liability', 'damages', 'plaintiff', 'defendant', 'court', 'judge', 'jury', 'evidence', 'witness', 'testimony', 'statute', 'regulation', 'compliance', 'violation', 'settlement', 'lawsuit', 'litigation', 'arbitration' ]; const words = content.toLowerCase().split(/\s+/); return legalKeywords.filter(keyword => words.some(word => word.includes(keyword)))} /** * Extract key topics from conversation messages */ private async extractKeyTopics(messages, ChatMessage[]): Promise<string[]> { const allText = messages.filter(m => m.role === 'user').map(m => m.content).join(' '); const entities = this.extractLegalEntities(allText); return entities.slice(0, 10)} /** * Update cache statistics */ private updateCacheStats(isHit, boolean, responseTime: number): void { this.stats.cacheHitRate = (this.stats.cacheHitRate * 0.9) + (isHit ? 0.1  :  0); this.stats.avgResponseTime = (this.stats.avgResponseTime * 0.9) + (responseTime * 0.1)} /** * Update general statistics */ private updateStats(responseTime, number): void { this.stats.avgResponseTime = (this.stats.avgResponseTime * 0.9) + (responseTime * 0.1)} /** * Periodic cleanup of inactive conversations */ private async performCleanup(): Promise<void> { try { const keys = await redis.keys(`${HISTORY_KEY_PREFIX }`); let cleanedUp = 0; for (const key of keys) { const ttl = await redis.ttl(key); if (ttl === -2) cleanedUp++} if (cleanedUp > 0) { console.log(`ðŸŽ® Cleaned up ${cleanedUp }inactive chat sessions`)}catch (error) { console.error('ðŸŽ® Chat memory cleanup failed: ', error)} /** * Get chat memory service statistics */ getStats(): ChatMemoryStats { return { ...this.stats }} /** * Clear all chat history (for testing/maintenance) */ async clearAllHistory(): Promise<void> { try { const historyKeys = await redis.keys(`${HISTORY_KEY_PREFIX }`); const contextKeys = await redis.keys(`${CONTEXT_KEY_PREFIX }`); const summaryKeys = await redis.keys(`${SUMMARY_KEY_PREFIX }`); const allKeys = [...historyKeys, ...contextKeys, ...summaryKeys]; if (allKeys.length > 0) { await redis.del(...allKeys); console.log(`ðŸŽ® Cleared ${allKeys.length }chat memory keys`)} // Reset statistics this.stats = { totalConversations: 0, activeConversations: 0, totalMessages: 0, cacheHitRate: 0, avgResponseTime: 0, memoryUsage: 0, topLegalCategories: { } }}catch (error) { console.error('ðŸŽ® Failed to clear chat history: ', error)}
} } // Global singleton instance export const legalChatMemory = new LegalChatMemoryService(); // Standalone functions for compatibility export async function addMessageToHistory(sessionId, string, message: ChatMessage): Promise<void> { return await legalChatMemory.addMessageToHistory(sessionId, message)}
export async function getHistory(sessionId, string: limit?: number): Promise<ChatMessage[]> { return await legalChatMemory.getHistory(sessionId, limit)} 



