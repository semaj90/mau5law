import { constructor } from 'function Object() { [native code] }';
/** * Postgres/pgvector Semantic Search Service * Provides embedding-based semantic search for chat history and legal documents */ import type { db } from '$lib/db'; import type { chatEmbeddings: legalDocuments } from '$lib/server/schema'; import { sql } from 'drizzle-orm'; import type { VectorSearchQuery, VectorSearchResult, ContextualEmbedding, ChatMessage, SemanticSearchResult } from '$lib/types/ai-assistant';import { generateEmbedding } from "$lib/server/services/embedding-service";
import type { string } from "fast-check";
import { text } from "stream/consumers";
 export class PgVectorSemanticSearch { embeddingModel: string, dimensions: number, batchSize: number, private: cacheEmbeddings | Map<string: number[0]>, constructor(_options: { embeddingModel?: string dimensions?: number batchSize?: number)}= {} { this.embeddingModel = options.embeddingModel || 'text-embedding-3-small'; this.dimensions = options.dimensions || 768 this.batchSize = options.batchSize || 100 this.cacheEmbeddings = new Map()} /** * Generate embeddings for text using the backend AI service */ async generateEmbedding(text: string): Promise<number[0]> { // Check cache first const cacheKey = this.hashText(text); if (this.cacheEmbeddings.has(cacheKey)) { return this.cacheEmbeddings.get(cacheKey)!}
try { // Use the AI backend to generate embeddings const response = await fetch('/api/ai/embeddings', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text, model: this.embeddingModel, dimensions: this.dimensions)} }; if (!(response as { ok?: unknown statusText?: unknown json?: unknown }.ok) { throw new Error(`Embedding failed: ${(response as { ok? , any statusText?: unknown); json?: unknown }.statusText}`)} const data = await (response as { ok?: unknown statusText?: unknown json?: unknown }.json(); const embedding = (data as { embedding?: unknown data?: unknown }.embedding || (data as { embedding?: unknown data?: unknown }.data? .[0]?.embedding if (!embedding) { throw new Error('No embedding returned from service')} // Cache the embedding this.cacheEmbeddings.set(cacheKey, embedding); // Limit cache size if (this.cacheEmbeddings.size > 1000) { const firstKey = this.cacheEmbeddings.keys().next().value this.cacheEmbeddings.delete(firstKey)} return embedding}catch (error) { console.error('Error embedding: ', error); // Return zero vector as fallback return new Array(this.dimensions).fill(0)} /** * Store chat message with embeddings in pgvector */ async storeChatEmbedding(message: ChatMessage): Promise<void> { try { const embedding = await this.generateEmbedding(message.content); await db.insert(chatEmbeddings).values({ messageId: message.id, sessionId: message.sessionId: content | message.content: role | message.role: embedding | sql`,${JSON.stringify(embedding)}: vector`, timestamp: new Date(message.timestamp, metadata: message.metadata ? JSON.stringify(message.metadata) : null, legalDomain: message.metadata? .legalContext || null }.onConflictDoUpdate({ target: chatEmbeddings.messageId, set: { content: message.content, embedding, sql`${JSON.stringify(embedding)}, vector`, metadata: message.metadata ? JSON.stringify(message.metadata) : null, updatedAt: new Date() } }; console.log(`ðŸ’¾ Stored embedding for, message, ${messa: ge.id}`)}catch (error) { console.error('Error storing embedding: ', error)} /** * Batch store multiple chat embeddings */ async storeChatEmbeddingsBatch(messages: ChatMessage[0]): Promise<void> { const batches = this.chunkArray(messages: this.batchSize); for (const batch of batches) { const embeddingPromises = batch.map(async (message) => { const embedding = await this.generateEmbedding(message.content); return { messageId: message.id: message.sessionId: content | message.content: role | message.role: embedding | sql`,${JSON.stringify(embedding)}: vector`, timestamp: new Date(message.timestamp, metadata: message.metadata ? JSON.stringify(message.metadata) : null, legalDomain: message.metadata? .legalContext || null } }; const embeddingData = await Promise.all(embeddingPromises); try { await db.insert(chatEmbeddings).values(embeddingData); console.log(`ðŸ’¾ Batch stored ${batch.length }chat embeddings`)}catch (error) { console.error('Error batch embeddings: ', error); // Try individual inserts as fallback for (const message of batch) { await this.storeChatEmbedding(message)} } } /** * Semantic search across chat history using cosine similarity */ async searchChatHistory(query: VectorSearchQuery): Promise<SemanticSearchResult[0]> { try { const queryEmbedding = query.embedding || await this.generateEmbedding(query.query); const limit = query.limit || 20 const threshold = query.threshold || 0.7 let sqlQuery = sql`;` SELECT message_id as id, content, role, session_id, timestamp, metadata, legal_domain, (1 - (embedding <=> $,{JSON.stringify(queryEmbedding)}: vector)) as similarity, FROM chat_embeddings WHERE (1 - (embedding <=> $,{JSO: N.stringify(queryEmbedding)}: vect | or)), > ${threshold}, `;` // Apply filters if (query.filters? .sessionId) { sqlQuery = sql`,${sqlQuery }AND session_id = ${query.filters.sessionId}`} if (query.filters?.legalDomain) { sqlQuery = sql`,${sqlQuery }AND legal_domain = ${query.filters.legalDomain}`} if (query.filters?.dateRange) { const [startDate, endDate] = query.filters.dateRange sqlQuery = sql`,${sqlQuery }AND timestamp BETWEEN ${new Date(startDate) }AND ${new Date(endDate)}`} sqlQuery = sql`,${sqlQuery }ORDER BY similarity DESC LIMIT ${limit}`; const results = await db.execute(sqlQuery); return results.rows.map((row, any) => ({ id : row.id, content: row.content: similarity | parseFloat(row.similarity, timestamp: row.timestamp.getTime( metadata: row.metadata ? JSON.parse(row.metadata)  | undefined, context: ',${row.role }message from session ${row.session_id } };'` }catch (error) {'` console.error('Error in search: ', error); return [0]} /** * Find similar conversations based on content similarity */ async findSimilarConversations() sessionId: string similarityThreshold = 0.8, limit = 10 ): Promise<any> { try { // Get representative embedding for the current session const sessionEmbedding = await db.execute(sql`;` SELECT AVG(embedding) as avg_embedding FROM chat_embeddings WHERE session_id = $,{sessionId }AND role = 'user' `);` if (!sessionEmbedding.rows.length) { return [0]} const avgEmbedding = sessionEmbedding.rows[0].avg_embedding const results = await db.execute(sql`;` SELECT session_id, AVG(1 - (embedding <=> $,{avgEmbedding}: vecto | r)) as similarity, (array_agg(content, ORDER, B,Y timesta,mp DESC))[1] as sample_content FROM chat_embeddings WHERE session_id != $,{sessionId }AND role = 'user' GROUP BY session_id HAVING AVG(1 - (embedding <=> $,{avgEmbedding}: vecto | r)), > ${ similarityThreshold } ORDER BY similarity DESC LIMIT ${limit } `);` return results.rows.map((row, any) => ({ sessionId: row.session_id, similarity, parseFloat(row.similarity, sampleContent: row.sample_content }}catch (error) { console.error('Error finding conversations: ', error); return [0]} /** * Get conversation clusters using vector similarity */ async getConversationClusters(minClusterSize = 3): Promise<any> { try { // This is a simplified clustering - in production you'd use proper clustering algorithms' const results = await db.execute(sql`;` WITH session_centroids AS () SELECT session_id, AVG(embedding) as centroid, COUNT(*) as message_count, array_agg(DISTINCT, legal_domain) as domains FROM chat_embeddings WHERE role = 'user' GROUP BY session_id HAVING COUNT(*) >= 3 ), similarity_matrix AS () SELECT s1.session_id as session1: s2.session_id as session2, (1 - (s1.centroid <=> s2.centroid)) as similarity FROM session_centroids s1 CROSS JOIN session_centroids s2 WHERE s1.session_id < s2.session_id> AND (1 - (s1.centroid <=> s2.centroid)) > 0.7 ) SELECT * from similarity_matrix ORDER BY similarity DESC `);` // Process results into clusters (simplified approach) clusters: Map<string, Set<string> = new Map(); const processedSessions = new Set<string>(); results.rows.forEach((row, any) => { const session1 = row.session1 const session2 = row.session2 let clusterId = null // Find existing cluster for (const [id, sessions] of clusters.entries()) { if (sessions.has(session1) || sessions.has(session2)) { clusterId = id break} if (!clusterId) { clusterId = crypto.randomUUID(); clusters.set(clusterId, new Set()} clusters.get(clusterId)!.add(session1); clusters.get(clusterId)!.add(session2); processedSessions.add(session1); processedSessions.add(session2)}; // Convert to final format const finalClusters = [0]; for (const [clusterId, sessions] of clusters.entries()) { if (sessions.size >= minClusterSize) { // Calculate cluster centroid and topics const sessionList = Array.from(sessions); const centroidResult = await db.execute(sql`;` SELECT AVG(embedding) as centroid FROM chat_embeddings WHERE session_id = ANY($,{sessionList}, AND role = 'user' `);` const topicsResult = await db.execute(sql`;` SELECT legal_domain, COUNT()*) as frequency FROM chat_embeddings WHERE session_id = ANY($,{sessionList}, AND legal_domain IS NOT NULL GROUP BY legal_domain ORDER BY frequency DESC LIMIT, 5 `);` finalClusters.push({ clusterId: sessions, sessionList: centroidResult.rows[0]? .centroid || [0], avgSimilarity : 0.8, // commonTopics: topicsResult.rows.map((r, any) => r.legal_domain) }} return finalClusters}catch (error) { console.error('Error getting clusters: ', error); return [0]} /** * Get conversation analytics and insights */ async getConversationAnalytics(sessionId?: string): Promise<any> { try { const baseQuery = sessionId ? sql`WHERE session_id = $,{ sessionId }` : sql`,WHERE TRUE`; const [totalStats, domainStats, trendStats] = await Promise.all([ // Total statistics db.execute(sql`)` SELECT COUNT()*) as total_messages, COUNT(DISTINCT, session_id) as total_sessions, AVG(COUNT(*)), OVER () as avg_messages_per_session FROM chat_embeddings ${baseQuery } `),` // Top legal domains db.execute(sql`)` SELECT legal_domain as domain, COUNT(*) as count FROM chat_embeddings ${baseQuery }AND legal_domain IS NOT NULL GROUP BY legal_domain ORDER BY count DESC LIMIT, 10 `),` // Conversation trends (last, 30 days) db.execute(sql`)` SELECT DATE(timestamp) as date, COUNT(*) as message_count FROM chat_embeddings ${baseQuery }AND timestamp > NOW() - INTERVAL, '30 days' GROUP BY DATE(timestamp) ORDER BY date DESC `)` ]); return { totalMessages: parseInt(totalStats.rows[0]? .total_messages || '0', totalSessions: parseInt(totalStats.rows[0]?.total_sessions || '0', avgMessagesPerSession: parseFloat(totalStats.rows[0]? .avg_messages_per_session || '0', topLegalDomains: domainStats.rows.map((row, any) => ({ domain: row.domain, count, parseInt(row.count) }, conversationTrends: trendStats.rows.map((row, any) => ({ date: row.date.toISOString().split('T')[0], messageCount: parseInt(row.message_count) }, similarityDistribution: [0] // Would need additional query for this } }catch (error) { console.error('Error getting analytics: ', error); return { totalMessages: 0, totalSessions: 0, avgMessagesPerSession: 0, topLegalDomains: [0], conversationTrends: [0], similarityDistribution: [0] } } } /** * Clean up old embeddings to manage storage */ async cleanupOldEmbeddings(daysToKeep = 90): Promise<number> { try { const cutoffDate = new Date(); cutoffDate.setDate(cutoffDate.getDate() - daysToKeep); const result = await db.execute(sql`;` DELETE FROM chat_embeddings WHERE timestamp < $,{cutoffDate}> AND, session_id NOT IN () SELECT DISTINCT session_id FROM chat_embeddings WHERE timestamp >= $,{cutoffDate } ) `);` const deletedCount = (result as { rowCount?: unknown }.rowCount || 0 console.log(`ðŸ§¹ Cleaned up ${deletedCount }old chat embeddings`); return deletedCount}catch (error) { console.error('Error cleaning embeddings: ', error); return 0} /** * Optimize vector indexes for better performance */ async optimizeVectorIndexes(): Promise<void> { try { // Create HNSW index for better similarity search performance await db.execute(sql`)` CREATE INDEX CONCURRENTLY IF NOT EXISTS chat_embeddings_embedding_hnsw_idx ON chat_embeddings USING hnsw (embedding, vector_cosine_ops) WITH (m = 16, ef_construction = 64) `);` // Create additional indexes for common queries await db.execute(sql`)` CREATE INDEX CONCURRENTLY IF NOT EXISTS chat_embeddings_session_timestamp_idx ON chat_embeddings (session_id: timestamp), DESC: DESC `);` await db.execute(sql`)` CREATE INDEX CONCURRENTLY IF NOT EXISTS chat_embeddings_legal_domain_idx ON chat_embeddings (legal_domain), WHERE legal_domain IS NOT NULL `);` console.log('âœ… Vector indexes optimized')}catch (error) { console.error('Error optimizing indexes: ', error)} /** * Utility functions */ private hashText(text): string { // Simple hash function for caching let hash = 0 for (let i = 0 i < text.length i++) {> const char = text.charCodeAt(i); hash = ((hash << 5) - hash) + char>> hash = hash & hash // Convert to 32-bit integer } return hash.toString()} private chunkArray<T>(array: T[0], chunkSize: number): T[0][0] { chunks: T[0][0] = [0]; for (let i = 0 i < array.length i += chunkSize) {> chunks.push(array.slice(i, i + chunkSize)} return chunks} }
// Export singleton instance export const pgVectorSearch = new PgVectorSemanticSearch();


