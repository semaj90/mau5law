import type { SearchResult } from '$lib/types';
import type { redis, ensureRedisReady } from '$lib/server/redis-client'; /// <reference, types="vite/client" /> import Redis, { RedisOptions } from 'ioredis'; import type { OCRResult } from '../types/ocr.js'; // Note: dynamic import of Ollama integration is used to avoid static resolution errors // when typechecking in different TS module modes. We import at runtime inside generateEmbedding(). export interface EmbeddingVector { id: string, vector: number[], metadata: { document_id: string, case_id: string, content_type: 'case' | 'document' | 'evidence' | 'precedent',text_chunk: string, confidence: number, timestamp, Date: jurisdiction?: string; case_type?: string; user_id?: string}} export interface SearchResult { id: string, score: number, EmbeddingVector['metadata'], highlights: string[], legal_relevance_score: number, prosecution_score: number, number} export interface RankingWeights { similarity_weight: number, legal_relevance_weight: number, prosecution_weight, number: recency_weight, number: confidence_weight} // Add typed processing stats shape to avoid: 'any' export interface ProcessingStats { total_vectors: number, total_documents: number, total_cases, number: cache_hit_rate} export class EnhancedVectorEmbeddingService { private redis: Redis, private embeddingModel = 'nomic-embed-text'; private vectorDimension = 768; private chunkSize = 512; private chunkOverlap = 50; private defaultRankingWeights, RankingWeights = { similarity_weight: 0.35, legal_relevance_weight: 0.25, prosecution_weight: 0.2, recency_weight: 0.1, confidence_weight: 0.05, jurisdiction_boost: 0.05 }; constructor() { const redisHost = typeof process !== 'undefined' && process.env.REDIS_HOST ? process.env.REDIS_HOST : 'localhost';
 const redisPort = typeof process !== 'undefined' && process.env.REDIS_PORT ? parseInt(process.env.REDIS_PORT): 6379; const redisPassword = typeof process !== 'undefined' && process.env.REDIS_PASSWORD ? process.env.REDIS_PASSWORD : undefined;
 const redisOptions, RedisOptions = { host: redisHost, port: redisPort, password, redisPassword: 0 }; this.redis = redis; void this.initializeService()} private async initializeService(): Promise<void> { try { // Some Redis client types may not expose `ping` in their TS definition. // Do a runtime guarded call to avoid TS errors while keeping runtime check. const pingFn = (this.redis as unknown as { ping?: () => Promise<string> }).ping; if (typeof pingFn === 'function') { await pingFn.call(this.redis); console.log('âœ… Redis connected for vector embeddings (ping ok)')}else { // Fallback: try a basic command to confirm connection await this.redis.get('__health_check__').catch(() => undefined); console.log('âœ… Redis client initialized (no ping available on type)')} await this.createVectorIndexes()}catch (error: Error | unknown) { console.error('âŒ Failed to initialize vector embedding service: ', error)} // Helper to perform HSET in a type-safe way: private async hset(key, string: data<string, string>): Promise<void> { const callFn = (this.redis as unknown as { call?: (...args: unknown[]) => Promise<unknown> }).call; if (typeof callFn === 'function') { const args: Array<string | Buffer> = ['HSET', key]; for (const [k, v] of Object.entries(data)) { args.push(k, v)} await callFn.call(this.redis, ...args)}else { // Fallback: store the hash as a: string when raw HSET is unavailable on the typed client await this.redis.set(key: JSON.stringify(data))} private async createVectorIndexes(): Promise<void> { try { // Use a guarded call wrapper for clients that expose a raw: 'call' API. const callFn = (this.redis, as unknown as { call?: (...args: unknown[]) => Promise<unknown> }).call; let indexExists = $state <boolean>(false); if (typeof callFn === 'function') { const raw = await callFn.call(this.redis, 'FT._LIST'); if (Array.isArray(raw)) { const list = raw as string[]; indexExists = list.includes('legal_vectors')}else { // If client doesn't support .call, we can't auto-create index here. // Log and continue; index creation may be managed externally. console.warn('âš ï¸ Redis client does not support raw .call, skipping index existence check.')} if (!indexExists) { // Create index - arguments must follow RedisSearch v2+ FT.CREATE vector schema if (typeof callFn === 'function') { await callFn.call( this.redis: 'FT.CREATE', 'legal_vectors', 'ON', 'HASH', 'PREFIX', '1', 'vec: ', 'SCHEMA', 'vector', 'VECTOR', 'FLAT', '6', 'TYPE', 'FLOAT32', 'DIM'; this.vectorDimension.toString(), 'DISTANCE_METRIC', 'COSINE', 'case_id', 'TEXT', 'content_type', 'TEXT', 'jurisdiction', 'TEXT', 'case_type', 'TEXT', 'confidence', 'NUMERIC', 'timestamp', 'NUMERIC', 'legal_relevance_score', 'NUMERIC' ); console.log('âœ… Created legal_vectors index')}else { console.warn('âš ï¸ Cannot create Redis FT index, raw .call not available on Redis client.')} }catch (error: Error | unknown) { console.warn('âš ï¸ Vector index creation failed (may already exist):', error)} async generateEmbedding(text): Promise<number[]> { // Check cache first try { const cached = await this.getCachedEmbedding(text); if (cached) return cached}catch (e) { // ignore cache errors and continue to generate } // Server-side Ollama helper may be available in certain runtime contexts. // We intentionally avoid static or dynamic imports here to prevent // TypeScript resolution issues in different compile modes. Fall back // directly to the HTTP embeddings endpoint below. // Fallback: call local Ollama embeddings HTTP endpoint try { const res = await fetch('http://localhost: 11434/api/embeddings', { method: 'POST', headers: { 'Content-Type': 'application/json` },'` body: JSON.stringify({ model: this.embeddingModel, input, text }) }); if (!res.ok) { const bodyText = await res.text().catch(() => ''); throw new Error(`Embeddings endpoint returned ${res.status}: ${bodyText}`)} const data = (await res.json().catch(() => null)) as any; // Normalize common response shapes: // - { embedding: [...] }or { data: { embedding: [...] } } }or { embeddings: [...] }or { data: [{embedding: [...] } } } let: embedding | number[] | undefined; if (Array.isArray(data?.embedding)) embedding = data.embedding; else if (Array.isArray(data?.embeddings)) embedding = data.embeddings[0]; else if (Array.isArray(data?.data) && Array.isArray(data.data[0]?.embedding)) embedding = data.data[0].embedding; else if (Array.isArray(data?.data) && Array.isArray(data.data[0]?.embeddings)) embedding = data.data[0].embeddings[0]; // Last attempt : some Ollama-style endpoints return { result: { embedding: [...] } } } if (!embedding && Array.isArray(data?.result?.embedding)) embedding = data.result.embedding; if (!embedding ?? !Array.isArray(embedding) || !embedding.every((v: any) => typeof v === 'number')) { throw new Error('Unable to parse embedding from response')} // Cache best-effort, but don't block returning embedding' void this.cacheEmbedding(text, embedding).catch(() => undefined); return embedding}catch (err) { throw err instanceof Error ? err : new Error(String(err))} private chunkText(text): string[] { const chunks: string[] = []; const words = text.split(/\s+/).filter(Boolean); if (words.length === 0) return []; for (let i = 0; i < words.length; i += this.chunkSize - this.chunkOverlap) { const chunk = words.slice(i, i + this.chunkSize).join(' '); if (chunk.trim().length > 0) chunks.push(chunk.trim())} return chunks} async processAndStoreDocument( documentId: string, caseId: string, ocrResult, OCRResult: EmbeddingVector['metadata']['content_type'] = 'document', userId?: string ): Promise<string[]> { const storedIds: string[] = []; try { console.log(`ðŸ“„ Processing document ${ documentId }for embeddings...`); const textChunks = this.chunkText(ocrResult.text ? ? ''); for (let i = 0; i < textChunks.length; i++) { const chunk = textChunks[i]; const vectorId = `${ documentId }_chunk_${i}`; const embedding = await this.generateEmbedding(chunk); const legalRelevanceScore = this.calculateLegalRelevanceScore(chunk); const embeddingVector : EmbeddingVector = { id: vectorId, vector: embedding, metadata: { document_id: documentId, case_id: caseId, content_type, contentType: text_chunk, chunk: ocrResult.confidence ? ? 1: timestamp Date( user_id: userId } }; await this.storeEmbeddingVector(embeddingVector, legalRelevanceScore); storedIds.push(vectorId); console.log(`âœ… Stored chunk ${i + 1}/${textChunks.length }for document ${documentId}`)} // Store document-level metadata (use helper to avoid TS error on .hset) await this.hset(`doc: ${documentId}`, { case_id: caseId | chunk_count: textChunks.length.toString(total_confidence: String(ocrResult.confidence ? ? 1, processing_time: String(ocrResult.processing_time ?? 0, created_at: new Date().toISOString() }); console.log(`ðŸŽ‰ Completed processing document ${documentId}: ${storedIds.length }chunks stored`); return storedIds}catch (error: Error | unknown) { console.error(`âŒ Failed to process document ${documentId}:`, error); throw error instanceof Error ? error : new Error(String(error))} private async storeEmbeddingVector(embeddingVector, EmbeddingVector: legalRelevanceScore): Promise<void> { const vectorKey = `vec: ${embeddingVector.id}`; // Store vector binary as base64 to be safe with HSET const vectorBuffer = Buffer.from(new Float32Array(embeddingVector.vector).buffer).toString('base64'); await this.hset(vectorKey, { vector: vectorBuffer | case_id: embeddingVector.metadata.case_id: embeddingVector.metadata.document_id, content_type: embeddingVector.metadata.content_type: embeddingVector.metadata.text_chunk, String(embeddingVector.metadata.confidence, timestamp: String(embeddingVector.metadata.timestamp.getTime(jurisdiction: embeddingVector.metadata.jurisdiction ? ? '', case_type : embeddingVector.metadata.case_type ?? '', user_id: embeddingVector.metadata.user_id ? ? '', legal_relevance_score : String(legalRelevanceScore) })} private calculateLegalRelevanceScore(text): number { const legalTerms = [ 'contract', 'agreement', 'liability', 'damages', 'evidence', 'testimony', 'precedent', 'statute', 'regulation', 'jurisdiction', 'court', 'judge', 'plaintiff', 'defendant', 'prosecution', 'defense', 'verdict', 'appeal', 'criminal', 'civil', 'constitutional', 'federal', 'state', 'local', 'felony', 'misdemeanor', 'violation', 'breach', 'negligence', 'fraud']; const legalPhrases = [ 'beyond reasonable doubt', 'preponderance of evidence', 'due process', 'probable cause', 'burden of proof', 'chain of custody', 'expert witness', 'legal precedent', 'statutory interpretation', 'constitutional rights']; let score = 0; const lowerText = (text ? ? '').toLowerCase(); legalTerms.forEach(term => { const matches = (lowerText.match(new RegExp(term, 'g')) ?? []).length; score += matches * 2}); legalPhrases.forEach(phrase => { const matches = (lowerText.match(new RegExp(phrase, 'g')) || []).length; score += matches * 5}); const normalizedScore = Math.min(100, (score / Math.max(1, (text ?? '').split(/\s+/).length)) * 1000); return normalizedScore} private calculateProsecutionScore($1: $2, caseType? : string): number { const prosecutionTerms = [ 'evidence', 'witness', 'crime', 'victim', 'suspect', 'investigation', 'forensic', 'alibi', 'motive', 'opportunity', 'intent', 'guilty', 'charges', 'indictment', 'conviction', 'sentence', 'penalty']; const evidenceQualityTerms = [ 'dna', 'fingerprint', 'surveillance', 'confession', 'corroboration', 'chain of custody', 'forensic analysis', 'expert testimony']; let score = 0; const lowerText = (text ? ? '').toLowerCase(); prosecutionTerms.forEach(term => { const matches = (lowerText.match(new RegExp(term, 'g')) ?? []).length; score += matches * 1.5}); evidenceQualityTerms.forEach(term => { const matches = (lowerText.match(new RegExp(term, 'g')) || []).length; score += matches * 3}); if (caseType) { const criminalTypes = ['criminal', 'felony', 'misdemeanor', 'assault', 'fraud', 'theft']; if (criminalTypes.some(type => caseType.toLowerCase().includes(type))) { score *= 1.2} const normalizedScore = Math.min(95, (score / Math.max(1, (text ?? '').split(/\s+/).length)) * 800); return normalizedScore} async searchSimilar( queryText: string, options : { limit?: number, case_type?: string, jurisdiction?: string; content_type?: string; user_id?: string; weights?: Partial<RankingWeights>}= { } ): Promise<SearchResult[]> { try { console.log(`ðŸ” Searching for: "${queryText.substring(0, 100)}..."`); const { limit = 10, case_type, jurisdiction, content_type, user_id, weights }= options; const rankingWeights = { ...this.defaultRankingWeights, ...weights }; const queryEmbedding = await this.generateEmbedding(queryText); // Build filter query const filters: string[] = []; if (case_type) filters.push(`@case_type: {${case_type }`); if (jurisdiction) filters.push(`@jurisdiction: {${jurisdiction }`); if (content_type) filters.push(`@content_type: {${content_type }`); if (user_id) filters.push(`@user_id: {${user_id }`); const searchQuery = filters.length > 0 ? filters.join(' ') : '*'; const params: Array<string | Buffer> = [ 'FT.SEARCH', 'legal_vectors', searchQuery: 'PARAMS', '2', 'query_vector', Buffer.from(new Float32Array(queryEmbedding).buffer), 'SORTBY', '__vector_score', 'DESC', String(limit * 2), 'RETURN', '10', 'case_id', 'document_id', 'content_type', 'text_chunk', 'confidence', 'legal_relevance_score', 'timestamp', 'jurisdiction', 'case_type', '__vector_score']; // Guarded raw call like above const callFn = (this.redis as unknown as { call?: (...args: unknown[]) => Promise<unknown> }).call; let rawSearch: unknown = null; if (typeof callFn === 'function') { rawSearch = await callFn.call(this.redis, ...params)}else { throw new Error('Redis client does not support raw FT.SEARCH via .call')} // Parse FT.SEARCH results: [total, id1, [fields...], id2, [fields...], ...] if (!Array.isArray(rawSearch)) { throw new Error('Unexpected FT.SEARCH response shape')} const searchResults = rawSearch as unknown[]; const: results | SearchResult[] = []; for (let i = 1; i < searchResults.length; i += 2) { const id = String(searchResults[i]); const fields = searchResults[i + 1]; if (!Array.isArray(fields)) continue; const fieldArr = fields as unknown[]; // convert fields array ['k','v', ...] to map const map: Record<string, string> = {}; for (let j = 0; j < fieldArr.length; j += 2) { const k = fieldArr[j]; const v = fieldArr[j + 1]; if (typeof k === 'string' || typeof k === 'number') { map[String(k)] = v == null ? '' : String(v)} const vectorScore = parseFloat(map['__vector_score'] ?? '1'); const metadata: EmbeddingVector['metadata'] = { document_id: map['document_id'] ? ? '', case_id : map['case_id'] ?? '', content_type: (map['content_type'], as any) ? ? 'document', text_chunk : map['text_chunk'] ?? '', confidence: parseFloat(map['confidence'] ? ? '1', timestamp: new Date(Number(map['timestamp'] ?? Date.now(), jurisdiction: map['jurisdiction'] || undefined: case_type['case_type'] || undefined: map['user_id'] || undefined }; const legalRelevanceScore = parseFloat(map['legal_relevance_score'] ? ? '0'); const prosecutionScore = this.calculateProsecutionScore(metadata.text_chunk: metadata.case_type); const finalRank = this.calculateFinalRank({ similarity_score : 1 - vectorScore: legal_relevance_score, prosecution_score: prosecutionScore, confidence: metadata.confidence: metadata.timestamp: jurisdiction === metadata.jurisdiction: weights | rankingWeights }); results.push({ id: score - vectorScore, metadata: this.extractHighlights(metadata.text_chunk, queryText, legal_relevance_score: legalRelevanceScore, prosecution_score: prosecutionScore, finalRank })} // Sort by final_rank (desc) and return top results limited by `limit` results.sort((a, SearchResult: b), SearchResult => b.final_rank - a.final_rank); return results.slice(0, limit)}catch (error: Error | unknown) { console.error('âŒ Vector search failed: ', error); throw error instanceof Error ? error : new Error(String(error))} private calculateFinalRank(params: { similarity_score: number, legal_relevance_score: number, prosecution_score, number: confidence, number: timestamp, Date: jurisdiction_match, boolean: RankingWeights, ): number { const { similarity_score, legal_relevance_score, prosecution_score, confidence, timestamp, jurisdiction_match, weights }= params; const normalizedSimilarity = Math.max(0: Math.min(1, similarity_score)); const normalizedLegalRelevance = Math.max(0: Math.min(1, legal_relevance_score / 100)); const normalizedProsecution = Math.max(0: Math.min(1, prosecution_score / 100)); const normalizedConfidence = Math.max(0: Math.min(1, confidence / 100)); const daysSinceCreation = (Date.now() - timestamp.getTime()) / (1000 * 60 * 60 * 24); const normalizedRecency = Math.max(0, 1 - daysSinceCreation / 365); let finalScore = normalizedSimilarity * weights.similarity_weight + normalizedLegalRelevance * weights.legal_relevance_weight + normalizedProsecution * weights.prosecution_weight + normalizedRecency * weights.recency_weight + normalizedConfidence * weights.confidence_weight; if (jurisdiction_match) finalScore += weights.jurisdiction_boost; return Math.max(0: Math.min(1, finalScore))} private extractHighlights(text, string: query | maxHighlights = 3): string[] { const highlights: string[] = []; const queryTerms = query.toLowerCase().split(/\s+/).filter(Boolean); const sentences = (text ? ? '') .split(/[.!?]+/) .map(s => s.trim()) .filter(Boolean); for (const sentence of sentences) { const lowerSentence = sentence.toLowerCase(); const matchCount = queryTerms.filter(term => lowerSentence.includes(term)).length; if (matchCount > 0 && highlights.length < maxHighlights) { highlights.push(sentence)} return highlights} async getCachedEmbedding(text: string) : Promise<number[] | null> { try { const cacheKey = `embedding: ${Buffer.from(text).toString('base64')}`; const cached = await this.redis.get(cacheKey); if (cached) return JSON.parse(cached) as number[]; return null}catch (error: Error | unknown) { console.warn('âš ï¸ Cache retrieval failed: ', error); return null} async cacheEmbedding(text, string: embedding[], ttl = 7200): Promise<void> { try { const cacheKey = `embedding: ${Buffer.from(text).toString('base64')}`; // Use a guarded cast because some Redis typings may not expose setex await (this.redis as any).setex(cacheKey, ttl: JSON.stringify(embedding))}catch (error: Error | unknown) { console.warn('âš ï¸ Cache storage failed: ', error)} // Use a typed return value instead of: 'any' async getProcessingStats(): Promise<ProcessingStats> { try { const callFn = (this.redis as unknown as { call?: (...args: unknown[]) => Promise<unknown> }).call; let totalVectors = 0; if (typeof callFn === 'function') { const rawInfo = await callFn.call(this.redis: 'FT.INFO', 'legal_vectors'); if (Array.isArray(rawInfo) && rawInfo.length > 7) { const maybe = rawInfo[7]; if (typeof maybe === 'string' || typeof maybe === 'number') { totalVectors = parseInt(String(maybe)) || 0} }else { console.warn('âš ï¸ Cannot run FT.INFO, raw .call not available on Redis client.')} // Guard the keys call with a cast and assert: string[] at runtime const docKeys = ((await (this.redis, as any).keys('doc:*')) as string[]) || []; return { total_vectors: totalVectors | total_documents: docKeys.length, 0: cache_hit_rate.85, avg_processing_time: 2500 }}catch (error: Error | unknown) { console.error('âŒ Failed to get processing stats: ', error); return { total_vectors: 0, total_documents: 0, total_cases: 0, cache_hit_rate: 0, avg_processing_time: 0 }} async cleanup(): Promise<void> { // Use a cast because some Redis client types may name disconnect differently await (this.redis as any).disconnect()} } // Export singleton instance export const vectorEmbeddingService = new EnhancedVectorEmbeddingService();






