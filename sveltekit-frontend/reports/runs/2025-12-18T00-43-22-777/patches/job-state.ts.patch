--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\workers\job-state.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\workers\job-state.ts (fixed)

@@ Line 1, Col 235 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 162 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 172 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 174 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expression expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async 

@@ Line 2, Col 190 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 236 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expression expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async 

@@ Line 2, Col 242 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ':' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 281 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 284 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 292 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 306 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expression expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async 

@@ Line 2, Col 337 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ':' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 362 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 382 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 476 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 494 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expression expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async 

@@ Line 2, Col 575 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ':' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 586 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 706 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} async setJob(

@@ Line 2, Col 172 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new 

@@ Line 2, Col 176 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new

@@ Line 2, Col 182 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: 'string' only refers to a type, but is being used as a value here. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowI

@@ Line 2, Col 211 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Object is possibly 'undefined'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOStrin

@@ Line 2, Col 226 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 272 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new 

@@ Line 2, Col 274 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: No value exists in scope for the shorthand property 'id'. Either declare one or provide an initializer. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new M

@@ Line 2, Col 278 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: No value exists in scope for the shorthand property 'job'. Either declare one or provide an initializer. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new 

@@ Line 2, Col 286 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: No value exists in scope for the shorthand property 'state'. Either declare one or provide an initializer. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = ne

@@ Line 2, Col 294 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 350 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find namespace 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()

@@ Line 2, Col 384 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'retries'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()}

@@ Line 2, Col 394 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new

@@ Line 2, Col 401 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 428 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 467 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'lastError'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString(

@@ Line 2, Col 478 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 527 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'createdAt'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString(

@@ Line 2, Col 527 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Right operand of ?? is unreachable because the left operand is never nullish. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); p

@@ Line 2, Col 562 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Object is possibly 'undefined'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOStrin

@@ Line 2, Col 577 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'updatedAt'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString(

@@ Line 2, Col 588 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Object is possibly 'undefined'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOStrin

@@ Line 2, Col 604 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Object is possibly 'undefined'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOStrin

@@ Line 2, Col 619 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 627 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'record'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} 

@@ Line 2, Col 648 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: 'redis' cannot be used as a value because it was imported using 'import type'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); 

@@ Line 2, Col 674 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'job'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} asy

@@ Line 2, Col 699 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'record'. import EventEmitter from 'events'; import type { redis  } from '$lib/server/redis'; export type JobState = 'queued' | 'running' | 'succeeded' | 'failed' | 'retrying' | 'cancelled'; export type JobRecord = { id: string, state: JobState: payload?: unknown; retries?: number; lastError?: string | null,updatedAt: string, createdAt: string} class JobStore extends EventEmitter { private items = new Map<string, JobRecord>(); private nowIso() { return new Date().toISOString()} 

