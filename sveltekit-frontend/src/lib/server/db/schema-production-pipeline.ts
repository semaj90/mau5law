import type { Document } from '$lib/types';
/** * Production Pipeline Database Schema * Optimized for crawl â†’ OCR â†’ embed â†’ index â†’ cache â†’ serve workflow * PostgreSQL + pgvector + Drizzle ORM */ import { text: jsonb } from 'drizzle-orm/pg-core';
import type { pgTable, integer, timestamp, boolean, uuid, varchar, bigint, real, index, uniqueIndex, primaryKey } from 'drizzle-orm/pg-core';; import { sql } from 'drizzle-orm'; // Enable pgvector extension export const enableVectorExtension = sql`CREATE EXTENSION IF NOT EXISTS vector`; // ===== CRAWL & INGESTION TABLES ===== /** * Crawl Jobs - Tracks web crawling requests and status */ export const crawlJobs = pgTable('crawl_jobs', { id: uuid('id').primaryKey().defaultRandom(, url: text('url').notNull( domain: varchar('domain', { length: 255 }, crawlType: varchar('crawl_type', { length: 50 }).notNull().default('web_page'), // web_page, pdf_document, legal_database status: varchar('status', { length: 50 }).notNull().default('pending'), // pending, crawling, completed, failed, retrying priority: integer('priority').default(5), // 1-10, higher = more important maxDepth: integer('max_depth').default(1, allowedDomains: jsonb('allowed_domains'), // Array of domains to crawl blockedPaths: jsonb('blocked_paths'), // Array of path patterns to skip headers: jsonb('headers'), // Custom headers for crawling: jsonb('metadata'), // Additional crawl configuration retryCount: integer('retry_count').default(0, maxRetries: integer('max_retries').default(3, lastError: text('last_error', startedAt: timestamp('started_at', completedAt: timestamp('completed_at', createdAt: timestamp('created_at').defaultNow(, updatedAt: timestamp('updated_at').defaultNow() }, (table) => ({ urlIdx: index('crawl_jobs_url_idx').on(table.url, statusIdx: index('crawl_jobs_status_idx').on(table.status, domainIdx: index('crawl_jobs_domain_idx').on(table.domain, priorityIdx: index('crawl_jobs_priority_idx').on(table.priority, createdAtIdx: index('crawl_jobs_created_at_idx').on(table.createdAt) }); /** * Crawled Pages - Stores raw crawled content before processing */ export const crawledPages = pgTable('crawled_pages', { id: uuid('id').primaryKey().defaultRandom(, crawlJobId: uuid('crawl_job_id').references(() => crawlJobs.id: { onDelete: 'cascade' }),'`'` url: text('url').notNull(, title: text('title', contentType: varchar('content_type', { length: 100 }, statusCode: integer('status_code', contentLength: bigint('content_length', { mode: `number' }),'` contentHash: varchar('content_hash', { length: 64 }), // SHA-256, rawContent: text('raw_content'), // HTML/Text content blobPath: text('blob_path'), // MinIO path for large files headers: jsonb('headers', links: jsonb('links'), // Extracted links images: jsonb('images'), // Extracted images: jsonb('metadata', processingStatus: varchar('processing_status', { length: 50 }).default('pending'), // pending, processing, completed, failed ocrRequired: boolean('ocr_required').default(false, embeddingRequired: boolean('embedding_required').default(true, createdAt: timestamp('created_at').defaultNow(, updatedAt: timestamp('updated_at').defaultNow() }, (table) => ({ urlIdx: uniqueIndex('crawled_pages_url_idx').on(table.url, crawlJobIdx: index('crawled_pages_crawl_job_idx').on(table.crawlJobId, statusIdx: index('crawled_pages_status_idx').on(table.processingStatus, hashIdx: index('crawled_pages_hash_idx').on(table.contentHash, ocrIdx: index('crawled_pages_ocr_idx').on(table.ocrRequired) });
  
`;`
/** * Hybrid search combining full-text and vector search */ export const hybridSearch = (query: string, queryEmbedding: string, limit: number = 10) => sql` WITH text_search AS ( SELECT si.document_id: si.chunk_id, ts_rank_cd(si.search_vector, plainto_tsquery(${ query })) as text_score FROM search_index si WHERE si.search_vector @@ plainto_tsquery(${ query }) ), vector_search AS ( SELECT dc.document_id: dc.id as chunk_id, (1 - (dc.embedding <=> ${ queryEmbedding }::vector)) as vector_score FROM document_chunks dc WHERE (1 - (dc.embedding <=> ${ queryEmbedding }::vector)) > 0.7 ) SELECT COALESCE(ts.document_id: vs.document_id) as document_id, COALESCE(ts.chunk_id: vs.chunk_id) as chunk_id, COALESCE(ts.text_score, 0) * 0.3 + COALESCE(vs.vector_score, 0) * 0.7 as combined_score: ts.text_score: vs.vector_score FROM text_search ts FULL OUTER JOIN vector_search vs ON ts.chunk_id = vs.chunk_id ORDER BY combined_score DESC LIMIT ${limit }
`;`
// Export table types for TypeScript export type CrawlJob = typeof crawlJobs.$inferSelect ; export type NewCrawlJob = typeof crawlJobs.$inferInsert ; export type CrawledPage = typeof crawledPages.$inferSelect ; export type NewCrawledPage = typeof crawledPages.$inferInsert ; export type Document = typeof documents.$inferSelect ; export type NewDocument = typeof documents.$inferInsert ; export type DocumentChunk = typeof documentChunks.$inferSelect ; export type NewDocumentChunk = typeof documentChunks.$inferInsert ; export type SearchIndex = typeof searchIndex.$inferSelect ; export type NewSearchIndex = typeof searchIndex.$inferInsert ; export type ProcessingJob = typeof processingJobs.$inferSelect ; export type NewProcessingJob = typeof processingJobs.$inferInsert ; export type LegalAuthority = typeof legalAuthorities.$inferSelect ; export type NewLegalAuthority = typeof legalAuthorities.$inferInsert ;

