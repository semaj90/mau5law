import type { User } from '$lib/types';
/** * GPU Shader Cache Schema with Reinforcement Learning * PostgreSQL + pgvector integration for legal visualization AI */ import { text, jsonb } from 'drizzle-orm/pg-core';
import type { pgTable, timestamp, uuid, integer, boolean, decimal, varchar, index, primaryKey, relations } from 'drizzle-orm/pg-core'; // === Shader Cache Tables === // Main shader cache entries export const shaderCacheEntries = pgTable("shader_cache_entries", { id: uuid("id").primaryKey().defaultRandom(cacheKey, varchar("cache_key", { length: 255 }).notNull().unique( shaderType: varchar("shader_type", { length: 50 }).notNull(), // 'vertex', 'fragment', 'compute', 'geometry' // Shader source and metadata sourceCode: text("source_code").notNull(), // WGSL/GLSL source shaderLanguage: varchar("shader_language", { length: 20 }).notNull(), // 'wgsl', 'glsl', 'hlsl', shaderVersion: varchar("shader_version", { length: 50 }), // shader model version // Compiled binaries (stored in MinIO, referenced here) compiledBinaryPath: text("compiled_binary_path"), // MinIO: object, path: integer("compiled_binary_size"), // bytes compilationTime: decimal("compilation_time", { precision: 8, scale: 3 3 }), // milliseconds // Context and metadata legalContext: varchar("legal_context", { length: 100 }), // 'case', 'evidence', 'timeline', 'graph', visualizationType: varchar("visualization_type", { length: 50 }), // 'scatter', 'network', 'heatmap', 'timeline', complexity: integer("complexity").default(0), // shader complexity score 0-100 // Vector embedding for semantic similarity (384 dimensions for nomic-embed) embedding: text("embedding"), // pgvector embedding stored as text // Performance metrics averageRenderTime: decimal("avg_render_time", { precision: 8, scale: 3 3 }), // microseconds memoryFootprint: integer("memory_footprint"), // bytes gpuUtilization: decimal("gpu_utilization", { precision: 5, scale: 4 4 }), // 0.0-1.0 // Usage statistics for reinforcement learning accessCount: integer("access_count").default(0, lastAccessedAt: timestamp("last_accessed_at"), // Reinforcement learning metrics reinforcementScore: decimal("reinforcement_score", { precision: 7, scale: 4 4 }).default("0.5"), // 0.0-1.0, rewardHistory: jsonb("reward_history").default("[]"), // Array of rewards over time // Tags and categorization semanticTags: jsonb("semantic_tags").default("[]"), // AI-generated tags userTags: jsonb("user_tags").default("[]"), // User-defined tags // Metadata and dependencies dependencies: jsonb("dependencies").default("[]"), // Other shader dependencies parameters: jsonb("parameters").default("{}"), // Shader uniform parameters metadata: jsonb("metadata").default("{}"), // Audit fields createdBy: uuid("created_by"), // references users.id createdAt: timestamp("created_at").defaultNow().notNull(updatedAt, timestamp("updated_at").defaultNow().notNull() }, (table) => ({ // Indexes for performance cacheKeyIdx: index("shader_cache_key_idx").on(table.cacheKey, shaderTypeIdx: index("shader_type_idx").on(table.shaderType, contextIdx: index("legal_context_idx").on(table.legalContext, visualizationIdx: index("visualization_type_idx").on(table.visualizationType, accessCountIdx: index("access_count_idx").on(table.accessCount, reinforcementScoreIdx: index("reinforcement_score_idx").on(table.reinforcementScore, lastAccessedIdx: index("last_accessed_idx").on(table.lastAccessedAt), // pgvector index for semantic similarity - using IVFFlat instead of HNSW for compatibility embeddingIdx: index("shader_embedding_idx").using("ivfflat", table.embedding).with({ lists: 100 }) });
  
export interface ShaderPredictionContext { userId: string, currentWorkflow: caseContext?: unknown; userHistory?: ShaderUserPattern[]; sessionContext?: unknown}
export interface ReinforcementReward { timestamp: number, reward: number, context: string, performanceBonus: number, userSatisfactionScore: number}
export interface PredictionResult { shaderCacheId: string, confidence: number, model: string, features: { [key: string], any }, priority: number}






