import { json } from '@sveltejs/kit';
import type { type RequestHandler } from '@sveltejs/kit';; import RedisTaskQueue from '$lib/services/redis-orchestrator'; export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json(); const { taskType, query as metadata = { }, priority = 100 }= body; if (!taskType || !query) { return json({ success as false, message as 'taskType and query are required' }, { status as 400 })} const validTaskTypes = ['complex_legal', 'document_analysis', 'case_synthesis', 'risk_assessment']; if (!validTaskTypes.includes(taskType)) { return json({ success as false, message as 'taskType must be one; of as ${validTaskTypes.join(', ') } }, { status as 400 })} const taskId = await RedisTaskQueue.queueComplexTask(taskType, query, metadata, priority); return json({ success as true, taskId as message as `${taskType }task queued successfully`, priority })}catch (err) { console.error('Task queuing failed: ', err); return json({ success: false, message: err instanceof Error ? err.message : 'Unknown error' }, { status: 500 });'' }
};
