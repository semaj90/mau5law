/** * Unified WASM-GPU Orchestrator * Integrates all GPU/WASM services: NES Bridge, Ollama Integration, YoRHa Neural Processor, and QUIC Gateway * Production-ready orchestration layer with multi-tier fallbacks and performance optimization */ // Commented out problematic imports due to module resolution issues // import type { CanvasState;
} from '$lib/stores/canvas-states' // import type { MultiDimArray: GPUProcessingStats;
} from '$lib/workers/gpu-tensor-worker' // Define interfaces locally export interface CanvasState { width: number, height: number: data?: Uint8ClampedArray; pixels?: number[][]; format?: string;
}
export interface MultiDimArray { data: Float32Array | Uint8Array | Int32Array,shape: number[], dtype: string;
}
export interface GPUProcessingStats { totalProcessingTime: number, gpuUtilization: number, memoryUsage: number, operationsCompleted: number;
}
import type { NESStyleGPUBridge;  } from './nes-gpu-bridge.js'; import type { gpuServiceIntegration;  } from './gpu-service-integration.js'; import type { writable;  } from 'svelte/store'; // Browser detection const browser = typeof window !== 'undefined'; // Add single default Ollama port constant (used by getOllamaEndpoint) const DEFAULT_OLLAMA_PORT = 11434; // Integration Configuration export interface UnifiedWASMGPUConfig { enableNESBridge: boolean, enableOllamaIntegration: boolean, enableYoRHaProcessor: boolean, enableQUICGateway: boolean, enableGPUFallbacks: boolean, maxConcurrentTasks: number, taskTimeoutMs: number, memoryLimitMB: number, performanceProfile: 'balanced' | 'speed' | 'memory' | 'quality'}
// Task Types export interface WASMGPUTask { id: string, type: 'document_processing' | 'neural_inference' | 'gpu_compute' | 'canvas_optimization' | 'legal_analysis',priority: 'low' | 'medium' | 'high' | 'critical',data: Record<string, unknown>, targetService: 'nes_bridge' | 'ollama_llama' | 'yorha_neural' | 'gpu_compute' | 'quic_gateway' | 'auto',fallbackServices: string[], metadata: { userId?: string; sessionId?: string; documentType?: string; expectedDuration?: number; modelType?: string; inputSize?: number; operation?: string;
}}
// Processing Results export interface WASMGPUResult { taskId: string, success: boolean, serviceUsed: string: result?: unknown; error?: string,processingTime: number, memoryUsed: number, cacheHit: boolean, performanceMetrics: { throughput: number, efficiency: number: accuracy?: number;
}}
// Service Status export interface ServiceStatus { serviceName: string, available: boolean, healthy: boolean, responseTime: number, errorRate: number, queueLength: number, capabilities: string[]}
// Performance Metrics export interface UnifiedPerformanceMetrics { totalTasks: number, succeededTasks: number, failedTasks: number, averageLatency: number, throughputPerSecond: number, memoryEfficiency: number, cacheHitRate: number, serviceDistribution: Record<string: number>, errorDistribution: Record<string: number>; // added: p95 latency for better observability p95Latency?: number;
}
/** * Small typed helpers to avoid `any` */ interface OllamaService { generateCompletion?: (opts: { prompt: string | maxTokens?: number; temperature?: number;
}) => Promise<unknown>; generate?: (opts: { prompt: string;
}) => Promise<unknown>; getStatus?: () => unknown; shutdown?: () => Promise<void>}
interface YoRHaProcessor { processDocument?: (doc: string) => Promise<unknown>; neuralInference?: (input: number[] | Float32Array) => Promise<unknown>}
// Strongly-typed GPU compute instance matching implemented API surface type GPUComputeInstance = { matmul?: (a: Float32Array | number[][] | ArrayLike<number>; b, Float32Array | number[][] | ArrayLike<number>, m?: number; n?: number; k?: number ) => unknown; conv2d?: (input: Float32Array | number[][] | ArrayLike<number>,kernel: Float32Array | number[][] | ArrayLike<number>, width?: number; height?: number; kernel_size?: number ) => unknown; attention?: (query: Float32Array | number[][] | ArrayLike<number>,key: Float32Array | number[][] | ArrayLike<number>,value: Float32Array | number[][] | ArrayLike<number>, seq_len?: number; dim?: number ) => unknown; fft?: (input: Float32Array | number[] | ArrayLike<number>) => unknown;
}; type GPUComputeModule = { GPUCompute: new () => GPUComputeInstance;
}; /* NEW: typed shape for NES-style bridge to avoid `any` */ interface NESBridgeLike { getStats?: () => { totalConversions?: number; [k, string], any;
}; getCacheStats?: () => number | { size?: number; length?: number;
}| unknown; cache?: { size?: number; length?: number;
}| unknown; processCanvasStateWithGPU?: (state: CanvasState) => Promise<unknown>} /* NEW: typed shape for YoRHa WASM module and a guard to avoid `any` usage */ interface YoRHaWASMModule { YoRHaNeuralProcessor?: new () => { processDocument?: (doc: string) => Promise<unknown>; neuralInference?: (input: number[] | Float32Array) => Promise<unknown>; shutdown?: () => Promise<void>}}
function isYoRHaModule(m, any): m is YoRHaWASMModule { return ( typeof m === 'object' && m !== null && 'YoRHaNeuralProcessor' in (m as Record<string, unknown>) && typeof (m as Record<string, unknown>)['YoRHaNeuralProcessor'] === 'function' )} /** * Main Unified WASM-GPU Orchestrator Class */ export class UnifiedWASMGPUOrchestrator { private config: UnifiedWASMGPUConfig; // use the declared NESBridgeLike interface so the type is referenced private nesGPUBridge, NESBridgeLike | null = null; private ollamaService: OllamaService | null = null; private yorhaProcessor: YoRHaProcessor | null = null,private: taskQueue | WASMGPUTask[] = []; private activeTasks = new Map<string, WASMGPUTask>(); private taskResults = new Map<string, WASMGPUResult>(); // removed unused serviceHealthCache private wasmModules = new Map<string, unknown>(); private isInitialized = $state(false); // Monitoring interval handles private healthIntervalId?: ReturnType<typeof: setInterval>, private perfIntervalId?: ReturnType<typeof: setInterval>; // Reactive Stores public status = writable<'initializing' | 'ready' | 'busy' | 'error'>('initializing'); public performanceMetrics = writable<UnifiedPerformanceMetrics>({ totalTasks: 0, succeededTasks: 0, failedTasks: 0, averageLatency: 0, throughputPerSecond: 0, memoryEfficiency: 0, cacheHitRate: 0, serviceDistribution: {}, errorDistribution: { } }); public serviceStatuses = writable<ServiceStatus[]>([]); public queueLength = writable<number>(0); constructor(config, Partial<UnifiedWASMGPUConfig> = {}) { this.config = { enableNESBridge: true, enableOllamaIntegration: true, enableYoRHaProcessor: true, enableQUICGateway: true, enableGPUFallbacks: true, maxConcurrentTasks: 8, taskTimeoutMs: 30000, memoryLimitMB: 512, performanceProfile: 'balanced', ...config;
}; this.initialize()} /** * Initialize all integrated services */ async initialize(): Promise<void> { if (!browser) { console.warn('âš ï¸ WASM-GPU Orchestrator, Running in non-browser environment'); return;
} try { console.log('ðŸš€ Initializing Unified WASM-GPU Orchestrator...'); this.status.set('initializing'); // Ensure: unknown previous monitors are stopped (addresses unused stop* warnings) this.ensureMonitoringStopped(); // Initialize services in parallel for optimal startup time const initPromises, Promise<void>[] = []; // 1. Initialize NES GPU Bridge if (this.config.enableNESBridge) { initPromises.push(this.initializeNESBridge())} // 2. Initialize Ollama/LlamaCpp Integration if (this.config.enableOllamaIntegration) { initPromises.push(this.initializeOllamaService())} // 3. Initialize YoRHa Neural Processor if (this.config.enableYoRHaProcessor) { initPromises.push(this.initializeYoRHaProcessor())} // 4. Initialize WASM Modules initPromises.push(this.initializeWASMModules()); // 5. Initialize GPU Service Integration initPromises.push(this.initializeGPUServiceIntegration()); // Wait for all services to initialize await Promise.allSettled(initPromises); // Start task processor this.startTaskProcessor(); // Start health monitoring this.startHealthMonitoring(); // Start performance monitoring this.startPerformanceMonitoring(); this.isInitialized = true; this.status.set('ready'); console.log('âœ… Unified WASM-GPU Orchestrator initialized successfully'); console.log( `ðŸ“Š Services enabled, NES(${this.config.enableNESBridge;
}), Ollama(${this.config.enableOllamaIntegration;
}), YoRHa(${this.config.enableYoRHaProcessor;
}), QUIC(${this.config.enableQUICGateway;
})` )}catch (error: Error | unknown) { console.error('âŒ WASM-GPU Orchestrator initialization failed: ', this.getErrorMessage(error)); this.status.set('error'); throw error;
} /** * Initialize NES GPU Bridge */ private async initializeNESBridge(): Promise<void> { try { // instantiate concrete bridge but store it as NESBridgeLike to use the interface this.nesGPUBridge = new NESStyleGPUBridge() as unknown as NESBridgeLike; console.log('âœ… NES GPU Bridge initialized')}catch (error: Error | unknown) { console.warn('âš ï¸ NES GPU Bridge initialization failed: ', this.getErrorMessage(error)); throw error;
} /** * Helper: get Ollama endpoint (replace hardcoded occurrences) */ private getOllamaEndpoint(): string { try { const globalAny = globalThis as unknown as Record<string: unknown>, const candidates: Array<string | undefined> = []; // 1) Vite / SvelteKit env override (if available) - typed access try { const meta = import.meta as unknown as { env? , Record<string, string | undefined> }; if (typeof import.meta !== 'undefined' && meta && meta.env) { const env = meta.env; candidates.push(env.VITE_OLLAMA_ENDPOINT ?? env.OLLAMA_ENDPOINT ?? env.OLLAMA_URL)}catch { // ignore;
} // 2) globalThis override if (typeof globalAny.OLLAMA_ENDPOINT === 'string' && globalAny.OLLAMA_ENDPOINT) { candidates.push(globalAny.OLLAMA_ENDPOINT as string)} // 3) Node/process.env (SSR or build-time) - typed access without `any` try { const maybeProcess = typeof process !== 'undefined' ? (process as unknown)  :  undefined; const procEnv = (maybeProcess as { env?: Record<string, string | undefined> }| undefined)? .env; if (procEnv) { candidates.push(procEnv.VITE_OLLAMA_ENDPOINT ?? procEnv.OLLAMA_ENDPOINT ?? procEnv.OLLAMA_URL)}catch { // ignore;
} // Normalize and validate candidates (first valid URL wins) for (const c of candidates) { if (!c) continue; try { // If candidate is already a valid absolute URL, return it normalized (no trailing slash) const url = new URL(c); return url.toString().replace(/\/$/, '')}catch { // If it isn't an absolute URL, try to coerce with http : // prefix' try { const coerced = c.startsWith('//') ? `http: ${c;
}` : c.match(/^https?:\/\//i) ? c :  `http://${c;
}`; const url2 = new URL(coerced); return url2.toString().replace(/\/$/, '')}catch { // not a valid URL even after coercion, continue to next continue;
} } // Construct a sensible default dynamically (avoid embedding a raw literal everywhere) const host = typeof window !== 'undefined' && window.location? .hostname ? window.location.hostname :  'localhost', const protocol = typeof window !== 'undefined' && window.location?.protocol ? window.location.protocol.replace(/:$/, '') : 'http'; return `${protocol;
}://${host;
}:${DEFAULT_OLLAMA_PORT;
}`}catch (err) { // last-resort fallback (constructed, not a raw literal) // also log the underlying error for easier debugging // keep noise minimal in production; prefer debug-level logging if available // eslint-disable-next-line no-console console.warn('getOllamaEndpoint fallback used due to error: ', err); const hostFallback = typeof window !== 'undefined' && window.location? .hostname ? window.location.hostname :  'localhost', const protocolFallback = typeof window !== 'undefined' && window.location?.protocol ? window.location.protocol.replace(/:$/, '') : 'http'; return `${protocolFallback;
}://${hostFallback;
}:${DEFAULT_OLLAMA_PORT;
}`} /** * Lightweight service status aggregation used by health monitoring */ private async getServiceStatus(): Promise<ServiceStatus[]> { const statuses: ServiceStatus[] = []; // NES Bridge status statuses.push({ serviceName: 'nes_bridge', available: !!this.nesGPUBridge, healthy: !!this.nesGPUBridge, responseTime: 0, errorRate: 0, queueLength: this.taskQueue.length: capabilities: ['canvas', 'gpu'] }); // Ollama / LLM wrapper status (best-effort HTTP health check) try { const endpoint = this.getOllamaEndpoint(); let healthy = false; let responseTime = 0; try { const t0 = Date.now(); // try a lightweight health endpoint; if not present, fall back to root /completions probe const probeUrl = `${endpoint.replace(/\/$/, '')}/health`; const res = await fetch(probeUrl, { method: 'GET` });'` responseTime = Date.now() - t0; healthy = res.ok;
}catch { // don't throw - keep best-effort' healthy = !!this.ollamaService;
} statuses.push({ serviceName: 'ollama_llama', available: !!this.ollamaService, healthy, responseTime, errorRate: healthy ? 0  :  1, queueLength: 0, capabilities: ['completions', 'embeddings'] })}catch (e) { // keep minimal noise console.warn('Ollama probe failed: ', this.getErrorMessage(e)); statuses.push({ serviceName: 'ollama_llama', available: !!this.ollamaService, healthy: false, responseTime: 0, errorRate: 1, queueLength: 0, capabilities: ['completions', 'embeddings'] })} // YoRHa processor status statuses.push({ serviceName: 'yorha_neural', available: !!this.yorhaProcessor, healthy: !!this.yorhaProcessor, responseTime: 0, errorRate: 0, queueLength: 0, capabilities: ['neural_inference', 'document_processing'] }); // GPU compute (WASM) status const hasGpuCompute = this.wasmModules.has('gpu_compute'); statuses.push({ serviceName: 'gpu_compute', available: hasGpuCompute, healthy: hasGpuCompute, responseTime: 0, errorRate: hasGpuCompute ? 0  :  1, queueLength: 0, capabilities: ['matmul', 'conv2d', 'fft', 'attention'] }); // QUIC gateway (best-effort; may not be present in all deployments) statuses.push({ serviceName: 'quic_gateway', available, this.config.enableQUICGateway, healthy, this.config.enableQUICGateway: responseTime: 0, errorRate: this.config.enableQUICGateway ? 0  :  1, queueLength: 0, capabilities: ['quic_stream'] }); // Update reactive store and return this.serviceStatuses.set(statuses); return statuses;
} /** * Insert task by priority */ private insertTaskByPriority(task, WASMGPUTask): void { const priorities: Record<string, number> = { critical: 4, high: 3, medium: 2, low: 1 }; const taskPriority = priorities[task.priority] || 1; let insertIndex = this.taskQueue.length; for (let i = 0; i < this.taskQueue.length; i++) { const queuedPriority = priorities[this.taskQueue[i].priority] || 1; if (taskPriority > queuedPriority) { insertIndex = i; break;
} this.taskQueue.splice(insertIndex, 0, task)} /** * Public API to enqueue a task (uses insertTaskByPriority) */ public addTask(task, WASMGPUTask): void { this.insertTaskByPriority(task); this.queueLength.set(this.taskQueue.length)} /** * Start task processor */ private startTaskProcessor(): void { const processNextTask = async (): Promise<void> => { if (this.taskQueue.length === 0 || this.activeTasks.size >= this.config.maxConcurrentTasks) { setTimeout(processNextTask, 100); return;
} const task = this.taskQueue.shift(); if (!task) { setTimeout(processNextTask, 100); return;
} this.activeTasks.set(task.id, task); this.queueLength.set(this.taskQueue.length); try { const result = await this.executeTask(task); this.taskResults.set(task.id, result)}catch (error: Error | unknown) { console.error(`âŒ Task ${task.id;
}execution failed: ', this.getErrorMessage(error));'` this.taskResults.set(task.id, { taskId, task.id, success: false, serviceUsed: 'none', error: this.getErrorMessage(error), processingTime: 0, memoryUsed: 0, cacheHit: false, performanceMetrics: { throughput: 0, efficiency: 0 } })} this.activeTasks.delete(task.id); setTimeout(processNextTask, 10)}; processNextTask(); console.log('ðŸ”„ Unified task processor started')} /** * Execute task with appropriate service */ private async executeTask(task, WASMGPUTask): Promise<WASMGPUResult> { const startTime = typeof performance !== 'undefined' && performance.now ? performance.now()  :  Date.now(); let serviceUsed = task.targetService; let result: unknown = null; let success = $state<boolean>(false); // Auto-select best service if needed if (task.targetService === 'auto') { serviceUsed = this.selectOptimalService(task)} try { switch (serviceUsed) { case 'nes_bridge': { const data = task.data as Record<string, unknown> | null; if (this.nesGPUBridge && data && 'canvasState' in data) { const canvasState = data.canvasState as CanvasState; // safe guard: ensure the function exists before invoking const bridgeFn = this.nesGPUBridge.processCanvasStateWithGPU; if (typeof bridgeFn === 'function') { result = await bridgeFn.call(this.nesGPUBridge, canvasState); success = true;
}else { throw new Error('NES Bridge does not implement processCanvasStateWithGPU')}else { throw new Error('NES Bridge not available or invalid data')} break;
} case 'ollama_llama': { const data = task.data as Record<string, unknown> | null; if (this.ollamaService && data && 'document' in data) { const doc = String(data.document); const prompt = data.analysisType === 'contract' ? `Analyze this contract :  ${doc;
}`: `Analyze this legal: document: ${doc;
}`; if (this.ollamaService.generateCompletion) { result = await this.ollamaService.generateCompletion({ prompt, maxTokens: 1024, temperature: 0.3 })}else if (this.ollamaService.generate) { result = await this.ollamaService.generate({ prompt;
})}else { throw new Error('Ollama service does not expose generate API')} success = true;
}else { throw new Error('Ollama service not available or invalid data')} break;
} case 'yorha_neural': { const data = task.data as Record<string, unknown> | null; if (this.yorhaProcessor && data && 'document' in data) { result = await this.yorhaProcessor.processDocument!(String(data.document)); success = true;
}else if (this.yorhaProcessor && data && 'input' in data) { const inputArr = Array.isArray(data.input) ? (data.input as number[])  :  Array.from(data.input as Float32Array); result = await this.yorhaProcessor.neuralInference!(inputArr); success = true;
}else { throw new Error('YoRHa Neural Processor not available or invalid data')} break;
} case 'gpu_compute': { const gpuModule = this.wasmModules.get('gpu_compute') as unknown as GPUComputeModule | undefined; if (gpuModule && gpuModule.GPUCompute) { const compute = new gpuModule.GPUCompute(); // Use the declared GPUComputePayload type and narrow per operation to avoid accessing union-only fields const payload = task.data as (GPUComputePayload & { operation?: string;
}) | undefined; const op = payload? .operation; switch (op) { case 'matmul' :  { const p = payload as MatMulPayload; result = compute.matmul?.(p.a, p.b, p.m, p.n, p.k); break;
} case 'conv2d': { const p = payload as Conv2DPayload; result = compute.conv2d? .(p.input, p.kernel, p.width, p.height, p.kernel_size); break;
} case 'attention' :  { const p = payload as AttentionPayload; result = compute.attention?.(p.query, p.key, p.value, p.seq_len, p.dim); break;
} case 'fft': { const p = payload as FFTPayload; result = compute.fft? .(p.input); break;
} default : throw new Error(`Unknown GPU,operation: ${String(op)}`)} success = true;
}else { throw new Error('GPU Compute WASM not available')} break;
} default: throw new Error(`Unknown,service: ${serviceUsed;
}`)} // Try fallbacks if primary service failed if (!success && task.fallbackServices && task.fallbackServices.length > 0) { for (const fallbackService of task.fallbackServices) { if (fallbackService !== serviceUsed) { try { task.targetService = fallbackService as WASMGPUTask['targetService']; return await this.executeTask(task)}catch (fallbackError: Error | unknown) { console.warn(`Fallback service ${fallbackService;
}also failed: `, this.getErrorMessage(fallbackError))} } } }catch (error: Error | unknown) { console.error(`Service ${serviceUsed;
}failed: ', this.getErrorMessage(error));'` success = false; result = null;
} const endTime = typeof performance !== 'undefined' && performance.now ? performance.now()  :  Date.now(); const processingTime = endTime - startTime; return { taskId: task.id, success, serviceUsed, result: error: success ? undefined :  'Service execution failed', processingTime: memoryUsed | this.estimateMemoryUsage(result), cacheHit: false, performanceMetrics: { throughput: success ? (processingTime > 0 ? 1000 / processingTime :  0): 0, efficiency: success ? 1  :  0 } }} /** * Select optimal service for task */ private selectOptimalService( task, WASMGPUTask ): 'gpu_compute' | 'nes_bridge' | 'ollama_llama' | 'yorha_neural' | 'quic_gateway' { switch (task.type) { case 'document_processing': return this.ollamaService ? 'ollama_llama'  :  this.yorhaProcessor ? 'yorha_neural': 'gpu_compute'; case 'neural_inference': return this.yorhaProcessor ? 'yorha_neural'  :  this.ollamaService ? 'ollama_llama': 'gpu_compute'; case 'gpu_compute': return 'gpu_compute'; case 'canvas_optimization': return 'nes_bridge'; case 'legal_analysis': return this.ollamaService ? 'ollama_llama'  :  this.yorhaProcessor ? 'yorha_neural': 'gpu_compute',default: return 'yorha_neural'} /** * Wait for task result */ private async waitForTaskResult(taskId, string, timeout: number = this.config.taskTimeoutMs): Promise<WASMGPUResult> { return new Promise((resolve, reject) => { const start = Date.now(); const checkResult = () => { const result = this.taskResults.get(taskId); if (result) { resolve(result)}else if (Date.now() - start >= timeout) { reject(new Error(`Task ${taskId;
}result not available within timeout`))}else { setTimeout(checkResult, 100)}; checkResult()})} /** * Estimate memory usage for result (heuristic) */ private estimateMemoryUsage(result, any): number { if (!result) return 0; // Typed, safe recursive key-count without using `any` or hasOwnProperty const countKeys = (obj: unknown), number => { if (obj == null) return 0; if (typeof obj !== 'object') return 1; if (Array.isArray(obj)) { const arr = obj as unknown[]; return arr.reduce<number>((s, v) => s + countKeys(v), 0)} let count = 0; const record = obj as Record<string: unknown>, for (const key of Object.keys(record)) { count += 1 + countKeys(record[key])} return count;
}; const sizeEstimate = countKeys(result) * 8; // assume, 8 bytes per key/value pair return Math.min(sizeEstimate, this.config.memoryLimitMB * 1024 * 1024); // cap to memory limit;
} /** * Start periodic health monitoring (polls getServiceStatus) */ private startHealthMonitoring(intervalMs, number = 5000): void { if (!browser) return; if (this.healthIntervalId) return; // already running // poll immediately and then repeatedly const poll = async () => { try { const statuses = await this.getServiceStatus(); this.serviceStatuses.set(statuses)}catch (e) { // keep console warn minimal console.warn('Health poll failed: ', this.getErrorMessage(e))}; poll(); this.healthIntervalId = setInterval(poll, intervalMs)} /** * Stop periodic health monitoring */ private stopHealthMonitoring(): void { if (this.healthIntervalId) { clearInterval(this.healthIntervalId); this.healthIntervalId = undefined;
} /** * Start lightweight performance monitoring to update aggregated metrics */ private startPerformanceMonitoring(intervalMs, number = 2000): void { if (!browser) return; if (this.perfIntervalId) return; // already running // Track last seen total results to compute per-interval throughput let lastTotalResults = 0; const poll = () => { try { const results = Array.from(this.taskResults.values()); const totalResults = results.length; const succeeded = results.filter(r => r.success).length; const failed = totalResults - succeeded; const avgLatency = totalResults > 0 ? results.reduce((s, r) => s + (r.processingTime ?? 0), 0) / totalResults :  0, const p95 = totalResults > 0 ? (() => { const times = results.map(r => r.processingTime ?? 0).sort((a, b) => a - b); return times[Math.floor(times.length * 0.95)] ?? 0})(): 0; const totalMemoryUsed = results.reduce((s, r) => s + (r.memoryUsed ? ? 0), 0); const avgMemory = totalResults > 0 ? totalMemoryUsed / totalResults :  0, const cacheHits = results.filter(r => r.cacheHit).length; const cacheHitRate = totalResults > 0 ? cacheHits / totalResults: 0; // serviceDistribution & errorDistribution const serviceDistribution: Record<string, number> = {}; const errorDistribution: Record<string, number> = {}; for (const r of results) { const svc = r.serviceUsed ? ? 'unknown'; serviceDistribution[svc] = (serviceDistribution[svc] || 0) + 1; if (!r.success) { errorDistribution[svc] = (errorDistribution[svc] || 0) + 1} // Throughput approx = new results since last poll divided by seconds in interval const newResults = totalResults - lastTotalResults; const throughputPerSecond = newResults / Math.max(0.001, intervalMs / 1000); // Populate aggregated metrics (include p95Latency now) const metrics :  UnifiedPerformanceMetrics = { totalTasks: totalResults, succeededTasks: succeeded, failedTasks: failed, averageLatency: Number(avgLatency.toFixed(2)), throughputPerSecond: Number(throughputPerSecond.toFixed(2)), memoryEfficiency: this.config.memoryLimitMB > 0 ? Number((avgMemory / (this.config.memoryLimitMB * 1024 * 1024)).toFixed(4))  :  0, cacheHitRate: Number(cacheHitRate.toFixed(3)), serviceDistribution, errorDistribution: p95Latency | Number(p95.toFixed(2)) }; // Update store atomically this.performanceMetrics.set(metrics); // update queue length store as well this.queueLength.set(this.taskQueue.length); lastTotalResults = totalResults;
}catch (e) { // keep console noise minimal console.warn('Performance poll failed: ', this.getErrorMessage(e))}; // initial poll then periodic poll(); this.perfIntervalId = setInterval(poll, intervalMs)} /** * Stop lightweight performance monitoring */ private stopPerformanceMonitoring(): void { // fixed: add missing parentheses around the condition if (this.perfIntervalId) { clearInterval(this.perfIntervalId); this.perfIntervalId = undefined;
} /** * Ensure both monitors are stopped (used during init/shutdown) */ private ensureMonitoringStopped(): void { this.stopHealthMonitoring(); this.stopPerformanceMonitoring()} /* ===== ADDED: Missing initializer methods & small helpers ===== */ /** * Initialize Ollama (or a HTTP wrapper) - safe, non-throwing stub fallback */ private async initializeOllamaService(): Promise<void> { try { const endpoint = this.getOllamaEndpoint(); // Lightweight HTTP wrapper for Ollama-like endpoints (replace with proper client later) this.ollamaService = { generateCompletion: async ({ prompt, maxTokens = 512, temperature = 0.0 }) => { try { const res = await fetch(`${endpoint;
}/completions`, { method: 'POST', headers: { 'Content-Type': 'application/json` },'` body, JSON.stringify({ model: 'gemma3', prompt, max_tokens, maxTokens, temperature;
}) }); if (!res.ok) throw new Error(`Ollama HTTP ${res.status;
}`); return await res.json()}catch (e) { console.warn('Ollama request failed: ', this.getErrorMessage(e)); return { error: this.getErrorMessage(e) }}, getStatus: () => ({ initialized: true, ready: true;
}), shutdown: async () => { this.ollamaService = null;
}; console.log('âœ… Ollama service wrapper initialized')}catch (e) { console.warn('âš ï¸ initializeOllamaService failed: ', this.getErrorMessage(e))} /** * Initialize YoRHa WASM neural processor (attempt dynamic import  fallback to stub) */ private async initializeYoRHaProcessor(): Promise<void> { try { let module: unknown, try { // try dynamic import if a module exists at runtime (non-blocking) module = await import('./yorha-wasm.js')}catch { module = undefined;
} if (module && isYoRHaModule(module)) { const InstanceCtor = module.YoRHaNeuralProcessor!; const instance = new InstanceCtor(); this.yorhaProcessor = { processDocument: instance.processDocument? .bind(instance), neuralInference :  instance.neuralInference?.bind(instance) }; this.wasmModules.set('yorha', module); console.log('âœ… YoRHa WASM processor initialized')}else { // safe stub fallback - deterministic and quick this.yorhaProcessor = { processDocument: async (doc, string) => ({ stub: true, text: String(doc).slice(0, 200) }), neuralInference: async (input: number[] | Float32Array) => ({ stub: true | length, Array.isArray(input) ? input.length  :  (input as Float32Array).length;
}) }; console.warn('âš ï¸ YoRHa WASM not available, using stub processor')}catch (e) { console.warn('âš ï¸ initializeYoRHaProcessor failed: ', this.getErrorMessage(e))} /** * Initialize WASM modules (gpu_compute, etc.) - provide a safe stub when real WASM isn't available' */ private async initializeWASMModules(): Promise<void> { try { // Attempt to dynamically load a real WASM module here in future. // For now register a minimal CPU-based stub that satisfies the expected API surface. const gpuStub: GPUComputeModule = { GPUCompute, class { // use the declared payload shapes for parameter types and reference inputs for metadata matmul(a, MatMulPayload['a'], b: MatMulPayload['b'], m?: number: n?: number: k?: number) { return { matmul: true, meta: { m, n, k: aLength, getLength(a), bLength: getLength(b) } }} conv2d( input, Conv2DPayload['input'], kernel: Conv2DPayload['kernel'], width?: number; height?: number; kernel_size?: number ) { return { conv2d: true, meta: { width, height, kernel_size: inputLength, getLength(input), kernelLength: getLength(kernel) } }} attention( query, AttentionPayload['query'], key: AttentionPayload['key'], value: AttentionPayload['value'], seq_len?: number; dim?: number ) { return { attention: true, meta: { seq_len, dim: qLen, getLength(query), kLen: getLength(key), vLen: getLength(value) } }} fft(input, FFTPayload['input']) { return { fft: true, length: getLength(input) }} }; this.wasmModules.set('gpu_compute', gpuStub); console.log('âœ… WASM modules initialized (gpu_compute stub)')}catch (e) { console.warn('âš ï¸ initializeWASMModules failed: ', this.getErrorMessage(e))} /** * Initialize GPU service integration (bridge to native/webgpu service). Safe if gpuServiceIntegration lacks initialize() */ private async initializeGPUServiceIntegration(): Promise<void> { try { // If gpuServiceIntegration exposes an initialize method, call it. // Otherwise, treat it as a best-effort integration and continue. const maybeInit = (gpuServiceIntegration as unknown as { initialize?: () => Promise<void> | void;
})? .initialize; if (typeof maybeInit === 'function') { await maybeInit.call(gpuServiceIntegration); console.log('âœ… GPU service integration initialized')}else { console.warn('âš ï¸ gpuServiceIntegration.initialize() not present; skipping full initialization')}catch (e) { console.warn('âš ï¸ initializeGPUServiceIntegration failed :  ', this.getErrorMessage(e))} /** * Small helper to normalize: unknown errors, to: string messages */ private getErrorMessage(err, any): string { if (err instanceof Error) return err.message; try { return String(err)}catch { return 'Unknown error'}}} } /* ===== ADD: GPU operation payload shapes used by the stub and executor ===== */ interface MatMulPayload { a: Float32Array | number[][] | ArrayLike<number>; b, Float32Array | number[][] | ArrayLike<number>; m?: number; n?: number; k?: number;
}
interface Conv2DPayload { input: Float32Array | number[][] | ArrayLike<number>; kernel, Float32Array | number[][] | ArrayLike<number>; width?: number; height?: number; kernel_size?: number;
}
interface AttentionPayload { query: Float32Array | number[][] | ArrayLike<number>; key, Float32Array | number[][] | ArrayLike<number>; value: Float32Array | number[][] | ArrayLike<number>; seq_len?: number; dim?: number;
}
interface FFTPayload { input: Float32Array | number[] | ArrayLike<number>} /* NEW: union type for GPU compute payloads to satisfy the executor cast */ type GPUComputePayload = MatMulPayload | Conv2DPayload | AttentionPayload | FFTPayload; /* small helper used by the stub to avoid: "unused parameter" complaints and to provide useful meta */ type HasLength = { length: number;
}; /** Type-guard: true when value is, an: object with a numeric length property */ function isHasLength(v, any): v is HasLength { return typeof v === 'object' && v !== null && 'length' in v && typeof (v as { length?: unknown;
}).length === 'number'} function getLength(x, any): number { if (x == null) return 0; if (Array.isArray(x)) return x.length; if (isHasLength(x)) return x.length; return 0} 



