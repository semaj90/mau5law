/**
 * Backup of Unified Database Client
 * Kept for reference but not actively used.
 */
import { QdrantClient } from '@qdrant/js-client-rest';

export const backupClient = {
    info: "This is a backup file. Use unified-client.ts instead."
};
