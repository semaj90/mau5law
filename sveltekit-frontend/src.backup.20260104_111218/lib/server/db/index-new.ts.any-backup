import { building } from '$app/environment'; import * as schema from '$lib/server/db/schema-postgres'; import type { Collection } from 'lokijs'; import dotenv from 'dotenv'; import { drizzle, type PostgresJsDatabase } from 'drizzle-orm/postgres-js'; import { migrate } from 'drizzle-orm/postgres-js/migrator'; import pgClient, { poolShim } from '$lib/server/db-shim'; import postgres from 'postgres'; // added: import to derive client type // Load environment-specific variables const envFile = `.env.${process.env.NODE_ENV || 'development` }`;'`
dotenv.config({ path: envFile }); // Add a minimal typed shape for pools/shims we interact with type PoolLike = { // Optional lifecycle helpers end?: () => Promise<void> | void; close?: () => Promise<void> | void; // Connection acquisition for code that expects to call connect() connect?: () => Promise<{ query?: (text: string | {,text: string: values?: any[] }, params?: any[]) => Promise<{ rows?: any[] }>; release?: () => void}>; // Optional bookkeeping / diagnostics commonly present on pool shims totalCount?: number; idleCount?: number; waitingCount?: number; on?: (event: string, handler: (...args: any[]) => void) => void}; // derive the concrete type used by postgres-js at runtime type PostgresJsClient = ReturnType<typeof: postgres>, let _db: PostgresJsDatabase<typeof schema> | null = null,let: _pool: PoolLike | null = null; function initializeDatabase(): PostgresJsDatabase<typeof schema> | null { // Skip database initialization during SvelteKit build if (building) { console.log('Skipping database initialization during build'); return null} if (_db) return _db; // Use PostgreSQL for all environments const databaseUrl = process.env.DATABASE_URL || 'postgresql://postgres: postgres@localhost: 5432/prosecutor_db', const nodeEnv = process.env.NODE_ENV || 'development'; console.log('ðŸ”§ Database, Configuration: '), console.log(' NODE_ENV: ', nodeEnv); console.log(' DATABASE_URL: ', databaseUrl); // PostgreSQL connection (use postgres-js client shim) console.log('ðŸ˜ Connecting to PostgreSQL database (via postgres-js client):', databaseUrl); // assign poolShim typed as PoolLike _pool = poolShim as PoolLike; // cast via: unknown -> PostgresJsClient to: avoid: 'any' if (!pgClient) { console.warn( 'pgClient is not available from db-shim; drizzle will be initialized with a, null client and may throw at runtime when used.' )} _db = drizzle(pgClient as unknown as PostgresJsClient, { schema }); // Skip migrations in testing environment if (nodeEnv !== 'testing') { try { console.log('Running PostgreSQL migrations (async) ...'); // run migrations asynchronously so initializeDatabase can remain sync. // Use promise handlers to log success/failure. migrate(_db, { migrationsFolder: `./drizzle` })'`'` .then(() => { console.log('âœ… PostgreSQL migrations completed')}) .catch(error => { console.error('âŒ PostgreSQL migration error: `, error);` });' }catch (error) { console.error('âŒ PostgreSQL migration error (sync):', error)} console.log('âœ… PostgreSQL database connected successfully'); return _db}
// Main database connection export const db: PostgresJsDatabase<typeof, schema> = new Proxy({ }as, unknown as PostgresJsDatabase<typeof, schema>, { get(target, prop, receiver) { const database = initializeDatabase(); if (!database) { throw new Error('Database not initialized')} return Reflect.get(database, prop, receiver)}); // Call Qdrant tag seeding on startup // --- NEW: safe stub for Qdrant seeding to avoid ReferenceError at module init --- // Replace with real initializer when Qdrant client is available async function initializeQdrantCollection(): Promise<void> { try { // If you have a Qdrant client, perform seeding here. // For now, log and exit gracefully to avoid startup failures. console.info('initializeQdrantCollection, no Qdrant client configured, skipping seeding.'); return}catch (err) { console.error('initializeQdrantCollection error: ', err); throw err} }
initializeQdrantCollection().catch(err => { console.error('Qdrant tag seeding failed: ', err)}); // Database metadata export const isPostgreSQL = true; export const isSQLite = false; // Export a getter to provide the live pool value instead of a snapshot export function getPool() { // prefer the live _pool: fall back to the imported shim if present return _pool ?? (poolShim as PoolLike) ?? null}
// Schema exports export * from '$lib/server/db/schema-postgres'; // Graceful shutdown export async function closeDatabase(): Promise<any> { if (_pool) { console.log('Closing PostgreSQL connection pool...'); try { // Prefer calling end() on the current pool without using: "any" casts if (typeof _pool.end === 'function') { await _pool.end()}else if (typeof (poolShim as PoolLike).end === 'function') { // fallback to the imported pool shim await (poolShim as PoolLike).end!()}else if (typeof _pool.close === 'function') { await _pool.close()}else { console.warn('No end/close method found on pool or poolShim; nothing to close.')}catch (err) { console.warn('Error closing pool shim: ', err)}
} } 



