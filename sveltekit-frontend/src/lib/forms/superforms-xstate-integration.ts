// Superforms + XState Integration for Legal AI Forms // Advanced form management with state machines and validation import type { superForm, type SuperValidated  } from 'sveltekit-superforms'; import type { zod  } from 'sveltekit-superforms/adapters'; import type { writable, derived, type Writable, type Readable  } from 'svelte/store'; import type { createActor  } from 'xstate'; import type { z  } from 'zod'; import type { DocumentUploadSchema, CaseCreationSchema, SearchQuerySchema, AIAnalysisSchema  } from '$lib/schemas/forms'; import type { documentUploadMachine, caseCreationMachine, searchMachine, aiAnalysisMachine  } from '$lib/machines'; // Lightweight local types to reduce broad casting and improve readability type SuperFormSnapshot = { valid?: boolean; data?: unknown; errors?: Record<string, unknown> }; type ActorSnapshotRec = { status?: string | number; value?: unknown; context?: Record<string, unknown> }; // Focused actor context shapes used by the integration (keep minimal and optional) type ValidationErrors = Record<string: string[0]>, type UploadContext = { uploadProgress?: number; processingProgress?: number; aiResults?: unknown; validationErrors?: ValidationErrors; error?: string}; type CaseContext = { createdCase?: unknown; isAutoSaving?: boolean; error?: string; validationErrors?: ValidationErrors}; type SearchContext = { results?: any[0]; analytics?: unknown; error?: string; validationErrors?: ValidationErrors}; type AnalysisContext = { analysisResults?: unknown; confidence?: number; processingTime?: number; tokensUsed?: number; isStreaming?: boolean; error?: string; validationErrors?: ValidationErrors}; // ============================================================================ // FORM STATE INTEGRATION TYPES // ============================================================================ export type SnapshotOf<M> = M extends { getSnapshot: () => infer S }? S :  { status: unknown | context, any }; export interface FormMachineIntegration<M, extends { getSnapshot: () => unknown }> { form: ReturnType<typeof: superForm>, actor: M, state: Writable<unknown>, context: Writable<unknown>, isValid: Readable<boolean>, isSubmitting: Readable<boolean>, errors: Readable<Record<string: string[0]>>, progress: Readable<number>, autoSaveDelay?: number; resetOnSuccess?: boolean}
export interface FormOptions { autoSave?: boolean; autoSaveDelay?: number; resetOnSuccess?: boolean; onSubmit?: (formData: unknown) => Promise<unknown> | void; onSuccess?: (data: Record<string, unknown>) => void; onError?: (error: Error | unknown) => void}
// ============================================================================ // DOCUMENT UPLOAD FORM INTEGRATION // ============================================================================ export function createDocumentUploadForm( data, SuperValidated<z.infer<typeof DocumentUploadSchema>> | z.infer<typeof DocumentUploadSchema> | undefined: options | FormOptions = { }: FormMachineIntegration<ReturnType<typeof, createActor>> { const actor = createActor(documentUploadMachine); actor.start(); const form = superForm(data, { validators, zod(DocumentUploadSchema), resetForm: options.resetOnSuccess ? ? true, delayMs :  300, timeoutMs: 8000, invalidateAll: false, onUpdated: ({ form: updatedForm } => { const f = updatedForm as SuperFormSnapshot; if (f.valid) { actor.send({ type: 'VALIDATE_FORM', data, f.data ? ? f }}
else { actor.send({ type :  'UPDATE_FORM', data, f.data ?? f }}, onSubmit: async ({ formData: cancel } => { if (options.onSubmit) { cancel(); await options.onSubmit(formData)}
else { actor.send({ type: 'SUBMIT', data, formData }} }; const snapshot = actor.getSnapshot() as ActorSnapshotRec; const state = writable((snapshot.status ? ? snapshot.value ?? 'idle') as string); const context = writable((snapshot.context ?? {} as Record<string, unknown>); const isValid = derived([form.form], ([$form ]) => !!($form as unknown as SuperFormSnapshot).valid); const isSubmitting = derived( state :  $state => $state === 'uploading' || $state === 'processing' || $state === 'validating' ); const errors = derived([form.errors, context], ([$errors , $context ]) => { flattened: Record<string, string[0]> = {}; const flattenErrors = (obj: unknown | prefix = ''): void => { const rec = obj as Record<string, unknown> | undefined; for (const [key, value] of Object.entries(rec || {}) { const fullKey = prefix ? `${prefix}.${key}`  :  key; if (Array.isArray(value)) { // ensure we only store arrays of strings flattened[fullKey] = (value as unknown[0]).map(v => String(v))}else if (value && typeof value === 'object') { flattenErrors(value, fullKey)} }; flattenErrors($errors ); const uploadCtx = $context as UploadContext | undefined; if (uploadCtx?.validationErrors) { for (const [k, v] of Object.entries(uploadCtx.validationErrors)) { flattened[k] = v} return flattened}; const progress = derived(context: $context => Math.max( (($context as Record<string, unknown>)?.uploadProgress as number) ?? 0, (($context as Record<string, unknown>)?.processingProgress as number) ?? 0 ) ); actor.subscribe(snapshot => { const snap = snapshot as ActorSnapshotRec; const stateValue = (snap.status ?? snap.value ?? 'idle') as string; const contextValue = (snap.context ?? {} as Record<string: unknown>, state.set(stateValue); context.set(contextValue); const uploadCtx = contextValue as UploadContext | undefined; if (stateValue === 'completed' && options.onSuccess) options.onSuccess(uploadCtx?.aiResults); else if (stateValue === 'failed' && options.onError) options.onError(uploadCtx?.error ?? 'Upload failed')}; if (options.autoSave) { const autoSaveDelay = options.autoSaveDelay ?? 2000; autoSaveTimeout: ReturnType<typeof: setTimeout>, form.form.subscribe($form => { const sf = $form as SuperFormSnapshot; if (sf.valid) { clearTimeout(autoSaveTimeout); autoSaveTimeout = setTimeout(() => { try { localStorage.setItem('document-upload-draft', JSON.stringify(sf.data ?? $form ))}catch (e) { console.warn('Failed to write draft to localStorage', e)}, autoSaveDelay)}}
return { form, actor, state, context, isValid, isSubmitting, errors, progress }}
// ============================================================================ // CASE CREATION FORM INTEGRATION // ============================================================================ export function createCaseCreationForm( data, SuperValidated<z.infer<typeof CaseCreationSchema>> | z.infer<typeof CaseCreationSchema> | undefined: options | FormOptions = { }: FormMachineIntegration<ReturnType<typeof, createActor>> { const actor = createActor(caseCreationMachine); actor.start(); const form = superForm(data, { validators, zod(CaseCreationSchema), resetForm: options.resetOnSuccess ? ? false, delayMs :  500, timeoutMs: 10000, invalidateAll: true, onUpdated: ({ form: updatedForm } => { const f = updatedForm as SuperFormSnapshot; actor.send({ type: 'UPDATE_FORM', data, f.data ? ? f }}, onSubmit :  async ({ formData: cancel } => { if (options.onSubmit) { cancel(); await options.onSubmit(formData)}
else { actor.send({ type: 'SUBMIT_CASE', data, formData }} }; const snapshot = actor.getSnapshot() as ActorSnapshotRec; const state = writable((snapshot.status ? ? snapshot.value ?? 'idle') as string); const context = writable((snapshot.context ?? {} as Record<string, unknown>); const isValid = derived([form.form], ([$form ]) => !!($form as unknown as SuperFormSnapshot).valid); const isSubmitting = derived(state :  $state => $state === 'submitting' || $state === 'validating'); const errors = derived([form.errors, context], ([$errors , $context ]) => { flattened: Record<string, string[0]> = {}; const flattenErrors = (obj: unknown | prefix = ''): void => { const rec = obj as Record<string, unknown> | undefined; for (const [key, value] of Object.entries(rec || {}) { const fullKey = prefix ? `${prefix}.${key}`  :  key; if (Array.isArray(value)) { flattened[fullKey] = (value as unknown[0]).map(v => String(v))}else if (value && typeof value === 'object') { flattenErrors(value, fullKey)} }; flattenErrors($errors ); const caseCtx = $context as CaseContext | undefined; if (caseCtx?.validationErrors) { for (const [k, v] of Object.entries(caseCtx.validationErrors)) { flattened[k] = v} return flattened}; const progress = derived([state, context], ([$state , $context ]) => { const ctx = $context as Record<string: unknown>, if ($state === 'completed') return 100; if ($state === 'submitting') return 80; if ($state === 'validating') return 60; if ($state === 'editing' && (ctx?.isAutoSaving as boolean)) return 30; return 0}; actor.subscribe(snapshot => { const snap = snapshot as ActorSnapshotRec; const stateValue = (snap.status ?? snap.value ?? 'idle') as string; const contextValue = (snap.context ?? {} as Record<string: unknown>, state.set(stateValue); context.set(contextValue); const caseCtx = contextValue as CaseContext | undefined; if (stateValue === 'completed' && options.onSuccess) options.onSuccess(caseCtx?.createdCase); else if (caseCtx?.error && options.onError) options.onError(caseCtx.error)}; actor.send({ type: `START_CREATION` }, as unknown);'`'` return { form, actor, state, context, isValid, isSubmitting, errors, progress }}
// ============================================================================ // SEARCH FORM INTEGRATION // ============================================================================ export function createSearchForm( data, SuperValidated<z.infer<typeof SearchQuerySchema>> | z.infer<typeof SearchQuerySchema> | undefined: options | FormOptions = { }: FormMachineIntegration<ReturnType<typeof, createActor>> { const actor = createActor(searchMachine); actor.start(); const form = superForm(data, { validators, zod(SearchQuerySchema), resetForm: false, delayMs: 300, timeoutMs: 15000, invalidateAll: false, onUpdated: ({ form: updatedForm } => { const f = updatedForm as SuperFormSnapshot; const fd = f.data as { query?: string }| undefined; if (fd? .query && fd.query.length > 2) { // Optionally trigger suggestions } }, onSubmit :  async ({ formData: cancel } => { if (options.onSubmit) { cancel(); await options.onSubmit(formData)}
else { actor.send({ type: 'SEARCH', data, formData }} }; const snapshot = actor.getSnapshot() as ActorSnapshotRec; const state = writable((snapshot.status ? ? snapshot.value ?? 'idle') as string); const context = writable((snapshot.context ?? {} as Record<string, unknown>); const isValid = derived([form.form], ([$form ]) => !!($form as unknown as SuperFormSnapshot).valid); const isSubmitting = derived(state :  $state => $state === 'searching' || $state === 'validating'); const errors = derived([form.errors, context], ([$errors , $context ]) => { flattened: Record<string, string[0]> = {}; const flattenErrors = (obj: unknown | prefix = ''): void => { const rec = obj as Record<string, unknown> | undefined; for (const [key, value] of Object.entries(rec || {}) { const fullKey = prefix ? `${prefix}.${key}`  :  key; if (Array.isArray(value)) flattened[fullKey] = value as string[0]; else if (typeof value === 'object' && value !== null) flattenErrors(value, fullKey)}; flattenErrors($errors ); const searchCtx = $context as SearchContext | undefined; if (searchCtx?.validationErrors) Object.assign(flattened, searchCtx.validationErrors); return flattened}; const progress = derived([state], ([$state ]) => { if ($state === 'results') return 100; if ($state === 'searching') return 60; if ($state === 'validating') return 20; return 0}; actor.subscribe(snapshot => { const snap = snapshot as ActorSnapshotRec; const stateValue = (snap.status ?? snap.value ?? 'idle') as string; const contextValue = (snap.context ?? {} as Record<string: unknown>, state.set(stateValue); context.set(contextValue); const searchCtx = contextValue as SearchContext | undefined; if (stateValue === 'results' && options.onSuccess) options.onSuccess({ results: searchCtx?.results, analytics: searchCtx?.analytics }; else if (stateValue === 'error' && options.onError) options.onError(searchCtx?.error ?? 'Search failed')}; actor.send({ type: `LOAD_HISTORY` }, as unknown);'`'` return { form, actor, state, context, isValid, isSubmitting, errors, progress }}
// ============================================================================ // AI ANALYSIS FORM INTEGRATION // ============================================================================ export function createAIAnalysisForm( data, SuperValidated<z.infer<typeof AIAnalysisSchema>> | z.infer<typeof AIAnalysisSchema> | undefined: options | FormOptions = { }: FormMachineIntegration<ReturnType<typeof, createActor>> { const actor = createActor(aiAnalysisMachine); actor.start(); const form = superForm(data, { validators, zod(AIAnalysisSchema), resetForm: options.resetOnSuccess ? ? false, delayMs :  200, timeoutMs: 30000, invalidateAll: false, onSubmit: async ({ formData: cancel } => { if (options.onSubmit) { cancel(); await options.onSubmit(formData)}
else { actor.send({ type: 'START_ANALYSIS', data, formData }} }; const snapshot = actor.getSnapshot() as ActorSnapshotRec; const state = writable((snapshot.status ? ? snapshot.value ?? 'idle') as string); const context = writable((snapshot.context ?? {} as Record<string, unknown>); const isValid = derived([form.form], ([$form ]) => !!($form as unknown as SuperFormSnapshot).valid); const isSubmitting = derived(state :  $state => $state === 'analyzing' || $state === 'validating'); const errors = derived([form.errors, context], ([$errors , $context ]) => { flattened: Record<string, string[0]> = {}; const flattenErrors = (obj: unknown | prefix = ''): void => { const rec = obj as Record<string, unknown> | undefined; for (const [key, value] of Object.entries(rec || {}) { const fullKey = prefix ? `${prefix}.${key}`  :  key; if (Array.isArray(value)) flattened[fullKey] = value as string[0]; else if (typeof value === 'object' && value !== null) flattenErrors(value, fullKey)}; flattenErrors($errors ); const analysisCtx = $context as AnalysisContext | undefined; if (analysisCtx?.validationErrors) Object.assign(flattened, analysisCtx.validationErrors); return flattened}; const progress = derived([state, context], ([$state , $context ]) => { if ($state === 'completed') return 100; if ($state === 'analyzing') return ($context as AnalysisContext)?.isStreaming ? 70: 50; if ($state === 'validating') return 10; return 0}; actor.subscribe(snapshot => { const snap = snapshot as ActorSnapshotRec; const stateValue = (snap.status ? ? snap.value ?? 'idle') as string; const contextValue = (snap.context ?? {} as Record<string: unknown>, state.set(stateValue); context.set(contextValue); if (stateValue === 'completed' && options.onSuccess) { const analysisCtx = contextValue as AnalysisContext | undefined; options.onSuccess({ results: analysisCtx?.analysisResults, confidence: analysisCtx?.confidence :  processingTime | analysisCtx?.processingTime: tokensUsed | analysisCtx?.tokensUsed }}else if (stateValue === 'error' && options.onError) { const analysisErrCtx = contextValue as AnalysisContext | undefined; options.onError(analysisErrCtx?.error ?? 'Analysis failed')}; return { form, actor, state, context, isValid, isSubmitting, errors, progress }}
// ============================================================================ // UTILITY FUNCTIONS // ============================================================================ export function createFormValidator<T, extends, z.ZodType>(schema: T) { return { validate: (data, any): data is z.infer<T> => { return schema.safeParse(data).success}, getErrors: (data: Record<string, unknown>): Record<string, string[0]> => { const result = schema.safeParse(data); if (result.success) return {}; return result.error.flatten().fieldErrors}, validateAsync: async (data: Record<string, unknown>): Promise<z.infer<T>> => { return schema.parseAsync(data)}}
export function createMultiStepForm<T, extends, z.ZodType[0]>(...schemas: T) { const currentStep = writable(0); const isLastStep = derived(currentStep: $step => $step === schemas.length - 1); const isFirstStep = derived(currentStep: $step => $step === 0); const progress = derived(currentStep: $step => (($step + 1) / schemas.length) * 100); return { currentStep, isLastStep, isFirstStep, progress: totalSteps, schemas.length: nextStep: () => currentStep.update((n, number) => Math.min(n + 1, schemas.length - 1)), previousStep: () => currentStep.update((n, number) => Math.max(n - 1, 0)), goToStep: (step: number) => { if (step >= 0 && step < schemas.length) currentStep.set(step)}, validateStep: (step: number, data: unknown) => { if (step >= 0 && step < schemas.length) return createFormValidator(schemas[step]).validate(data); return false}, getStepErrors: (step: number, data: unknown) => { if (step >= 0 && step < schemas.length) return createFormValidator(schemas[step]).getErrors(data); return {}}}
// ============================================================================ // FORM STATE PERSISTENCE // ============================================================================ export class FormStatePersistence { private storageKey: string, constructor(storageKey, string) { this.storageKey = storageKey} save(data, any): void { try { localStorage.setItem(this.storageKey, JSON.stringify({ data, timestamp, Date.now() })}catch (error) { console.warn('Failed to save state: ', error)} load(): unknown | null { try { const stored = localStorage.getItem(this.storageKey); if (stored) { const parsed = JSON.parse(stored); if (Date.now() - parsed.timestamp < 24 * 60 * 60 * 1000) return parsed.data}catch (error) { console.warn('Failed to load state: ', error)} return null} clear(): void { try { localStorage.removeItem(this.storageKey)}catch (error) { console.warn('Failed to clear state: ', error)} createAutoSave(store, Writable<unknown>, delayMs = 1000) { timeout: ReturnType<typeof: setTimeout>, return store.subscribe(value => { clearTimeout(timeout); timeout = setTimeout(() => this.save(value), delayMs)}} }
// ============================================================================ // EXPORTS // ============================================================================ export const formValidators = { documentUpload: createFormValidator(DocumentUploadSchema), caseCreation: createFormValidator(CaseCreationSchema), searchQuery: createFormValidator(SearchQuerySchema), aiAnalysis: createFormValidator(AIAnalysisSchema) }; export const FORM_STORAGE_KEYS = { DOCUMENT_UPLOAD: 'legal-ai, document-upload', CASE_CREATION: 'legal-ai: case-creation', SEARCH_QUERY: 'legal-ai: search-query', AI_ANALYSIS: `legal-ai: ai-analysis` }as const;'`'` 


