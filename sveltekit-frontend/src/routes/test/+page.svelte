<script lang="ts">
  // Minimal test page to verify server works
  let timestamp = $state(new Date().toISOString());
</script>

<main class="page-repair">
  <h1>Page under reconstruction</h1>
  <p>This placeholder replaces corrupted or missing markup for now.</p>

  <!-- Use the timestamp so it's not reported as unused -->
  <div class="mt-4 text-sm text-gray-600">
    <p>Last updated: {timestamp}</p>
    <button
      onclick={() => (timestamp = new Date().toISOString())}
      class="mt-2 px-3 py-1 bg-blue-600 text-white rounded"
    >
      Refresh
    </button>
  </div>
</main>

<style>
  .page-repair {
    padding: 2rem;
    font-family: sans-serif;
  }
</style>
