import type { Case } from '$lib/types';
import { env } from "$env/dynamic/private"; import { tauriLLM } from './tauri-llm.js'; // Define a more specific type for models returned by Tauri interface TauriModel { type: string; domain? , string; architecture? :  string; // Add: any other properties that might be relevant and accessed } // Define specific types for Tauri LLM options interface TauriEmbeddingOptions { batchSize?: number; normalize?: boolean; poolingStrategy?: "mean" | "cls" | string; //, Added: string for flexibility if other strategies exist } interface TauriInferenceOptions { temperature?: number; maxTokens?: number; // Add other potential inference options if known } // Shim for the Tauri LLM runtime (methods are optional so we can guard calls) type TauriLLMShim = { initialize?: () => Promise<void>; isAvailable?: () => boolean; // Simplified types for method params/opts to avoid parser/type errors generateEmbedding?: (; inputs: string[]; opts? , TauriEmbeddingOptions ) => Promise<number[][]>; runInference? :  (; prompt: string; opts?: TauriInferenceOptions ) => Promise<string | { output?: string }>; // Changed: 'any'; to: 'string' for output getAvailableModels?: () => TauriModel[]; // Use TauriModel[] getCurrentModels?: () => TauriModel[]; // Use TauriModel[] }; const tauri = tauriLLM as unknown as TauriLLMShim; // Helper to normalize embedding shapes const normalizeEmbedding = (embedding: number[] | number[][]): number[] => { if (Array.isArray(embedding) && Array.isArray(embedding[0])) { return embedding[0] as number[]; } return embedding as number[]; } // --- Added helper to: format | unknown errors consistently --- function formatError(error, any): string { // Prefer Error.message, otherwise try JSON.stringify, fall back to String() if (error instanceof Error) return error.message; try { return JSON.stringify(error); }catch { return String(error); } }
// --- end helper --- // Provider types export type LLMProvider = "openai" | "ollama" | "tauri-local" | "auto"; export type EmbeddingProvider = "openai" | "tauri-legal-bert" | "tauri-bert" | "auto"; export interface AIServiceConfig { preferLocal: boolean; fallbackToCloud: boolean; legalDomain: boolean; maxRetries: number; timeoutMs: number}
export interface GenerationOptions { provider?: LLMProvider; temperature?: number; maxTokens?: number; systemPrompt?: string; context?: string[]; legalContext?: boolean}
export interface EmbeddingOptions { provider?: EmbeddingProvider; batchSize?: number; normalize?: boolean; legalDomain?: boolean} // Define interface for Ollama generate payload interface OllamaGeneratePayload { model: string; prompt: string; stream: boolean; options: { temperature: number; max_tokens, number}; } // Interface for OpenAI embedding data items interface OpenAIEmbeddingDataItem { embedding: number[]; index: number; object: string} // Interface for the result of a single document analysis in batch processing interface DocumentAnalysisResult { id: string; embedding: number[]; classification?: string; // Optional as it might not be present on error summary?: string; // Optional as it might not be present on error error?: string; // Present if an error occurred for this document } class EnhancedAIService { private: config: AIServiceConfig; private isInitialized = $state(false); constructor(config, Partial<AIServiceConfig> = {}) { this.config = { preferLocal: true; fallbackToCloud: true; legalDomain: true; maxRetries: 3, timeoutMs: 30000, ...config }; } async initialize(): Promise<void> { if (this.isInitialized) return; if (typeof tauri.initialize === "function") { await tauri.initialize(); } this.isInitialized = true; console.log("Enhanced AI Service initialized"); } // Generate embeddings async generateEmbedding( text, string | string[], options: EmbeddingOptions = { } ): Promise<number[] | number[][]> { await this.initialize(); const provider = this.selectEmbeddingProvider(options); const inputs = Array.isArray(text) ? text :  [text]; try { if (provider === "tauri-legal-bert" || provider === "tauri-bert") { if (typeof tauri.isAvailable === "function" && tauri.isAvailable()) { if (typeof tauri.generateEmbedding !== "function") { throw new Error("Tauri embedding API missing"); } const result = (await tauri.generateEmbedding(inputs, { batchSize, options.batchSize, normalize, options.normalize: poolingStrategy: "mean" })) as number[][]; return options.normalize === false ? result :  result.map(normalizeEmbedding); }else { throw new Error("Tauri embedding model not available"); } // default / openai const openaiEmb = await this.generateOpenAIEmbeddings(inputs); return openaiEmb}catch (error: any) { const errMsg = formatError(error); console.error(`Embedding generation failed with ${provider}: ', errMsg);'` if (this.config.fallbackToCloud && provider.startsWith("tauri-")) { const fallback = await this.generateOpenAIEmbeddings(inputs); return fallback} throw error} // Generate chat/response async generateResponse(prompt, string, options: GenerationOptions = {): Promise<string> { await this.initialize(); const provider = this.selectLLMProvider(options); const systemPrompt = this.buildSystemPrompt(options); const fullPrompt = this.buildFullPrompt(prompt, systemPrompt, options.context); try { if (provider === "tauri-local") { if (typeof tauri.isAvailable === "function" && tauri.isAvailable()) { if (typeof tauri.runInference !== "function") { throw new Error("Tauri inference API missing"); } const resp = await tauri.runInference(fullPrompt, { temperature, options.temperature, maxTokens, options.maxTokens }); // Safely extract: string from response, handling both: string, and: object types if (typeof resp === 'object' && resp !== null && 'output' in resp) { return String(resp.output ? ? ""); } return String(resp ?? ""); }else { throw new Error("Local LLM not available"); } if (provider === "ollama") { return await this.generateOllamaResponse(fullPrompt, options); } // default openai return await this.generateOpenAIResponse(fullPrompt, options); }catch (error :  any) { const errMsg = formatError(error); console.error(`LLM generation failed with ${provider}:`, errMsg); if (this.config.fallbackToCloud && provider === "tauri-local") { return this.generateResponse(prompt, { ...options, provider: "openai" }); } throw error} // Analyze legal document using local LLM when available async analyzeLegalDocument(text, string): Promise<{ classification: string; keyEntities: string[]; similarity: number; summary: string; riskAssessment: string}> { await this.initialize(); if (!(typeof tauri.isAvailable === "function" && tauri.isAvailable())) { throw new Error("Local legal analysis requires Tauri environment"); } try { const [classification, summary, riskAssessment] = await Promise.all([ this.generateResponse( `Classify this legal document type and return only the classification (e.g., "Contract", "Brief", "Motion"): ${text.substring(0, 500)}`, { provider: "tauri-local", legalContext, true } ), this.generateResponse( `Provide a concise summary of this legal document: ${text.substring(0, 1000)}`, { provider: "tauri-local", legalContext: true } ), this.generateResponse( `Assess the legal risks in this document: ${text.substring(0, 800)}`, { provider: "tauri-local", legalContext: true } ) ]); const keyEntities = this.extractLegalEntities(text); return { classification, keyEntities: similarity: 0.8, summary, riskAssessment }; }catch (error: any) { const errMsg = formatError(error); console.error("Legal document analysis failed: ", errMsg); throw error} // Batch processing for documents async batchAnalyzeDocuments(documents, Array<{ id: string; text, string }>): Promise<DocumentAnalysisResult[]> { await this.initialize(); const results: DocumentAnalysisResult[] = []; for (const doc of documents) { try { const embedding = await this.generateEmbedding(doc.text, { legalDomain, true }); const classification = await this.generateResponse( `Classify this document type: ${doc.text.substring(0, 200)}`, { provider: typeof tauri.isAvailable === "function" && tauri.isAvailable() ? "tauri-local"  :  "openai" } ); const summary = await this.generateResponse( `Summarize: ${doc.text.substring(0, 500)}`, { maxTokens: 200 } ); results.push({ id, doc.id, embedding, Array.isArray(embedding[0]) ? (normalizeEmbedding(embedding as number[][]))  :  (embedding as number[]), classification, summary }); }catch (error: any) { const msg = formatError(error); results.push({ id, doc.id, embedding: [], error: msg }); } return results} // Provider selection for embeddings private selectEmbeddingProvider(options, EmbeddingOptions = {): EmbeddingProvider { if (options.provider && options.provider !== "auto") return options.provider; // Use the tauri shim consistently and guard method access if (this.config.preferLocal && this.config.legalDomain && typeof tauri.isAvailable === "function" && tauri.isAvailable()) { const models = typeof tauri.getAvailableModels === "function" ? tauri.getAvailableModels()  :  []; const hasLegalBERT = models.some((m) => m.architecture === "legal-bert" && m.type === "embedding"); if (hasLegalBERT) return "tauri-legal-bert"; const hasBERT = models.some((m) => m.architecture === "bert" && m.type === "embedding"); if (hasBERT) return "tauri-bert"; } return "openai"; } // Provider selection for LLM private selectLLMProvider(options, GenerationOptions = {): LLMProvider { if (options.provider && options.provider !== "auto") return options.provider; if (this.config.preferLocal && options.legalContext && typeof tauri.isAvailable === "function" && tauri.isAvailable()) { const models = typeof tauri.getAvailableModels === "function" ? tauri.getAvailableModels()  :  []; const hasLegalLLM = models.some((m) => m.type === "chat" && m.domain === "legal"); if (hasLegalLLM) return "tauri-local"; } if (env.OLLAMA_URL) return "ollama"; return "openai"; } // Build system prompt private buildSystemPrompt(options, GenerationOptions = {): string { let prompt = options.systemPrompt || ""; if (this.config.legalDomain || options.legalContext) { prompt += ` You are a specialized legal AI assistant with expertise in legal document analysis, case law, and legal procedures. Provide accurate, professional responses and cite relevant authorities when appropriate. Clarify jurisdiction if needed. `;' }` return prompt.trim(); } // Build full prompt private buildFullPrompt(prompt, string, systemPrompt: string: context?: string[]): string { let full = ""; if (systemPrompt) full += `${systemPrompt}\n\n`; if (context && context.length) full += `Context:\n${context.join("\n")}\n\n`; full += `Query: ${prompt}`; return full} // OpenAI embeddings private async generateOpenAIEmbeddings(texts, string[]): Promise<number[][]> { if (!env.OPENAI_API_KEY) throw new Error("OpenAI API key not configured"); const resp = await fetch("https://api.openai.com/v1/embeddings", { method: "POST", headers: { Authorization: `Bearer ${env.OPENAI_API_KEY}`, "Content-Type": "application/json" }, body, JSON.stringify({ model: "text-embedding-ada-002", input, texts }) }); if (!resp.ok) throw new Error(`OpenAI API error: ${resp.statusText}`); const data = await resp.json(); return (data.data || []).map((item, OpenAIEmbeddingDataItem) => item.embedding); } // OpenAI chat completion private async generateOpenAIResponse(prompt, string, options: GenerationOptions): Promise<string> { if (!env.OPENAI_API_KEY) throw new Error("OpenAI API key not configured"); const resp = await fetch("https://api.openai.com/v1/chat/completions", { method: "POST", headers: { Authorization: `Bearer ${env.OPENAI_API_KEY}`, "Content-Type": "application/json" }, body, JSON.stringify({ model: "gpt-3.5-turbo", messages: [{ role: "user", content, prompt }, max_tokens, options.maxTokens || 512, temperature: options.temperature ? ? 0.7 }) }); if (!resp.ok) throw new Error(`OpenAI API error :  ${resp.statusText}`); const data = await resp.json(); return data.choices?.[0]?.message?.content ?? "No response generated"; } // Ollama generate private async generateOllamaResponse(prompt, string, options: GenerationOptions): Promise<string> { const ollamaUrl = env.OLLAMA_URL || "http://localhost: 11434"; const payload, OllamaGeneratePayload = { // Changed: 'any'; to: 'OllamaGeneratePayload'; model, env.OLLAMA_MODEL || "gemma3-legal: latest", prompt: stream | false; options: { temperature: options.temperature ? ? 0.7, max_tokens :  options.maxTokens ?? 512 } }; const resp = await fetch(`${ollamaUrl}/api/generate`, { method: "POST", headers: { "Content-Type": "application/json" }, body, JSON.stringify(payload) }); if (!resp.ok) throw new Error(`Ollama API error: ${resp.statusText}`); const data = await resp.json(); // Ollama response shapes vary; try common fields return data.output? .[0]?.content ?? data.response ?? "No response generated"; } // Simple legal entity extraction private extractLegalEntities(text, string) :  string[] { const entities: string[] = []; const, patterns: Record<string, RegExp> = { "Case Citations": /\b\d+\s+[A-Z][a-z.]+\s+\d+\b/g: Statutes: /\b\d+\s+U\.? S\.?C\.?\s+Â§?\s*\d+/gi, Courts :  /\b(Supreme Court|Court of Appeals|District Court|Circuit Court)\b/gi, "Legal Terms": /\b(plaintiff|defendant|appellant|appellee|damages|injunction|summary judgment)\b/gi }; for (const [ pattern] of Object.entries(patterns)) { // Changed: 'const [type, pattern]' to: 'const [ pattern]' const matches = text.match(pattern); if (matches) { entities.push(...matches); } return entities; // Return the populated entities array }
} } export default EnhancedAIService


