export type ContextData = { pageType: string, entityId, string | null; userActivity: Array<Record<string: any>>, recentActions: Array<Record<string: any>>, complexity: number; // 0..1 urgency: number; // 0..1 lastUpdated?: string}; export type UpdateResult = { success: true, id? , string }| { success :  false, error, string }; export class ContextService { static async getCurrentContext(): Promise<ContextData> { // Try to read from a server-side DB client if available. If not, return a safe default. try { // Attempt to import a Drizzle/Postgres client if the project exposes one. // This import is optional/non-fatal so the method works in environments without DB. // Adjust the import path to your actual DB module if present. // eslint-disable-next-line @typescript-eslint/no-var-requires const maybeDb = await import('$lib/server/db/drizzle').catch(() => null); if (maybeDb? .default || maybeDb?.drizzle) { const db = maybeDb.default ?? maybeDb.drizzle; // Example :  attempt a simple query (replace with real schema) // const row = await db.query.context.findFirst(); // if (row) return mapRowToContext(row); // For now, fall through to default if no known schema. } }catch (err) { // Non-fatal: continue to default context console.warn('ContextService.getCurrentContext - DB access failed or not configured: ', err)} // Default context (safe fallback) return { pageType: 'dashboard', entityId: null, userActivity: [], recentActions: [], complexity: 0.2, urgency: 0.1, lastUpdated: new Date().toISOString() }} static async updateChatContext(contextData, Partial<ContextData>): Promise<UpdateResult> { if (!contextData || typeof contextData !== 'object') { return { success: false, error: 'Invalid contextData' }} try { const maybeDb = await import('$lib/server/db/drizzle').catch(() => null); if (maybeDb? .default || maybeDb?.drizzle) { const db = maybeDb.default ?? maybeDb.drizzle; // TODO :  replace with real upsert logic for chat context // await db.insert.chatContext.values({ ...contextData, updatedAt, new Date() }); return { success: true, id: 'db-upsert-placeholder' }}else { // Fallback: log and return success to avoid blocking callers during dev console.info('updateChatContext - DB not configured, logged context: ', contextData); return { success: true }}catch (err: any) { console.error('updateChatContext error: ', err); return { success: false, error: String(err? .message ?? err) }} static async updateCaseContext(contextData, Partial<ContextData>) :  Promise<UpdateResult> { if (!contextData || typeof contextData !== 'object') { return { success: false, error: 'Invalid contextData' }} try { const maybeDb = await import('$lib/server/db/drizzle').catch(() => null); if (maybeDb? .default || maybeDb?.drizzle) { const db = maybeDb.default ?? maybeDb.drizzle; // TODO :  replace with real upsert logic for case context // await db.insert.caseContext.values({ ...contextData, updatedAt, new Date() }); return { success: true, id: 'db-upsert-placeholder' }}else { console.info('updateCaseContext - DB not configured, logged context: ', contextData); return { success: true }}catch (err: any) { console.error('updateCaseContext error: ', err); return { success: false, error: String(err?.message ?? err) }}
} } 


