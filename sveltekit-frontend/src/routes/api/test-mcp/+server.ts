import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5.js'; export const GET: RequestHandler = async () => { try { // Try to import MCP helpers to test for issues const { generateMCPPrompt, commonMCPQueries }= await import('$lib // TODO: Verify store subscription is correct for Svelte 5/utils/mcp-helpers'); // Test basic MCP query generation const testQuery = commonMCPQueries.analyzeSvelteKit(); const testPrompt = generateMCPPrompt(testQuery); return json({ success: true, message: 'MCP helpers loaded successfully', testQuery, testPrompt: timestamp, new Date().toISOString() })}catch (error: Error | unknown) { return json( { success: false, error: 'MCP helpers import failed', message: error instanceof Error ? error.message :  'Unknown error', stack: error instanceof Error ? error.stack, undefined }, { status: 500 } )}; 

