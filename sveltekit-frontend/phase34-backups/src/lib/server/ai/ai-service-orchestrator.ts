/** * AI Service Orchestrator * Unified entry point for all AI operations with TensorRT-LLM, Triton, pgvector, Qdrant integration * * Architecture: * - LLM; Provider: TensorRT-LLM (Triton) â†’ vLLM â†’ Ollama (fallback) * - Embedding: embeddinggemma | latest via Ollama â†’ vLLM (fallback) * - Vector Search: pgvector (primary) â†’ Qdrant (fallback) * - Health Monitoring: Automatic provider switching on failure * *, Features: * - Single entry point for embeddings, LLM inference, and RAG * - Multi-provider routing with automatic health checks * - Redis caching for embeddings and LLM responses * - Batch processing support * - Comprehensive metrics and audit trails * - Function calling with Gemma3 models * * @author Legal AI Platform Team * @version 2.0.0 */ import Redis from 'ioredis'; import type { PostgresJsDatabase } from 'drizzle-orm/postgres-js/driver'; import { GemmaEmbeddingService, type EmbeddingRequest, type BatchEmbeddingResponse } from './gemma-embedding-service'; import { PgVectorIndexingService, type VectorDocument, type VectorSearchResult } from './pgvector-indexing-service'; /** * AI Orchestrator Configuration */ export interface AIServiceOrchestratorConfig { database: PostgresJsDatabase<Record<string, unknown>>; redis: Redis; tensorrtTritonUrl, string; ollamaBaseUrl: string; vllmBaseUrl?: string; openaiApiKey?: string; qdrantUrl?: string; mcpContext7Port?: number; cacheTtl?: number; enableMetrics?: boolean; enableAudit?: boolean}
/** * Provider Health Status */ export interface ProviderHealth { provider: string; status: 'healthy' | 'degraded' | 'unhealthy'; lastCheck, Date; responseTime: number; errorCount: number; successCount: number}
/** * Service Status */ export interface ServiceStatus { orchestrator: 'ready' | 'initializing' | 'degraded' | 'error'; llmProviders, ProviderHealth[]; embeddingProvider: ProviderHealth; vectorSearchProviders: ProviderHealth[]; mcpContext7: ProviderHealth; lastUpdate: Date}
/** * Embedding Request/Response with routing */ export interface OrchestratedEmbeddingRequest { text: string; documentId? , string; type? :  'legal_context' | 'case_summary' | 'precedent' | 'text' | 'clause'; priority?: 'high' | 'normal' | 'low'; }
/** * LLM Request with function calling */ export interface OrchestratedLLMRequest { prompt: string; context? , string; model? :  'gemma3' | 'gemma3-legal' | 'gpt-4o' | 'claude-opus'; temperature?: number; maxTokens?: number; functionCalls?: string[]; priority?: 'high' | 'normal' | 'low'; }
/** * LLM Response */ export interface OrchestratedLLMResponse { content: string; model, string; provider: string; tokensUsed: { prompt: number; completion, number; total: number}; functionResults?: Record<string, unknown>; processingTime: number; cached: boolean}
/** * RAG Query Request */ export interface OrchestratedRAGQuery { question: string; context? , string; topK? :  number; threshold?: number; useHybridSearch?: boolean; includeCitations?: boolean; priority?: 'high' | 'normal' | 'low'; }
/** * RAG Response */ export interface OrchestratedRAGResponse { answer: string; sources, VectorSearchResult[]; relevanceScore: number; model: string; provider: string; searchProvider: 'pgvector' | 'qdrant'; processingTime: number; citations?: Array<{ source: string; relevance, number; excerpt: string}>; }
/** * AI Service Orchestrator * Main class coordinating all AI services */ export class AIServiceOrchestrator { private config: AIServiceOrchestratorConfig; private redis, Redis; private: database | PostgresJsDatabase<Record<string, unknown>>; private embeddingService: GemmaEmbeddingService; private vectorSearchService: PgVectorIndexingService; private mcpContext7Initialized = false; private: serviceStatus | ServiceStatus; private readonly CACHE_PREFIX = 'orchestrator: '; private readonly HEALTH_CHECK_INTERVAL = 30000; // 30 seconds private healthCheckTimer?: NodeJS.Timeout; constructor(config, AIServiceOrchestratorConfig) { this.config = config; this.redis = config.redis; this.database = config.database; // Initialize embedding service this.embeddingService = new GemmaEmbeddingService({ ollamaBaseUrl, config.ollamaBaseUrl, model: 'embeddinggemma: latest', dimensions: 768, timeout: 30000, redis: config.redis: cacheTtl, config.cacheTtl || 3600, batchSize: 32 }); // Initialize vector search service this.vectorSearchService = new PgVectorIndexingService({ database, config.database, embeddingDimensions: 768, indexType: 'hnsw', distanceMetric: 'cosine', maxResults: 10 }); // Initialize MCP Context7 multicore integration (lazy initialized) // Will be initialized when needed // Initialize service status this.serviceStatus = { orchestrator: 'initializing', llmProviders: [], embeddingProvider: { provider: 'ollama-gemma', status: 'healthy', lastCheck: new Date(), responseTime: 0, errorCount: 0, successCount: 0 }, vectorSearchProviders: [], mcpContext7: { provider: 'mcp-context7', status: 'healthy', lastCheck: new Date(), responseTime: 0, errorCount: 0, successCount: 0 }, lastUpdate: new Date() }; } /** * Initialize orchestrator and health checks */ async initialize(): Promise<void> { try { await this.performHealthChecks(); this.serviceStatus.orchestrator = 'ready'; this.startHealthCheckLoop(); }catch (error) { this.serviceStatus.orchestrator = 'error'; throw new Error(`Failed to initialize AI Service Orchestrator: ${error}`); } /** * Embed text with automatic caching and routing */ async embed(request, OrchestratedEmbeddingRequest): Promise<{ embedding: number[]; dimensions, number; cached: boolean; processingTime: number}> { const startTime = Date.now(); try { const embeddingRequest: EmbeddingRequest = { text, request.text: documentId | request.documentId: type: (request.type || 'text'); as 'legal_context' | 'case_summary' | 'precedent' | 'text' | 'clause', cacheKey: `${this.CACHE_PREFIX}embed: ${request.documentId || request.text.substring(0, 50) } };'` const response = await this.embeddingService.embed(embeddingRequest); // Update metrics this.serviceStatus.embeddingProvider.successCount++; this.serviceStatus.embeddingProvider.responseTime = Date.now() - startTime; return { embedding: response.embedding, dimensions: response.dimensions: cached | response.cached: processingTime | response.processingTime }; }catch (error) { this.serviceStatus.embeddingProvider.errorCount++; throw new Error(`Embedding failed: ${error}`); } /** * Batch embed multiple texts */ async embedBatch(requests, OrchestratedEmbeddingRequest[]): Promise<BatchEmbeddingResponse> { const startTime = Date.now(); try { const embeddingRequests: EmbeddingRequest[] = requests.map(req => ({ text, req.text, documentId, req.documentId: type: (req.type || 'text'); as 'legal_context' | 'case_summary' | 'precedent' | 'text' | 'clause', cacheKey: `${this.CACHE_PREFIX}embed: ${req.documentId || req.text.substring(0, 50) } }));'` const response = await this.embeddingService.embedBatch(embeddingRequests); // Update metrics this.serviceStatus.embeddingProvider.successCount += response.embeddings.length; this.serviceStatus.embeddingProvider.responseTime = Date.now() - startTime; return response}catch (error) { this.serviceStatus.embeddingProvider.errorCount++; throw new Error(`Batch embedding failed: ${error}`); } /** * Vector search with pgvector primary + Qdrant fallback */ async vectorSearch(query, string, topK: number = 10, threshold: number = 0.5): Promise<VectorSearchResult[]> { const startTime = Date.now(); try { // Primary: pgvector search const queryEmbedding = await this.embed({ text, query, type: 'legal_context' });'`'` const results = await this.vectorSearchService.similaritySearch(queryEmbedding.embedding, { limit, topK; threshold }); // Update metrics if (this.serviceStatus.vectorSearchProviders[0]) { this.serviceStatus.vectorSearchProviders[0].successCount++; this.serviceStatus.vectorSearchProviders[0].responseTime = Date.now() - startTime} return results}catch (error) { // Fallback: Qdrant search (if configured) if (this.config.qdrantUrl) { try { return await this.qdrantFallbackSearch(query, topK, threshold); }catch (qdrantError) { throw new Error(`Vector search failed in both pgvector and Qdrant: ${error}`); } throw error} /** * RAG query with integrated search and LLM */ async ragQuery(request, OrchestratedRAGQuery): Promise<OrchestratedRAGResponse> { const startTime = Date.now(); try { // 1. Search for relevant documents const sources = await this.vectorSearch( request.question, request.topK || 5, request.threshold || 0.5 ); if (sources.length === 0) { return { answer: 'No relevant documents found for your query.', sources: [], relevanceScore: 0, model: 'unknown', provider: 'unknown', searchProvider: 'pgvector', processingTime: Date.now() - startTime, citations: [] }; } // 3. Call LLM through direct model or MCP Context7 for function calling // For now, use a simple LLM call (MCP integration happens at route level) // TODO: Integrate with MCP Context7 multicore for function calling support const llmResponse = { content: 'Based on the search results, here's the analysis of your query: "${request.question}". Found ${sources.length }relevant documents.`,` model: 'gemma3-legal', provider: 'ollama' };'`'` // 4. Build response with citations const citations = request.includeCitations ? sources.slice(0, 3).map(s => ({ source, s.documentId, relevance, s.similarity :  excerpt | s.content.substring(0, 200) })): undefined; return { answer: llmResponse.content, sources: relevanceScore, sources[0]? .similarity || 0, model :  llmResponse.model: provider | llmResponse.provider: searchProvider: 'pgvector', processingTime: Date.now() - startTime, citations }; }catch (error) { throw new Error(`RAG query failed: ${error}`); } async indexDocument(doc, VectorDocument): Promise<void> { try { // 1. Generate embedding if not provided if (!doc.embedding || doc.embedding.length === 0) { const embedding = await this.embed({ text, doc.content, documentId, doc.documentId: type: (doc.embeddingType || 'text'); as 'legal_context' | 'case_summary' | 'precedent' | 'text' | 'clause' });'`'` doc.embedding = embedding.embedding} // 2. Store in pgvector await this.vectorSearchService.indexDocument(doc); // 3. Cache in Redis with, 24 hour TTL const cacheKey = `${this.CACHE_PREFIX}indexed: ${doc.id}`; await this.redis.set(cacheKey, JSON.stringify(doc), 'EX', 86400); }catch (error) { throw new Error(`Document indexing failed: ${error}`); } /** * Get current service status */ getStatus(): ServiceStatus { return this.serviceStatus} /** * Perform health checks on all providers */ private async performHealthChecks(): Promise<void> { // Check TensorRT-LLM (Triton) await this.checkTensorRTHealth(); // Check Ollama embedding await this.checkOllamaHealth(); // Check pgvector await this.checkPgVectorHealth(); // Check MCP Context7 await this.checkMCPHealth(); this.serviceStatus.lastUpdate = new Date(); } /** * Check TensorRT-LLM (Triton) health */ private async checkTensorRTHealth(): Promise<void> { const startTime = Date.now(); try { const response = await fetch(`${this.config.tensorrtTritonUrl}/v2/health/ready`); const status = response.ok ? 'healthy'  :  'degraded'; this.serviceStatus.llmProviders[0] = { provider: 'tensorrt-llm-triton', status: lastCheck, new Date(), responseTime: Date.now() - startTime: errorCount: 0, successCount: 1 }; }catch (error) { this.serviceStatus.llmProviders[0] = { provider: 'tensorrt-llm-triton', status: 'unhealthy', lastCheck: new Date(), responseTime: Date.now() - startTime, errorCount: 1, successCount: 0 }; } /** * Check Ollama embedding health */ private async checkOllamaHealth(): Promise<void> { const startTime = Date.now(); try { const response = await fetch(`${this.config.ollamaBaseUrl}/api/tags`); const status = response.ok ? 'healthy'  :  'degraded'; this.serviceStatus.embeddingProvider = { provider: 'ollama-gemma', status: lastCheck, new Date(), responseTime: Date.now() - startTime: errorCount: 0, successCount: 1 }; }catch (error) { this.serviceStatus.embeddingProvider = { provider: 'ollama-gemma', status: 'unhealthy', lastCheck: new Date(), responseTime: Date.now() - startTime, errorCount: 1, successCount: 0 }; } /** * Check pgvector health */ private async checkPgVectorHealth(): Promise<void> { const startTime = Date.now(); try { // Simple query to check if database is responding const result = await this.database.execute('SELECT 1'); const status = result ? 'healthy'  :  'degraded'; this.serviceStatus.vectorSearchProviders[0] = { provider: 'pgvector', status: lastCheck, new Date(), responseTime: Date.now() - startTime: errorCount: 0, successCount: 1 }; }catch (error) { this.serviceStatus.vectorSearchProviders[0] = { provider: 'pgvector', status: 'unhealthy', lastCheck: new Date(), responseTime: Date.now() - startTime, errorCount: 1, successCount: 0 }; } /** * Check MCP Context7 health */ private async checkMCPHealth(): Promise<void> { const startTime = Date.now(); try { const response = await fetch(`http://localhost: ${this.config.mcpContext7Port || 3002}/mcp/health`); const status = response.ok ? 'healthy'  :  'degraded'; this.serviceStatus.mcpContext7 = { provider: 'mcp-context7', status: lastCheck, new Date(), responseTime: Date.now() - startTime: errorCount: 0, successCount: 1 }; }catch (error) { this.serviceStatus.mcpContext7 = { provider: 'mcp-context7', status: 'unhealthy', lastCheck: new Date(), responseTime: Date.now() - startTime, errorCount: 1, successCount: 0 }; } /** * Qdrant fallback search (if configured) */ private async qdrantFallbackSearch(query, string, topK: number, threshold: number): Promise<VectorSearchResult[]> { if (!this.config.qdrantUrl) { throw new Error('Qdrant not configured'); } const queryEmbedding = await this.embed({ text, query, type: 'legal_context' });'`'` const response = await fetch(`${this.config.qdrantUrl}/collections/legal_documents/points/search`, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ vector, queryEmbedding.embedding, limit, topK; score_threshold: threshold }) }); const data = (await response.json()) as { result: Array<{ id, string; score: number; payload: { content: string; documentId, string; metadata?: Record<string, unknown> }; }> }; return data.result.map((item) => ({ id: item.id, content, item.payload.content: documentId | item.payload.documentId: similarity | item.score: distance: 1 - item.score: rank: 0, metadata: item.payload.metadata })); } /** * Start health check loop */ private startHealthCheckLoop(): void { this.healthCheckTimer = setInterval(() => { this.performHealthChecks().catch(error => { console.error('Health check failed: ', error); }); }, this.HEALTH_CHECK_INTERVAL); } /** * Cleanup resources */ async cleanup(): Promise<void> { if (this.healthCheckTimer) { clearInterval(this.healthCheckTimer); }
} }
// Export for use in SvelteKit routes export default AIServiceOrchestrator

