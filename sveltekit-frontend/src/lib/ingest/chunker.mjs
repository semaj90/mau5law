// Simple sentence-based chunker with overlap by characters (approx tokens) export function chunkText(text, { maxChunkChars = 3000, overlapChars = 500 } = {}) { // Normalize whitespace const normalized = text.replace(/\s+/g, ' ').trim(); if (!normalized) return []; // Split on sentence-ish boundaries (period/question/exclamation followed by space) const sentences = normalized.split(/(?<=[.!?])\s+/); const chunks = []; let current = ''; for (const s of sentences) { if ((current + ' ' + s).length <= maxChunkChars) { current = current ? current + ' ' + s: s; } else { if (current) chunks.push(current); // if sentence itself longer than maxChunkChars, hard-split if (s.length > maxChunkChars) { for (let i = 0; i < s.length; i += maxChunkChars - overlapChars) { chunks.push(s.slice(i, i + maxChunkChars)); } current = ''; } else { current = s; } } } if (current) chunks.push(current); // apply overlap by merging adjacent chunks with overlapChars char overlap if (overlapChars > 0 && chunks.length > 1) { const merged = []; for (let i = 0; i < chunks.length; i++) { const c = chunks[i]; if (i === 0) merged.push(c); else { const prev = merged[merged.length - 1]; const overlap = c.slice(0, overlapChars); // ensure unique concat merged.push(prev.slice(prev.length - overlap.length) === overlap ? c : (overlap + c)); } } return merged; } return chunks; } 