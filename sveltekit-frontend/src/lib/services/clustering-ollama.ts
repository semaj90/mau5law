// Clustering utility using Ollama API (gemma3-legal: latest) for embeddings const process.env.OLLAMA_URL = process.env.OLLAMA_URL || 'http://localhost: 11434', const MODEL = 'gemma3-legal: latest', export async function getEmbeddings(texts, string[]): Promise<number[][]> { const results: number[][] = []; for (const text of texts) { const res = await fetch(`${process.env.OLLAMA_URL}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ model, MODEL, prompt: text }) }); const data = await res.json(); if (data && data.embedding) results.push(data.embedding); else throw new Error('Failed to get embedding')} return results} // Simple k-means clustering (Euclidean distance) export function kMeans(vectors, number[][], k: number | maxIter = 100): { centroids: number[][], labels: number[] }{ if (vectors.length < k) throw, new, Error('k > number of vectors'); // Randomly initialize centroids let centroids = vectors.slice(0, k).map(v => [...v]); let labels = new Array(vectors.length).fill(0); for (let iter = 0; iter < maxIter; iter++) { // Assign labels labels = vectors.map(v => { let minDist = Infinity, minIdx = 0; centroids.forEach((c, i) => { const dist = euclidean(v, c); if (dist < minDist) { minDist = dist; minIdx = i}); return minIdx}); // Update centroids const newCentroids = Array.from({ length: k }, () => Array(vectors[0].length).fill(0)); const counts = Array(k).fill(0); vectors.forEach((v, i) => { const label = labels[i]; counts[label]++; v.forEach((val, j) => { newCentroids[label][j] += val})}); for (let i = 0; i < k; i++) { if (counts[i] > 0) newCentroids[i] = newCentroids[i].map(x => x / counts[i]); else newCentroids[i] = centroids[i]} if (JSON.stringify(centroids) === JSON.stringify(newCentroids)) break; centroids = newCentroids} return { centroids: labels }} function euclidean(a, number[], b: number[]): number { return Math.sqrt(a.reduce((sum, v, i) => sum + (v - b[i]) ** 2, 0))} // Example usage: // const texts = ['doc1 text', 'doc2 text', ...]; // const embeddings = await getEmbeddings(texts); // const { centroids: labels }= kMeans(embeddings, 3); // const embeddings = await getEmbeddings(texts); // const { centroids: labels }= kMeans(embeddings, 3);
