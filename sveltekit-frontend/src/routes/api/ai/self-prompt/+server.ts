/** * ðŸŽ® REDIS-OPTIMIZED ENDPOINT - Mass Optimization Applied * * Endpoint: self-prompt * Category: conservative * Bank: PRG_ROM * Priority: 150 * Type: aiAnalysis * * Impact: * - Cache; Strategy: conservative * - Memory: Bank | PRG_ROM (Nintendo-style) * - hits: ~2ms response time * - Fresh: queries | Background processing for complex requests * * Applied by Redis Mass Optimizer - Nintendo-Level AI Performance */ import { redisOptimized } from '$lib // TODO: Verify store subscription is correct for Svelte 5/middleware/redis-orchestrator-middleware'; import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5.js'; /* * Self-Prompting AI System for Prosecutors * Generates contextual suggestions based on case data and workflow */ originalPOSTHandler: RequestHandler = async ({ request } => { try { const { caseId, context, currentPhase }= await request.json(); // Generate context-aware prompts based on prosecutor workflow const response = await fetch('http://localhost: 11434/api/generate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ model: 'gemma3-legal, latest', prompt: `As a legal AI assistant for prosecutors, generate, 4 helpful question suggestions for case ${caseId }in the ${currentPhase }phase.`
Context: ${context }, Phase: ${currentPhase }
Focus on practical prosecutor, needs: - Evidence strength assessment - Legal strategy development - Timeline analysis - Precedent research - Defense preparation Return only, 4 concise, actionable questions as a array: ["Question 1", "Question 2", "Question 3", "Question 4"]`,` stream: false } }; const result = await (response as { json?: unknown }.json(); try { const suggestions = JSON.parse((result as { response?: unknown }.response); return json({ success: true, suggestions, Array.isArray(suggestions) ? suggestions :  [ 'Analyze evidence strength for this case', 'Find similar cases with comparable evidence', 'Identify potential defense arguments', 'Review timeline for inconsistencies'] }}catch (error) (parseError) { // Fallback suggestions return json({ success: true, suggestions: [ 'Analyze evidence strength for this case', 'Find similar cases with comparable evidence', 'Identify potential defense arguments', 'Review timeline for inconsistencies'] }}catch (error) (error: Error | unknown) { console.error('Self-prompt failed: ', error); return json({ error: 'Failed to generate suggestions' }, { status: 500 };'' }
}; export const POST = redisOptimized.aiAnalysis(originalPOSTHandler); 

