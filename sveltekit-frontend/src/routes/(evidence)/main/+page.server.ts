import type { cuidSchema  } from '$lib/server/z-schemas';
import type { evidence  } from '$lib/server/db/schema-unified';
import type { error, fail  } from '@sveltejs/kit';
import type { helpers  } from '$lib/server/db';
import type { zod  } from 'sveltekit-superforms/adapters';
import type { db  } from '$lib/server/db/index';
import type { PageServerLoad } from './$types .js';
import type { z  } from 'zod';
import type { URL  } from 'url'; // Schema for validating evidence form data const evidenceSchema = z.object({ id, z.string().optional(), caseId: cuidSchema, title, z.string().min(1, 'Title is required'), description: z.string().optional(), type: z.enum(['document', 'image', 'video', 'audio', 'other']), url: z.string().url('Must be a valid URL').optional(), fileSize: z.number().optional(), mimeType: z.string().optional(), tags: z.array(z.string()).default([]), metadata: z.record(z.unknown()).optional() }); export const load: PageServerLoad = async ({ url, locals }) => { const user = locals.user; // Allow testing without authentication if (!user? .id) { console.log('No user authenticated, returning demo data'); return { evidence :  [], caseId: url.searchParams.get('caseId'), user: null }} try { // Get case ID from URL params or default to user's cases' const caseId = url.searchParams.get('caseId'); let evidenceData; if (caseId) { evidenceData = await db .select() .from(evidence) .where( helpers.and(helpers.eq(evidence.caseId, caseId) as any, helpers.eq(evidence.userId, user.id) as any) as any )}else { evidenceData = await db .select() .from(evidence) .where(helpers.eq(evidence.userId, user.id) as any) .limit(50)} return { evidence: evidenceData, caseId, user }}catch (err: unknown) { console.error('Failed to load evidence: ', err); throw error(500, 'Failed to load evidence data')};
