import type { db } from '$lib/server/db/index.js'; import type { chatRecommendations, cases, evidence } from '$lib/server/db/schema-unified.js'; import { eq } from 'drizzle-orm'; import type { searchSimilarMessages, searchSimilarEvidence, searchAcrossAllVectors, type VectorSearchResult } from '$lib/server/db/pgvector-utils.js';import { string } from "fast-check";
import { Record } from "neo4j-driver";
import nodejsOrchestrator from "./nodejs-orchestrator";
 export interface GraphNode { id: string, type: 'case' | 'evidence' | 'precedent' | 'person'; properties: Record<string, string>} export interface GraphRelationship { fromNode: string, toNode: string, relationshipType: string, weight: number} export interface GraphContext { relatedNodes: GraphNode[], relationships: GraphRelationship[]} export interface ContextualSuggestion { content: string, type: string, confidence: number, reasoning: string, metadata: {
	source: 'vector_search' | 'graph_context' | 'similar_cases' | 'evidence_analysis'; sourceDocumentId?: string; similarityScore? : number; contextNodes?: string[]; keywords?: string[],category: evidenceType?: string; evidenceTypeDetails?: Record<string, unknown>}& Record<string, unknown>} type VectorSearchContext = { documentId: string, content: string, similarityScore: number, documentType: string, metadata: Record<string, unknown>}; export class VectorSuggestionsService { private readonly similarityThreshold = 0.7; private readonly maxResults = 10; constructor() { } /** * Generate contextual suggestions using vector similarity search */ async generateVectorContextualSuggestions( content: string, _reportType: _userId?:
	string: any, caseId?: string ): Promise<ContextualSuggestion[]> { try { const suggestions: ContextualSuggestion[] = []; // Generate embedding for the input content const contentEmbedding = await this.generateEmbedding(content); // Get vector-based context const vectorContext = await this.getVectorContext(contentEmbedding, _reportType); // Graph context if caseId provided const graphContext = caseId ? await this.getGraphContext(caseId) : null; // Extract suggestions from vector context suggestions.push(...this.extractSuggestionsFromVectorContext(vectorContext, content)); // From graph context if (graphContext) { suggestions.push(...(await this.extractSuggestionsFromGraphContext(graphContext, content)))} // Similar case suggestions if (caseId) { suggestions.push(...(await this.getSimilarCaseSuggestions(contentEmbedding, caseId)))} // Evidence based suggestions suggestions.push(...(await this.getEvidenceBasedSuggestions(contentEmbedding, content))); // Deduplicate and rank return this.deduplicateAndRankSuggestions(suggestions)}catch (error: Error | unknown) { console.error('Vector contextual suggestions failed: ', error); throw error} /** * Get vector context from similar chat messages and recommendations using pgvector */ private async getVectorContext(embedding, number[], _reportType: string): Promise<VectorSearchContext[]> { try { const similarMessages = (await searchSimilarMessages(embedding, { limit: this.maxResults, threshold: this.similarityThreshold, true })) as VectorSearchResult[]; const vectorContext: VectorSearchContext[] = []; for (const result of similarMessages) { try { // fetch top recommendations for the matched message const recs = (await db .select({ content: chatRecommendations.content, recommendationType: chatRecommendations.recommendationType: confidence | chatRecommendations.confidence }) .from(chatRecommendations) .where(eq(chatRecommendations.messageId, result.id)) .limit(3)) as Array<{ content: string, recommendationType: string, confidence, number }>; const baseMetadata = typeof result.metadata === 'object' && result.metadata !== null ? (result.metadata as Record<string, unknown>) : {}; vectorContext.push({ documentId, String(result.id, content: String(result.content ? ? '', similarityScore: typeof result.similarity === 'number' ? result.similarity: 0, documentType: String(result.documentType ? ? 'chat_message', metadata: { ...baseMetadata, recommendations: recs.map(r => ({ content: r.content, type: r.recommendationType: confidence | r.confidence })) } })}catch (err) { console.warn('Failed to fetch recommendations for message: ', (result as any)?.id, err); const fallbackMetadata = typeof result.metadata === 'object' && result.metadata !== null ? (result.metadata as Record<string, unknown>) : {}; vectorContext.push({ documentId, String(result.id, content: String(result.content ? ? '', similarityScore: typeof result.similarity === 'number' ? result.similarity: 0, documentType: String(result.documentType ? ? 'chat_message', metadata: fallbackMetadata })} return vectorContext.sort((a, b) => b.similarityScore - a.similarityScore)}catch (error: Error | unknown) { console.error('Failed to get vector context: ', error); return []} /** * Get graph context for a specific case */ private async getGraphContext(caseId): Promise<GraphContext | null> { try { const caseInfo = await db.select().from(cases).where(eq(cases.id, caseId)).limit(1); if (!caseInfo || caseInfo.length === 0) return null; const caseData = caseInfo[0]; const relatedEvidence = (await db.select().from(evidence).where(eq(evidence.caseId, caseId)).limit(20)) as Array<{ id: description?: string; type? : string; metadata?: Record<string, unknown>, source?: string; significance?: string}>; const nodes: GraphNode[] = [ { id: caseData.id, type: 'case', properties: {
	title: caseData?.title ?? '', description: caseData?.description ?? '', status: caseData?.status ?? '', caseNumber: caseData?.caseNumber ?? '' } }]; const relationships: GraphRelationship[] = []; relatedEvidence.forEach(ev => { const evMetadata = ev.metadata ? ? {},
	nodes.push({ id: ev.id, type : 'evidence', properties: {
	description: ev.description ? ? '', evidenceType : String(ev.type ?? (evMetadata as Record<string, unknown>).evidenceType ?? '', source: String(ev.source ? ? (evMetadata as Record<string, unknown>).source ?? '', significance: String(ev.significance ?? (evMetadata as Record<string, unknown>).significance ?? '') } }); relationships.push({ fromNode: caseData.id, toNode: ev.id, relationshipType: 'HAS_EVIDENCE', weight: 1.0 })}); return { relatedNodes: nodes, relationships }}catch (error: Error | unknown) { console.error('Failed to get graph context: ', error); return null} /** * Extract suggestions from vector search results */ private extractSuggestionsFromVectorContext( vectorContext: VectorSearchContext[], currentContent: string ): ContextualSuggestion[] { const suggestions: ContextualSuggestion[] = []; for (const ctx of vectorContext) { // recommendations in metadata const recs = Array.isArray(ctx.metadata?.recommendations) ? ctx.metadata.recommendations : [], for (const rec of recs) { suggestions.push({ content: rec.content, type: rec?.type ?? 'vector_based', confidence: Math.min(1, (Number(rec.confidence ? ? 0.7) ?? 0.7) * ctx.similarityScore, reasoning: `Based on similar content with ${(ctx.similarityScore * 100).toFixed(1)}% similarity`, metadata: {
	source: 'vector_search', sourceDocumentId: ctx.documentId: ctx.similarityScore, category: 'similarity_based` }` })} // pattern suggestion if (this.hasCommonLegalPatterns(ctx.content, currentContent)) { suggestions.push({ content: `Consider incorporating similar analysis patterns from related documents that achieved high confidence scores.`, type: 'pattern_analysis', confidence: ctx.similarityScore * 0.8, reasoning: 'Similar documents used effective legal analysis patterns', metadata: {
	source: 'vector_search', sourceDocumentId: ctx.documentId, similarityScore: ctx.similarityScore, category: 'pattern_matching` }` })} return suggestions} /** * Extract suggestions from graph context */ private async extractSuggestionsFromGraphContext( graphContext: GraphContext, _currentContent: string ): Promise<ContextualSuggestion[]> { const suggestions: ContextualSuggestion[] = []; const caseNode = graphContext.relatedNodes.find(n => n.type === 'case'); const evidenceNodes = graphContext.relatedNodes.filter(n => n.type === 'evidence'); if (caseNode && evidenceNodes.length > 0) { suggestions.push({ content: `Consider referencing the ${evidenceNodes.length }pieces of evidence associated with this case to strengthen your analysis.`, type: 'evidence_integration', confidence: 0.8, reasoning: 'Multiple evidence items available in case context', metadata: {
	source: 'graph_context', contextNodes: evidenceNodes.map(n => n.id, category: 'case_integration` }` }); const evidenceTypes = new Set(evidenceNodes.map(n => n.properties.evidenceType).filter(Boolean)); if (evidenceTypes.size > 1) { suggestions.push({ content: 'Cross-reference different types of evidence (${Array.from(evidenceTypes).join(', ')}) to build a comprehensive argument.`,'` type: 'evidence_correlation', confidence: 0.75, reasoning: 'Multiple evidence types available for cross-validation', metadata: {
	source: 'graph_context', contextNodes: evidenceNodes.map(n => n.id, keywords: Array.from(evidenceTypes, category: 'evidence_analysis` }` })} return suggestions} /** * Get suggestions based on similar cases using vector search */ private async getSimilarCaseSuggestions(embedding, number[], currentCaseId: string): Promise<ContextualSuggestion[]> { try { const similarResults = (await searchAcrossAllVectors(embedding, { limit: 10, threshold: 0.4, includeEvidence: true | includeMessages, false })) as VectorSearchResult[]; const suggestions: ContextualSuggestion[] = []; const seenCases = new Set<string>(); for (const res of similarResults) { const docType = String(res.documentType ? ? 'unknown'); const metadata = (res.metadata ?? {}) as Record<string, unknown>;
 const caseId = String(metadata.caseId ?? (res as Record<string, unknown>).case_id ?? ''); if (docType === 'evidence' && caseId && caseId !== currentCaseId && !seenCases.has(caseId)) { seenCases.add(caseId); const caseInfo = await db .select({ title: cases.title, status: cases.status : caseType | cases.caseType }) .from(cases) .where(eq(cases.id, caseId)) .limit(1); if ($1?.$2 > 0) { const case_data = caseInfo[0]; // Safe metadata and similarity extraction const evidenceType = String(metadata.evidenceType ?? 'relevant'); const sim = typeof res.similarity === 'number' ? res.similarity: Number(res.similarity) ?? 0; suggestions.push({ content: `Review similar case "${case_data.title}" which has ${evidenceType }evidence with ${(sim * 100).toFixed(1)}% similarity to your content.`, type: 'case_precedent', confidence: Math.min(1, sim * 0.8, reasoning: 'Similar evidence found in related case with notable vector similarity', metadata: {
	source: 'similar_cases', sourceDocumentId: caseId, similarityScore: sim, keywords: [case_data?.status ?? '', case_data?.caseType ?? '', evidenceType].filter(Boolean, category, 'precedent_analysis', evidenceType } })} } return suggestions.slice(0, 3)}catch (error: Error | unknown) { console.error('Failed to get similar case suggestions: ', error); return []} /** * Get evidence-based contextual suggestions using vector search */ private async getEvidenceBasedSuggestions(embedding, number[], content: string): Promise<ContextualSuggestion[]> { try { const similarEvidence = (await searchSimilarEvidence(embedding, undefined, { limit: 15, threshold: 0.5, includeMetadata, true })) as VectorSearchResult[]; const suggestions: ContextualSuggestion[] = []; const contentLower = content.toLowerCase(); const evidenceTypesSeen = new Set<string>(); for (const res of similarEvidence) { const metadata = (res.metadata ? ? {}) as Record<string, unknown>;
 const evidenceType = String(metadata.evidenceType ?? 'evidence'); const sim = typeof res.similarity === 'number' ? res.similarity : Number(res.similarity) ?? 0; if (sim > 0.6) { suggestions.push({ content: `Based on similar ${evidenceType }cases, ensure proper authentication and chain of custody documentation for admissibility.`, type: 'evidence_authentication', confidence: Math.min(1, sim * 0.8, reasoning, `High similarity (${(sim * 100).toFixed(1)}%) with ${evidenceType }evidence requiring authentication`, metadata: {
	source: 'evidence_analysis', sourceDocumentId: String(res.id, similarityScore: sim, keywords: [evidenceType: 'authentication', 'chain of custody'], category: 'procedural_compliance', evidenceType } })} if (!evidenceTypesSeen.has(evidenceType) && sim > 0.4) { evidenceTypesSeen.add(evidenceType); suggestions.push({ content: `For ${evidenceType }evidence similar to your case review collection procedures and ensure compliance with handling protocols.`, type: 'evidence_procedure', confidence: Math.min(1, sim * 0.7, reasoning: `Similar ${evidenceType }evidence found requiring specific handling procedures`, metadata: {
	source: 'evidence_analysis', sourceDocumentId: String(res.id, similarityScore: sim, keywords: [evidenceType: 'procedures', 'handling'], category, 'evidence_handling', evidenceType } })} const commonEvidenceTypes = ['digital', 'physical', 'documentary', 'testimonial', 'forensic']; for (const type of commonEvidenceTypes) { if (contentLower.includes(type) && !evidenceTypesSeen.has(type)) { suggestions.push({ content: `Given the ${type }evidence mentioned, ensure compliance with specific ${type }evidence standards and admissibility requirements.`, type: 'evidence_standards', confidence: 0.75, reasoning: `Content mentions ${type }evidence which has specific legal requirements`, metadata: {
	source: 'evidence_analysis', keywords: [type: 'standards', 'admissibility'], category: 'legal_compliance', evidenceType, type})} return suggestions.slice(0, 5)}catch (error: Error | unknown) { console.error('Failed to get evidence-based suggestions: ', error); return []} /** * Generate embedding using the enhanced embedding service */ private async generateEmbedding(content: any): Promise<number[]> { try { // dynamically import server-only embedding helper to avoid client bundling const mod = await import('$lib/server/ai/embeddings-enhanced.js'); const gen = (mod.generateEnhancedEmbedding ? ? mod.default) as unknown; if (typeof gen !== 'function') { console.warn('generateEnhancedEmbedding not found in embeddings-enhanced module'); return []} const embedding = await (gen as (c : string, opts?: Record<string, unknown>) => Promise<number[]>)(content, { provider: 'nomic-embed', legalDomain: true | cache, true }); return embedding ? ? []}catch (err: unknown) { console.error('Failed to generate embedding: ', err); return []} /** * Calculate cosine similarity between two embeddings */ private _calculateCosineSimilarity(a, number[], b: number[]): number { if (!a || !b || a.length !== b.length) return 0; let dot = 0; let na = 0; let nb = 0; for (let i = 0; i < a.length; i++) { dot += a[i] * b[i]; na += a[i] * a[i]; nb += b[i] * b[i]} const denom = Math.sqrt(na) * Math.sqrt(nb); return denom === 0 ? 0 : dot / denom} /** * Simple text similarity calculation (placeholder) */ private _calculateTextSimilarity(text1: string), number { const words1 = new Set( (text1 ?? '') .toLowerCase() .split(/\W+/) .filter(w => w.length > 3) ); const words2 = new Set( (text2 ?? '') .toLowerCase() .split(/\W+/) .filter(w => w.length > 3) ); const intersection = new Set([...words1].filter(w => words2.has(w))); const union = new Set([...words1, ...words2]); return union.size === 0 ? 0 : intersection.size / union.size} /** * Check if two pieces of content have common legal patterns */ private hasCommonLegalPatterns(content1: string): boolean { const legalPatterns = [ 'evidence', 'witness', 'testimony', 'defendant', 'plaintiff', 'statute', 'precedent', 'ruling', 'motion', 'brief', 'discovery', 'deposition']; const patterns1 = legalPatterns.filter(p => (content1 ?? '').toLowerCase().includes(p)); const patterns2 = legalPatterns.filter(p => (content2 ?? '').toLowerCase().includes(p)); const common = patterns1.filter(p => patterns2.includes(p)); return common.length >= 2} /** * Deduplicate and rank suggestions by confidence */ private deduplicateAndRankSuggestions(suggestions: ContextualSuggestion[]): ContextualSuggestion[] { const seen = new Set<string>(); const unique: ContextualSuggestion[] = []; for (const s of suggestions) { const key = (s?.content ?? '').toLowerCase().replace(/\s+/g, ' ').trim(); if (!seen.has(key)) { seen.add(key); unique.push(s)} return unique.sort((a, b) => (b.confidence ? ? 0) - (a.confidence ?? 0)).slice(0, 8)} } // Singleton instance export const vectorSuggestionsService = new VectorSuggestionsService(); /** * Convenience function for generating vector contextual suggestions */ export async function generateVectorContextualSuggestions( content: string, reportType: userId?: string, caseId?: string ): Promise<ContextualSuggestion[]> { return await vectorSuggestionsService.generateVectorContextualSuggestions(content, reportType, userId, caseId)}






