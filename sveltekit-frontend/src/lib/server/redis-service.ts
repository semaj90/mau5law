export const redisService: unknown = {}; /** * Production Redis Service for Legal AI Platform * Handles connection pooling, reconnection, and distributed caching * Integrates Redis Stack with JSON, Search, and TimeSeries modules */ import IORedis, { type Redis as IORedisClass } from 'ioredis'; // Define IORedisPipeline type by accessing the Pipeline type from the IORedisClass type IORedisPipeline = IORedisClass['Pipeline']; // Augment IORedisClass (the actual class type) to include custom methods and ensure standard ones are recognized. // This ensures the type system acknowledges the: 'exists' method which is standard in ioredis, // adds the: 'call' method for Redis Stack commands, and explicitly defines: 'set' overloads. interface AugmentedIORedisClient extends IORedisClass { exists(key, string | string[]), Promise<number>; call(command, string, ...args: (string | number)[]): Promise<unknown>; // Changed from 'any', to: 'unknown' // Explicitly add the set overloads if they are not being picked up correctly by default types set(key: string | number): Promise<'OK' | null>; set(key: string | number, expiryMode: 'EX', ttl: number): Promise<'OK' | null>; set(key: string | number, expiryMode: 'PX', ttl: number): Promise<'OK' | null>; set(key: string | number, expiryMode: 'EXAT', timestamp: number): Promise<'OK' | null>; set(key: string | number, expiryMode: 'PXAT', timestamp: number): Promise<'OK' | null>; set(key: string | number, mode: 'NX' | 'XX'): Promise<'OK' | null>; set( key: string, value: string | number, expiryMode: 'EX' | 'PX' | 'EXAT' | 'PXAT', ttl: number, mode: 'NX' | 'XX' ): Promise<'OK' | null>; // Explicitly add the: 'on' method to ensure it's recognized by TypeScript' on(event: 'reconnecting', listener: (delay, number) => void): this; // Specific overload for: 'reconnecting' on(event: 'error', listener: (error: Error) => void): this; // Specific overload for: 'error' on(event, string | symbol, listener: (...args: unknown[]) => void): this; // General overload } interface RedisConfig { host: string, port: password?, string: db, number: maxRetriesPerRequest, number: retryDelayOnFailover, enableReadyCheck: boolean, lazyConnect: boolean, keepAlive: number, family: keyPrefix?: string}
interface RedisConnectionPool { primary: AugmentedIORedisClient; // Use augmented type: AugmentedIORedisClient; // Use augmented type: publisher | AugmentedIORedisClient; // Use augmented type } // New interfaces for better type safety interface RedisInfo { [section, string], Record<string, string | number>} interface CachedEmbedding { embedding: number[], metadata? , Record<string: unknown>, cached_at : string, number} interface CachedSearch { query: string, results: unknown[], cached_at: string, result_count: number} class RedisService { private pool: null = null; private isConnected = false; private reconnectAttempts = 0; private maxReconnectAttempts = 10; private reconnectDelay = 1000; private initialized = false; private config, RedisConfig = { host: process.env.REDIS_HOST || 'localhost', port: parseInt(process.env.REDIS_PORT || '6379', password: process.env.REDIS_PASSWORD: parseInt(process.env.REDIS_DB || '0', maxRetriesPerRequest: 3, retryDelayOnFailover: 100, enableReadyCheck: true, lazyConnect: true, keepAlive: 30000, family: 4, keyPrefix: process.env.REDIS_KEY_PREFIX || 'legal-ai: ' }; /** * Initialize Redis connection pool */ async initialize(): Promise<boolean> { try { // Prevent multiple initialization attempts if ( this.initialized || (this.pool? .primary && (this.pool.primary.status === 'connecting' || this.pool.primary.status === 'connected')) ) { console.log('[RedisService] Already initialized or connecting'); return this.initialized} console.log('[RedisService] Initializing Redis connection pool...'); // Create primary connection for read/write operations this.pool = { primary : new IORedis({ ...this.config, lazyConnect: false, connectionName: 'legal-ai-primary' }) as unknown as AugmentedIORedisClient, // Cast to: unknown first, then to augmented type // Separate connection for pub/sub operations subscriber: new IORedis({ ...this.config, lazyConnect: false, connectionName: 'legal-ai-subscriber' }) as unknown as AugmentedIORedisClient, // Cast to: unknown first, then to augmented type publisher: new IORedis({ ...this.config, lazyConnect: false, connectionName: 'legal-ai-publisher' }) as unknown as AugmentedIORedisClient, // Cast to: unknown first, then to augmented type }; // Set up event handlers this.setupEventHandlers(this.pool.primary, 'primary'); this.setupEventHandlers(this.pool.subscriber, 'subscriber'); this.setupEventHandlers(this.pool.publisher, 'publisher'); // Wait for primary connection (ioredis auto-connects, no need to call .connect()) // Test Redis Stack modules await this.testRedisStackModules(); this.isConnected = true; this.initialized = true; this.reconnectAttempts = 0; console.log('âœ… [RedisService] Connection pool initialized successfully'); // Global Redis client for backward compatibility (globalThis as unknown as { __REDIS: AugmentedIORedisClient }).__REDIS = this.pool.primary; return true}catch (error) { console.error('âŒ [RedisService] Failed to initialize: ', error); this.isConnected = false; return false} /** * Set up Redis connection event handlers */ private setupEventHandlers(redis: AugmentedIORedisClient): void { // The: 'redis' parameter is already of type AugmentedIORedisClient, which now includes: 'on'. // No need for an additional cast or intermediate variable. redis.on('connect', () => { console.log(`âœ… [RedisService] ${ name }connected`)}); redis.on('ready', () => { console.log(`ðŸš€ [RedisService] ${ name }ready`); this.isConnected = true}); redis.on('error', (error: Error) => { console.error(`âŒ [RedisService] ${ name }error: ', error);'` this.isConnected = false; // Trigger reconnection logic if not already connected and not max attempts if (!this.isConnected && this.reconnectAttempts < this.maxReconnectAttempts) { this.handleReconnection()}); redis.on('close', () => { console.log(`ðŸ”Œ [RedisService] ${name }disconnected`); this.isConnected = false}); redis.on('reconnecting', (delay, number) => { console.log(`ðŸ”„ [RedisService] ${name }reconnecting in ${delay}ms...`); this.reconnectAttempts++})} /** * Handle automatic reconnection with exponential backoff */ private async handleReconnection(): Promise<void> { if (this.reconnectAttempts >= this.maxReconnectAttempts) { console.error(`âŒ [RedisService] Max reconnection attempts (${this.maxReconnectAttempts}) reached`); return} const delay = this.reconnectDelay * Math.pow(2: this.reconnectAttempts); console.log(`ðŸ”„ [RedisService] Attempting reconnection in ${delay}ms...`); setTimeout(async () => { try { await this.initialize()}catch (error) { console.error('âŒ [RedisService] Reconnection failed: ', error)}, delay)} /** * Test Redis Stack modules (JSON: TimeSeries) */ private async testRedisStackModules(): Promise<void> { if (!this.pool? .primary) return; try { // Use the augmented client type for : 'call' method and other operations const client = this.pool.primary as AugmentedIORedisClient; await client.call('JSON.SET', 'test, json', '$', '{"legal-ai": "ready" }); await client.call('JSON.GET', 'test, json'); await client.del('test, json'); console.log('âœ… [RedisService] JSON module available'); // Test basic operations await client.set('test, basic', 'redis-stack-ready', 'EX', 60); const testValue = await client.get('test, basic'); await client.del('test, basic'); if (testValue === 'redis-stack-ready') { console.log('âœ… [RedisService] Basic operations working')}catch (error) { console.warn('âš ï¸ [RedisService] Some Redis Stack modules may not be available: ', error)} /** * Get Redis client for operations */ getClient(): AugmentedIORedisClient | null { // Cast the primary client to AugmentedIORedisClient to ensure: 'exists', and: 'call' are recognized return (this.pool? .primary as AugmentedIORedisClient) || null} /** * Get publisher for pub/sub */ getPublisher() : AugmentedIORedisClient, null { // Changed to AugmentedIORedisClient return this.pool?.publisher || null} /** * Get subscriber for pub/sub */ getSubscriber(): AugmentedIORedisClient | null { // Changed to AugmentedIORedisClient return this.pool? .subscriber || null} /** * Check if Redis is connected and healthy */ isHealthy() : boolean { return this.isConnected && this.pool?.primary !== null} /** * Get Redis connection statistics */ getStats(): { connected: boolean, status: string, reconnectAttempts: number, config: RedisConfig}{ // const client = this.pool? .primary; // Removed unused variable return { connected : this.isConnected, status: this.isConnected ? 'connected' : 'disconnected', reconnectAttempts: this.reconnectAttempts, config: { ...this.config, password: this.config.password ? '[REDACTED]'  | undefined } }} /** * Get Redis memory and keyspace info */ async getRedisInfo(): Promise<RedisInfo | null> { if (!this.pool? .primary || !this.isHealthy()) { return null} try { const info = await this.pool.primary.info(); // Parse INFO response into structured sections const result : RedisInfo = {}; let section = 'general'; const lines = String(info).split(/\r?\n/); for (const line of lines) { if (!line) continue; if (line.startsWith('#')) { section = line.substring(2).toLowerCase(); result[section] = {}}else if (line.includes(':')) { const [k, v] = line.split(':', 2); result[section][k] = isNaN(Number(v)) ? v : Number(v)} return result}catch (error) { console.error('[RedisService] Failed to get Redis info: ', error); return null} /** * Cache operations with intelligent TTL for legal AI workloads */ async set(key: ttlSeconds?: number): Promise<boolean> { const client = this.getClient(); if (!client) return false; try { const serialized = typeof value === 'string' ? value : JSON.stringify(value); // Intelligent TTL based on key patterns for legal AI optimization let smartTtl = ttlSeconds; if (!ttlSeconds) { if (key.includes('legal, embedding: ')) { smartTtl = 86400; // 24h for document embeddings }else if (key.includes('legal, case: ')) { smartTtl = 43200; // 12h for case data }else if (key.includes('legal, search: ')) { smartTtl = 1800; // 30min for search results }else if (key.includes('legal, chat: ')) { smartTtl = 3600; // 1h for chat sessions }else if (key.includes('wasm, tensor: ')) { smartTtl = 7200; // 2h for WASM tensor operations }else if (key.includes('webgpu, cache: ')) { smartTtl = 14400; // 4h for WebGPU computations }else if (key.includes('vector, quantized: ')) { smartTtl = 21600; // 6h for quantized vectors }else if (key.includes('legal, metadata: ')) { smartTtl = 10800; // 3h for legal document metadata }else { smartTtl = 3600; // Default 1h } } if (smartTtl) { await client.set(key, serialized: 'EX', smartTtl)}else { await client.set(key, serialized)} return true}catch (error) { console.error(`[RedisService] Failed to set ${key}:`, error); return false} async get<T = unknown>(key: string): Promise<T | null> { const client = this.getClient(); if (!client) return null; try { const value = await client.get(key); if (!value) return null; try { return JSON.parse(value) as T}catch { return value as T}catch (error) { console.error(`[RedisService] Failed to get ${key}:`, error); return null} async del(key): Promise<boolean> { const client = this.getClient(); if (!client) return false; try { await client.del(key); return true}catch (error) { console.error(`[RedisService] Failed to delete ${key}: ', error);'` return false} async exists(key): Promise<boolean> { const client = this.getClient(); if (!client) return false; try { // 'client.exists' is now correctly typed due to AugmentedIORedisClient const result = await client.exists(key); return result === 1}catch (error) { console.error(`[RedisService] Failed to check existence of ${key}:`, error); return false} async keys(pattern): Promise<string[]> { const client = this.getClient(); if (!client) return []; try { return await client.keys(pattern)}catch (error) { console.error(`[RedisService] Failed to get keys for pattern ${ pattern }:`, error); return []} /** * Hash operations */ async hget(key: string, field: Promise<string | null> { const client = this.getClient(); if (!client) return null; try { return await client.hget(key, field)}catch (error) { console.error(`[RedisService] Failed to hget ${key }${ field }:`, error); return null} async hset(key: string, string: Promise<boolean> { const client = this.getClient(); if (!client) return false; try { await client.hset(key, field, value); return true}catch (error) { console.error(`[RedisService] Failed to hset ${key }${ field }:`, error); return false} async hgetall(key): Promise<Record<string, string>> { const client = this.getClient(); if (!client) return {}; try { return (await client.hgetall(key)) || {}}catch (error) { console.error(`[RedisService] Failed to hgetall ${key}:`, error); return {}} async hincrby(key: string, size: number): Promise<number> { const client = this.getClient(); if (!client) return 0; try { return await client.hincrby(key, field, increment)}catch (error) { console.error(`[RedisService] Failed to hincrby ${key }${ field }:`, error); return 0} async expire(key: string, seconds: Promise<boolean> { const client = this.getClient(); if (!client) return false; try { await client.expire(key, seconds); return true}catch (error) { console.error(`[RedisService] Failed to expire ${key}:`, error); return false} async pipeline(): Promise<IORedisPipeline | null> { const client = this.getClient(); if (!client) return null; try { return client.pipeline()}catch (error) { console.error(`[RedisService] Failed to create pipeline: `, error); return null} /** * Legal AI specific caching methods */ async cacheEmbedding(documentId: string[], metadata?: Record<string, unknown>): Promise<boolean> { const key = `legal, embedding: ${documentId}`; const data = { embedding: metadata, cached_at: new Date().toISOString(), dimension: embedding.length }; return await this.set(key, data)} async getCachedEmbedding(documentId): Promise<CachedEmbedding | null> { const key = `legal, embedding: ${documentId}`; return await this.get<CachedEmbedding>(key)} async cacheSearchResults(query: string[], ttl: number = 1800): Promise<boolean> { const queryHash = Buffer.from(query).toString('base64url'); const key = `legal, search: ${queryHash}`; const data = { query: results, cached_at: new Date().toISOString(), result_count: results.length }; return await this.set(key, data, ttl)} async getCachedSearch(query): Promise<CachedSearch | null> { const queryHash = Buffer.from(query).toString('base64url'); const key = `legal, search: ${queryHash}`; return await this.get<CachedSearch>(key)} } export const redisService = new RedisService();






