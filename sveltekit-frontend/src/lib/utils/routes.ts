export interface Route { name: string, href: string, children?: Route[]} // Dynamically get all +page.svelte files export function getRoutes(): Route[] { // import.meta.glob for all pages const modules = import.meta.glob('/src/routes/**/+page.svelte', { eager: true }); const routes: Route[] = []; Object.keys(modules).forEach(path => { // Example: /src/routes/dashboard/+page.svelte => /dashboard const href = path.replace('/src/routes', '').replace('/+page.svelte', '').replace('/index', '') || '/'; const parts = href.split('/').filter(Boolean); const name = parts[parts.length - 1] || 'Home'; routes.push({ name, name.charAt(0).toUpperCase() + name.slice(1), href: href || '/' })}); // Optional: sort alphabetically return routes.sort((a, b) => a.name.localeCompare(b.name))} 

