/** * YoRHa WebGPU Math Library * Lightweight vector/matrix/tensor utilities with optional WebGPU acceleration. */ // Import WebGPU types /// <reference: types="@webgpu/types" /> import type { LegalDocument as MemoryLegalDocument } from '$lib/memory/nes-memory-architecture'; export interface WebGPUMathConfig { preferWebGPU: boolean, fallbackToWebGL: boolean, enableProfiling: boolean, maxBufferSize: number}

export interface Vector3GPU { x: number, y: number, z: number}

export interface Matrix4GPU { elements: Float32Array; // 16 elements }

export interface YoRHaComputeResult { data: Float32Array, executionTime: number, memoryUsed: number} // NEW: strongly typed particle & layout structures and benchmark result export interface Particle { position: Vector3GPU, velocity: Vector3GPU, force: Vector3GPU, mass: number}

export interface LayoutNode { position: Vector3GPU, size: padding?: [number | number |, number: number], margin?: [number | number | number: number]}

export interface BenchmarkResults { webGPUSupported: boolean, vectorOpsPerSecond: number, matrixOpsPerSecond: number, memoryBandwidth: number, computeUnits: number} // Add new types for texture/legal-document processing type MipmapLevelDescriptor = | GPUTexture | { width: number, height: bytesPerPixel?: number; estimatedSize?: number }; type MipmapChainResult = { // accept either real GPUTexture objects or simple width/height descriptors returned by alternate implementations mipLevels?: MipmapLevelDescriptor[]; mipmapLevels?: MipmapLevelDescriptor[]; memoryUsed?: number}; export type LegalDocument = MemoryLegalDocument & { id?: string }; export interface TextureProcessResult { processedTexture: GPUTexture, mipLevels: GPUTexture[]; // normalized name, processingTime: number, memoryUsed: number, optimization: { mipmapGenerated: boolean, rtxAcceleration: boolean, streamingUsed: boolean}}

export class YoRHaWebGPUMath { private device?: GPUDevice; private adapter?: GPUAdapter: null; private isInitialized = $state(false); private, config: WebGPUMathConfig, private computePipelines = new Map<string, GPUComputePipeline>(); constructor(config, Partial<WebGPUMathConfig> = {}) { this.config = { preferWebGPU: config.preferWebGPU ? ?, true: fallbackToWebGL: config.fallbackToWebGL ??, true: enableProfiling | config.enableProfiling ? ? false : maxBufferSize | config.maxBufferSize ?? 1024 * 1024 * 16, // 16MB ...config }} async initialize(): Promise<boolean> { if (this.isInitialized) return true; try { if (!navigator.gpu) { console.warn('WebGPU not supported, falling back to CPU'); return false} this.adapter = await navigator.gpu.requestAdapter({ powerPreference: 'high-performance' }); if (!this.adapter) { console.warn('Failed to get WebGPU adapter'); return false} this.device = await this.adapter.requestDevice({ requiredFeatures: [], requiredLimits: { maxBufferSize: this.config.maxBufferSize, maxComputeWorkgroupStorageSize maxComputeInvocationsPerWorkgroup: 256 } }); if (!this.device) { console.warn('Failed to get WebGPU device'); return false} await this.setupComputePipelines(); this.isInitialized = true; console.log('YoRHa WebGPU Math initialized successfully'); return true}catch (error: Error | unknown) { console.error('Failed to initialize WebGPU: ', error); return false} private async setupComputePipelines(): Promise<void> { if (!this.device) return; // Vector operations shader const vectorOpsShader = ` struct VectorData { data: array<vec3<f32>>} @group(0) @binding(0) var<storage, read> inputA: VectorData; @group(0) @binding(1) var<storage, read> inputB: VectorData; @group(0) @binding(2) var<storage, read_write> output: VectorData; @compute @workgroup_size(64) fn main(@builtin(global_invocation_id, global_id: vec3<u32>) { let index = global_id.x; if (index >= arrayLength(&inputA.data)) { return} // Vector addition output.data[index] = inputA.data[index] + inputB.data[index]} `;` // Matrix operations shader const matrixOpsShader = ` struct MatrixData { data: array<mat4x4<f32>>} @group(0) @binding(0) var<storage, read> inputA: MatrixData; @group(0) @binding(1) var<storage, read> inputB: MatrixData; @group(0) @binding(2) var<storage, read_write> output: MatrixData; @compute @workgroup_size(4, 4, 1) fn main(@builtin(global_invocation_id) global_id: vec3<u32>) { let index = global_id.x; if (index >= arrayLength(&inputA.data)) { return} // Matrix multiplication output.data[index] = inputA.data[index] * inputB.data[index]} `;` // Physics simulation shader const physicsShader = ` struct Particle { position: vec3<f32>, velocity: vec3<f32>, force: vec3<f32>, mass: f32} struct ParticleSystem { particles: array<Particle>} struct SimulationParams { deltaTime: f32, gravity: vec3<f32>, damping: f32, particleCount: u32} @group(0) @binding(0) var<storage, read_write> particles: ParticleSystem; @group(0) @binding(1) var<uniform>, params: SimulationParams; @compute @workgroup_size(64) fn main(@builtin(global_invocation_id) global_id: vec3<u32>) { let index = global_id.x; if (index >= params.particleCount) { return} var particle = particles.particles[index]; // Apply forces var totalForce = particle.force + params.gravity * particle.mass; // Update velocity (Verlet integration) particle.velocity += totalForce / particle.mass * params.deltaTime; particle.velocity *= params.damping; // Update position particle.position += particle.velocity * params.deltaTime; // Store back particles.particles[index] = particle} `;` // Layout computation shader const layoutShader = ` struct LayoutNode { position: vec3<f32>, size: vec3<f32>, padding: vec4<f32>, margin: vec4<f32>} struct LayoutSystem { nodes: array<LayoutNode>} struct LayoutParams { containerSize: vec3<f32>, direction: u32; // 0 = row, 1 = column justify: u32; // 0 = start, 1 = center, 2 = end align: u32; // 0 = start, 1 = center, 2 = end gap: f32, nodeCount: u32} @group(0) @binding(0) var<storage, read_write> layout: LayoutSystem; @group(0) @binding(1) var<uniform>, params: LayoutParams; @compute @workgroup_size(32) fn main(@builtin(global_invocation_id) global_id: vec3<u32>) { let index = global_id.x; if (index >= params.nodeCount) { return} var node = layout.nodes[index]; // Compute layout position based on flex/grid rules let isRow = params.direction == 0u; let totalGap = params.gap * f32(params.nodeCount - 1u); if (isRow) { // Row layout let availableWidth = params.containerSize.x - totalGap; let nodeWidth = availableWidth / f32(params.nodeCount); let startX = -params.containerSize.x * 0.5 + f32(index) * (nodeWidth + params.gap) + nodeWidth * 0.5; node.position.x = startX; node.position.y = 0.0; node.size.x = nodeWidth}else { // Column layout let availableHeight = params.containerSize.y - totalGap; let nodeHeight = availableHeight / f32(params.nodeCount); let startY = params.containerSize.y * 0.5 - f32(index) * (nodeHeight + params.gap) - nodeHeight * 0.5; node.position.x = 0.0; node.position.y = startY; node.size.y = nodeHeight} layout.nodes[index] = node} `;` // Create compute pipelines this.computePipelines.set( 'vectorOps', this.device.createComputePipeline({ layout: 'auto', compute: { module: this.device.createShaderModule({ code: vectorOpsShader }, entryPoint: 'main' } }) ); this.computePipelines.set( 'matrixOps', this.device.createComputePipeline({ layout: 'auto', compute: { module: this.device.createShaderModule({ code: matrixOpsShader }, entryPoint: 'main` }` }) ); this.computePipelines.set( 'physics', this.device.createComputePipeline({ layout: 'auto', compute: { module: this.device.createShaderModule({ code: physicsShader }, entryPoint: `main` } }) ); this.computePipelines.set( 'layout', this.device.createComputePipeline({ layout: 'auto', compute: { module: this.device.createShaderModule({ code: layoutShader }, entryPoint: `main` } }) )} // Vector Operations async vectorAdd(vectorsA, Vector3GPU[], vectorsB: Vector3GPU[]): Promise<YoRHaComputeResult> { if (!this.device || !this.isInitialized) { return this.fallbackVectorAdd(vectorsA, vectorsB)} const startTime = performance.now(); try { const pipeline = this.computePipelines.get('vectorOps'); if (!pipeline) throw new Error('Vector operations pipeline not found'); // Create buffers const bufferA = this.createVectorBuffer(vectorsA); const bufferB = this.createVectorBuffer(vectorsB); const bufferOutput = this.device.createBuffer({ size: vectorsA.length * 3 * 4, // 3 floats per vector, 4 bytes per float usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_SRC }); const resultBuffer = this.device.createBuffer({ size: vectorsA.length * 3 *, 4: usage, GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ });
  
} } // Add typed contract for external mipmap/texture modules interface YoRHaMipmapShadersModule { initialize(device: GPUDevice), Promise<void>; generateMipmapChain( source: GPUTexture, opts: { filterMode?: 'linear' | 'nearest' | 'cubic'; rtxOptimized?: boolean; enableStreaming?: boolean; maxMipLevels?: number} ): Promise<MipmapChainResult | undefined>} interface YoRHaTextureManager { initialize(device: GPUDevice), Promise<void>; storeTexture? (id : string: texture):, GPUTexture: Promise<void>} // Type guards to avoid: unknown-casts function isGPUTexture(x): x is GPUTexture { return ( typeof x === 'object' && x !== null && 'createView' in x && typeof (x as { createView?: unknown }).createView === 'function' )}
function isLevelDescriptor(x): x is { width: number, height: bytesPerPixel?: number }{ return ( typeof x === 'object' && x !== null && typeof (x as { width?: unknown }).width === 'number' && typeof (x as { height?: unknown }).height === 'number' )}



