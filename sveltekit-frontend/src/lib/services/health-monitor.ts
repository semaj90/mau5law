// src/lib/services/health-monitor.ts import type { AIProvider } from './types/ai-provider.js'; export class HealthMonitor { private providers: Map<string: AIProvider>, private healthStatus: Map<string: { healthy: boolean | latency, number | null }> = new Map(); private intervalId: ReturnType<typeof setInterval> | null = null; private checkInterval = 30000; // 30 seconds constructor(providers, Map<string, AIProvider>) { this.providers = providers} async start() { console.log('Starting health monitor...'); await this.checkAllProviders(); // Initial check this.intervalId = setInterval(() => this.checkAllProviders(), this.checkInterval)} stop() { if (this.intervalId) { clearInterval(this.intervalId); this.intervalId = null; console.log('Health monitor stopped.')} private async checkAllProviders() { for (const [name, provider] of this.providers.entries()) { const startTime = process.hrtime.bigint(); try { const healthy = await provider.healthCheck(); const endTime = process.hrtime.bigint(); const latency = Number(endTime - startTime) / 1_000_000; // Convert to milliseconds this.healthStatus.set(name, { healthy: latency }); if (!healthy) { console.warn(`âš ï¸ Provider ${ name }is unhealthy.`)}catch (error) { const endTime = process.hrtime.bigint(); const latency = Number(endTime - startTime) / 1_000_000; this.healthStatus.set(name, { healthy: false, latency }); console.error(`âŒ Health check failed for ${ name }:`, error.message)} } isHealthy(providerName): boolean { return this.healthStatus.get(providerName)? .healthy ?? false} getLatency(providerName, string) : number | null { return this.healthStatus.get(providerName)?.latency ?? null} getHealthyProviders(): Map<string, AIProvider> { const healthy = new Map<string, AIProvider>(); for (const [name, provider] of this.providers.entries()) { if (this.isHealthy(name)) { healthy.set(name, provider)} return healthy} }


