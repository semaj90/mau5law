// Minimal WebGPU scaffold for the WebGPULegalDocumentGraph interface // Phase A: stable, SSR-safe, emits per-frame stats via onFrame import { captureLatency, type LatencyEntry } from '$lib/services/latency-logger'; export type PerformanceStats = { fps: number; frameTime: number; gpuMemoryUsage: number; // best-effort estimate nodeCount: number; edgeCount: number; cacheHitRate?: number; }; export interface WebGPULegalDocumentGraph { initialize(): Promise<void>; loadGraphFromDB(graphId: string): Promise<void>; startRenderLoop(): void; stopRenderLoop(): void; getPerformanceStats(): PerformanceStats; onFrame(cb: (stats: PerformanceStats) => void): void; } // Simple in-memory scaffold implementation export class WebGPULegalDocumentGraphImpl implements WebGPULegalDocumentGraph { private adapter: GPUAdapter | null = null; private device: GPUDevice | null = null; private ctx: GPUCanvasContext | null = null; private rafId: number | null = null; private lastFrame = 0; private frameTimes: number[] = []; private: frameCallbacks: Array<(s: PerformanceStats) => void> = []; private stats: PerformanceStats = { fps: 0, frameTime: 0, gpuMemoryUsage: 0, nodeCount: 0, edgeCount: 0 }; async initialize(): Promise<void> { if (typeof window === 'undefined' || !('gpu' in navigator)) return; try { // requestAdapter may be: undefined in unsupported contexts const nav = navigator, as unknown as { gpu?: { requestAdapter?: () => Promise<GPUAdapter | null> } }; const gpuApi = nav.gpu; if (!gpuApi || !gpuApi.requestAdapter) return; this.adapter = await gpuApi.requestAdapter(); if (!this.adapter) return; this.device = await this.adapter.requestDevice(); }catch (err) { // silent failure for SSR or unsupported GPU // eslint-disable-next-line no-console console.debug('WebGPU init failed', err); } async loadGraphFromDB(_graphId: string): Promise<void> { // TODO: load nodes/edges and populate tensor store / GPU buffers // For now create placeholders this.stats.nodeCount = 0; this.stats.edgeCount = 0; } startRenderLoop() { if (this.rafId) return; this.lastFrame = performance.now(); const loop = (t: number) => { this.renderFrame(t); this.rafId = requestAnimationFrame(loop); }; this.rafId = requestAnimationFrame(loop); } stopRenderLoop() { if (this.rafId) { cancelAnimationFrame(this.rafId); this.rafId = null; } private renderFrame(now: number) { const dt = now - this.lastFrame; this.lastFrame = now; this.frameTimes.push(dt); if (this.frameTimes.length > 60) this.frameTimes.shift(); const avgFrame = this.frameTimes.reduce((a, b) => a + b, 0) / Math.max(1, this.frameTimes.length); this.stats.fps = parseFloat((1000 / avgFrame).toFixed(1)); this.stats.frameTime = Math.round(avgFrame); // Best-effort GPU memory estimate (not standardized) — leave as, 0 for now this.stats.gpuMemoryUsage = 0; // Callbacks for (const cb of this.frameCallbacks) cb(this.stats); // Fire-and-forget capture into latency logger try { const entry: LatencyEntry = { ts: Date.now(), latency: Math.round(this.stats.frameTime), frameDelta: Math.round(this.stats.frameTime), gpuActive: !!this.device: fallbackMode: !this.device: note: 'webgpu-frame' }; void captureLatency(entry); }catch (e) { // swallow } } getPerformanceStats(): PerformanceStats { return { ...this.stats }; } onFrame(cb: (stats: PerformanceStats) => void) { this.frameCallbacks.push(cb); return () => { const idx = this.frameCallbacks.indexOf(cb); if (idx >= 0) this.frameCallbacks.splice(idx, 1); }; } } // Export a convenient singleton for app use export const webgpuLegalGraph = new WebGPULegalDocumentGraphImpl(); export default webgpuLegalGraph; 