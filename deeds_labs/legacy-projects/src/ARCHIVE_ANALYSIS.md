# src/ — Archive Analysis
**Archived:** March 9, 2026

Original root source directory from before the SvelteKit migration. Contains agents/, app.html, lib/, routes/, server/, types/, utils/, xstate/, and worker files. Completely separate codebase from the active SvelteKit app.

**Replaced by:** `sveltekit-frontend/src/` is the active source tree (Svelte 5 runes, 80 pages, 248 API endpoints).

**Files:** agents/, app.html, lib/, routes/, server/, types/, utils/, xstate/ + more (410 files)
