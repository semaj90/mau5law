# CH-ROM97 Implementation Roadmap (A-H)\n\n**Status**: Phase 75-82 Integration + GPU Tokenization + CH-ROM97 Cartridge Format\n\n---\n\n## ✅ Completed\n\n### G — CH-ROM97 Cartridge File Format Spec\n**File**: `backend/formats/ch_rom97_spec.md`\n- Master specification for multimodal cartridge format\n- Binary layout: header + runes + tiles + tensors + latents + graph + metadata + visual\n- 4096-byte header with flags, counts, dimensions\n- Supports compression, quantization, encryption, vision tensors\n\n### A — Rune-to-Tensor UUID Generator\n**File**: `backend/services/rune_uuid_generator.ts`\n- Converts legal atoms (POIs, statutes, evidence) → stable UUIDs\n- Deduplication via SHA256 hashing\n- Rune types: POI, STATUTE, EVIDENCE, CHARGE, LOCATION, TIMELINE, RELATIONSHIP, THREAT\n- Export for CH-ROM97 cartridge\n- Statistics tracking\n\n---\n\n## 🔄 Next: B-H Implementation\n\n### B — GPU Tile Processor Kernel (CUDA)\n**Purpose**: Process 32×32 N64-style tiles on GPU\n**Components**:\n- CUDA kernel for tile texture memory mapping\n- Tile atlas loading into CUDA texture memory\n- Tile-to-tensor projection\n- Batch tile processing (RTX 3060 Ti: 4 tiles/batch)\n\n**Files to Create**:\n- `backend/cuda/tile_processor.cu` — CUDA kernel\n- `backend/cuda/tile_processor.h` — Header\n- `backend/services/tile_processor_service.py` — Python wrapper\n\n**Key Functions**:\n```cuda\n__global__ void process_tile_batch(\n  uint8_t* tile_atlas,      // 32×32 tiles\n  float* output_tensors,    // 768d embeddings\n  uint16_t* tile_indices,   // which tiles to process\n  int batch_size\n);\n```\n\n---\n\n### C — Rune Atlas Generator (32×32 N64-style tile sheet)\n**Purpose**: Generate visual tile representations for runes\n**Components**:\n- Rune → tile mapping (26 runes = 26 tiles)\n- Grayscale texture generation (32×32 pixels)\n- Tile atlas packing\n- SDXL integration for visual generation\n\n**Files to Create**:\n- `backend/services/rune_atlas_generator.ts` — Tile generation\n- `backend/services/sdxl_tile_renderer.py` — SDXL integration\n\n**Key Functions**:\n```typescript\nfunction generateTileAtlas(runes: Rune[]): Buffer {\n  // For each rune, generate 32×32 grayscale tile\n  // Pack into atlas\n  // Return binary buffer\n}\n```\n\n---\n\n### D — SvelteKit N64 Glyph Viewer Component\n**Purpose**: Render CH-ROM97 cartridge in SvelteKit UI\n**Components**:\n- Cartridge loader (SSE stream)\n- Tile atlas renderer (WebGL/Canvas)\n- Rune inspector (click tile → show metadata)\n- 4D manifold visualization (UMAP)\n- Real-time embedding updates\n\n**Files to Create**:\n- `sveltekit-frontend/src/lib/components/GlyphViewer.svelte` — Main component\n- `sveltekit-frontend/src/lib/components/TileAtlas.svelte` — Tile rendering\n- `sveltekit-frontend/src/lib/components/ManifoldViewer.svelte` — 4D visualization\n- `sveltekit-frontend/src/lib/stores/cartridgeStore.ts` — State management\n\n**Key Features**:\n- Real-time SSE updates\n- Click-to-inspect runes\n- Zoom/pan tile atlas\n- 4D manifold rotation\n- Export cartridge\n\n---\n\n### E — Neo4j KAG Loader\n**Purpose**: Load CH-ROM97 graph into Neo4j Knowledge-Action-Graph\n**Components**:\n- CSR graph format → Neo4j nodes/edges\n- Rune metadata → node properties\n- Relationship types (POI-knows-POI, Statute-applies-to-Charge, etc.)\n- A* pathfinding setup\n\n**Files to Create**:\n- `backend/services/neo4j_kag_loader.ts` — Graph loader\n- `backend/services/neo4j_queries.ts` — Query templates\n\n**Key Functions**:\n```typescript\nasync function loadCartridgeToNeo4j(\n  cartridge: CH_ROM97,\n  neo4jDriver: Driver\n): Promise<void> {\n  // Create nodes for each rune\n  // Create edges from CSR graph\n  // Index for A* pathfinding\n}\n```\n\n---\n\n### F — RAG + KAG + VAG Retriever Engine\n**Purpose**: Unified retrieval combining:\n- **RAG**: Retrieval-Augmented Generation (768d embeddings)\n- **KAG**: Knowledge-Action-Graph (Neo4j reasoning)\n- **VAG**: Visual-Action-Graph (tile-based visual memory)\n\n**Components**:\n- Qdrant search (768d cosine similarity)\n- Neo4j A* pathfinding\n- Tile atlas visual search\n- Fusion scoring (RAG + KAG + VAG weights)\n\n**Files to Create**:\n- `backend/services/rag_kag_vag_retriever.ts` — Unified retriever\n- `backend/services/fusion_scorer.ts` — Score fusion\n\n**Key Functions**:\n```typescript\nasync function retrieve(\n  query: string,\n  qdrant: QdrantClient,\n  neo4j: Driver,\n  cartridge: CH_ROM97\n): Promise<RetrievalResult[]> {\n  // RAG: search embeddings\n  // KAG: find related nodes\n  // VAG: find similar tiles\n  // Fuse results\n  // Return ranked results\n}\n```\n\n---\n\n### H — Visual Memory Palace Integration\n**Purpose**: Render CH-ROM97 as interactive 3D memory palace\n**Components**:\n- Three.js 3D scene\n- Tile atlas as 3D objects\n- Rune metadata as interactive labels\n- Graph edges as visual connections\n- SDXL visual cards as textures\n\n**Files to Create**:\n- `sveltekit-frontend/src/lib/components/MemoryPalace.svelte` — 3D viewer\n- `sveltekit-frontend/src/lib/utils/memory_palace_renderer.ts` — Three.js setup\n\n**Key Features**:\n- 3D tile placement\n- Interactive rune inspection\n- Graph edge visualization\n- SDXL texture mapping\n- VR-ready (WebXR)\n\n---\n\n## 🔗 Integration Points\n\n### Phase 75 Integration\n```\nCode Ingest Watcher\n    ↓ (chunks)\nEmbedding gRPC Service (768d)\n    ↓ (embeddings)\nRune UUID Generator (A)\n    ↓ (runes)\nRune Atlas Generator (C)\n    ↓ (tiles)\nGPU Tile Processor (B)\n    ↓ (processed tiles)\nCH-ROM97 Cartridge Writer\n    ↓ (cartridge)\nQdrant (R3 Cache)\nNeo4j (KAG)\nRedis (cache)\n    ↓ (SSE stream)\nSvelteKit UI (D, H)\n```\n\n### GPU Tokenization (VLM-style)\n```\nGemma-3 VLM (1024d)\n    ↓ (multimodal tokens)\nGPU Tile Processor (B)\n    ↓ (tile embeddings)\nFusion Head (FP16)\n    ↓ (fused 768d)\nRune UUID Generator (A)\n    ↓ (stable UUIDs)\nCH-ROM97 Cartridge\n```\n\n---\n\n## 📊 Implementation Order\n\n1. ✅ **G** — CH-ROM97 Spec (master format)\n2. ✅ **A** — Rune UUID Generator (semantic atoms)\n3. **B** — GPU Tile Processor (CUDA kernel)\n4. **C** — Rune Atlas Generator (visual tiles)\n5. **E** — Neo4j KAG Loader (graph reasoning)\n6. **F** — RAG+KAG+VAG Retriever (unified search)\n7. **D** — SvelteKit Glyph Viewer (UI component)\n8. **H** — Visual Memory Palace (3D viewer)\n\n---\n\n## 🎯 Deliverables\n\n### By End of Phase 75-82:\n- ✅ Code Ingest Watcher (continuous monitoring)\n- ✅ Embedding gRPC Service (768d + 1024d)\n- ✅ S3+S4 Hybrid Summaries\n- ✅ R3 Semantic Cache (Qdrant)\n- ✅ GPU Tokenization (VLM-style)\n- ✅ CH-ROM97 Cartridge Format\n- ✅ Rune UUID Generator\n- ✅ GPU Tile Processor (CUDA)\n- ✅ Rune Atlas Generator\n- ✅ Neo4j KAG Loader\n- ✅ RAG+KAG+VAG Retriever\n- ✅ SvelteKit Glyph Viewer\n- ✅ Visual Memory Palace (3D)\n\n---\n\n## 🚀 Next Steps\n\n1. Generate **B** — GPU Tile Processor Kernel (CUDA)\n2. Generate **C** — Rune Atlas Generator\n3. Generate **E** — Neo4j KAG Loader\n4. Generate **F** — RAG+KAG+VAG Retriever\n5. Generate **D** — SvelteKit Glyph Viewer\n6. Generate **H** — Visual Memory Palace\n7. Integration testing\n8. Performance optimization\n9. Production deployment\n\n---\n\n## 📝 Notes\n\n- **CH-ROM97** is the master format that ties everything together\n- **Rune UUID Generator** creates stable identifiers for legal atoms\n- **GPU Tile Processor** handles visual tile processing on CUDA\n- **Neo4j KAG** enables legal reasoning via graph traversal\n- **RAG+KAG+VAG** provides unified retrieval across embeddings, graphs, and visuals\n- **SvelteKit Glyph Viewer** and **Memory Palace** provide UI/UX\n\n---\n\n## 🎉 Status\n\n**CH-ROM97 Stack: 2/8 Complete (G, A)**\n\nReady for B-H implementation.\n"