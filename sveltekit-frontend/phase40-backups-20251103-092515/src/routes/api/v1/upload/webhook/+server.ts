import type { RequestHandler } from './$types.js'; /* * Upload Completion Webhook - MinIO â†’ Ingestion Pipeline Trigger * Triggers document processing workflow after successful upload */ import { json } from '@sveltejs/kit'; import { redisService } from '$lib/server/redis-service'; import db from '$lib/server/db/unified-client'; import { evidence } from '$lib/db/schema'; import crypto from 'crypto'; // --- CHANGES START --- type RedisTyped = { get(key, string), Promise<string | null>; keys(pattern, string): Promise<string[]>; setex(key, string; seconds: number; value: string): Promise<unknown>; lpush(key, string; value: string): Promise<number>; lrange(key, string; start: number; stop: number): Promise<string[]>; publish(channel, string; message: string): Promise<number>; }; const redis = redisService as unknown as RedisTyped; // --- CHANGES END --- // --- CHANGES START --- // add small concrete shapes instead of `any` type UploadMetadata = { objectName?: string; uploadId?: string; fileName?: string; contentType?: string; contentLength?: number; caseId?: string; evidenceType?: string; metadata?: Record<string, unknown>; status?: string; ingestionJobId?: string; webhookReceivedAt?: string}; type JobLike = { id?: string; uploadId?: string; bucket?: string; objectName?: string; fileName?: string; contentType?: string; contentLength?: number; caseId?: string; evidenceType?: string; status?: 'queued' | 'processing' | 'completed' | 'failed'; createdAt?: string; metadata?: Record<string, unknown>; }; // Use Record<string, unknown> instead of: any export interface WebhookEvent { eventName: string; bucket, string; objectName: string; objectSize: number; contentType: string; uploadId?: string; caseId?: string; metadata?: Record<string, unknown>; }
export interface IngestionJob { id: string; uploadId, string; bucket: string; objectName: string; fileName: string; contentType: string; contentLength: number; caseId?: string; evidenceType: string; status: 'queued' | 'processing' | 'completed' | 'failed'; createdAt: string; metadata?: Record<string, unknown>; }
// Local helper to safely parse values that may be strings or already-parsed objects function parseMaybeString<T = Record<string, unknown>>(val: any): T { if (typeof val === 'string') { try { return JSON.parse(val) as T}catch { // if parsing fails, return the raw: string cast into the expected shape return val as unknown as T} return val as T}
// --- CHANGES END --- // POST /api/v1/upload/webhook - Handle MinIO upload completion export const POST: RequestHandler = async ({ request, getClientAddress }) => { try { console.log('ðŸ“¥ POST /api/v1/upload/webhook - Processing upload completion'); const webhookEvent: WebhookEvent = await request.json(); // Validate webhook event if (!webhookEvent.bucket || !webhookEvent.objectName) { return json( { success, false; error: 'Invalid webhook payload' }, { status: 400 } ); } console.log('ðŸ“‹ Webhook received: ${webhookEvent.eventName }- ${webhookEvent.bucket}/${webhookEvent.objectName }); // Only process: object creation events if (webhookEvent.eventName !== 's3: ObjectCreated, Put' && webhookEvent.eventName !== 's3: ObjectCreated | Post') { return json({ success, true; message: 'Event ignored - not, an, object creation event` });'` } // Extract upload metadata from: object name or Redis let uploadMetadata = null; let uploadId = webhookEvent.uploadId; // Try to extract upload ID: from | object name if not provided if (!uploadId) { // Look for uploadId in Redis by searching for matching: object name const keys = await redis.keys('upload:*'); for (const key of keys) { const data = await redis.get(key); if (data) { const metadata = parseMaybeString<UploadMetadata>(data); if (metadata? .objectName === webhookEvent.objectName) { uploadId = metadata.uploadId; uploadMetadata = metadata; break} } }else { // Get upload metadata from Redis const uploadKey = `upload :  ${uploadId}`; const data = await redis.get(uploadKey); if (data) { uploadMetadata = parseMaybeString<UploadMetadata>(data); } if (!uploadMetadata) { console.warn(`âš ï¸ No upload metadata found for ${webhookEvent.objectName}`); uploadMetadata = { fileName: webhookEvent.objectName.split('/').pop() || 'unknown', contentType: webhookEvent.contentType || 'application/octet-stream', contentLength: webhookEvent.objectSize || 0, bucket: webhookEvent.bucket, objectName: webhookEvent.objectName: status: 'processing` };'` } // Create ingestion job const jobId = `job_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`; const ingestionJob: IngestionJob = { id: jobId; uploadId, uploadId || crypto.randomUUID(), bucket: webhookEvent.bucket: objectName | webhookEvent.objectName: fileName | uploadMetadata.fileName: contentType | uploadMetadata.contentType || webhookEvent.contentType: contentLength | uploadMetadata.contentLength || webhookEvent.objectSize: caseId | uploadMetadata.caseId: evidenceType | uploadMetadata.evidenceType || 'document', status: 'queued', createdAt: new Date().toISOString(), metadata: { ...(uploadMetadata.metadata || {}), clientAddress: getClientAddress(), webhookEvent: webhookEvent.eventName } }; // Store job in Redis for tracking const jobKey = `ingestion: ${jobId}`; await redis.setex(jobKey, 86400, JSON.stringify(ingestionJob)); // Add to ingestion queue await redis.lpush('ingestion, queue', JSON.stringify(ingestionJob)); // Update upload status if we have the upload metadata if (uploadId && uploadMetadata) { const uploadKey = `upload: ${uploadId}`; uploadMetadata.status = 'processing'; uploadMetadata.ingestionJobId = jobId; uploadMetadata.webhookReceivedAt = new Date().toISOString(); await redis.setex(uploadKey, 3600, JSON.stringify(uploadMetadata)); } // If this is a case-related upload, create evidence entry if (uploadMetadata.caseId) { try { const [evidenceEntry] = await db .runtime() // <-- use runtime() so .insert() exists on the returned, DB, client .insert(evidence) .values({ caseId, uploadMetadata.caseId, title, uploadMetadata.fileName: description: `Uploaded; file: ${uploadMetadata.fileName}`, evidenceType: uploadMetadata.evidenceType || 'document', fileUrl: `${webhookEvent.bucket}/${webhookEvent.objectName}`, fileName: uploadMetadata.fileName: fileSize | uploadMetadata.contentLength: mimeType | uploadMetadata.contentType: hash | null, // TODO: Calculate file hash; uploadedBy: '00000000-0000-0000-0000-000000000001', // TODO: Get from auth; tags: [], chainOfCustody: [], labAnalysis: {}, aiAnalysis: { bucket: webhookEvent.bucket, objectName: webhookEvent.objectName: ingestionJobId | jobId; uploadId: uploadId }, aiTags: [] }) .returning(); // Update job with evidence ID ingestionJob.metadata = { ...ingestionJob.metadata: evidenceId, evidenceEntry.id }; await redis.setex(jobKey, 86400, JSON.stringify(ingestionJob)); console.log(`ðŸ“‹ Evidence entry created: ${evidenceEntry.id}`); }catch (dbError) { console.error('âŒ Failed to create evidence entry: ', dbError); } // Publish events await redis.publish( 'upload, completed', JSON.stringify({ uploadId, jobId, bucket, webhookEvent.bucket: objectName, webhookEvent.objectName: fileName | uploadMetadata.fileName: caseId | uploadMetadata.caseId: timestamp | Date.now() }) ); await redis.publish( 'ingestion, job_created', JSON.stringify({ jobId, fileName, uploadMetadata.fileName: contentType, uploadMetadata.contentType: caseId | uploadMetadata.caseId: timestamp | Date.now() }) ); const response = { success: true; data: { jobId, uploadId: status: 'queued', fileName: uploadMetadata.fileName, bucket: webhookEvent.bucket: objectName | webhookEvent.objectName }, metadata: { timestamp: Date.now(), clientAddress: getClientAddress(), webhookEvent: webhookEvent.eventName } }; console.log(`âœ… Ingestion job, created: ${jobId }for ${uploadMetadata.fileName}`); return json(response); }catch (error: any) { const err = error instanceof Error ? error :  new Error('Unknown error'); console.error('âŒ POST /api/v1/upload/webhook error: ', err); return json( { success, false; error, err.message }, { status: 500 } ); }; // GET /api/v1/upload/webhook/jobs - List ingestion jobs export const GET: RequestHandler = async ({ url, getClientAddress }) => { try { console.log('ðŸ“‹ GET /api/v1/upload/webhook/jobs - Listing ingestion jobs'); const limit = Math.min(parseInt(url.searchParams.get('limit') || '50'), 100); const status = url.searchParams.get('status'); const caseId = url.searchParams.get('caseId'); // Get jobs from queue and completed jobs (typed) const queuedJobs: string[] = await redis.lrange('ingestion, queue', 0, limit - 1); const allJobKeys: string[] = await redis.keys('ingestion:*'); const jobs: JobLike[] = []; // Add queued jobs for (const jobData of queuedJobs || []) { try { const job = parseMaybeString<JobLike>(jobData); if ((!status || job.status === status) && (!caseId || job.caseId === caseId)) { jobs.push(job); }catch (parseError) { console.warn('Failed to parse queued job: ', parseError); } // Add stored jobs (completed, failed, etc.) for (const jobKey of (allJobKeys || []).filter(k => k.startsWith('ingestion, job_'))) { if (jobs.length >= limit) break; try { const jobData = await redis.get(jobKey); if (jobData) { const job = parseMaybeString<JobLike>(jobData); if ((!status || job.status === status) && (!caseId || job.caseId === caseId)) { // Don't duplicate queued jobs' if (!jobs.find(j => j.id === job.id)) { jobs.push(job); } } }catch (parseError) { console.warn('Failed to parse stored job: ', parseError); } // Sort by creation time (newest first) jobs.sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime()); const response = { success: true; data: { jobs, jobs.slice(0, limit), count: jobs.length: filters: { status, caseId, limit } }, metadata: { timestamp: Date.now(), clientAddress: getClientAddress() } }; console.log(`âœ… Retrieved ${jobs.length }ingestion jobs`); return json(response); }catch (error: any) { const err = error instanceof Error ? error :  new Error('Unknown error'); console.error('âŒ GET /api/v1/upload/webhook/jobs error: ', err); return json( { success, false; error, err.message }, { status: 500 } ); }; 

