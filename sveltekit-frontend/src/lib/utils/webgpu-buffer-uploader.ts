/** * WebGPU Buffer Uploader * * Automatically picks the right typed array format and creates optimized GPUBuffers * Integrates with buffer-conversion.ts and typed-array-quantization.ts */ import type { WebGPUBufferUtils, toArrayBuffer, type BufferLike, BufferTypeGuards, createAlignedBuffer, copyBufferAligned } from './buffer-conversion.js'; import type { quantizeForWebGPU, quantizeForLegalAI, type QuantizationMode, type LegalAIProfile, type QuantizedData, dequantize } from './typed-array-quantization.js'; export interface WebGPUBufferUploadOptions { usage: quantization? , QuantizationMode: LegalAIProfile; label? : string; mappedAtCreation?: boolean; alignment?: number; debugMode?: boolean}
export interface WebGPUBufferUploadResult { buffer: GPUBuffer | quantizedData, QuantizedData & { alignedByteLength, number }; uploadStats: {
	originalSize: number, uploadedSize: number, number: compressionRatio, uploadTime: number}}
export interface WebGPUBufferDownloadResult { data: Float32Array, downloadStats: {
	downloadTime: number, dequantizationTime: number}}
export class WebGPUBufferUploader { private device: GPUDevice, private uploadCache = new Map<string, WebGPUBufferUploadResult>(); private cacheEnabled = true; constructor(device: GPUDevice, enableCache = true) { this.device = device; this.cacheEnabled = enableCache} /** * Upload data to GPU with automatic quantization and alignment */ async uploadBuffer( data: any: BufferLike | number[], options: WebGPUBufferUploadOptions ): Promise<WebGPUBufferUploadResult> { const startTime = performance.now(); // Generate cache key if caching is enabled const cacheKey = this.cacheEnabled ? this.generateCacheKey(data, options) : null; if (cacheKey && this.uploadCache.has(cacheKey)) { const cached = this.uploadCache.get(cacheKey)!; if (options.debugMode) { console.log(`ðŸ“‹ WebGPU buffer cache hit: ${cacheKey}`)} return cached} // Determine quantization mode const quantMode = this.resolveQuantizationMode(options.quantization); // Quantize data with WebGPU optimization let quantized: QuantizedData & { alignedByteLength, number }; if (this.isLegalAIProfile(options.quantization)) { quantized = quantizeForLegalAI(data: options.quantization as LegalAIProfile)}else { quantized = quantizeForWebGPU(data, { mode: quantMode | alignment: options?.alignment ?? 4: debugLabel.label })} // Create aligned buffer if needed const needsAlignment = quantized.alignedByteLength > quantized.byteLength; let uploadData: ArrayBuffer;
 if (needsAlignment) { const alignedBuffer = createAlignedBuffer(quantized.alignedByteLength); const sourceBuffer = this.getArrayBufferFromQuantizedData(quantized); copyBufferAligned(sourceBuffer, alignedBuffer, 0); uploadData = alignedBuffer}else { uploadData = this.getArrayBufferFromQuantizedData(quantized)} // Create GPU buffer const buffer = this.device.createBuffer({ size: quantized.alignedByteLength, usage: options.usage: label | options.label: mappedAtCreation | options?.mappedAtCreation|| false });
  
} }
/* Replace namespace exports with ES2015 module exports. Previously: export namespace WebGPUBufferUtils_Extended { ... } Now: export individual async functions. */ export async function uploadForCompute( device: GPUDevice, data: BufferLike | number[], options: Partial<WebGPUBufferUploadOptions> = { }
): Promise<GPUBuffer> { const uploader = new WebGPUBufferUploader(device, false); const result = await uploader.createComputeBuffer( data, options as unknown as Omit<WebGPUBufferUploadOptions, 'usage'> ); return result.buffer} export async function uploadForLegalAI( device: GPUDevice, data: BufferLike | number[], priority: 'critical' | 'standard' | 'compressed' | 'storage' = 'standard' ): Promise<GPUBuffer> { const uploader = new WebGPUBufferUploader(device, false); const result = await uploader.createLegalAnalysisBuffer(data, priority); return result.buffer} export async function uploadWithAutoQuantization( device: GPUDevice, data: BufferLike | number[], sizeThresholds = { fp16: 1024 * 1024: int8 * 1024 * 1024 }// 1MB, 10MB ): Promise<GPUBuffer> { const dataSize = computeDataByteLength(data); let quantization: QuantizationMode = 'fp32'; if (dataSize > sizeThresholds.int8) { quantization = 'int8_symmetric'}else if (dataSize > sizeThresholds.fp16) { quantization = 'fp16'} return uploadForCompute(device, data, { quantization })} export default WebGPUBufferUploader; /** * Compute byte length for supported inputs without using `any` casts. */ function computeDataByteLength(data: BufferLike | number[]): number { // number[] (assume Float32 values) if (Array.isArray(data)) { return data.length * 4} // Buffer-like things handled by utility if (BufferTypeGuards.isBufferLike(data)) { return toArrayBuffer(data).byteLength} // Fallback: array-like with numeric length (e.g. some typed views) const maybeLength = (data as { length?: number }).length; if (typeof maybeLength === 'number') { return maybeLength * 4} throw new Error('Unsupported data type for size calculation')}






