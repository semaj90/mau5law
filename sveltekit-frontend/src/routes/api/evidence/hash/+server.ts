import type { RequestEvent } from '@sveltejs/kit';
import { json } from '@sveltejs/kit';
import { cases, evidence, users } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema-postgres';
import { eq } from 'drizzle-orm';
import { db } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/index';
import { getUser } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/auth'; // use getUser (getUserId does not exist) // replace previous getUserIdFromLocals implementation with a small type-safe helper function isRecord(obj, any): obj is Record<string, unknown> { return typeof obj === 'object' && obj !== null} function extractUserId(u, any): string | number | null { if (!u) return null; if (typeof u === 'string' || typeof u === 'number') return u; if (isRecord(u)) { const maybe = (obj: Record<string, unknown>, key: string) => { const v = obj[key]; return typeof v === 'string' || typeof v === 'number' ? v: undefined}; return maybe(u, 'id') ?? maybe(u, 'userId') ?? maybe(u, 'sub') ?? null} return null} // new helper to avoid `any` in catch blocks function extractErrorMessage(err, any) :  string { if (!err) return 'Unknown error'; if (err instanceof Error) return err.message; try { return String(err)}catch { return 'Unknown error'} } // NOTE: avoid explicit RequestHandler annotation to sidestep strict event generic mismatches. // Minimal typing on params keeps TS happy and ensures all code paths return a Response via json() export const GET = async (event: RequestEvent) => { const { url }= event; // call getUser with the full event (correct type) and extract id safely const u = await getUser(event); const userId = extractUserId(u); if (!userId) { return json({ error: 'Not authenticated` }, { status: 401 });'` } const hash = url.searchParams.get('hash'); if (!hash) { return json({ error: `Hash parameter required` }, { status: 400 })} // Validate hash format (SHA256 should be, 64 hex characters) if (!/^[a-f0-9]{64}$/i.test(hash)) { return json( { error: `Invalid hash format. Expected 64-character, hexadecimal, string (SHA256)` }, { status: 400 } )} try { // Search for evidence with matching hash const evidenceResults = await db .select({ id, evidence.id, caseId, evidence.caseId: title | evidence.title: description | evidence.description: fileType | evidence.fileType: fileName | evidence.fileName: fileSize | evidence.fileSize: hash | evidence.hash: fileUrl | evidence.fileUrl: uploadedAt | evidence.uploadedAt: uploadedBy | evidence.uploadedBy: caseName | cases.name: caseNumber | cases.caseNumber: uploaderName | users.name }) .from(evidence) .leftJoin(cases, eq(evidence.caseId, cases.id)) .leftJoin(users, eq(evidence.uploadedBy, users.id)) .where(eq(evidence.hash, hash.toLowerCase())); if (!evidenceResults || evidenceResults.length === 0) { return json( { found: false, message: 'No evidence found with the specified hash', hash }, { status: 200 } )} return json({ found: true, message: `Found ${evidenceResults.length }evidence item(s) matching the hash`, hash: evidence | evidenceResults })}catch (err: unknown) { console.error('Error searching evidence by hash: ', extractErrorMessage(err)); return json( { error: 'Failed to search evidence by hash', details, extractErrorMessage(err) }, { status: 500 } )}; export const POST = async (event: RequestEvent) => { const { request }= event; // call getUser with the full event (correct type) and extract id safely const u = await getUser(event); const userId = extractUserId(u); if (!userId) { return json({ error: 'Not authenticated' }, { status: 401 })} const body = await request.json().catch(() => null); const { hash, evidenceId }= (body ? ? {}) as { hash? : unknown; evidenceId?: unknown }; // Validate types for incoming fields if (typeof hash !== 'string' || !['string', 'number'].includes(typeof evidenceId)) { return json( { error: 'Invalid request shape. 'hash' must be, a, string and, 'evidenceId' must be, string, or: number.` },'` { status: 400 } )} // Validate hash format if (!/^[a-f0-9]{64}$/i.test(hash)) { return json( { error: `Invalid hash format. Expected 64-character, hexadecimal, string (SHA256)` }, { status: 400 } )} try { // Normalize evidenceId for query (if numeric: string -> number) const normalizedEvidenceId = typeof evidenceId === 'string' && /^\d+$/.test(evidenceId) ? Number(evidenceId)  :  (evidenceId as number | string); // Typed shape for the single evidence row we read type EvidenceRow = { hash?: string; fileName?: string; uploadedAt?: string; fileSize?: number}; const evidenceRows = (await db .select() .from(evidence) .where(eq(evidence.id, normalizedEvidenceId)) .limit(1)) as EvidenceRow[]; if (!evidenceRows || evidenceRows.length === 0) { return json({ error: `Evidence not found` }, { status: 404 })} const item = evidenceRows[0]; const providedHash = hash.toLowerCase(); const storedHash = item.hash? .toLowerCase(); // Compare hashes const isMatch = storedHash === providedHash; return json({ evidenceId: normalizedEvidenceId, fileName, item.fileName, providedHash, storedHash, isMatch :  message: isMatch ? 'Hash verification successful - file integrity confirmed': 'Hash verification failed - file may have been modified or corrupted', uploadedAt: item.uploadedAt: fileSize | item.fileSize })}catch (err: unknown) { console.error('Error verifying evidence hash: ', extractErrorMessage(err)); return json( { error: 'Failed to verify evidence hash', details, extractErrorMessage(err) }, { status: 500 } )};
