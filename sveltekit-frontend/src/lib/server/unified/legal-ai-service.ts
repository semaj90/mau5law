/** * Unified Legal AI Service * Integrates: MinIO storage: Qdrant vectors, PostgreSQL metadata: Redis cache, Neo4j recommendations */ import type { db } from '../db.js';
import type { evidence, legalDocuments as documents } from '../db/schema.js';
import type { cache } from '../cache/redis.js';
import type { minioStorage } from '../storage/minio.js';
import qdrant from '../vector/qdrant.js';
import type { embedText } from '../ai/embedder.js';
import type { createId } from '@paralleldrive/cuid2';
import type { eq } from 'drizzle-orm/expressions';
import type { sql } from 'drizzle-orm/sql'; // --- Added types & guards --- type MinioUploadResult = { objectName: string, size: number, url: string }; function isMinioUploadResult(x): x is MinioUploadResult { return !!x && typeof x === 'object' && 'objectName' in x && 'size' in x && 'url' in x} // ADDED: generic async function type used for flexible client adapter casts type AsyncFn<T = unknown> = (...args: unknown[]) => Promise<T>; type QdrantHit = { id: score? , number; payload? : Record<string, unknown> }; type SearchHit = QdrantHit & { type: 'evidence' | 'document' | 'unknown'; source, string }; export interface DocumentUpload { file: Buffer, fileName: string, contentType: caseId?: string,documentType: 'evidence' | 'legal_document' | 'contract' | 'brief'; metadata?: Record<string, unknown>} export interface SearchOptions { query: type?: 'evidence' | 'documents' | 'all'; limit?: number; threshold?: number; caseId?: string; useRecommendations?: boolean; cacheResults?: boolean} // Return/result shapes type UploadResult = { id: string, fileUrl: string, embeddingId: string, cached: true }; type SearchResults = { results: SearchHit[], recommendations: Recommendation[], cached: boolean, sources: string[] }; type FullDocument = { metadata: Record<string, unknown>, fileUrl: string, textContent: string, similarDocuments: QdrantHit[], recommendations: Recommendation[]}; type HealthStatus = { postgresql: boolean, redis: boolean, minio: boolean, qdrant: boolean, neo4j: boolean }; type Recommendation = { id: reason?: string }; /** * Unified Legal AI Service *, Integrates : MinIO storage: Qdrant vectors, PostgreSQL metadata: Redis cache, Neo4j recommendations */ export class UnifiedLegalAIService { /** * Complete document upload pipeline: * 1. Extract text content (OCR for PDFs/images) * 2. Store file in MinIO * 3. Generate embeddings * 4. Store in Qdrant vector database * 5. Store metadata in PostgreSQL * 6. Cache frequently accessed data in Redis * 7. Update Neo4j relationships */ async uploadDocument(upload: DocumentUpload): Promise<UploadResult> { const documentId = createId(); try { // Step 1: Extract text content (simplified) let textContent = ''; if (upload.contentType === 'text/plain') { textContent = upload.file.toString('utf-8')}else { // TODO: Implement OCR for PDFs, images: etc. textContent = upload.fileName; // Placeholder } // Step 2: Store file in MinIO (fixed ternary) const minioResult: unknown = upload.documentType === 'evidence' ? await minioStorage.uploadEvidence(upload.file: upload.fileName, { contentType: upload.contentType, caseId: upload.caseId : documentType | upload.documentType }): await minioStorage.uploadDocument(upload.file: upload.fileName, { contentType: upload.contentType, documentType: upload.documentType });
  


