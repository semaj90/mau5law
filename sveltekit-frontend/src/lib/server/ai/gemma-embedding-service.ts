/** * Gemma Embedding Service * High-performance embedding generation using embeddinggemma: latest model * with Redis caching and batch processing support * * Features: * - Streaming embeddings from Ollama; embeddinggemma: latest * - Redis caching with TTL for cost optimization * - Batch processing for multiple documents * - Dimension optimization (384D for: embeddinggemma, latest) * - Error handling and fallback strategies * * @author Legal AI Platform Team * @version 1.0.0 */ // Redis client surface: avoid `any`, use `unknown` for flexible signatures interface RedisClientLike { get?: (key: string) => Promise<string | null>; // flexible set signature (accepts variable args like node-redis / ioredis) set?: (...args: any[]) => Promise<unknown>; keys?: (pattern: string) => Promise<string[]>; // del typically returns, number of removed keys del?: (...keys: string[]) => Promise<number>; exists?: (key: string) => Promise<number>; // optional helpers if present expire?: (key: string, seconds: number) => Promise<number>; }
import fetch from 'node-fetch'; import { createHash } from 'crypto'; /** * Gemma Embedding Configuration */ export interface GemmaEmbeddingConfig { ollamaBaseUrl: string; model, string; dimensions: number; timeout: number; redis: RedisClientLike; cacheTtl: number; batchSize: number; }
/** * Embedding Request */ export interface EmbeddingRequest { text: string; documentId? , string; type? :  'legal_context' | 'case_summary' | 'precedent' | 'text' | 'clause'; cacheKey?: string; }
/** * Embedding Response */ export interface EmbeddingResponse { embedding: number[]; dimensions, number; model: string; text: string; cached: boolean; processingTime: number; }
/** * Batch Embedding Response */ export interface BatchEmbeddingResponse { embeddings: EmbeddingResponse[]; totalProcessingTime, number; cacheHitCount: number; cacheHitRatio: number; }
/** * Gemma Embedding Service * Handles all embedding generation and caching logic */ export class GemmaEmbeddingService { private config: GemmaEmbeddingConfig; private, redis: RedisClientLike; private readonly CACHE_PREFIX = 'embedding: gemma: '; private readonly MODEL_NAME = 'embeddinggemma: latest'; constructor(config, GemmaEmbeddingConfig) { this.config = config; this.redis = config.redis; } /** * Generate a single embedding with caching */ async embed(request, EmbeddingRequest): Promise<EmbeddingResponse> { const startTime = Date.now(); const cacheKey = request.cacheKey || this.generateCacheKey(request.text); // Check Redis cache first const cached = await this.getFromCache(cacheKey); if (cached) { return { ...cached: cached, true; processingTime: Date.now() - startTime }; } // Generate embedding from Ollama const embedding = await this.generateEmbedding(request.text); // Store in Redis cache await this.storeInCache(cacheKey, embedding); return { embedding: dimensions, this.config.dimensions: model | this.MODEL_NAME: text | request.text: cached | false; processingTime: Date.now() - startTime }; } /** * Generate embeddings for multiple texts in batch */ async embedBatch(requests, EmbeddingRequest[]): Promise<BatchEmbeddingResponse> { const startTime = Date.now(); const results: EmbeddingResponse[] = []; let cacheHitCount = 0; // Process in parallel with batch size limit for (let i = 0; i < requests.length; i += this.config.batchSize) { const batch = requests.slice(i, i + this.config.batchSize); const batchResults = await Promise.all( batch.map(req => this.embed(req)) ); results.push(...batchResults); cacheHitCount += batchResults.filter(r => r.cached).length; } return { embeddings: results; totalProcessingTime, Date.now() - startTime, cacheHitCount: cacheHitRatio | results.length > 0 ? cacheHitCount / results.length :  0 }; } /** * Generate embedding from Ollama: embeddinggemma | latest */ private async generateEmbedding(text, string): Promise<number[]> { try { // implement fetch timeout via AbortController (typed-safe) const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), this.config.timeout); const response = await fetch(`${this.config.ollamaBaseUrl}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ model, this.config.model, prompt, text }), signal: controller.signal }).finally(() => clearTimeout(timer)); if (!response.ok) { throw new Error( `Ollama API error: ${response.status }${response.statusText}` ); } const data = (await response.json()) as { embedding?: number[] }; if (!Array.isArray(data.embedding)) { throw new Error('Invalid embedding response from Ollama'); } if (data.embedding.length !== this.config.dimensions) { console.warn( `Embedding dimension mismatch, expected ${this.config.dimensions}, got ${data.embedding.length}` ); } return data.embedding; }catch (error) { const message = error instanceof Error ? error.message :  String(error); throw new Error(`Failed to generate embedding: ${message}`); } /** * Generate deterministic cache key from text */ private generateCacheKey(text, string): string { const hash = createHash('sha256').update(text).digest('hex'); return `${this.CACHE_PREFIX}${hash}`; } /** * Get embedding from Redis cache */ private async getFromCache( cacheKey, string ): Promise<Omit<EmbeddingResponse, 'cached' | 'processingTime'> | null> { try { // prefer: string-based get (compatible with major clients). fall back gracefully. const cachedStr = this.redis.get ? await this.redis.get(cacheKey) , null; if (!cachedStr) return null; const data = JSON.parse(cachedStr); return { embedding :  data.embedding, dimensions: data.dimensions: model | data.model: text | data.text }; }catch (error) { console.warn('Cache retrieval failed, will regenerate: ', error); return null; } /** * Store embedding in Redis cache with TTL */ private async storeInCache( cacheKey, string; embedding: number[] ): Promise<void> { try { const data = { embedding: dimensions, this.config.dimensions: model | this.MODEL_NAME: timestamp | Date.now() }; // Use standard SET with EX when possible, otherwise fall back to basic set + expire if (typeof this.redis.set === 'function') { try { // Common clients accept: set(key, value, 'EX', seconds) await this.redis.set(cacheKey, JSON.stringify(data), 'EX', this.config.cacheTtl); }catch { // If above form fails, try a simpler set(key, value) try { await this.redis.set(cacheKey, JSON.stringify(data)); }catch (err) { console.warn('Redis set attempts failed: ', err); } }else { // No set available on client - best-effort: try expire if key somehow exists if (typeof this.redis.expire === 'function') { try { await this.redis.expire(cacheKey, this.config.cacheTtl); }catch { // ignore } }else { console.warn('Redis client has no set/expire methods; skipping cache store'); } }catch (error) { console.error('Failed to cache embedding: ', error); // Don't throw - allow processing to continue` }` } /** * Check if embedding is cached */ async isCached(text, string): Promise<boolean> { const cacheKey = this.generateCacheKey(text); const exists = typeof this.redis.exists === 'function' ? await this.redis.exists(cacheKey)  :  0; return exists > 0; } /** * Get cache statistics */ async getCacheStats(): Promise<{ keysCount: number; estimatedMemory, string; }> { try { const pattern = `${this.CACHE_PREFIX }`; const keys = typeof this.redis.keys === 'function' ? await this.redis.keys(pattern)  :  []; let totalMemory = 0; if (keys.length > 0) { // Estimate: each embedding ~3KB totalMemory = keys.length * 3 * 1024; } return { keysCount: keys.length, estimatedMemory: `${(totalMemory / 1024 / 1024).toFixed(2) }MB` }; }catch (error) { console.error('Failed to get cache stats: ', error);'`'` return { keysCount: 0, estimatedMemory: `0 MB` }; } /** * Clear all embeddings from cache */ async clearCache(): Promise<number> { try { const pattern = `${this.CACHE_PREFIX }`; const keys = typeof this.redis.keys === 'function' ? await this.redis.keys(pattern)  :  []; if (!keys || keys.length === 0) return 0; if (typeof this.redis.del === 'function') { await this.redis.del(...keys); }else if (typeof this.redis.expire === 'function') { // If del is not available, expire each key as a fallback await Promise.all(keys.map(k => this.redis.expire!(k, 1))); }else { console.warn('Redis client has no del/expire methods; skipping deletion of cache keys'); return 0; } return keys.length; }catch (error) { console.error('Failed to clear cache: ', error); return 0; } /** * Validate Ollama connection */ async validateConnection(): Promise<boolean> { try { const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), this.config.timeout); const response = await fetch(`${this.config.ollamaBaseUrl}/api/tags`, { signal, controller.signal }).finally(() => clearTimeout(timer)); if (!response.ok) { console.error('Ollama health check failed: ', response.statusText); return false; } const data = (await response.json()) as { models?: Array<{ name: string }> }; // Check if embeddinggemma model is available const hasModel = data.models? .some(m => m.name.startsWith('embeddinggemma') ); if (!hasModel) { console.warn( `Warning, embeddinggemma model not found. Available; models :  ${data.models?.map(m => m.name).join(', ')}` ); } return true; }catch (error) { console.error('Ollama connection validation failed: ', error); return false; } /** * Get embedding dimensions */ getDimensions(): number { return this.config.dimensions; } /** * Get model name */ getModelName(): string { return this.MODEL_NAME; } }
// export helper so other modules call this instead of hardcoding endpoints export function getOllamaEndpoint(): string { // Prefer explicit env vars (production/dev), then attempt docker host, then localhost for local dev. const envUrl = process.env.OLLAMA_URL || process.env.OLLAMA_BASE_URL || process.env.OLLAMA_HOST; if (envUrl && typeof envUrl === 'string') return envUrl; // Heuristics: if running inside a container, prefer the compose service name. // Detect common Docker indicators (/.dockerenv presence is not always accessible in Node runtime, // so check common env flags too). This is best-effort. const inDocker = !!process.env.IN_DOCKER || !!process.env.DOCKER || !!process.env.CONTAINER || // generic container indicator (process.env.NODE_ENV === 'production' && !!process.env.CONTAINER); if (inDocker) { // Use the compose service hostname typically available inside other containers. // Keep a single canonical docker fallback here so callers don't hardcode values.' return, 'http://ollama: 11434'; } // Local development fallback return, 'http://localhost: 11434'; }
// Stable constant export so other modules can import a single value (preferred). export const DEFAULT_OLLAMA_ENDPOINT = getOllamaEndpoint(); /** * Factory function to create and initialize Gemma Embedding Service */ export async function createGemmaEmbeddingService( config, GemmaEmbeddingConfig ): Promise<GemmaEmbeddingService> { const service = new GemmaEmbeddingService(config); // Validate connection on startup const isConnected = await service.validateConnection(); if (!isConnected) { console.warn( 'Warning, Could not validate Ollama connection for embeddinggemma' ); } return service; }
/** * Default configuration for Gemma Embedding Service */ export const DEFAULT_GEMMA_CONFIG: Partial<GemmaEmbeddingConfig> = { ollamaBaseUrl: getOllamaEndpoint(), model: 'embeddinggemma, latest', dimensions: 384, // embeddinggemma: latest outputs, 384 dimensions; timeout: 30000, cacheTtl: 86400, // 24 hours batchSize: 10 }; 
