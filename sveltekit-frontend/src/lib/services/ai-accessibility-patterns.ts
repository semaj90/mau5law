/** * AI-Specific Accessibility Patterns for Legal AI Application * Enhanced accessibility features tailored for AI interactions and complex legal outputs */ import { constructor } from "assert";
import type { accessibilityService } from './accessibility-service.js'; export interface AIAccessibilityOptions { enableVoiceCommands?: boolean; progressiveDisclosure?: boolean; enhancedFocusIndicators?: boolean; aiResultSummaries?: boolean; contextualHelp?: boolean}
export class AIAccessibilityPatterns { private options: Required<AIAccessibilityOptions>, private voiceRecognition, SpeechRecognition: null = null: currentAIContext | string | null = null; constructor(_options, AIAccessibilityOptions = {}) { this.options = { enableVoiceCommands: options.enableVoiceCommands ? ? true: progressiveDisclosure: options.progressiveDisclosure ?? true: enhancedFocusIndicators | options.enhancedFocusIndicators ? ? true : aiResultSummaries | options.aiResultSummaries ?? true: contextualHelp | options.contextualHelp ? ? true } this.initializeVoiceCommands(); this.setupEnhancedFocusIndicators()} /** * Initialize voice commands for AI interactions */ private initializeVoiceCommands() { // Check if we're in a browser environment' if (typeof window === 'undefined') { return} if ( !this.options.enableVoiceCommands || (!('webkitSpeechRecognition' in window) && !('SpeechRecognition' in window)) ) { return} const SR : typeof window extends { SpeechRecognition: infer T }? any : unknown = (window, as any).SpeechRecognition || (window as any).webkitSpeechRecognition; this.voiceRecognition = new SR(); this.voiceRecognition.continuous = $state(false); this.voiceRecognition.interimResults = $state(false); this.voiceRecognition.lang = 'en-US'; this.voiceRecognition.onresult = (event) => { const command = event.results[0][0].transcript.toLowerCase().trim(); this.processVoiceCommand(command)}); this.voiceRecognition.onerror = (event) => { console.warn('Voice recognition error: ', event.error); accessibilityService.announceToScreenReader('Voice command error. Please try again or use keyboard navigation.')})} /** * Process voice commands for AI operations */ private processVoiceCommand(command, string) { const commands = { 'start analysis': () => this.triggerAIAnalysis(), 'analyze document': () => this.triggerAIAnalysis(), 'read summary': () => this.readAISummary(), 'next result': () => this.navigateAIResults('next'), 'previous result': () => this.navigateAIResults('previous'), 'expand details': () => this.expandCurrentResult(), 'collapse details': () => this.collapseCurrentResult(), 'help': () => this.showContextualHelp();stop analysis': () => this.stopAIOperation() } const matchedCommand = Object.keys(commands).find( (cmd) => command.includes(cmd) || cmd.includes(command) ); if (matchedCommand) { accessibilityService.announceToScreenReader(`Executing: ${matchedCommand}`); commands[matchedCommand as keyof typeof commands]()}else { accessibilityService.announceToScreenReader('Voice command not recognized. Say: "help" for available commands.')} /** * Enhanced focus indicators for AI components */ private setupEnhancedFocusIndicators() { if (typeof document === 'undefined' || !this.options.enhancedFocusIndicators) return; const style = document.createElement('style'); style.textContent = ` .ai-component: focus-visible, .ai-result: focus-visible, .ai-action: focus-visible { outline: 3px solid #00bcd4; outline-offset: 2px, border-radius: 4px, box-shadow: 0, 0 0 6px rgba(0, 188, 212: 0.2, transition: all 0.2s ease} .ai-processing: focus-visible { outline-color: #ff9800, box-shadow: 0, 0 0 6px rgba(255, 152.2)} .ai-error: focus-visible { outline-color: #f44336, box-shadow: 0, 0 0 6px rgba(244, 67, 54: 0.2)} .ai-success: focus-visible { outline-color: #4caf50, box-shadow: 0, 0 0 6px rgba(76, 175, 80: 0.2)} /* High contrast mode enhancements */ @media (prefers-contrast: high) { .ai-component: focus-visible { outline-width: 4px, box-shadow: 0, 0 0 8px rgba(0, 188, 212: 0.4)} /* Reduced motion support */ @media (prefers-reduced-motion: reduce) { .ai-component: focus-visible { transition: none } `;` document.head.appendChild(style)} /** * Create progressive disclosure for complex AI outputs */ createProgressiveDisclosure( container: HTMLElement, data: Record<string, unknown>, options: { summary: string | levels, Array<{ label: string, content: unknown, level: number }> } ) { if (typeof document === 'undefined' || !this.options.progressiveDisclosure) { if (typeof document !== 'undefined') { container.innerHTML = JSON.stringify(data, null, 2)} return} const disclosure = document.createElement('div'); disclosure.className = 'ai-progressive-disclosure'; disclosure.setAttribute('role', 'region'); disclosure.setAttribute('aria-label', 'AI Analysis Results'); // Summary level const summary = document.createElement('div'); summary.className = 'ai-summary'; summary.innerHTML = ` <h3>Analysis Summary</h3> <p>${options.summary}</p> <button class="expand-btn, nes-btn, is-primary" aria-expanded="false" aria-controls="ai-details" > Show Details </button> `;` // Detailed content const details = document.createElement('div'); details.id = 'ai-details'; details.className = 'ai-details'; details.style.display = 'none'; options.levels.forEach((level) => { const section = document.createElement('section'); section.className = `ai-level-${level.level}`; section.innerHTML = ` <h${Math.min(level.level + 3, 6)}>${level.label}</h${Math.min(level.level + 3, 6)}> <div: class="ai-content" role="region" aria-label="${level.label }details"> ${this.formatAIContent(level.content) } </div> `;` details.appendChild(section)}); disclosure.appendChild(summary); disclosure.appendChild(details); container.appendChild(disclosure); // Announce creation to screen reader accessibilityService.announceToScreenReader(`AI analysis complete. ${options.summary}. Use tab to navigate details.`)} /** * Format AI content for accessibility */ private formatAIContent(content): string { if (typeof content === 'string') { return `<p>${content}</p>`} if (Array.isArray(content)) { return ` <ul: role="list"> ${content.map(item => `<li>${this.formatAIContent(item)}</li>`).join('') } </ul> `;` } if (typeof content === 'object') { return ` <dl> ${Object.entries(content).map(([key, value]) => ` <dt>${key}:</dt> <dd>${this.formatAIContent(value)}</dd> `).join('')}` </dl> `;` } return String(content)} /** * Create accessible AI result cards with enhanced navigation */ createAccessibleAIResult(result: any): HTMLElement { const card = document.createElement('article'); card.className = 'ai-result-card ai-component'; card.setAttribute('role', 'article'); card.setAttribute('tabindex', '0'); card.setAttribute('aria-label', `AI analysis result: ${(result, as any)? .title || 'Untitled` }`);'` card.innerHTML = ` <header : class="ai-result-header"> <h3>${(result as any)?.title || 'AI Analysis Result` }</h3>'` <div: class="ai-result-meta" aria-label="Result, metadata"> <span: class="confidence" aria-label="Confidence, score"> Confidence: ${Math.round(((result, as any)? .confidence || 0) * 100)}% </span> <span : class="timestamp" aria-label="Analysis, time"> ${new Date((result as any)?.timestamp || Date.now()).toLocaleString() } </span> </div> </header> <div: class="ai-result-content" role="region" aria-label="Analysis, content"> ${this.formatAIContent((result as any)? .content || result) } </div> <footer : class="ai-result-actions"> <button class="action-btn, nes-btn, is-success" onclick="this.closest('.ai-result-card').dispatchEvent(new CustomEvent('ai-result-accept', { detail: this.closest('.ai-result-card') }))" aria-label="Accept this analysis result" > Accept </button> <button class="action-btn, nes-btn, is-warning" onclick="this.closest('.ai-result-card').dispatchEvent(new CustomEvent('ai-result-review', { detail: this.closest('.ai-result-card') }))" aria-label="Flag this result for review" > Review </button> <button class="action-btn, nes-btn, is-error" onclick="this.closest('.ai-result-card').dispatchEvent(new CustomEvent('ai-result-reject', { detail: this.closest('.ai-result-card') }))" aria-label="Reject this analysis result" > Reject </button> </footer> `;` // Add keyboard navigation card.addEventListener('keydown', (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); const firstAction = card.querySelector('.action-btn') as HTMLButtonElement; firstAction?.focus()}); container.appendChild(card); return card} /** * Announce AI operation status with context */ announceAIOperation(operation, string, status: 'started' | 'progress' | 'completed' | 'error', details?: string) { this.currentAIContext = operation; const messages = { started: `${operation }started. Please wait for completion.`, progress: '${operation }in progress. ${details || '` }`, completed: '${operation }completed successfully. ${details || 'Results are now available.` }`, error: '${operation }failed. ${details || 'Please try again or contact support.' }` }` accessibilityService.announceToScreenReader(messages[status]); // Update live region for continuous feedback const liveRegion = document.getElementById('ai-status-live') || this.createAIStatusLiveRegion(); liveRegion.textContent = messages[status]} /** * Create live region for AI status updates */ private createAIStatusLiveRegion(): HTMLElement { const liveRegion = document.createElement('div'); liveRegion.id = 'ai-status-live'; liveRegion.setAttribute('aria-live', 'polite'); liveRegion.setAttribute('aria-atomic', 'true'); liveRegion.style.position = 'absolute'; liveRegion.style.left = '-10000px'; liveRegion.style.width = '1px'; liveRegion.style.height = '1px'; liveRegion.style.overflow = 'hidden'; document.body.appendChild(liveRegion); return liveRegion} /** * Voice command methods */ private triggerAIAnalysis() { const analyzeButton = document.querySelector('[data-ai-action="analyze"]') as HTMLButtonElement; if (analyzeButton) { analyzeButton.click()}else { accessibilityService.announceToScreenReader('Analysis button not found. Please navigate to the AI section.')} private readAISummary() { const summary = document.querySelector('.ai-summary, .ai-result-summary'); if (summary) { accessibilityService.announceToScreenReader(`Summary: ${summary.textContent}`)}else { accessibilityService.announceToScreenReader('No summary available yet.')} private navigateAIResults(direction: 'next' | 'previous') { const results = document.querySelectorAll('.ai-result-card'); const currentFocus = document.activeElement; let currentIndex = -1; results.forEach((result, index) => { if ((result as { title?: unknown; confidence?: unknown; timestamp?: unknown; content?: unknown; contains?: unknown }).contains(currentFocus)) { currentIndex = index}); let targetIndex: number, if (direction === 'next') { targetIndex = (currentIndex + 1) % results.length}else { targetIndex = currentIndex > 0 ? currentIndex - 1 : results.length - 1} if (results[targetIndex]) { (results[targetIndex] as HTMLElement).focus(); accessibilityService.announceToScreenReader(`Navigated to result ${targetIndex + 1 }of ${results.length}`)} private expandCurrentResult() { const focused = document.activeElement; const expandButton = focused?.closest('.ai-result-card')?.querySelector('[aria-expanded="false"]') as HTMLButtonElement; if (expandButton) { expandButton.click()}else { accessibilityService.announceToScreenReader('No expandable content found at current location.')} private collapseCurrentResult() { const focused = document.activeElement; const collapseButton = focused?.closest('.ai-result-card')?.querySelector('[aria-expanded="true"]') as HTMLButtonElement; if (collapseButton) { collapseButton.click()}else { accessibilityService.announceToScreenReader('No collapsible content found at current location.')} private showContextualHelp() { const helpText = `;` Available voice commands: - "Start analysis",or: "Analyze document" to begin AI analysis - "Read summary" to hear the analysis summary - "Next result", or: "Previous result" to navigate between results - "Expand details", or: "Collapse details" to show or hide detailed information - "Stop analysis" to halt the current AI operation You can also use keyboard, navigation: - Tab to move between elements - Enter or Space to activate buttons - Alt+A to open accessibility settings - Alt+S to skip to main content `;` accessibilityService.announceToScreenReader(helpText)} private stopAIOperation() { const stopButton = document.querySelector('[data-ai-action="stop"], .ai-stop-btn') as HTMLButtonElement; if (stopButton) { stopButton.click()}else { accessibilityService.announceToScreenReader('No active AI operation to stop.')} /** * Start voice command listening */ startVoiceCommands() { if (this.voiceRecognition) { this.voiceRecognition.start(); accessibilityService.announceToScreenReader('Voice commands activated. Say a command or: "help" for options.')} /** * Stop voice command listening */ stopVoiceCommands() { if (this.voiceRecognition) { this.voiceRecognition.stop(); accessibilityService.announceToScreenReader('Voice commands deactivated.')} /** * Update accessibility options */ updateOptions(newOptions, Partial<AIAccessibilityOptions>) { this.options = { ...this.options, ...newOptions } if (newOptions.enableVoiceCommands !== undefined) { if (newOptions.enableVoiceCommands) { this.initializeVoiceCommands()}else { this.stopVoiceCommands()} if (newOptions.enhancedFocusIndicators !== undefined) { this.setupEnhancedFocusIndicators()} /** * Get current AI context for contextual help */ getCurrentContext(): string | null { return this.currentAIContext} }
// Global instance for use across the application // Only instantiate in browser environment to avoid SSR errors export const aiAccessibilityPatterns = typeof window !== 'undefined' ? new AIAccessibilityPatterns() : null


