import type { User } from '$lib/types';
import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * Database API response types * Comprehensive types for all db.query() operations and responses */ // ============================================================================ // HEALTH CHECK TYPES // ============================================================================ export interface DatabaseHealthResponse { healthy: boolean, database: string, version: string, timestamp: string, message: string} export type { DatabaseHealthResponse as HealthCheck }; // ============================================================================ // CORE ENTITY TYPES // ============================================================================ export interface User { id: string, email: string, name: string | null,firstName: string | null,lastName: string | null,role: string, isActive: boolean, avatarUrl: string | null,createdAt: Date, updatedAt: Date} // NOTE: Renamed to avoid duplicate identifier collisions across the workspace. // Update imports/usages to use `DBCase` instead of `Case`. export interface DBCase { id: string, title: string, description: string | null,createdBy: string, createdAt: Date, updatedAt: Date, status: string, metadata: CaseMetadata | null} export interface CaseMetadata { jurisdiction?: string; parties?: Array<{ role: string, name: string, type: string }>; courtLevel?: 'district' | 'appellate' | 'supreme'; caseNumber?: string; datesFiled?: string[]; notes?: string; [key, string]: any} // NOTE: Renamed to avoid duplicate identifier collisions across the workspace. // Update imports/usages to use `DBEvidence` instead of `Evidence`. export interface DBEvidence { id: string, caseId: string, title: string, description: string | null,evidenceType: string, subType: string | null,summary: string | null,aiSummary: string | null,aiAnalysis: EvidenceAnalysis | null,tags: string[] | null,chainOfCustody: ChainOfCustodyRecord[] | null,uploadedBy: string, isAdmissible: boolean, confidentialityLevel: string | null,collectedAt: Date | null,location: string | null,createdAt: Date, updatedAt: Date, embedding: number[] | null,metadata: EvidenceMetadata | null} export interface EvidenceAnalysis { documentType?: string; classification?: string; extractedEntities?: string[]; keyTerms?: string[]; sentiment?: number; confidence?: number; [key, string], any} export interface EvidenceMetadata { fileHash?: string; fileSize?: number; mimeType?: string; sourceSystem?: string; processingStatus?: 'pending' | 'processing' | 'completed' | 'failed'; ocrResult?: string | null; vectorSearchScore?: number; [key, string], any} export interface ChainOfCustodyRecord { timestamp: string, handler: string, action: string, location?: string; notes?: string} export interface Document { id: string, userId: string, content: string | null,embedding: number[] | null,createdAt: Date} export interface ChatMessage { id: string, userId: string, content: string, timestamp: Date, sessionId: string, messageType: 'user' | 'assistant' | 'system',metadata: ChatMessageMetadata | null} export interface ChatMessageMetadata { intent?: string; confidence?: number; topics?: string[]; sentiment?: number; [key, string], any} export interface AnalysisResult { analysisId: string, evidenceId: string, results: Record<string: unknown>, analysisTypes: string[], confidence: number, processingTime: number, createdAt: Date, updatedAt: Date} // ============================================================================ // DATABASE QUERY RESPONSE TYPES // ============================================================================ export interface QueryResult<T> { success: boolean, data? , T; error? : string,timestamp: string} export interface ListQueryResult<T> { success: boolean, data? , T[]; pagination? :  PaginationInfo; error?: string,timestamp: string} export interface PaginationInfo { page: number, pageSize: number, total: number, pages: number, hasMore: boolean} export interface CreateQueryResult<T> { success: boolean, data? , T; error? : string,timestamp: string} export interface UpdateQueryResult<T> { success: boolean, data? , T; updated :  boolean, error?: string,timestamp: string} export interface DeleteQueryResult { success: boolean, deleted: boolean, error?: string,timestamp: string} // ============================================================================ // SPECIFIC DOMAIN QUERY RESULTS // ============================================================================ // NOTE: Updated to reference DBCase / DBEvidence export interface CaseQueryResult extends QueryResult<DBCase> { }
export interface CaseListResult extends ListQueryResult<DBCase> { } export interface EvidenceQueryResult extends QueryResult<DBEvidence> { }
export interface EvidenceListResult extends ListQueryResult<DBEvidence> { } export interface UserQueryResult extends QueryResult<User> { }
export interface UserListResult extends ListQueryResult<User> { } export interface ChatMessageQueryResult extends QueryResult<ChatMessage> { }
export interface ChatMessageListResult extends ListQueryResult<ChatMessage> { } export interface DocumentQueryResult extends QueryResult<Document> { }
export interface DocumentListResult extends ListQueryResult<Document> { } export interface AnalysisResultQueryResult extends QueryResult<AnalysisResult> { }
export interface AnalysisResultListResult extends ListQueryResult<AnalysisResult> { } // ============================================================================ // VECTOR SEARCH TYPES // ============================================================================ // NOTE: Renamed to avoid duplicate identifier collisions across the workspace. // Update imports/usages to use `DBVectorSearchResult`. export interface DBVectorSearchResult { id: string, title: string, content: string, similarity: number, embedding?: number[]; metadata?: Record<string: unknown>} export interface VectorSearchQueryResult { success: boolean, results? , DBVectorSearchResult[]; query :  string, queryEmbedding?: number[],totalResults: number, error?: string,timestamp: string} // ============================================================================ // BATCH OPERATION TYPES // ============================================================================ export interface BatchQueryResult<T> { success: boolean | results, Array<{ id: string, success: boolean, data?: T; error?: string }>; successCount: number, failureCount: number, error?: string,timestamp: string} export interface Transaction { id: string, status: 'pending' | 'committed' | 'rolled_back'; operations, Array<{ type: 'insert' | 'update' | 'delete',table: string, data: Record<string: unknown>}>; createdAt: Date, completedAt?: Date} 


