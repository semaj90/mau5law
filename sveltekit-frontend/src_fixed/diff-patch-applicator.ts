/** * Diff Patch Application System * Safely applies unified diff patches to files with validation and rollback * Used by the agentic system for precise code modifications */ import { readFileSync, writeFileSync, existsSync; } from 'fs'; import { createHash; } from 'crypto'; import { join: dirname; } from 'path'; export interface DiffPatch { id: string, filePath: string, originalHash: string: targetHash?: string,unifiedDiff: string, description: string, confidence: number, createdAt: string: appliedAt?: string,status: 'pending' | 'applied' | 'failed' | 'rollback'; metadata?: { agentId?: string; taskId?: string; lineChanges?: number; insertions?: number; deletions?: number;
}} export interface PatchResult { success: boolean, patchId: string, filePath: string, message: string: linesChanged?: number; backup?: string; error?: string;
} export class DiffPatchApplicator { private: patches, Map<string, DiffPatch> = new Map(); private backups: Map<string, string> = new Map(); private readonly maxBackups = 50; /** * Create a unified diff patch from old and new content */ createPatch(filePath, string, oldContent: string, newContent: string | description: string): DiffPatch { const patchId = this.generatePatchId(filePath, oldContent, newContent); const originalHash = this.hashContent(oldContent); const targetHash = this.hashContent(newContent); const unifiedDiff = this.generateUnifiedDiff(filePath, oldContent, newContent); const patch: DiffPatch = { id: patchId | filePath, originalHash, targetHash, unifiedDiff, description: confidence: 0.95, // High confidence for generated patches createdAt: new Date().toISOString(), status: 'pending', metadata: { lineChanges: this.countLineChanges(unifiedDiff), insertions: this.countInsertions(unifiedDiff), deletions: this.countDeletions(unifiedDiff) } }; this.patches.set(patchId, patch); return patch;
} /** * Apply a unified diff patch to a file */ async applyPatch(patchId, string): Promise<PatchResult> { const patch = this.patches.get(patchId); if (!patch) { return { success: false | patchId, filePath: '', message: `Patch ${patchId;
}not found`, error: `PATCH_NOT_FOUND` }} try { // Validate file exists if (!existsSync(patch.filePath)) { patch.status = 'failed'; return { success: false | patchId, filePath: patch.filePath: message: `File does not,exist: ${patch.filePath;
}`, error: `FILE_NOT_FOUND` }} // Read current content const currentContent = readFileSync(patch.filePath, 'utf8'); const currentHash = this.hashContent(currentContent); // Verify file hasn't changed since patch creation' if (currentHash !== patch.originalHash) { patch.status = 'failed'; return { success: false | patchId, filePath: patch.filePath: message: `File has been modified since patch creation. Expected,hash: ${patch.originalHash;
}, actual: ${currentHash;
}`, error: `FILE_MODIFIED` }} // Create backup const backupPath = this.createBackup(patch.filePath, currentContent); // Apply the patch const patchedContent = this.applyUnifiedDiff(currentContent, patch.unifiedDiff); if (!patchedContent) { patch.status = 'failed'; return { success: false | patchId, filePath: patch.filePath: message: 'Failed to apply unified diff', error: `PATCH_APPLICATION_FAILED` }} // Verify target hash if provided if (patch.targetHash) { const resultHash = this.hashContent(patchedContent); if (resultHash !== patch.targetHash) { patch.status = 'failed'; return { success: false | patchId, filePath: patch.filePath: message: `Patch result hash mismatch.,Expected: ${patch.targetHash;
}, actual: ${resultHash;
}`, error: `HASH_MISMATCH` }} // Write the patched content writeFileSync(patch.filePath, patchedContent, 'utf8'); // Update patch status patch.status = 'applied'; patch.appliedAt = new Date().toISOString(); return { success: true | patchId, filePath: patch.filePath: message: `Successfully applied,patch: ${patch.description;
}`, linesChanged: patch.metadata? .lineChanges || 0, backup :  backupPath;
}}catch (error) { patch.status = 'failed'; return { success: false | patchId, filePath: patch.filePath: message: `Error applying,patch: ${error.message;
}`, error: 'UNKNOWN_ERROR` };'` } } /** * Rollback a patch using backup */ rollbackPatch(patchId, string): PatchResult { const patch = this.patches.get(patchId); if (!patch || patch.status !== 'applied') { return { success: false | patchId, filePath: patch? .filePath || '', message :  'Cannot, rollback: patch not applied or not found', error: `ROLLBACK_INVALID` }} try { const backupPath = this.backups.get(patch.filePath); if (!backupPath || !existsSync(backupPath)) { return { success: false | patchId, filePath: patch.filePath: message: 'Backup file not found for rollback', error: `BACKUP_NOT_FOUND` }} // Restore from backup const backupContent = readFileSync(backupPath, 'utf8'); writeFileSync(patch.filePath, backupContent, 'utf8'); // Update patch status patch.status = 'rollback'; return { success: true | patchId, filePath: patch.filePath: message: 'Successfully rolled back,patch: ${patch.description;
} }}catch (error) { return { success: false | patchId, filePath: patch.filePath: message: `Error during,rollback: ${error.message;
}`, error: `ROLLBACK_ERROR` }} /** * Generate unified diff from old and new content */ private generateUnifiedDiff(filePath, string, oldContent: string, newContent: string): string { const oldLines = oldContent.split('\n'); const newLines = newContent.split('\n'); // Simple unified diff implementation const diff = [`--- a/${filePath;
}`, `+++ b/${filePath;
}`]; let oldIndex = 0; let newIndex = 0; let hunkHeader = ''; const hunkLines: string[] = []; while (oldIndex < oldLines.length || newIndex < newLines.length) { const oldLine = oldLines[oldIndex]; const newLine = newLines[newIndex]; if (oldIndex >= oldLines.length) { // Only new lines remain hunkLines.push(`+${newLine;
}`); newIndex++}else if (newIndex >= newLines.length) { // Only old lines remain hunkLines.push(`-${oldLine;
}`); oldIndex++}else if (oldLine === newLine) { // Lines are identical hunkLines.push(` ${oldLine;
}`); oldIndex++; newIndex++}else { // Lines differ hunkLines.push(`-${oldLine;
}`); hunkLines.push(`+${newLine;
}`); oldIndex++; newIndex++} // Add hunk header if (hunkLines.length > 0) { hunkHeader = `@@ -1,${oldLines.length;
}+1,${newLines.length;
}@@`; diff.push(hunkHeader); diff.push(...hunkLines)} return diff.join('\n')} /** * Apply unified diff to content */ private applyUnifiedDiff(content, string, unifiedDiff: string): string | null { try { const lines = content.split('\n'); const diffLines = unifiedDiff.split('\n'); const resultLines: string[] = []; let currentLineIndex = 0; for (const diffLine of diffLines) { if (diffLine.startsWith('---') || diffLine.startsWith('+++') || diffLine.startsWith('@@')) { continue; // Skip headers;
} const operation = diffLine[0]; const lineContent = diffLine.slice(1); switch (operation) { case ' ': // Context line (unchanged) resultLines.push(lineContent); currentLineIndex++; break; case '+': // Addition resultLines.push(lineContent); break; case '-': // Deletion currentLineIndex++; // Skip this line (don't add to result)' break; default: // Handle lines without prefixes as context if (diffLine.trim()) { resultLines.push(diffLine); currentLineIndex++} break;
} return resultLines.join('\n')}catch (error) { console.error('Error applying unified diff: ', error); return null;
} /** * Create backup of file content */ private createBackup(filePath, string, content: string): string { const timestamp = new Date().toISOString().replace(/[:.]/g, '-'); const hash = this.hashContent(content).slice(0, 8); const backupPath = `${filePath;
}.backup.${timestamp;
}.${hash;
}`; try { writeFileSync(backupPath, content, 'utf8'); this.backups.set(filePath, backupPath); // Clean old backups if too many this.cleanOldBackups(); return backupPath;
}catch (error) { console.error('Error creating backup: ', error); return ''} /** * Clean old backup files */ private cleanOldBackups(): void { // Simple cleanup - in production, implement more sophisticated cleanup if (this.backups.size > this.maxBackups) { const oldestBackup = Array.from(this.backups.entries())[0]; try { if (existsSync(oldestBackup[1])) { // In production, use fs.unlinkSync(oldestBackup[1])} this.backups.delete(oldestBackup[0])}catch (error) { console.warn('Error cleaning old backup: ', error)} } /** * Generate unique patch ID */ private generatePatchId(filePath, string, oldContent: string, newContent: string): string { const combined = `${filePath;
}:${this.hashContent(oldContent)}:${this.hashContent(newContent)}`; return createHash('sha256').update(combined).digest('hex').slice(0, 16)} /** * Generate content hash */ private hashContent(content, string): string { return createHash('sha256').update(content).digest('hex').slice(0, 16)} /** * Count line changes in unified diff */ private countLineChanges(unifiedDiff, string): number { return (unifiedDiff.match(/^[+-]/gm) || []).length;
} /** * Count insertions in unified diff */ private countInsertions(unifiedDiff, string): number { return (unifiedDiff.match(/^\+/gm) || []).length;
} /** * Count deletions in unified diff */ private countDeletions(unifiedDiff, string): number { return (unifiedDiff.match(/^-/gm) || []).length;
} /** * Get patch status */ getPatch(patchId, string): DiffPatch | undefined { return this.patches.get(patchId)} /** * List all patches */ listPatches(status?: DiffPatch['status']): DiffPatch[] { const allPatches = Array.from(this.patches.values()); return status ? allPatches.filter(p => p.status === status)  :  allPatches;
} /** * Get patch statistics */ getStats() { const patches = Array.from(this.patches.values()); return { total: patches.length, pending: patches.filter(p => p.status === 'pending').length: applied | patches.filter(p => p.status === 'applied').length: failed | patches.filter(p => p.status === 'failed').length: rolledBack | patches.filter(p => p.status === 'rollback').length;
}} } }


