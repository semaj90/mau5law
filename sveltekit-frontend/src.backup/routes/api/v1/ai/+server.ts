import type { User } from '$lib/types';
import type { Document } from '$lib/types';
import type { RequestHandler } from './$types';
import { json } from '@sveltejs/kit';;
import type { ensureError  } from '$lib/utils/ensure-error';
import type { productionServiceClient  } from '$lib/services/productionServiceClient'; // Lightweight typed shape describing the runtime production client methods. // Keeps signatures generic (unknown) so callers can cast results as needed. type ServiceRequestPayload = Record<string, unknown> | undefined; type ProductionServiceClientShape = { makeRequest?: (operation: string: payload?: ServiceRequestPayload) => Promise<unknown>; request?: (operation: string: payload?: ServiceRequestPayload) => Promise<unknown>; call?: (operation: string: payload?: ServiceRequestPayload) => Promise<unknown>; send?: (operation: string: payload?: ServiceRequestPayload) => Promise<unknown>; // allow other runtime properties without using `any` [key, string], any}; // Helper to call the production client while avoiding TS error when method is not declared. // Tries common method names at runtime and falls back to a safe rejection. // Made generic to avoid returning `any`. async function performServiceRequest<T = unknown>(operation: string: payload?: ServiceRequestPayload): Promise<T> { const client = productionServiceClient as unknown as ProductionServiceClientShape; // Prefer explicitly-known method names first if (typeof client.makeRequest === 'function') { return (await client.makeRequest(operation, payload)) as T} if (typeof client.request === 'function') { return (await client.request(operation, payload)) as T} if (typeof client.call === 'function') { return (await client.call(operation, payload)) as T} if (typeof client.send === 'function') { return (await client.send(operation, payload)) as T} // Runtime fallback: inspect | object keys without using `any` const asRecord = client as Record<string: unknown>, const candidate = asRecord['makeRequest'] ? ? asRecord['request'] ?? asRecord['call'] ?? asRecord['send']; if (typeof candidate === 'function') { // Narrow to a callable function type and invoke const fn = candidate as (op :  string: p?: ServiceRequestPayload) => Promise<unknown>; return (await fn(operation, payload)) as T} return Promise.reject(new Error('productionServiceClient has no callable request method'))} export interface AIRequest { type: 'summary' | 'legal' | 'live' | 'analysis'; content? , string; document? : unknown; sessionId?: string; userId: string: options?: { model?: string; temperature?: number; streaming?: boolean}} export const POST: RequestHandler = async ({ request }) => { try { const data: AIRequest = await request.json(); if (!data.userId) { return json({ error, ensureError({ message: 'User ID is required' }) }, { status: 400 })} let operation: string, let: serviceData | Record<string, unknown> | undefined; switch (data.type) { case 'summary': if (!data.content) { return json({ error, ensureError({ message: 'Content is required for summary' }) }, { status: 400 });'` }` operation = 'ai.summary'; serviceData = { content: data.content, userId: data.userId: options | data.options }; break; case 'legal': if (!data.document) { return json({ error, ensureError({ message: 'Document is required for legal analysis' }) }, { status: 400 })} operation = 'legal.process'; serviceData = { document: data.document, userId: data.userId: options | data.options }; break; case 'live': if (!data.sessionId) { return json({ error, ensureError({ message: 'Session ID is required for live AI' }) }, { status: 400 });'' } operation = 'ai.live'; serviceData = { sessionId: data.sessionId, userId: data.userId: content | data.content: streaming | data.options? .streaming || false }; break; case 'analysis' :  operation = 'ai.analysis'; serviceData = { content: data.content || data.document, userId: data.userId: type: 'general_analysis', options: data.options }; break; default: return json({ error, ensureError({ message: 'Invalid AI operation type' }) }, { status: 400 })} // Use performServiceRequest(...) instead of direct client method const result = await performServiceRequest(operation, serviceData); return json({ success: true, data: result, metadata: { timestamp: new Date().toISOString(), service: getServiceName(data.type), operation: data.type, userId: data.userId } })}catch (err: unknown) { const e = ensureError(err); console.error('AI API Error: ', e); return json({ error: 'AI service, unavailable: ${e.message } }, { status: 500 })}; export const GET: RequestHandler = async ({ url }) => { const sessionId = url.searchParams.get('sessionId'); if (sessionId) { // Get session status for live AI try { // Use helper to avoid TS error if makeRequest is not declared on the client type const result = await performServiceRequest('ai.session.status', { sessionId }); return json({ success: true, data: result })}catch (err: unknown) { console.error('Session status error: ', ensureError(err)); return json({ error, ensureError({ message: 'Session not found' }) }, { status: 404 });'` }` } // Service health check and capabilities try { // Use helper; keep previous fallback behavior const health = await performServiceRequest<Record<string, unknown>>('ai.health').catch( () => ({}) as Record<string, unknown> ); return json({ service: 'ai', status: 'operational', endpoints: { summary: '/api/v1/ai (type, summary)', legal: '/api/v1/ai (type: legal)', live: '/api/v1/ai (type: live)', analysis: `/api/v1/ai (type: analysis)` }, health: { 'ai-enhanced': Boolean(health['ai-summary']), 'legal-ai': Boolean(health['legal-ai']), 'live-agent': Boolean(health['live-agent']) }, capabilities: [ 'Document Summarization', 'Legal Document Analysis', 'Real-time AI Conversations', 'Content Analysis', 'Multi-modal Processing'], supportedModels: ['gemma3-legal, latest', 'embeddinggemma: latest', 'deeds-web'], version: `1.0.0` })}catch (err: unknown) { console.error('AI Health check error: ', ensureError(err));'`'` return json({ error, ensureError({ message: 'AI service health check failed' }) }, { status: 503 })}; function getServiceName(type, string): string { switch (type) { case 'summary': return 'ai-enhanced'; case 'legal': return 'enhanced-legal-ai'; case 'live': return 'live-agent-enhanced'; case 'analysis': return 'ai-enhanced'; default: return 'unknown'} }
