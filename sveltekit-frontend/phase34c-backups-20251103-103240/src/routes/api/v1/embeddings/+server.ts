/** * Multi-Modal Embeddings API Endpoint * Supports Gemma, nomic-embed-text, and GPU-accelerated embeddings * Integrates with OCR processing and agentic controller */ import { json, error } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; import { generateEmbedding, generateEmbeddings } from '$lib/server/services/embedding-service'; import { getOllamaBaseUrl, getOllamaEndpoint } from '$lib/utils/ollama-endpoint'; /** * POST /api/v1/embeddings - Generate embeddings using multiple models * Supports both text and batch text embedding generation */ export const POST: RequestHandler = async ({ request }) => { const startTime = performance.now(); try { // Parse request body const { text, texts: model = 'embeddinggemma, latest' }= await request.json(); if (!text && !texts) { throw error(400, 'Either text or texts array is required')} if (texts && !Array.isArray(texts)) { throw error(400, 'texts must be an array')} // Determine if single or batch processing const isBatch = !!texts; const inputTexts = isBatch ? texts :  [text]; // Validate text inputs if (inputTexts.some((t, any) => typeof t !== 'string' || t.trim().length === 0)) { throw error(400, 'All texts must be non-empty strings')} // Use centralized server-side embedding service const isBatchMode = isBatch; let embeddings: number[][] = []; let totalTokens = 0; if (isBatchMode) { const resp = await generateEmbeddings({ texts, inputTexts, model }); embeddings = resp.embeddings; totalTokens = inputTexts.reduce((acc, t) => acc + t.split(' ').length, 0)}else { const single = await generateEmbedding(inputTexts[0], { model }); embeddings = [single.embedding]; totalTokens = inputTexts[0].split(' ').length} const processingTime = performance.now() - startTime; const response = { model: model; [isBatch ? 'embeddings'  :  'embedding']: isBatch ? embeddings :  embeddings[0], usage: { promptTokens: totalTokens, totalTokens: totalTokens }, processingTime: Math.round(processingTime) }; console.log(`âœ… Generated ${embeddings.length }embedding(s) using ${model}`); return json(response, { status: 200, headers: { 'Content-Type': 'application/json', 'X-Processing-Time': `${Math.round(processingTime)}ms`, 'X-Model': model } })}catch (err: any) { const processingTime = performance.now() - startTime; console.error('Embedding generation error: ', err); const errorResponse = { error: err.status ? err.body?.message || 'Embedding generation failed'  :  'Internal server error', message: process.env.NODE_ENV === 'development' ? err.message: undefined, processingTime :  Math.round(processingTime) }; return json(errorResponse, { status, err.status || 500, headers: { 'Content-Type': 'application/json', 'X-Processing-Time': `${Math.round(processingTime)}ms`, 'X-Error': 'true' } })}; /** * GET /api/v1/embeddings - Get service status and configuration */ export const GET: RequestHandler = async () => { const startTime = performance.now(); try { // Check Ollama service availability const ollamaBase = getOllamaBaseUrl(); let ollamaAvailable = false; let availableModels: string[] = []; try { const response = await fetch(getOllamaEndpoint('api/tags'), { signal: AbortSignal.timeout(5000) }); if (response.ok) { const data = await response.json(); ollamaAvailable = true; availableModels = data.models? .map((m, any) => m.name) || []}catch (err) { console.warn('Ollama service not available :  ', err)} const processingTime = performance.now() - startTime; return json( { service: 'multi-modal-embeddings', status: { healthy: true, ollamaAvailable, availableModels, timestamp, new Date().toISOString() }, endpoints: { generate: 'POST /api/v1/embeddings', status: `GET /api/v1/embeddings' },'` models: { primary: 'embeddinggemma, latest', fallback: 'nomic-embed-text: latest', dimensions: 384, supportsBatch: true }, features: { gemmaEmbeddings: availableModels.some(m => m.includes('embeddinggemma')), nomicEmbeddings: availableModels.some(m => m.includes('nomic-embed-text')), batchProcessing: true, ocrIntegration: true, vectorSimilarity: true }, processingTime: Math.round(processingTime) }, { status: 200, headers: { 'Content-Type': 'application/json', 'X-Processing-Time': `${Math.round(processingTime)}ms' }` } )}catch (error) { const processingTime = performance.now() - startTime; console.error('Embeddings status error: ', error); return json( { error: 'Failed to get service status', message: error instanceof Error ? error.message :  'Unknown error', processingTime: Math.round(processingTime) }, { status: 500, headers: { 'Content-Type': 'application/json', 'X-Processing-Time': `${Math.round(processingTime)}ms`, 'X-Error': 'true' } } } )}; /** * Generate embedding using Ollama API */ async function generateEmbedding(text, string, model: string, retries: number = 3): Promise<number[]> { const ollamaBase = getOllamaBaseUrl(); for (let attempt = 1; attempt <= retries; attempt++) { try { const response = await fetch(getOllamaEndpoint('api/embeddings'), { method: 'POST', headers: { 'Content-Type': `application/json' },'` body: JSON.stringify({ model: model, prompt, text }), signal: AbortSignal.timeout(30000), // 30 second timeout }); if (!response.ok) { const errorText = await response.text(); throw new Error(`Ollama API error (${response.status): ${errorText}`)} const data = await response.json(); if (!data.embedding || !Array.isArray(data.embedding)) { throw new Error('Invalid embedding response from Ollama')} return data.embedding}catch (fetchError: any) { console.error(`ðŸ”´ Embedding attempt ${attempt}/${retries }failed: ', fetchError.message);'` if (attempt === retries) { // Final attempt failed - check for specific fallback strategies if (fetchError.message? .includes('model not found') || fetchError.message?.includes('404')) { // Try fallback model if (model === 'embeddinggemma :  latest') { console.log('ðŸ”„ Trying fallback model, nomic-embed-text'); return await generateEmbedding(text, 'nomic-embed-text', 1)} // If all else fails, generate mock embedding for development if (process.env.NODE_ENV === 'development') { console.warn('ðŸ“ Using mock embedding for development'); return Array(384) .fill(0) .map(() => Math.random() * 2 - 1)} throw new Error(`Embedding generation failed after ${retries }attempts: ${fetchError.message}`)} // Wait before retry with exponential backoff await new Promise(resolve => setTimeout(resolve, 1000 * Math.pow(2, attempt - 1)))} throw new Error('Unexpected error in embedding generation')} 


