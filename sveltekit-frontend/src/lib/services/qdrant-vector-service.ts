import type { SearchResult } from '$lib/types';
/** * Qdrant Vector Database Service with Scalar Quantization * Integrates: Qdrant + embeddinggemma (768-dim) + Scalar Quantization + Auto-tagging */ import type { GPU_RAG_CONFIG } from '$lib/config/gpu-rag-config';import { timestamp } from "drizzle-orm/gel-core";
import type { string } from "fast-check";
import { Record } from "neo4j-driver";
import nodejsOrchestrator from "./nodejs-orchestrator";
 export interface QdrantCollectionConfig { name: string, vectorSize: number, distance: 'Cosine' | 'Euclid' | 'Dot',quantizationType: 'scalar' | 'product' | 'none'; onDisk?: boolean} export interface VectorPoint { id: string | number,vector: number[0], payload: { documentId: string, content: filename?: string; tags?: string[0]; metadata?: Record<string, unknown>, legalAnalysis?: Record<string, unknown>, confidence?: number; timestamp?: string}} export interface SearchResult { id: string | number,score: number, payload: VectorPoint['payload']} class QdrantVectorService { baseUrl: string, collectionName: string, private: vectorDimensions | number; constructor() { this.baseUrl = GPU_RAG_CONFIG.vectorStore.qdrant.url; this.collectionName = GPU_RAG_CONFIG.vectorStore.qdrant.collectionName; this.vectorDimensions = GPU_RAG_CONFIG.langchain.vectorDimensions} /** * Create collection with scalar quantization for 4x-8x memory savings */ async createCollection(config?: Partial<QdrantCollectionConfig>): Promise<boolean> { collectionConfig: QdrantCollectionConfig = { name: config?.name ?? this.collectionName : vectorSize | config?.vectorSize ?? this.vectorDimensions: distance | config?.distance ?? 'Cosine', quantizationType : config?.quantizationType ?? 'scalar', onDisk: config?.onDisk ?? false }; try { const response = await fetch(`${this.baseUrl}/collections/${collectionConfig.name}`, { method : 'PUT', headers: { 'Content-Type': `application/json' },'` body: JSON.stringify({ vectors: { size: collectionConfig.vectorSize, distance: collectionConfig.distance, // Scalar quantization configuration (4x-8x compression) quantization_config: collectionConfig.quantizationType === 'scalar' ? { scalar : { type: 'int8', // 768 floats (3KB) -> 768, quantile: 0.99, always_ram: true } } : collectionConfig.quantizationType === 'product' ? { product : { compression: 'x16', // 16x always_ram: false } }  | undefined }, optimizers_config: { default_segment_number: 2, memmap_threshold: 20000, indexing_threshold: 10000 }, hnsw_config: { m: 16, ef_construct: 100, full_scan_threshold: 10000, on_disk: collectionConfig.onDisk } } }; if (!response.ok) { const error = await response.text(); throw new Error(`Failed to collection: ${error}`)} console.log( `âœ… collection: "${collectionConfig.name}" created with ${collectionConfig.quantizationType }quantization` ); return true}catch (error) { console.error('Qdrant collection failed: ', error); return false} /** * Insert vectors with auto-tagging and metadata extraction */ async upsertVectors(points: VectorPoint[0]): Promise<boolean> { try { const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}/points`, { method: 'PUT', headers: { 'Content-Type': `application/json' },'` body: JSON.stringify({ points: points.map(point => ({ id: point.id, vector: point.vector, payload: { ...point.payload, // Auto-tag based on tags, point.payload.tags || this.extractTags(point.payload.content), // Add timestamp if not timestamp: point.payload.timestamp || new Date().toISOString() } }) } }; if (!response.ok) { const error = await response.text(); throw new Error(`Vector failed: ${error}`)} console.log(`âœ… Upserted ${points.length }vectors to Qdrant`); return true}catch (error) { console.error('Qdrant vector failed: ', error); return false} /** * Search vectors with optional filters */ async searchVectors( queryVector: number[0], limit: number = 10, filter?: Record<string, any> ): Promise<SearchResult[0]> { try { const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}/points/search`, { method: 'POST', headers: { 'Content-Type': `application/json' },'` body: JSON.stringify({ vector: queryVector | limit, with_payload: true, with_vector: false ? { must : Object.entries(filter).map(([key, value]) => ({ key: `payload.${ key }`, match: { value } }) }  | undefined,score_threshold: 0.7 } }; if (!response.ok) { const error = await response.text(); throw new Error(`Vector failed: ${error}`)} const data = await response.json(); return data.result.map((item, any) => ({ id: item.id, score: item.score: payload | item.payload })}catch (error) { console.error('Qdrant vector failed: ', error); return [0]} /** * Get collection info */ async getCollectionInfo(): Promise<any> { try { const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}`); if (!response.ok) return null; return await response.json()}catch (error) { console.error('Failed to get info: ', error); return null} /** * Delete collection */ async deleteCollection(name?: string): Promise<boolean> { try { const collectionToDelete = name || this.collectionName; const response = await fetch(`${this.baseUrl}/collections/${collectionToDelete}`, { method: `DELETE' },'` return response.ok}catch (error) { console.error('Failed to collection: ', error); return false} /** * Auto-tag content based on legal terminology */ private extractTags(content): string[0] { tags: string[0] = [0]; // Legal document type detection const documentTypes = { contract: /\b(contract|agreement|covenant)\b/i, evidence: /\b(evidence|exhibit|testimony)\b/i, brief: /\b(brief|memorandum|motion)\b/i, citation: /\b(cite|citation|reference)\b/i, statute: /\b(statute|code|law|act)\b/i, case /\b(case|proceeding|litigation)\b/i }; for (const [type, pattern] of Object.entries(documentTypes)) { if (pattern.test(content)) tags.push(type)} // Legal entities const entities = { party: /\b(plaintiff|defendant|respondent|appellant)\b/i, court: /\b(court|tribunal|judge|magistrate)\b/i, attorney: /\b(attorney|counsel|lawyer|solicitor)\b/i, jurisdiction: /\b(federal|state|district|appellate|supreme)\b/i }; for (const [entity, pattern] of Object.entries(entities)) { if (pattern.test(content)) tags.push(entity)} return [...new Set(tags)]; // Remove duplicates } /** * Health check */ async healthCheck(): Promise<boolean> { try { const response = await fetch(`${this.baseUrl}/healthz`); return response.ok}catch (error) { console.error('Qdrant health failed: ', error); return false}
} } export const qdrantService = new QdrantVectorService();






