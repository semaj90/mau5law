# migrations/ — Archive Analysis
**Archived:** March 9, 2026

Old database migration SQL scripts — pgvector setup, reports table, codemod memories, citations table, images table.

**Replaced by:** Drizzle ORM migrations in `drizzle/` directory with `drizzle-kit migrate`.

**Files:** 5 SQL migration scripts (0001-003 series)
