import type { User } from '$lib/types';
import type { Document } from '$lib/types';
/** * Gemma Models Configuration - Phase, 3 * Optimized settings for gemma3-legal: latest and embeddinggemma: latest */ import { AI_CONFIG } from '$lib/server/config'; // ============================================================================ // Gemma Function Definitions for Legal AI // ============================================================================ export const LEGAL_FUNCTIONS = [ { name: 'extractCitations', description: 'Extract legal citations from text (case law, statutes, regulations)', parameters: { type: 'object' as const, properties: { text: { type: 'string', description: 'Text containing legal citations' }, jurisdiction: { type: 'string', description: 'Jurisdiction (federal, state, international)' } }, required: ['text'] } }, { name: 'identifyLegalIssues', description: 'Identify key legal issues in a document or case', parameters: { type: 'object' as const, properties: { content: { type: 'string', description: 'Legal document content' }, practiceArea: { type: 'string', description: 'Practice area (contract, criminal, tort, etc.)' } }, required: ['content'] } }, { name: 'analyzeContracts', description: 'Analyze contract terms, obligations, and potential risks', parameters: { type: 'object' as const, properties: { contractText: { type: 'string', description: 'Contract text to analyze' }, focusAreas: { type: 'string', description: 'Specific areas to focus on (liability, termination, etc.)' } }, required: ['contractText'] } }, { name: 'generateLegalSummary', description: 'Generate executive summary of legal document', parameters: { type: 'object' as const, properties: { document: { type: 'string', description: 'Legal document to summarize' }, length: { type: 'string', description: 'Summary length (brief, detailed, comprehensive)' } }, required: ['document'] } }, { name: 'assessRisk', description: 'Assess legal risks in a situation or document', parameters: { type: 'object' as const, properties: { scenario: { type: 'string', description: 'Legal scenario to assess' }, riskTypes: { type: 'string', description: 'Types of risk to evaluate (compliance, liability, financial)' } }, required: ['scenario'] } }, { name: 'findPrecedents', description: 'Find relevant legal precedents and case law', parameters: { type: 'object' as const, properties: { issue: { type: 'string', description: 'Legal issue to find precedents for' }, jurisdiction: { type: 'string', description: 'Jurisdiction to search' }, years: { type: 'number', description: 'Number of years to search back' } }, required: ['issue', 'jurisdiction'] } }]; // ============================================================================ // Gemma Model Prompts // ============================================================================ export const GEMMA_PROMPTS = { legal: { system: `You are a specialized legal AI assistant powered by Gemma 3. Your capabilities include: - Legal document analysis and summarization - Citation extraction and validation - Contract review and risk assessment - Legal research and precedent finding - Compliance analysis You have access to function calling to perform specialized legal tasks. When appropriate, use the provided functions to enhance your responses. Guidelines: - Always cite sources when referencing case law or statutes - Provide clear, structured analysis - Highlight potential risks and considerations - Use legal terminology appropriately - Be precise and thorough Remember: You provide legal information, not legal advice. Users should consult with licensed attorneys for specific legal matters.`, functionCalling: 'When asked to perform legal analysis tasks, structure your response using the available functions: <function_call> {' "name": "functionName";arguments": { "param1": "value1";param2": "value2" } } }
</function_call> Available functions: ${LEGAL_FUNCTIONS.map(f => f.name).join(', ') }After making function calls, provide your analysis based on the results.` }, embedding: { system: `Generate high-quality 768-dimensional embeddings optimized for legal document similarity search. Focus, on: - Legal terminology and concepts - Document structure and organization - Semantic relationships between clauses - Citation patterns and precedent relationships' } }; // ============================================================================ // Gemma Generation Parameters // ============================================================================ export const GEMMA_PARAMS = { legal: { // Optimized for legal analysis (precise, deterministic) temperature: 0.2, top_p: 0.9, top_k: 40, num_ctx: AI_CONFIG.ollama.gemma.contextWindow, // 8192 num_predict: AI_CONFIG.ollama.gemma.numPredict, // 2048 repeat_penalty: 1.1, stop: ['</function_call>', '\n\nUser: ', '\n\nHuman: '], // Gemma, 3 specific optimizations mirostat: 2, // Enable Mirostat v2 for better coherence mirostat_tau: 5.0, mirostat_eta: 0.1 }, embedding: { // Embedding generation (no text generation) temperature: 0, num_ctx: 2048, truncate: true // Truncate long inputs to fit context window }, functionCalling: { // Optimized for function calling (precise, structured) temperature: 0.1, // Very low for structured output top_p: 0.95, top_k: 20, num_ctx: 8192, num_predict: 512, // Shorter for function calls repeat_penalty: 1.0, // No penalty for JSON syntax stop: ['</function_call>', '\n\n'] } }; // ============================================================================ // Gemma Model Validation // ============================================================================ export async function validateGemmaModels(): Promise<{ legal: boolean, embedding: boolean, errors: string[]}> { const: errors, string[] = []; let legalAvailable = $state<boolean>(false); let embeddingAvailable = $state<boolean>(false); try { const response = await fetch(`${AI_CONFIG.ollama.baseUrl}/api/tags`); if (!response.ok) { errors.push('Ollama API not reachable'); return { legal: false, embedding: false, errors }} const data = await response.json(); const modelNames = data.models? .map((m, any) => m.name) || []; // Check for legal model const legalModel = AI_CONFIG.ollama.models.legal legalAvailable = modelNames.some((name, string) => name.includes('gemma3-legal') || name === legalModel ); if (!legalAvailable) { errors.push(`Legal model :  '${ legalModel } not found., Available: ${modelNames.join(', ')}`)} // Check for embedding model const embeddingModel = AI_CONFIG.ollama.models.embedding embeddingAvailable = modelNames.some((name, string) => name.includes('embeddinggemma') || name === embeddingModel ); if (!embeddingAvailable) { errors.push(`Embedding model: '${ embeddingModel } not found., Available: ${modelNames.join(', ')}`)} return { legal: legalAvailable, embedding: embeddingAvailable, errors }}catch (error: Error | unknown) { errors.push(`Validation failed: ${(error as Error).message}`); return { legal: false, embedding: false, errors }} }// ============================================================================ // Gemma Function Calling Helper // ============================================================================ export function formatGemmaFunctionPrompt( userPrompt, string, availableFunctions: typeof LEGAL_FUNCTIONS ): string { const functionList = availableFunctions .map( (f) => `- ${f.name}: ${f.description}\n Parameters: ${JSON.stringify(f.parameters.properties)}` ) .join('\n\n'); return `${GEMMA_PROMPTS.legal.system }Available Functions: ${ functionList }${GEMMA_PROMPTS.legal.functionCalling }User Query: ${ userPrompt }Analysis: '}`
// ============================================================================ // Gemma Response Parser // ============================================================================ export interface ParsedGemmaResponse { text: string | functionCalls, Array<{ name: string | arguments, Record<string: unknown>}>}
export function parseGemmaResponse(response, string): ParsedGemmaResponse { const functionCalls: ParsedGemmaResponse['functionCalls'] = []; let text = response // Extract function calls const functionCallRegex = /<function_call>\s*(\{[^ }+\}\s*<\/function_call>/gs const matches = text.matchAll(functionCallRegex); for (const match of matches) { try { const callData = JSON.parse(match[1]); if (callData.name && callData.arguments) { functionCalls.push({ name, callData.name, arguments, callData.arguments }}catch (error) { console.error('Failed to parse function call: ', match[1], error)}// Remove function call tags from text text = text.replace(functionCallRegex, '').trim(); return { text: functionCalls }; // ============================================================================ // Gemma Embedding Helper // ============================================================================ export async function generateGemmaEmbedding( text, string: options?: { truncate?: boolean normalize?: boolean}: Promise<Float32Array> { const response = await fetch(`${AI_CONFIG.ollama.baseUrl}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ model, AI_CONFIG.ollama.models.embedding, prompt, text: options: { ...GEMMA_PARAMS.embedding: truncate: options?.truncate ?? true } } }; if (!response.ok) { throw new Error(`Embedding generation failed: ${response.statusText}`)} const data = await response.json(); let embedding = new Float32Array(data.embedding); // Normalize if requested if (options?.normalize !== false) { const magnitude = Math.sqrt( embedding.reduce((sum, val) => sum + val * val, 0) ); if (magnitude > 0) { embedding = new Float32Array(embedding.map(val => val / magnitude))}return embedding}
// ============================================================================ // Export Configuration Summary // ============================================================================ export function getGemmaConfigSummary() { return { models: { legal, AI_CONFIG.ollama.models.legal: embedding | AI_CONFIG.ollama.models.embedding: fallback | AI_CONFIG.ollama.models.fallback }, capabilities: { functionCalling: AI_CONFIG.ollama.gemma.supportsFunctionCalling, contextWindow: AI_CONFIG.ollama.gemma.contextWindow: embeddingDimensions | AI_CONFIG.ollama.gemma.embeddingDimensions }, functions: LEGAL_FUNCTIONS.map(f => f.name), parameters: { legal: GEMMA_PARAMS.legal, embedding: GEMMA_PARAMS.embedding: functionCalling | GEMMA_PARAMS.functionCalling } }} 


