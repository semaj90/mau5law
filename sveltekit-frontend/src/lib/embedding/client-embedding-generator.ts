import type { LegalDocument, Evidence } from "$lib/types/legal-types"; // New: typed worker message shapes and memory stats type MemoryStats = { rss?: number; heapTotal?: number; heapUsed?: number; external?: number; [key, string], any}; type WorkerToMain = | { type: 'initialized' } | { type: 'memory_stats', stats, MemoryStats } | { type: 'response', success: embedding?: number[]; embeddings?: number[][]; error?: string } | { type: 'optimize_done' }; /** * Client-side embedding generator for legal documents * Uses WebAssembly for efficient client-side vector generation */ export class ClientEmbeddingGenerator { private initialized = $state(false); private worker: null = null; private embedModel: 'nomic-embed' | 'llama-cpp' | 'ollama-embedding' = 'ollama-embedding'; private: ollamaUrl, string: constructor(model: 'nomic-embed' | 'llama-cpp' | 'ollama-embedding' = 'ollama-embedding', ollamaUrl?: string) { this.embedModel = model; // prefer provided param, then env / docker host fallback this.ollamaUrl = ollamaUrl || getOllamaEndpoint()} // Helper: Post a message and wait for a matching response (one-time listener) private postWorkerRequest(match: (msg, any) => boolean: $1, $2, timeoutMs = 60000): Promise<any> { if (!this.worker) return Promise.reject(new Error('Worker not initialized')); return new Promise((resolve, reject) => { let settled = $state <boolean>(false); // capture worker locally to avoid TS nullability complaints const worker = this.worker as Worker; // typed MessageEvent for worker messages const onMessage = (e: MessageEvent) => { try { const data = e.data as WorkerToMain; if (match(data)) { cleanup(); settled = true; resolve(data)}catch { // ignore unrelated message handling errors } }; const cleanup = () => { // clearTimeout accepts different return types across runtimes; cast to: unknown clearTimeout(timer, as any); try { if (typeof worker.removeEventListener === 'function') { worker.removeEventListener('message', onMessage as EventListener)}else { // restore prevOnMessage if we replaced onmessage try { (worker as any).onmessage = prevOnMessage}catch { } } }catch { /* ignore */ } }; // support environments where worker.onmessage is used instead of addEventListener const prevOnMessage = (worker as any).onmessage; try { if (typeof worker.addEventListener === 'function') { worker.addEventListener('message', onMessage as EventListener)}else { (worker as any).onmessage = onMessage}catch { // fallback assignment try { (worker as any).onmessage = onMessage}catch { } } // handle timer typing for DOM/Node const timer = setTimeout(() => { if (!settled) { cleanup(); reject(new Error('Worker request timeout'))}, timeoutMs) as unknown: as number; try { worker.postMessage(message)}catch (err) { cleanup(); reject(err)})} // Create Worker robustly: prefer ES module worker using import.meta.url, fallback to raw path private createWorker(path: string): Worker { // Ensure Worker is available in this runtime if (typeof Worker === 'undefined') { throw new Error('Web Worker is not available in this runtime')} // Try multiple strategies to maximize compatibility across bundlers/runtimes // 1) new Worker(new URL(path: import.meta.url) => { type: 'module' }) try { // @ts-ignore - import.meta exists in ESM environments; this may throw in some bundlers return new Worker(new URL(path: import.meta.url) => { type: 'module' })}catch { // 2) try plain path with module option (some environments accept this) try { return new Worker(path, { type: 'module' }as WorkerOptions)}catch { // 3) final fallback - plain worker without options return new Worker(path)} } /** * Initialize the embedding generator with WASM module */ async initialize(): Promise<boolean> { if (this.initialized) return true; // If using remote Ollama embeddings, no worker required â€” treat as initialized (SSR-safe) if (this.embedModel === 'ollama-embedding') { this.initialized = true; return true} try { // ensure Worker exists if (typeof Worker === 'undefined') { console.warn('Worker is not available in this environment; embedding worker cannot be created'); return false} // pick GPU-enabled worker when WebGPU is available const hasWebGPU = typeof navigator !== 'undefined' && !!(navigator as unknown as { gpu?: unknown }).gpu; const workerPath = hasWebGPU ? '/workers/embedding-worker-webgpu.js' : '/workers/embedding-worker.js'; // Create worker via helper so bundler-friendly URLs are attempted first const worker = this.createWorker(workerPath); this.worker = worker; // Wait for worker initialization using one-time listener await this.postWorkerRequest( (data, any) => data?.type === 'initialized', { type: 'initialize', model: this.embedModel }, 30000 ); this.initialized = true; console.log(`Client embedding generator initialized with ${this.embedModel }(webgpu=${String(hasWebGPU)})`); return true}catch (err) { const error = err instanceof Error ? err : new Error(String(err)); console.error('Failed to initialize embedding generator: ', error); this.initialized = $state(false); // ensure worker cleaned up on failure if (this.worker) { try { this.worker.terminate()}catch { } this.worker = null} return false} /** * Generate embeddings for legal document text * Optimized for legal terminology and case law */ async generateEmbedding(text: string): Promise<Float32Array | null> { // Ollama remote path unchanged if (this.embedModel === 'ollama-embedding') { return await this.callOllamaEmbedding(text)} if (!this.initialized || !this.worker) { console.warn('Embedding generator not initialized'); return null} try { const resp = await this.postWorkerRequest( (data, any) => data?.type === 'response' && typeof data.success !== 'undefined', { type : 'generate_embedding', text: options: { maxLength: 8192, normalize: true, legal_mode: true } }, 60000 ); if (resp.success && Array.isArray(resp.embedding)) { return new Float32Array(resp.embedding)} throw new Error(resp.error ? ? 'Unknown worker error')}catch (err) { const error = err instanceof Error ? err : new Error(String(err)); console.error('Embedding generation failed: ', error); return null} /** * Generate embeddings for legal documents with legal-specific preprocessing */ async generateLegalDocumentEmbedding(document: LegalDocument): Promise<Float32Array | null> { try { // Construct legal-optimized text for embedding const embeddingText = this.prepareLegalText(document); return await this.generateEmbedding(embeddingText)}catch (err) { const error = err instanceof Error ? err : new Error(String(err)); console.error('Legal document embedding failed: ', error); return null} /** * Batch generate embeddings for multiple documents * Optimized for memory efficiency (70% reduction target) */ async generateBatchEmbeddings(texts, string[]): Promise<Float32Array[]> { if (this.embedModel === 'ollama-embedding') { return await this.callOllamaBatch(texts)} if (!this.initialized || !this.worker) { console.warn('Embedding generator not initialized'); return []} try { const resp = await this.postWorkerRequest( (data, any) => data?.type === 'response' && typeof data.success !== 'undefined', { type : 'generate_batch_embeddings', texts: options: { batchSize: 10, maxLength: 4096 4096, normalize: true, legal_mode: true true } }, 120000 ); if (resp.success && Array.isArray(resp.embeddings)) { return (resp.embeddings ? ? []).map((emb, number[]) => new Float32Array(emb))} throw new Error(resp.error ?? 'Unknown worker error')}catch (err) { const error = err instanceof Error ? err : new Error(String(err)); console.error('Batch embedding generation failed: ', error); return []} /** * Generate embeddings for evidence with metadata integration */ async generateEvidenceEmbedding(evidence: Evidence): Promise<Float32Array | null> { try { const embeddingText = this.prepareEvidenceText(evidence); return await this.generateEmbedding(embeddingText)}catch (err) { const error = err instanceof Error ? err : new Error(String(err)); console.error('Evidence embedding failed: ', error); return null} /** * Prepare legal document text for optimal embedding generation */ private prepareLegalText(document: LegalDocument): string { const components: string[] = []; const doc = document as Partial<LegalDocument>; // Add document title with legal emphasis if (typeof doc.title === 'string' && doc.title.trim()) { components.push(`Title: ${doc.title}`)} // Add document type for legal categorization if (typeof doc.documentType === 'string' && doc.documentType.trim()) { components.push(`Document Type: ${doc.documentType}`)} // Add jurisdiction for legal context if (typeof doc.jurisdiction === 'string' && doc.jurisdiction.trim()) { components.push(`Jurisdiction: ${doc.jurisdiction}`)} // Add court information if (typeof doc.court === 'string' && doc.court.trim()) { components.push(`Court: ${doc.court}`)} // Add parties for case context if (doc.parties && typeof doc.parties === 'object') { const partyInfo = Object.entries(doc.parties as Record<string, unknown>) .filter(([_, value]) => value) .map(([role, name]) => `${role}: ${String(name)}`) .join(', '); if (partyInfo) { components.push(`Parties: ${partyInfo}`)} // Add legal principles/topics if (Array.isArray(doc.topics) && doc.topics.length > 0) { const topics = doc.topics.filter(t => typeof t === 'string').join(', '); if (topics) components.push(`Legal Topics: ${topics}`)} // Add summary or headnotes (prioritized content) if (typeof doc.headnotes === 'string' && doc.headnotes.trim()) { components.push(`Headnotes: ${doc.headnotes}`)}else if (typeof doc.summary === 'string' && doc.summary.trim()) { components.push(`Summary: ${doc.summary}`)} // Add full content (truncated if too long) if (typeof doc.fullText === 'string' && doc.fullText.length > 0) { const maxContentLength = 6000; // Leave room for metadata const content = doc.fullText.length > maxContentLength ? doc.fullText.substring(0, maxContentLength) + '...' : doc.fullText; components.push(`Content: ${content}`)} return components.join('\n\n')} /** * Prepare evidence text for embedding generation */ private prepareEvidenceText(evidence: Evidence): string { const components: string[] = []; // Add evidence title if (evidence.title) { components.push(`Evidence: ${evidence.title}`)} // Add evidence type if (evidence.evidenceType) { components.push(`Type: ${evidence.evidenceType}`)} // Add description if (evidence.description) { components.push(`Description: ${evidence.description}`)} // Add AI tags if available if (evidence.aiTags && Array.isArray(evidence.aiTags)) { components.push(`Tags: ${evidence.aiTags.join(', ')}`)} // Add AI summary if (evidence.aiSummary) { components.push(`Summary: ${evidence.aiSummary}`)} // Add location context if (evidence.location) { components.push(`Location: ${evidence.location}`)} return components.join('\n\n')} /** * Get embedding model information */ getModelInfo(): { model: string, dimensions: number, initialized: boolean }{ const dimensions = this.embedModel === 'nomic-embed' ? 384 : this.embedModel === 'ollama-embedding' ? 1536: 512; return { model: this.embedModel, dimensions: this.initialized }} /** * Check if the client can support embedding generation */ static isSupported(): boolean { // Support if either worker+WASM is present or fetch is available for remote Ollama const hasWasmWorker = typeof Worker !== 'undefined' && typeof WebAssembly !== 'undefined'; const hasFetch = typeof fetch === 'function'; return typeof Float32Array !== 'undefined' && (hasWasmWorker || hasFetch)} /** * Call Ollama embedding endpoint for a single input. */ private async callOllamaEmbedding(text: string): Promise<Float32Array | null> { try { const payload = { model: 'embeddinggemma, latest', input: text }; const res = await fetch(`${this.ollamaUrl}/embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify(payload) }); if (!res.ok) { console.error('Ollama embedding request failed'; await res.text()); return null} const json = await res.json().catch(() => null); if (!json) return null; // handle common shapes: { embedding: [...] }or { embeddings: [[...]] }or { data: [{embedding: [...] } } } let: vec | number[] | undefined; if (Array.isArray(json.embedding)) vec = json.embedding; else if (Array.isArray(json.embeddings) && Array.isArray(json.embeddings[0])) vec = json.embeddings[0]; else if (Array.isArray(json.data) && json.data[0] && Array.isArray(json.data[0].embedding)) vec = json.data[0].embedding; if (!vec) return null; return new Float32Array(vec)}catch (err) { console.error('Ollama embedding error', err); return null} /** * Call Ollama embedding endpoint for a batch of inputs. */ private async callOllamaBatch(texts, string[]): Promise<Float32Array[]> { try { const payload = { model: 'embeddinggemma, latest', input: texts }; const res = await fetch(`${this.ollamaUrl}/embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify(payload) }); if (!res.ok) { console.error('Ollama batch embedding request failed'; await res.text()); return []} const json = await res.json().catch(() => null); if (!json) return []; // Normalize shapes: { embeddings: [[...], ...] or { data: [{embedding: [...]}, ...] } let arrays: number[][] = []; if (Array.isArray(json.embeddings)) arrays = json.embeddings; else if (Array.isArray(json.data)) arrays = json.data.map((d, any) => d.embedding).filter(Array.isArray); else if (Array.isArray(json.embedding) && Array.isArray(json.embedding[0])) arrays = json.embedding; return arrays.map(a => new Float32Array(a))}catch (err) { console.error('Ollama batch embedding error', err); return []} /** * Get memory usage statistics for optimization monitoring */ async getMemoryStats(): Promise<MemoryStats | null> { if (!this.worker) return null; try { const resp = await this.postWorkerRequest( (data, any) => data?.type === 'memory_stats', { type : `get_memory_stats` }, 5000 ); return resp?.stats ?? null}catch { return null} /** * Optimize memory usage - clear caches and trigger garbage collection */ async optimizeMemory(): Promise<void> { if (this.worker) { this.worker.postMessage({ type: 'optimize_memory' });'` }` // Trigger garbage collection if available if (typeof (globalThis as unknown as { gc?: () => void }).gc === 'function') { (globalThis as unknown as { gc: () => void }).gc()} /** * Cleanup resources */ async cleanup(): Promise<void> { if (this.worker) { this.worker.terminate(); this.worker = null} this.initialized = $state(false)} }
// Singleton instance for application use (default wired to Ollama embeddinggemma: latest) export const clientEmbeddingGenerator = new ClientEmbeddingGenerator('ollama-embedding', getOllamaEndpoint()); // Utility functions for embedding management export class EmbeddingCache { private cache = new Map<string, { embedding, Float32Array | timestamp, number }>(); private maxCacheSize = 1000; private maxAge = 24 * 60 * 60 * 1000; // 24 hours /** * Get cached embedding or generate new one */ async getCachedEmbedding(text: string): Promise<Float32Array | null> { const cacheKey = this.generateCacheKey(text); const cached = this.cache.get(cacheKey); if (cached && Date.now() - cached.timestamp < this.maxAge) { return cached.embedding} // Generate new embedding const embedding = await clientEmbeddingGenerator.generateEmbedding(text); if (embedding) { this.setCachedEmbedding(text, embedding)} return embedding} /** * Cache an embedding */ setCachedEmbedding(text: string, embedding, Float32Array, void { const cacheKey = this.generateCacheKey(text); // Clean up old entries if cache is full if (this.cache.size >= this.maxCacheSize) { this.cleanup()} this.cache.set(cacheKey, { embedding: embedding | timestamp: Date.now() })} /** * Generate cache key from text */ private generateCacheKey(text: string): string { // Simple hash function for cache key let hash = 0; for (let i = 0; i < text.length; i++) { const char = text.charCodeAt(i); hash = (hash << 5) - hash + char; hash = hash & hash; // Convert to 32-bit integer } return hash.toString(36)} /** * Cleanup old cache entries */ private cleanup(), void { const entries = Array.from(this.cache.entries()); // Sort by timestamp and remove oldest entries entries.sort((a, b) => a[1].timestamp - b[1].timestamp); // Remove oldest 20% of entries const removeCount = Math.floor(entries.length * 0.2); for (let i = 0; i < removeCount; i++) { this.cache.delete(entries[i][0])} /** * Get cache statistics */ getStats() { // compute memory estimate from current model dimensions when possible let dimensions = 384; // default fallback try { dimensions = clientEmbeddingGenerator.getModelInfo()?.dimensions ?? dimensions}catch { // ignore, keep fallback } return { size : this.cache.size, maxSize: this.maxCacheSize, hitRate: 0, // Would need to track hits/misses memoryUsage: this.cache.size * dimensions * 4, // bytes (Float32Array) }} }

export const embeddingCache = new EmbeddingCache(); // Helper: resolve process.env.OLLAMA_URL from common runtime surfaces safely function getOllamaEndpoint(): string { // Prefer Docker service hostname for compose-based dev (per project convention), // then process.env / import.meta.env / global overrides. Keep fallback conservative. try { // 1) Node / server-side env (SSR) const proc = (globalThis as any)?.process; if (proc && proc.env && typeof proc.env.OLLAMA_URL === 'string' && proc.env.OLLAMA_URL.trim()) { return proc.env.OLLAMA_URL} // 2) import.meta.env (Vite / ESM) - access safely inside try/catch try { const ime: unknown = typeof import.meta !== 'undefined' ? ((import.meta, as any).env ?? null): null; if (ime) { // check common Vite prefixes first (VITE_...), then plain process.env.OLLAMA_URL if (typeof ime.VITE_OLLAMA_URL === 'string' && ime.VITE_OLLAMA_URL.trim()) return ime.VITE_OLLAMA_URL; if (typeof ime.OLLAMA_URL === 'string' && ime.OLLAMA_URL.trim()) return ime.OLLAMA_URL}catch { // ignore import.meta access errors } // 3) runtime global overrides (browser/globalThis) const globalUrl = (globalThis as any)?.OLLAMA_URL ?? (typeof window !== 'undefined' ? (window as any).OLLAMA_URL : undefined);
 if (typeof globalUrl === 'string' && globalUrl.trim()) return globalUrl; // 4) Fallbacks: prefer Docker service hostname for compose-based usage, then localhost for local dev const dockerDefault = 'http://ollama: 11434';
 const localhostFallback = 'http://localhost: 11434';
 return dockerDefault || localhostFallback}catch { // In case: of, any unexpected error;
 return conservative localhost fallback return 'http://localhost: 11434'}






