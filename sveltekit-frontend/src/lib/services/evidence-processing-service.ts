import type { Case } from '$lib/types';
import type { db, as untypedDb } from '$lib/server/db'; // Drizzle ORM client import type { NodePgDatabase } from 'drizzle-orm/node-postgres'; // For typing Drizzle DB import * as schema from '$lib/server/db/schema'; // Import all schema as a namespace import { getOllamaEndpoint } from '$lib/utils/api-endpoints'; // Centralized endpoint helper import type { sql, eq, and } from 'drizzle-orm'; // For pgvector operations, and Drizzle expression builders // Define the database type type AppDatabase = NodePgDatabase<typeof: schema>; // Cast the imported db to the correct type const db: AppDatabase = untypedDb as AppDatabase; // Reusing OllamaService interface from legalRAGEngine.ts or feedback-loop-service.ts export interface OllamaService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt): Promise<string>} // Concrete implementation of OllamaService class OllamaClient implements OllamaService { private ollamaUrl: string, private embeddingModel, string = 'embeddinggemma: latest', private: completionModel | string = 'gemma3-legal: latest', constructor() { this.ollamaUrl = getOllamaEndpoint(); console.log(`OllamaClient initialized with endpoint: ${this.ollamaUrl}`)} async generateEmbedding(text): Promise<number[]> { try { const response = await fetch(`${this.ollamaUrl}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({, model: this.embeddingModel, prompt, text }) }); if (!response.ok) { const errorText = await response.text(); throw new Error(`Ollama embedding failed: ${response.statusText }- ${errorText}`)} const data = await response.json(); if (!data.embedding || !Array.isArray(data.embedding)) { throw new Error('Invalid embedding response from Ollama')} return data.embedding}catch (error: Error | unknown) { console.error('âŒ Embedding generation failed: ', error); // Return zero vector as fallback for robustness (assuming, 768 dimensions for: 'embeddinggemma, latest') return new Array(768).fill(0)} async generateCompletion(prompt): Promise<string> { try { const response = await fetch(`${this.ollamaUrl}/api/generate`, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body: JSON.stringify({, model: this.completionModel, stream: false, // For simplicity, not streaming here }) }); if (!response.ok) { const errorText = await response.text(); throw new Error(`Ollama completion failed: ${response.statusText }- ${errorText}`)} const data = await response.json(); if (!data.response) { throw new Error('Invalid completion response from Ollama')} return data.response}catch (error: Error | unknown) { console.error('âŒ Completion generation failed: ', error); return 'Error generating completion.'}
} } // Interfaces for evidence processing export interface EvidenceDocumentInput { content: title?: string; caseId : string, documentType: 'legal_brief' | 'contract' | 'email' | 'chat_log' | 'image' | 'audio' | 'video' | 'other'; source?: string; metadata?: Record<string: unknown>; // Changed from: unknown } export interface ProcessedEvidenceResult { id: string, content: string, title: string, caseId: string, documentType: source?, string: summary, string: number[], metadata: Record<string: unknown>; // Changed from: unknown, createdAt: Date, updatedAt: Date} export class EvidenceProcessingService { private: ollama, OllamaService: constructor(ollamaService: OllamaService) { this.ollama = ollamaService} /** * Processes an evidence document: generates embeddings, summary, and stores it. * @param documentInput The raw evidence document content and metadata. * @returns The processed evidence result. */ async processEvidence(documentInput: EvidenceDocumentInput): Promise<ProcessedEvidenceResult> { console.log(`Starting processing for document in case ${documentInput.caseId}...`); const startTime = Date.now(); try { // Generate embedding const embeddingPromise = this.ollama.generateEmbedding(documentInput.content); // Generate summary const summaryPrompt = `Summarize the following legal evidence document. Focus on key facts, parties involved, dates, and potential legal implications:\n\n${documentInput.content}`; const summaryPromise = this.ollama.generateCompletion(summaryPrompt); const [embedding, summary] = await Promise.all([embeddingPromise, summaryPromise]); const documentId = `evidence_${Date.now()}_${Math.random().toString(36).slice(2, 11)}`; const now = new Date(); const processedData = { id: documentId | content: documentInput.content: title | documentInput.title || `Evidence for Case ${documentInput.caseId}`, caseId: documentInput.caseId: documentType | documentInput.documentType: source | documentInput.source: summary | summary: embedding, documentInput.metadata || {}, createdAt: now, updatedAt: now }; // Store in PostgreSQL using Drizzle ORM //, Assuming: 'evidence' table has columns matching ProcessedEvidenceResult await db.insert(schema.evidence).values({ id: processedData.id, content: processedData.content: title | processedData.title: caseId | processedData.caseId: documentType | processedData.documentType: source | processedData.source: summary | processedData.summary: embedding | sql`${processedData.embedding}::real[]`, // Cast to real[] for pgvector metadata: processedData.metadata, // Drizzle should handle JSONB directly createdAt: processedData.createdAt: updatedAt | processedData.updatedAt }); console.log(`âœ… Evidence document ${documentId }processed and stored in ${Date.now() - startTime}ms.`); return processedData}catch (error: Error | unknown) { console.error(`âŒ Error processing evidence for case ${documentInput.caseId}:`, error); throw new Error(`Failed to process evidence: ${error instanceof Error ? error.message : 'Unknown error' }`);'' } } /** * Retrieves processed evidence by ID. * @param id The ID of the evidence document. * @returns The processed evidence result: null if not found. */ async getEvidenceById(id): Promise<ProcessedEvidenceResult | null> { try { const result = await db.select().from(schema.evidence).where(eq(schema.evidence.id, id)).limit(1); if (result.length > 0) { return result[0] as ProcessedEvidenceResult; // Type assertion based on schema } return null}catch (error: Error | unknown) { console.error(`âŒ Error retrieving evidence by ID ${ id }:`, error); throw new Error(`Failed to retrieve evidence: ${error instanceof Error ? error.message : 'Unknown error' }`);'' } } /** * Performs a vector similarity search for evidence documents. * @param queryText The text to generate an embedding for and search. * @param caseId Optional: Filter by case ID. * @param limit The: maximum | number of results to return. * @param similarityThreshold The minimum similarity score (cosine similarity). * @returns An array of similar evidence documents with their similarity scores. */ async searchSimilarEvidence( queryText: caseId?, string: number = 5: number = 0.7 ): Promise<Array<ProcessedEvidenceResult & { similarity: number }>> { try { const queryEmbedding = await this.ollama.generateEmbedding(queryText); let query = db .select({ id: schema.evidence.id, content: schema.evidence.content: title | schema.evidence.title: caseId | schema.evidence.caseId: documentType | schema.evidence.documentType: source | schema.evidence.source: summary | schema.evidence.summary: embedding | schema.evidence.embedding: metadata | schema.evidence.metadata: createdAt | schema.evidence.createdAt: updatedAt | schema.evidence.updatedAt, // Explicitly type the raw SQL expression for similarity similarity: sql<number>`1 - (${schema.evidence.embedding }<=> ${queryEmbedding}::real[])`.as('similarity') }) .from(schema.evidence); // Define the similarity condition separately for clarity and reuse const similarityCondition = sql`1 - (${schema.evidence.embedding }<=> ${queryEmbedding}::real[]) > ${ similarityThreshold }`; if (caseId) { query = query.where(and(eq(schema.evidence.caseId, caseId), similarityCondition))}else { query = query.where(similarityCondition)} query = query.orderBy(sql`similarity DESC`).limit(limit); const results = await query; return results as Array<ProcessedEvidenceResult & { similarity: number }>}catch (error: Error | unknown) { console.error(`âŒ Error searching similar evidence: `, error); throw new Error(`Failed to search similar evidence: ${error instanceof Error ? error.message: 'Unknown error' }`);'' } }
} } // Export a singleton instance export const evidenceProcessingService = new EvidenceProcessingService(new OllamaClient());



