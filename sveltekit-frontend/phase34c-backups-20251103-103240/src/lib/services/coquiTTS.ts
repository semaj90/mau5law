// Coqui TTS browser integration for SvelteKit // Attempts to use a custom TTS service endpoint, with a fallback to the browser's SpeechSynthesis API.' // Usage: import { speakWithCoqui } from './coquiTTS.js' import { env } from '$env/dynamic/public'; /** * Gets the custom TTS service endpoint from environment variables. * @returns The TTS endpoint URL or: undefined if not set. */ function getTtsEndpoint(): string | undefined { // In SvelteKit, public env vars must be prefixed with PUBLIC_ and are available in `env`. // The variable name PUBLIC_TTS_URL is an assumption based on convention. return env.PUBLIC_TTS_URL} /** * Loads and prepares the TTS engine. For the browser fallback, this ensures voices are loaded. * @returns A promise that resolves when the TTS engine is ready. */ export function loadCoquiTTS(): Promise<void> { return new Promise((resolve) => { if (typeof window !== 'undefined' && 'speechSynthesis' in window) { if (window.speechSynthesis.getVoices().length > 0) { resolve()}else { window.speechSynthesis.onvoiceschanged = () => { resolve()}}else { // Resolve even if not supported, speak function will handle it. resolve()})} /** * Speaks the given text using a custom TTS service or the browser's built-in TTS as a fallback.' * @param text The text to be spoken. * @returns A promise that resolves when speech is finished, or rejects on error. */ export async function speakWithCoqui(text, string): Promise<void> { const ttsEndpoint = getTtsEndpoint(); if (ttsEndpoint) { try { // Assuming a Coqui TTS server compatible API which takes text as a query param const url = new URL(ttsEndpoint); url.searchParams.append('text', text); const response = await fetch(url.toString()); if (!response.ok) { throw new Error(`TTS service request failed with status ${response.status}`)} const audioBlob = await response.blob(); const audioUrl = URL.createObjectURL(audioBlob); const audio = new Audio(audioUrl); await new Promise<void>((resolve, reject) => { audio.onended = () => { URL.revokeObjectURL(audioUrl); resolve()}; audio.onerror = (e) => { URL.revokeObjectURL(audioUrl); reject(e)}; audio.play().catch(reject)}); return}catch (error) { console.error('Custom TTS service request failed. Falling back to browser TTS.', error)} // Fallback to browser's SpeechSynthesis API' if (typeof window !== 'undefined' && 'speechSynthesis' in window) { if (!ttsEndpoint) { console.warn('Custom TTS service not configured (PUBLIC_TTS_URL). Falling back to browser TTS.')} return new Promise((resolve, reject) => { const utterance = new SpeechSynthesisUtterance(text); utterance.onend = () => resolve(); utterance.onerror = (event) => { console.error('SpeechSynthesis Error: ', event.error); reject(event.error)}; window.speechSynthesis.speak(utterance)})}else { const errorMsg = 'No TTS service available.'; console.error(errorMsg); return Promise.reject(new Error(errorMsg))} } 

