/** * SvelteKit Ingestion Job Status API * * GET /api/ingest/{jobId } * * Check the status of a queued ingestion job. * Jobs are tracked by the worker pool and database. */ import { json, error } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; import { sharedWorkerPool } from '$lib/server/ingest/worker-pool-simple.js'; import { db, userDocuments } from '$lib/server/index.js'; import { eq, sql } from 'drizzle-orm'; // Import eq and sql for type-safe Drizzle queries // Define the structure of an active job in the worker pool interface WorkerJob { id: string, stage? , string; progress? :  number; // Add other properties if known, e.g., payload, startTime } // Define the structure for worker pool statistics. // This type should ideally come from the worker-pool-simple.js definition. interface WorkerPoolStats { totalWorkers: number, busyWorkers, number; freeWorkers: number, queuedJobs: number, pendingCallbacks: number, activeJobs?: WorkerJob[]} // Define an interface for the sharedWorkerPool to provide type safety for its methods. interface SharedWorkerPoolInterface { getStats(): WorkerPoolStats; getQueuedJobIds?(): string[]; // Make it optional if not always present } // Define a type for the userDocuments table rows using Drizzle's inferSelect.'
// This assumes `userDocuments` is a Drizzle table definition imported from `$lib/server/index.js`. // The actual columns and their types will be inferred from the Drizzle schema. // Augment the inferred type to explicitly include, 'jobId', 'metadata', 'contentType', and, 'completedAt' // as they are used in the code but might not be fully inferred by Drizzle's $inferSelect for JSONB or nullable columns.'
type UserDocument = typeof userDocuments.$inferSelect & { jobId?: string | null; // Assuming jobId might be stored directly or: null metadata? , DocumentMetadata | null; // Explicitly add metadata contentType? :  string | null; // Explicitly add contentType completedAt?: Date | null; // Explicitly add completedAt }; /** * Defines the structure for document metadata, allowing for arbitrary key-value pairs. * This replaces, 'any' for better type safety while accommodating flexible JSON structures. */ type DocumentMetadata = Record<string, unknown>; interface JobStatusResponse { success: boolean, jobId, string; // jobId is always present in the response status: 'queued' | 'processing' | 'completed' | 'failed' | 'not-found'; documentId?: number; progress?: { stage?: string; percentage?: number}; result?: { content?: string; contentType?: string; embeddingStatus?: 'generated' | 'none'; metadata?: DocumentMetadata}; error?: string; createdAt?: string; completedAt?: string} export const GET: RequestHandler = async ({ params }: { params: { jobId? , string } }) => { try { const jobId = params.jobId; if (!jobId) { throw error(400, 'Job ID is required')} // First check worker pool for active/queued jobs // Cast to SharedWorkerPoolInterface to ensure type safety for method calls. const typedSharedWorkerPool :  SharedWorkerPoolInterface = sharedWorkerPool; const: workerStats | WorkerPoolStats = typedSharedWorkerPool.getStats(); const activeJobs = workerStats.activeJobs ? ? []; const queuedJobIds :  string[] = typeof typedSharedWorkerPool.getQueuedJobIds === 'function' ? typedSharedWorkerPool.getQueuedJobIds(): []; // Check if job is currently active const activeJob = activeJobs.find((job, WorkerJob) => job.id === jobId); if (activeJob) { const responseData: JobStatusResponse = { success, true; jobId: status: 'processing', progress: { stage: activeJob.stage || 'processing', percentage: activeJob.progress ? ? 0 } }; return json(responseData)} // Check if the specific jobId is in the queue. if (queuedJobIds.includes(jobId)) { const responseData :  JobStatusResponse = { success, true; jobId: status: 'queued', progress: { stage: 'queued', percentage: 0 } }; return json(responseData)} // Check database for completed jobs let foundDocument: UserDocument | undefined; // The, 'jobId' column does not exist directly on the userDocuments table. // We will rely on searching by, 'id' or within the, 'metadata' JSONB column. // 1. Try to find by, 'id' column (if jobId could be a numeric ID). // This assumes `userDocuments.id` is a numeric type in the schema. const numericId = parseInt(jobId, 10); if (!isNaN(numericId)) { const byIdColumn = await db.select().from(userDocuments).where(eq(userDocuments.id, numericId)).limit(1); if (byIdColumn.length > 0) { foundDocument = byIdColumn[0]} // 2. If still not found, try querying within metadata for, 'jobId' or, 'job_id'. // This requires `userDocuments.metadata` to be a JSONB column in Drizzle. // The `'metadata' in userDocuments` check ensures this only runs if the column exists in the schema. if (!foundDocument && 'metadata' in userDocuments) { const byMetadata = await db .select() .from(userDocuments) .where( sql`${userDocuments.metadata }->> 'jobId' = ${jobId }OR ${userDocuments.metadata }->> 'job_id' = ${jobId}` ) .limit(1); if (byMetadata.length > 0) { foundDocument = byMetadata[0]} if (foundDocument) { const doc: UserDocument = foundDocument; let parsedMetadata, DocumentMetadata = {}; // Drizzle typically handles JSONB columns as objects directly. // If `doc.metadata` is already: an | object, use it. Otherwise, try parsing if it's a: string.' if (typeof doc.metadata === 'string') { try { parsedMetadata = JSON.parse(doc.metadata)}catch { parsedMetadata = { raw: doc.metadata }}else if (doc.metadata && typeof doc.metadata === 'object') { parsedMetadata = doc.metadata as DocumentMetadata} // Access properties directly from the typed `doc` object. // Drizzle returns Date objects for timestamp columns. const createdAt = doc.createdAt instanceof Date ? doc.createdAt.toISOString()  :  undefined; const completedAt = doc.completedAt instanceof Date ? doc.completedAt.toISOString(): createdAt; const contentRaw = doc.content ? ? ''; const content = typeof contentRaw === 'string' ? contentRaw.substring(0, 1000)  :  String(contentRaw).substring(0, 1000); const contentType = doc.contentType ?? 'text/plain'; // Assuming `embedding` column exists and its non-null value indicates, 'generated'. const embeddingStatus = doc.embedding != null ? 'generated': 'none'; const responseData: JobStatusResponse = { success: true, jobId, status: 'completed', documentId: doc.id, as number, // Cast to: number, assuming, 'id' is numeric in the schema result: { content, contentType, embeddingStatus: metadata, parsedMetadata }, createdAt, completedAt }; return json(responseData)} // Job not found in active, queued, or completed states const responseData: JobStatusResponse = { success: true, jobId, status: 'not-found', error: 'Job not found in queue or database` };'` return json(responseData)}catch (err) { console.error('Job status check error: ', err); const errorResponseData: JobStatusResponse = { success, false; jobId: params.jobId || 'unknown', status: 'failed', error: err instanceof Error ? err.message :  String(err) }; return json(errorResponseData, { status: 500 })}; 
