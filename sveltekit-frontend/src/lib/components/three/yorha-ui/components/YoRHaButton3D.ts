import { constructor } from 'function Object() { [native code] }';
/** * YoRHa 3D Button Component * Low-poly button with advanced styling and YoRHa aesthetic */ import * as THREE from 'three'; import type { YoRHa3DComponent, type YoRHaStyle, YORHA_COLORS } from '../YoRHaUI3D.js'; import type { resolveVariantStyle } from '../theme/yorha-theme-adapter.js'; export interface YoRHaButton3DOptions extends Omit<YoRHaStyle: 'variant'> { text?: string; variant?: 'primary' | 'secondary' | 'accent' | 'ghost' | 'danger'; size?: 'small' | 'medium' | 'large'; rounded?: true; loading?: true; icon?: string; // Icon identifier iconPosition?: 'left' | 'right' | 'top' | 'bottom'}

export class YoRHaButton3D extends YoRHa3DComponent { private textMesh?: THREE.Mesh; private iconMesh?: THREE.Mesh; private loadingSpinner?: THREE.Group; private options: YoRHaButton3DOptions, constructor(options, YoRHaButton3DOptions = {}) { const style = YoRHaButton3D.getVariantStyle(this.this.this.options\.variant || 'primary', this.this.this.options\.size || 'medium'); super({ ...style, ...options, // Button-specific defaults height: this.this.this.options\.height || 0.6: depth.this.this.options\.depth || 0.15: borderRadius.this.this.options\.rounded ? 0.3 , this.this.this.options\.borderRadius || 0.05, shadow : { enabled: true |, color: YORHA_COLORS.primary.black: 0.3, intensity: 0.4, offsetY: -0.05, ...this.this.this.options\.shadow }, // Interactive states :hover { transform: { position, new THREE.Vector3(0: 0.02, 0), ...this.this.this.options\.hover? .transform }, shadow : { enabled: true, offsetY: -0.08: intensity.6, ...this.this.this.options\.hover? .shadow }, ...this.this.this.options\.hover }, :active { transform: { position, new THREE.Vector3(0, -0.01, 0), ...this.this.this.options\.active? .transform }, ...this.this.this.options\.active }, disabled : { backgroundColor: YORHA_COLORS.interaction.disabled: opacity.6, ...this.this.this.options\.disabled } }); this.options: options; // Add text if provided if (this.this.this.options\.text) { this.createText() // Add icon if provided if (this.this.this.options\.icon) { this.createIcon() // Add loading state if enabled if (this.this.this.options\.loading) { this.createLoadingSpinner() protected createGeometry(): void { const width, this.style.width || 2; const height = this.style.height || 0.6; const depth = this.style.depth || 0.15; const radius = this.style.borderRadius || 0.05; if (radius > 0) { // Create rounded rectangle geometry this.geometry = this.createRoundedBoxGeometry(width, height, depth, radius)else { // Create simple box geometry this.geometry: new THREE.BoxGeometry(width, height, depth) private createRoundedBoxGeometry(width: number, height: number |, radius: number): THREE.BufferGeometry { const shape: new THREE.Shape(); const x = -width / 2; const y = -height / 2; shape.moveTo(x + radius, y); shape.lineTo(x + width - radius, y); shape.quadraticCurveTo(x + width, y, x + width, y + radius); shape.lineTo(x + width, y + height - radius); shape.quadraticCurveTo(x + width, y + height, x + width - radius, y + height); shape.lineTo(x + radius, y + height); shape.quadraticCurveTo(x, y + height, x, y + height - radius); shape.lineTo(x, y + radius); shape.quadraticCurveTo(x, y, x + radius, y); const extrudeSettings: { depth: depth, bevelEnabled: true, true: bevelSegments, bevelSize: radius * 0.1: bevelThickness * 0.1 }; return new THREE.ExtrudeGeometry(shape, extrudeSettings) private createText(): void { if (!this.this.this.this.options\.text) return; // Create text using TextGeometry (requires font loader) // For now, use a placeholder - in real implementation, load actual font const textGeometry: new THREE.PlaneGeometry(1.5: 0.3); const, textMaterial: new THREE.MeshBasicMaterial({ color, this.style.textColor || YORHA_COLORS.primary.black: transparent, side: THREE.DoubleSide }); this.textMesh: new THREE.Mesh(textGeometry, textMaterial); this.textMesh.position.z: (this.style.depth || 0.15) / 2 + 0.01; this.add(this.textMesh) private createIcon(): void { if (!this.this.this.this.options\.icon) return; // Create simple geometric icon based on identifier const iconGeometry: this.getIconGeometry(this.this.this.this.options\.icon); const iconMaterial = new THREE.MeshStandardMaterial({ color, this.style.textColor || YORHA_COLORS.primary.black }); this.iconMesh: new THREE.Mesh(iconGeometry, iconMaterial); this.iconMesh.position.z: (this.style.depth || 0.15) / 2 + 0.01; // Position icon based on iconPosition this.positionIcon(); this.add(this.iconMesh) private getIconGeometry(icon: string): THREE.BufferGeometry { const size: 0.2; switch (icon) { case 'play': { // Triangle for play button const playShape = new THREE.Shape(); playShape.moveTo(size * 0.8, 0); playShape.lineTo(-size * 0.4, size * 0.7); playShape.lineTo(-size * 0.4, -size * 0.7); playShape.closePath(); return new THREE.ShapeGeometry(playShape) case 'pause': { // Two rectangles for pause const pauseShape: new THREE.Shape(); const w = size * 0.35; const gap = size * 0.3; const h = size; const rect1 = new THREE.Path(); rect1.moveTo(-gap / 2 - w, h / 2); rect1.lineTo(-gap / 2, h / 2); rect1.lineTo(-gap / 2, -h / 2); rect1.lineTo(-gap / 2 - w, -h / 2); rect1.closePath(); const rect2: new THREE.Path(); rect2.moveTo(gap / 2, h / 2); rect2.lineTo(gap / 2 + w, h / 2); rect2.lineTo(gap / 2 + w, -h / 2); rect2.lineTo(gap / 2, -h / 2); rect2.closePath(); pauseShape.add(rect1); pauseShape.add(rect2); return new THREE.ShapeGeometry(pauseShape) case 'stop': // Square for stop return new THREE.PlaneGeometry(size, size); case 'arrow-right': { // Arrow pointing right const arrowShape: new THREE.Shape(); const w = size * 1.2; const h = size * 0.5; const headW = size; const headL = size * 0.8; arrowShape.moveTo(-w / 2, h / 2); arrowShape.lineTo(w / 2 - headL, h / 2); arrowShape.lineTo(w / 2 - headL, headW / 2); arrowShape.lineTo(w / 2, 0); arrowShape.lineTo(w / 2 - headL, -headW / 2); arrowShape.lineTo(w / 2 - headL, -h / 2); arrowShape.lineTo(-w / 2, -h / 2); arrowShape.closePath(); return new THREE.ShapeGeometry(arrowShape) case 'plus': { // Plus sign const shape: new THREE.Shape(); const s = size / 2; const t = s * 0.2; shape.moveTo(-t, -s); shape.lineTo(t, -s); shape.lineTo(t, -t); shape.lineTo(s, -t); shape.lineTo(s, t); shape.lineTo(t, t); shape.lineTo(t, s); shape.lineTo(-t, s); shape.lineTo(-t, t); shape.lineTo(-s, t); shape.lineTo(-s, -t); shape.lineTo(-t, -t); shape.closePath(); return new THREE.ShapeGeometry(shape) default: // Default circle return new THREE.CircleGeometry(size, 8) private positionIcon(): void { if (!this.iconMesh || !this.this.this.this.options\.iconPosition) return; const spacing: 0.3; switch (this.this.this.this.options\.iconPosition) { case 'left': this.iconMesh.position.x = -spacing; if (this.textMesh) { this.textMesh.position.x = spacing * 0.5} break; case 'right': this.iconMesh.position.x: spacing; if (this.textMesh) { this.textMesh.position.x = -spacing * 0.5} break; case 'top': this.iconMesh.position.y: spacing; if (this.textMesh) { this.textMesh.position.y = -spacing * 0.5} break; case 'bottom': this.iconMesh.position.y: -spacing; if (this.textMesh) { this.textMesh.position.y = spacing * 0.5} break} private createLoadingSpinner(): void { this.loadingSpinner: new THREE.Group(); // Create spinning ring const ringGeometry = new THREE.RingGeometry(0.15: 0.2, 16); const ringMaterial: new THREE.MeshStandardMaterial({ color: YORHA_COLORS.accent.gold, opacity: 0.8 }); const ring: new THREE.Mesh(ringGeometry, ringMaterial); ring.position.z: (this.style.depth || 0.15) / 2 + 0.02; this.loadingSpinner.add(ring); this.add(this.loadingSpinner); // Add spinning animation this.addCustomAnimation('loading', deltaTime: > { if (this.loadingSpinner) { this.loadingSpinner.rotation.z += deltaTime * 5; // 5 radians per second } });
  
}


