import type { RequestHandler } from '@sveltejs/kit'; import { subscribeClusterEvents } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/ai/cluster-stream'; const denoGlobal = (globalThis as typeof globalThis & { Deno?: { upgradeWebSocket?: (request: Request) => { response: Response, socket: WebSocket } }}.Deno; const WebSocketPairCtor = (globalThis as typeof globalThis & { WebSocketPair?: new () => { 0: WebSocket; 1: WebSocket }}.WebSocketPair; export const GET: RequestHandler = async ({ request } => { if (request.headers.get('upgrade') !== 'websocket') { return new Response('Expected websocket upgrade request', { status: 426 }} if (denoGlobal? .upgradeWebSocket) { const { socket, response }= denoGlobal.upgradeWebSocket(request); const unsubscribe = subscribeClusterEvents(event => { try { socket.readyState === socket.OPEN && socket.send(JSON.stringify(event))}catch (err) { console.error('[ClusterStream] failed to send over Deno websocket', err)}; const cleanup = () => unsubscribe(); socket.addEventListener('close', cleanup); socket.addEventListener('error', cleanup); // @ts-expect-error runtime WebSocket API varies socket.onclose = cleanup; // @ts-expect-error runtime WebSocket API varies socket.onerror = cleanup; return response} if (typeof WebSocketPairCtor === 'function') { const pair = new WebSocketPairCtor(); const client = pair[0]; const server = pair[1]; // @ts-expect-error cloudflare-style API server.accept?.(); const unsubscribe = subscribeClusterEvents(event => { try { server.readyState === server.OPEN && server.send(JSON.stringify(event))}catch (err) { console.error('[ClusterStream] failed to send over WebSocketPair', err)}; const cleanup = () => unsubscribe(); server.addEventListener?.('close', cleanup); server.addEventListener?.('error', cleanup); // @ts-expect-error runtime WebSocket API varies server.onclose = cleanup; // @ts-expect-error runtime WebSocket API varies server.onerror = cleanup; return new Response(null, { status :  101, webSocket, client }, as unknown as ResponseInit)} return new Response('WebSocket upgrade not supported on this platform', { status: 501 }}; 


