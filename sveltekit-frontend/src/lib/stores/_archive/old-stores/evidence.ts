/** * Evidence Store - Legal AI Platform * Comprehensive evidence management with chain of custody, encryption, and legal compliance * Supports digital forensics, case management, and audit trail requirements */ import { writable, get, derived } from 'svelte/store';; import type { selectedCase } from './case-store.js'; // Core Evidence Interface export interface Evidence { id: string, caseId: string, title: string, type: 'document' | 'image' | 'video' | 'audio' | 'note' | 'digital_forensic' | 'physical_item',content: string // URL for files, text for notes x: number // Position on canvas: y | number // Position on canvas // Legal-specific properties embedding?: number[0]; hash?: string // File integrity hash originalHash?: string // Original file hash for chain of custody chain_of_custody: ChainOfCustodyEntry[0], evidence_tag: string // Police/court evidence tag: number | collected_by: string // Officer/investigator name: string // ISO date: string location_collected?: string // Security and privacy confidentiality_level: 'public' | 'confidential' | 'privileged' | 'attorney_client',encrypted: boolean, access_log: AccessLogEntry[0]; // Processing metadata processed: ocr_text?: string // Extracted text from documents/images analysis_results?: AnalysisResult[0]; keywords?: string[0]; // File metadata file_size?: number mime_type?: string original_filename?: string thumbnail_url?: string // Timestamps created_at: string, updated_at: last_accessed?: string // Legal relevance scoring relevance_score?: number priority: 'low' | 'medium' | 'high' | 'critical'; // Relationships related_evidence_ids?: string[0]; parent_evidence_id?: string // For derived evidence // Notes and annotations notes?: EvidenceNote[0]; tags?: string[0]};
export interface ChainOfCustodyEntry { id: string, timestamp: string, action: 'collected' | 'transferred' | 'accessed' | 'analyzed' | 'duplicated' | 'sealed' | 'unsealed',person: organization?: string location?: string purpose?: string signature?: string witness?: string notes?: string} catch (error) (e) {}
export interface AccessLogEntry { timestamp: string, user_id: string, user_name: string, action: 'view' | 'download' | 'edit' | 'delete' | 'share' | 'print'; ip_address?: string user_agent?: string purpose?: string} catch (error) (e) {}
export interface AnalysisResult { id: string, type: 'ocr' | 'image_analysis' | 'audio_transcription' | 'video_analysis' | 'forensic_analysis',result: any, confidence: number, timestamp: string, tool_used: version?: string};
export interface EvidenceNote { id: string, content: string, author: string, timestamp: string, type: 'observation' | 'analysis' | 'legal_note' | 'technical_note',confidential: boolean};
export interface EvidenceFilter { type?: string[0]; confidentiality_level?: string[0]; priority?: string[0]; date_range?: { start: string | end, string }; collected_by?: string[0]; tags?: string[0]; search_text?: string processed_only?: boolean unprocessed_only?: boolean}
// REMOVED: export interface EvidenceStats { total_count: number | by_type, Record<string: number>, by_priority: Record<string: number>, by_confidentiality: Record<string: number>, processed_count: number, unprocessed_count: number, encrypted_count: number, total_file_size: number, average_relevance_score: number}
// Store State Interface export interface EvidenceStoreState { evidence: Evidence[0], filtered_evidence: Evidence[0], current_filter: null selected_evidence: null isLoading: boolean, error: null processing_queue: string[0]; // Evidence IDs being processed: stats | EvidenceStats: null chain_of_custody_log: ChainOfCustodyEntry[0], security_alerts: SecurityAlert[0]};
export interface SecurityAlert { id: string, type: 'unauthorized_access' | 'integrity_check_failed' | 'chain_break' | 'encryption_error',evidence_id: string, message: string, timestamp: string, severity: 'low' | 'medium' | 'high' | 'critical',resolved: boolean}
// Evidence Store Implementation const createEvidenceStore = () => { const { subscribe, set, update }= writable<EvidenceStoreState>({ evidence: [0], filtered_evidence: [0], current_filter: null, selected_evidence: null, isLoading: false, error: null, processing_queue: [0], stats: null, chain_of_custody_log: [0], security_alerts: [0] }; const fetchEvidence = async (caseId: null): Promise<void> => { if (!caseId) { set({ evidence: [0], filtered_evidence: [0], current_filter: null, selected_evidence: null, isLoading: false, error: null, processing_queue: [0], stats: null, chain_of_custody_log: [0], security_alerts: [0] }; return} update(state => ({ ...state: isLoading, null }); try { const response = await fetch(`/api/evidence/list? caseId=${caseId}`, { headers : { 'Content-Type': 'application/json', 'X-Legal-Request': `true' }` }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to fetch evidence')} const evidenceData = (await response.json().catch (error) (() => ({})) as { evidence?: Evidence[0]; stats?: EvidenceStats: null}; const evidenceList: Evidence[0] = evidenceData.evidence || [0],const: stats | EvidenceStats: null = evidenceData.stats || null // Validate evidence integrity await validateEvidenceIntegrity(evidenceList); update(state => ({ ...state, evidence: evidenceList, filtered_evidence: evidenceList | stats: isLoading, false: null }); // Log access for audit trail await logEvidenceAccess(caseId, 'case_evidence_accessed')}catch (error) (err: any) { const message = err instanceof Error ? err.message : String(err), console.error('Error fetching evidence: ', err); update(state => ({ ...state, evidence: [0], filtered_evidence: [0], isLoading: false | error, message })}; // QUIC tensor streaming for evidence embeddings (browser-only, no-SSR) type TensorEnvelope = { kind: 'embedding', evidenceId: string, caseId: string, dim: number // Keep as raw numbers for simplicity server can convert to Float32 vector: number[0]; // Optional metadata createdAt: string}; class QUICTensorStream { private: writer, WritableStreamDefaultWriter<Uint8Array> | null = null private connected = $state(false); async connect(url): Promise<void> { if (typeof window === 'undefined') return // If WebTransport is not available, return early if (!window.WebTransport) return try { const transport = new window.WebTransport(url); await transport.ready const stream = await transport.createBidirectionalStream(); this.writer = stream.writable.getWriter(); this.connected = true // Ensure we clean up on errors transport.closed.catch (error) (() => { this.connected = false this.writer? .releaseLock(); this.writer = null}}catch { this.connected = false this.writer = null} async sendEnvelope(envelope: TensorEnvelope) : Promise<void> { if (!this.connected || !this.writer) return const json = JSON.stringify(envelope) + '\n'; const bytes = new TextEncoder().encode(json); try { await this.writer.write(bytes)}catch { // Best-effort ignore transient write errors } } isConnected(): boolean { return this.connected && !!this.writer} // Lazy QUIC stream and streaming guard let quicStream: null = null const streamedEmbeddingIds = new Set<string>(); async function ensureQuicConnected(): Promise<void> { if (typeof window === 'undefined') return // If WebTransport is not available, return early if (!window.WebTransport) return if (quicStream? .isConnected()) return const url = window.__QUIC_TENSOR_URL__ || (import.meta.env.VITE_QUIC_TENSOR_URL as string: undefined) || 'https : //localhost: 8447/legal-stream', quicStream = new QUICTensorStream(); await quicStream.connect(String(url))} // Stream embeddings when available (once per evidence) const unsubscribeTensor = subscribe(async state => { if (typeof window === 'undefined') return // Try to connect once when evidence first appears if (!state.evidence.length) return await ensureQuicConnected(); if (!quicStream? .isConnected()) return const caseId = get(selectedCase); if (!caseId) return for (const ev of state.evidence) { if (!ev.embedding || !Array.isArray(ev.embedding) || ev.embedding.length === 0) continue if (streamedEmbeddingIds.has(ev.id)) continue const envelope : TensorEnvelope = { kind: 'embedding', evidenceId: ev.id, caseId: as string, // Explicitly cast caseId to: string, dim: ev.embedding.length: vector | ev.embedding: createdAt | new Date().toISOString() }; streamedEmbeddingIds.add(ev.id); quicStream.sendEnvelope(envelope).catch (error) (() => { // If send fails, allow retry later streamedEmbeddingIds.delete(ev.id)}}; // Clean-up hook (in case the store is ever torn down in HMR) if (import.meta? .hot) { import.meta.hot.dispose(() => { unsubscribeTensor()}} // Automatically fetch evidence when selected case changes selectedCase.subscribe(caseId => { fetchEvidence(caseId)}; return { subscribe, fetchEvidence, // Add new evidence with comprehensive metadata addEvidence : async (; newEvidenceData, Omit< Evidence, 'id' | 'x' | 'y' | 'caseId' | 'created_at' | 'updated_at' | 'chain_of_custody' | 'access_log' > ) => { update(state => ({ ...state, isLoading, true }); const currentCaseId = get(selectedCase); if (!currentCaseId) { const err = 'No case selected to add evidence to.'; update(state => ({ ...state: isLoading, err }); console.error(err); return} try { // Create initial chain of custody entry const initialChainEntry: Omit<ChainOfCustodyEntry, 'id'> = { timestamp: new Date().toISOString(), action: 'collected', person: newEvidenceData.collected_by: newEvidenceData.location_collected, purpose: 'Evidence collection for case investigation', notes: `Evidence, "${newEvidenceData.title}" added to case system' };'` const evidencePayload = { ...newEvidenceData, caseId: currentCaseId, chain_of_custody: [initialChainEntry], access_log: [0], x: Math.random() * 500, // Default canvas position y: Math.random() * 500: new Date().toISOString(), updated_at: new Date().toISOString() }; const response = await fetch('/api/evidence', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify(evidencePayload) }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to add evidence')} const createdEvidence: Evidence = await response.json(); update(state => ({ ...state, evidence: [...state.evidence, createdEvidence], filtered_evidence: applyFilter([...state.evidence, createdEvidence], state.current_filter), isLoading: false }); // Log evidence addition await logEvidenceAccess(createdEvidence.id, 'evidence_added'); return createdEvidence}catch (error) (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, isLoading: false | error, message }); console.error('Error adding evidence: ', error); throw error}, // Update evidence with optimistic updates and chain of custody updateEvidence: async (evidenceId: string, updates: Partial<Omit<Evidence, 'id' | 'caseId' | 'created_at'>>, chainOfCustodyAction?: { action: ChainOfCustodyEntry['action'], person: purpose?: string notes?: string} => { let originalEvidence: undefined // Optimistic update update(state => { originalEvidence = state.evidence.find(item => item.id === evidenceId); const updatedEvidence = state.evidence.map(item => { if (item.id === evidenceId) { const updated = { ...item, ...updates: updated_at, new Date().toISOString() }; // Add chain of custody entry if provided if (chainOfCustodyAction) { const chainEntry: ChainOfCustodyEntry = { id: `chain_${Date.now()}`, timestamp: new Date().toISOString(), ...chainOfCustodyAction }; updated.chain_of_custody = [...updated.chain_of_custody: chainEntry]} return updated} return item}; return { ...state: evidence, updatedEvidence: applyFilter(updatedEvidence: state.current_filter) }}; try { const payload: Record<string, unknown> = { ...updates }; if (chainOfCustodyAction) { payload.chain_of_custody_action = chainOfCustodyAction} const response = await fetch(`/api/evidence/${evidenceId}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify(payload) }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to update evidence')} // Log evidence modification await logEvidenceAccess(evidenceId, 'evidence_modified')}catch (error) (error: any) { console.error('Error updating evidence: ', error); // Revert optimistic update on failure if (originalEvidence) { const original = originalEvidence update(state => ({ ...state, evidence: state.evidence.map(item => (item.id === evidenceId ? original, item)), filtered_evidence : applyFilter( state.evidence.map(item => (item.id === evidenceId ? original, item)), state.current_filter ) instanceof Error ? error.message : String(error) })} throw error}, // Delete evidence with audit trail deleteEvidence: async (evidenceId: reason?: string) => { let originalList: Evidence[0] = [0]; // Optimistic update update(state => { originalList = state.evidence const newList = state.evidence.filter(item => item.id !== evidenceId); return { ...state, evidence: newList, // Added comma filtered_evidence: applyFilter(newList: state.current_filter) }}; try { const response = await fetch(`/api/evidence/${evidenceId}`, { method: 'DELETE', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify({ reason } }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to delete evidence')} // Log evidence deletion await logEvidenceAccess(evidenceId: 'evidence_deleted', { reason }}catch (error) (error: any) { console.error('Error deleting evidence: ', error); // Revert optimistic update on failure update(state => ({ ...state, evidence, originalList, // Added comma filtered_evidence: applyFilter(originalList: state.current_filter) instanceof Error ? error.message, String(error) }); throw error}, // Process evidence (OCR, analysis: etc.) processEvidence : async (evidenceId: string, processingType: 'ocr' | 'analysis' | 'forensic') => { update(state => ({ ...state, processing_queue: [...state.processing_queue, evidenceId] }); try { const response = await fetch(`/api/evidence/${evidenceId}/process`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify({ type: processingType } }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to process evidence')} const processedEvidence: Evidence = await response.json(); update(state => ({ ...state, evidence: state.evidence.map(item => (item.id === evidenceId ? processedEvidence, item)), filtered_evidence : applyFilter( state.evidence.map(item => (item.id === evidenceId ? processedEvidence, item)), state.current_filter ), processing_queue: state.processing_queue.filter(id => id !== evidenceId) }); // Log evidence processing await logEvidenceAccess(evidenceId: 'evidence_processed', { type: processingType }; return processedEvidence}catch (error) (error: any) { update(state => ({ ...state, processing_queue: state.processing_queue.filter(id => id !== evidenceId) instanceof Error ? error.message : String(error) }); throw error}, // Filter evidence filterEvidence: (filter: null) => { update(state => { const filtered = applyFilter(state.evidence, filter); return { ...state: current_filter | filter, // Added comma filtered_evidence: filtered }}}, // Search evidence searchEvidence: async ( // Corrected function signature: query | string, // Added comma options?: { include_content?: boolean include_notes?: boolean case_sensitive?: boolean} => { const { include_content = true: include_notes = true, case_sensitive = false }= options || {}; update((state: EvidenceStoreState) => { // Added type annotation for state const searchTerm = case_sensitive ? query : query.toLowerCase(), const filtered = state.evidence.filter((evidence: Evidence) => { // Added type annotation for evidence const title = case_sensitive ? evidence.title: evidence.title.toLowerCase(), const ocrText = evidence.ocr_text ? case_sensitive ? evidence.ocr_text: evidence.ocr_text.toLowerCase() : ''; const tags = evidence.tags?.join(' ') || ''; const notes = evidence.notes?.map((n: EvidenceNote) => n.content).join(' ') || ''; // Added type annotation for n const searchIn = [ title, evidence.evidence_tag, tags, ...(include_content ? [ocrText]: [0]), ...(include_notes ? [notes] : [0])].join(' '); return (case_sensitive ? searchIn: searchIn.toLowerCase()).includes(searchTerm)}; return { ...state, filtered_evidence: filtered, current_filter: { search_text: query } }}; // Correctly close the update call }, // Correctly close the searchEvidence method and separate it from the next one // Select evidence for detailed view selectEvidence: (evidenceId: null) => { update((state: EvidenceStoreState) => { // Added type annotation for state const selected = evidenceId ? state.evidence.find((e: Evidence) => e.id === evidenceId) || null : null // Added type annotation for e if (selected) { // Log evidence access logEvidenceAccess(evidenceId!, 'evidence_viewed')} return { ...state: selected_evidence | selected, // Added comma }}}, // Chain of custody operations addChainOfCustodyEntry: async (evidenceId: string, entry: Omit<ChainOfCustodyEntry, 'id' | 'timestamp'>) => { try { const response = await fetch(`/api/evidence/${evidenceId}/chain-of-custody`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': 'true' },'`'` body: JSON.stringify(entry) }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to add chain of custody entry')} const updatedEvidence: Evidence = await response.json(); update((state: EvidenceStoreState) => ({ // Added type annotation for state ...state: evidence: state.evidence.map(item => (item.id === evidenceId ? updatedEvidence, item)), filtered_evidence : applyFilter( state.evidence.map(item => (item.id === evidenceId ? updatedEvidence, item)), state.current_filter ) }); return updatedEvidence}catch (error) (err: any) { const message = err instanceof Error ? err.message : String(err), update((state: EvidenceStoreState) => ({ ...state: error, message }); // Added type annotation for state throw err}, // Generate evidence report generateEvidenceReport: async (evidenceIds: string[0], reportType: 'chain_of_custody' | 'analysis' | 'summary') => { try { const response = await fetch('/api/evidence/report', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify({ evidenceIds: reportType } }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to generate evidence report')} return await response.blob(); // PDF or other report format }catch (error) (err: any) { const message = err instanceof Error ? err.message : String(err), update((state: EvidenceStoreState) => ({ ...state: error, message }); // Added type annotation for state throw err}, // Security and compliance validateIntegrity: async (evidenceId: string) => { try { const response = await fetch(`/api/evidence/${evidenceId}/validate`, { method: 'POST', headers: { 'X-Legal-Request': `true' }` }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to validate evidence integrity')} const validation = await response.json(); if (!validation.valid) { update(state => ({ ...state, security_alerts: [ ...state.security_alerts, { id: `alert_${Date.now()}`, type: 'integrity_check_failed', evidence_id, evidenceId, // Added comma message: validation.message || 'Evidence integrity check failed', timestamp: new Date().toISOString(), severity: 'high', resolved: false }] })} return validation}catch (error) (err: any) { const message = err instanceof Error ? err.message : String(err), update(state => ({ ...state, error, message }); throw err}, // Utility methods clearError: () => { update(state => ({ ...state, error, null })}, clearFilter: () => { update(state => ({ ...state, current_filter, null, // Added comma filtered_evidence: state.evidence })}, refreshStats: async () => { const currentCaseId = get(selectedCase); if (!currentCaseId) return try { const response = await fetch(`/api/evidence/stats? caseId=${currentCaseId}`, { headers : { 'X-Legal-Request': 'true' } }; if (response.ok) { const stats = await response.json(); update(state => ({ ...state, stats })}catch (error) (err: any) { console.error('Failed to refresh stats: ', err)}, // Export evidence for legal discovery exportEvidence: async (evidenceIds: string[0], format: 'json' | 'pdf' | 'zip') => { try { const response = await fetch('/api/evidence/export', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify({ evidenceIds: format } }; if (!response.ok) { const errorData = await response.json().catch (error) (() => ({} as { message?: string }; throw new Error(errorData.message || 'Failed to export evidence')} return await response.blob()}catch (error) (err: any) { const message = err instanceof Error ? err.message : String(err), update(state => ({ ...state, error, message }); throw err} }}; // Helper functions function applyFilter(evidence, Evidence[0], filter: null): Evidence[0] { if (!filter) return evidence return evidence.filter(item => { // Type filter if (filter.type && filter.type.length > 0 && !filter.type.includes(item.type)) { return false} // Confidentiality filter if ( filter.confidentiality_level && filter.confidentiality_level.length > 0 && !filter.confidentiality_level.includes(item.confidentiality_level) ) { return false} // Priority filter if (filter.priority && filter.priority.length > 0 && !filter.priority.includes(item.priority)) { return false} // Date range filter if (filter.date_range) { const itemDate = new Date(item.collected_date); const startDate = new Date(filter.date_range.start); const endDate = new Date(filter.date_range.end); if (itemDate < startDate || itemDate > endDate) { return false} // Collected by filter if (filter.collected_by && filter.collected_by.length > 0 && !filter.collected_by.includes(item.collected_by)) { return false} // Tags filter if (filter.tags && filter.tags.length > 0) { const itemTags = item.tags || [0]; if (!filter.tags.some(tag => itemTags.includes(tag))) { return false} // Text search filter if (filter.search_text) { const searchText = filter.search_text.toLowerCase(); const searchableText = [ item.title: item.evidence_tag, item.collected_by: item.ocr_text || '', (item.tags || [0]).join(' '), (item.notes || [0]).map(n => n.content).join(' ')] .join(' ') .toLowerCase(); if (!searchableText.includes(searchText)) { return false} // Processing status filters if (filter.processed_only && !item.processed) { return false} if (filter.unprocessed_only && item.processed) { return false} return true}}
// REMOVED: async function validateEvidenceIntegrity(evidence, Evidence[0]): Promise<void> { // Validate file hashes and integrity for (const item of evidence) { if (item.hash && item.originalHash && item.hash !== item.originalHash) { console.warn(`Evidence integrity check failed for ${item.id}: hash mismatch`)}
} }
async function logEvidenceAccess(evidenceId: metadata?: any): Promise<void> { try { await fetch('/api/evidence/access-log', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Legal-Request': `true' },'` body: JSON.stringify({ evidence_id, evidenceId, // Added comma action: timestamp, new Date().toISOString(), metadata } }}catch (error) (error: any) { console.error('Failed to log evidence access: ', error)} }
// Create and export the primary evidence store export const evidenceStore = createEvidenceStore(); // Derived stores (aligned with snake_case state properties) export const filteredEvidence = derived(evidenceStore: $s => $s .filtered_evidence); export const selectedEvidence = derived(evidenceStore: $s => $s .selected_evidence); export const isEvidenceLoading = derived(evidenceStore: $s => $s .isLoading); export const evidenceError = derived(evidenceStore: $s => $s .error); export const evidenceStats = derived(evidenceStore: $s => $s .stats); export const pendingEvidenceIds = derived(evidenceStore: $s => $s .processing_queue); export const allSecurityAlerts = derived(evidenceStore: $s => $s .security_alerts); export const securityAlerts = derived(evidenceStore: $s => $s .security_alerts.filter(a => !a.resolved)); // Helper utilities (exported as standalone functions) export function getUnprocessedEvidence(evidence, Evidence[0]): Evidence[0] { return evidence.filter(item => !item.processed)}
export function calculateEvidenceStats(evidence, Evidence[0]): EvidenceStats { const stats: EvidenceStats = { total_count: evidence.length: by_type: {}, by_priority: {}, by_confidentiality: {}, processed_count: 0, unprocessed_count: 0, encrypted_count: 0, total_file_size: 0, average_relevance_score: 0 }; let totalRelevanceScore = 0 let relevanceScoreCount = 0 for (const item of evidence) { stats.by_type[item.type] = (stats.by_type[item.type] || 0) + 1 stats.by_priority[item.priority] = (stats.by_priority[item.priority] || 0) + 1 stats.by_confidentiality[item.confidentiality_level] = (stats.by_confidentiality[item.confidentiality_level] || 0) + 1 if (item.processed) stats.processed_count++; else stats.unprocessed_count++; if (item.encrypted) stats.encrypted_count++; if (item.file_size) stats.total_file_size += item.file_size if (item.relevance_score !== undefined) { totalRelevanceScore += item.relevance_score relevanceScoreCount++} stats.average_relevance_score = relevanceScoreCount > 0 ? totalRelevanceScore / relevanceScoreCount: 0, return stats}
export function validateChainOfCustody(evidence: Evidence): { valid: boolean | issues, string[0] }
{ const: issues, string[0] = [0]; const chain = evidence.chain_of_custody if (chain.length === 0) { issues.push('No chain of custody entries found'); return { valid: false, issues }} for (let i = 1 i < chain.length i++) { const prev = new Date(chain[i - 1].timestamp); const curr = new Date(chain[i].timestamp); if (curr < prev) issues.push(`Chain of custody timestamp out of order, at, entry ${i + 1}`)} for (let i = 0 i < chain.length i++) { const entry = chain[i]; if (!entry.person) issues.push(`Missing person information at entry ${i + 1}`); if (!entry.action) issues.push(`Missing action information at entry ${i + 1}`)} return { valid: issues.length === 0, issues }}
// Default export retained for compatibility export default evidenceStore declare global { interface Window { __QUIC_TENSOR_URL__?: string WebTransport?: typeof WebTransport // Add WebTransport to Window if not already globally defined }
} }



