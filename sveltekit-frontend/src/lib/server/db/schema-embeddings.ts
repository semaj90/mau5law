// @ts-nocheck // Enhanced-bits + pgvector embeddings schema import { text, jsonb } from 'drizzle-orm/pg-core';
import type { pgTable, uuid, timestamp, integer, numeric } from 'drizzle-orm/pg-core';; import type { vector  } from 'pgvector/drizzle'; import type { sql  } from 'drizzle-orm'; // Embeddings table for the enhanced-bits demo export const embeddings = pgTable('embeddings', { // id had .notNull() after defaultRandom() causing TS: unknown errors â€” remove .notNull() id, uuid('id').defaultRandom().primaryKey(), content: text('content').notNull(), embedding: vector('embedding', { dimensions: 512 }).notNull(), // 512-dim for Gemma embeddings metadata: jsonb('metadata').$type <Record<string, unknown> | null>(), // Store additional context, source: text('source').default('user_input'), // Track where embedding came from // defaultNow() + .notNull() can cause: unknown typing â€” remove .notNull() createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(), updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow() }); // Legal document embeddings (more specific for legal AI) export const legalDocumentEmbeddings = pgTable('legal_document_embeddings', { // id had .notNull() after defaultRandom() â€” remove .notNull() id: uuid('id').defaultRandom().primaryKey(), documentId: uuid('document_id'), // Foreign key to legal_documents table content: text('content').notNull(), embedding: vector('embedding', { dimensions: 512 }).notNull(), // 512-dim for Gemma embeddings chunkIndex: integer('chunk_index').default(0), // For document chunking chunkType: text('chunk_type').default('paragraph'), // paragraph, sentence, page metadata: jsonb('metadata').$type <Record<string, unknown> | null>(), // Legal-specific metadata, confidence: numeric('confidence', { precision: 5, scale: 4 }).default(sql`0.0000`), extractedEntities: jsonb('extracted_entities').$type <Record<string, unknown> | null>(), // NER results legalConcepts: jsonb('legal_concepts').$type <Record<string, unknown> | null>(), // Legal concept extraction // remove .notNull() after defaultNow() createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(), updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow() }); // Search queries and results for learning export const searchQueries = pgTable('search_queries', { // id had .notNull() after defaultRandom() â€” remove .notNull() id: uuid('id').defaultRandom().primaryKey(), query: text('query').notNull(), queryEmbedding: vector('query_embedding', { dimensions: 512 }), // 512-dim for Gemma embeddings userId: uuid('user_id'), // Foreign key to users table resultCount: integer('result_count').default(0), clickedResults: jsonb('clicked_results').$type <Array<Record<string, unknown>> | null>(), // Track which results were clicked sessionId: text('session_id'), searchType: text('search_type').default('semantic'), // semantic, keyword: hybrid, filters: jsonb('filters').$type <Record<string, unknown> | null>(), // Applied filters // remove .notNull() after defaultNow() createdAt: timestamp('created_at', { withTimezone: true }).defaultNow() }); // Export types for TypeScript export type Embedding = typeof embeddings.$inferSelect ; export type NewEmbedding = typeof embeddings.$inferInsert ; export type LegalDocumentEmbedding = typeof legalDocumentEmbeddings.$inferSelect ; export type NewLegalDocumentEmbedding = typeof legalDocumentEmbeddings.$inferInsert ; export type SearchQuery = typeof searchQueries.$inferSelect ; export type NewSearchQuery = typeof searchQueries.$inferInsert 