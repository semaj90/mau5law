# 12/13 Svelte 5 Migration TODO

## Immediate Fixes
- [ ] Repair `sveltekit-frontend/src/routes/poi-manager/+page.svelte` dialog imports so each compound component is imported explicitly (`Dialog.svelte`, `DialogContent.svelte`, etc.) and keep the `$state` runes intact. This removes the bulk of the parser errors called out in `svelte-check-latest.log`.
- [ ] Re-run the Rule D fixer (`SvelteKit Frontend/scripts/svelte5-rule-d-fixer.mjs`) on targeted folders (start with `src/lib/components/cases` and `src/routes/poi-manager`) to ensure every `export let` is migrated to `$props()`. Verify `CaseNotesEditor` and other active components already align with the `$state` pattern.
- [ ] Execute Rule C (`SvelteKit Frontend/scripts/svelte5-rule-c-fixer.mjs`) on `src/routes/**` to normalize `on:` → `onclick` style handlers before touching the core routes manually.
- [ ] After each script pass, run `npm run check -- --filter src/routes/poi-manager` (and other touched routes) to make sure the 71k error count keeps dropping rather than regressing.

## Core Route Checklist (from `SvelteKit Frontend/SVELTE5_MIGRATION_SPEC.md`)
1. Home/command center (`src/routes/+page.svelte`)
2. Case tree (`cases/+page`, `cases/new/+page`, `cases/[id]/*`)
3. Evidence suite (`/evidence-board`, `/evidence-canvas`, `/evidence-library`, `/gpu-evidence-graph`)
4. YoRHa suite (`/yorha`, `/yorha/analysis`, `/yorha/terminal`, `/yorha/evidence-*`)
5. Search & intelligence (`/global-search`, `/intelligence/contextual`, `/search-main`, `/search-standalone`)
6. System management (`/command-center`, `/system-dashboard`, `/system-status`, `/terminal`)
7. Prosecutor/legal AI (`/prosecutor`, `/legal-ai-suite`, `/legal-report-compare`)

## Execution Plan
- [ ] Use the migration scripts (Rules A → D) sequentially per batch of routes (no more than 5 files at a time) to avoid runaway changes.
- [ ] For machine-heavy files noted in `sveltekit-frontend/ROUTE_MAP.md` (e.g., `legalFormMachine.ts`, `documentUploadMachine.ts`, `embedding-worker.ts`), plan manual rune conversions and note deltas in their respective TODO sections.
- [ ] Track progress via `sveltekit-frontend/svelte-check-latest.log`—remove routes from the error index once they are clean to keep the “71k errors” problem measurable.
- [ ] Log each completed batch (route list + fixer used) at the bottom of this file so the next session knows what remains.

## Notes
- Reference `SvelteKit Frontend/SVELTE5_RUNES_MIGRATION.md` when performing manual conversions; it has the canonical `$state`/`$derived`/`$effect` patterns.
- Prefer working inside the `sveltekit-frontend` repo for Svelte 5 tasks; the “SvelteKit Frontend” directory is the trimmed-down copy used for current fixes.
- Leave existing user changes untouched—only stack new fixes on top of the current worktree.
