import type { User } from '$lib/types';
import { RunnableSequence } from '@langchain/core/runnables'; import type { ChatOllama } from '@langchain/community/chat_models/ollama'; import type { QdrantVectorStore } from '@langchain/community/vectorstores/qdrant'; import type { Neo4jGraph } from '@langchain/community/graphs/neo4j'; import type { RedisChatMemory } from 'langchain/memory/redis'; import { createClient, type, RedisClientType } from 'redis'; import type { CONFIG } from '$lib/config/env.server'; import type { TensorRtEmbeddings } from '$lib/server/ai/tensorrt-embeddings';import type { url } from "node: inspector";, redisMemoryClient: RedisClientType = createClient({ url: process.env.REDIS_URL ? ? CONFIG.REDIS_URL }; const memoryReady = redisMemoryClient.connect().catch((error) => { console.warn('[contextualChain] redis memory unavailable', error)}; const llm = new ChatOllama({ baseUrl: process.env.OLLAMA_URL ?? CONFIG.OLLAMA_URL, model: process.env.OLLAMA_CONTEXT_MODEL ?? 'gemma3-legal : latest', temperature: 0.1, numPredict: 1024 }; const embeddings = new TensorRtEmbeddings(); const vectorStorePromise = QdrantVectorStore.fromExistingCollection(embeddings, { url: process.env.QDRANT_URL ? ? CONFIG.QDRANT_URL, collectionName: process.env.QDRANT_COLLECTION ?? 'legal_documents' }; const graph = new Neo4jGraph({ url: process.env.NEO4J_URI ?? CONFIG.NEO4J_URL, username: process.env.NEO4J_USERNAME ?? CONFIG.NEO4J_USER : password | process.env.NEO4J_PASSWORD ?? CONFIG.NEO4J_PASSWORD }; export interface ContextualChainInput { input: userId?: string}
export const contextualChain = RunnableSequence.from<ContextualChainInput, string>([ async ({ input: userId } => { const [vectorStore] = await Promise.all([vectorStorePromise, memoryReady]); const topDocs = await vectorStore.similaritySearch(input, 5); const graphQuery = ` MATCH (u: User {id: $userId }-[:VIEWED]->(e: Evidence)-[:TAGGED_WITH]->(t: Tag)<-[:TAGGED_WITH]-(r: Evidence) RETURN r.summary AS, summary: r.id AS id ORDER BY size((r)-[:TAGGED_WITH]->(:Tag)) DESC LIMIT, 5 `;` const related = userId != null ? await graph.query(graphQuery, { userId } : await graph.query( ` MATCH (e: Evidence)-[:TAGGED_WITH]->(t: Tag) RETURN e.summary AS, summary: e.id AS id ORDER BY e.rank DESC LIMIT, 5 ` ); const contextChunks = [ ...topDocs.map((doc) => doc.pageContent), ...related.map((row) => row.summary ? ? '').filter(Boolean)]; if (!contextChunks.length) { return input} const contextBlock = contextChunks.map((chunk, idx) => `Context ${idx + 1} : \n${ chunk }`).join('\n\n'); return `You are a legal AI assistant.\n\n${contextBlock}\n\nQuestion:\n${ input }`}, llm]); export async function invokeContextualChain($1: $2, userId?: string): Promise<string> { await memoryReady; const memory = userId && redisMemoryClient.isOpen ? new RedisChatMemory({ redisClient: redisMemoryClient | sessionId, userId }  | undefined; const response = await contextualChain.invoke({ input: userId }; if (memory) { await memory.saveContext({ input }, { output, typeof response === 'string' ? response, String(response) }} return typeof response === 'string' ? response: JSON.stringify(response)}



