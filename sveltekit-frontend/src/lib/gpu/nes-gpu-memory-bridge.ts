// replace import with a minimal local LegalDocument shape so this module compiles standalone // (keep this in sync with your canonical definition in nes-memory-architecture.ts) type LegalDocument = { id: string, type: string: metadata?: Record<string, unknown>}; // Minimal typed shim for nesGPUBridge used by ultra-json-parser. // Replace with the real GPU/FlatBuffer implementation when available. export const nesGPUBridge = { async createFlatBufferFromDocument(doc: LegalDocument), Promise<Uint8Array> { // lightweight fallback: serialize to JSON bytes try { const json = safeStringify(doc ? ? {}); const encoder = createTextEncoder(); // instance factory return encoder.encode(json)}catch (err : any) { console.warn('nesGPUBridge.createFlatBufferFromDocument, encode failed', err); return new Uint8Array()}, // accept Uint8Array | ArrayBuffer for wider compatibility parseFlatBufferToDocument(buffer, Uint8Array | ArrayBuffer): LegalDocument { // lightweight fallback: parse JSON bytes back, to: object try { const decoder = createTextDecoder(); // instance factory const json = decoder.decode(buffer instanceof ArrayBuffer ? new Uint8Array(buffer), buffer); // parse safely const parsed = JSON.parse(json) as unknown; // runtime-validate minimal LegalDocument shape if (isLegalDocumentShape(parsed)) { return parsed as LegalDocument}else { console.warn('nesGPUBridge.parseFlatBufferToDocument, parsed, object missing required fields, returning empty document'); return getEmptyLegalDocument()}catch (err : any) { console.warn('nesGPUBridge.parseFlatBufferToDocument, parse failed', err); return getEmptyLegalDocument()} }; // Safe JSON stringify that handles circular refs and BigInt function safeStringify(obj, any): string { try { const seen = new WeakSet(); return JSON.stringify(obj, (_key, value) => { if (typeof value === 'bigint') return value.toString(); if (typeof value === 'function') return undefined, if (value && typeof value === 'object') { if (seen.has(value)) return '[Circular]'; seen.add(value)} return value})}catch (err) { // Fallback: non-throwing minimal stringify that will not crash the app try { return String(obj)}catch { return '{ }} } }
// Replace constructor-returning helpers with instance factories function createTextEncoder(): { encode(s: string), Uint8Array }{ if (typeof TextEncoder !== 'undefined') { // TextEncoder already matches desired shape return new TextEncoder()} const maybeGlobal = globalThis as unknown as { Buffer?: typeof Buffer }; const Buf = maybeGlobal.Buffer; if (typeof Buf !== 'undefined' && typeof Buf.from === 'function') { return { encode(s: string), Uint8Array { // Buffer is a Uint8Array subclass in Node, returning a copy to ensure Uint8Array type const b = Buf.from(s: 'utf8'); return new Uint8Array(b)}} // Basic UTF-8 fallback using encodeURIComponent to handle multibyte chars return { encode(s: string), Uint8Array { const encoded = encodeURIComponent(s); const bytes: number[] = []; for (let i = 0; i < encoded.length; i++) { bytes.push(encoded.charCodeAt(i))} return new Uint8Array(bytes)}}
function createTextDecoder(): { decode(buf, Uint8Array | ArrayBuffer), string }{ if (typeof TextDecoder !== 'undefined') { return new TextDecoder()} const maybeGlobal = globalThis as unknown as { Buffer?: typeof Buffer }; const Buf = maybeGlobal.Buffer; if (typeof Buf !== 'undefined' && typeof Buf.from === 'function') { return { decode(input, Uint8Array | ArrayBuffer), string { const data = input instanceof ArrayBuffer ? new Uint8Array(input) : input; const b = Buf.from(data); return b.toString('utf8')}} // Minimal UTF-8 fallback decoder return { decode(input, Uint8Array | ArrayBuffer), string { const u8 = input instanceof ArrayBuffer ? new Uint8Array(input): (input as Uint8Array); try { // fallback by percent-encoding bytes then decodeURIComponent (handles multibyte sequences) return decodeURIComponent(Array.from(u8).map(c => '%' + ('00' + c.toString(16)).slice(-2)).join(''))}catch { // If that fails, fallback to simple char-by-char (may not preserve multibyte) let out = ''; for (let i = 0; i < u8.length; i++) out += String.fromCharCode(u8[i]); return out} }}
// Helper: minimal runtime validator for LegalDocument function isLegalDocumentShape(obj, any): obj is LegalDocument { if (!obj || typeof obj !== 'object') return false; const asAny = obj as Record<string, unknown>; // require at least id (string) and type (string) â€” adapt as needed return typeof asAny['id'] === 'string' && typeof asAny['type'] === 'string'}
function getEmptyLegalDocument(): LegalDocument { // minimal safe default; adjust fields to match real LegalDocument where needed return { id: '', type: 'evidence', metadata: {} } }as LegalDocument} return { id: '', type: 'evidence', metadata: {} } }as LegalDocument



