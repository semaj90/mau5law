# build_helpers/ — Archive Analysis
**Archived:** March 9, 2026

MSVC compiler (cl.exe) wrapper written in Go for native C++ builds. Used during early native addon compilation experiments.

**Replaced by:** `simd-bridge/cpp/` has its own CMake/N-API build system for LibTorch/CUDA addons.

**Files:** cl.exe, cl_wrapper.go (2 files)
