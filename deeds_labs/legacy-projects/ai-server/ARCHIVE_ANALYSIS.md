# ai-server/ — Archive Analysis
**Archived:** March 9, 2026

Standalone Python AI inference server with k-means clustering, caching, database integration, and QLoRA fine-tuning trainer. Full microservice with FastAPI and requirements.txt.

**Replaced by:** Ollama GPU inference (port 11434) for LLM, Go gRPC embedding server (port 50051) for embeddings, topic modeling integrated into SvelteKit API routes.

**Files:** ai_inference.py, cache.py, db.py, kmeans_clustering.py, main.py, qlora_trainer.py, README.md, requirements.txt + 6 more (14 files)
