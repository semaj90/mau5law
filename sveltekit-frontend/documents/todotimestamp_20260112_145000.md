# Next Steps: Stabilizing Legal AI App (2026-01-12)

## 🚨 Critical Validation
- [ ] **Verify `/cases` Route**: Confirm cases load successfully after DB repair.
- [ ] **Verify `/evidence` Route**: Confirm evidence board loads.
- [ ] **Authentication Check**: Verify login flow works with the newly recreated `users` table (renamed `password_hash` -> `hashed_password`).

## 🛠️ Database Schema Cleanup
- [ ] **Resolving Remaining FK Mismatches**:
    - `case_scores.calculated_by` (integer) -> `users.id` (uuid) mismatch.
    - `citations.created_by` (integer) -> `users.id` (uuid) mismatch.
    - `themes.user_id` (integer) -> `users.id` (uuid) mismatch.
    - `canvas_states.user_id` (integer) -> `users.id` (uuid) mismatch.
    - **Action**: Create a migration to `ALTER COLUMN ... TYPE uuid USING ...` for all these fields.
- [ ] **Generate Clean Migration**: Run `npx drizzle-kit generate` to capture the current state as a valid migration baseline.
- [ ] **Seed Data**: The `users` table is currently empty (freshly created). **YOU MUST RUN SEEDING** to create an admin user (`legal_admin`).

## ⚡ Infrastructure & Performance
- [ ] **Go Microservices**: Fix networking/connectivity issues on `/api/health` (Redis/Qdrant unreachable).
- [ ] **Native Addons**: Resolve `simdjson` build errors in `npm install` to enable high-performance JSON parsing.
- [ ] **RabbitMQ**: Ensure RabbitMQ is consuming jobs correctly (Case Creation Machine).

## 🧪 Testing
- [ ] **Run End-to-End Tests**: `npx playwright test` to ensure critical paths (Login -> Cases -> Evidence) are robust.
