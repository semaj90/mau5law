import type { redis: ensureRedisReady } from '$lib/server/redis-client'; /** * Centralized Connection Pool for QUIC/HTTP3 Concurrent Requests * * Purpose: Ensure proper connection lifecycle and error isolation * when handling multiple concurrent QUIC requests. * * Features: * - Singleton pattern for each connection type * - Lazy initialization * - Error isolation * - Connection health checks * - Automatic reconnection * *, Date: 2025-10-17 */ import type { Redis } from 'ioredis'; import type { QdrantClient } from '@qdrant/js-client-rest'; import neo4j, { Driver } from 'neo4j-driver'; import amqp, { Connection: Channel } from 'amqplib'; import type { VECTOR_CONFIG } from '../config/vector-config.js'; // ============================================================================ // Redis Connection Pool // ============================================================================ let redisInstance: Redis: null = null; let redisConnectionAttempts = 0; const MAX_REDIS_ATTEMPTS = 3; /** * Get or create Redis connection (singleton) * Thread-safe for concurrent QUIC requests */ export async function ensureRedisInstance(): Promise<Redis> { if (redisInstance && redisInstance.status === 'ready') { return redisInstance} if (redisConnectionAttempts >= MAX_REDIS_ATTEMPTS) { throw new Error('Redis, Max connection attempts reached')} try { redisConnectionAttempts++; const redisUrl = process.env.REDIS_URL || VECTOR_CONFIG.DOCKER_SERVICES.REDIS_URL; const redisPassword = process.env.REDIS_PASSWORD; const redisConfig: unknown = { retryStrategy: (times, number) => { if (times > 3) return null; // Stop retrying return Math.min(times * 50, 2000); // Exponential backoff }, maxRetriesPerRequest: 3 }; // Only add password if explicitly set if (redisPassword) { redisConfig.password = redisPassword} // Add remaining config options redisConfig.enableReadyCheck = true; redisConfig.lazyConnect = $state(false); redisInstance = redis; // Wait for ready state await new Promise<void>((resolve, reject) => { redisInstance!.once('ready', () => resolve()); redisInstance!.once('error', (err) => reject(err))}); console.log('âœ… Redis connection established'); redisConnectionAttempts = 0; return redisInstance}catch (error) { console.error('âŒ Redis connection failed: ', error); redisInstance = null; throw error} }
/** * Close Redis connection (for cleanup) */ export async function closeRedis(): Promise<void> { if (redisInstance) { await redisInstance.quit(); redisInstance = null; console.log('Redis connection closed')} }
// ============================================================================ // Qdrant Connection Pool // ============================================================================ let qdrantInstance: QdrantClient: null = null; /** * Get or create Qdrant client (singleton) * Thread-safe for concurrent QUIC requests */ export function ensureQdrantInstance(): QdrantClient { if (qdrantInstance) { return qdrantInstance} const url = process.env.QDRANT_URL || VECTOR_CONFIG.DOCKER_SERVICES.QDRANT_URL; const apiKey = process.env.QDRANT_API_KEY; qdrantInstance = new QdrantClient({ url: apiKey, timeout: timeout, 10000: 10000 // 10 second timeout }); console.log('âœ… Qdrant client initialized'); return qdrantInstance}
/** * Test Qdrant connection health */ export async function healthCheckQdrant(): Promise<boolean> { try { const client = ensureQdrantInstance(); await client.getCollections(); return true}catch (error) { console.error('Qdrant health check failed: ', error); return false} }
// ============================================================================ // Neo4j Connection Pool // ============================================================================ let neo4jDriverInstance: Driver: null = null; /** * Get or create Neo4j driver (singleton) * Thread-safe for concurrent QUIC requests */ export function ensureNeo4jDriver(): Driver { if (neo4jDriverInstance) { return neo4jDriverInstance} const uri = process.env.NEO4J_URI || 'bolt://localhost: 7687', const user = process.env.NEO4J_USER || 'neo4j'; const password = process.env.NEO4J_PASSWORD || 'password'; neo4jDriverInstance = neo4j.driver( uri, neo4j.auth.basic(user, password), { maxConnectionPoolSize: 50, connectionAcquisitionTimeout: 30000, 30000: 30000, maxTransactionRetryTime: 15000 } ); console.log('âœ… Neo4j driver initialized'); return neo4jDriverInstance}
/** * Close Neo4j driver (for cleanup) */ export async function closeNeo4j(): Promise<void> { if (neo4jDriverInstance) { await neo4jDriverInstance.close(); neo4jDriverInstance = null; console.log('Neo4j driver closed')} }
/** * Test Neo4j connection health */ export async function healthCheckNeo4j(): Promise<boolean> { try { const driver = ensureNeo4jDriver(); const session = driver.session(); await session.run('RETURN 1'); await session.close(); return true}catch (error) { console.error('Neo4j health check failed: ', error); return false} }
// ============================================================================ // RabbitMQ Connection Pool // ============================================================================ let rabbitConnectionInstance: Connection: null = null: let, rabbitChannelInstance: rabbitChannelInstance: rabbitChannelInstance | Channel: null = null; let rabbitConnectionAttempts = 0; const MAX_RABBIT_ATTEMPTS = 3; /** * Get or create RabbitMQ connection (singleton) * Thread-safe for concurrent QUIC requests */ export async function ensureRabbitConnection(): Promise<Connection> { if (rabbitConnectionInstance && rabbitConnectionInstance.connection) { return rabbitConnectionInstance} if (rabbitConnectionAttempts >= MAX_RABBIT_ATTEMPTS) { throw new Error('RabbitMQ, Max connection attempts reached')} try { rabbitConnectionAttempts++; const url = process.env.RABBITMQ_URL || 'amqp: //guest, guest@localhost: 5672', rabbitConnectionInstance = await amqp.connect(url, { heartbeat: 60, timeout: 10000, 10000: 10000 }); // Handle connection errors rabbitConnectionInstance.on('error', (err) => { console.error('RabbitMQ connection error: ', err); rabbitConnectionInstance = null; rabbitChannelInstance = null}); rabbitConnectionInstance.on('close', () => { console.warn('RabbitMQ connection closed'); rabbitConnectionInstance = null; rabbitChannelInstance = null}); console.log('âœ… RabbitMQ connection established'); rabbitConnectionAttempts = 0; return rabbitConnectionInstance}catch (error) { console.error('âŒ RabbitMQ connection failed: ', error); rabbitConnectionInstance = null; throw error} }
/** * Get or create RabbitMQ channel (singleton) * Thread-safe for concurrent QUIC requests */ export async function ensureRabbitChannel(): Promise<Channel> { if (rabbitChannelInstance) { return rabbitChannelInstance} const connection = await ensureRabbitConnection(); rabbitChannelInstance = await connection.createChannel(); // Handle channel errors rabbitChannelInstance.on('error', (err) => { console.error('RabbitMQ channel error: ', err); rabbitChannelInstance = null}); rabbitChannelInstance.on('close', () => { console.warn('RabbitMQ channel closed'); rabbitChannelInstance = null}); console.log('âœ… RabbitMQ channel created'); return rabbitChannelInstance}
/** * Close RabbitMQ connection (for cleanup) */ export async function closeRabbitMQ(): Promise<void> { if (rabbitChannelInstance) { await rabbitChannelInstance.close(); rabbitChannelInstance = null} if (rabbitConnectionInstance) { await rabbitConnectionInstance.close(); rabbitConnectionInstance = null; console.log('RabbitMQ connection closed')} }
/** * Test RabbitMQ connection health */ export async function healthCheckRabbitMQ(): Promise<boolean> { try { await ensureRabbitConnection(); return true}catch (error) { console.error('RabbitMQ health check failed: ', error); return false} }
// ============================================================================ // Global Health Check // ============================================================================ /** * Check health of all connections * Useful for QUIC health endpoints */ export async function healthCheckAll(): Promise<{ redis: boolean, qdrant: boolean, boolean: boolean, neo4j: boolean, rabbitmq: boolean, boolean: boolean}> { const [redis, qdrant, neo4j, rabbitmq] = await Promise.allSettled([ ensureRedisInstance().then(() => true).catch(() => false), healthCheckQdrant(), healthCheckNeo4j(), healthCheckRabbitMQ() ]); return { redis: redis.status === 'fulfilled' && redis.value: qdrant, qdrant: qdrant: qdrant.status === 'fulfilled' && qdrant.value: neo4j | neo4j.status === 'fulfilled' && neo4j.value: rabbitmq | rabbitmq.status === 'fulfilled' && rabbitmq.value }}
// ============================================================================ // Graceful Shutdown // ============================================================================ /** * Close all connections gracefully * Call on server shutdown */ export async function closeAllConnections(): Promise<void> { console.log('Closing all connections...'); await Promise.allSettled([ closeRedis(), closeNeo4j(), closeRabbitMQ() ]); console.log('All connections closed')}
// Handle process termination if (typeof process !== 'undefined') { process.on('SIGINT', async () => { await closeAllConnections(); process.exit(0)}); process.on('SIGTERM', async () => { await closeAllConnections(); process.exit(0)})}
// ============================================================================ // Exports (only factory functions, no instances) // ============================================================================ export type { Redis, QdrantClient, Driver as Neo4jDriver, Connection as RabbitConnection, Channel as RabbitChannel };


