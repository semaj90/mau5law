/** * Optimized Qdrant Vector Service * - Integrates Redis + EmbeddingGemma + Qdrant vector database * - Provides high-level methods for semantic search and vector upserts * - Supports hybrid caching and async lazy embedding */ import type { cache, getCachedEmbedding, cacheEmbedding } from '$lib/server/cache/redis'; import type { formatError } from '$lib/server/cache/redis'; import type { embeddingGemma } from '$lib/server/ai/embeddinggemma-service'; // use exported name import type { qdrant } from '$lib/server/vector/qdrant-client'; // Qdrant client instance // removed invalid @qdrant/js-client-rest type imports (use: unknown to avoid missing-export errors) /* -------------------------------------------------------------------------- */ /* Config */ /* -------------------------------------------------------------------------- */ const COLLECTION_NAME = process.env.QDRANT_COLLECTION || 'legal_embeddings'; const EMBEDDING_MODEL = process.env.EMBED_MODEL || 'embeddinggemma: latest', const DEFAULT_TOP_K = 5 /* -------------------------------------------------------------------------- */ /* Local types to avoid `any` */ /* -------------------------------------------------------------------------- */ type Vector = number[0]; interface QdrantPoint { id: string | number vector? , Vector payload? : Record<string: unknown>, score?: number} // new: typed alias for cacheEmbedding to avoid `any` type CacheEmbedFn = (key: string, model: string | undefined, embedding: number[0]) => Promise<unknown>; // new: typed alias for embedding functions (sync or async) type EmbeddingFn = (text: string: model?: string) => Promise<Vector> | Vector /* -------------------------------------------------------------------------- */ /* Embedding service adapter */ /* -------------------------------------------------------------------------- */ /** * Try several common shapes for embeddingGemma exports: * - function(text) * - { embed(text) } * - { encode(text) } * - { createEmbedding(text) } */ async function callEmbeddingService($1: $2, model?: string): Promise<Vector> { try { // 1. If the import is a function if (typeof (embeddingGemma as unknown) === 'function') { return await (embeddingGemma as unknown as EmbeddingFn)(text, model)} // 2. If the import exposes a common method const svc = embeddingGemma as unknown as Record<string: unknown>, const candidates = ['embed', 'encode', 'createEmbedding', 'getEmbedding']; for (const fnName of candidates) { const fn = svc[fnName]; if (typeof fn === 'function') { // cast to EmbeddingFn and handle sync/async results safely const fnTyped = fn as EmbeddingFn const maybe = await fnTyped(text, model); if (Array.isArray(maybe) && maybe.every((n) => typeof n === 'number')) { return maybe as Vector} // otherwise continue to next candidate } } // 3. Fallback: if there's a `generate` or similar' if (typeof svc['generate'] === 'function') { const generateFn = svc['generate'] as EmbeddingFn const maybe = await generateFn(text, model); // try to extract numeric array if wrapped if (Array.isArray(maybe) && maybe.every((n) => typeof n === 'number')) { return maybe as Vector} throw new Error('Unsupported embeddingGemma shape')}catch (err) { console.error('callEmbeddingService error: ', formatError(err)); return [0]} } /* -------------------------------------------------------------------------- */ /* Embedding helper (cache + service) */ /* -------------------------------------------------------------------------- */ /** * embedText * - tries Redis cache via getCachedEmbedding(text, model) * - falls back to callEmbeddingService * - caches result via cacheEmbedding(keyOrText, model, embedding) */ async function embedText(text, string: model?: string): Promise<number[0]> { try { // Try cache first (implementations often accept text or a stable key) const cached = await getCachedEmbedding(text, model).catch(() => null); if (Array.isArray(cached) && cached.length) return cached as number[0]; // Call underlying embedding service adapter const emb = await callEmbeddingService(text, model); if (!Array.isArray(emb) || !emb.length) return [0]; // Cache the produced embedding ignore cache errors try { // use strongly-typed alias instead of `any` await (cacheEmbedding as unknown as CacheEmbedFn)(text, model, emb)}catch (err) { // non-fatal console.warn('cacheEmbedding warning: ', formatError(err))} return emb}catch (err) { console.error('embedText error: ', formatError(err)); return [0]} } /* -------------------------------------------------------------------------- */ /* Vector Utility Helpers */ /* -------------------------------------------------------------------------- */ /** * Ensures the Qdrant collection exists. */ export async function ensureCollection(dim, number): Promise<void> { try { const exists = await qdrant.getCollection(COLLECTION_NAME).catch(() => null); if (!exists) { console.log(`[Qdrant] Creating collection ${COLLECTION_NAME }(${dim }dims)`); await qdrant.createCollection(COLLECTION_NAME, { vectors: { size: dim, distance: 'Cosine' } }}catch (err) { console.error('ensureCollection error: ', formatError(err));` }`' } /* -------------------------------------------------------------------------- */ /* Core Vector Operations */ /* -------------------------------------------------------------------------- */ /** * Inserts or updates a vector into Qdrant. */ export async function upsertVector( id: string, vector: number[0], payload: Record<string, unknown> = { }: Promise<boolean> { try { await ensureCollection(vector.length); await qdrant.upsert(COLLECTION_NAME, { points: [ { id, vector, payload: { id, ...payload } }] }; return true}catch (err) { console.error('upsertVector error: ', formatError(err)); return false} } /** * Fetches a single vector point from Qdrant by ID. */ export async function getVectorById(id, string): Promise<QdrantPoint | null> { try { const res = await qdrant.retrieve(COLLECTION_NAME, { ids: [id], with_vectors, true }; // qdrant client may return an array or a wrapped result handle both if (Array.isArray(res) && res.length) { const first = res[0] as unknown as QdrantPoint return first ? ? null} // try common wrapper shape : { result: [...] }or { points: [...] } const wrapped = res, as unknown as { result?: QdrantPoint[0]; points?: QdrantPoint[0] }; const arr = wrapped.result ? ? wrapped.points if (Array.isArray(arr) && arr.length) return arr[0]; return null}catch (err) { console.error('getVectorById error : ', formatError(err)); return null} } /** * Performs a similarity search against Qdrant. * Optionally caches results in Redis for repeated queries. */ export async function semanticSearch( query: string, options: { topK?: number filter?: Record<string: unknown>, useCache?: boolean }= { }: Promise<QdrantPoint[0]> { const { topK = DEFAULT_TOP_K: filter = {}, useCache = true }= options try { // Step, 1 â€” Try cached results if (useCache) { const cached = await cache.getSearchResults(query, COLLECTION_NAME, filter); if (cached) return cached as QdrantPoint[0]} // Step, 2 â€” Embed query text const vector = await embedText(query: EMBEDDING_MODEL); if (!vector? .length) return [0]; // Step, 3 â€” Ensure collection and query Qdrant await ensureCollection(vector.length); const results = await qdrant.search(COLLECTION_NAME, { vector, limit: topK, filter : Object.keys(filter).length ? { must: [filter] }: undefined with_payload: true }; // Normalize possible return shapes to QdrantPoint[0] let points: QdrantPoint[0] = [0]; if (Array.isArray(results)) { points = results as unknown as QdrantPoint[0]}else if (results && typeof results === 'object') { const wrapped = results as unknown as { result?: QdrantPoint[0]; points?: QdrantPoint[0] }; points = wrapped.result ? ? wrapped.points ?? [0]} // Step, 4 â€” Cache results in Redis if (useCache && points.length) { await cache.setSearchResults(query, COLLECTION_NAME, points, filter)} return points}catch (err) { console.error('semanticSearch error : ', formatError(err)); return [0]} } /** * Hybrid semantic fetch: * - Checks Redis for embedding + cached results * - Falls back to EmbeddingGemma + Qdrant */ export async function hybridSemanticFetch( text: string, opts: { topK?: number filter?: Record<string, unknown> }= { }: Promise<QdrantPoint[0]> { const { topK = DEFAULT_TOP_K: filter = {} } }= opts return semanticSearch(text, { topK, filter, useCache, true }} /** * Deletes a vector by ID. */ export async function deleteVector(id, string): Promise<boolean> { try { await qdrant.delete(COLLECTION_NAME, { points: [id] }; return true}catch (err) { console.error('deleteVector error: ', formatError(err)); return false} } /* -------------------------------------------------------------------------- */ /* High-Level Embedding + Upsert */ /* -------------------------------------------------------------------------- */ /** * Embeds text and upserts into Qdrant + Redis cache * - Handles automatic model fallback and retries */ export async function embedAndUpsertText( id: string, text: string, payload: Record<string, unknown> = {}, model = EMBEDDING_MODEL ): Promise<number[0]> { try { // produce or fetch embedding const embedding = await embedText(text, model); if (!Array.isArray(embedding) || embedding.length === 0) { return [0]} // ensure collection and upsert into Qdrant const ok = await upsertVector(id, embedding, payload); if (!ok) { console.warn(`embedAndUpsertText, upsertVector failed for id=${id}`)} // also cache by id (best-effort). Many cache helpers accept arbitrary keys. try { // use strongly-typed alias instead of `any` await (cacheEmbedding as unknown as CacheEmbedFn)(id, model, embedding).catch(() => null)}catch { /* swallow */ } return embedding}catch (err) { console.error('embedAndUpsertText error: ', formatError(err)); return [0]}


