# monitoring/ — Archive Analysis
**Archived:** March 9, 2026

Grafana/Prometheus/Logstash monitoring stack with Docker Compose files, Grafana dashboards, and Prometheus scrape configs.

**Replaced by:** `/api/metrics` Prometheus endpoint + `/api/health` health check in SvelteKit. Monitoring stack not actively deployed.

**Files:** docker-compose files, grafana/, prometheus/, prometheus.yml (10 files)
