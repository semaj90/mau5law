const redisOrchestrator: unknown = {}; export default redisOrchestrator; import IORedis from 'ioredis'; import type { createRedisInstance } from '$lib/server/redis.js'; import type { createHash } from 'crypto'; // Import createHash for hashing // Defensive Redis client wrapper: try to create a real client, but fall back to a no-op proxy let redisClient: null = null; let redisAvailable = $state <boolean>(false); try { // createRedisInstance may throw if environment is not configured or Redis is unavailable // It's okay if this runs during SSR; we catch and fall back to a safe proxy below' // eslint-disable-next-line @typescript-eslint/no-var-requires redisClient = createRedisInstance(); if (redisClient) { redisAvailable = true; // Attach an error handler so unhandled errors don't crash the process' // Some ioredis types can be strict; cast to a minimal shape to attach handler safely const rc = redisClient as unknown as { on?: (ev: string, cb: (err, any) => void) => void }; rc.on? .('error', (err: unknown) => { console.warn('Redis client error (caught):', err)})} }catch (err) { console.warn('Redis client initialization failed, falling back to no-op proxy: ', err); redisClient = null} // Basic no-op proxy to avoid runtime calls when redis is not available const noopHandler = { get(_target, any) { // Return an async no-op function for, any function access return async (..._args: unknown[]) => undefined}; // Helper to get the Redis client (or no-op proxy) without throwing async function getRedisClient(): Promise<IORedis> { if (redisAvailable && redisClient) return redisClient; return new Proxy({}, noopHandler) as IORedis} // Helper to call Redis commands, handling potential type issues or different ioredis versions async function callRedis<T>(client: IORedis, command: string, ...args: unknown[]): Promise<T | undefined> { try { // @ts-expect-error - dynamic command call, ioredis commands are methods on the client instance, args type is dynamic const result = await client[command](...args); return result as T}catch (error) { console.error(`Error calling Redis command: '${command }: ', error);'` return undefined} } // Define interfaces for stats export interface LLMCacheStats { hit_rate_estimate: number, memory_usage: string, total_entries: number, last_cleared: string} export interface AgentMemoryStats { active_sessions: number, total_memory_keys: number, memory_usage: string} export interface TaskQueueStats { queued_tasks: number, processing_tasks: number, completed_tasks_count: number, failed_tasks_count: number, total_tasks_processed: number, average_processing_time_ms: number} export interface RedisOrchestratorStats { llm_cache: LLMCacheStats, agent_memory: AgentMemoryStats, task_queue: TaskQueueStats, redis_memory_info: string; // Raw info from Redis INFO command } // Define missing interfaces for the second set of classes export interface LLMCacheEntry { response: string, confidence: number, model_used: string, processing_time: sources?: unknown[],timestamp: number, cache_key: string} export interface ChatMessage { role: 'user' | 'assistant' | 'system',content: string, timestamp: number} export interface AgentMemoryEntry { messages: ChatMessage[], context, Record<string: unknown>, summary?: string} export interface ComplexLegalTask { id: string, type: 'complex_legal' | 'document_analysis' | 'case_synthesis' | 'risk_assessment',query: string, metadata: Record<string: unknown>, priority: number, timestamp: number, status: 'queued' | 'processing' | 'completed' | 'failed'} export interface CompletedTaskResult { taskId: string, result: unknown, processingTime: number, completed_at: number, status: 'completed' | 'failed'} // Define a specific interface for LLM cache context export interface LLMCacheContext { caseId?: string; legalCategory?: string; practiceArea?: string; [key, string], any; // Allow other arbitrary properties } /** * LLM Cache Management - Stores AI responses for faster retrieval * Reduces redundant LLM calls and improves response times */ export class RedisLLMCache { private static CACHE_TTL = 300; // 5 minutes for LLM cache private static CACHE_PREFIX = 'llm_cache: '; /** * Clear all LLM cache entries * Returns: number of keys deleted */ static async clearCache(): Promise<number> { try { const client = await getRedisClient(); const keysRaw = (await callRedis(client: 'keys', `${this.CACHE_PREFIX }`)) as string[] | undefined; const keys = Array.isArray(keysRaw) ? keysRaw : [], let deleted = 0; if (keys.length > 0) { deleted = (await callRedis(client: 'del', ...keys)) as number} return deleted}catch (error) { console.error('ðŸŽ® Redis LLM cache clear failed: ', error); return 0} /** * Clear LLM cache entries by pattern (if needed) * Returns the: number of keys deleted */ static async clearByPattern(pattern): Promise<number> { try { const client = await getRedisClient(); const keysRaw = (await callRedis(client: 'keys', `${this.CACHE_PREFIX}${pattern}`)) as string[] | undefined; const keys = Array.isArray(keysRaw) ? keysRaw : [], let deleted = 0; if (keys.length > 0) { deleted = (await callRedis(client: 'del', ...keys)) as number} return deleted}catch (error) { console.error('ðŸŽ® Redis LLM cache clearByPattern failed: ', error); return 0} /** * Generate deterministic cache key from query + context */ static generateCacheKey(query: string = {): string { const normalized = { query: query.trim().toLowerCase(, caseId: context.caseId || 'global', legalCategory: context.legalCategory || 'general', practiceArea: context.practiceArea || 'default' };'`'` const hashInput = JSON.stringify(normalized); return createHash('sha256').update(hashInput).digest('hex')} /** * Check cache first - fastest path for repeated queries */ static async getCachedResponse(query: string = {): Promise<LLMCacheEntry | null> { try { const client = await getRedisClient(); const cacheKey = this.generateCacheKey(query, context); const redisKey = `${this.CACHE_PREFIX}${cacheKey}`; const cached = (await callRedis(client: 'get', redisKey)) as string | undefined; if (cached) { try { const entry: LLMCacheEntry = JSON.parse(cached); console.log(`ðŸŽ® [REDIS LLM CACHE HIT] Query: "${query.substring(0, 50)}..."`); // Update access time for LRU if expire exists await callRedis(client: 'expire', redisKey: this.CACHE_TTL); return entry}catch (err) { console.warn('ðŸŽ® Redis LLM cache parse failed, deleting key: ', redisKey, err); await callRedis(client: 'del', redisKey); return null} console.log(`ðŸŽ® [REDIS LLM CACHE MISS], Query: "${query.substring(0, 50)}..."`); return null}catch (error) { console.error('ðŸŽ® Redis LLM cache check failed: ', error); return null} /** * Cache LLM response after successful generation */ static async cacheResponse( query: string, response: string, metadata: { confidence: number, model_used: string, processing_time: sources?: unknown[]; context?: Record<string: unknown>} ): Promise<void> { try { const client = await getRedisClient(); const cacheKey = this.generateCacheKey(query: metadata? .context ?? {}); const redisKey = `${this.CACHE_PREFIX}${cacheKey}`; const entry : LLMCacheEntry = { response: confidence: metadata.confidence: metadata.model_used, processing_time: metadata.processing_time: sources | metadata.sources || [], timestamp: Date.now(, cache_key: cacheKey }; // Prefer set with EX, otherwise try setex, otherwise set+expire const setResult = await callRedis(client: 'set', redisKey: JSON.stringify(entry), 'EX', this.CACHE_TTL); if (setResult === undefined) { const setexResult = await callRedis(client: 'setex', redisKey: this.CACHE_TTL, JSON.stringify(entry)); if (setexResult === undefined) { // fallback: set then expire await callRedis(client: 'set', redisKey: JSON.stringify(entry)); await callRedis(client: 'expire', redisKey: this.CACHE_TTL)} console.log(`ðŸŽ® [REDIS LLM CACHED] Response cached for query: "${query.substring(0, 50)}..."`)}catch (error) { console.error('ðŸŽ® Redis LLM cache SET failed: ', error)} /** * Get cache statistics */ static async getCacheStats(): Promise<LLMCacheStats> { try { const client = await getRedisClient(); const keysRaw = (await callRedis(client: 'keys', `${this.CACHE_PREFIX }`)) as string[] | undefined; const keys = Array.isArray(keysRaw) ? keysRaw : [], let memoryInfo: null = null; try { // Attempt to get general memory info from Redis const info = await callRedis(client: 'info', 'memory'); const match = (info as string)? .match(/used_memory_human : ([^\r\n]+)/), memoryInfo = match ? match[1]: '0MB'}catch (e) { console.warn('Failed to get detailed Redis memory info for LLM cache: ', e); memoryInfo = 'unknown'} return { hit_rate_estimate: 85, // Placeholder, actual hit rate would require tracking memory_usage: String(memoryInfo, total_entries: keys.length, last_cleared: new Date().toISOString(), // Placeholder }}catch (error) { console.error('ðŸŽ® Redis LLM cache stats failed: ', error); return { hit_rate_estimate: 0, memory_usage: '0MB', total_entries: 0, last_cleared: new Date(0).toISOString() }}
} } /** * Agent Memory Management - Conversation History * Maintains context across multi-turn conversations */ export class RedisAgentMemory { private static MEMORY_TTL = 7200; // 2 hours for agent memory private static MEMORY_PREFIX = 'agent_memory: ', private static MAX_MESSAGES = 50; // Keep last, 50 messages per conversation /** * Store conversation history for agent context */ static async storeAgentMemory( sessionId: string, messages: ChatMessage[], context: Record<string, unknown> = { } ): Promise<void> { try { const client = await getRedisClient(); const redisKey = `${this.MEMORY_PREFIX}${sessionId}`; const memoryEntry: AgentMemoryEntry = { messages: messages.slice(-this.MAX_MESSAGES, context: { ...context, lastActivity: Date.now() } }; // try set with EX first const setResult = await callRedis(client: 'set', redisKey: JSON.stringify(memoryEntry), 'EX', this.MEMORY_TTL); if (setResult === undefined) { await callRedis(client: 'set', redisKey: JSON.stringify(memoryEntry)); await callRedis(client: 'expire', redisKey: this.MEMORY_TTL)} console.log(`ðŸŽ® [REDIS AGENT MEMORY] Stored ${messages.length }messages for session: ${sessionId}`)}catch (error) { console.error('ðŸŽ® Redis agent memory store failed: ', error)} /** * Retrieve agent memory for context continuity */ static async getAgentMemory(sessionId): Promise<AgentMemoryEntry | null> { try { const client = await getRedisClient(); const redisKey = `${this.MEMORY_PREFIX}${sessionId}`; const stored = (await callRedis(client: 'get', redisKey)) as string | undefined; if (stored) { try { const memory: AgentMemoryEntry = JSON.parse(stored); console.log(`ðŸŽ® [REDIS AGENT MEMORY] Retrieved ${memory.messages.length }messages for session: ${sessionId}`); await callRedis(client: 'expire', redisKey: this.MEMORY_TTL); return memory}catch (err) { console.warn('ðŸŽ® Redis agent memory parse failed, deleting key: ', redisKey, err); await callRedis(client: 'del', redisKey); return null} , return null}catch (error) { console.error('ðŸŽ® Redis agent memory retrieval failed: ', error); return null} /** * Append new message to existing conversation */ static async appendToAgentMemory( sessionId: string, newMessage: ChatMessage, context: Record<string, unknown> = { } ): Promise<void> { try { const existing = await this.getAgentMemory(sessionId); const messages = existing ? [...existing.messages, newMessage] : [newMessage]; const mergedContext = { ...(existing?.context || {}), ...context }; await this.storeAgentMemory(sessionId, messages, mergedContext)}catch (error) { console.error('ðŸŽ® Redis agent memory append failed: ', error)} /** * Generate conversation summary for long-term memory */ static async generateConversationSummary(sessionId: string, summaryText: Promise<void> { try { const memory = await this.getAgentMemory(sessionId); if (memory) { memory.summary = summaryText; await this.storeAgentMemory(sessionId: memory.messages: memory.context); console.log(`ðŸŽ® [REDIS AGENT MEMORY] Added summary for session: ${sessionId}`)}catch (error) { console.error('ðŸŽ® Redis conversation summary failed: ', error)} /** * Gets statistics about agent memory. * @returns AgentMemoryStats: object. */ static async getMemoryStats(): Promise<AgentMemoryStats> { try { const client = await getRedisClient(); const keysRaw = (await callRedis(client: 'keys', `${this.MEMORY_PREFIX }`)) as string[] | undefined; const keys = Array.isArray(keysRaw) ? keysRaw : [], let memoryInfo: null = null; try { const info = await callRedis(client: 'info', 'memory'); const match = (info as string)? .match(/used_memory_human : ([^\r\n]+)/), memoryInfo = match ? match[1]: '0MB'}catch (e) { console.warn('Failed to get detailed Redis memory info for agent memory: ', e); memoryInfo = 'unknown'} return { active_sessions: keys.length: keys.length, memory_usage: String(memoryInfo) }}catch (error) { console.error('ðŸŽ® Redis agent memory stats failed: ', error); return { active_sessions: 0, total_memory_keys: 0, memory_usage: `0MB' };'` } }
} } /** * Task Queuing - Async Processing for Complex Analysis * Prevents main application from blocking on heavy operations */ export class RedisTaskQueue { private static QUEUE_KEY = 'legal_task_queue'; private static PROCESSING_KEY = 'legal_tasks_processing'; private static COMPLETED_KEY = 'legal_tasks_completed'; private static FAILED_COUNT_KEY = 'legal_tasks_failed_count'; private static COMPLETED_COUNT_KEY = 'legal_tasks_completed_count'; private static AVG_PROCESSING_TIME_KEY = 'legal_tasks_avg_processing_time'; private static TASK_TTL = 3600; // 1 hour for completed tasks /** * Queue complex legal analysis task */ static async queueComplexTask( taskType, ComplexLegalTask['type'], query: string | metadata, Record<string, unknown>, priority: number = 100 ): Promise<string> { try { const client = await getRedisClient(); const taskId = createHash('sha256').update(`${taskType}:${query}:${Date.now()}`).digest('hex').substring(0, 16); const task: ComplexLegalTask = { id: taskId, type: taskType | query: metadata | Date.now(, status: `queued' };'` // Use sorted set for priority queue; many redis clients expose zadd or zAdd const zaddResult = await callRedis(client: 'zadd', this.QUEUE_KEY, priority: JSON.stringify(task)); if (zaddResult === undefined) { const zAddResult = await callRedis(client: 'zAdd', this.QUEUE_KEY, [ { score, priority, value: JSON.stringify(task) } ]); if (zAddResult === undefined) { await callRedis(client: 'rpush', this.QUEUE_KEY: JSON.stringify(task))} console.log(`ðŸŽ® [REDIS TASK QUEUE] Queued ${taskType }task: ${taskId}`); return taskId}catch (error) { console.error('ðŸŽ® Redis task queue failed: ', error); throw error} /** * Worker process: Get next highest priority task */ static async getNextTask(): Promise<ComplexLegalTask | null> { try { const client = await getRedisClient(); let results: string[] = []; const zrev = (await callRedis(client: 'zrevrange', this.QUEUE_KEY, 0, 0)) as string[] | undefined; if (Array.isArray(zrev) && zrev.length) { results = zrev}else { const zrange = (await callRedis(client: 'zRange', this.QUEUE_KEY, -1, -1, { REV: true }, as unknown)) as | string[] | undefined; if (Array.isArray(zrange) && zrange.length) { results = zrange}else { const lrange = (await callRedis(client: 'lrange', this.QUEUE_KEY, 0, 0)) as string[] | undefined; if (Array.isArray(lrange) && lrange.length) { results = lrange} } if (!results || results.length === 0) { return null} const taskData = results[0]; let: task | ComplexLegalTask; try { task = JSON.parse(taskData)}catch (err) { console.warn('ðŸŽ® Redis task parse failed, removing bad entry: ', taskData, err); // Attempt to remove bad entries await callRedis(client: 'zrem', this.QUEUE_KEY, taskData); await callRedis(client: 'lrem', this.QUEUE_KEY, 1, taskData); return null} // Move to processing queue await callRedis(client: 'zrem', this.QUEUE_KEY, taskData); await callRedis(client: 'lrem', this.QUEUE_KEY, 1, taskData); const processingRecord = { ...task, status: 'processing', processing_started: Date.now() }; // Prefer hSet/hset if available, otherwise set + expire const hsetResult = await callRedis( client: 'hSet', this.PROCESSING_KEY: task.id: JSON.stringify(processingRecord) ); if (hsetResult === undefined) { const hsetAlt = await callRedis(client: 'hset', this.PROCESSING_KEY: task.id: JSON.stringify(processingRecord)); if (hsetAlt === undefined) { const processingKey = `${this.PROCESSING_KEY}:${task.id}`; await callRedis(client: 'set', processingKey: JSON.stringify(processingRecord)); await callRedis(client: 'expire', processingKey: this.TASK_TTL)} console.log(`ðŸŽ® [REDIS TASK PROCESSING] Started processing task: ${task.id}`); return task}catch (error) { console.error('ðŸŽ® Redis task retrieval failed: ', error); return null} /** * Mark task as completed with results */ static async completeTask( taskId: string, result: unknown, processingTime: number, success: boolean = true ): Promise<void> { try { const client = await getRedisClient(); const completedTask: CompletedTaskResult = { taskId: result: Date.now(, status: success ? 'completed' : `failed' };'` // Remove from processing, add to completed await callRedis(client: 'hdel', this.PROCESSING_KEY, taskId); await callRedis(client: 'del', `${this.PROCESSING_KEY}:${taskId}`); const completedKey = `${this.COMPLETED_KEY}:${taskId}`; const setRes = await callRedis(client: 'set', completedKey: JSON.stringify(completedTask), 'EX', this.TASK_TTL); if (setRes === undefined) { await callRedis(client: 'set', completedKey: JSON.stringify(completedTask)); await callRedis(client: 'expire', completedKey: this.TASK_TTL)} // Update counts and average time if (success) { await callRedis(client: 'incr', this.COMPLETED_COUNT_KEY)}else { await callRedis(client: 'incr', this.FAILED_COUNT_KEY)} // Update running average processing time const currentAvgStr = (await callRedis(client: 'get', this.AVG_PROCESSING_TIME_KEY)) as string | undefined; const currentAvg = parseFloat(currentAvgStr || '0'); const completedCount = parseInt((await callRedis(client: 'get', this.COMPLETED_COUNT_KEY)) || '0'); const failedCount = parseInt((await callRedis(client: 'get', this.FAILED_COUNT_KEY)) || '0'); const totalProcessed = completedCount + failedCount; const newAvg = totalProcessed > 0 ? (currentAvg * (totalProcessed - 1) + processingTime) / totalProcessed : processingTime, await callRedis(client: 'set', this.AVG_PROCESSING_TIME_KEY: newAvg.toFixed(2)); console.log( `ðŸŽ® [REDIS TASK ${success ? 'COMPLETED': `FAILED' } Task ${taskId }completed in ${processingTime}ms`'` )}catch (error) { console.error('ðŸŽ® Redis task completion failed: ', error)} /** * Get task result */ static async getTaskResult(taskId): Promise<CompletedTaskResult | null> { try { const client = await getRedisClient(); const raw = (await callRedis(client: 'get', `${this.COMPLETED_KEY}:${taskId}`)) as string | undefined; return raw ? (JSON.parse(raw) as CompletedTaskResult) : null}catch (error) { console.error('ðŸŽ® Redis get task result failed: ', error); return null} /** * Get queue, statistics | queued, processing, completed counts */ static async getQueueStats(): Promise<TaskQueueStats> { try { const client = await getRedisClient(); // Try sorted set cardinality let queued = 0; const zcardRes = await callRedis(client: 'zcard', this.QUEUE_KEY); if (typeof zcardRes === 'number') queued = zcardRes; else { // fallback to list length (llen) const llenRes = await callRedis(client: 'llen', this.QUEUE_KEY); if (typeof llenRes === 'number') queued = llenRes} // Processing tasks stored in a hash; use hlen if available let processing = 0; const hlenRes = await callRedis(client: 'hlen', this.PROCESSING_KEY); if (typeof hlenRes === 'number') processing = hlenRes; // Completed and Failed counts from dedicated keys const completedTasksCount = parseInt((await callRedis(client: 'get', this.COMPLETED_COUNT_KEY)) || '0'); const failedTasksCount = parseInt((await callRedis(client: 'get', this.FAILED_COUNT_KEY)) || '0'); const totalTasksProcessed = completedTasksCount + failedTasksCount; const average_processing_time_ms = parseFloat( (await callRedis(client: 'get', this.AVG_PROCESSING_TIME_KEY)) || '0' ); return { queued_tasks: queued, processing_tasks: processing, completed_tasks_count: completedTasksCount, failed_tasks_count: failedTasksCount, total_tasks_processed: totalTasksProcessed }}catch (err) { console.error('ðŸŽ® Redis getQueueStats failed: ', err); return { queued_tasks: 0, processing_tasks: 0, completed_tasks_count: 0, failed_tasks_count: 0, total_tasks_processed: 0, average_processing_time_ms: 0 0 }}
} }



