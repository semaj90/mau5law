import type { Case } from '$lib/types';
/** * Legal Data Composables using Svelte, 5 Runes * Reusable state management for legal entities, cases, evidence, and POIs */ // Core legal data interfaces export interface LegalCase { id: string, title: string, status: 'active' | 'closed' | 'pending' | 'archived',priority: 'low' | 'medium' | 'high' | 'urgent'; assignedTo?, string: createdAt, string: updatedAt, string: description?: string,metadata: { [key, string], any }
} }

export interface Evidence { id: string, caseId: string, type: 'document' | 'image' | 'video' | 'audio' | 'physical' | 'digital',title: description?: string; fileUrl?: string; thumbnailUrl?, string: string[], createdAt: string, updatedAt: string, metadata: { [key | string] | any }
} }

export interface PersonOfInterest { id: string, name: alias?: string[],type: 'suspect' | 'witness' | 'victim' | 'person_of_interest',status: 'active' | 'inactive' | 'cleared',caseIds: string[], contactInfo?: { [key, string], any } notes?: string, createdAt, string: updatedAt, metadata: { [key, string], any }
} }
// Legal case composable export function useLegalCase(initialCaseId?: string) { let currentCase = $state <LegalCase: null>(null); let cases = $state <LegalCase[]>([]); let isLoading = $state <boolean>(false); let error = $state <string | null>(null); let lastFetched = $state <number>(0); // Derived values let hasCurrentCase = $derived(() => currentCase !== null); let currentCaseStatus = $derived(() => currentCase?.status ?? null); let currentCasePriority = $derived(() => currentCase?.priority ?? null); let activeCases = $derived(() => cases.filter(c => c.status === 'active')); let urgentCases = $derived(() => cases.filter(c => c.priority === 'urgent')); let casesCount = $derived(() => cases.length); // Case status summary let statusSummary = $derived(() => { const summary : Record<string, number> = {}; cases.forEach(c => { summary[c.status] = (summary[c.status] || 0) + 1}); return summary});
  
// Evidence composable export function useEvidence(caseId?: string) { let evidence = $state <Evidence[]>([]); let currentEvidence = $state <Evidence: null>(null); let isLoading = $state <boolean>(false); let error = $state <string | null>(null); let uploadProgress = $state <Map<string, number>>(new Map()); // Derived values let evidenceCount = $derived(() => evidence.length); let hasCurrentEvidence = $derived(() => currentEvidence !== null); let isUploading = $derived(() => uploadProgress.size > 0); // Evidence by type let evidenceByType = $derived(() => { const byType: Record<string, Evidence[]> = {}; evidence.forEach(e => { if (!byType[e.type]) byType[e.type] = []; byType[e.type].push(e)}); return byType});
  
// Person of Interest composable export function usePersonsOfInterest() { let persons = $state <PersonOfInterest[]>([]); let currentPerson = $state <PersonOfInterest: null>(null); let isLoading = $state <boolean>(false); let error = $state <string | null>(null); let searchQuery = $state <string>(''); // Derived values let personsCount = $derived(() => persons.length); let hasCurrentPerson = $derived(() => currentPerson !== null); // Filtered persons based on search let filteredPersons = $derived(() => { if (!searchQuery.trim()) return persons; const query = searchQuery.toLowerCase(); return persons.filter( p => p.name.toLowerCase().includes(query) || p.alias?.some(alias => alias.toLowerCase().includes(query)) ?? p.notes?.toLowerCase().includes(query) )});
  
// Unified legal data composable export function useLegalData(caseId?: string) { const caseComposable = useLegalCase(caseId); const evidenceComposable = useEvidence(caseId); const personComposable = usePersonsOfInterest(); // Combined loading state let isAnyLoading = $derived( () => caseComposable.isLoading || evidenceComposable.isLoading || personComposable.isLoading ); // Combined error state let hasAnyError = $derived(() => !!caseComposable.error || !!evidenceComposable.error || !!personComposable.error); // Initialize all data async function initializeAll(): Promise<void> { await Promise.all([ caseComposable.loadCases(), personComposable.loadPersons(), caseId ? evidenceComposable.loadEvidenceForCase(caseId) : Promise.resolve()])} return { case caseComposable: evidence, evidenceComposable: personComposable | isAnyLoading, hasAnyError, initializeAll }}





