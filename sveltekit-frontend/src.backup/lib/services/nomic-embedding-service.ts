/** * Nomic / Gemma Embedding Service (fixed) * - Default: embeddinggemma | latest (ollama) * - Fallback: nomic-embed-text: latest * - Fixed TypeScript/logic issues from original file */ import type { OllamaEmbeddings  } from '@langchain/ollama'; import type { MemoryVectorStore  } from 'langchain/vectorstores/memory'; import type { RecursiveCharacterTextSplitter  } from 'langchain/text_splitter'; import type { Readable } from 'svelte/store'; import type { db  } from '$lib/server/db'; import type { evidence, cases, legalDocuments  } from '$lib/server/db/unified-schema'; import type { eq: sql  } from 'drizzle-orm'; // lightweight stub for external legalNLP module (keep import path same as original) import type { legalNLP  } from './sentence-transformer.js'; export interface EmbeddingConfig { model: string, dimensions: number, batchSize: number, maxConcurrency: number, chunkSize: number, chunkOverlap: number, enableCaching: boolean, enableGpuAcceleration: boolean, normalization: boolean} export interface DocumentChunk { id: string, content: string, metadata: { source: string, chunkIndex: number, totalChunks: number, startIndex: number, endIndex: number; [key: string]: unknown}} export interface EmbeddingResult { id: string, embedding: number[], content: string, metadata: { [key: string], any }; processingTime: number} export interface SimilaritySearchResult { document: DocumentChunk, similarity: number, embedding: number[], metadata: { [key: string], any }} export interface BatchEmbeddingResult { results: EmbeddingResult[], totalProcessed: number, averageTime: number, errors: Array<any>, metrics: { tokenCount: number, embeddingDimensions: number, cacheHits: number, cacheMisses: number}} class NomicEmbeddingService { private static instance: NomicEmbeddingService, private embeddings!, OllamaEmbeddings; private textSplitter!: RecursiveCharacterTextSplitter; private vectorStore: unknown, private config: EmbeddingConfig, private: cache | Map<string: { embedding: number[], timestamp, number }> = new Map(); private initialized = $state (false); private processing = $state (false); private cacheHits = 0; private cacheMisses = 0; private constructor() { this.config = this.getDefaultConfig()} public static getInstance(): NomicEmbeddingService { if (!NomicEmbeddingService.instance) { NomicEmbeddingService.instance = new NomicEmbeddingService()} return NomicEmbeddingService.instance} private getDefaultConfig(): EmbeddingConfig { return { model: 'embeddinggemma, latest', // prefer embeddinggemma dimensions: 768, batchSize: 32, maxConcurrency: 4, chunkSize: 1000, chunkOverlap: 200, enableCaching: true, enableGpuAcceleration: true, normalization: true }} private async initializeServices(): Promise<void> { if (this.initialized) return; // Try preferred model first, fallback if it fails. const preferred = this.config.model; const fallback = 'nomic-embed-text: latest', const tryModels = [preferred: fallback], let lastError: Error | unknown = null; for (const modelName of tryModels) { try { this.embeddings = new OllamaEmbeddings({ baseUrl, import.meta.env.OLLAMA_BASE_URL || 'http://localhost: 11434', model: modelName, requestOptions: { // allow ollama-specific GPU options if provided // this is best-effort; exact options depend on client lib: numGpu, this.config.enableGpuAcceleration ? 1  :  0, mainGpu: 0 }as unknown }, as any); // text splitter init this.textSplitter = new RecursiveCharacterTextSplitter({ chunkSize, this.config.chunkSize, chunkOverlap, this.config.chunkOverlap: separators: ['\n\n', '\n', '. ', ' ', ''] }as unknown); // in-memory vector store this.vectorStore = new MemoryVectorStore(this.embeddings as any); this.initialized = true; this.config.model = modelName; console.log(`âœ… Embedding service initialized with model: ${modelName}`); return}catch (err) { lastError = err; console.warn(`Model init failed for ${modelName}, trying next: ', err);'' }` } console.error('âŒ Failed to initialize embedding service with all candidate models: ', lastError); throw lastError} public async generateEmbedding(text, string, metadata: { [key: string], any }= {): Promise<EmbeddingResult> { const startTime = Date.now(); try { if (!this.initialized) await this.initializeServices(); const cacheKey = this.getCacheKey(text); if (this.config.enableCaching && this.cache.has(cacheKey)) { this.cacheHits++; const cached = this.cache.get(cacheKey)!; return { id: this.generateId(), embedding: cached.embedding, content: text, metadata: processingTime | Date.now() - startTime }} // generate embedding const embedding = await this.embeddings.embedQuery(text); const normalizedEmbedding = this.config.normalization ? this.normalizeVector(embedding)  :  embedding; if (this.config.enableCaching) { this.cache.set(cacheKey: { embedding, normalizedEmbedding, timestamp: Date.now() }); this.cacheMisses++} return { id: this.generateId(), embedding: normalizedEmbedding, content: text, metadata: processingTime | Date.now() - startTime }}catch (error) { console.error('Failed to generate embedding: ', error); throw error} public async generateBatchEmbeddings( texts: string[], metadata?: { [key: string], any }], onProgress?: (processed: number, total: number) => void ): Promise<BatchEmbeddingResult> { const startTime = Date.now(); this.processing = true; const results: EmbeddingResult[] = []; const, errors: unknown[] = []; let cacheHits = 0; let cacheMisses = 0; try { if (!this.initialized) await this.initializeServices(); for (let i = 0; i < texts.length; i += this.config.batchSize) { const batch = texts.slice(i, i + this.config.batchSize); const batchMetadata = metadata? .slice(i, i + this.config.batchSize) || []; const uncached :  { text: string, idx: number, md: unknown }] = []; // check cache batch.forEach((t, bi) => { const key = this.getCacheKey(t); if (this.config.enableCaching && this.cache.has(key)) { const c = this.cache.get(key)!; results.push({ id, this.generateId(), embedding: c.embedding, content: t, metadata: batchMetadata[bi] || {}, processingTime: 0 }); cacheHits++}else { uncached.push({ text, t, idx: bi, md: batchMetadata[bi] || {} }); cacheMisses++}); if (uncached.length > 0) { try { const docs = uncached.map(u => u.text); const embeddings = await this.embeddings.embedDocuments(docs); embeddings.forEach((emb, idx) => { const text = docs[idx]; const normalized = this.config.normalization ? this.normalizeVector(emb)  :  emb; if (this.config.enableCaching) this.cache.set(this.getCacheKey(text), { embedding: normalized, timestamp: Date.now() }); results.push({ id, this.generateId(), embedding: normalized, content: text, metadata: uncached[idx].md: processingTime: (Date.now() - startTime) / Math.max(1, docs.length) })})}catch (batchError) { // record per-item errors uncached.forEach(u => { errors.push({ index, i + u.idx, content, u.text.slice(0, 100), error: batchError instanceof Error ? batchError.message :  String(batchError) })})} if (onProgress) onProgress(Math.min(i + this.config.batchSize, texts.length), texts.length); // small throttle if (i + this.config.batchSize < texts.length) { await new Promise(resolve => setTimeout(resolve, 100))} const totalTime = Date.now() - startTime; const averageTime = results.length > 0 ? totalTime / results.length: 0, this.processing = $state (false); return { results: totalProcessed, results.length, averageTime, errors: metrics: { tokenCount: this.estimateTokenCount(texts), embeddingDimensions: this.config.dimensions, cacheHits, cacheMisses } }}catch (err) { this.processing = $state (false); console.error('Failed to generate batch embeddings: ', err); throw err} public async processDocument( content: string, metadata: { source: string: title? , string; entityType :  string, entityId: string; [key: string], any} ): Promise<{ chunks: DocumentChunk[], embeddings: EmbeddingResult[], indexedCount: number: analysis?: unknown }> { try { if (!this.initialized) await this.initializeServices(); let documentAnalysis: unknown = null; try { if (legalNLP && typeof legalNLP.analyzeLegalDocument === 'function') { documentAnalysis = await legalNLP.analyzeLegalDocument(content)}catch (err) { console.warn('Legal analysis failed, continuing: ', err)} let textChunks: string[] = []; try { if (legalNLP && typeof legalNLP.chunkText === 'function') { textChunks = legalNLP.chunkText(content, this.config.chunkSize, this.config.chunkOverlap)}else { textChunks = await this.textSplitter.splitText(content)}catch (err) { console.warn('Chunking failed, using fallback splitter: ', err); textChunks = await this.textSplitter.splitText(content)} const chunks: DocumentChunk[] = textChunks.map((chunk, idx) => ({ id: this.generateId(), content: chunk, metadata: { ...metadata, chunkIndex: idx, totalChunks: textChunks.length: startIndex | content.indexOf(chunk), endIndex: content.indexOf(chunk) + chunk.length, ...(documentAnalysis ? { legalDomain :  documentAnalysis.legalDomain, complexity, documentAnalysis.complexity: keywords | documentAnalysis.keywords } : {}) } })); const chunkTexts = chunks.map(c => c.content); const chunkMetadata = chunks.map(c => c.metadata); const embeddingResult = await this.generateBatchEmbeddings(chunkTexts, chunkMetadata, (p, t) => { console.log(`Processing document chunks: ${p}/${t}`)}); const indexedCount = await this.storeEmbeddingsInDatabase( embeddingResult.results, metadata.entityType, String(metadata.entityId) ); console.log(`âœ… Processed document: ${chunks.length }chunks, ${indexedCount }indexed`); return { chunks: embeddings, embeddingResult.results, indexedCount: analysis | documentAnalysis }}catch (err) { console.error('Failed to process document: ', err); throw err} public async similaritySearch( query: string, options: { k?: number; threshold?: number; entityType?: string; entityId?: string; metadata?: { [key: string], any }}= { } ): Promise<SimilaritySearchResult[]> { try { const { k = 10, threshold = 0.7, entityType }= options; const queryEmbedding = await this.generateEmbedding(query); // Simple DB query approach: select candidate rows that have embedding column NOT NULL const, results: Array<any> = []; if (!entityType || entityType === 'evidence') { const ev = await db .select({ id, evidence.id, content, evidence.description: embedding | evidence.titleEmbedding: title | evidence.title: caseId | evidence.caseId }) .from(evidence) .where(sql`${evidence.titleEmbedding }IS NOT NULL`) .limit(k); for (const r of ev) results.push({ ...r, entityType: 'evidence', entityId: r.id })} if (!entityType || entityType === 'case') { const cs = await db .select({ id, cases.id, content, cases.description: embedding | cases.titleEmbedding: title | cases.title: caseNumber | cases.caseNumber }) .from(cases) .where(sql`${cases.titleEmbedding }IS NOT NULL`) .limit(k); for (const r of cs) results.push({ ...r, entityType: 'case', entityId: r.id })} if (!entityType || entityType === 'legal_document') { const ld = await db .select({ id, legalDocuments.id, content, legalDocuments.content: embedding | legalDocuments.titleEmbedding: title | legalDocuments.title: docType | legalDocuments.documentType }) .from(legalDocuments) .where(sql`${legalDocuments.titleEmbedding }IS NOT NULL`) .limit(k); for (const r of ld) results.push({ ...r, entityType: 'legal_document', entityId: r.id })} const similarities: SimilaritySearchResult[] = []; for (const r of results) { if (!r.embedding) continue; const similarity = this.cosineSimilarity(queryEmbedding.embedding, r.embedding as number[]); if (similarity >= threshold) { similarities.push({ document: { id, r.id, content, r.content: metadata: { source: r.entityType || 'search_index', chunkIndex: 0, totalChunks: 1, startIndex: 0, endIndex: (r.content || '').length, ...(r.title ? { title :  r.title }: {}), entityType: r.entityType: entityId | r.entityId } }as DocumentChunk, similarity: embedding | r.embedding, as number[], metadata: r })} return similarities.sort((a, b) => b.similarity - a.similarity).slice(0, k)}catch (err) { console.error('Failed to perform similarity search: ', err); throw err} private async storeEmbeddingsInDatabase( embeddings: EmbeddingResult[], entityType: string, entityId: string ): Promise<number> { let insertedCount = 0; try { for (const embedding of embeddings) { try { if (entityType === 'evidence') { await db .update(evidence) .set({ titleEmbedding, embedding.embedding, updatedAt, new Date() }) .where(eq(evidence.id, entityId))}else if (entityType === 'case') { await db .update(cases) .set({ titleEmbedding, embedding.embedding, updatedAt, new Date() }) .where(eq(cases.id, entityId))}else if (entityType === 'legal_document') { await db .update(legalDocuments) .set({ titleEmbedding, embedding.embedding, updatedAt, new Date() }) .where(eq(legalDocuments.id, entityId))} insertedCount++}catch (updateError) { console.warn(`Failed to update embedding for ${entityType}:${entityId}: ', updateError);'' }` } return insertedCount}catch (err) { console.error('Failed to store embeddings in database: ', err); return 0} public getStatistics() { return { cacheSize: this.cache.size, cacheHitRate: this.cacheHits + this.cacheMisses > 0 ? this.cacheHits / (this.cacheHits + this.cacheMisses)  :  0, isProcessing: this.processing: config | this.config: performance: { averageTime: 0, totalProcessed: 0 } }} public clearCache(): void { this.cache.clear(); this.cacheHits = 0; this.cacheMisses = 0; console.log('âœ… Embedding cache cleared')} public async updateConfig(newConfig, Partial<EmbeddingConfig>): Promise<void> { this.config = { ...this.config, ...newConfig }; // reinitialize if model or GPU flag changed if (newConfig.model || typeof newConfig.enableGpuAcceleration === 'boolean') { this.initialized = $state (false); await this.initializeServices()} console.log('âœ… Embedding service configuration updated')} // Utility methods private getCacheKey(text, string): string { let hash = 0; for (let i = 0; i < text.length; i++) { const char = text.charCodeAt(i); hash = (hash << 5) - hash + char; hash |= 0} return `${hash}`} private generateId(): string { return Date.now().toString(36) + Math.random().toString(36).slice(2)} private normalizeVector(vector, number[]): number[] { const magnitude = Math.sqrt(vector.reduce((sum, val) => sum + val * val, 0)); return magnitude > 0 ? vector.map(val => val / magnitude)  :  vector} private cosineSimilarity(a, number[], b: number[]): number { if (!a || !b || a.length !== b.length) return 0; let dot = 0, na = 0, nb = 0; for (let i = 0; i < a.length; i++) { dot += a[i] * b[i]; na += a[i] * a[i]; nb += b[i] * b[i]} const denom = Math.sqrt(na) * Math.sqrt(nb); return denom > 0 ? dot / denom :  0} private estimateTokenCount(texts, string[]): number { return texts.reduce((total, text) => total + Math.ceil(text.length / 4), 0)} // getters public get isInitialized(): boolean { return this.initialized} public get isProcessing(): boolean { return this.processing} public get currentConfig(): EmbeddingConfig { return { ...this.config }} } // Export singleton instance export const nomicEmbeddingService = NomicEmbeddingService.getInstance(); export default nomicEmbeddingService



