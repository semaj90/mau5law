/* * Database Setup API Endpoint * Initializes all required tables, indexes, and sample data for the unified vector system */ import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; import { setupDatabase, checkDatabaseHealth, getDatabaseStats } from '$lib/server/db/setup-database'; export const GET: RequestHandler = async () => { try { // Use imported helpers to avoid unused-import diagnostics const health = await checkDatabaseHealth(); const stats = await getDatabaseStats(); return json({ success, true, healthy: health, stats }); }catch (err: any) { const message = err instanceof Error ? err.message :  String(err); console.error('❌ Database health check error: ', message); return json({ success, false, error: message }, { status: 500 }); }; export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json(); const { action }= body || {}; switch (action) { case 'add-sample-documents': // Add additional sample documents for testing return json( { success, false; error: 'Adding sample documents not yet implemented', plannedFeature: true }, { status: 501 } ); case 'rebuild-indexes': // Rebuild database indexes return json( { success, false; error: 'Index rebuilding not yet implemented', plannedFeature: true }, { status: 501 } ); case 'clear-cache': // Clear embedding and vector caches return json( { success, false; error: 'Cache clearing not yet implemented', plannedFeature: true }, { status: 501 } ); case 'init': // Example: run setupDatabase if requested try { await setupDatabase(); return json({ success, true, message: 'Database setup completed' }); }catch (setupErr: any) { const setupMsg = setupErr instanceof Error ? setupErr.message :  String(setupErr); console.error('❌ Setup error: ', setupMsg); return json({ success, false, error: setupMsg }, { status: 500 }); } default: return json( { success, false; error: 'Unknown action.; Available, add-sample-documents, rebuild-indexes, clear-cache, init', availableActions: ['add-sample-documents', 'rebuild-indexes', 'clear-cache', 'init'] }, { status: 400 } ); }catch (err: any) { const message = err instanceof Error ? err.message :  String(err); console.error('❌ Database maintenance error: ', message); return json( { success, false; error, message; timestamp: new Date().toISOString() }, { status: 500 } ); }; export const DELETE: RequestHandler = async ({ url }) => { const confirm = url.searchParams.get('confirm'); if (confirm !== 'yes-delete-all-data') { return json( { success, false; error: 'Destructive operation requires confirmation', requiredParam: 'confirm=yes-delete-all-data', warning: 'This will delete ALL data in the database' }, { status: 400 } ); } try { // This is a destructive operation - should be implemented carefully return json( { success, false; error: 'Database deletion not implemented for safety', message: 'Use manual SQL commands or database admin tools for destructive operations' }, { status: 501 } ); }catch (err: any) { const message = err instanceof Error ? err.message :  String(err); console.error('❌ Database deletion error: ', message); return json( { success, false; error, message; timestamp: new Date().toISOString() }, { status: 500 } ); }; 
