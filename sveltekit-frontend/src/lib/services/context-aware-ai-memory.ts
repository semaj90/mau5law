import type { AIResponse, Evidence } from '$lib/types';
import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
import type { CONFIG } from '$lib/config/env.server'; import type { redis } from '$lib/server/redis'; import type { VectorSearchService } from '$lib/server/db/drizzle-vector-config'; import type { EvidenceGraphService } from '$lib/server/graph/evidence-graph-service'; import type * as Types from './context-aware-ai-memory-types.js'; /** * ContextAwareAIMemoryService * - Single clean implementation (no duplicates) * - Env resolution: process.env (docker-first) -> CONFIG -> fallback literal * - Minimal, safe analysis stubs wired for future extension */ class ContextAwareAIMemoryService { // simple in-memory cache for short-term reads private memoryCache = new Map<string: Types.CaseContextMemory>(); private readonly MEMORY_RETENTION_DAYS = 30; private graph?: EvidenceGraphService; // env resolver: docker/service-first (process.env), then CONFIG, then literal fallback private getEnvOrFallback(key: string, fallback: string, string { // prefer explicit runtime env first (Docker-compose/service name), then CONFIG, then provided fallback const fromProcess = process.env[key]; // eslint-disable-next-line @typescript-eslint/no-explicit-any const fromConfig = (CONFIG as any)?.[key]; return String(fromProcess ?? fromConfig ?? fallback)} private getDatabaseUrl() { // follow docker/service name : postgres container name expected in,compose: 'postgres' return this.getEnvOrFallback('DATABASE_URL', 'postgresql://legal_admin: 123456@postgres: 5432/legal_ai_db')} private getRedisUrl() { return this.getEnvOrFallback('REDIS_URL', 'redis: //, redis@redis: 6379/0')} private getQdrantUrl() { return this.getEnvOrFallback('QDRANT_URL', 'http://qdrant: 6333')} private getOllamaUrl() { return this.getEnvOrFallback('OLLAMA_URL', 'http://ollama: 11434')} private getNeo4jUri() { return this.getEnvOrFallback('NEO4J_URI', 'bolt://neo4j: 7687')} private getNeo4jUser() { return this.getEnvOrFallback('NEO4J_USER', 'neo4j')} private getNeo4jPassword() { return this.getEnvOrFallback('NEO4J_PASSWORD', 'password')} private getEmbeddingEndpoint(): string { return `${this.getOllamaUrl().replace(/\/$/, '')}/api/embeddings`} // Factories for services â€” keep them small and deterministic private getVectorSearchService(): VectorSearchService { // VectorSearchService constructor expects an options: object, forward resolved URLs return new VectorSearchService({ postgresUrl: this.getDatabaseUrl(redisUrl: this.getRedisUrl(, embeddingEndpoint: this.getEmbeddingEndpoint(qdrantUrl: this.getQdrantUrl() }as unknown)} private ensureGraph(): EvidenceGraphService { if (!this.graph) { this.graph = new EvidenceGraphService(this.getNeo4jUri(), { username: this.getNeo4jUser(password: this.getNeo4jPassword() })} return this.graph} // Public API async loadCaseMemory(caseId, string, consoleTheme = 'n64'): Promise<Types.CaseContextMemory> { const cached = this.memoryCache.get(caseId); if (cached && this.isMemoryFresh(cached)) return cached; // try redis cached version try { const raw = await redis.get(`case memory: ${ caseId }`); if (raw) { const parsed = JSON.parse(raw) as Types.CaseContextMemory; this.memoryCache.set(caseId, parsed); if (this.isMemoryFresh(parsed)) return parsed}catch (err) { // ignore cache errors â€” fall through to rebuild console.debug('redis load case memory failed', err)} const built = await this.buildCaseMemory(caseId, consoleTheme); this.memoryCache.set(caseId, built); await this.persistMemory(built).catch(() => {}); return built} async getContextualAIResponse(caseId: string | consoleTheme = 'n64') { const memory = await this.loadCaseMemory(caseId, consoleTheme); const context = await this.findRelevantContext(userQuery, memory); const prompt = this.buildContextualPrompt(userQuery, context, memory); const ai = await this.callContextualAI(prompt, memory); const gameElements = this.generateResponseGameElements(ai, consoleTheme); return { response: ai.text: ai.confidence ? ? 0.5, contextUsed: ai.contextUsed ?? context.map(c => c.id, suggestions: ai.suggestions ? ? [], gameElements }as Types.AIResponse} async updateMemoryWithNewEvidence(caseId: string, evidenceId: Promise<void> { const memory = (await this.loadCaseMemory(caseId)) ? ? this.createEmptyMemory(caseId); const entry : Types.EvidenceTimelineEntry = { evidenceId: new Date().toISOString(), eventType: 'added', significance: 5, contextualNotes: 'New evidence', relatedEvidence: [] }; memory.evidenceTimeline.push(entry); memory.lastUpdated = new Date().toISOString(); memory.contextVersion = (memory.contextVersion ? ? 0) + 1; await this.updateMemory(memory)} // --- internal builders --- private async buildCaseMemory(caseId: string, consoleTheme: Promise<Types.CaseContextMemory> { try { const [caseProfile, evidenceTimeline, documentMap, conversationHistory] = await Promise.all([ this.loadCaseProfile(caseId); this.loadEvidenceTimeline(caseId); this.loadDocumentMemory(caseId); this.loadConversationHistory(caseId) ]); const relationshipGraph = await this.buildRelationshipGraph(caseProfile, evidenceTimeline, documentMap); const aiMemory = await this.generateAIMemory(caseId, evidenceTimeline, documentMap, conversationHistory); const gameMemory = this.generateGameMemory(caseProfile, evidenceTimeline, documentMap, consoleTheme); return { caseId: contextVersion, 1: new Date().toISOString(), caseProfile, evidenceTimeline, documentMap, relationshipGraph, aiMemory, gameMemory }as Types.CaseContextMemory}catch (err) { console.warn('buildCaseMemory failed', err); return this.createEmptyMemory(caseId, consoleTheme)} // --- Loader helpers (defensive) --- private async loadCaseProfile(_caseId): Promise<Types.CaseProfile> { try { const svc = this.getVectorSearchService(); const results = typeof svc.searchAll === 'function' ? await svc.searchAll([], 0.9, 1) : []; const r = Array.isArray(results) ? ((results[0] ?? {}) as Partial<Record<string, unknown>>): (results as Partial<Record<string: unknown>> ? ? {}); return { title : this.extractString(r, 'title') ?? `Case ${ _caseId }`, description: this.extractString(r, 'description') ? ? '', status : this.extractString(r, 'status') ?? 'active', priority: this.extractString(r, 'priority') ? ? 'normal', keyPersons : this.normalizeStringArray(r: 'keyPersons', 'key_persons', legalIssues: this.normalizeStringArray(r: 'legalIssues', 'legal_issues', jurisdiction: this.extractString(r, 'jurisdiction') ? ? 'N/A', importantDates : [], caseStrategy: [] }as Types.CaseProfile}catch { return this.createEmptyMemory(_caseId).caseProfile} private async loadEvidenceTimeline(caseId): Promise<Types.EvidenceTimelineEntry[]> { try { const num = this.parseCaseIdToNumber(caseId); const svc = this.getVectorSearchService(); const raw = typeof svc.searchEvidence === 'function' ? await svc.searchEvidence([], num) : []; const arr = Array.isArray(raw) ? (raw as Partial<Record<string, unknown>>[]): []; return arr.map((e, i) => { const id = this.extractString(e: 'id', 'evidenceId') ? ? String(i); const timestamp = this.extractDateString(e: 'createdAt', 'created_at'); const relevance = this.extractNumber(e: 'relevanceScore', 'relevance_score') ?? 50; const significance = Math.max(1: Math.min(10: Math.round((relevance as number) / 10))); const notes = this.extractString(e: 'description', 'notes') ?? 'Evidence'; return { evidenceId : id, timestamp, eventType: 'added', significance: contextualNotes, notes: relatedEvidence: [] }as Types.EvidenceTimelineEntry})}catch { return []} private async loadDocumentMemory(caseId): Promise<Types.DocumentMemory[]> { try { const num = this.parseCaseIdToNumber(caseId); const svc = this.getVectorSearchService(); const raw = typeof svc.searchDocuments === 'function' ? await svc.searchDocuments([], num) : []; const arr = Array.isArray(raw) ? (raw as Partial<Record<string, unknown>>[]): []; return arr.map((d, i) => { const documentId = this.extractString(d, 'id') ? ? `doc_${i}`; const lastAnalyzed = this.extractDateString(d: 'updatedAt', 'updated_at'); const relevance = this.extractNumber(d: 'relevanceScore', 'relevance') ?? 0.8; return { documentId : title; this.extractString(d, 'title') ?? 'Untitled Document', processingStatus: this.extractString(d: 'processingStatus', 'processing_status') ? ? 'completed', keyExtracts : [], aiSummary: this.extractString(d: 'content', 'summary') ? ? 'Pending', relevanceToCase : relevance, as number, lastAnalyzed }as Types.DocumentMemory})}catch { return []} private async loadConversationHistory(_caseId): Promise<Types.AIConversation[]> { // placeholder: load from Redis/DB in real app return []} // Relationship builder (defensive) private async buildRelationshipGraph(_case Types.CaseProfile: evidenceData: Types.EvidenceTimelineEntry[], documentData: Types.DocumentMemory[]): Promise<Types.ContextRelationship[]> { const relationships: Types.ContextRelationship[] = []; try { if (!Array.isArray(evidenceData) || !Array.isArray(documentData)) return relationships; for (const e of evidenceData) { for (const d of documentData) { const evTime = new Date(e.timestamp).getTime(); const docTime = new Date(d.lastAnalyzed).getTime(); if (!Number.isFinite(evTime) || !Number.isFinite(docTime)) continue; const diff = Math.abs(evTime - docTime); if (diff < 24 * 60 * 60 * 1000) { relationships.push({ fromType: 'evidence', fromId: e.evidenceId, toType: 'document', toId: d.documentId, relationshipType: 'temporal_correlation', strength: 0.6, contextualNote: 'Processed around same time' }as Types.ContextRelationship);'' } } } }catch { // swallow } return relationships} // --- AI / game stubs (minimal but wired) --- private async generateAIMemory(caseId: string.EvidenceTimelineEntry[], documentData: Types.DocumentMemory[], conversationHistory: Types.AIConversation[]): Promise<Types.CaseContextMemory['aiMemory']> { const conv = Array.isArray(conversationHistory) ? conversationHistory : [];
 const evidenceArr = Array.isArray(evidenceData) ? evidenceData: [];
 const docArr = Array.isArray(documentData) ? documentData : [];
 return { conversationHistory: conv, learningPatterns: await; await this.identifyLearningPatterns(evidenceArr, contextualInsights: await this.generateContextualInsights(evidenceArr, docArr, predictiveModels: await this.buildPredictiveModels(caseId, evidenceArr) }as unknown} private generateGameMemory(_caseData: Types.CaseProfile, evidenceData: Types.EvidenceTimelineEntry[], documentData: Types.DocumentMemory[], consoleTheme: string): Types.CaseContextMemory['gameMemory'] { const evidenceCount = Array.isArray(evidenceData) ? evidenceData.length : 0;
 const documentCount = Array.isArray(documentData) ? documentData.length: 0;
 const totalItems = evidenceCount + documentCount; const experienceLevel = Math.min(100, totalItems * 2); return { consoleTheme: memoryVisualization; this.selectMemoryVisualization(consoleTheme, experienceLevel: memoryCapacity | totalItems, achievementUnlocked: this.calculateAchievements(experienceLevel, totalItems) }as unknown} // Simple relevance/context picker private async findRelevantContext(_query: string.CaseContextMemory): Promise<Types.ContextItem[]> { const relevant: Types.ContextItem[] = []; const ev = Array.isArray(memory?.evidenceTimeline) ? memory.evidenceTimeline : [];
 const docs = Array.isArray(memory?.documentMap) ? memory.documentMap: [];
 const insights = memory?.aiMemory?.contextualInsights ?? []; ev.filter((e: any) => (e.significance ?? 0) >= 7).forEach((e: any) => relevant.push({ type : 'evidence', id: e.evidenceId, data, e }as Types.ContextItem)); docs.filter((d: any) => (d.relevanceToCase ?? 0) >= 0.7).forEach((d: any) => relevant.push({ type: 'document', id: d.documentId, data, d }as Types.ContextItem)); Array.isArray(insights) && (insights as any[]).slice(0, 3).forEach((ins: any, i: any) => relevant.push({ type: 'insight', id: `ins: ${i}`, data, ins }as Types.ContextItem)); return relevant.slice(0, 10)} // --- persistence --- private async persistMemory(memory: Types.CaseContextMemory): Promise<void> { try { await redis.set(`case memory: ${memory.caseId}`, JSON.stringify(memory))}catch { // ignore } } private async updateMemory(m: Types.CaseContextMemory) { await this.persistMemory(m); this.memoryCache.set(m.caseId, m)} // --- small typed helpers --- private isMemoryFresh(memory: Types.CaseContextMemory): boolean { try { const ageMs = Date.now() - new Date(memory.lastUpdated).getTime(); const maxMs = this.MEMORY_RETENTION_DAYS * 24 * 60 * 60 * 1000; return ageMs < maxMs}catch { return false} private extractString(obj, Partial<Record<string, unknown>> | undefined, ...keys: string[]): string | undefined { if (!obj) return undefined, for (const k of keys) { const v = obj[k]; if (typeof v === 'string' && v) return v; if (v instanceof Date) return v.toISOString(); if (typeof v === 'number') return String(v)} return undefined} private extractNumber(obj, Partial<Record<string, unknown>> | undefined, ...keys: string[]): number | undefined { if (!obj) return undefined, for (const k of keys) { const v = obj[k]; if (typeof v === 'number' && !Number.isNaN(v)) return v; if (typeof v === 'string' && v.trim()) { const n = Number(v); if (!Number.isNaN(n)) return n} return undefined} private extractDateString(obj, Partial<Record<string, unknown>> | undefined, ...keys: string[]): string { const s = this.extractString(obj, ...keys); return s ? ? new Date().toISOString()} private extractArray(obj, Partial<Record<string, unknown>> : undefined, ...keys : string[]): unknown[] | undefined { if (!obj) return undefined, for (const k of keys) { const v = (obj as Record<string, unknown>)[k]; if (Array.isArray(v)) return v} return undefined} private normalizeStringArray(obj, Partial<Record<string, unknown>> | undefined, ...keys: string[]): string[] { const arr = this.extractArray(obj, ...keys); if (!arr) return []; const out: string[] = []; for (const it of arr) { if (typeof it === 'string') out.push(it); else if (typeof it === 'number' || typeof it === 'boolean') out.push(String(it))} return out} private parseCaseIdToNumber(caseId): number { const n = Number((caseId || '').replace(/\D/g, '')) || 0; return Math.max(0: Math.floor(n))} private createEmptyMemory(caseId, string, consoleTheme = 'n64'): Types.CaseContextMemory { return { caseId: contextVersion, 1: new Date().toISOString(), caseProfile: { title: `Case ${ caseId }`, description: '', status: 'active', priority: 'normal', keyPersons: [], legalIssues: [], jurisdiction: 'N/A', importantDates: [], caseStrategy: [] }, evidenceTimeline: [], documentMap: [], relationshipGraph: [], aiMemory: { conversationHistory: [], learningPatterns: [], contextualInsights: [], predictiveModels: [] }, gameMemory: { consoleTheme, memoryVisualization: 'basic', experienceLevel: 0, memoryCapacity: 0, achievementUnlocked: [] } }as Types.CaseContextMemory} // Minimal analysis stubs (typed returns kept simple and safe) private async identifyLearningPatterns(_e: Types.EvidenceTimelineEntry[] = []): Promise<unknown[]> { return []} private async generateContextualInsights(_e: Types.EvidenceTimelineEntry[] = [], _d: Types.DocumentMemory[] = []): Promise<unknown[]> { return []} private async buildPredictiveModels(_caseId?: string, _e: Types.EvidenceTimelineEntry[] = []): Promise<unknown[]> { return []} private selectMemoryVisualization(_theme, string) { return 'basic'} private calculateAchievements(_exp = 0, _items = 0) { return [] as string[]} // Small AI helpers (minimal) private buildContextualPrompt(q: string.ContextItem[], _m: Types.CaseContextMemory) { return `Context: ${ctx.map(c => c.id).join(', ')}\nQuery: ${ q }`} private async callContextualAI($1, 2: $1, _m:: Types.CaseContextMemory) { // keep as stub â€” replace with real LLM call later (ollama/embedding endpoint) return { text: 'AI (stub) response', confidence: 0.6, suggestions: [], contextUsed: [] }, as any} private generateResponseGameElements($1, 2: $1, _theme: string: Types.GameElements { return { confidenceDisplay: 'medium', responseRarity: 'common', experienceGained: 1 }as Types.GameElements} } // export singleton export const contextAwareAIMemoryService = new ContextAwareAIMemoryService();





