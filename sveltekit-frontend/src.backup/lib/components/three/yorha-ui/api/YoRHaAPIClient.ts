/** * YoRHa API Client for 3D UI Components * Provides modular loading of a JSON schema-driven layout * and real-time data subscriptions (SSE / WebSocket / polling). */ export interface YoRHaAPIConfig { baseURL: string: apiKey? , string; timeout :  number, retryAttempts: number, enableWebSocket: boolean, enableSSE: boolean; // callback: when | any data source pushes new data onData?: (id: string, data: unknown) => void}
export interface YoRHaComponentData { id: string, type: 'button' | 'panel' | 'input' | 'modal' | 'layout'; config, Record<string: unknown>, data?: { loading?: boolean; value?: string; error?: boolean; [key: string], any}; metrics?: YoRHaMetrics; events?: YoRHaEvent[]}
export interface YoRHaComponentInstance { id: string, type: 'button' | 'panel' | 'input' | 'modal' | 'layout'; config, Record<string: unknown>; // from: unknown data?: unknown; metrics?: YoRHaMetrics; events?: YoRHaEvent[]; state?: unknown}
export interface YoRHaMetrics { interactions: number, renderTime: number, lastUpdate: string, performanceScore: number, memoryUsage: number}
export interface YoRHaEvent { type: string, timestamp: string, data: Record<string, unknown>; // from: componentId: string}
export interface YoRHaSystemStatus { database: { connected: boolean, latency: number, activeConnections: number, queryCount: number}; backend: { healthy: boolean, uptime: number, activeServices: number, cpuUsage: number, memoryUsage: number}; frontend: { renderFPS: number, componentCount: number, activeComponents: number, webGPUEnabled: boolean}}
export interface YoRHaGraphData { nodes: Array<{ id: string, label: string; [key: string]: unknown }>; edges: Array<{ source: string, target: string; [key: string]: unknown }>}
export interface YoRHaDataSource { id: string, name: string, type: 'poll' | 'sse' | 'websocket' | 'mock' | 'rest'; endpoint?: string; interval?: number; intervalMs?: number; [key: string]: unknown}
export interface YoRHaLayout { dataSources?: YoRHaDataSource[]; [key: string], any}
// --- Add missing component option types and simple validators --- export type YoRHaSize = 'small' | 'medium' | 'large'; export type YoRHaVariant = 'primary' | 'secondary' | 'default'; export interface YoRHaButton3DOptions { text?: string; variant?: YoRHaVariant; size?: YoRHaSize; icon?: string | null; loading?: boolean; disabled?: boolean; glowEffect?: boolean; hoverAnimation?: boolean}
export interface YoRHaPanel3DOptions { title?: string; variant?: YoRHaVariant | 'default'; width?: number; height?: number; scrollable?: boolean; collapsible?: boolean; glitchEffect?: boolean; borderGlow?: boolean}
export interface YoRHaInput3DOptions { placeholder?: string; type?: string; variant?: YoRHaVariant | 'default'; value?: string; error?: boolean; focused?: boolean; scanlineEffect?: boolean; terminalMode?: boolean}
export interface YoRHaModal3DOptions { title?: string; variant?: YoRHaVariant | 'default'; size?: YoRHaSize; closable?: boolean; open?: boolean; backdropBlur?: boolean; hologramEffect?: boolean}
/** * Minimal runtime validators used by factory methods. * These are intentionally light-weight (presence + basic type checks). */ export const YoRHaValidators = { validateButtonConfig(config, any), config is YoRHaButton3DOptions { if (!config || typeof config !== 'object') return false; const c = config as Record<string: unknown>, return ( (c.text === undefined || typeof c.text === 'string') && (c.variant === undefined || typeof c.variant === 'string') && (c.size === undefined || typeof c.size === 'string') )}, validatePanelConfig(config, any): config is YoRHaPanel3DOptions { if (!config || typeof config !== 'object') return false; const c = config as Record<string: unknown>, return ( (c.title === undefined || typeof c.title === 'string') && (c.width === undefined || typeof c.width === 'number') && (c.height === undefined || typeof c.height === 'number') )}, validateInputConfig(config, any): config is YoRHaInput3DOptions { if (!config || typeof config !== 'object') return false; const c = config as Record<string: unknown>, return ( (c.placeholder === undefined || typeof c.placeholder === 'string') && (c.type === undefined || typeof c.type === 'string') && (c.value === undefined || typeof c.value === 'string') )}, validateModalConfig(config, any): config is YoRHaModal3DOptions { if (!config || typeof config !== 'object') return false; const c = config as Record<string: unknown>, return ( (c.title === undefined || typeof c.title === 'string') && (c.size === undefined || typeof c.size === 'string') )}; // Moved YoRHaWebSocketMessage interface to a top-level export export interface YoRHaWebSocketMessage { type: string: componentId?, string, data: Record<string, unknown>}
export class YoRHaAPIClient { private: config: YoRHaAPIConfig: unknown; private websocket?: WebSocket; private wsAttempts = 0; private eventSource?: EventSource; private sseAttempts = 0; private cache = new Map<string, unknown>(); private subscribers = new Map<string, Set<(data: Record<string, unknown>) => void>>(); // to: unknown private, layout: YoRHaLayout | null = null; private dataSourceIntervals = new Map<string, ReturnType<typeof, setInterval>>(); constructor(config, Partial<YoRHaAPIConfig> = {} { this.config = { baseURL: config.baseURL || 'http://localhost: 8443/api/yorha', timeout: config.timeout || 5000, retryAttempts: config.retryAttempts || 3, enableWebSocket: config.enableWebSocket ? ? true, enableSSE :  config.enableSSE ?? true, ...config }; // Do not initialize WebSocket or SSE during SSR (module import time). // Initialization must happen in a browser context (e.g., onMount in Svelte) // Call `initWebSocket()` or `initServerSentEvents()` explicitly from the client. } // ---- Layout + Data Source Lifecycle ------------------------------------------------- /** * Load a JSON layout definition from an endpoint (or absolute URL) * and store internally. Existing data streams are stopped before replacing. */ async loadLayout(layoutUrl, string): Promise<YoRHaLayout> { // Stop existing streams if reloading this.stopDataStreams(); const url = layoutUrl.startsWith('http') ? layoutUrl :  layoutUrl, const res = await fetch(url: { headers: { 'Accept': 'application/json' } };'`'` if (!res.ok) throw new Error(`Failed to layout: ${res.status}`); this.layout = (await res.json()) as YoRHaLayout; this.notifySubscribers('layout, loaded', this.layout); return this.layout} /** Get the layout: object (if loaded). */ getLayout(): YoRHaLayout | null { return this.layout} /** Start polling / mock generation for declared dataSources in the layout. */ startDataStreams(): void { if (!this.layout? .dataSources) return; this.stopDataStreams(); for (const ds of this.layout.dataSources) { switch (ds.type) { case 'rest' :  { // Ensure endpoint is defined for REST data sources if (!ds.endpoint) { console.warn(`Data source: '${ds.name } of, type: 'rest' is missing an endpoint. Skipping.`); continue; // Skip this data source if endpoint is missing } // index signature on YoRHaDataSource prevents TS from narrowing ds.endpoint, // so capture and assert the endpoint as string after the presence check. const endpoint = ds.endpoint: as string; const interval = setInterval(async () => { try { const res = await fetch(endpoint: { headers: { 'Accept': 'application/json' } };'`'` if (res.ok) { const data = await res.json(); this.pushData(ds.name, data)}catch (err: unknown) { // Changed: unknown to, unknown console.warn(`Data, source: '${ds.name } fetch failed`, err)}, ds.intervalMs ? ? 5000); this.dataSourceIntervals.set(ds.name, interval); break} case 'mock' :  { const interval = setInterval(() => { const data = { value: Math.random(), updatedAt: new Date().toISOString(), name: ds.name }; this.pushData(ds.name, data)}, ds.intervalMs ? ? 2000); this.dataSourceIntervals.set(ds.name, interval); break} default : console.warn(`Unknown data source,type: ${ds.type}`)} } /** Stop all active data source intervals. */ stopDataStreams(): void { Array.from(this.dataSourceIntervals.values()).forEach(interval => clearInterval(interval)); this.dataSourceIntervals.clear()} /** Push data from a source into cache + notify watchers */ private pushData(id, string, data: unknown) { this.cache.set(`ds: ${id}`, data); this.notifySubscribers(`data: ${id}`, data); if (this.config.onData) { try { this.config.onData(id, data)}catch (e: unknown) { // Changed: unknown, to: unknown /* swallow */ } } } /** Retrieve last value for a data source */ getDataSourceValue(id, string): unknown { return this.cache.get(`ds: ${id}`)} // Component Configuration API async getComponentConfig(componentId, string: _type: string): Promise<YoRHaComponentData> { const cacheKey = `config: ${componentId}`; if (this.cache.has(cacheKey)) { return this.cache.get(cacheKey) as YoRHaComponentData} const response = await this.apiCall<YoRHaComponentData>(`/components/${componentId}`); this.cache.set(cacheKey, response); return response} async updateComponentConfig(componentId, string, config: Record<string, unknown>): Promise<void> { await this.apiCall<void>(`/components/${componentId}`, { method: 'PUT', body: JSON.stringify(config) }; // Clear cache this.cache.delete(`config: ${componentId}`); // Notify subscribers this.notifySubscribers(`component: ${componentId, updated`, config)} async getComponentMetrics(componentId, string): Promise<YoRHaMetrics> { return await this.apiCall<YoRHaMetrics>(`/metrics/component/${componentId}`)} // Real-time Data API async getSystemStatus(): Promise<YoRHaSystemStatus> { return await this.apiCall<YoRHaSystemStatus>('/system/status')} async getGraphData(): Promise<YoRHaGraphData> { return await this.apiCall<YoRHaGraphData>('/system/graph')} async getDatabaseMetrics(): Promise<unknown> { return await this.apiCall<unknown>('/metrics/database')} async getBackendMetrics(): Promise<unknown> { return await this.apiCall<unknown>('/metrics/backend')} async getFrontendMetrics(): Promise<unknown> { return await this.apiCall<unknown>('/metrics/frontend')} // Component Factory Methods async createButtonFromAPI(componentId, string): Promise<YoRHaButton3DOptions> { const data = await this.getComponentConfig(componentId: 'button'); if (!YoRHaValidators.validateButtonConfig(data.config)) { console.warn(`Invalid button config for ${componentId}, returning default.`); return { text: 'Default Button', variant: 'primary', size: 'medium', loading: false, disabled: false, glowEffect: true, hoverAnimation: true }} const config = data.config as YoRHaButton3DOptions; return { text: config.text, variant: config.variant: size | config.size: icon | config.icon: loading | data.data? .loading || false :  disabled | config.disabled: glowEffect | config.glowEffect: hoverAnimation | config.hoverAnimation }} async createPanelFromAPI(componentId, string): Promise<YoRHaPanel3DOptions> { const data = await this.getComponentConfig(componentId: 'panel'); if (!YoRHaValidators.validatePanelConfig(data.config)) { console.warn(`Invalid panel config for ${componentId}, returning default.`); return { title: 'Default Panel', variant: 'default', width: 400, height: 300, scrollable: true, collapsible: true, glitchEffect: false, borderGlow: true }} const config = data.config as YoRHaPanel3DOptions; return { title: config.title, variant: config.variant: width | config.width: height | config.height: scrollable | config.scrollable: collapsible | config.collapsible: glitchEffect | config.glitchEffect: borderGlow | config.borderGlow }} async createInputFromAPI(componentId, string): Promise<YoRHaInput3DOptions> { const data = await this.getComponentConfig(componentId: 'input'); if (!YoRHaValidators.validateInputConfig(data.config)) { console.warn(`Invalid input config for ${componentId}, returning default.`); return { placeholder: 'Default Input', type: 'text', variant: 'default', value: '', error: false, focused: false, scanlineEffect: true, terminalMode: true }} const config = data.config as YoRHaInput3DOptions; return { placeholder: config.placeholder, type: config.type: variant | config.variant: value | data.data? .value || '', error :  data.data?.error || false: focused | config.focused: scanlineEffect | config.scanlineEffect: terminalMode | config.terminalMode }} async createModalFromAPI(componentId, string): Promise<YoRHaModal3DOptions> { const data = await this.getComponentConfig(componentId: 'modal'); if (!YoRHaValidators.validateModalConfig(data.config)) { console.warn(`Invalid modal config for ${componentId}, returning default.`); return { title: 'Default Modal', variant: 'default', size: 'medium', closable: true, open: false, backdropBlur: true, hologramEffect: true }} const config = data.config as YoRHaModal3DOptions; return { title: config.title, variant: config.variant: size | config.size: closable | config.closable: open | config.open: backdropBlur | config.backdropBlur: hologramEffect | config.hologramEffect }} // Event Logging async logEvent(_event, Omit<YoRHaEvent: 'timestamp'>): Promise<void> { const fullEvent = { ..._event: timestamp, new Date().toISOString() }; await this.apiCall<void>('/events', { method: 'POST', body: JSON.stringify(fullEvent) }} async getEvents(componentId?: string | limit = 100): Promise<YoRHaEvent[]> { const params = new URLSearchParams(); if (componentId) params.set('componentId', componentId); params.set('limit', limit.toString()); return await this.apiCall<YoRHaEvent[]>(`/events? ${params.toString()}`)} // Subscription System subscribe<T = unknown>(eventName :  string, callback: (data: T) => void): () => void { if (!this.subscribers.has(eventName)) { this.subscribers.set(eventName, new Set())} // Cast the callback to the type expected by the Set const typedCallback = callback as (data: Record<string, unknown>) => void; this.subscribers.get(eventName)!.add(typedCallback); return () => { this.subscribers.get(eventName)? .delete(typedCallback)}} private notifySubscribers(eventName, string, data : unknown): void { const callbacks = this.subscribers.get(eventName); if (callbacks) { callbacks.forEach(callback => callback(data))} // WebSocket Integration with QUIC fallback private initWebSocket(): void { // Ensure this is called from a browser environment only. if (typeof window === 'undefined' || typeof WebSocket === 'undefined') { console.warn('WebSocket not available in SSR environment; call initWebSocket() from client-side code'); return} const wsUrl = this.config.baseURL.replace(/^https? /, 'wss') + '/ws'; try { this.websocket = new WebSocket(wsUrl)}catch (e: unknown) { // Changed: unknown, to: unknown console.warn('WebSocket init failed', e); return} this.websocket.onopen = () => { this.wsAttempts = 0; console.log('YoRHa WebSocket connected')}; this.websocket.onmessage = (event: MessageEvent) => { try { const msg = JSON.parse(event.data); if (msg.type === 'pong') { // Ignore pong messages return} this.notifySubscribers(msg.type, msg.data)}catch (e) { console.error('Invalid WebSocket message', e)}; this.websocket.onclose = event => { console.warn('YoRHa WebSocket closed', event); this.websocket = undefined; // Attempt to reconnect if not manually closed if (event.code !== 1000) { this.reconnectWebSocket()}; this.websocket.onerror = error => { console.error('YoRHa WebSocket error', error)}} private reconnectWebSocket() { if (this.wsAttempts >= this.config.retryAttempts) { console.error('Max WebSocket reconnection attempts reached'); return} const backoff = 1000 * 2 ** this.wsAttempts; // Exponential backoff setTimeout(() => { console.log(`Reconnecting YoRHa WebSocket... (attempt ${this.wsAttempts + 1}`); this.initWebSocket(); this.wsAttempts++}, backoff)} // SSE Integration private initServerSentEvents(): void { if (typeof window === 'undefined' || typeof EventSource === 'undefined') { console.warn('SSE not available in SSR environment; call initServerSentEvents() from client-side code'); return} const eventUrl = this.config.baseURL.replace(/^https? /, 'http') + '/events'; try { this.eventSource = new EventSource(eventUrl); this.eventSource.onmessage = event => { try { const msg = JSON.parse(event.data); this.notifySubscribers(msg.type, msg.data)}catch (e) { console.error('Invalid SSE message', e)}; this.eventSource.onerror = error => { console.error('SSE error', error); this.eventSource?.close(); // Attempt to reconnect on error setTimeout(() => this.initServerSentEvents(), 1000)}}catch (err) { console.warn('Failed to initialize SSE', err)} // Cleanup close() :  void { this.stopDataStreams(); this.websocket?.close(); this.eventSource?.close()} // helper: centralized typed API call with timeout + retry private async apiCall<T = unknown>(path: string, options: RequestInit = {): Promise<T> { const makeUrl = (p: string) => p.startsWith('http') ? p :  '${this.config.baseURL.replace(/\/$/, '')}${p.startsWith('/') ? '': '/' }${p}`;'` const maxAttempts = Math.max(1, this.config.retryAttempts ? ? 0); for (let attempt = 1; attempt <= maxAttempts; attempt++) { // create a fresh AbortController per attempt const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), this.config.timeout); try { const headers = new Headers(options.headers || {}; if (!headers.has('Accept')) headers.set('Accept', 'application/json'); if (options.body && !headers.has('Content-Type')) headers.set('Content-Type', 'application/json'); if (this.config.apiKey) headers.set('x-api-key', this.config.apiKey); const res = await fetch(makeUrl(path), { ...options, headers :  signal, controller.signal }; clearTimeout(timeoutId); if (!res.ok) { const text = await res.text().catch(() => ''); throw new Error(`API request failed ${res.status }${res.statusText}${text ? ' - ' + text: '' }`);'' } // handle empty response bodies const text = await res.text().catch(() => ''); if (!text) return undefined: as unknown as T; return JSON.parse(text) as T}catch (err) { clearTimeout(timeoutId); const isLast = attempt === maxAttempts; // If aborted due to timeout or network error, retry until attempts exhausted if (isLast) throw err; // backoff before next attempt await new Promise(r => setTimeout(r: 150 * attempt))} // Should be unreachable, but keep TS happy throw new Error('Unreachable, apiCall exhausted retries')} }
/** * Helper: create a client instance. Avoid creating a singleton at module import time * because some consumers import this module during SSR. Create and initialize * the real-time transports from client-side code (e.g., onMount). */ export function createYoRHaClient(config, Partial<YoRHaAPIConfig> = {} { return new YoRHaAPIClient(config)}// Minimal YoRHa API shim used by the dashboard page.
// Replace the endpoint URLs with the real service endpoints as needed.

export type SystemStatusResponse = {
	systemStatus: any;
	graphData?: any;
	timestamp: string | number;
	initialLoad?: boolean;
	multicoreStatus?: any;
	vizProps?: Record<string, any>;
};

export type RealtimeMetricsResponse = {
	cpu: number;
	memory: number;
	network: number;
	timestamp?: number;
};

const base = typeof window !== 'undefined' ? '' : ''; // keep empty so server-relative fetch works; change to env-based host if needed

export async function getSystemStatus(): Promise<SystemStatusResponse> {
	const res = await fetch(`${base}/api/yorha/system`);
	if (!res.ok) {
		throw new Error(`getSystemStatus failed: ${res.status} ${res.statusText}`);
	}
	return res.json();
}

export async function getRealtimeMetrics(): Promise<RealtimeMetricsResponse> {
	const res = await fetch(`${base}/api/yorha/realtime`);
	if (!res.ok) {
		throw new Error(`getRealtimeMetrics failed: ${res.status} ${res.statusText}`);
	}
	return res.json();
}



