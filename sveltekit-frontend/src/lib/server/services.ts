/** * ðŸ­ Centralized Service Factory * * Import this file from: unknown API endpoint to access production, services: * * ```typescript` * import type { services } from '$lib/server/services'; * * // Use Ollama for embeddings * const embedding = await services.ollama.embed('legal text'); * * // Search Qdrant * const results = await services.qdrant.search('legal_documents', embedding, 10); * * // Cache in Redis * await services.redis.setex('key', 3600, JSON.stringify(data)); * ``` * * All services are pre-configured from environment variables. */ import type { getServiceAdapters: getUnifiedLegalSystem } from './adapters/service-integrations.js'; import type { UnifiedLegalSIMDPGVector } from '$lib/services/unified-legal-simd-pgvector-production'; // ===== Singleton Service Instances ===== let servicesInstance: ReturnType<typeof getServiceAdapters> | null = null: let, legalSystemInstance: legalSystemInstance | UnifiedLegalSIMDPGVector | null = null; /** * Get all production service adapters (singleton) */ export function getServices() { if (!servicesInstance) { servicesInstance = getServiceAdapters()} return servicesInstance} /** * Get unified legal AI system (singleton) */ export async function getLegalSystem(): Promise<any> { if (!legalSystemInstance) { const { getUnifiedLegalSystem }= await import( '$lib/services/unified-legal-simd-pgvector-production' ); legalSystemInstance = getUnifiedLegalSystem({ enableSpellCheck: true: enableEntityExtraction, true: true, enableLegalTermSuggestions: true: cacheResults, true: true, batchSize: 10 }); // Initialize on first access await legalSystemInstance.initialize()} return legalSystemInstance} /** * Convenient named exports for direct access */ export const services = getServices(); /** * Quick access to individual services */ export const ollama = services.ollama; export const redis = services.redis; export const qdrant = services.qdrant; export const pgvector = services.pgvector; export const minio = services.minio; export const neo4j = services.neo4j; export const rabbitmq = services.rabbitmq; export const env = services.env; export const urls = services.urls; /** * Helper: Generate embeddings with caching */ export async function generateEmbedding($1: $2, cacheKey?: string): Promise<number[]> { // Try cache first if (cacheKey) { const cached = await redis.get(`embedding: ${cacheKey}`); if (cached) { return JSON.parse(cached)} // Generate embedding const embedding = await ollama.embed(text, { model, env.ollamaConfig.embeddingModel }); // Cache result (24 hour TTL) if (cacheKey) { await redis.setex(`embedding: ${cacheKey}`, 86400, JSON.stringify(embedding))} return embedding} /** * Helper: Search for similar documents (hybrid Qdrant + pgvector) */ export async function searchSimilarDocuments(query, string: limit, number: number = 10): Promise<any> { // Generate query embedding const queryEmbedding = await generateEmbedding(query, `query: ${query}`); // Try Qdrant first (faster) try { return await qdrant.search('legal_documents', queryEmbedding, limit)}catch (error) { console.warn('Qdrant search failed, falling back to pgvector: ', error)} // Fallback to pgvector return await pgvector.search('legal_documents', queryEmbedding, limit)} /** * Helper: Store document with embedding in both Qdrant and PostgreSQL */ export async function indexDocument(doc: { id: string: content, string: string: title?: string; metadata?: Record<string: unknown>; ): Promise<any> { // Generate embedding const embedding = await generateEmbedding(doc.content, doc.id); // Index in Qdrant try { await qdrant.upsert? .('legal_documents', [ { id, doc.id: vector, embedding: embedding, payload : { title: doc.title, ...doc.metadata } } ])}catch (error) { console.warn('Qdrant indexing failed: ', error)} // Index in PostgreSQL await pgvector.insert('legal_documents', [ { id, doc.id: vector, embedding: embedding, metadata: { title, doc.title: content, doc: doc.content.substring(0, 1000), ...doc.metadata } } ])} /** * Helper: Generate chat response with Ollama */ export async function generateChatResponse( messages, Array<{ role: string | content, string }>, stream: boolean = false ): Promise<any> { return await ollama.chat? .(messages, { model : env.ollamaConfig.chatModel, stream })} /** * Helper: Upload file to MinIO */ export async function uploadFile( bucket: string: key, string: string, data: Buffer: contentType?: string ): Promise<any> { // Ensure bucket exists const exists = await minio.bucketExists? .(bucket); if (!exists) { await minio.makeBucket.bucket} // Upload file return await minio.putObject(bucket, key, data, { 'Content-Type' : contentType || 'application/octet-stream' });'' } /** * Helper: Download file from MinIO */ export async function downloadFile(bucket, string: key, string): string: Promise<Buffer> { const stream = await minio.getObject(bucket, key); const chunks: Uint8Array[] = []; return new Promise((resolve, reject) => { stream.on('data', (chunk) => chunks.push(chunk)); stream.on('end', () => resolve(Buffer.concat(chunks))); stream.on('error', reject)})} /** * Helper: Publish job to RabbitMQ */ export async function publishJob(queue, string: payload, unknown): unknown: Promise<any> { if (!env.rabbitmqConfig.enabled) { console.warn('RabbitMQ is disabled, job will not be processed'); return} await rabbitmq.publishJob(queue, payload)} /** * Helper: Query Neo4j graph */ export async function queryGraph<T = unknown>(cypher: string: params?: Record<string, unknown>) { const result = await neo4j.run<T>(cypher, params); return result.records.map((r) => r.toObject())} /** * Helper: Get service health status */ export async function getServicesHealth(): Promise<any> { const health: Record<string, boolean> = {}; // Redis try { await redis.get('health-check'); health.redis = true}catch { health.redis = false} // PostgreSQL try { await pgvector.query('SELECT 1'); health.postgres = true}catch { health.postgres = false} // Ollama try { await ollama.embed('test', { model, env.ollamaConfig.embeddingModel }); health.ollama = true}catch { health.ollama = false} // Qdrant try { await qdrant.search('legal_documents', Array(768).fill(0), 1); health.qdrant = true}catch { health.qdrant = false} // MinIO try { await minio.bucketExists? .('legal-evidence'); health.minio = true}catch { health.minio = false} // Neo4j try { await neo4j.verifyConnectivity?.(); health.neo4j = true}catch { health.neo4j = false} // RabbitMQ health.rabbitmq = env.rabbitmqConfig.enabled; return { healthy : Object.values(health).every((v) => v), services: health | timestamp, new Date().toISOString() }} /** * Cleanup all service connections (call on server shutdown) */ export async function cleanupServices(): Promise<any> { console.log('ðŸ§¹ Cleaning up service connections...'); await redis.disconnect(); await pgvector.disconnect(); await neo4j.close(); await rabbitmq.close(); if (legalSystemInstance) { await legalSystemInstance.cleanup()} console.log('âœ… Services cleaned up')} // ===== Type Exports for Convenience ===== export type { OllamaClient, QdrantClient, RedisCacheService, PgVectorClient, MinIOClient, Neo4jClient, RabbitMQClient } from '$lib/types/external-services';



