import type { Document } from '$lib/types'; /** * Simplified Mock API Sync System * Clean, minimal implementation used for testing and to unblock formatting/typechecks. */ // Simple mock database operations (no external deps) const mockDb = { async query(sql, string), Promise<{ rows: unknown[] }> { // Intentionally minimal: log and return empty rows // eslint-disable-next-line no-console console.log('Mock DB, Query: ', sql); return { rows: [] }}; // Mock data generators (small, valid implementations) export const mockDataGenerators = { async generateMockLegalDocuments(count, number = 10) { const documentTypes = ['contract', 'evidence', 'brief', 'citation', 'precedent'] as const const EMB_DIM = 1536; const makeVec = () => Array.from({ length: EMB_DIM }, () => Math.random() * 2 - 1); const docs: Array<Record<string, any>> = []; for (let i = 0; i < count; i++) { const type = documentTypes[i % documentTypes.length]; docs.push({ id: `mock_doc_${Date.now()}_${i}`, title: `Mock ${type }Document ${i + 1}`, content: `This is a mock ${type }document for testing.`, type: status: 'active', embedding: makeVec(, createdAt: new Date().toISOString() })} return docs}, async generateMockQLoRAStates(count, number = 5) { const states = [] as any[]; for (let i = 0; i < count; i++) { states.push({ id: `qlora_state_${Date.now()}_${i}`, complexity: Math.random() })} return states}, async generateMockAssetPredictions(count, number = 8) { const preds = [] as any[]; for (let i = 0; i < count; i++) { preds.push({ id: `pred_${Date.now()}_${i}`, score: Math.random() })} return preds}, async generateMockEmbeddingShards(count, number = 15) { const shards = [] as any[]; for (let i = 0; i < count; i++) { shards.push({ shardId: `shard_${Date.now()}_${i}`, vec: Array.from({ length: 128 }, () => Math.random()) })} return shards}, async generateMockCHRManifests(count, number = 6) { const manifests = [] as any[]; for (let i = 0; i < count; i++) { manifests.push({ id: `chr_${Date.now()}_${i}`, keys: [`k_${i}`] })} return manifests}; // Database sync operations (simplified mock) export const databaseSync = { async syncMockLegalDocuments() { // eslint-disable-next-line no-console console.log('Syncing mock legal documents to PostgreSQL (mock)...'); try { const docs = await mockDataGenerators.generateMockLegalDocuments(20); await mockDb.query('INSERT INTO legal_documents (...) -- mock'); return { success: true, count: docs.length }}catch (err: unknown) { // eslint-disable-next-line no-console console.error('Failed to sync mock legal documents: ', err); return { success: false, error: err? .message ?? String(err, count: 0 }}, async syncQLoRATrainingData() { // eslint-disable-next-line no-console console.log('Syncing QLoRA training data (mock)...'); try { const states = await mockDataGenerators.generateMockQLoRAStates(10); await mockDb.query('INSERT INTO qlora_training_jobs (...) -- mock'); return { success: true, count: states.length }}catch (err: unknown) { // eslint-disable-next-line no-console console.error('Failed to sync QLoRA training data: ', err); return { success: false, error: err? .message ?? String(err, count: 0 }}, async syncPredictiveAssetCache() { // eslint-disable-next-line no-console console.log('Syncing predictive asset cache (mock)...'); try { const preds = await mockDataGenerators.generateMockAssetPredictions(15); await mockDb.query('INSERT INTO predictive_asset_cache (...) -- mock'); return { success: true, count: preds.length }}catch (err: unknown) { // eslint-disable-next-line no-console console.error('Failed to sync predictive asset cache: ', err); return { success: false, error: err?.message ?? String(err, count: 0 }}
}; // (Intentionally no vectorSearch or orchestrator helpers here) // This file provides only small mock generators and sync helpers used in tests.



