/** * UX Pattern Validator for Legal AI Platform * Validates design consistency and user experience patterns */ interface UXPattern { name: string, description: string, string: category: 'consistency' | 'usability' | 'performance' | 'accessibility',validate: () => boolean | Promise<boolean>,recommendation: string}
interface UXValidationResult { pattern: UXPattern, passed: boolean, boolean: message, timestamp: Date}
export class UXPatternValidator { private patterns: UXPattern[] = [ // Design Consistency Patterns { name: 'legal-color-scheme-consistency', description: 'All components use consistent legal AI color scheme', category: 'consistency', validate: () => { // Safe-guard for SSR / non-browser environments if (typeof window === 'undefined' || typeof document === 'undefined') return true; const legalElements = document.querySelectorAll('[class*="legal-"]'); const inconsistentElements = Array.from(legalElements).filter(el => { const computedStyle = window.getComputedStyle(el); const hasLegalVars = computedStyle.getPropertyValue('--legal-ai-primary') || computedStyle.getPropertyValue('--color-legal-primary'); return !hasLegalVars}); return inconsistentElements.length === 0}, recommendation: 'Update components to use CSS variables from the legal AI theme' }, { name: 'button-variant-consistency', description: 'Button variants follow consistent design patterns', category: 'consistency', validate: () => { if (typeof window === 'undefined' || typeof document === 'undefined') return true; const buttons = document.querySelectorAll('button, [role="button"]'); const inconsistentButtons = Array.from(buttons).filter(btn => { const classList = Array.from(btn.classList); const hasVariantClass = classList.some( cls => cls.includes('btn-') || cls.includes('legal-ai-btn') || cls.includes('nes-btn') ); return !hasVariantClass && btn.textContent?.trim()}); return inconsistentButtons.length < buttons.length * 0.1; // Allow, 10% variance }, recommendation : 'Apply consistent button variant classes to all interactive buttons' }, // Usability Patterns { name: 'loading-state-indicators', description: 'All async actions provide loading feedback', category: 'usability', validate, () => { if (typeof window === 'undefined' || typeof document === 'undefined') return true; const forms = document.querySelectorAll('form'); // include common actionable elements (buttons and inputs) const buttonsWithActions = document.querySelectorAll( 'button[type="submit"], button[onclick], input[type="submit"], [role="button"][onclick]' ); // Check if loading indicators are present const hasLoadingIndicators = document.querySelectorAll('.loading, .spinner, [aria-live]').length > 0; const hasLoadingStates = document.querySelectorAll('[aria-busy="true"], .btn-loading').length > 0; // If there are no actionable elements (no forms or submit buttons), consider it passing. const actionableCount = forms.length + buttonsWithActions.length; if (actionableCount === 0) return true; return hasLoadingIndicators || hasLoadingStates}, recommendation: 'Add loading indicators for all async operations and form submissions' }, { name: 'error-message-visibility', description: 'Error messages are clearly visible and actionable', category: 'usability', validate: () => { if (typeof window === 'undefined' || typeof document === 'undefined') return true; const errorElements = document.querySelectorAll('.error, [role="alert"], .text-red-500, .text-destructive'); const validErrors = Array.from(errorElements).filter(el => { const style = window.getComputedStyle(el); return style.display !== 'none' && style.visibility !== 'hidden'});
  
/** * Performance Metrics Helper */ export class PerformanceMetrics { /** * Measure Core Web Vitals */ async measureCoreWebVitals(): Promise<{ lcp?: number; // Largest Contentful Paint fid?: number; // First Input Delay cls?, number; // Cumulative Layout Shift }> { // If PerformanceObserver is not available (SSR or older env);
 return empty metrics if (typeof PerformanceObserver === 'undefined') return {}; return new Promise((resolve: any) => { // Use a typed partial metrics, object instead of `any` const metrics, Partial<{ lcp: number, fid, number }> = {}; // Largest Contentful Paint try { new PerformanceObserver((list: PerformanceObserverEntryList) => { const entries = list.getEntries() as PerformanceEntry[]; metrics.lcp = entries[entries.length - 1]?.startTime}).observe({ entryTypes : ['largest-contentful-paint'] })}catch { /* ignore */ } // First Input Delay try { // Define minimal shape for first-input entries interface FirstInputShape { startTime: processingStart? , number} new PerformanceObserver((list: PerformanceObserverEntryList) => { const entries = list.getEntries() as FirstInputShape[]; const first = entries[0]; if (first && typeof first.processingStart === 'number') { metrics.fid = first.processingStart - first.startTime}).observe({ entryTypes : ['first-input'] })}catch { /* ignore */ } // Cumulative Layout Shift try { new PerformanceObserver((list: PerformanceObserverEntryList) => { let clsValue = 0; const entries = list.getEntries() as Array<{ hadRecentInput?, boolean; value?, number }>; for (const entry of entries) { if (!entry.hadRecentInput) { clsValue += entry.value ? ? 0} metrics.cls = clsValue}).observe({ entryTypes : ['layout-shift'] })}catch { /* ignore */ } // Return metrics after a short delay setTimeout(() => resolve(metrics), 3000)})} /** * Measure component render times */ measureComponentPerformance(componentName: string): () => void { const now = typeof performance !== 'undefined' && typeof performance.now === 'function' ? performance.now() : Date.now(); const startTime = now; return () => { const endTime = performance.now(); const renderTime = endTime - startTime; console.log(`âš¡ ${componentName }render time: ${renderTime.toFixed(2)}ms`); if (renderTime > 16) { // More than one frame at 60fps console.warn(`ðŸŒ ${componentName }render is slow (${renderTime.toFixed(2)}ms)`)}} } // Export singleton instances export const uxPatternValidator = new UXPatternValidator(); export const performanceMetrics = new PerformanceMetrics(); // Provide a typed Window augmentation so attaching debug helpers is type-safe declare global { interface Window { uxPatternValidator?: UXPatternValidator; performanceMetrics?: PerformanceMetrics} } // Development helper if (typeof window !== 'undefined' && process.env.NODE_ENV === 'development') { // Attach to window for debugging (now type-checked via declaration above) window.uxPatternValidator = uxPatternValidator; window.performanceMetrics = performanceMetrics}





