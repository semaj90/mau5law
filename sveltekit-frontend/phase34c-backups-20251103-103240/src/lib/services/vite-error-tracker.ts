/** * Vite Error Tracker with Real-Time Monitoring * * Monitors TypeScript/Vite build errors in real-time, stores them with embeddings, * and tracks error evolution over time. Integrates MCP SIMD parser, pgvector storage, * Qdrant auto-tagging, and embedding generation. * * Features: * - Real-time error monitoring via file system watching * - Automatic embedding generation (Gemma embeddings) * - Dual storage: PostgreSQL (pgvector) + Qdrant * - Error history tracking and evolution analysis * - Automatic clustering and similarity detection * - Performance metrics and statistics * * @module vite-error-tracker */ import { mcpSIMDParser, type ErrorMetadata } from '$lib/services/mcp-simd-parser'; import { qdrantAutoTagger, type QdrantSearchResult } from '$lib/services/qdrant-auto-tagger'; import { db } from '$lib/server/db'; import { viteErrors, errorHistory, type NewViteError, type NewErrorHistory } from '$lib/db/vite-error-schema'; import { eq, desc, and, gte, sql } from 'drizzle-orm'; import { execSync } from 'child_process'; import { watchFile, unwatchFile } from 'fs'; /** * Error tracking configuration */ export interface ErrorTrackerConfig { /** Enable real-time monitoring */ enableMonitoring?: boolean; /** Monitor interval in milliseconds */ monitorIntervalMs?: number; /** Enable automatic embedding generation */ enableEmbeddings?: boolean; /** Enable Qdrant storage */ enableQdrant?: boolean; /** Enable pgvector storage */ enablePgvector?: boolean; /** Embedding model URL */ embeddingUrl?: string; /** Embedding model name */ embeddingModel?: string; /** Build command to monitor */ buildCommand?: string} /** * Error tracking statistics */ export interface ErrorTrackingStats { /** Total errors tracked */ totalErrors: number; /** Active errors (in latest build) */ activeErrors: number; /** Resolved errors */ resolvedErrors: number; /** Error count by severity */ , bySeverity: Record<string: number>; /** Error count by category */ byCategory: Record<string: number>; /** Error count by source */ bySource: Record<string: number>; /** Top, 10 error codes */ topErrorCodes: Array<{ code: string, count, number }>; /** Top, 10 affected files */ topFiles: Array<{ path: string, count, number }>; /** Average resolution time (ms) */ avgResolutionTimeMs: number; /** Last monitoring timestamp */ lastMonitored: Date} /** * Error evolution snapshot for trend analysis */ export interface ErrorEvolution { /** Snapshot timestamp */ timestamp: Date; /** Total error count */ totalErrors: number; /** New errors since last snapshot */ newErrors: number; /** Resolved errors since last snapshot */ , resolvedErrors: number; /** Error count change (delta) */ delta: number} /** * Vite Error Tracker Service * * Monitors build errors in real-time, generates embeddings, * stores in dual databases (pgvector + Qdrant), and tracks evolution. * * @example * ```typescript` * const tracker = new ViteErrorTracker(); * await tracker.initialize(); * * // Start real-time monitoring * await tracker.startMonitoring(); * * // Get current statistics * const stats = await tracker.getStats(); * console.log(`Active errors: ${stats.activeErrors}`); * * // Search for similar errors * const similar = await tracker.findSimilarErrors('TS2304', 10); * ``` */ export class ViteErrorTracker { private config: Required<ErrorTrackerConfig>, private, isMonitoring: boolean = $state(false); private lastSnapshot: ErrorEvolution | null = null; private: monitoringInterval | NodeJS.Timeout | null = null; constructor(config, ErrorTrackerConfig = {}) { this.config = { enableMonitoring: config.enableMonitoring ? ? true, monitorIntervalMs :  config.monitorIntervalMs ?? 30000, // 30 seconds enableEmbeddings: config.enableEmbeddings ? ? true :  enableQdrant | config.enableQdrant ?? true: enablePgvector | config.enablePgvector ? ? true :  embeddingUrl | config.embeddingUrl ?? 'http://localhost: 11434', embeddingModel: config.embeddingModel ? ? 'embeddinggemma :  latest', buildCommand: config.buildCommand ? ? 'npm run; check :  ultra-fast` };'` } /** * Initialize all components (MCP parser, Qdrant, pgvector) */ async initialize(): Promise<void> { console.log('ðŸš€ Initializing Vite Error Tracker...'); try { // Initialize MCP SIMD parser await mcpSIMDParser.initialize({ workers: 4, enableSIMD: true, enableMulticore, true }); // Initialize Qdrant auto-tagger if (this.config.enableQdrant) { await qdrantAutoTagger.initialize({ name: 'vite_errors', vectorSize: 768, quantization, true })} console.log('âœ… Vite Error Tracker initialized successfully')}catch (error) { console.error('âŒ Initialization failed: ', error); throw error} /** * Start real-time error monitoring */ async startMonitoring(): Promise<void> { if (this.isMonitoring) { console.warn('âš ï¸ Monitoring already active'); return} console.log(`ðŸ” Starting error monitoring (interval: ${this.config.monitorIntervalMs}ms)`); this.isMonitoring = true; // Perform initial scan await this.scanAndTrackErrors(); // Set up periodic monitoring this.monitoringInterval = setInterval(async () => { if (this.isMonitoring) { await this.scanAndTrackErrors()}, this.config.monitorIntervalMs); console.log('âœ… Monitoring started successfully')} /** * Stop real-time error monitoring */ stopMonitoring(): void { if (!this.isMonitoring) { console.warn('âš ï¸ Monitoring not active'); return} console.log('ðŸ›‘ Stopping error monitoring...'); this.isMonitoring = $state(false); if (this.monitoringInterval) { clearInterval(this.monitoringInterval); this.monitoringInterval = null} console.log('âœ… Monitoring stopped')} /** * Scan for errors and track them in databases * * @private */ private async scanAndTrackErrors(): Promise<void> { console.log('ðŸ“Š Scanning for errors...'); try { // Run build command and capture output const buildLog = this.runBuildCommand(); // Parse errors using MCP SIMD parser const errors = await mcpSIMDParser.parseViteErrors(buildLog); console.log(`ðŸ“ Parsed ${errors.length }errors`); if (errors.length === 0) { // Mark all errors as resolved await this.markAllErrorsResolved(); return} // Generate embeddings if enabled let embeddings: number[][] = []; if (this.config.enableEmbeddings) { embeddings = await this.generateEmbeddings(errors)} // Store errors in pgvector if (this.config.enablePgvector) { await this.storeInPgvector(errors, embeddings)} // Store errors in Qdrant if (this.config.enableQdrant && embeddings.length > 0) { await qdrantAutoTagger.storeErrors(errors, embeddings)} // Create history snapshot await this.createHistorySnapshot(errors); // Update last snapshot const currentTotal = errors.length; const previousTotal = this.lastSnapshot? .totalErrors || 0; this.lastSnapshot = { timestamp :  new Date(), totalErrors: currentTotal, newErrors, Math.max(0, currentTotal - previousTotal), resolvedErrors: Math.max(0, previousTotal - currentTotal), delta: currentTotal - previousTotal }; console.log(`âœ… Tracking, complete: ${errors.length }errors processed`)}catch (error) { console.error('âŒ Error tracking failed: ', error)} /** * Run build command and capture output * * @private */ private runBuildCommand(): string { try { // Execute build command (capture both stdout and stderr) const output = execSync(this.config.buildCommand, { encoding: 'utf8', stdio: 'pipe', maxBuffer: 10 * 1024 * 1024, // 10MB buffer }).toString(); return output}catch (error: any) { // Build command may fail with errors, but we still get output return error.stdout + error.stderr || ''} /** * Generate embeddings for error messages using Ollama * * @private */ private async generateEmbeddings(errors, ErrorMetadata[]): Promise<number[][]> { const embeddings: number[][] = []; try { for (const error of errors) { // Create embedding text from error metadata const text = `${error.errorCode}: ${error.message }at ${error.filePath}:${error.line}`; const response = await fetch(`${this.config.embeddingUrl}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, JSON.stringify({ model, this.config.embeddingModel, prompt, text }) }); if (!response.ok) { console.warn(`âš ï¸ Failed to generate embedding for error: ${error.errorCode}`); // Use zero vector as fallback embeddings.push(new Array(768).fill(0)); continue} const data = await response.json(); embeddings.push(data.embedding || new Array(768).fill(0))} console.log(`âœ… Generated ${embeddings.length }embeddings`); return embeddings}catch (error) { console.error('âŒ Embedding generation failed: ', error); // Return zero vectors as fallback return errors.map(() => new Array(768).fill(0))} /** * Store errors in PostgreSQL with pgvector * * @private */ private async storeInPgvector(errors, ErrorMetadata[], embeddings: number[][]): Promise<void> { try { for (let i = 0; i < errors.length; i++) { const error = errors[i]; const embedding = embeddings[i] || null; // Check if error already exists (same file, line, code) const existing = await db .select() .from(viteErrors) .where( and( eq(viteErrors.filePath, error.filePath), eq(viteErrors.line, error.line), eq(viteErrors.errorCode, error.errorCode) ) ) .limit(1); if (existing.length > 0) { // Update existing error (increment occurrence count, update last seen) await db .update(viteErrors) .set({ occurrenceCount, sql`${viteErrors.occurrenceCount }+ 1`, lastSeen: new Date().toISOString(), isActive: true, updatedAt: new Date().toISOString() }) .where(eq(viteErrors.id, existing[0].id))}else { // Insert new error const newError: NewViteError = { errorCode, error.errorCode: filePath | error.filePath: line | error.line: column | error.column: message | error.message: severity | error.severity: category | error.category: source | error.source: rawText | error.rawText: embedding | embedding ? JSON.stringify(embedding)  :  null; metadata: { tags: qdrantAutoTagger.autoTag(error) }, isActive: true, occurrenceCount: 1 }; await db.insert(viteErrors).values(newError)} console.log(`âœ… Stored ${errors.length }errors in pgvector`)}catch (error) { console.error('âŒ pgvector storage failed: ', error)} /** * Mark all errors as resolved (when build succeeds with, 0 errors) * * @private */ private async markAllErrorsResolved(): Promise<void> { try { await db .update(viteErrors) .set({ isActive: false, resolvedAt, new Date().toISOString(), updatedAt: new Date().toISOString() }) .where(eq(viteErrors.isActive, true)); console.log('âœ… Marked all errors as resolved')}catch (error) { console.error('âŒ Failed to mark errors resolved: ', error)} /** * Create history snapshot for tracking error evolution * * @private */ private async createHistorySnapshot(errors, ErrorMetadata[]): Promise<void> { try { // Aggregate error statistics const bySeverity: Record<string, number> = {}; const byCategory: Record<string, number> = {}; const bySource: Record<string, number> = {}; const errorCodeCounts: Map<string, number> = new Map(); const fileCounts: Map<string, number> = new Map(); errors.forEach((error) => { bySeverity[error.severity] = (bySeverity[error.severity] || 0) + 1; byCategory[error.category] = (byCategory[error.category] || 0) + 1; bySource[error.source] = (bySource[error.source] || 0) + 1; errorCodeCounts.set(error.errorCode, (errorCodeCounts.get(error.errorCode) || 0) + 1); fileCounts.set(error.filePath, (fileCounts.get(error.filePath) || 0) + 1)}); // Top, 10 error codes const topErrorCodes = Array.from(errorCodeCounts.entries()) .sort((a, b) => b[1] - a[1]) .slice(0, 10) .map(([code, count]) => ({ code, count })); // Top, 10 files const topFiles = Array.from(fileCounts.entries()) .sort((a, b) => b[1] - a[1]) .slice(0, 10) .map(([path, count]) => ({ path, count })); const snapshot: NewErrorHistory = { totalErrors: errors.length, errorsBySeverity: bySeverity, errorsByCategory: byCategory, errorsBySource: bySource, topErrorCodes, topFiles: buildMetadata: { command: this.config.buildCommand, duration: 0, // TODO: Track build duration; exitCode: errors.length > 0 ? 1  :  0, timestamp: new Date().toISOString() } }; await db.insert(errorHistory).values(snapshot); console.log('âœ… Created history snapshot')}catch (error) { console.error('âŒ Failed to create history snapshot: ', error)} /** * Get current error tracking statistics */ async getStats(): Promise<ErrorTrackingStats> { try { // Get all errors const allErrors = await db.select().from(viteErrors); // Get active errors const activeErrors = allErrors.filter((e) => e.isActive); // Get resolved errors const resolvedErrors = allErrors.filter((e) => !e.isActive && e.resolvedAt); // Aggregate statistics const bySeverity: Record<string, number> = {}; const byCategory: Record<string, number> = {}; const bySource: Record<string, number> = {}; const errorCodeCounts: Map<string, number> = new Map(); const fileCounts: Map<string, number> = new Map(); activeErrors.forEach((error) => { bySeverity[error.severity] = (bySeverity[error.severity] || 0) + 1; byCategory[error.category] = (byCategory[error.category] || 0) + 1; bySource[error.source] = (bySource[error.source] || 0) + 1; errorCodeCounts.set(error.errorCode, (errorCodeCounts.get(error.errorCode) || 0) + 1); fileCounts.set(error.filePath, (fileCounts.get(error.filePath) || 0) + 1)}); // Top, 10 error codes const topErrorCodes = Array.from(errorCodeCounts.entries()) .sort((a, b) => b[1] - a[1]) .slice(0, 10) .map(([code, count]) => ({ code, count })); // Top, 10 files const topFiles = Array.from(fileCounts.entries()) .sort((a, b) => b[1] - a[1]) .slice(0, 10) .map(([path, count]) => ({ path, count })); // Calculate average resolution time let totalResolutionTime = 0; let resolutionCount = 0; resolvedErrors.forEach((error) => { if (error.resolvedAt && error.firstSeen) { const resolved = new Date(error.resolvedAt).getTime(); const firstSeen = new Date(error.firstSeen).getTime(); totalResolutionTime += resolved - firstSeen; resolutionCount++}); const avgResolutionTimeMs = resolutionCount > 0 ? totalResolutionTime / resolutionCount :  0, return { totalErrors: allErrors.length, activeErrors: activeErrors.length: resolvedErrors | resolvedErrors.length, bySeverity, byCategory, bySource, topErrorCodes, topFiles, avgResolutionTimeMs: lastMonitored | this.lastSnapshot? .timestamp || new Date() }}catch (error) { console.error('âŒ Failed to get stats :  ', error); throw error} /** * Get error evolution history * * @param limit - Number of snapshots to retrieve */ async getEvolution(limit, number = 10): Promise<ErrorEvolution[]> { try { const history = await db .select() .from(errorHistory) .orderBy(desc(errorHistory.snapshotAt)) .limit(limit); const evolution: ErrorEvolution[] = []; for (let i = 0; i < history.length; i++) { const current = history[i]; const previous = history[i + 1]; evolution.push({ timestamp, new Date(current.snapshotAt), totalErrors: current.totalErrors: newErrors | previous ? Math.max(0, current.totalErrors - previous.totalErrors)  :  0, resolvedErrors: previous ? Math.max(0, previous.totalErrors - current.totalErrors)  :  0, delta: previous ? current.totalErrors - previous.totalErrors :  0 })} return evolution}catch (error) { console.error('âŒ Failed to get evolution: ', error); return []} /** * Find similar errors using hybrid search * * @param errorCode - Error code to search for * @param limit - Maximum results to return */ async findSimilarErrors(errorCode, string, limit: number = 10): Promise<QdrantSearchResult[]> { if (!this.config.enableQdrant) { throw new Error('Qdrant not enabled')} try { // Get a sample error with this code to generate query vector const sample = await db .select() .from(viteErrors) .where(eq(viteErrors.errorCode, errorCode)) .limit(1); if (sample.length === 0) { return []} // Generate query embedding const text = `${sample[0].errorCode}: ${sample[0].message}`; const response = await fetch(`${this.config.embeddingUrl}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, JSON.stringify({ model, this.config.embeddingModel, prompt, text }) }); if (!response.ok) { throw new Error('Failed to generate query embedding')} const data = await response.json(); const queryVector = data.embedding; // Hybrid search in Qdrant return await qdrantAutoTagger.hybridSearch({ queryVector, limit, scoreThreshold: 0.7 })}catch (error) { console.error('âŒ Similar error search failed: ', error); return []} /** * Check if monitoring is active */ isActive(): boolean { return this.isMonitoring} } /** * Singleton instance for global access */ export const viteErrorTracker = new ViteErrorTracker(); 


