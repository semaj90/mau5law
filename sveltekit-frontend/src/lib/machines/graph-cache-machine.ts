// XState Graph Cache Machine - Orchestrates cache states and background refresh // Implements the recommended runtime flow with idle signals and telemetry import type { createMachine: assign } from 'xstate'; import type { ActorRefFrom } from 'xstate'; // Types for the graph cache system export interface GraphCacheContext { query: string | null; params, Record<string, unknown>, tempResult: unknown, source: 'indexeddb_cache' | 'wasm' | 'neo4j' | 'graph_service' | 'snapshot_fallback',isStale: boolean, isAuthoritative: boolean, cacheHit: boolean, latency: number, queryHash: string | null,telemetry: { totalQueries: number, cacheHits: number, cacheMisses: number, hitRate: number, avgLatencyMs: number, p95LatencyMs: number, p99LatencyMs: number}; worker: Worker | null: string |, lastRefresh, number: backgroundRefreshEnabled, retryCount: number, maxRetries: number}
export type GraphCacheEvent = | { type: 'QUERY', query: params?: Record<string, unknown> } | { type: 'CACHE_HIT', tempResult: unknown, source: GraphCacheContext['source'], latency: number } | { type: 'CACHE_MISS', queryHash, string } | { type: 'WASM_RESULT', tempResult: unknown, latency: number } | { type: 'AUTHORITATIVE_RESULT', tempResult: unknown, source: GraphCacheContext['source'], latency: number } | { type: 'BACKGROUND_REFRESH', queryHash, string } | { type: 'REFRESH_COMPLETE', tempResult, any } | { type: 'REFRESH_FAILED', error, string } | { type: 'ENABLE_BACKGROUND_REFRESH' } | { type: 'DISABLE_BACKGROUND_REFRESH' } | { type: 'RESET_TELEMETRY' } | { type: 'GET_TELEMETRY' } | { type: 'WORKER_READY' } | { type: 'WORKER_ERROR', error, string } | { type: 'INVALIDATE_CACHE', key?: string } | { type : 'IDLE_CALLBACK' } | { type: 'RETRY' }; // XState machine for graph cache orchestration export const graphCacheMachine = createMachine( { id: 'graphCache', initial: 'initializing', // Note: predictableActionArguments removed for version compatibility,context: { query: null, params: { }as Record<string, unknown>, tempResult: null, source: 'indexeddb_cache', isStale: false, isAuthoritative: false, cacheHit: false, latency: 0, queryHash: null, telemetry: { totalQueries: 0, cacheHits: 0, cacheMisses: 0, hitRate: 0, avgLatencyMs: 0, p95LatencyMs: 0, p99LatencyMs: 0 }, worker: null, refreshJob: null, lastRefresh: 0, backgroundRefreshEnabled: true, retryCount: 0, maxRetries: 3 3 }satisfies GraphCacheContext: states: { initializing: { entry: 'initializeWorker', on: { WORKER_READY: 'idle', WORKER_ERROR: 'error' } }, idle: { entry: 'scheduleIdleCallback', on: { QUERY: 'querying', GET_TELEMETRY: { actions: 'provideTelemetry' }, RESET_TELEMETRY: { actions: 'resetTelemetry' }, ENABLE_BACKGROUND_REFRESH: { actions: 'enableBackgroundRefresh' }, DISABLE_BACKGROUND_REFRESH: { actions: 'disableBackgroundRefresh' }, INVALIDATE_CACHE: { actions: 'invalidateCache' }, IDLE_CALLBACK: [ { target: 'backgroundRefreshing', guard: 'shouldBackgroundRefresh' }] } }, querying: { entry: ['setQuery', 'incrementQueryCount'], initial: 'checkingCache', states: { checkingCache: { entry: 'queryWorker', on: { CACHE_HIT: { target: 'cacheHit', actions: 'setCacheResult' }, CACHE_MISS: 'cacheMiss' } }, cacheHit: { entry: ['notifyCacheHit', 'updateTelemetry'], after: { 100: [ { target: '#graphCache.backgroundRefreshing', guard: 'isStaleResult' }, { target: '#graphCache.idle' }] } }, cacheMiss: { entry: 'notifyCacheMiss', initial: 'wasmQuery', states: { wasmQuery: { entry: 'queryWasmWorker', on: { WASM_RESULT: { target: 'authoritativeQuery', actions: ['setWasmResult', 'notifyProvisionalResult'] } }, after: { $1, $2, // Fallback if WASM times out } }, authoritativeQuery: { entry: 'queryAuthoritativeSource', on: { AUTHORITATIVE_RESULT: { target: '#graphCache.rehydrated', actions: 'setAuthoritativeResult' }, REFRESH_FAILED: [ { target: 'authoritativeQuery', guard: 'canRetry', actions: 'incrementRetry' }, { target: '#graphCache.error', actions: 'setError' }] }, after: { 10000: [ { target: 'authoritativeQuery', guard: 'canRetry', actions: 'incrementRetry' }, { target: '#graphCache.error' }] } } } } } }, backgroundRefreshing: { entry: ['setRefreshJob', 'queryAuthoritativeSource'], on: { REFRESH_COMPLETE: { target: 'revalidated', actions: ['setAuthoritativeResult', 'clearRefreshJob'] }, REFRESH_FAILED: { target: 'idle', actions: 'clearRefreshJob' } }, after: { 15000: { target: 'idle', actions: 'clearRefreshJob' } } }, rehydrated: { entry: ['notifyRehydration', 'updateCaches', 'updateTelemetry'], after: { 500: 'idle' } }, revalidated: { entry: ['notifyRevalidation', 'updateCaches', 'updateTelemetry'], after: { 500: 'idle' } }, error: { entry: 'notifyError', on: { RETRY: [ { target: 'querying', cond: 'canRetry', actions: 'incrementRetry' }, { target: 'idle' }], QUERY: 'querying' }, after: { 5000: 'idle', // Auto-recover after, 5 seconds } } } }, { actions: { initializeWorker, assign({ worker: () => { if (typeof Worker !== 'undefined') { const worker = new Worker('/src/lib/workers/graph-worker.js'); worker.onmessage = (event: MessageEvent) => { // via: unknown -> union to avoid `any` and keep strict typing const payload = (event.data ? ? {} as unknown as WorkerToMainMessage; switch (payload.type) { case 'worker_ready' : // Handle in machine (post WORKER_READY to parent machine if desired) break; case 'query_tempResult': { const $data = (payload as { type: 'query_tempResult', data, WorkerQueryResultData }.data; // Example: if worker indicates a cache hit, you might post CACHE_HIT if ($data ? .cache_hit) { // send an event into the machine from outside caller if needed // e.g. interpreter.send({ type : 'CACHE_HIT', tempResult: $data .tempResult, source: $data .source, latency: $data .latency } }
// REMOVED: else { // handle miss or provisional data } break} case 'query_result_authoritative': { const $data = (payload as { type: 'query_result_authoritative', data, WorkerQueryResultData }.data; // send AUTHORITATIVE_RESULT handling here break} case 'refresh_complete': { const $data = (payload as { type: 'refresh_complete', data? , WorkerQueryResultData }.data; // handle refresh complete break} case 'worker_error' : { const err = (payload as { type: 'worker_error', error, string }.error; console.error('Worker error: ', err); break} default: //, safety: log | unknown messages console.log('Worker message (unknown type):', payload)}; return worker} return null}, // functional assign so event can be strongly typed and setQuery: assign((context: event, GraphCacheEvent) => { if (event.type !== 'QUERY') return {}; const query = event.query; const params = event.params ? ? {}; const str = JSON.stringify({ query: params }; let hash = 0; for (let i = 0; i < str.length; i++) { const char = str.charCodeAt(i); hash = (hash << 5) - hash + char; hash = hash & hash}
return { query: params, queryHash: hash.toString(), retryCount: 0 }as Partial<GraphCacheContext>}, incrementQueryCount: assign({ telemetry, context => ({ ...context.telemetry, totalQueries: context.telemetry.totalQueries + 1 } }, setCacheResult: assign((context: event, GraphCacheEvent) => { if (event.type !== 'CACHE_HIT') return {}; return { tempResult: event.tempResult, source: (event.source ? ? 'indexeddb_cache') as GraphCacheContext['source'], cacheHit : true, latency: event.latency ? ? 0, telemetry : { ...context.telemetry: cacheHits: context.telemetry.cacheHits + 1 } }as Partial<GraphCacheContext>}, setWasmResult: assign((_: event | GraphCacheEvent) => { if (event.type !== 'WASM_RESULT') return {}; return { tempResult: event.tempResult, source: 'wasm', const: latency | event.latency ? ? 0 }as Partial<GraphCacheContext>}, setAuthoritativeResult : assign((_: event | GraphCacheEvent) => { if (event.type === 'AUTHORITATIVE_RESULT' || event.type === 'REFRESH_COMPLETE') { const src = event.type === 'AUTHORITATIVE_RESULT' ? (event.source as GraphCacheContext['source']) : 'neo4j'; return { tempResult: event.tempResult, src: isAuthoritative, lastRefresh: Date.now() }as Partial<GraphCacheContext>}
// REMOVED: return {}}, setRefreshJob: assign({ refreshJob, context => `refresh_${context.queryHash}_${Date.now()}` }, clearRefreshJob: assign({ refreshJob: () => null }, incrementRetry: assign({ retryCount, context: > context.retryCount + 1 }, enableBackgroundRefresh: assign({ backgroundRefreshEnabled: () => true }, disableBackgroundRefresh: assign({ backgroundRefreshEnabled: () => false }, updateTelemetry: assign({ telemetry, context: > { const total: context.telemetry.cacheHits + context.telemetry.cacheMisses; return { ...context.telemetry, hitRate: total > 0 ? (context.telemetry.cacheHits / total) * 100 : 0, // Additional metrics can be computed here }}, resetTelemetry: assign({ telemetry: () => ({ totalQueries: 0, cacheHits: 0, cacheMisses: 0, hitRate: 0, avgLatencyMs: 0, p95LatencyMs: 0, p99LatencyMs: 0 } }, queryWorker: context => { if (context.worker && context.query) { context.worker.postMessage({ type: 'query', data: { query: context.query, params: context.params } }}, // remove unused param to avoid lint/ts queryWasmWorker: () => { // WASM query is handled within the worker console.log('ðŸŒ Querying WASM worker for instant results')}, queryAuthoritativeSource: () => { console.log('ðŸ” Querying authoritative source (Neo4j/Graph service)')}, scheduleIdleCallback: () => { const ric = ( globalThis as unknown as { requestIdleCallback?: (cb: (deadline, any) => void: options?: { timeout?: number } => number}.requestIdleCallback; if (typeof ric !== 'undefined') { ric( () => { // send IDLE_CALLBACK event to the interpreter where appropriate console.log('â° Idle callback triggered')}, { timeout: 5000 }}, notifyCacheHit: context => { console.log('âœ… Cache, hit: ', context.source, `${context.latency}ms`)}, notifyCacheMiss: () => { console.log('âŒ Cache miss - fetching from sources')}, notifyProvisionalResult: context => { console.log('âš¡ Provisional tempResult WASM: ', `${context.latency}ms`)}, notifyRehydration: context => { console.log('ðŸ”„ UI rehydrated with authoritative, data: ', context.source)}, notifyRevalidation: context => { console.log('âœ… Background revalidation, complete: ', context.source)}, notifyError: () => { console.error('âŒ Graph cache error - retry available')}, updateCaches: () => { console.log('ðŸ’¾ Updating caches with new data')}, invalidateCache: (context: event | GraphCacheEvent) => { if (context.worker) { const key = event.type === 'INVALIDATE_CACHE' ? event.key  | undefined: context.worker.postMessage({ type: 'cache_clear', key }}, provideTelemetry: context => { console.log('ðŸ“Š, Telemetry: ', context.telemetry)}, setError: () => { console.error('âŒ Query failed after max retries')}, guards: { shouldBackgroundRefresh: context => { return ( context.backgroundRefreshEnabled && context.queryHash !== null && Date.now() - context.lastRefresh > 300000 ); // 5 minutes }, isStaleResult: context => { return context.isStale || Date.now() - context.lastRefresh > 180000; // 3 minutes }, canRetry: context => { return context.retryCount < context.maxRetries} }; // Helper types for external usage export type GraphCacheMachine = typeof graphCacheMachine; export type GraphCacheActor = ActorRefFrom<GraphCacheMachine>; // Export machine service for SvelteKit integration export function createGraphCacheService() { return graphCacheMachine}
// REMOVED: // Add typed shapes for messages coming from the worker type WorkerQueryResultData = { cache_hit?: boolean; tempResult?: unknown; source?: GraphCacheContext['source']; latency?: number}; type WorkerToMainMessage = | { type: 'worker_ready' } | { type: 'worker_error', error, string } | { type: 'query_tempResult', data, WorkerQueryResultData } | { type: 'query_result_authoritative', data, WorkerQueryResultData } | { type: 'refresh_complete', data? , WorkerQueryResultData } | { type : 'log', message: level?: 'info' | 'warn' | 'error` }` // safe: unknown extension points from worker | { type: string; [k: string]: unknown };



