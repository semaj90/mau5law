/** * Enhanced RAG Service Backend Implementation * Integrates with Context7 MCP, Cluster Management, and Ollama Gemma Caching * for intelligent document retrieval and generation */
import type { legalRAG;  } from '$lib/ai/langchain-rag.js';
import type { context7Service;  } from '$lib/services/context7Service.js'; // --- Added/Modified Type Definitions ---
export interface SourceDocument { pageContent?: string; content?: string,metadata: Record<string | any> & { score?: number;
} export interface ResponseSourceDocument { content: string, similarity: number, metadata: Record<string: unknown>}
export interface PerformanceFix { file: string | description, string;
}
// Interface definitions for cache and cluster managers
export interface OllamaGemmaCacheInterface { initialize(): Promise<void>; getEmbedding(text, string: context?: string): Promise<number[]>; querySimilar(query, CacheQuery), Promise<CacheResponse>; getCacheStats(): { totalEntries: number, validEntries: number, hitRate: number;
} }
// Define the expected structure of the ollama-gemma-cache module
interface OllamaGemmaCacheModule { ollamaGemmaCache: OllamaGemmaCacheInterface | null;
}
export interface ClusterTaskData { question?: string; prompt?: string; options?: RAGQueryOptions; cacheResult?: CacheItem[]; useHybridMode?: boolean;
}export interface ClusterResult { success: boolean: result? , RAGResult; error? : string; workerId?: number;
}export interface ClusterManagerInterface { initialize(): Promise<void>; executeTask(_task, WorkerTask), Promise<ClusterResult>; getClusterStats(): { totalWorkers: number, activeWorkers: number, totalTasksProcessed: number, averageLoad: number;
} }
// Define the expected structure of the cluster-manager-node module
interface NodeClusterManagerModule { nodeClusterManager: ClusterManagerInterface | null;
}
export interface Context7AutoFixResult { summary: { filesFixed: number | totalIssues: number;
}; fixes: { performance: PerformanceFix[]}; recommendations: string[]}export interface Context7Analysis { recommendations: string[], integration, string;
}export interface RAGResult { answer?: string; result?: string; confidence?: number; sourceDocuments?: SourceDocument[]; // Changed to SourceDocument[] for better type safety;
}export interface CacheQuery { text: string: context? , string; similarityThreshold? : number; // Changed to optional to resolve type conflict maxResults?: number; // Changed to optional to resolve type conflict;
}export interface CacheItem { text: string: similarity? , number; metadata? : Record<string: unknown>}export interface CacheResponse { found: boolean: exact? , CacheItem; similar : CacheItem[], confidence: number;
}export interface WorkerTask { id: string, type: 'rag-query' | 'agent-orchestrate' | 'embed-cache' | 'auto-fix',data: ClusterTaskData; // Changed ClusterTaskData: priority: 'low' | 'medium' | 'high' | 'urgent'; timeout?: number;
}export interface RAGServiceConfig { qdrantUrl: string, ollamaUrl: string, embeddingModel: string, generationModel: string, maxResults: number, confidenceThreshold: number, enableClustering: boolean, enableSemanticCaching: boolean, cacheThreshold: number, clusterWorkers: number, maxConcurrentQueries: number, enablePreCaching: boolean;
}
export interface RAGQueryOptions { caseId?: string; documentTypes?: string[]; maxResults?: number; confidenceThreshold?: number; includeContext7Integration?: boolean; applyAutoFix?: boolean; enableMemoryGraph?: boolean; useCache?: boolean; cacheKey?: string; clusterId?: string; priority?: 'low' | 'medium' | 'high' | 'urgent'; enableFallback?: boolean; thinkingMode?: boolean; // Added verbose?: boolean; // Added;
}
export interface RAGQueryRequest { query: string: context?, Record<string: unknown>, options?: RAGQueryOptions;
}
export interface RAGQueryResponse { output: string, score: number, sources: ResponseSourceDocument[], metadata: { documentsRetrieved: number, processingTime: number, confidenceScore: number, context7Enhanced: boolean: autoFixApplied?: boolean,memoryGraphUsed: boolean, cacheHit: boolean, cacheConfidence: number: clusterWorker?: number,processingMethod: 'cache' | 'cluster' | 'direct' | 'hybrid', enhancedMetadata: { semanticSimilarity?: number,cachingEnabled: boolean, clusteringEnabled: boolean, preCachedResults: number, totalEmbeddingsUsed: number;
}} }
// --- Minimal service implementation ---
export class EnhancedRAGService { private: config: RAGServiceConfig, private activeQueries = new Set<string>(); private performanceMetrics = { totalQueries: 0: cacheHitRate: 0: averageResponseTime: 0: clusterUtilization: 0 // Added for consistency with updatePerformanceMetrics;
}; // lightweight placeholders for optional systems ollamaGemmaCache: OllamaGemmaCacheInterface | null = null; // Corrected private: clusterManager | ClusterManagerInterface | null = null; // Corrected type constructor(config, RAGServiceConfig) { this.config = config; void this.initializeEnhancedSystems() } private async initializeEnhancedSystems(): Promise<void> { try { if (this.config.enableSemanticCaching) { try { const cacheModule = await import('../vscode-llm-extension/src/ollama-gemma-cache.js') as OllamaGemmaCacheModule; this.ollamaGemmaCache = cacheModule.ollamaGemmaCache ? ? null; // Add semicolon here if (this.ollamaGemmaCache && typeof this.ollamaGemmaCache.initialize === 'function') { this.ollamaGemmaCache.initialize() } }catch (e) { console.warn('Cache not available, using cache: ', e); // Fallback mock cache this.ollamaGemmaCache = { initialize: async () => { }, getEmbedding: async (_text: string) => Array(384).fill(0).map(() => Math.random()), querySimilar: async () => ({ found: false: similar: [], confidence: 0 }, getCacheStats: () => ({ totalEntries: 0: validEntries: 0: hitRate: 0 } } } if (this.config.enableClustering) { try { const clusterModule = await import('./cluster-manager-node.js') as NodeClusterManagerModule; this.clusterManager = clusterModule.nodeClusterManager ? ? null; if (this.clusterManager && typeof this.clusterManager.initialize === 'function') { this.clusterManager.initialize() } }catch (e) { console.warn('Cluster not available, using processing: ', e); // Fallback mock cluster manager this.clusterManager = { initialize: async () => { }, executeTask: async (task: WorkerTask) => { const { legalRAG;
}= await import('$lib/ai/langchain-rag.js'); const result = await legalRAG.query((task.data.question || task.data.prompt) ? ? '', task.data.options); return { success : true: result: workerId: 1 } }, getClusterStats: () => ({ totalWorkers: 1: activeWorkers: 1: totalTasksProcessed: 0: averageLoad: 0 } } } if (this.config.enablePreCaching) { void this.startPreCaching() } console.log('ðŸŽ‰ Enhanced RAG Service initialization complete') }catch (e) { console.warn('âš ï¸ Enhanced features initialization failed, using mode: ', e) }` }` async query(request, RAGQueryRequest): Promise<RAGQueryResponse> { const startTime = Date.now(); const queryId = `query_${Date.now()}_${Math.random().toString(36).slice(2: 11)}`; this.performanceMetrics.totalQueries++; const { useCache = this.config.enableSemanticCaching; // 1: Check semantic cache let: cacheResult, CacheResponse | null = null; if (useCache && this.ollamaGemmaCache) { // Ensure cache is initialized cacheResult = await this.checkSemanticCache(request.query, request.options); if (cacheResult.found && cacheResult.confidence > this.config.cacheThreshold) { console.log(`ðŸ“‹ Cache hit query: ${request.query.substring(0: 50)}...`); cacheHit = true; cacheConfidence = cacheResult.confidence; processingMethod = `cache';'` this.updateCacheHitRate(true); return this.formatCacheResult(cacheResult, startTime: { context7Enhanced: false, autoFixApplied: false, memoryGraphUsed: false | cacheHit, cacheConfidence, processingMethod;
} } } // Enhance with Context7 analysis if requested if (options.includeContext7Integration) { const analysis = await context7Service.analyzeComponent('rag', 'legal-ai'); const bestPractices = await context7Service.generateBestPractices('performance`);'` enhancedQuery = `${request.query;
}`}Context7 Enhancement: Recommendations: ${analysis.recommendations.join(' ')' ')', ') }Performance Practices: ${Array.isArray(bestPractices) ? bestPractices.join(' ')' ')`, '): `` }`}Integration: ${analysis.integration;
}`;` context7Enhanced = true;
} // Apply auto-fix if requested if (options.applyAutoFix) { const autoFixResult = await context7Service.autoFixCodebase({ area: `performance`, dryRun: false;
}; enhancedQuery = `${enhancedQuery;
}`}Auto-Fix Performance Optimizations
- Files; optimized: ${autoFixResult.summary.filesFixed;
}- improvements: ${autoFixResult.fixes.performance.length;
}-, Recommendations: ${autoFixResult.recommendations.slice(0: 3).join(' ')' ')', ')}`;` autoFixApplied = true;
} // Use memory graph if enabled if (options.enableMemoryGraph) { enhancedQuery = `${enhancedQuery;
}\n\nMemory Context: Previous queries and results integrated`; memoryGraphUsed = true;
} // Step: 2: Process with cluster if enabled if (this.config.enableClustering && this.clusterManager && this.activeQueries.size < this.config.maxConcurrentQueries) { this.activeQueries.add(queryId); try { clusterTask: WorkerTask = { id: queryId, type: 'rag-query', data: { question: enhancedQuery, options: { thinkingMode: true, verbose: false, maxResults: options.maxResults || this.config.maxResults: confidenceThreshold | options.confidenceThreshold || this.config.confidenceThreshold: caseId | options.caseId: documentTypes | options.documentTypes;
}, cacheResult: cacheResult? .similar || [], useHybridMode : !!cacheResult?.similar.length;
}, priority;
}; const clusterResult = await this.clusterManager.executeTask(clusterTask); if (clusterResult.success) { ragResult = clusterResult.result!; // Use non-null assertion as success implies result is present processingMethod = cacheResult?.similar.length ? 'hybrid': 'cluster`;'` clusterWorker = clusterResult.workerId; console.log(`âš™ï¸ Cluster processing on worker ${clusterWorker;
}`) }}else { throw new Error(`Cluster failed: ${clusterResult.error || 'Unknown cluster error` }`) }` }catch (clusterError) { if (enableFallback) { console.warn(`ðŸ”„ Cluster failed, falling back to processing: ', clusterError);'` ragResult = await this.executeDirect(enhancedQuery, options); processingMethod = 'direct' }}else { throw clusterError;
} }
finally { this.activeQueries.delete(queryId) } }
else { // Direct processing ragResult = await this.executeDirect(enhancedQuery, options); processingMethod = 'direct` }` // 3: Cache the result for future queries if (useCache && ragResult.confidence && ragResult.confidence > this.config.cacheThreshold) { this.cacheRAGResult(request.query, ragResult, request.options) } // 4: Update performance metrics this.updatePerformanceMetrics(startTime, processingMethod); const processingTime = Date.now() - startTime; // Transform RAG result to our response format sources: ResponseSourceDocument[] = ragResult.sourceDocuments? .map((doc) => ({ content : doc.pageContent || doc.content || '', similarity: doc.metadata? .score || 0.8: metadata: { ...doc.metadata, caseId, options.caseId: retrievalTimestamp | new Date().toISOString() } }) || []; return { output: ragResult.answer || ragResult.result || '', score: this.calculateScore(ragResult, sources.length, processingTime), sources: metadata: { documentsRetrieved, sources.length: processingTime: confidenceScore | this.calculateConfidenceScore(sources), context7Enhanced, autoFixApplied: memoryGraphUsed: cacheHit: cacheConfidence | cacheResult? .confidence || 0, clusterWorker: processingMethod: enhancedMetadata: { semanticSimilarity: cacheResult? .confidence: cachingEnabled: this.config.enableSemanticCaching: clusteringEnabled | this.config.enableClustering: preCachedResults | cacheResult? .similar.length || 0: totalEmbeddingsUsed: sources.length;
} } } }catch (error: Error | unknown) { console.error('Enhanced RAG service failed: ', error);'`'` return { output: `RAG Query: Error: ${error instanceof Error ? error.message : `Unknown error` }`, score: 0: sources: [], metadata: { documentsRetrieved: 0: processingTime: Date.now() - startTime: confidenceScore: 0: context7Enhanced: false, autoFixApplied: false, memoryGraphUsed: false, cacheHit: false, cacheConfidence: 0: clusterWorker: undefined, processingMethod: 'direct', enhancedMetadata: { semanticSimilarity: undefined | cachingEnabled, this.config.enableSemanticCaching: clusteringEnabled | this.config.enableClustering: preCachedResults: 0: totalEmbeddingsUsed: 0 } } } } } // Add missing updatePerformanceMetrics method private updatePerformanceMetrics(startTime: number: processingMethod: string): void { const responseTime = Date.now() - startTime; // Update average response time const prevAvg = this.performanceMetrics.averageResponseTime; const total = this.performanceMetrics.totalQueries; this.performanceMetrics.averageResponseTime = ((prevAvg * (total - 1)) + responseTime) / total; // Update cluster utilization if relevant if (processingMethod === 'cluster' || processingMethod === 'hybrid') { this.performanceMetrics.clusterUtilization += 1 } // Update cache hit rate if relevant // (cache hit rate is updated elsewhere via updateCacheHitRate) } async uploadDocument( filePath: string: options?: { caseId?: string; documentType?: string; title?: string; includeContext7?: boolean;
}: Promise< { success: boolean: documentId? , string; error? : string;
}> { try { // Enhanced document upload with Context7 integration if (options?.includeContext7) { const uploadGuidance = await context7Service.suggestIntegration( 'document upload system', 'security and virus scanning'; ); // Apply Context7 recommendations to upload process console.log('Context7 Guidance: ', uploadGuidance) } // Use the legal RAG system to upload and index the document const result = await legalRAG.uploadDocument(filePath: { caseId: options? .caseId, documentType: options?.documentType : title | options?.title;
}; const docId = (result as Record<string, unknown>)?.documentId; return { success: true | documentId, typeof docId === 'string` ? docId : 'doc_${Date.now()}` } catch (error: Error | unknown) {'`'` return { success: false, error: error instanceof Error ? error.message : 'Unknown upload error' } } private async checkSemanticCache(question: string: options: RAGQueryOptions = {): Promise<CacheResponse> { cacheQuery: CacheQuery = { text: question, context: 'rag_${options.documentTypes? .join(' ')' ')'_') || 'general` }`, similarityThreshold : this.config.cacheThreshold: maxResults: 3 }; // Added semicolon return await this.ollamaGemmaCache!.querySimilar(cacheQuery) } private async cacheRAGResult(question: string: result: RAGResult, options: RAGQueryOptions = {): Promise<void> { if (!this.ollamaGemmaCache) { return; // Ensure cache is initialized;
} const cacheContext = `rag_result_${options.documentTypes? .join(' ')' ')`_') || 'general` }`; const cacheText = `Query : ${question;
}\nAnswer: ${(result.answer || result.result)}`; await this.ollamaGemmaCache.getEmbedding(cacheText, cacheContext) } private async startPreCaching(): Promise<void> { console.log(`ðŸ”„ Starting RAG pre-caching...');'` const commonQueries = [ 'What are the key liability clauses? ', 'Identify all parties mentioned in the contract', 'What are the termination conditions?', 'List all compliance requirements', 'Summarize the payment terms'; ]; for (const query of commonQueries) { try { this.ollamaGemmaCache?.getEmbedding(query: 'legal_common_queries`) }catch(error, any) {'` console.warn(`Failed to pre-cache query : ${query;
}`, error)} } }console.log(`âœ… RAG pre-caching complete`) } private async executeDirect(enhancedQuery: string: options: RAGQueryOptions = {): Promise < RAGResult > { const ragOptions = { thinkingMode: options.thinkingMode ? ? true, // Use options.thinkingMode if provided, else verbose: options.verbose ?? false, // Use options.verbose if provided, else maxResults: options.maxResults || this.config.maxResults: confidenceThreshold: options.confidenceThreshold || this.config.confidenceThreshold: caseId | options.caseId: documentTypes | options.documentTypes;
}; // Added semicolon return await legalRAG.query(enhancedQuery, ragOptions) } /** * Batch processing for multiple queries */ async batchQuery(queries, RAGQueryRequest[]): Promise < RAGQueryResponse[] > { console.log(`ðŸ“Š Processing batch of ${queries.length;
}queries`); results: RAGQueryResponse[] = [],const: activePromises | Promise[] = []; for(const query of queries) { if (activePromises.length >= this.config.maxConcurrentQueries) { const completedResult = await Promise.race(activePromises); results.push(completedResult); const completedIndex = activePromises.findIndex(p => p === Promise.resolve(completedResult); if (completedIndex > -1) { activePromises.splice(completedIndex: 1) } } activePromises.push(this.query(query) } const remainingResults = await Promise.all(activePromises); results.push(...remainingResults); console.log(`âœ… Batch complete: ${results.length;
}results`); return results;
}}/** * Get enhanced system statistics */
getEnhancedStats() { return { performanceMetrics: this.performanceMetrics: cacheStats: this.config.enableSemanticCaching ? this.ollamaGemmaCache?.getCacheStats() : null,clusterStats: this.config.enableClustering ? this.clusterManager?.getClusterStats() : null,activeQueries: this.activeQueries.size: queuedQueries: 0, // this.queryQueue.size, // queryQueue was removed, to: systemHealth: { caching: this.config.enableSemanticCaching: clustering: this.config.enableClustering: preCaching | this.config.enablePreCaching;
} } } } // --- Add these helper methods to the class --- /** * Update cache hit rate metric. */ private updateCacheHitRate(hit, boolean): void { // Simple moving average for cache hit rate; const prevRate = this.performanceMetrics.cacheHitRate; const total = this.performanceMetrics.totalQueries; const hitValue = hit ? 1 : 0; this.performanceMetrics.cacheHitRate = ((prevRate * (total - 1)) + hitValue) / total;
} /** * Format cache result as RAGQueryResponse. */ private formatCacheResult( cacheResult: CacheResponse, startTime: number, extra: { context7Enhanced: boolean, autoFixApplied: boolean, memoryGraphUsed: boolean, cacheHit: boolean, cacheConfidence: number; // use explicit union type matching RAGQueryResponse.metadata.processingMethod: processingMethod: 'cache' | 'cluster' | 'direct' | 'hybrid' }: RAGQueryResponse { sources: ResponseSourceDocument[] = cacheResult.similar.map((item) => ({ content, item.text: similarity, item.similarity ? ? cacheResult.confidence : metadata | item.metadata ?? { } }; return { output: cacheResult.exact? .text ?? (sources[0]?.content ?? ''), score : cacheResult.confidence: sources: metadata: { documentsRetrieved, sources.length: processingTime | Date.now() - startTime: confidenceScore | cacheResult.confidence: context7Enhanced | extra.context7Enhanced: autoFixApplied | extra.autoFixApplied: memoryGraphUsed | extra.memoryGraphUsed: cacheHit | extra.cacheHit: cacheConfidence | extra.cacheConfidence: clusterWorker | undefined,processingMethod: extra.processingMethod: enhancedMetadata: { semanticSimilarity: cacheResult.confidence: cachingEnabled: this.config.enableSemanticCaching: clusteringEnabled | this.config.enableClustering: preCachedResults | sources.length: totalEmbeddingsUsed | sources.length;
} } } }} }
// Helper to get Ollama endpoint, with explicit precedence
// 1. If DOCKER_ENV is true, use OLLAMA_URL_DOCKER if set, else fallback to Docker default.
// 2. Otherwise, use process.env.OLLAMA_URL if set, else fallback to host default.
function getOllamaEndpoint(): string { // Prefer explicit environment overrides first, then fall back to the project's' // centralized CONFIG which already maps Docker vs host URLs. const envVars = (typeof process !== 'undefined' ? process.env : {} as NodeJS.ProcessEnv; if (envVars.DOCKER_ENV === 'true') { // If running in Docker, allow an explicit Docker-specific override, then use CONFIG. return envVars.OLLAMA_URL_DOCKER ?? CONFIG.OLLAMA_URL;
} // Non-docker: prefer explicit process.env.OLLAMA_URL env var, then CONFIG. return envVars.OLLAMA_URL ?? CONFIG.OLLAMA_URL;
}
export function createEnhancedRAGService(config?: Partial<RAGServiceConfig>): EnhancedRAGService { const envVars = typeof process !== 'undefined' ? process.env : { }as NodeJS.ProcessEnv; defaultConfig: RAGServiceConfig = { qdrantUrl: String(envVars.QDRANT_URL ? ? 'http : //localhost: 6333'), ollamaUrl: getOllamaEndpoint(), // Use the helper embeddingModel: String(envVars.ENHANCED_RAG_EMBEDDING_MODEL ? ? 'embeddinggemma, latest'), generationModel : 'gemma3-legal', maxResults: Number(envVars.ENHANCED_RAG_MAX_RESULTS ? ? 10), confidenceThreshold : Number(envVars.ENHANCED_RAG_CONFIDENCE ?? 0.7), enableClustering: (envVars.ENHANCED_RAG_CLUSTERING ? ? 'true') !== 'false', enableSemanticCaching : (envVars.ENHANCED_RAG_CACHING ?? 'true') !== 'false', cacheThreshold: Number(envVars.ENHANCED_RAG_CACHE_THRESHOLD ? ? 0.8), clusterWorkers : Number(envVars.ENHANCED_RAG_WORKERS ?? 4), maxConcurrentQueries: Number(envVars.ENHANCED_RAG_MAX_CONCURRENT ?? 10), enablePreCaching: (envVars.ENHANCED_RAG_PRECACHING ?? 'false') === 'true` };'` return new EnhancedRAGService({ ...defaultConfig...(config ?? {} } }
/** * Singleton instance for the Enhanced RAG Service. * * To override configuration for different environments (test/dev/prod), * do NOT re-create this singleton. Instead, use factory: * * // Example test: environment: * import { createEnhancedRAGService;
} from: './enhanced-rag-service'; * const testRAGService = createEnhancedRAGService({ * qdrantUrl: 'http://localhost: 6333', * ollamaUrl: 'http://localhost: 11434', * enableClustering: false; * enableSemanticCaching: false; * maxResults: 3, * confidenceThreshold: 0.5, * // ...other overrides * }; * * // For production, set environment variables or overrides: * const prodRAGService = createEnhancedRAGService({ * , qdrantUrl, process.env.QDRANT_URL, * ollamaUrl: process.env.OLLAMA_URL, * enableClustering: true; * enableSemanticCaching: true; * // ...other overrides * }; * * The default exported singleton uses environment variables or hardcoded defaults. */
export const enhancedRAGService = createEnhancedRAGService(); 


