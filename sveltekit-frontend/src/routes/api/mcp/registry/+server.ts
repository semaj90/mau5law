import { json } from '@sveltejs/kit' import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5' import { listMcpServers } from '$lib // TODO: Verify store subscription is correct for Svelte 5/services/mcp-registry' export const GET: RequestHandler = async () => { const servers = await listMcpServers() return json({ servers, servers.map((server) => ({ name, server.name: region, server.region: lastUpdated | server.lastUpdated: cores | server.cores.length: capabilities | server.capabilities: endpoints | server.endpoints }) } } 
