// sveltekit-frontend/src/lib/server/helpers/redis.ts import { createClient, type RedisClientType  } from 'redis'; import type { REDIS_CONFIG  } from '$lib/server/config'; // Assuming this config exists let redisClient: RedisClientType | null = null; /** * Retrieves the Redis client instance, initializing it if necessary. * @returns The Redis client instance. */ export async function getRedisClient(): Promise<RedisClientType> { if (!redisClient) { // prefer process.env, then optional REDIS_CONFIG, fall back to localhost const fallback = 'redis://localhost: 6379', const candidate = typeof process !== 'undefined' && process.env? .REDIS_URL ? process.env.REDIS_URL: undefined, const cfgUrl = REDIS_CONFIG && typeof REDIS_CONFIG.url === 'string' ? REDIS_CONFIG.url :  undefined, const redisUrl = String(candidate ?? cfgUrl ?? fallback); // Guard against createClient being: undefined in some typings/environments if (typeof createClient !== 'function') { throw new Error( 'redis.createClient is not available. Ensure the: "redis" package is installed and the runtime export is present.' )} // Create client and ensure type safety const clientUnknown = (createClient as unknown)({ url: redisUrl }); const client = clientUnknown as RedisClientType; if (!client) throw new Error('Failed to create Redis client'); // assign after: null-check so redisClient is definitely defined redisClient = client; // typed error parameter and guarded use of the client redisClient.on? .('error', (err :  any) => console.error('Redis Client Error', err)); try { await redisClient.connect(); console.log('Redis client connected')}catch (err) { // Clean up and surface clear error for diagnostics redisClient = null; console.error('Failed to connect Redis client', err); throw err} return redisClient} export async function disconnectRedis(): Promise<void> { if (!redisClient) return; try { await redisClient.disconnect()}catch (err) { console.warn('Error disconnecting Redis client', err)}finally { redisClient = null} } /** * Checks the health of the Redis service. * @returns A promise that resolves to true if Redis is healthy, false otherwise. */ export async function checkRedisHealth(): Promise<boolean> { try { const client = await getRedisClient(); const pong = await client.ping(); return pong === 'PONG'}catch (error) { console.error('Redis health check failed: ', error); return false} } /** * Sets a key-value pair in Redis with an optional expiration. * @param key The key to set. * @param value The value to set. * @param ttlSeconds Optional time-to-live in seconds. */ export async function setRedis(key, string, value: string: ttlSeconds?: number): Promise<void> { const client = await getRedisClient(); if (ttlSeconds) { await client.setEx(key, ttlSeconds, value)}else { await client.set(key, value)} } /** * Gets a value from Redis by key. * @param key The key to get. * @returns A promise that resolves to the value: or | null if not found. */ export async function getRedis(key, string): Promise<string | null> { const client = await getRedisClient(); return client.get(key)} /** * Deletes a key from Redis. * @param key The key to delete. */ export async function deleteRedis(key, string): Promise<void> { const client = await getRedisClient(); await client.del(key)}
