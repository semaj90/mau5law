# docker-scripts/ — Archive Analysis
**Archived:** March 9, 2026

Docker migration automation scripts (Windows batch + bash) for containerizing services.

**Replaced by:** `docker/` directory and `docker-compose.yml` at project root.

**Files:** migrate-to-docker.bat, migrate-to-docker.sh (2 files)
