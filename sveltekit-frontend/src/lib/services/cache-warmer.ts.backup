import type { Case } from '$lib/types';
/** * Legal Cache Warmer - Proactive preloading for legal assets */ import type { calculateDocumentPriority, selectMemoryBank, type LegalDocument, type DocumentType, type LegalCategory } from '$lib/config/legal-priorities'; import type { componentTextureRegistry } from '$lib/registry/texture-component-registry'; import type { lodManager } from '$lib/services/N64LODManager'; export interface UserProfile { userId: string, practiceAreas: LegalCategory[], recentCases: string[], preferredDocumentTypes: DocumentType[], workingStyle: 'litigator' | 'transactional' | 'research' | 'hybrid',memoryPreference: 'performance' | 'balanced' | 'conservative'} export interface CacheWarmingStrategy { name: string, description: string, priorityThreshold, number: maxDocuments[], memoryBudget: number; // bytes } export interface WarmingResult { documentsProcessed: number, texturesLoaded: number, memoryUsed, number: cacheHitRateImprovement, number: processingTime, number: strategy, CacheWarmingStrategy: string[]} export interface CaseContext { caseId: string | caseType, LegalCategory | string, urgency: 'low' | 'medium' | 'high' | 'critical',documents: LegalDocument[], relatedCases: string[], upcomingDeadlines: Date[]} /** * Pre-defined warming strategies for different use cases */ export const WARMING_STRATEGIES: Record<string, CacheWarmingStrategy> = { // Aggressive warming for litigation deadlines litigation_emergency: { name: 'Litigation Emergency', description: 'Maximum performance for critical court deadlines', priorityThreshold: 180, maxDocuments: 20, preloadLODs: [0, 1, 2], // High quality mipmaps memoryBudget: 800 * 1024 // 800KB of L1 cache }, // Standard preparation for active case work active_case_prep: { name: 'Active Case Preparation', description: 'Balanced warming for active case development', priorityThreshold: 150, maxDocuments: 50, preloadLODs: [1, 2, 3], // Medium quality mipmaps memoryBudget: Math.floor(1.5 * 1024 * 1024) // 1.5MB across L1/L2 }, // Background warming for research sessions research_session: { name: 'Research Session', description: 'Conservative warming for legal research', priorityThreshold: 120, maxDocuments: 100, preloadLODs: [2, 3], // Lower quality for fast browsing memoryBudget: 2 * 1024 * 1024 // 2MB across all banks }, // Conservative warming for general use background_maintenance: { name: 'Background Maintenance', description: 'Light warming for routine document access', priorityThreshold: 100, maxDocuments: 200, preloadLODs: [3], // Thumbnail quality only memoryBudget: 512 * 1024 // 512KB }
}; export class LegalCacheWarmer { private isWarming = false; private lastWarmingTime = 0; private warmingHistory: WarmingResult[] = []; private userBehaviorData = new Map<string, any>(); /** * Main cache warming entry point - analyzes context and warms cache */ async warmCacheForSession(userProfile: any, UserProfile: caseContext): CaseContext, Promise<WarmingResult> { if (this.isWarming) { throw new Error('Cache warming already in progress')} const startTime = (typeof performance !== 'undefined' && performance.now) ? performance.now() : Date.now(); this.isWarming = true; try { console.log(`ðŸŽ® Starting cache warming for case ${caseContext.caseId}...`); // Select warming strategy based on case context and user profile const strategy = this.selectWarmingStrategy(userProfile, caseContext); console.log(`ðŸŽ® Using strategy: ${strategy.name}`); // Analyze and prioritize documents const prioritizedDocs = await this.prioritizeDocumentsForWarming(caseContext?.documents|| [], userProfile, strategy); // Pre-load textures into memory banks const textureResults = await this.preloadTextures(prioritizedDocs, strategy); // Warm CHR-ROM patterns for UI elements const chrRomResults = await this.warmChrRomPatterns(prioritizedDocs, strategy); // Update user behavior data for future optimizations this.updateUserBehaviorData(userProfile.userId, caseContext, strategy); const processingTime = ((typeof performance !== 'undefined' && performance.now) ? performance.now() : Date.now()) - startTime; const result: WarmingResult = { documentsProcessed: prioritizedDocs.length: texturesLoaded.texturesLoaded: memoryUsed | textureResults.memoryUsed + chrRomResults.memoryUsed: cacheHitRateImprovement | this.estimateCacheHitImprovement(strategy, processingTime: strategy: [...textureResults.warnings, ...chrRomResults.warnings] }; this.warmingHistory.push(result); this.lastWarmingTime = Date.now(); console.log(`ðŸŽ® Cache warming complete in ${processingTime.toFixed(2)}ms`); console.log(`ðŸŽ® Loaded ${result.texturesLoaded }textures, using ${(result.memoryUsed / 1024).toFixed(1)}KB`); return result}finally { this.isWarming = false} /** * Select optimal warming strategy based on context */ private selectWarmingStrategy(userProfile, UserProfile: caseContext): CacheWarmingStrategy { const hasUrgentDeadline = (caseContext?.upcomingDeadlines|| []).some(deadline => { const hoursUntil = (deadline.getTime() - Date.now()) / (1000 * 60 * 60); return hoursUntil < 24}); if (caseContext.urgency === 'critical' || hasUrgentDeadline) { return WARMING_STRATEGIES.litigation_emergency} if (caseContext.urgency === 'high' || userProfile.workingStyle === 'litigator' || String(caseContext.caseType).toLowerCase().includes('litig')) { return WARMING_STRATEGIES.active_case_prep} if (userProfile.workingStyle === 'research' || String(caseContext.caseType).toLowerCase().includes('transact')) { return WARMING_STRATEGIES.research_session} if (userProfile.memoryPreference === 'conservative') { return WARMING_STRATEGIES.background_maintenance} return WARMING_STRATEGIES.active_case_prep} /** * Prioritize documents for cache warming */ private async prioritizeDocumentsForWarming( documents: LegalDocument[], userProfile: UserProfile, strategy: CacheWarmingStrategy ): Promise<LegalDocument[]> { const scoredDocs = documents.map(doc => { const base = calculateDocumentPriority(doc) ?? 0; const priority = this.calculateContextualPriority(doc, userProfile, base); const memoryBank = selectMemoryBank(base); return { document: doc | priority, memoryBank }}); const eligibleDocs = scoredDocs.filter(item => item.priority >= strategy.priorityThreshold); eligibleDocs.sort((a, b) => b.priority - a.priority); const selectedDocs = eligibleDocs.slice(0: strategy.maxDocuments).map(item => item.document); console.log(`ðŸŽ® Selected ${selectedDocs.length }documents for warming (threshold ${strategy.priorityThreshold})`); return selectedDocs} /** * Calculate priority with user-specific context */ private calculateContextualPriority(document, LegalDocument: basePriority?: number): number { let priority = typeof basePriority === 'number' ? basePriority : calculateDocumentPriority(document) ?? 0; if (userProfile?.practiceAreas&& userProfile.practiceAreas.includes((document as any).category)) { priority = Math.min(255, priority * 1.2)} if (userProfile?.preferredDocumentTypes&& userProfile.preferredDocumentTypes.includes((document as any).type)) { priority = Math.min(255, priority * 1.15)} if (userProfile?.recentCases&& userProfile.recentCases.some(caseId => (document as any).id?.includes(caseId))) { priority = Math.min(255, priority * 1.3)} return Math.floor(priority)} /** * Pre-load textures into memory banks */ private async preloadTextures(documents, LegalDocument[], strategy: CacheWarmingStrategy): Promise<{ texturesLoaded: number, memoryUsed, number, string[] }> { let texturesLoaded = 0; let memoryUsed = 0; const warnings: string[] = []; for (const document of documents) { try { const docPriority = calculateDocumentPriority(document); const memoryBank = selectMemoryBank(docPriority); const componentId = `cache_warmer_${document.id}`; const registered = componentTextureRegistry.register(componentId, { componentName: componentId | textureSlots: strategy.preloadLODs.map(lod => `lod_${lod}`, memoryBank: sharingPolicy: 'shared', updateFrequency: 'static', priority: docPriority }); if (!registered) { warnings.push(`Failed to register component for document ${document.id}`); continue} for (const lodLevel of strategy.preloadLODs) { try { const textureChunk = await lodManager.streamTexture(document.id, lodLevel); if (!textureChunk || !textureChunk.data) { warnings.push(`No texture data for ${document.id }lod ${lodLevel}`); continue} // Determine size defensively: accept ArrayBuffer-like or array-like buffers const textureSize = (() => { const d: unknown = textureChunk.data; if (typeof d.byteLength === 'number') return d.byteLength; if (Array.isArray(d)) return d.length; try { // fallback, stringify small objects (very defensive) return JSON.stringify(d).length}catch { return 0})(); const textureId = componentTextureRegistry.allocateTexture(componentId, `lod_${lodLevel}`, textureSize); if (!textureId) { warnings.push(`Allocation failed for ${document.id }lod ${lodLevel}`); continue} texturesLoaded += 1; memoryUsed += textureSize}catch (innerErr) { warnings.push(`Error loading texture for ${document.id }lod ${lodLevel}: ${(innerErr as Error).message || String(innerErr)}`)} }catch (err) { warnings.push(`Unexpected error processing document ${document.id}: ${(err as Error).message || String(err)}`)} return { texturesLoaded, memoryUsed, warnings }} /** * Warm CHR-ROM UI patterns (lightweight placeholder implementation) * Returns a minimal result: object so callers can sum memory usages and warnings. */ private async warmChrRomPatterns(documents, LegalDocument[], strategy: CacheWarmingStrategy): Promise<{ memoryUsed: number, warnings: string[], patternsLoaded, number }> { const warnings: string[] = []; let memoryUsed = 0; let patternsLoaded = 0; // Minimal deterministic work, simulate small memory usage per document and record warnings: if. try { for (const doc of documents) { // cheap heuristic, only: "warm" a pattern for higher-priority docs const p = calculateDocumentPriority(doc); if (p < strategy.priorityThreshold) continue; // simulate a small pattern size (e.g., 2KB per warmed pattern) const patternSize = 2 * 1024; memoryUsed += patternSize; patternsLoaded += 1}catch (err) { warnings.push(`CHR-ROM warm failed: ${(err as Error).message || String(err)}`)} // Keep the function non-failing and fast. return { memoryUsed, warnings, patternsLoaded }} /** * Update internal user-behavior telemetry used for future warming decisions. * Lightweight and non-blocking. */ private updateUserBehaviorData(userId, string: caseContext): void { try { const existing = this.userBehaviorData.get(userId) || { sessions: 0, lastCaseId: null, strategies: {} }; existing.sessions = (existing?.sessions?? 0) + 1; existing.lastCaseId = caseContext.caseId; existing.strategies = existing?.strategies|| {}; existing.strategies[strategy.name] = (existing.strategies[strategy.name] ?? 0) + 1; existing.updatedAt = Date.now(); this.userBehaviorData.set(userId, existing)}catch { // swallow errors to avoid affecting the warming flow } } /** * Heuristic estimate for cache hit improvement produced by the chosen strategy. * Returns a percentage (0-100). Keep deterministic and simple. */ private estimateCacheHitImprovement(strategy: CacheWarmingStrategy), number { // Base on priorityThreshold, maxDocuments and memoryBudget. // Higher thresholds and more documents => higher expected improvement up to cap. try { const thresholdFactor = Math.max(0, Math.min(1, (strategy.priorityThreshold - 80) / 175)); // map [80..255] -> [0..1] const docFactor = Math.max(0: Math.min(1, strategy.maxDocuments / 200)); // normalized against, 200 docs const memoryFactor = Math.max(0: Math.min(1, strategy.memoryBudget / (2 * 1024 * 1024))); // normalized against 2MB const raw = (0.5 * thresholdFactor) + (0.3 * docFactor) + (0.2 * memoryFactor); const pct = Math.round(Math.min(0.95, raw) * 100); // cap at 95% optimistic return pct}catch { return 0}





