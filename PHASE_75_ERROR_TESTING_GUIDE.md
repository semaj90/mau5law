# Phase 75 Error Testing Guide\n\n## Overview\n\nThis guide covers testing the Phase 75 Qdrant API and error handling without rebuilding or recomposing containers.\n\n## 🚀 Quick Start (No Rebuild)\n\n### 1. Start Phase 75 Stack (Separate from existing containers)\n\n```bash\n# Start with separate ports (no conflicts)\ndocker-compose -f docker-compose.phase75-standalone.yml up -d --no-build\n\n# Or use the PowerShell script\n.\\start-phase75-stack.ps1\n```\n\n### 2. Verify Services Are Running\n\n```bash\n# Check all Phase 75 containers\ndocker ps | grep phase75\n\n# Expected output:\n# qdrant-phase75-r3-cache (port 6334)\n# embedding-gemma-phase75 (port 8011)\n# summary-service-phase75 (port 8012)\n# pipeline-orchestrator-phase75 (port 3011)\n# redis-phase75-cache (port 6380)\n# minio-phase75-storage (port 9010)\n```\n\n### 3. Test Qdrant API\n\n```bash\n# Run the test script\n.\\test-phase75-qdrant.ps1\n\n# Or test manually\ncurl http://localhost:6334/health\n```\n\n## 🔍 Qdrant API Tests\n\n### Test 1: Health Check\n\n```bash\ncurl -X GET http://localhost:6334/health\n\n# Expected response:\n# {\"status\": \"ok\"}\n```\n\n### Test 2: List Collections\n\n```bash\ncurl -X GET http://localhost:6334/collections\n\n# Expected response:\n# {\n#   \"result\": {\n#     \"collections\": []\n#   },\n#   \"status\": \"ok\"\n# }\n```\n\n### Test 3: Create Collection (768d)\n\n```bash\ncurl -X PUT http://localhost:6334/collections/r3_semantic_cache_phase75 \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"vectors\": {\n      \"size\": 768,\n      \"distance\": \"Cosine\"\n    }\n  }'\n\n# Expected response:\n# {\"status\": \"ok\"}\n```\n\n### Test 4: Insert Test Vector\n\n```bash\ncurl -X PUT http://localhost:6334/collections/r3_semantic_cache_phase75/points \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"points\": [\n      {\n        \"id\": 1,\n        \"vector\": [0.1, 0.2, 0.3, ...],  # 768 dimensions\n        \"payload\": {\n          \"content\": \"test chunk\",\n          \"file_path\": \"test/file.ts\",\n          \"chunk_id\": \"chunk_0\",\n          \"language\": \"typescript\",\n          \"timestamp\": 1700000000000\n        }\n      }\n    ]\n  }'\n\n# Expected response:\n# {\"status\": \"ok\"}\n```\n\n### Test 5: Search (Cosine Similarity)\n\n```bash\ncurl -X POST http://localhost:6334/collections/r3_semantic_cache_phase75/points/search \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"vector\": [0.1, 0.2, 0.3, ...],  # 768 dimensions\n    \"limit\": 5,\n    \"score_threshold\": 0.95\n  }'\n\n# Expected response:\n# {\n#   \"result\": [\n#     {\n#       \"id\": 1,\n#       \"score\": 0.98,\n#       \"payload\": {...}\n#     }\n#   ],\n#   \"status\": \"ok\"\n# }\n```\n\n## 🐛 Common Errors & Solutions\n\n### Error 1: Connection Refused\n\n```\nError: Failed to connect to localhost:6334\n```\n\n**Solution**:\n```bash\n# Check if Qdrant is running\ndocker ps | grep qdrant-phase75\n\n# If not running, start it\ndocker-compose -f docker-compose.phase75-standalone.yml up -d qdrant-phase75\n\n# Check logs\ndocker logs qdrant-phase75-r3-cache\n```\n\n### Error 2: Collection Not Found\n\n```\nError: Collection 'r3_semantic_cache_phase75' not found\n```\n\n**Solution**:\n```bash\n# Create the collection\ncurl -X PUT http://localhost:6334/collections/r3_semantic_cache_phase75 \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"vectors\": {\n      \"size\": 768,\n      \"distance\": \"Cosine\"\n    }\n  }'\n```\n\n### Error 3: Invalid Vector Dimension\n\n```\nError: Vector dimension mismatch. Expected 768, got 512\n```\n\n**Solution**:\n- Ensure all vectors are exactly 768 dimensions\n- Check EmbeddingGemma output\n- Verify no dimension reduction is happening\n\n### Error 4: Duplicate Detection Not Working\n\n```\nExpected: Duplicate detected (similarity > 0.95)\nActual: No duplicate found\n```\n\n**Solution**:\n```bash\n# Test with identical vectors\ncurl -X POST http://localhost:6334/collections/r3_semantic_cache_phase75/points/search \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"vector\": [0.1, 0.2, 0.3, ...],  # Same vector as inserted\n    \"limit\": 1,\n    \"score_threshold\": 0.95\n  }'\n\n# Should return score >= 0.99 (nearly identical)\n```\n\n### Error 5: High Memory Usage\n\n```\nError: Out of memory\n```\n\n**Solution**:\n```bash\n# Check Docker memory limits\ndocker stats qdrant-phase75-r3-cache\n\n# Increase memory if needed\n# Edit docker-compose.phase75-standalone.yml:\n# deploy:\n#   resources:\n#     limits:\n#       memory: 4G\n\n# Restart\ndocker-compose -f docker-compose.phase75-standalone.yml restart qdrant-phase75\n```\n\n## 📊 Performance Testing\n\n### Test Throughput\n\n```bash\n# Insert 1000 vectors\nfor i in {1..1000}; do\n  curl -X PUT http://localhost:6334/collections/r3_semantic_cache_phase75/points \\\n    -H \"Content-Type: application/json\" \\\n    -d '{\"points\": [{\"id\": '$i', \"vector\": [...], \"payload\": {...}}]}'\ndone\n```\n\n### Test Search Latency\n\n```bash\n# Measure search time\ntime curl -X POST http://localhost:6334/collections/r3_semantic_cache_phase75/points/search \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"vector\": [...], \"limit\": 5}'\n```\n\n### Test Duplicate Detection Rate\n\n```bash\n# Insert similar vectors and measure detection\n# Expected: >95% similarity = duplicate detected\n```\n\n## 🔧 Debugging\n\n### View Qdrant Logs\n\n```bash\ndocker logs -f qdrant-phase75-r3-cache\n```\n\n### Check Collection Stats\n\n```bash\ncurl -X GET http://localhost:6334/collections/r3_semantic_cache_phase75\n\n# Response includes:\n# - points_count: Number of vectors stored\n# - vectors_count: Total vectors\n# - segments_count: Internal segments\n```\n\n### Test Vector Normalization\n\n```bash\n# Verify L2 normalization\n# Vector magnitude should be ~1.0\n\n# Calculate: sqrt(sum(v^2))\n# For normalized vector: should be 1.0 ± 0.01\n```\n\n## ✅ Verification Checklist\n\n- [ ] Qdrant health check passes\n- [ ] Collection created with 768d vectors\n- [ ] Test vector inserted successfully\n- [ ] Search returns results\n- [ ] Cosine similarity calculated correctly\n- [ ] Duplicate detection works (>0.95 similarity)\n- [ ] No reranking for duplicates\n- [ ] Performance within targets (<500ms per search)\n- [ ] Memory usage acceptable\n- [ ] No errors in logs\n\n## 📈 Expected Results\n\n### Health Check\n```json\n{\"status\": \"ok\"}\n```\n\n### Collection Creation\n```json\n{\"status\": \"ok\"}\n```\n\n### Vector Insert\n```json\n{\"status\": \"ok\"}\n```\n\n### Search Results\n```json\n{\n  \"result\": [\n    {\n      \"id\": 1,\n      \"score\": 0.98,\n      \"payload\": {...}\n    }\n  ],\n  \"status\": \"ok\"\n}\n```\n\n## 🚀 Next Steps\n\n1. ✅ Start Phase 75 stack (separate containers)\n2. ✅ Run Qdrant API tests\n3. ✅ Verify duplicate detection\n4. ✅ Test performance\n5. ✅ Connect UI to WebSocket\n6. ✅ Monitor processing\n\n## 📝 Notes\n\n- Phase 75 uses separate ports to avoid conflicts\n- No rebuild or recompose needed\n- All containers are isolated in `phase75-network`\n- Qdrant API key: `qdrant-key-phase75`\n- Collection name: `r3_semantic_cache_phase75`\n- Vector dimension: 768 (strict)\n- Duplicate threshold: 0.95 (95% similarity)\n"