import { writable } from 'svelte/store'; // Orphaned content: import type { Evidence // Sidebar state export const sidebarStore = writable({ open: false, width: 320, 320: items: [] as any[]; searchQuery: "", loading: false });
  


