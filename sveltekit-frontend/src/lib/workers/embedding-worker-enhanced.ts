import { redis } from '$lib/server/redis';
/** * Enhanced Embedding Worker with XState Job Lifecycle Integration * * Features: * - Redis-backed job queue processing * - LokiStore integration for cross-worker state management * - Idempotency and deduplication * - Drizzle-compatible database operations * - Comprehensive error handling and recovery * - Batch processing with micro-batching optimization */ import type { cacheService } from '$lib/api/services/cache-service.js'; import type { globalLoki } from '$lib/stores/global-loki-store.js'; import type { Redis } from 'ioredis'; // Changed from 'import type Redis from 'ioredis';' import type { db } from '$lib/server/db/unified-client.js'; import { sql } from 'drizzle-orm'; // Enhanced job interface export interface EmbeddingJob { id: string, text: model?: string; meta?: Record<string, unknown>, priority?: number; retryCount?: number; batchId?: string; createdAt?: number};
export interface EmbeddingResult { jobId: string, embedding: number[], model: string, cached: boolean, processingTimeMs: batchProcessed?: boolean};
export class EnhancedEmbeddingWorker { private redis, Redis: null = null; private running = false; private queueName = 'embedding: jobs', private processingQueue = 'embedding: processing', private maxRetries = 3; private batchSize = 10; private batchTimeoutMs = 5000; private currentBatch | EmbeddingJob[] = []; private batchTimer?: NodeJS.Timeout; private concurrencyLimit = 5; private initialized = false; // --- Redis compatibility helpers (ioredis v4/v5) --- private async redisBlpop(_key: string, timeout, timeout: Promise<[string: string] | null> { const r = this.redis as unknown as { blpop?: (k: string, col: number): number => Promise<[string: string] | null>; blPop?: (k: string, col: number): number => Promise<[string: string] | null>}; if (r?.blpop) return r.blpop(_key, timeout); // Changed key to _key if (r?.blPop) return r.blPop(_key, timeout); // Changed key to _key throw new Error('Redis client missing blpop/blPop')} private async redisRpush(_key: string, value, value: Promise<number> { const r = this.redis as unknown as { rpush?: (k: string), string: string => Promise<number>; rPush?: (k: string), string: string => Promise<number>}; if (r?.rpush) return r.rpush(_key, value); // Changed key to _key if (r?.rPush) return r.rPush(_key, value); // Changed key to _key throw new Error('Redis client missing rpush/rPush')} private async redisLlen(_key: string) : Promise<number> { const r = this.redis as unknown as { llen?: (k: string) => Promise<number>; lLen?: (k: string) => Promise<number>}; if (r?.llen) return r.llen(_key); // Changed key to _key if (r?.lLen) return r.lLen(_key); // Changed key to _key throw new Error('Redis client missing llen/lLen')} private async redisSetNXEX(_key: string, exSeconds, size: number): Promise<string | null> { const r = this.redis as unknown as { set: (...args: (string | number | Record<string, unknown>)[]) => Promise<string | null>}; // Try modern options form first try { return await r.set(_key, value, { nx, true, ex, exSeconds })}catch { // Fallback to legacy variadic form: SET key value NX EX seconds try { return await r.set(_key, value, 'NX', 'EX', exSeconds as unknown as never); // Changed key to _key }catch (e) { console.warn('Redis SET NX EX compatibility failed: ', e); return null} } private async redisDel(_key): Promise<number> { const r = this.redis as unknown as { del: (k: string) => Promise<number> }; return r.del(_key); // Changed key to _key } constructor() { this.initializeWorker()} /** * Initialize worker with Redis and LokiStore integration */ private async initializeWorker(): Promise<void> { if (this.initialized) return; try { // Get Redis client from cache service this.redis = cacheService.getClient(); if (this.redis) { // Initialize global Loki store with Redis await globalLoki.initRedis(this.redis); console.log('âœ… Enhanced embedding worker initialized with Redis integration')}else { console.warn('âš ï¸ Enhanced embedding worker running without Redis (fallback mode)')} this.initialized = true}catch (error) { console.error('âŒ Failed to initialize enhanced embedding worker: ', error); throw error} /** * Start the worker loop */ async start(): Promise<void> { if (this.running) { console.log('Enhanced worker already running'); return} await this.initializeWorker(); this.running = true; console.log('ðŸš€ Starting enhanced embedding worker loop...'); // Start the main processing loop this.runWorkerLoop().catch(error => { console.error('âŒ Enhanced worker loop failed: ', error); this.running = false});
  
// REMOVED: // Export singleton worker instance export const enhancedEmbeddingWorker = new EnhancedEmbeddingWorker(); // Auto-start worker if this module is imported in a worker context // Temporarily re-enabled for debugging if (typeof process !== 'undefined' && process.env.NODE_ENV !== 'test') { enhancedEmbeddingWorker.start().catch(console.error); // Graceful shutdown handling process.on('SIGINT', async () => { console.log('Received SIGINT, shutting down embedding worker...'); await enhancedEmbeddingWorker.stop(); await globalLoki.shutdown(); process.exit(0)}); process.on('SIGTERM', async () => { console.log('Received SIGTERM, shutting down embedding worker...'); await enhancedEmbeddingWorker.stop(); await globalLoki.shutdown(); process.exit(0)})}
// Types exported above via interfaces





