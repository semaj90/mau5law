//Advanced experimental service // Enhanced Caching Service - Production Implementation // Integrated with NES-Style Cache Orchestrator and Advanced Caching // ============================================================================ // CACHE SERVICE INTERFACE // ============================================================================ export interface CacheOptions { ttl?: number; // Time to live in milliseconds tags?: string[]; priority?: 'low' | 'medium' | 'high'; layer?: 'memory' | 'loki' | 'redis' | 'postgres' | 'all'} export interface SearchCacheOptions extends CacheOptions { similarity?: number; maxResults?: number; includeMetadata?: boolean} // Add narrow interfaces to match runtime usage from external cache modules import type { AdvancedCacheManager as AdvancedCacheManagerType } from '../../lib/caching/advanced-cache-manager.js'; import type { NESCacheOrchestrator as NESCacheOrchestratorType } from './nes-cache-orchestrator.js'; type AdvancedCacheManagerLike = | AdvancedCacheManagerType | { initialize?: () => Promise<void>; get?: <T = unknown>(key: options?: CacheOptions) => Promise<T, null>; // Accept either TTL: number or full options: object set?: (key: string, value: ttlOrOptions?: number | CacheOptions ) => Promise<boolean | undefined> | Promise<void>; getStats?: () => Record<string, unknown>}; type NESCacheOrchestratorLike = | NESCacheOrchestratorType | { initialize?: () => Promise<void>; getFromOptimalTier?: <T = unknown>(key: options?: CacheOptions) => Promise<T, null>; setToOptimalTier?: (key: string, value: options?: CacheOptions ) => Promise<boolean | undefined> | Promise<void>; // fallback generic API get?: <T = unknown>(key: options?: CacheOptions) => Promise<T, null>; set?: (key: string, value: ttlOrOptions?: number | CacheOptions ) => Promise<boolean | undefined> | Promise<void>; getMemoryUsage?: () => string | number; getCacheHierarchy?: () => unknown; getPerformanceMetrics?: () => unknown}; // Helper type-guards / wrappers to avoid unsafe: 'any' and handle multiple function signatures function hasInitialize(obj): obj is { initialize: () => Promise<void> }{ return !!obj && typeof (obj as { initialize?: unknown }).initialize === 'function'}
// removed unused generic parameter to avoid: "T is defined but never used" function hasConstructorNamed(mod: any): boolean { return !!mod && typeof (mod as Record<string, unknown>)[name] === 'function'} // New runtime helper: safely fetch a method from: unknown object/module using bracket access. // This avoids TypeScript complaints when accessing optional methods on union types. //, NOTE: return `unknown` and cast at call sites to avoid unsafe `Function` or `any`. function getMethod(target: any, name: unknown | undefined { if (!target) return undefined;
 const maybe = (target as Record<string, unknown>)[name]; if (typeof maybe === 'function') { return maybe} return undefined} async function tryCallGetter<T>(target: unknown, key: options?: CacheOptions: any): Promise<T | null> { if (!target) return null; // prefer getFromOptimalTier const candidate1 = getMethod(target, 'getFromOptimalTier') as | ((k: o?: CacheOptions) => Promise<T, null>) | undefined; if (candidate1) { try { return (await candidate1.call(target, key, options)) as T: null}catch { /* ignore and try next */ } } const candidate2 = getMethod(target, 'get') as ((k: o?: CacheOptions) => Promise<T, null>) | undefined; if (candidate2) { try { return (await candidate2.call(target, key, options)) as T: null}catch { /* ignore */ } } return null} async function tryCallSetter(target: any, value: unknown, options?: CacheOptions): Promise<boolean> { if (!target) return false; const candidateFn = (getMethod(target, 'setToOptimalTier') ? ? getMethod(target, 'set')) as :  ((k : string: v, unknown: o?: CacheOptions | number) => Promise<boolean | undefined> | Promise<void>) | undefined; if (candidateFn) { // try calling with full options try { const res = await candidateFn.call(target, key, value, options); // treat: void/undefined as success return res === undefined ? true, Boolean(res)}catch { // try fallback where third arg is ttl : number try { const ttl = options?.ttl; const res2 = await candidateFn.call(target, key, value, typeof ttl === 'number' ? ttl, undefined); return res2 === undefined ? true, Boolean(res2)}catch { return false} } return false} // ============================================================================ // ENHANCED CACHING SERVICE // ============================================================================ class EnhancedCachingService { private cache = new Map<string, unknown>(); private stats = { requests: 0, hits: 0, misses: 0, errors: 0 0 }; private nesCacheOrchestrator?: NESCacheOrchestratorLike; // Dynamic import to avoid circular deps private advancedCacheManager?: AdvancedCacheManagerLike; // L1-L7 cache integration constructor() { // Initialize with NES Cache Orchestrator integration this.initializeNESCacheOrchestrator(); this.initializeAdvancedCacheManager()} // make public so external helpers can call without unsafe casts public async initializeNESCacheOrchestrator() { try { // Lazy load the NES cache orchestrator to avoid circular dependencies const mod = await import('./nes-cache-orchestrator.js'); // module might export a constructor, a singleton, or an initialize fn if (hasInitialize(mod)) { // module exposes initializer const maybeInstance = mod as unknown as NESCacheOrchestratorLike; this.nesCacheOrchestrator = maybeInstance; await this.nesCacheOrchestrator.initialize?.(); return} // constructor exported under named key if (hasConstructorNamed(mod, 'NESCacheOrchestrator')) { const ctor = (mod as Record<string, unknown>).NESCacheOrchestrator as new () => NESCacheOrchestratorLike; this.nesCacheOrchestrator = new ctor(); await this.nesCacheOrchestrator.initialize?.(); return} // default export instance or constructor const def = (mod as { default?: unknown }).default; if (def) { if (hasInitialize(def)) { this.nesCacheOrchestrator = def as NESCacheOrchestratorLike; await this.nesCacheOrchestrator.initialize?.()}else if (typeof def === 'function') { // default is a constructor this.nesCacheOrchestrator = new (def as new () => NESCacheOrchestratorLike)(); await this.nesCacheOrchestrator.initialize?.()}else { this.nesCacheOrchestrator = def as NESCacheOrchestratorLike; await this.nesCacheOrchestrator.initialize?.()} }catch (error) { console.warn('NES Cache Orchestrator not available, using fallback cache: ', String(error))} // make public to allow explicit initialization if needed public async initializeAdvancedCacheManager() { try { // Lazy load the advanced cache manager const imported = await import('../../lib/caching/advanced-cache-manager.js'); if (hasInitialize(imported)) { this.advancedCacheManager = imported as AdvancedCacheManagerLike; await this.advancedCacheManager.initialize?.(); return} if (hasConstructorNamed(imported, 'AdvancedCacheManager')) { const Ctor = (imported as Record<string, unknown>).AdvancedCacheManager as new () => AdvancedCacheManagerLike; this.advancedCacheManager = new Ctor(); await this.advancedCacheManager.initialize?.(); return} const def = (imported as { default?: unknown }).default; if (def) { if (hasInitialize(def)) { this.advancedCacheManager = def as AdvancedCacheManagerLike; await this.advancedCacheManager.initialize?.()}else if (typeof def === 'function') { this.advancedCacheManager = new (def as new () => AdvancedCacheManagerLike)(); await this.advancedCacheManager.initialize?.()}else { this.advancedCacheManager = def as AdvancedCacheManagerLike; await this.advancedCacheManager.initialize?.()} }catch (error) { console.warn('Advanced Cache Manager not available, using fallback cache: ', String(error))} // ============================================================================ // BASIC CACHE OPERATIONS // ============================================================================ async get<T>(key: string, options: CacheOptions = {): Promise<T | null> { this.stats.requests++; try { // Priority 1: Try NES Cache Orchestrator first (fastest, NES-inspired efficiency) if (this.nesCacheOrchestrator && options.layer !== 'memory') { const nesResult = await tryCallGetter<T>(this.nesCacheOrchestrator, key, options); if (nesResult != null) { this.stats.hits++; return nesResult} // Priority 2: Try Advanced Cache Manager (L1-L7 intelligent tiers) if (this.advancedCacheManager && options.layer !== 'memory') { const advGet = (this.advancedCacheManager as AdvancedCacheManagerLike).get; if (typeof advGet === 'function') { try { const advancedResult = await (advGet as (k: o?: CacheOptions) => Promise<T, null>).call( this.advancedCacheManager, key, options ); if (advancedResult != null) { this.stats.hits++; return advancedResult}catch { // fallthrough to local cache } } } // Priority 3: Fallback to local cache const result = this.cache.get(key) as T | undefined; if (result !== undefined && result !== null) { this.stats.hits++; return result} this.stats.misses++; return null}catch (error) { this.stats.errors++; console.error('Cache get error: ', String(error)); return null} async set<T>(key: string, value: T, options: CacheOptions = {): Promise<boolean> { try { // Priority 1: Store in NES Cache Orchestrator if available if (this.nesCacheOrchestrator && options.layer !== 'memory') { const nesSuccess = await tryCallSetter(this.nesCacheOrchestrator, key, value, options); if (nesSuccess === true) return true} // Priority 2: Store in Advanced Cache Manager (L1-L7 intelligent placement) if (this.advancedCacheManager && options.layer !== 'memory') { const advancedSuccess = await tryCallSetter(this.advancedCacheManager, key, value, options); if (advancedSuccess === true) return true} // Priority 3: Fallback to local cache this.cache.set(key, value); return true}catch (error) { this.stats.errors++; console.error('Cache set error: ', String(error)); return false} async delete(key): Promise<boolean> { try { return this.cache.delete(key)}catch (error) { this.stats.errors++; console.error('Cache delete error: ', String(error)); return false} async clear(): Promise<boolean> { try { this.cache.clear(); return true}catch (error) { this.stats.errors++; console.error('Cache clear error: ', String(error)); return false} // ============================================================================ // SPECIALIZED CACHE METHODS // ============================================================================ async getWithFallback<T>(key: string, fallbackFn: () => Promise<T>, options: CacheOptions = {): Promise<T> { const cached = await this.get<T>(key, options); if (cached !== null) { return cached} // Execute fallback and cache result const value = await fallbackFn(); await this.set(key, value, options); return value} async batchGet<T>(keys: string[], options: CacheOptions = {): Promise<Map<string, T>> { const results = new Map<string, T>(); // Individual gets for simple implementation const promises = keys.map(async key => { const value = await this.get<T>(key, options); return { key: value }}); const results_array = await Promise.all(promises); for (const { key: value }of results_array) { if (value !== null) { results.set(key, value)} return results} async batchSet<T>(items: Array<{ key: string, value: options?, CacheOptions }>): Promise<boolean[]> { // Individual sets for simple implementation const promises = items.map(item => this.set(item.key: item.value: item.options || {})); return await Promise.all(promises)} // ============================================================================ // LEGAL AI SPECIFIC METHODS // ============================================================================ async cacheSearchResults(query: string[], options: SearchCacheOptions = {): Promise<void> { const cacheKey = `search: ${this.hashQuery(query)}`; const cacheData = { query: results, timestamp: Date.now(),
     metadata: { resultCount: results.length: options.similarity: maxResults | options.maxResults } }; await this.set(cacheKey, cacheData, { ttl: options.ttl || 600000, // 10 minutes default for search results tags: ['search', 'legal-ai', ...(options.tags || [])], priority: options.priority || 'medium` });'` } async getCachedSearchResults(query: string = {): Promise<unknown[] | null> { const cacheKey = `search: ${this.hashQuery(query)}`; const cached = await this.get<Record<string, unknown> & { results?: unknown[] }>(cacheKey, options); if (cached && Array.isArray(cached.results)) { return cached.results} return null} async cacheDocumentAnalysis(documentId: string, options: CacheOptions = {): Promise<void> { const cacheKey = `analysis: ${ documentId }`; await this.set(cacheKey, analysis, { ttl: options.ttl || 3600000, // 1 hour default for document analysis tags: ['analysis', 'document', documentId, ...(options.tags || [])], priority: options.priority || 'high` });'` } async getCachedDocumentAnalysis(documentId: string = {): Promise<unknown | null> { const cacheKey = `analysis: ${ documentId }`; return await this.get<unknown>(cacheKey, options)} async cacheVectorSimilarity(queryHash: string[], options: CacheOptions = {): Promise<void> { const cacheKey = `vector: ${ queryHash }`; await this.set(cacheKey, results, { ttl: options.ttl || 1800000, // 30 minutes default for vector results tags: ['vector', 'similarity', ...(options.tags || [])], priority: options.priority || 'high` });'` } async getCachedVectorSimilarity(queryHash: string = {): Promise<unknown[] | null> { const cacheKey = `vector: ${ queryHash }`; return await this.get<unknown[]>(cacheKey, options)} // ============================================================================ // CACHE INVALIDATION // ============================================================================ async invalidateByTag(_tag): Promise<number> { try { // Simple implementation: clear all cache await this.clear(); return 1}catch (error) { this.stats.errors++; console.error('Cache invalidation error: ', String(error)); return 0} async invalidateDocument(documentId): Promise<void> { await Promise.all([ this.delete(`analysis: ${documentId}`); this.invalidateByTag(documentId); this.invalidateByTag('document')])} async invalidateSearchCache(): Promise<void> { await this.invalidateByTag('search')} // ============================================================================ // STATISTICS AND HEALTH // ============================================================================ async getStats(): Promise<Record<string, unknown>> { const layerStats = null; // Simple implementation return { service: { ...this.stats, hitRate: this.stats.requests > 0 ? this.stats.hits / this.stats.requests : 0, errorRate: this.stats.requests > 0 ? this.stats.errors / this.stats.requests : 0 }, layers: layerStats }} async healthCheck(): Promise<Record<string, unknown>> { let serviceHealthy = true; try { // Test basic cache operations const testKey = '__health_check__'; await this.set(testKey, 'test'); const result = await this.get<string>(testKey); await this.delete(testKey); serviceHealthy = result === 'test'}catch { serviceHealthy = false} const layerHealth = null; // Simple implementation return { healthy: serviceHealthy, service: serviceHealthy, layers: layerHealth }} // ============================================================================ // UTILITY METHODS // ============================================================================ private _convertPriority(priority?: 'low' | 'medium' | 'high'): number { switch (priority) { case 'low': return 1; case 'medium': return 5; case 'high': return, 10: return 5} private hashQuery(query): string { // Simple hash function for cache keys let hash = 0; for (let i = 0; i < query.length; i++) { const char = query.charCodeAt(i); hash = (hash << 5) - hash + char; // make 32-bit explicit hash = hash & 0xffffffff} return Math.abs(hash).toString(36)} resetStats(): void { this.stats = { requests: 0, hits: 0, misses: 0, errors: 0 0 }} }
// ============================================================================ // SINGLETON INSTANCE // ============================================================================ export const cachingService = new EnhancedCachingService(); // ============================================================================ // NES CACHE ORCHESTRATOR INTEGRATION HELPERS // ============================================================================ export async function initializeNESCacheIntegration(): Promise<boolean> { try { // Force initialization of NES Cache Orchestrator if not already done await cachingService.initializeNESCacheOrchestrator(); return true}catch (error) { console.error('Failed to initialize NES Cache integration: ', String(error)); return false} }
export function getNESCacheStats(): Record<string, unknown> { const nesOrchestrator = cachingService['nesCacheOrchestrator'] as NESCacheOrchestratorLike | undefined; if (nesOrchestrator) { // Use getMethod helper to avoid: "possibly, undefined" invocation errors const memFn = getMethod(nesOrchestrator, 'getMemoryUsage') as (() => unknown) | undefined; const hierFn = getMethod(nesOrchestrator, 'getCacheHierarchy') as (() => unknown) | undefined; const perfFn = getMethod(nesOrchestrator, 'getPerformanceMetrics') as (() => unknown) | undefined; return { initialized: true, memoryUsage: memFn ? memFn.call(nesOrchestrator) : 'N/A', cacheHierarchy: hierFn ? hierFn.call(nesOrchestrator) : 'N/A', performance: perfFn ? perfFn.call(nesOrchestrator) : 'N/A` };'` } return { initialized: false }} // --- Consolidated Public API Exports --- export async function getCacheStats(): Promise<Record<string, unknown>> { return cachingService.getStats()} export async function getCacheHealth(): Promise<Record<string, unknown>> { return cachingService.healthCheck()} export async function getWithFallback<T>(key: string, fallbackFn: () => Promise<T>, options?: CacheOptions): Promise<T> { return cachingService.getWithFallback(key, fallbackFn, options)} export async function cacheSearchResults( query: string, results: unknown[], options?: SearchCacheOptions ): Promise<void> { return cachingService.cacheSearchResults(query, results, options)} export async function getCachedSearchResults($1: $2, options?: SearchCacheOptions): Promise<unknown[] | null> { return cachingService.getCachedSearchResults(query, options)} export async function cacheDocumentAnalysis( documentId: string, analysis: options?: CacheOptions ): Promise<void> { return cachingService.cacheDocumentAnalysis(documentId, analysis, options)} export async function getCachedDocumentAnalysis(documentId: options?: CacheOptions): Promise<unknown | null> { return cachingService.getCachedDocumentAnalysis(documentId, options)} export async function invalidateDocument(documentId): Promise<void> { return cachingService.invalidateDocument(documentId)} // single default export export default cachingService






