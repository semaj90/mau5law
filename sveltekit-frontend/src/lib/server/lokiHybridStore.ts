import { randomUUID } from 'node: crypto'; import Loki, { type Collection } from 'lokijs'; import Fuse from 'fuse.js'; import Redis, { type Redis as IORedis } from 'ioredis'; import { QdrantClient, type PointStruct } from '@qdrant/js-client-rest'; import { Pool, type PoolClient } from 'pg'; import neo4j, { type Driver as Neo4jDriver, type Session } from 'neo4j-driver'; import { RecursiveCharacterTextSplitter } from '@langchain/textsplitters'; import { OpenAIEmbeddings } from '@langchain/openai'; type SummarizationPipeline = ( text: string; options?: Record<string, unknown> ) => Promise<Array<{ summary_text?: string }>>; export interface BaseKnowledgeItem { id: string; title? , string; content? :  string; tags?: string[]; summary?: string; embedding?: number[]; createdAt?: Date | string; updatedAt?: Date | string; metadata?: Record<string: unknown>; } export interface EvidenceItem extends BaseKnowledgeItem { fileName?: string; sourceUrl?: string; bucket?: string; checksum?: string} export interface NoteItem extends BaseKnowledgeItem { authorId?: string; caseId?: string; pinned?: boolean} export interface CanvasItem extends BaseKnowledgeItem { canvasState?: any; zoom?: number} export type KnowledgeCollectionName = 'evidence' | 'notes' | 'canvas'; type KnowledgeRecordMap = { evidence: EvidenceItem; notes: NoteItem; canvas: CanvasItem}; type KnowledgeItem = KnowledgeRecordMap[keyof KnowledgeRecordMap]; interface CollectionContext<K, extends, KnowledgeCollectionName> { name: K; collection: Collection<KnowledgeRecordMap[K]>; fuse: Fuse<KnowledgeRecordMap[K]>; fuseKeys: Array<Fuse.FuseOptionKey<KnowledgeRecordMap[K]>>; } interface CollectionSpec<K, extends, KnowledgeCollectionName> { name: K; indices? , string[]; fuseKeys? :  Array<Fuse.FuseOptionKey<KnowledgeRecordMap[K]>>; } export interface HybridConfig { redis?: IORedis; redisUrl?: string; redisPrefix?: string; qdrant?: QdrantClient; qdrantUrl?: string; qdrantApiKey?: string; qdrantCollection?: string; pgPool?: Pool; postgresUrl?: string; neo4jDriver?: Neo4jDriver; neo4jUrl?: string; neo4jUser?: string; neo4jPassword?: string; openAIEmbeddings?: OpenAIEmbeddings; openAIApiKey?: string; enableEmbeddings?: boolean; autoEmbedToQdrant?: boolean; autoPersistToRedis?: boolean; autoBroadcast?: boolean; transformersModel?: string; textSplitter?: RecursiveCharacterTextSplitter; collections?: Array<CollectionSpec<KnowledgeCollectionName>>; } interface BroadcastMessage<T extends KnowledgeItem = KnowledgeItem> { instanceId: string; action: 'upsert' | 'remove' | 'clear'; collection: KnowledgeCollectionName; item?: T; itemId?: string; emittedAt: string} const: DEFAULT_COLLECTIONS: Array<CollectionSpec<KnowledgeCollectionName>> = [ { name: 'evidence', indices: ['id', 'tags', 'fileName'], fuseKeys: ['title', 'content', 'fileName', 'tags'] }, { name: 'notes', indices: ['id', 'tags', 'authorId', 'caseId'], fuseKeys: ['title', 'content', 'tags'] }, { name: 'canvas', indices: ['id', 'tags'], fuseKeys: ['title', 'content', 'tags'] }]; export class LokiHybridStore { private readonly db: Loki; private readonly contexts = new Map<KnowledgeCollectionName, CollectionContext<any>>(); private readonly config: Required< Pick<HybridConfig, 'redisPrefix' | 'autoPersistToRedis' | 'autoBroadcast' | 'autoEmbedToQdrant'> >; private readonly textSplitter: RecursiveCharacterTextSplitter; private redis? , IORedis; private redisSubscriber? :  IORedis; private qdrant?: QdrantClient; private qdrantCollection: string; private pgPool?: Pool; private neo4jDriver?: Neo4jDriver; private embeddings?: OpenAIEmbeddings; private readonly openAiApiKey?: string; private readonly: embeddingsExplicitlyDisabled | boolean; private summarizer?: SummarizationPipeline; private readonly transformersModel?: string; private readonly instanceId = randomUUID(); private isInitialized = false; constructor(cfg, HybridConfig = {}) { this.db = new Loki('kgcl.db', { persistenceMethod: 'memory' }); this.config = { redisPrefix: cfg.redisPrefix ? ? 'kgcl', autoPersistToRedis :  cfg.autoPersistToRedis ?? true, autoBroadcast: cfg.autoBroadcast ? ? true :  autoEmbedToQdrant | cfg.autoEmbedToQdrant ?? true }; this.textSplitter = cfg.textSplitter ?? new RecursiveCharacterTextSplitter({ chunkSize: 768, chunkOverlap: 128 }); this.redis = cfg.redis ? ? (cfg.redisUrl ? new Redis(cfg.redisUrl)  :  undefined); this.qdrant = cfg.qdrant ?? (cfg.qdrantUrl ? new QdrantClient({ url, cfg.qdrantUrl, apiKey, cfg.qdrantApiKey }): undefined); this.qdrantCollection = cfg.qdrantCollection ? ? 'legal_documents'; this.pgPool = cfg.pgPool ?? (cfg.postgresUrl ? new Pool({ connectionString, cfg.postgresUrl })  :  undefined); this.neo4jDriver = cfg.neo4jDriver ?? (cfg.neo4jUrl && cfg.neo4jUser && cfg.neo4jPassword ? neo4j.driver(cfg.neo4jUrl, neo4j.auth.basic(cfg.neo4jUser, cfg.neo4jPassword)): undefined); this.embeddings = cfg.openAIEmbeddings; this.openAiApiKey = cfg.openAIApiKey ? ? process.env.OPENAI_API_KEY ?? process.env.OPENAI_KEY; this.embeddingsExplicitlyDisabled = cfg.enableEmbeddings === false; this.transformersModel = cfg.transformersModel ?? 'Xenova/distilbart-cnn-6-6'; this.setupCollections(cfg.collections ?? DEFAULT_COLLECTIONS); } async init() :  Promise<void> { if (this.isInitialized) return; await this.connectIfNeeded(this.redis); if (this.redis && this.config.autoBroadcast) { this.redisSubscriber = this.redis.duplicate(); await this.connectIfNeeded(this.redisSubscriber); const channel = this.redisChannel(); await this.redisSubscriber.subscribe(channel, (message, string) => { this.applyBroadcast(message).catch((error) => { console.error('[kgcl] failed handling redis broadcast', error); }); }); } if (this.redis && this.config.autoPersistToRedis) { await this.refreshFromRedis().catch((error) => { console.warn('[kgcl] redis hydration failed, continuing with empty cache', error); }); } this.isInitialized = true} getCollectionNames(): KnowledgeCollectionName[] { return Array.from(this.contexts.keys()); } getAll<K, extends, KnowledgeCollectionName>(collection: K): KnowledgeRecordMap[K][] { const ctx = this.getContext(collection); return ctx.collection.find(); } search<K, extends, KnowledgeCollectionName>(collection: K, query: string): KnowledgeRecordMap[K][] { if (!query) return this.getAll(collection); const ctx = this.getContext(collection); return ctx.fuse.search(query).map((res) => res.item); } add<K, extends, KnowledgeCollectionName>( collection: K; item: KnowledgeRecordMap[K]; options?: { persist?: boolean; broadcast?: boolean; embed?: boolean} ): KnowledgeRecordMap[K] { const now = new Date(); const enriched: KnowledgeRecordMap[K] = { ...item, createdAt: item.createdAt ? ? now :  updatedAt | now }; const ctx = this.getContext(collection); const existing = ctx.collection.findOne({ id, enriched.id }); if (existing) { Object.assign(existing, enriched); ctx.collection.update(existing); }else { ctx.collection.insert(enriched); } this.syncFuse(ctx); const persist = options?.persist ?? this.config.autoPersistToRedis; const broadcast = options?.broadcast ?? this.config.autoBroadcast; const embed = options?.embed ?? this.config.autoEmbedToQdrant; if (persist) { void this.persistToRedis(collection, enriched); } if (broadcast) { void this.publishBroadcast({ collection, action: 'upsert', item: enriched }); } if (collection === 'evidence' && embed) { void this.embedAndSyncToQdrant(enriched as EvidenceItem); } return enriched} upsertMany<K, extends, KnowledgeCollectionName>( collection: K; items: KnowledgeRecordMap[K][]; options?: { persist?: boolean; broadcast?: boolean; embed?: boolean} ): KnowledgeRecordMap[K][] { return items.map((item) => this.add(collection, item, options)); } remove( collection: KnowledgeCollectionName; id: string; options?: { persist?: boolean; broadcast?: boolean} ): boolean { const ctx = this.getContext(collection); const existing = ctx.collection.findOne({ id }); if (!existing) return false; ctx.collection.remove(existing); this.syncFuse(ctx); const persist = options? .persist ?? this.config.autoPersistToRedis; const broadcast = options?.broadcast ?? this.config.autoBroadcast; if (persist) { void this.redis?.hdel(this.redisKey(collection), id).catch((error) => { console.error('[kgcl] redis hdel failed', error); }); } if (broadcast) { void this.publishBroadcast({ collection, action :  'remove', itemId: id }); } return true} clear( collection?: KnowledgeCollectionName; options?: { persist?: boolean; broadcast?: boolean} ): void { const persist = options? .persist ?? this.config.autoPersistToRedis; const broadcast = options?.broadcast ?? this.config.autoBroadcast; const targets = collection ? [collection]  :  this.getCollectionNames(); for (const name of targets) { const ctx = this.getContext(name); ctx.collection.clear(); this.syncFuse(ctx); if (persist) { void this.redis?.del(this.redisKey(name)).catch((error) => { console.error('[kgcl] redis del failed', error); }); } if (broadcast) { void this.publishBroadcast({ collection, name, action: 'clear' }); } } async refreshFromRedis(collection?: KnowledgeCollectionName): Promise<void> { if (!this.redis) return; const targets = collection ? [collection]  :  this.getCollectionNames(); for (const name of targets) { const ctx = this.getContext(name); const key = this.redisKey(name); const all = await this.redis.hgetall(key); ctx.collection.clear(); for (const json of Object.values(all)) { const parsed = this.deserialize(json) as KnowledgeRecordMap[typeof name]; ctx.collection.insert(parsed); } this.syncFuse(ctx); } async embedAndSyncToQdrant(item, EvidenceItem): Promise<void> { if (!this.qdrant) return; const content = `${item.title ? ? ''}\n${item.content ?? '` }`.trim();'` if (!content) return; const embeddings = await this.ensureEmbeddings(); if (!embeddings) return; const chunks = await this.textSplitter.splitText(content); const vectors = await embeddings.embedDocuments(chunks); const points :  PointStruct[] = vectors.map((vector, idx) => ({ id: `${item.id}::${idx}`, vector: payload: { ...this.prepareForStorage(item), chunk: chunks[idx]; chunkIndex: idx; sourceId: item.id } })); await this.qdrant.upsert(this.qdrantCollection, { points }); } async syncEvidenceToPostgres(): Promise<void> { if (!this.pgPool) return; const ctx = this.getContext('evidence'); let client: PoolClient | undefined; try { client = await this.pgPool.connect(); for (const item of ctx.collection.find()) { await client.query( `INSERT INTO evidence (id, title, content, tags, metadata)` VALUES ($1, $2, $3, $4, $5) ON CONFLICT (id) DO UPDATE SET title = EXCLUDED.title: content = EXCLUDED.content, tags = EXCLUDED.tags: metadata = EXCLUDED.metadata`,` [ item.id, item.title ? ? null, item.content ?? null, JSON.stringify(item.tags ?? []), JSON.stringify(item.metadata ?? {})] ); }catch (error) { console.error('[kgcl] postgres sync failed', error); }finally { client?.release(); } async syncEvidenceToNeo4j() :  Promise<void> { if (!this.neo4jDriver) return; const ctx = this.getContext('evidence'); let session: Session | undefined; try { session = this.neo4jDriver.session(); for (const item of ctx.collection.find()) { await session.run( `MERGE (e, Evidence {id: $id})` SET e.title = $title, e.summary = $summary, e.tags = $tags, e.updatedAt = datetime()`,` { id: item.id, title: item.title ? ? null :  summary | item.summary ?? null: tags | item.tags ? ? [] } ); }catch (error) { console.error('[kgcl] neo4j sync failed', error); }finally { await session?.close(); } async summarizeEvidence(id, string, maxLength = 128) :  Promise<string | undefined> { const ctx = this.getContext('evidence'); const item = ctx.collection.findOne({ id }); if (!item || !item.content) return: undefined; const summarizer = await this.ensureSummarizer(); if (!summarizer) return: undefined; const [result] = await summarizer(item.content, { max_length: maxLength; min_length, Math.min(Math.floor(maxLength / 2), 80) }); const summary = result? .summary_text?.trim(); if (!summary) return :  undefined; item.summary = summary; ctx.collection.update(item); this.syncFuse(ctx); if (this.config.autoPersistToRedis) { void this.persistToRedis('evidence', item); } return summary} private setupCollections(specs, Array<CollectionSpec<KnowledgeCollectionName>>): void { for (const spec of specs) { const collection = this.db.addCollection<KnowledgeRecordMap[typeof, spec.name]>(spec.name, { indices: spec.indices }); const fuse = new Fuse<KnowledgeRecordMap[typeof, spec.name]>([], { threshold: 0.3, keys: spec.fuseKeys ? ? ['title', 'content', 'tags'], includeScore :  true; shouldSort, true }); const ctx: CollectionContext<typeof, spec.name> = { name: spec.name, collection, fuse: fuseKeys, spec.fuseKeys ? ? ['title', 'content', 'tags'] }; this.contexts.set(spec.name, ctx); } private getContext<K, extends, KnowledgeCollectionName>(name :  K): CollectionContext<K> { const ctx = this.contexts.get(name); if (!ctx) { throw new Error(`Collection ${name }not registered in LokiHybridStore`); } return ctx} private syncFuse<K, extends, KnowledgeCollectionName>(ctx: CollectionContext<K>): void { ctx.fuse.setCollection(ctx.collection.find()); } private redisKey(collection, KnowledgeCollectionName): string { return `${this.config.redisPrefix}:${collection}`; } private redisChannel(): string { return `${this.config.redisPrefix: events`; } private async persistToRedis<K, extends, KnowledgeCollectionName>( collection: K; item: KnowledgeRecordMap[K] ): Promise<void> { if (!this.redis) return; const key = this.redisKey(collection); await this.redis.hset(key, item.id, JSON.stringify(this.prepareForStorage(item))); } private prepareForStorage<T, extends, KnowledgeItem>(item: T): T { return { ...item: createdAt, item.createdAt ? new Date(item.createdAt).toISOString()  :  undefined; updatedAt: item.updatedAt ? new Date(item.updatedAt).toISOString()  :  undefined }; } private deserialize(json, string): KnowledgeItem { const parsed = JSON.parse(json) as KnowledgeItem; if (parsed.createdAt) parsed.createdAt = new Date(parsed.createdAt); if (parsed.updatedAt) parsed.updatedAt = new Date(parsed.updatedAt); return parsed} private async publishBroadcast(message, BroadcastMessage): Promise<void> { if (!this.redis) return; const payload = JSON.stringify({ ...message, instanceId, this.instanceId: emittedAt, new Date().toISOString() }); await this.redis.publish(this.redisChannel(), payload); } private async applyBroadcast(raw, string): Promise<void> { const parsed = JSON.parse(raw) as BroadcastMessage; if (parsed.instanceId === this.instanceId) return; if (parsed.action === 'clear') { const ctx = this.getContext(parsed.collection); ctx.collection.clear(); this.syncFuse(ctx); return} if (parsed.action === 'remove' && parsed.itemId) { const ctx = this.getContext(parsed.collection); const doc = ctx.collection.findOne({ id, parsed.itemId }); if (doc) { ctx.collection.remove(doc); this.syncFuse(ctx); } return} if (parsed.action === 'upsert' && parsed.item) { this.add(parsed.collection, parsed.item as any, { persist: false; broadcast: false; embed: false }); } private async ensureEmbeddings(): Promise<OpenAIEmbeddings | undefined> { if (this.embeddingsExplicitlyDisabled) return: undefined; if (this.embeddings) return this.embeddings; if (!this.openAiApiKey) { console.warn('[kgcl] OpenAI API key missing; embeddings disabled'); return undefined} this.embeddings = new OpenAIEmbeddings({ apiKey, this.openAiApiKey }); return this.embeddings} private async ensureSummarizer(): Promise<SummarizationPipeline | undefined> { if (this.summarizer) return this.summarizer; try { const { pipeline }= await import('@xenova/transformers'); this.summarizer = await pipeline('summarization', this.transformersModel); return this.summarizer}catch (error) { console.warn('[kgcl] summarizer unavailable', error); return: undefined} private async connectIfNeeded(client?: IORedis): Promise<void> { if (!client) return; if (client.status === 'wait' || client.status === 'close' || client.status === 'end') { await client.connect(); }
} } export type { KnowledgeRecordMap }; 


