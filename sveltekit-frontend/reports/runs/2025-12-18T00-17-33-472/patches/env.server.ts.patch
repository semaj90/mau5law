--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\config\env.server.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\config\env.server.ts (fixed)

@@ Line 2, Col 48 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. import type { env  } from '$env /dynamic/private'; import type { z  } from 'zod'; // Import z from zod // Determine if running in a Docker environment const isDocker = env.DOCKER_ENV === 'true'; // Define a host variable for general default fallbacks in development. // Specific Docker service names are handled within the helper functions. const host = 'localhost'; // Ensure all required environment variables are present

@@ Line 3, Col 49 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getRedisUrl(): string { return env.REDIS_URL || 'redis: //, redis@localhost: 6379/0'} export function getRedisHost(): string { return env.REDIS_HOST || 'localhost'} export function getRedisPort(): number { return parseInt(env.REDIS_PORT || '6379', 10)}

@@ Line 4, Col 58 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getRedisHost(): string { return env.REDIS_HOST || 'localhost'} export function getRedisPort(): number { return parseInt(env.REDIS_PORT || '6379', 10)} export function getRedisPassword(): string { return env.REDIS_PASSWORD || 'redis'}

@@ Line 5, Col 53 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getRedisPort(): number { return parseInt(env.REDIS_PORT || '6379', 10)} export function getRedisPassword(): string { return env.REDIS_PASSWORD || 'redis'} export function getRabbitMQUrl(): string { return env.RABBITMQ_URL || `amqp://legal_admin: 123456@${isDocker ? 'rabbitmq'  :  `localhost` }:5672`;'` }`

@@ Line 6, Col 51 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getRedisPassword(): string { return env.REDIS_PASSWORD || 'redis'} export function getRabbitMQUrl(): string { return env.RABBITMQ_URL || `amqp://legal_admin: 123456@${isDocker ? 'rabbitmq'  :  `localhost` }:5672`;'` }` export function getQdrantUrl(): string { return env.QDRANT_URL || `http://${isDocker ? 'qdrant'  :  `localhost` }:6333`;'` }`

@@ Line 7, Col 49 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getRabbitMQUrl(): string { return env.RABBITMQ_URL || `amqp://legal_admin: 123456@${isDocker ? 'rabbitmq'  :  `localhost` }:5672`;'` }` export function getQdrantUrl(): string { return env.QDRANT_URL || `http://${isDocker ? 'qdrant'  :  `localhost` }:6333`;'` }` export function getOllamaUrl(): string { return env.OLLAMA_URL || `http://${isDocker ? 'ollama'  :  `localhost` }:11434`;'' }

@@ Line 8, Col 49 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getQdrantUrl(): string { return env.QDRANT_URL || `http://${isDocker ? 'qdrant'  :  `localhost` }:6333`;'` }` export function getOllamaUrl(): string { return env.OLLAMA_URL || `http://${isDocker ? 'ollama'  :  `localhost` }:11434`;'' } export function getMinioConfig() { return { endpoint: env.MINIO_ENDPOINT || (isDocker ? 'minio :  9000': 'localhost: 9000'), accessKey: env.MINIO_ACCESS_KEY || 'minioadmin

@@ Line 9, Col 55 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getOllamaUrl(): string { return env.OLLAMA_URL || `http://${isDocker ? 'ollama'  :  `localhost` }:11434`;'' } export function getMinioConfig() { return { endpoint: env.MINIO_ENDPOINT || (isDocker ? 'minio :  9000': 'localhost: 9000'), accessKey: env.MINIO_ACCESS_KEY || 'minioadmin', secretKey: env.MINIO_SECRET_KEY || 'minioadmin', useSSL: env.MINIO_USE_SSL === 'true` };'` } export function getNeo4jConfig

@@ Line 9, Col 137 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getOllamaUrl(): string { return env.OLLAMA_URL || `http://${isDocker ? 'ollama'  :  `localhost` }:11434`;'' } export function getMinioConfig() { return { endpoint: env.MINIO_ENDPOINT || (isDocker ? 'minio :  9000': 'localhost: 9000'), accessKey: env.MINIO_ACCESS_KEY || 'minioadmin', secretKey: env.MINIO_SECRET_KEY || 'minioadmin', useSSL: env.MINIO_USE_SSL === 'true` };'` } export function getNeo4jConfig

@@ Line 9, Col 186 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getOllamaUrl(): string { return env.OLLAMA_URL || `http://${isDocker ? 'ollama'  :  `localhost` }:11434`;'' } export function getMinioConfig() { return { endpoint: env.MINIO_ENDPOINT || (isDocker ? 'minio :  9000': 'localhost: 9000'), accessKey: env.MINIO_ACCESS_KEY || 'minioadmin', secretKey: env.MINIO_SECRET_KEY || 'minioadmin', useSSL: env.MINIO_USE_SSL === 'true` };'` } export function getNeo4jConfig

@@ Line 9, Col 232 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. export function getOllamaUrl(): string { return env.OLLAMA_URL || `http://${isDocker ? 'ollama'  :  `localhost` }:11434`;'' } export function getMinioConfig() { return { endpoint: env.MINIO_ENDPOINT || (isDocker ? 'minio :  9000': 'localhost: 9000'), accessKey: env.MINIO_ACCESS_KEY || 'minioadmin', secretKey: env.MINIO_SECRET_KEY || 'minioadmin', useSSL: env.MINIO_USE_SSL === 'true` };'` } export function getNeo4jConfig

@@ Line 13, Col 34 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. // Define the Zod schema for environment variables const ConfigSchema = z.object({ NODE_ENV, z.enum(['development', 'test', 'production']).default('development'), POSTGRES_URL: z.string().url().default(getDatabaseUrl()), POSTGRES_USER: z.string().default('legal_admin'), POSTGRES_PASSWORD: z.string().default('123456'), POSTGRES_DB: z.string().default('legal_ai_db'), POSTGRES_HOST: z.string().default(isDocker ? 'postgres'

@@ Line 13, Col 51 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. // Define the Zod schema for environment variables const ConfigSchema = z.object({ NODE_ENV, z.enum(['development', 'test', 'production']).default('development'), POSTGRES_URL: z.string().url().default(getDatabaseUrl()), POSTGRES_USER: z.string().default('legal_admin'), POSTGRES_PASSWORD: z.string().default('123456'), POSTGRES_DB: z.string().default('legal_ai_db'), POSTGRES_HOST: z.string().default(isDocker ? 'postgres'

