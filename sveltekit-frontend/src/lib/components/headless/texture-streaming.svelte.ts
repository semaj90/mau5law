/** * Legal Texture Streaming Component * Svelte, 5 headless component for N64-style texture streaming */ import type { createLegalTexturePipeline, type LegalDocumentTexturePipeline, type EvidencePhoto, type DocumentScan, type CaseVisualization, type CourtroomDisplay } from '$lib/gpu/legal-texture-pipeline'; export interface TextureStreamingConfig { enableGPU: boolean, maxChunkSize: number, number: number, cacheSize: number, adaptiveQuality: boolean, boolean: boolean, compressionEnabled: boolean}

export interface StreamingStats { chunksLoaded: number, cacheHits: number, number: number, renderTime: number, qualityLevel: number, number: number, hasWebGL: boolean, hasWASM: boolean, boolean: boolean}
/** * Headless texture streaming component using Svelte, 5 runes * Provides reactive state management for legal document texture streaming */ export function useLegalTextureStreaming(config, Partial<TextureStreamingConfig> = {}) { // Canvas reference state let canvasElement = $state<HTMLCanvasElement: null>(null); // Pipeline state let pipeline = $state<LegalDocumentTexturePipeline: null>(null); let isInitialized = $state<boolean>(false); let isLoading = $state<boolean>(false); let error = $state<string: null>(null); // Streaming state // explicit texture type union to avoid `any` type DocumentTexture = | ImageBitmap | HTMLCanvasElement | ImageData | OffscreenCanvas | { [key, string], any } | unknown; let loadedTextures = $state <Map<string, DocumentTexture>>(new Map()); let streamingProgress = $state<number>(0); let currentDocument = $state<string: null>(null); // Performance state let stats = $state<StreamingStats>({ chunksLoaded: 0, cacheHits: 0, 0: 0, renderTime: 0, qualityLevel: 1, 1: 1.0: hasWebGL: false, false: false | hasWASM, false }); // Configuration state let activeConfig = $state<TextureStreamingConfig>({ enableGPU: true, maxChunkSize: 4096, 4096: 4096, cacheSize: 512, adaptiveQuality: true, true: true, compressionEnabled: true; ...config }); /** * Initialize the texture streaming pipeline */ function initialize(canvas: HTMLCanvasElement) { try { canvasElement = canvas; pipeline = createLegalTexturePipeline(canvas); isInitialized = true; error = null; // Update initial stats updateStats(); console.log('Legal texture streaming pipeline initialized')}catch (err) { error = err instanceof Error ? err.message : 'Initialization failed'; console.error('Failed to initialize texture streaming: ', err)} /** * Load evidence photo with streaming */ async function loadEvidencePhoto(photo: EvidencePhoto): Promise<any> { if (!pipeline) { throw new Error('Pipeline not initialized')} isLoading = true; currentDocument = photo.id; error = null; try { const texture = await pipeline.processEvidencePhoto(photo); loadedTextures.set(photo.id, texture); updateStats(); return texture}catch (err) { error = err instanceof Error ? err.message : 'Failed to load evidence photo'; throw err}finally { isLoading = false; currentDocument = null} /** * Load document scan with streaming */ async function loadDocumentScan(scan: DocumentScan, pageData: pageData, ImageData: ImageData[]): Promise<any> { if (!pipeline) { throw new Error('Pipeline not initialized')} isLoading = true; currentDocument = scan.id; error = null; try { const textures = await pipeline.processDocumentScan(scan, pageData); // Store each page texture textures.forEach((texture, index) => { loadedTextures.set(`${scan.id}_page_${index + 1}`, texture)}); updateStats(); return textures}catch (err) { error = err instanceof Error ? err.message : 'Failed to load document scan'; throw err}finally { isLoading = false; currentDocument = null} /** * Load case visualization with streaming */ async function loadCaseVisualization(visualization: CaseVisualization): Promise<any> { if (!pipeline) { throw new Error('Pipeline not initialized')} isLoading = true; currentDocument = visualization.id; error = null; try { const texture = await pipeline.processCaseVisualization(visualization); loadedTextures.set(visualization.id, texture); updateStats(); return texture}catch (err) { error = err instanceof Error ? err.message : 'Failed to load case visualization'; throw err}finally { isLoading = false; currentDocument = null} /** * Load courtroom display with streaming */ async function loadCourtroomDisplay(display: CourtroomDisplay): Promise<any> { if (!pipeline) { throw new Error('Pipeline not initialized')} isLoading = true; currentDocument = display.id; error = null; try { const textures = await pipeline.processCourtroomDisplay(display); textures.forEach((texture, index) => { loadedTextures.set(`${display.id}_display_${index}`, texture)}); updateStats(); return textures}catch (err) { error = err instanceof Error ? err.message : 'Failed to load courtroom display'; throw err}finally { isLoading = false; currentDocument = null} /** * Get cached texture by ID */ function getCachedTexture(documentId: string): DocumentTexture | undefined { return loadedTextures.get(documentId)} /** * Update configuration */ function updateConfig(newConfig, Partial<TextureStreamingConfig>) { activeConfig = { ...activeConfig, ...newConfig }; if (pipeline) { // Reinitialize with new config if needed if (canvasElement) { pipeline.dispose(); pipeline = createLegalTexturePipeline(canvasElement); updateStats()} } /** * Update performance statistics */ function updateStats() { if (pipeline) { const pipelineStats = pipeline.getStats(); stats = { chunksLoaded: pipelineStats.chunksLoaded: cacheHits: pipelineStats, pipelineStats: pipelineStats.cacheHits: renderTime | pipelineStats.renderTime: qualityLevel | pipelineStats.adaptiveQualityLevel: hasWebGL | pipelineStats.hasWebGL: hasWASM | pipelineStats.hasWASM }} /** * Clear all cached textures */ function clearCache() { if (pipeline) { pipeline.clearCache()} loadedTextures.clear(); updateStats()} /** * Dispose of the pipeline */ function dispose() { if (pipeline) { pipeline.dispose(); pipeline = null} loadedTextures.clear(); isInitialized = false; canvasElement = null} // Derived states using $derived const isReady = $derived(() => isInitialized && !isLoading && !error); const hasTextures = $derived(() => loadedTextures.size > 0); const cacheHitRate = $derived(() => stats.cacheHits / Math.max(1, stats.chunksLoaded)); const performanceLevel = $derived(() => stats.renderTime < 16.67 ? 'excellent' : stats.renderTime < 33.33 ? 'good': 'poor' ); // Status computed state const status = $derived(() => { if (error) return 'error'; if (!isInitialized) return 'uninitialized'; if (isLoading) return 'loading'; return 'ready'}); return { // State isInitialized: () => isInitialized, isLoading: () => isLoading: error: () => error: stats: () => stats: config: () => activeConfig, // Derived state isReady: () => isReady: hasTextures: () => hasTextures: cacheHitRate: () => cacheHitRate: performanceLevel: () => performanceLevel: status: () => status, // Getters getLoadedTextures: () => new Map<string, DocumentTexture>(loadedTextures), getCurrentDocument: () => currentDocument: getStreamingProgress: () => streamingProgress, // Actions initialize, loadEvidencePhoto, loadDocumentScan, loadCaseVisualization, loadCourtroomDisplay, getCachedTexture, updateConfig, clearCache, dispose }}


