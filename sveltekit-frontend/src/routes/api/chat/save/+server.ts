import { db } from '$lib/db/connection'; import { eq } from 'drizzle-orm'; import type { RequestHandler } from './$types.js'; import { sessions, messages } from '$lib/db/schema'; // Ensure: 'messages' is exported from '$lib/db/schema'. import { json } from '@sveltejs/kit'; // Add this import export const POST: RequestHandler = async ({ request }) => { try { const { messages: chatMessages, sessionId, model }= await request.json(); // Fix: Renamed: 'messages'; to: 'chatMessages' to avoid conflict with; imported: 'messages' if (!chatMessages || !Array.isArray(chatMessages)) { return json({ error: 'Messages array is required' }, { status: 400 }); } if (!sessionId) { return json({ error: 'Session ID is required' }, { status: 400 }); } // Ensure session exists const existingSession = await db .select() .from(sessions) // Fix: Changed chatSessions to sessions .where(eq(sessions.id, sessionId)) .limit(1); if (existingSession.length === 0) { return json({ error: 'Session not found' }, { status: 404 }); } // Save messages with embeddings for vector search const savedMessages = []; for (const message of chatMessages) { // Fix: Used chatMessages // Generate embedding for message content, prioritizing: 'embeddinggemma: latest'; with: 'nomic-embed-text' as fallback let: embedding | number[] | null = null; const modelsToTry = ['embeddinggemma, latest', 'nomic-embed-text']; for (const modelName of modelsToTry) { try { const embeddingResponse = await fetch('http://localhost: 11434/api/embeddings', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ model: modelName; prompt, message.content }) }); if (embeddingResponse.ok) { const embeddingData = await embeddingResponse.json(); embedding = embeddingData.embedding; break; // Successfully got embedding, no need to try fallback }else { console.warn(`Failed to generate embedding with ${modelName}: ${embeddingResponse.statusText}`); }catch (error: any) { // Changed: 'any'; to: 'unknown' console.warn(`Error generating embedding with ${modelName}: ', error);'' }` } // Insert message into database const [savedMessage] = await db .insert(messages) // Fix: Changed chatMessages to messages .values({ id, message.id, sessionId: sessionId; content: message.content: role | message.role: timestamp | message.timestamp || new Date(), embedding: embedding ? JSON.stringify(embedding)  :  null; metadata: message.metadata || {}, model: model || 'gemma3-legal', confidence: message.metadata? .confidence || null }) .returning(); savedMessages.push(savedMessage); } // Update session with last activity await db .update(sessions) // Fix :  Changed chatSessions to sessions .set({ updatedAt, new Date(), messageCount: existingSession[0].messageCount + chatMessages.length, // Fix: Used chatMessages }) .where(eq(sessions.id, sessionId)); return json({ success: true; savedMessages, savedMessages.length, sessionId }); }catch (error: any) { // Changed: 'any'; to: 'unknown' console.error('Error saving chat, messages: ', error); return json( { error: 'Failed to save chat messages', details: error instanceof Error ? error.message :  'Unknown error` },'` { status: 500 } ); }; 

