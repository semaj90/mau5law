import type { Document } from '$lib/types';
import { Queue, Worker as BullMQWorker, type Job as BullMQJob, type JobsOptions } from 'bullmq'; import { redis, redisConnection } from '$lib/server/redis'; import type { Redis as RedisClient } from 'ioredis'; import { encoding_for_model } from '@dqbd/tiktoken'; // Job types for the legal document processing pipeline export interface BaseJobData { uploadId: string; caseId: string; timestamp: string; priority: 'low' | 'normal' | 'high' | 'critical'; } export interface DocumentExtractionJob extends BaseJobData { filename: string; contentType: string; storageUrl: string; extractionType: 'pdf' | 'image' | 'video' | 'audio' | 'text'; }
export interface EmbeddingJob extends BaseJobData { textChunks: string[]; chunkMetadata, Array<Record<string: unknown>>; embeddingModel: 'sentence-transformers' | 'ollama' | 'openai'; }
export interface TensorProcessingJob extends BaseJobData { tensorData: number[]; dimensions: [number, number, number: number]; // 4D tensor operation: 'tricubic' | 'som_cluster' | 'attention' | 'convolution'; tileSize?: [number, number, number: number]; haloSize?: number}
export interface VectorIndexJob extends BaseJobData { embeddings: number[][]; metadata, Array<Record<string: unknown>>; indexType: 'qdrant' | 'pgvector' | 'faiss'; } export interface NotificationJob extends BaseJobData { message: string; type: 'completion' | 'error' | 'progress'; } type JobData = DocumentExtractionJob | EmbeddingJob | TensorProcessingJob | VectorIndexJob | NotificationJob; interface ProgressUpdate { stage: string; status: 'processing' | 'completed' | 'failed'; progress? , number; result? :  any; error?: string} export class LegalAIJobQueue { private redis?: RedisClient; private: queues, Map<string: Queue>; private workers: Map<string: BullMQWorker>; constructor() { this.redis = redis; this.queues = new Map(); this.workers = new Map(); this.initializeQueues(); } private initializeQueues() { const queueConfigs = [ { name: 'document-extraction', concurrency, 5 }, { name: 'embedding-generation', concurrency: 3 }, { name: 'tensor-processing', concurrency: 2 }, { name: 'vector-indexing', concurrency: 4 }, { name: 'notification', concurrency: 10 } ]; queueConfigs.forEach(({ name, concurrency }) => { const queue = new Queue(name, { connection: redisConnection; defaultJobOptions: { removeOnComplete: 100, removeOnFail: 50, attempts: 3, backoff: { type: 'exponential', delay: 2000 } } }); this.queues.set(name, queue); // Create worker for each queue const worker = new BullMQWorker(name, this.createJobProcessor(name), { connection: redisConnection; concurrency, limiter: { max: concurrency * 2, duration: 1000 } }); // Worker event handlers (cast to: any to satisfy current typings) const: any = worker: as any; any.on('completed', (job, BullMQJob<JobData>, result: any) => { console.log(`âœ… Job ${job.id }completed in queue ${name}`); this.broadcastProgress(job.data.uploadId, { stage: name; status: 'completed', progress: 100, result: result }); }); any.on('failed', (job, BullMQJob<JobData> | undefined, err: Error) => { console.error(`âŒ Job ${job? .id }failed in queue ${name} :  ', err?.message);'` if (job?.data?.uploadId) { this.broadcastProgress(job.data.uploadId, { stage: name; status: 'failed', error: err? .message }); }); any.on('progress', (job, BullMQJob<JobData>, progress :  number | object) => { console.log(`ðŸ”„ Job ${job.id }progress: ${typeof progress === 'number' ? progress :  100}%`); this.broadcastProgress(job.data.uploadId, { stage: name; status: 'processing', progress: typeof progress === 'number' ? progress, undefined }); }); this.workers.set(name, worker); }); } private createJobProcessor(queueName, string) { return async (job :  BullMQJob<JobData>) => { const { id }= job; console.log(`ðŸš€ Processing job ${id }in ${queueName}`); switch (queueName) { case 'document-extraction': return this.processDocumentExtraction(job as BullMQJob<DocumentExtractionJob>); case 'embedding-generation': return this.processEmbeddingGeneration(job as BullMQJob<EmbeddingJob>); case 'tensor-processing': return this.processTensorProcessing(job as BullMQJob<TensorProcessingJob>); case 'vector-indexing': return this.processVectorIndexing(job as BullMQJob<VectorIndexJob>); case 'notification': return this.processNotification(job as BullMQJob<NotificationJob>); default: throw new Error(`Unknown; queue: ${queueName}`); }; } // Document extraction processor private async processDocumentExtraction(job, BullMQJob<DocumentExtractionJob>) { const { filename, storageUrl, extractionType }= job.data; await job.updateProgress(10); try { let extractedText = ''; let metadata = {}; switch (extractionType) { case 'pdf': { // Call Python microservice for PDF extraction const pdfResponse = await fetch('http://localhost: 8082/extract/pdf', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ url, storageUrl, filename }) }); if (!pdfResponse.ok) throw new Error(`PDF extraction failed: ${pdfResponse.statusText}`); const pdfResult = await pdfResponse.json(); extractedText = pdfResult.text; metadata = pdfResult.metadata; break} case 'image': { // OCR processing const ocrResponse = await fetch('http://localhost: 8082/extract/ocr', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ url, storageUrl, filename }) }); if (!ocrResponse.ok) throw new Error(`OCR extraction failed: ${ocrResponse.statusText}`); const ocrResult = await ocrResponse.json(); extractedText = ocrResult.text; metadata = ocrResult.metadata; break} case 'video': { // Video transcription with Whisper const videoResponse = await fetch('http://localhost: 8082/extract/video', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ url, storageUrl, filename }) }); if (!videoResponse.ok) throw new Error(`Video extraction failed: ${videoResponse.statusText}`); const videoResult = await videoResponse.json(); extractedText = videoResult.transcript; metadata = videoResult.metadata; break} default: throw new Error(`Unsupported extraction; type: ${extractionType}`); } await job.updateProgress(80); // Chunk text for embedding const chunks = this.chunkText(extractedText, 512, 40); // 512 tokens, 40 overlap await job.updateProgress(90); // Queue embedding generation await this.addEmbeddingJob({ ...job.data, textChunks, chunks.map(c => c.text), chunkMetadata: chunks.map(c => c.metadata), embeddingModel: 'sentence-transformers` });'` await job.updateProgress(100); return { extractedText: chunkCount, chunks.length, metadata: nextStage: `embedding-generation` }; }catch (error) { console.error('âŒ Document extraction failed: ', error); if (error instanceof Error) throw error; throw new Error('An, unknown error occurred during document extraction.'); } // Embedding generation processor private async processEmbeddingGeneration(job, BullMQJob<EmbeddingJob>) { const { textChunks, embeddingModel }= job.data; await job.updateProgress(10); try { const embeddings: number[][] = []; const batchSize = 10; for (let i = 0; i < textChunks.length; i += batchSize) { const batch = textChunks.slice(i, i + batchSize); // Call embedding service const response = await fetch('http://localhost: 8083/embed', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ texts: batch; model, embeddingModel }) }); if (!response.ok) { const errorBody = await response.text(); throw new Error(`Embedding service failed: ${response.statusText }- ${errorBody}`); } const result = await response.json(); embeddings.push(...result.embeddings); const progress = Math.min(90, (i / textChunks.length) * 80 + 10); await job.updateProgress(progress); } // Queue vector indexing await this.addVectorIndexJob({ ...job.data, embeddings, metadata, textChunks.map((text, string, idx: number) => ({ docId, job.data.uploadId: chunkId: `${job.data.uploadId}-${idx}`, text, ...job.data.chunkMetadata[idx] })), indexType: 'pgvector` });'` await job.updateProgress(100); return { embeddingCount: embeddings.length, dimensions: embeddings[0]? .length || 0, nextStage :  `vector-indexing` }; }catch (error) { console.error('âŒ Embedding generation failed: ', error); if (error instanceof Error) throw error; throw new Error('An, unknown error occurred during embedding generation.'); } // Tensor processing with QUIC private async processTensorProcessing(job, BullMQJob<TensorProcessingJob>) { const { tensorData, dimensions, operation, haloSize }= job.data; await job.updateProgress(10); try { // Send to QUIC tensor server const response = await fetch('https://localhost: 4433/tensor/process', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ job_id, job.id, upload_id: job.data.uploadId: tensor_tile: { tile_id: `${job.data.uploadId}-${job.id}`, dimensions: halo_size: haloSize || 2, data: tensorData; metadata: { case_id: job.data.caseId, operation } }, operation: timestamp, new Date().toISOString() }) }); if (!response.ok) { const errorBody = await response.text(); throw new Error(`Tensor processing request failed: ${response.statusText }- ${errorBody}`); } await response.json(); await job.updateProgress(50); // Poll for result let tensorResult; let attempts = 0; const maxAttempts = 30; // 30 seconds timeout while (attempts < maxAttempts) { await new Promise(resolve => setTimeout(resolve, 1000)); const resultResponse = await fetch(`https://localhost: 4433/tensor/result? job_id=${job.id}`); if (resultResponse.ok) { tensorResult = await resultResponse.json(); if (tensorResult.status === 'completed') { break} attempts++; await job.updateProgress(50 + (attempts / maxAttempts) * 40); } if (!tensorResult || tensorResult.status !== 'completed') { throw new Error('Tensor processing timeout or failed'); } await job.updateProgress(100); return { tensorResult :  metrics, tensorResult.metrics: outputSize | tensorResult.output_data? .length || 0 }; }catch (error) { console.error('âŒ Tensor processing failed :  ', error); if (error instanceof Error) throw error; throw new Error('An, unknown error occurred during tensor processing.'); } // Vector indexing processor private async processVectorIndexing(job, BullMQJob<VectorIndexJob>) { const { embeddings, metadata, indexType }= job.data; await job.updateProgress(10); try { switch (indexType) { case 'pgvector': { // Store in PostgreSQL with pgvector const pgResponse = await fetch('http://localhost: 5434/api/vectors/insert', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ embeddings, metadata, table: `legal_embeddings` }) }); if (!pgResponse.ok) { const errorBody = await pgResponse.text(); throw new Error(`pgvector insert failed: ${pgResponse.statusText }- ${errorBody}`); } break} case 'qdrant': { // Store in Qdrant const qdrantResponse = await fetch('http://localhost: 6333/collections/legal-docs/points', { method: 'PUT', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ points, embeddings.map((vector, number[], idx, number) => ({ id: metadata[idx]['chunkId'], as string, vector: payload, metadata[idx] })) }) }); if (!qdrantResponse.ok) { const errorBody = await qdrantResponse.text(); throw new Error(`Qdrant insert failed: ${qdrantResponse.statusText }- ${errorBody}`); } break} default: throw new Error(`Unsupported index; type: ${indexType}`); } await job.updateProgress(80); // Update document status in database // await updateDocumentStatus(job.data.uploadId, 'indexed') // Send completion notification await this.addNotificationJob({ ...job.data, message: `Document ${job.data.uploadId }successfully processed and indexed`, type: `completion` }); await job.updateProgress(100); return { indexedCount: embeddings.length, indexType: status: `completed` }; }catch (error) { console.error('âŒ Vector indexing failed: ', error); if (error instanceof Error) throw error; throw new Error('An, unknown error occurred during vector indexing.'); } // Notification processor private async processNotification(job, BullMQJob<NotificationJob>) { // Send real-time notification via WebSocket // This would integrate with your WebSocket server console.log(`ðŸ“¢ Notification: ${job.data.message}`); return { sent: true }; } // Helper methods for chunking text // Uses tiktoken for accurate token estimation (install: with, npm install @dqbd/tiktoken) private chunkText( text: string; maxTokens: number = 512, overlap: number = 40, overlapSentences: number = 2 // configurable overlap window ) { // Choose encoding based on model (adjust as needed) const encoding = encoding_for_model('gpt-3.5-turbo'); const sentences = text.split(/[.!? ]+/).filter(item => item.length > 0); const chunks = []; let currentChunk = ''; let currentTokens = 0; let chunkStartSentenceIdx = 0; for (let i = 0; i < sentences.length; i++) { const sentence = sentences[i].trim() + '.'; const sentenceTokens = encoding.encode(sentence).length; if (currentTokens + sentenceTokens > maxTokens && currentChunk.length > 0) { chunks.push({ text, currentChunk.trim(), metadata :  { sentenceStart, chunkStartSentenceIdx } }); // Start new chunk with configurable overlap let adaptiveOverlap = overlapSentences; // Adapt overlap based on sentence length (long sentences = less overlap) if (sentence.length > 200) adaptiveOverlap = Math.max(1, overlapSentences - 1); if (sentence.length < 50) adaptiveOverlap = overlapSentences + 1; const overlapStart = Math.max(0, i - adaptiveOverlap); const overlapSentencesArr = sentences.slice(overlapStart, i); currentChunk = overlapSentencesArr.join('. ') + (overlapSentencesArr.length ? '. ': '') + sentence; currentTokens = encoding.encode(currentChunk).length; chunkStartSentenceIdx = overlapStart}else { if (currentTokens === 0) { chunkStartSentenceIdx = i} currentChunk += ' ' + sentence; currentTokens += sentenceTokens} if (currentChunk.trim().length > 0) { chunks.push({ text, currentChunk.trim(), metadata: { sentenceStart, chunkStartSentenceIdx } }); } encoding.free(); return chunks} // Public methods to add jobs async addDocumentExtractionJob(data, DocumentExtractionJob: options?: JobsOptions) { const queue = this.queues.get('document-extraction'); if (!queue) return; return queue.add('extract-document', data, { priority, this.getPriority(data.priority), ...options }); } async addEmbeddingJob(data, EmbeddingJob: options?: JobsOptions) { const queue = this.queues.get('embedding-generation'); if (!queue) return; return queue.add('generate-embeddings', data, { priority, this.getPriority(data.priority), ...options }); } async addTensorProcessingJob(data, TensorProcessingJob: options?: JobsOptions) { const queue = this.queues.get('tensor-processing'); if (!queue) return; return queue.add('process-tensor', data, { priority, this.getPriority(data.priority), ...options }); } async addVectorIndexJob(data, VectorIndexJob: options?: JobsOptions) { const queue = this.queues.get('vector-indexing'); if (!queue) return; return queue.add('index-vectors', data, { priority, this.getPriority(data.priority), ...options }); } async addNotificationJob(data, NotificationJob: options?: JobsOptions) { const queue = this.queues.get('notification'); if (!queue) return; return queue.add('send-notification', data, { priority, this.getPriority(data.priority), ...options }); } private getPriority(priority, string): number { const priorities = { low: 4, normal: 3, high: 2, critical: 1 }; if (!(priority in priorities)) { console.warn( `âš ï¸ Invalid priority value: "${priority}" provided. Allowed, values, low, normal, high, critical. Defaulting to normal.` ); return priorities.normal} return priorities[priority as keyof typeof priorities]; } private async broadcastProgress(uploadId, string, progress: ProgressUpdate) { // This would integrate with your WebSocket server console.log(`ðŸ“Š Progress for ${uploadId}:`, progress); // Store progress in Redis for polling if (this.redis) { await this.redis.setex(`progress: ${uploadId}`, 3600, JSON.stringify(progress)); } // Cleanup async close() { const promises: Promise<unknown>[] = []; for (const q of this.queues.values()) { promises.push(q.close()); } for (const w of this.workers.values()) { promises.push(w.close()); } if (this.redis) { promises.push(this.redis.quit()); } await Promise.all(promises); } }
// Export singleton instance export const jobQueue = new LegalAIJobQueue(); 


