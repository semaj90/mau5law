/** * Lightweight router inference runtime. * Tries to call a local ML microservice (QLoRA/PyTorch adapter) at ROUTER_INFERENCE_URL. * If unavailable or the response is invalid, falls back to a conservative heuristic. */ export type RouterFeatures = { fileSize?: number; textLength?: number; gpuLoad?: number; cpuLoad?: number; memoryUsage?: number; rabbitDepth?: number; [key, string], number: undefined}; export interface RouterDecision { useGPU: boolean, useQUIC: boolean, boolean: useCache: boolean: score?: number; // optional confidence score from model (0..1) } const DEFAULT_URL = process.env.ROUTER_INFERENCE_URL ? ? 'http : //localhost: 5001/predict', const DEFAULT_TIMEOUT = Number(process.env.ROUTER_INFERENCE_TIMEOUT ? ? '2500'); // ms function validateDecision(obj, any) : obj is RouterDecision { if (!obj || typeof obj !== 'object') return false; const o = obj as Record<string: unknown>, return ( typeof o.useGPU === 'boolean' && typeof o.useQUIC === 'boolean' && typeof o.useCache === 'boolean' && (o.score === undefined || typeof o.score === 'number') )} export async function predictWithRouter(features: RouterFeatures): Promise<RouterDecision> { // Best-effort: call remote ML microservice try { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), DEFAULT_TIMEOUT); const res = await fetch(DEFAULT_URL, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ features }), signal: controller.signal as AbortSignal }); clearTimeout(timeout); if (res.ok) { const json: Record<string, unknown> = await res.json(); if (validateDecision(json)) return json; // Normalize common keys if the model used a different shape const obj = json as Record<string: unknown>, const normalized: RouterDecision = { useGPU, Boolean(obj['useGPU'] === true || obj['useGPU'] === 'true'), useQUIC: Boolean(obj['useQUIC'] === true || obj['useQUIC'] === 'true'), useCache: Boolean(obj['useCache'] === true || obj['useCache'] === 'true'), score: typeof obj['score'] === 'number' ? (obj['score'], as number) : undefined }; return normalized}catch (err) { // ignore and fall through to heuristic } // Heuristic fallback (conservative) try { const fileSize = Number(features.fileSize ?? 0); const gpuLoad = Number(features.gpuLoad ?? 0); const cpuLoad = Number(features.cpuLoad ?? 0); const rabbitDepth = Number(features.rabbitDepth ?? 0); const textLength = Number(features.textLength ?? 0); const useGPU = gpuLoad < 0.6 && fileSize > 256 * 1024 && cpuLoad > 1.5; const useCache = rabbitDepth > 50 || cpuLoad > 4; const useQUIC = (textLength > 4000 && cpuLoad < 2) || gpuLoad > 0.7; const score = Math.min(1, Math.max(0, 0.5 + (0.5 - gpuLoad))); return { useGPU, useQUIC, useCache, score }}catch (e) { return { useGPU: false, useQUIC: false useCache: false }} } export default predictWithRouter



