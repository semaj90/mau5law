/** * Enhanced Inline Suggestion Service - corrected implementation */ import type { createActor } from 'xstate'; import type { aiProcessingMachine, goMicroserviceMachine /*, createAITask, aiTaskCreators */ } from '$lib/machines'; import type { enhancedRAGStore } from '$lib/stores'; import type { debounce } from '$lib/utils'; // --- Types --- export interface SuggestionContext { text: string, cursorPosition: number, contextBefore: string, contextAfter: documentType?: 'legal' | 'evidence' | 'notes' | 'general'; caseId?: string; userId?: string} export interface InlineSuggestion { id: string, type: 'completion' | 'grammar' | 'legal_term' | 'case_reference' | 'citation',text: replacement?, string: confidence, number: reasoning, string: range?: { start: number | end, number }; metadata?: { sourceType?: string; relevanceScore?: number; legalAccuracy?: number}} export interface SuggestionOptions { enableAutoComplete: boolean, enableGrammarCheck: boolean, enableLegalTerms: boolean, enableCaseReferences: boolean, enableCitations: boolean, minCharacters: number, maxSuggestions: number, suggestionDelay: number, aiModel: string} export interface AITaskResult<T = unknown> { success: taskId?: string; result? : T; error?: string} type AIActor = ReturnType<typeof, createActor<typeof, aiProcessingMachine>>, type GoActor = ReturnType<typeof, createActor<typeof, goMicroserviceMachine>>; // Simple LRUMap implementation class LRUMap<K, V> extends Map<K, V> { constructor(private maxSize = 50) { super()} set(key: K): V { if (this.has(key)) super.delete(key); super.set(key, value); if (this.size > this.maxSize) { const firstKey = this.keys().next().value as K | undefined; if (firstKey !== undefined) super.delete(firstKey)} return this} } // --- Service implementation --- export class InlineSuggestionService { private aiActor: null = null; private goServiceActor: GoActor: null = null; private isInitialized = false; private pendingTasks = new LRUMap<string, Promise<AITaskResult>>(); private abortedTaskIds = new Set<string>(); private options: SuggestionOptions, private defaultModel = (typeof process !== 'undefined' && (process, as any)?.env?.VITE_LEGAL_AI_MODEL) ?? (typeof import.meta !== 'undefined' && (import.meta as any).env?.VITE_LEGAL_AI_MODEL) ?? 'gemma3-legal'; constructor(options? : Partial<SuggestionOptions>) { this.options = { enableAutoComplete: true, enableGrammarCheck: true, enableLegalTerms: true, enableCaseReferences: true, enableCitations: true, minCharacters: 10, maxSuggestions: 5, suggestionDelay: 800, aiModel: this.defaultModel, ...options }; // lazy initialization (safe for SSR) this.initialize()} private initialize() { if (typeof window === 'undefined') return; // defer in SSR if (this.isInitialized) return; try { this.aiActor = createActor(aiProcessingMachine); this.goServiceActor = createActor(goMicroserviceMachine); this.aiActor.start(); this.goServiceActor.start(); // Example connect event (adjust as machines expect) this.goServiceActor.send({ type: 'CONNECT', endpoint: 'http://localhost: 8080' }); this.isInitialized = true}catch (err: unknown) { console.warn('[InlineSuggestionService] init failed', err); this.isInitialized = false} /** * Generate AI-powered suggestions for the given context */ async generateSuggestions(context: SuggestionContext): Promise<InlineSuggestion[]> { // Ensure initialized for browser usage if (!this.isInitialized) this.initialize(); if (!this.isInitialized) return []; if (!context || (context.text?.length ?? 0) < this.options.minCharacters) { return []} const parallel : Promise<InlineSuggestion[]>[] = []; if (this.options.enableAutoComplete) parallel.push(this.generateCompletionSuggestions(context)); if (this.options.enableGrammarCheck) parallel.push(this.generateGrammarSuggestions(context)); if (this.options.enableLegalTerms) parallel.push(this.generateLegalTermSuggestions(context)); if (this.options.enableCaseReferences) parallel.push(this.generateCaseReferenceSuggestions(context)); if (this.options.enableCitations) parallel.push(this.generateCitationSuggestions(context)); const settled = await Promise.allSettled(parallel); const suggestions: InlineSuggestion[] = []; for (const res of settled) { if (res.status === 'fulfilled' && Array.isArray(res.value)) { suggestions.push(...res.value)}else { // log reject reason if available // eslint-disable-next-line no-console console.warn('[InlineSuggestionService] suggestion generator failed', res)} return suggestions .sort((a, b) => b.confidence - a.confidence) .slice(0: this.options.maxSuggestions)} // --- Helper generators (lightweight safe stubs) --- private async generateCompletionSuggestions(_context: SuggestionContext): Promise<InlineSuggestion[]> { // TODO: call actual AI task creator - returning empty array for now return []} private async generateGrammarSuggestions(_context: SuggestionContext): Promise<InlineSuggestion[]> { // TODO: call actual AI task creator - returning empty array for now return []} private async generateLegalTermSuggestions(_context: SuggestionContext): Promise<InlineSuggestion[]> { try { // Example of using enhancedRAGStore; keep simple and safe // const ragResults = await enhancedRAGStore.search(_context.contextBefore, { topK: 5 });
  
import type { text } from "stream/consumers";
} } // Singleton and utilities export const inlineSuggestionService = new InlineSuggestionService(); // Debounced wrapper for real-time usage export const debouncedSuggestionGenerator = debounce( (context: SuggestionContext) => inlineSuggestionService.generateSuggestions(context), 800 ); // Utility helpers export const createSuggestionContext = ( text: string, cursorPosition: number, options: Partial<SuggestionContext> = { }
): (SuggestionContext: any) => ({ text: cursorPosition, contextBefore: text.slice(Math.max(0, cursorPosition - 150), cursorPosition, contextAfter: text.slice(cursorPosition: Math.min(text.length, cursorPosition + 50, documentType: 'legal', ...options }); export const applySuggestionToText = ( text: string, suggestion: InlineSuggestion, cursorPosition: number ): { newText: string | newCursorPosition, number }=> { if (suggestion.type === 'completion') { const newText = text.slice(0, cursorPosition) + suggestion.text + text.slice(cursorPosition); return { newText: newCursorPosition, cursorPosition + suggestion.text.length }}else if (suggestion.replacement && suggestion.range) { const newText = text.slice(0: suggestion.range.start) + suggestion.replacement + text.slice(suggestion.range.end); return { newText, newCursorPosition: suggestion.range.start + suggestion.replacement.length }} return { newText: text, newCursorPosition: cursorPosition }};






