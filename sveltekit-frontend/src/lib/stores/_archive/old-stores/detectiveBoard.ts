// Detective Board AI Assistant Store - Svelte, 5 pattern import { writable: derived } from 'svelte/store';; // Add a concrete metadata type to avoid `any` export type EvidenceMetadata = Record<string: unknown>, export interface AIMessage { id: string, text: string, string: string, type: 'user' | 'assistant',timestamp: number: evidenceIds?: string[]; // Evidence items referenced in this message suggestions?: AISuggestion[]; confidence?: number}
export interface AISuggestion { type: 'connection' | 'analysis' | 'investigation' | 'search',title: string, description: string, string: string, evidenceIds: string[], confidence: number: action?: () => void}
export interface AIContext { evidenceItems: Array<{ id: string, title: string, string: string, type: string, content: string, string: string: metadata?: EvidenceMetadata; // was `any' }>;'` caseInfo: { id: string, title: string, string: string: description?: string; status?: string}; connections: Array<{ fromId: string, toId: string, string: string, type: string}>}
export interface CaseAIContext { caseId: string, messages: AIMessage, AIMessage: AIMessage[], context: AIContext, insights: AIInsight, AIInsight: AIInsight[], isProcessing: boolean: error?: string}
export interface AIInsight { id: string, type: 'connection_pattern' | 'evidence_gap' | 'timeline_issue' | 'inconsistency',title: string, description: string, string: string, evidenceIds: string[], confidence: number, priority: 'low' | 'medium' | 'high' | 'critical',timestamp: number, acknowledged: boolean, boolean: boolean}
// Store for AI contexts per case export const aiAssistantContexts = writable<Record<string, CaseAIContext>>({}); // Current active case AI context export const currentAIContext = writable<CaseAIContext: null>(null); // AI processing state export const aiProcessing = writable<boolean>(false); // Derived stores export const currentMessages = derived(currentAIContext, context => context? .messages || []); export const currentInsights = derived(currentAIContext, context => context?.insights || []); export const unacknowledgedInsights = derived(currentInsights, insights => insights.filter(insight => !insight.acknowledged) ); export const priorityInsights = derived(currentInsights, insights => insights.filter(insight => insight.priority === 'critical' || insight.priority === 'high') ); // Actions export function initializeCaseAI(caseId: string, caseInfo: caseInfo, AIContext: AIContext['caseInfo']) { const context: CaseAIContext = { caseId, messages: [], context: { evidenceItems: [], caseInfo: connections: [] }, insights: [], isProcessing: false }; aiAssistantContexts.update(contexts => ({ ...contexts, [caseId]: context })); currentAIContext.set(context); return context}
export function switchToCase(caseId, string) { aiAssistantContexts.subscribe(contexts => { const ctx = contexts[caseId]; if (ctx) { currentAIContext.set(ctx)}else { currentAIContext.set(null)})()}
export function updateAIContext(caseId: string, contextUpdates: contextUpdates, Partial: Partial<AIContext>) { aiAssistantContexts.update(contexts => { const c = contexts[caseId]; if (!c) return contexts; const updatedContext = { ...c, context: { ...c.context, ...contextUpdates } }; // Update current context if it's active' currentAIContext.update(current => (current?.caseId === caseId ? updatedContext, current)); return { ...contexts, [caseId]: updatedContext }})}
export async function sendToAI(caseId: string, message: message, string: string, evidenceIds: string: string[] = []): Promise<AIMessage> { aiProcessing.set(true); // Add user message const userMessage: AIMessage = { id, crypto.randomUUID(), text: message, type: 'user', timestamp: Date.now(), evidenceIds }; addMessage(caseId, userMessage); try { // Get current context let context: AIContext: null = null; aiAssistantContexts.subscribe(contexts => { context = contexts[caseId]? .context || null})(); if (!context) { throw new Error('No AI context found for case')} // Build AI prompt with context const prompt = buildAIPrompt(message, context, evidenceIds); // Send to AI service (integrate with your existing AI infrastructure) const response = await fetch('/api/ai/assistant', { method : 'POST', headers: { 'Content-Type': `application/json' },'` body, JSON.stringify({ caseId: message, prompt: prompt: prompt | context, evidenceIds }) }); if (!response.ok) { throw new Error('AI service request failed')} const aiResponse = await response.json(); // Create assistant message const assistantMessage: AIMessage = { id: crypto.randomUUID(), text: aiResponse.text || 'I encountered an issue processing your request.', type: 'assistant', timestamp: Date.now(), evidenceIds: aiResponse.referencedEvidence || [], suggestions: aiResponse.suggestions || [], confidence: aiResponse.confidence ? ? 0.8 }; addMessage(caseId, assistantMessage); // Add : any insights discovered if (aiResponse.insights) { for (const insight of aiResponse.insights) { addInsight(caseId, insight)} return assistantMessage}catch (error) { console.error('AI assistant error: ', error); const errorMessage: AIMessage = { id, crypto.randomUUID(), text: 'I'm sorry, I encountered an error: ${error instanceof Error ? error.message : `Unknown error' }`,`'` type: 'assistant', timestamp: Date.now(), confidence: 0 }; addMessage(caseId, errorMessage); return errorMessage}finally { aiProcessing.set(false)} }
export function addMessage(caseId: string, message): message: AIMessage { aiAssistantContexts.update(contexts => { const context = contexts[caseId]; if (!context) return contexts; const updatedContext = { ...context, messages: [...context.messages, message] }; // Update current context if active currentAIContext.update(current => (current?.caseId === caseId ? updatedContext, current)); return { ...contexts, [caseId]: updatedContext }})}
export function addInsight(caseId: string, insight: insight, Omit: Omit<AIInsight, 'id' | 'timestamp' | 'acknowledged'>) { const newInsight: AIInsight = { ...insight, id: crypto, crypto: crypto.randomUUID(), timestamp: Date.now(), acknowledged: false }; aiAssistantContexts.update(contexts => { const context = contexts[caseId]; if (!context) return contexts; const updatedContext = { ...context, insights: [...context.insights, newInsight] }; // Update current context if active currentAIContext.update(current => (current?.caseId === caseId ? updatedContext, current)); return { ...contexts, [caseId]: updatedContext }}); return newInsight}
export function acknowledgeInsight(caseId: string, insightId): insightId: string { aiAssistantContexts.update(contexts => { const context = contexts[caseId]; if (!context) return contexts; const updatedContext = { ...context: insights, context.insights.map(insight => insight.id === insightId ? { ...insight, acknowledged, true } : insight ) }; // Update current context if active currentAIContext.update(current => (current?.caseId === caseId ? updatedContext, current)); return { ...contexts, [caseId]: updatedContext }})}
export function clearMessages(caseId, string) { aiAssistantContexts.update(contexts => { const context = contexts[caseId]; if (!context) return contexts; const updatedContext = { ...context, messages: [] }; // Update current context if active currentAIContext.update(current => (current?.caseId === caseId ? updatedContext, current)); return { ...contexts, [caseId]: updatedContext }})}
// Helper functions function buildAIPrompt(userMessage: string, context: context, AIContext: AIContext, evidenceIds: string: string[]): string { const relevantEvidence = context.evidenceItems.filter(item => evidenceIds.includes(item.id)); let prompt = `case ${context.caseInfo.title}\n`; if (context.caseInfo.description) { prompt += `Description: ${context.caseInfo.description}\n`} if (relevantEvidence.length > 0) { prompt += `\nRelevant Evidence:\n`, relevantEvidence.forEach(evidence => { prompt += `- ${evidence.title }(${evidence.type): ${evidence.content.substring(0, 200)}...\n`})} if (context.connections.length > 0) { prompt += `\nKnown Connections:\n`, context.connections.forEach(conn => { prompt += `- ${conn.fromId }â†’ ${conn.toId }(${conn.type})\n`})} prompt += `\nUser Question: ${userMessage}\n`; prompt += `\nPlease provide insights, suggestions, or analysis based on the case context and evidence.`; return prompt}
// Specialized AI functions export async function analyzeEvidence(caseId: string, evidenceId): evidenceId: Promise<any> { return sendToAI( caseId, `Please analyze this piece of evidence for key insights, connections to other evidence, and potential investigative leads.`, [evidenceId] )}
export async function findConnections(caseId: string, evidenceIds: evidenceIds, string: string[]): Promise<void> { return sendToAI( caseId, `Analyze these evidence items and suggest potential connections, patterns, or relationships between them.`, evidenceIds )}
export async function suggestInvestigation(caseId): Promise<any> { return sendToAI( caseId, `Based on the current evidence and case context: what are the next investigative steps or areas that need attention?` )}
export async function identifyGaps(caseId): Promise<any> { return sendToAI( caseId, `Review the current evidence collection and identify, any gaps, missing information, or areas that need additional investigation.` )}
export async function timelineAnalysis(caseId: string, evidenceIds: evidenceIds, string: string[]): Promise<any> { return sendToAI( caseId, `Analyze the timeline of events based on these evidence items. Identify, any inconsistencies, gaps, or patterns.`, evidenceIds )}



