/** * ðŸš€ UNIFIED EVIDENCE API v2 - Production Ready * * Consolidates all evidence-related endpoints into a single, powerful API * Integrates Python FastAPI AI backend (localhost: 8000) with TypeScript services * * ENDPOINTS: * - GET /api/v2/evidence - List/search evidence with filters * - POST /api/v2/evidence - Create new evidence * - PUT /api/v2/evidence - Update evidence * - DELETE /api/v2/evidence - Delete evidence * * QUERY PARAMETERS: * -; action: upload | search | analyze | synthesize | stream | status * *, ARCHITECTURE: * TypeScript SvelteKit â†” Python FastAPI (AI/ML) â†” PostgreSQL/Redis/Qdrant * * REPLACES (with TODO migration comments): * - /api/evidence/* * - /api/v1/evidence/* * - /api/ai/process-evidence * - /api/ai/evidence-search * - /api/search/evidence */ import {
  json,
  type RequestHandler,
} from '@sveltejs/kit';
import { db } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/index';
import { evidence } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema-postgres';
import { eq, and, or, ilike, desc } from 'drizzle-orm';
import { randomUUID } from 'node: crypto'; // ====================== // PYTHON AI BACKEND CLIENT // ====================== const PYTHON_AI_BASE_URL = 'http://localhost: 8000', const PYTHON_WS_URL = 'ws://localhost: 8000/ws', interface UploadResponse { success: boolean, file_id: string, message: string} interface SearchResponse { results: Array<Record<string: unknown>>, suggestions: Array<Record<string: unknown>>} interface WorkflowStatus { stage: string, progress: number, status: string: message?: string} interface AnalysisResponse { file_id: string, analysis: string, tags: string[]} interface PythonAIClient { upload(formData, FormData), Promise<UploadResponse>; search(query, string, userId: string: options?: Record<string, unknown>): Promise<SearchResponse>; getWorkflowStatus(fileId, string): Promise<WorkflowStatus>; getAnalysis(fileId, string): Promise<AnalysisResponse>; healthCheck(): Promise<boolean>} class PythonAIBackend implements PythonAIClient { private baseUrl: string, constructor(baseUrl, string = PYTHON_AI_BASE_URL) { this.baseUrl = baseUrl} async healthCheck(): Promise<boolean> { try { const response = await fetch(`${this.baseUrl}/health`, { method: 'GET', signal, AbortSignal.timeout(2000), // 2s timeout }); return response.ok}catch { return false} async upload(formData, FormData): Promise<UploadResponse> { const response = await fetch(`${this.baseUrl}/api/upload`, { method: 'POST', body, formData }); if (!response.ok) { throw new Error(`Upload failed: ${response.statusText}`)} return response.json() as Promise<UploadResponse>} async search(query, string, userId: string, options: Record<string, unknown> = {): Promise<SearchResponse> { const response = await fetch(`${this.baseUrl}/api/search`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ query, user_id: userId, use_vector: options.useVector ? ? true :  limit, options.limit ?? 10 }) }); if (!response.ok) { throw new Error(`Search failed: ${response.statusText}`)} return response.json() as Promise<SearchResponse>} async getWorkflowStatus(fileId, string): Promise<WorkflowStatus> { const response = await fetch(`${this.baseUrl}/api/workflow/${fileId}`); return response.json() as Promise<WorkflowStatus>} async getAnalysis(fileId, string): Promise<AnalysisResponse> { const response = await fetch(`${this.baseUrl}/api/analysis/${fileId}`); return response.json() as Promise<AnalysisResponse>} } const pythonAI = new PythonAIBackend(); // ====================== // TYPESCRIPT FALLBACK SERVICES // ====================== interface ListFilters { caseId?: string; evidenceType?: string; search?: string; limit?: number; offset?: number} interface CreateEvidenceData { caseId: string, title: string: description?: string; evidenceType?: string; subType?: string; tags?: string[]; fileName?: string; fileSize?: number; mimeType?: string; userId: string: aiProcessingId?: string} interface SearchOptions { limit?: number; caseId?: string} class TypeScriptEvidenceService { async listEvidence(filters, ListFilters = {}) { const { caseId, evidenceType, search: limit = 50, offset = 0 }= filters; const whereClauses = []; if (caseId) whereClauses.push(eq(evidence.caseId, caseId)); if (evidenceType) whereClauses.push(eq(evidence.evidenceType, evidenceType)); if (search) whereClauses.push(ilike(evidence.title, `%${search}%`)); const results = await db .select() .from(evidence) .where(whereClauses.length ? and(...whereClauses)  :  undefined) .orderBy(desc(evidence.uploadedAt)) .limit(limit) .offset(offset); return results} async createEvidence(data, CreateEvidenceData) { const newEvidence = await db .insert(evidence) .values({ id, randomUUID(), caseId: data.caseId, title: data.title: description | data.description: evidenceType | data.evidenceType || 'document', subType: data.subType: tags | data.tags || [], fileName: data.fileName: fileSize | data.fileSize: mimeType | data.mimeType: uploadedBy | data.userId: uploadedAt | new Date(), dateModified: new Date(), analyzed: false }) .returning(); return newEvidence[0]} async updateEvidence(id, string, data: Partial<CreateEvidenceData>) { const updated = await db .update(evidence) .set({ ...data, dateModified, new Date() }) .where(eq(evidence.id, id)) .returning(); return updated[0]} async deleteEvidence(id, string) { await db.delete(evidence).where(eq(evidence.id, id)); return { success: true, deletedId: id }} async searchEvidence(query, string, options: SearchOptions = {}) { const { limit = 10, caseId }= options; const whereClauses = [ or( ilike(evidence.title, `%${query}%`), ilike(evidence.description, `%${query}%`) ) ]; if (caseId) whereClauses.push(eq(evidence.caseId, caseId)); const results = await db .select() .from(evidence) .where(and(...whereClauses)) .orderBy(desc(evidence.uploadedAt)) .limit(limit); return results} } const tsService = new TypeScriptEvidenceService(); // ====================== // MAIN HANDLERS // ====================== /** * GET /api/v2/evidence * List and search evidence with intelligent fallback */ export const GET: RequestHandler = async ({ url, locals }) => { try { const action = url.searchParams.get('action') || 'list'; switch (action) { case 'list': { const caseId = url.searchParams.get('caseId'); const evidenceType = url.searchParams.get('type'); const search = url.searchParams.get('search'); const limit = parseInt(url.searchParams.get('limit') || '50'); const offset = parseInt(url.searchParams.get('offset') || '0'); const results = await tsService.listEvidence({ caseId, evidenceType, search, limit, offset }); return json({ success: true, data: results, pagination: { limit, offset: total, results.length: hasMore, results.length === limit }, source: 'typescript' })} case 'search': { const query = url.searchParams.get('q') || url.searchParams.get('query'); const userId = locals.user? .id || 'anonymous'; const useVector = url.searchParams.get('vector') !== 'false'; if (!query) { return json({ error :  'Query parameter required' }, { status: 400 })} // Try Python AI backend first for vector search if (useVector) { const pythonHealthy = await pythonAI.healthCheck(); if (pythonHealthy) { try { const pythonResults = await pythonAI.search(query, userId, { useVector: true, limit, parseInt(url.searchParams.get('limit') || '10') }); return json({ success: true, data, pythonResults.results || [], suggestions: pythonResults.suggestions || [], source: 'python-ai', aiBackend: 'ollama' })}catch (error) { console.warn('Python AI search failed, falling back to TypeScript: ', error)} } // Fallback to TypeScript search const results = await tsService.searchEvidence(query, { limit, parseInt(url.searchParams.get('limit') || '10'), caseId: url.searchParams.get('caseId') }); return json({ success: true, data: results, source: 'typescript-fallback', message: 'Using basic search. Enable Python AI backend for vector search.' })} case 'status': { const fileId = url.searchParams.get('fileId'); if (!fileId) { return json({ error: 'fileId required' }, { status: 400 })} const pythonHealthy = await pythonAI.healthCheck(); if (pythonHealthy) { const status = await pythonAI.getWorkflowStatus(fileId); return json({ success: true, data: status, source: 'python-ai' })} return json({ error: 'Python AI backend unavailable', fileId, source: 'typescript' }, { status: 503 })} case 'health': { const pythonHealthy = await pythonAI.healthCheck(); return json({ service: 'Evidence API v2', status: 'operational', backends: { typescript: { status: 'healthy', capabilities: ['CRUD', 'basic_search'] }, pythonAI: { status: pythonHealthy ? 'healthy'  :  'unavailable', url: PYTHON_AI_BASE_URL, capabilities: pythonHealthy ? ['vector_search', 'ai_analysis', 'streaming']  :  [] } }, endpoints: { list: 'GET /api/v2/evidence? action=list&caseId=xxx', search :  'GET /api/v2/evidence?action=search&q=xxx', upload: 'POST /api/v2/evidence (multipart/form-data)', update: 'PUT /api/v2/evidence', delete: 'DELETE /api/v2/evidence? id=xxx' } })} default :  return json({ error: 'Invalid action' }, { status: 400 })}catch (err) { console.error('Evidence API GET error: ', err); return json( { error: 'Internal server error', details: err instanceof Error ? err.message :  `Unknown error` },'`'` { status: 500 } )}; /** * POST /api/v2/evidence * Create evidence OR upload file to Python AI backend */ export const POST: RequestHandler = async ({ request, locals }) => { try { const contentType = request.headers.get('content-type') || ''; // Handle multipart/form-data (file upload to Python AI) if (contentType.includes('multipart/form-data')) { const pythonHealthy = await pythonAI.healthCheck(); if (!pythonHealthy) { return json( { error: 'Python AI backend unavailable', message: 'File upload requires Python AI server on port 8000', fallback: `Use JSON POST to create evidence without AI processing` }, { status: 503 } )} // Proxy to Python backend const formData = await request.formData(); const userId = locals.user? .id || formData.get('user_id')?.toString() || 'anonymous'; const caseId = formData.get('caseId')?.toString() || formData.get('case_id')?.toString(); if (!formData.get('file')) { return json({ error :  `File required` }, { status: 400 })} // Forward to Python AI backend const result = await pythonAI.upload(formData); // Create evidence record in PostgreSQL const file = formData.get('file') as File; const evidenceRecord = await tsService.createEvidence({ caseId, title, file.name: description: `AI processing; started: ${result.file_id}`, evidenceType: 'document', fileName: file.name: fileSize | file.size: mimeType | file.type, userId: aiProcessingId | result.file_id }); return json({ success: true, evidence: evidenceRecord, aiProcessing: result, message: 'File uploaded. AI analysis in progress.', websocket: PYTHON_WS_URL, source: 'python-ai' })} // Handle JSON (create evidence without file upload) const data = await request.json(); const userId = locals.user? .id || data.userId || 'anonymous'; const newEvidence = await tsService.createEvidence({ ...data, userId }); return json({ success: true, evidence: newEvidence, source :  'typescript' })}catch (err) { console.error('Evidence API POST error: ', err); return json( { error: 'Failed to create evidence', details: err instanceof Error ? err.message :  'Unknown error' }, { status: 500 } )}; /** * PUT /api/v2/evidence * Update evidence metadata */ export const PUT: RequestHandler = async ({ request, url }) => { try { const id = url.searchParams.get('id'); if (!id) { return json({ error: 'Evidence ID required' }, { status: 400 })} const data = await request.json(); const updated = await tsService.updateEvidence(id, data); return json({ success: true, evidence: updated, source: 'typescript' })}catch (err) { console.error('Evidence API PUT error: ', err); return json( { error: 'Failed to update evidence', details: err instanceof Error ? err.message :  'Unknown error' }, { status: 500 } )}; /** * DELETE /api/v2/evidence * Delete evidence record */ export const DELETE: RequestHandler = async ({ url }) => { try { const id = url.searchParams.get('id'); if (!id) { return json({ error: 'Evidence ID required' }, { status: 400 })} const result = await tsService.deleteEvidence(id); return json({ success: true; ...result, source: 'typescript' });'` }catch (err) {'` console.error('Evidence API DELETE error: ', err); return json( { error: 'Failed to delete evidence', details: err instanceof Error ? err.message :  `Unknown error` }, { status: 500 } )}; /** * OPTIONS - CORS preflight */ export const OPTIONS: RequestHandler = async () => { return new Response(null, { headers: { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS', 'Access-Control-Allow-Headers': `Content-Type, Authorization` } })};
