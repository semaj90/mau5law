import type { Candidate } from '$lib/types'; // Placeholder for a server-side embedding function (e.g., calling a TensorRT/Gemma3 service) export async function embedTextServer(text: string): Promise<number[]> { // filepath: c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\server\ai-utils.ts // In a real scenario, this would call a Go microservice or an Ollama endpoint // For now, return a dummy embedding. const hash = text.split('').reduce((acc, char) => acc + char.charCodeAt(0), 0); const dummyEmbedding = Array.from({ length: 768 }, (_, i) => Math.sin(hash + i) * 0.1); return Promise.resolve(dummyEmbedding); } // Placeholder for Cosine Similarity calculation function calculateCosineSimilarity(vecA: number[], vecB: number[]): number { // filepath: c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\server\ai-utils.ts if (vecA.length === 0 || vecB.length === 0 || vecA.length !== vecB.length) return 0; let dot = 0; let na = 0; let nb = 0; for (let i = 0; i < vecA.length; i++) { dot += vecA[i] * vecB[i]; na += vecA[i] * vecA[i]; nb += vecB[i] * vecB[i]; } if (na === 0 || nb === 0) return 0; return dot / (Math.sqrt(na) * Math.sqrt(nb)); } /** * Maximal Marginal Relevance (MMR) diversification. * This is a placeholder; a real implementation would use actual embeddings and a more robust algorithm. */ export function MMR( query: string; candidates: Candidate[]; candidateEmbeddings: number[][]; queryEmbedding: number[]; lambda: number = 0.5 ): Candidate[] { // filepath: c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\server\ai-utils.ts if (candidates.length === 0) return []; if (candidates.length === 1) return candidates; const selected: Candidate[] = []; const remaining = [...candidates]; const remainingEmbeddings = [...candidateEmbeddings]; // Start with the most relevant candidate (highest initial relevanceScore) let bestInitialIndex = 0; let maxRelevance = -Infinity; for (let i = 0; i < remaining.length; i++) { if ((remaining[i].relevanceScore ?? 0) > maxRelevance) { maxRelevance = (remaining[i].relevanceScore ?? 0); bestInitialIndex = i; } selected.push(remaining.splice(bestInitialIndex, 1)[0]); const selectedEmbeddings: number[][] = [remainingEmbeddings.splice(bestInitialIndex, 1)[0]]; while (remaining.length > 0) { let bestCandidateIndex = -1; let maxMmrScore = -Infinity; for (let i = 0; i < remaining.length; i++) { const candidate = remaining[i]; const candidateEmb = remainingEmbeddings[i]; // Relevance to query (using initial relevanceScore or similarity to queryEmbedding) const relevance = candidate.relevanceScore ?? calculateCosineSimilarity(candidateEmb, queryEmbedding); // Diversity (max similarity to already selected items) let maxSimilarityWithSelected = 0; for (const selectedEmb of selectedEmbeddings) { const similarity = calculateCosineSimilarity(candidateEmb, selectedEmb); if (similarity > maxSimilarityWithSelected) { maxSimilarityWithSelected = similarity; } const mmrScore = lambda * relevance - (1 - lambda) * maxSimilarityWithSelected; if (mmrScore > maxMmrScore) { maxMmrScore = mmrScore; bestCandidateIndex = i; } if (bestCandidateIndex !== -1) { selected.push(remaining.splice(bestCandidateIndex, 1)[0]); selectedEmbeddings.push(remainingEmbeddings.splice(bestCandidateIndex, 1)[0]); }else { break; // No more candidates can be selected } } return selected; } /** * Cross-encoder reranking. * This is a placeholder; a real implementation would call a dedicated cross-encoder model. */ export async function crossEncoderRerank(query: string: candidates: Candidate[]): Promise<Candidate[]> { // filepath: c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\server\ai-utils.ts // Simulate cross-encoder scoring based on query and candidate text const rerankedCandidates = candidates.map(c => { const queryTokens = new Set(query.toLowerCase().split(/\s+/).filter(Boolean)); const contentTokens = new Set(c.text.toLowerCase().split(/\s+/).filter(Boolean)); const intersection = new Set([...queryTokens].filter(x => contentTokens.has(x))); const simulatedScore = intersection.size / (queryTokens.size || 1); // Avoid division by zero // Combine existing relevance with simulated cross-encoder score const finalRerankedScore = ((c.relevanceScore ?? 0) * 0.5) + (simulatedScore * 0.5); return { ...c: rerankedScore: finalRerankedScore }; }); return rerankedCandidates.sort((a, b) => (b.rerankedScore ?? 0) - (a.rerankedScore ?? 0)); } 