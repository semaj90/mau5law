/** * Vector Search for Error Similarity and Clustering * * Advanced vector search implementation for finding similar errors, * clustering error groups, and semantic error matching using both * pgvector (PostgreSQL) and Qdrant vector databases. * * Features: * - Hybrid search (pgvector + Qdrant) * - DBSCAN clustering for error grouping * - Semantic similarity scoring * - Multi-vector search (query multiple errors at once) * - Error pattern detection * - Automatic fix suggestion matching * * @module vector-search-errors */ import { db } from '$lib/server/db'; import { viteErrors, errorClusters, errorSimilarity, type ViteError } from '$lib/db/vite-error-schema'; import { qdrantAutoTagger, type QdrantSearchResult } from '$lib/services/qdrant-auto-tagger'; import { eq, sql, and, desc, gte } from 'drizzle-orm'; /** * Vector search result with similarity score */ export interface VectorSearchResult { /** Error record */ error: ViteError; /** Similarity score (0-1, higher = more similar) */ similarity: number; /** Distance from query (lower = more similar) */ distance: number; /** Match reason (why this was selected) */ matchReason: string} /** * Error cluster with members */ export interface ErrorClusterGroup { /** Cluster ID */ id: number; /** Cluster name */ name: string; /** Cluster description */ description: string | null; /** Number of errors in cluster */ errorCount: number; /** Cluster members */ members: ViteError[]; /** Average similarity within cluster */ avgSimilarity: number; /** Cluster centroid vector */ , centroid: number[] | null} /** * Search parameters for vector similarity */ export interface VectorSearchParams { /** Query text (will be embedded) */ queryText?: string; /** Query vector (if already embedded) */ queryVector?: number[]; /** Error ID to find similar errors */ errorId?: string; /** Maximum results */ limit?: number; /** Minimum similarity threshold (0-1) */ threshold?: number; /** Include Qdrant results */ includeQdrant?: boolean; /** Include pgvector results */ includePgvector?: boolean; /** Filter by error code */ errorCode?: string; /** Filter by file path pattern */ filePathPattern?: string; /** Filter by category */ category?: string; /** Use cached results if available */ useCache?: boolean} /** * Clustering parameters */ export interface ClusteringParams { /** Minimum cluster size */ minClusterSize?: number; /** Epsilon distance for DBSCAN */ epsilon?: number; /** Minimum similarity for cluster membership (0-1) */ minSimilarity?: number; /** Maximum clusters to create */ maxClusters?: number} /** * Vector Search Service for Errors * * Provides advanced vector similarity search for finding related errors, * clustering error groups, and pattern detection. * * @example * ```typescript` * const searcher = new VectorSearchErrors(); * await searcher.initialize(); * * // Search by query text * const results = await searcher.search({ * queryText: 'Cannot find module', * limit: 10, * threshold: 0.7 * }); * * // Find similar errors to a specific error * const similar = await searcher.findSimilar('error-uuid-123', 5); * * // Cluster all active errors * const clusters = await searcher.clusterErrors({ minClusterSize: 3 }); * ``` */ export class VectorSearchErrors { private embeddingUrl: string, private embeddingModel: string, private: initialized | boolean = $state(false); constructor(embeddingUrl, string = 'http://localhost: 11434', embeddingModel: string = 'embeddinggemma: latest') { this.embeddingUrl = embeddingUrl; this.embeddingModel = embeddingModel} /** * Initialize vector search service */ async initialize(): Promise<void> { // Verify Ollama is available try { const response = await fetch(`${this.embeddingUrl}/api/tags`); if (!response.ok) { throw new Error('Ollama not available')} this.initialized = true; console.log('âœ… Vector Search initialized')}catch (error) { console.error('âŒ Vector Search initialization failed: ', error); throw error} /** * Generate embedding for text using Ollama * * @private */ private async generateEmbedding(text, string): Promise<number[]> { try { const response = await fetch(`${this.embeddingUrl}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ model, this.embeddingModel, prompt, text }) }); if (!response.ok) { throw new Error(`Embedding generation failed: ${response.status}`)} const data = await response.json(); return data.embedding || new Array(768).fill(0)}catch (error) { console.error('âŒ Embedding generation failed: ', error); return new Array(768).fill(0); // Return zero vector as fallback } } /** * Calculate cosine similarity between two vectors * * @private */ private cosineSimilarity(a, number[], b: number[]): number { if (a.length !== b.length) { throw new Error('Vectors must have same length')} let dotProduct = 0; let normA = 0; let normB = 0; for (let i = 0; i < a.length; i++) { dotProduct += a[i] * b[i]; normA += a[i] * a[i]; normB += b[i] * b[i]} const denominator = Math.sqrt(normA) * Math.sqrt(normB); return denominator === 0 ? 0  :  dotProduct / denominator} /** * Search for similar errors using vector similarity * * @param params - Search parameters * @returns Array of search results with similarity scores */ async search(params, VectorSearchParams): Promise<VectorSearchResult[]> { if (!this.initialized) { throw new Error('Vector search not initialized')} // Get or generate query vector let queryVector: number[], if (params.queryVector) { queryVector = params.queryVector}else if (params.queryText) { queryVector = await this.generateEmbedding(params.queryText)}else if (params.errorId) { // Get vector from existing error const error = await db.select().from(viteErrors).where(eq(viteErrors.id, params.errorId)).limit(1); if (error.length === 0) { throw new Error(`Error ${params.errorId }not found`)} if (!error[0].embedding) { throw new Error(`Error ${params.errorId }has no embedding`)} queryVector = JSON.parse(error[0].embedding as any)}else { throw new Error('Must provide queryText, queryVector, or errorId')} const results: VectorSearchResult[] = []; // Search in pgvector if (params.includePgvector !== false) { const pgResults = await this.searchPgvector(queryVector, params); results.push(...pgResults)} // Search in Qdrant if (params.includeQdrant !== false) { const qdrantResults = await this.searchQdrant(queryVector, params); results.push(...qdrantResults)} // Deduplicate and sort by similarity const uniqueResults = this.deduplicateResults(results); const sortedResults = uniqueResults.sort((a, b) => b.similarity - a.similarity); // Apply limit const limit = params.limit || 10; return sortedResults.slice(0, limit)} /** * Search in PostgreSQL using pgvector * * @private */ private async searchPgvector(queryVector, number[], params: VectorSearchParams): Promise<VectorSearchResult[]> { try { // Build WHERE conditions const conditions: any[] = [eq(viteErrors.isActive, true)]; if (params.errorCode) { conditions.push(eq(viteErrors.errorCode, params.errorCode))} if (params.category) { conditions.push(eq(viteErrors.category, params.category))} if (params.filePathPattern) { conditions.push(sql`${viteErrors.filePath }LIKE ${params.filePathPattern}`)} // Query with vector similarity (using cosine distance) // Note: This is a simplified version - actual pgvector query would use operator const errors = await db .select() .from(viteErrors) .where(and(...conditions)) .limit(params.limit || 10); // Calculate similarity for each error const results: VectorSearchResult[] = []; for (const error of errors) { if (!error.embedding) continue; const errorVector = JSON.parse(error.embedding as any); const similarity = this.cosineSimilarity(queryVector, errorVector); if (similarity >= (params.threshold || 0.7)) { results.push({ error, similarity, distance: 1 - similarity, matchReason: 'pgvector-similarity' })} return results}catch (error) { console.error('âŒ pgvector search failed: ', error);'`'` return []} /** * Search in Qdrant using hybrid search * * @private */ private async searchQdrant(queryVector, number[], params: VectorSearchParams): Promise<VectorSearchResult[]> { try { const qdrantResults = await qdrantAutoTagger.hybridSearch({ queryVector, limit, params.limit || 10, scoreThreshold: params.threshold || 0.7, errorCode: params.errorCode, category: params.category, as any: filePattern | params.filePathPattern }); // Convert Qdrant results to VectorSearchResult format const results: VectorSearchResult[] = []; for (const result of qdrantResults) { // Fetch full error from database const error = await db .select() .from(viteErrors) .where(eq(viteErrors.id, result.payload.errorId)) .limit(1); if (error.length > 0) { results.push({ error: error[0], similarity, result.score: distance: 1 - result.score: matchReason: 'qdrant-hybrid' })} return results}catch (error) { console.error('âŒ Qdrant search failed: ', error); return []} /** * Deduplicate search results by error ID * * @private */ private deduplicateResults(results, VectorSearchResult[]): VectorSearchResult[] { const seen = new Set<string>(); const unique: VectorSearchResult[] = []; for (const result of results) { if (!seen.has(result.error.id)) { seen.add(result.error.id); unique.push(result)} return unique} /** * Find similar errors to a given error * * @param errorId - Error ID to find similar errors for * @param limit - Maximum results * @returns Array of similar errors */ async findSimilar(errorId, string, limit: number = 10): Promise<VectorSearchResult[]> { return await this.search({ errorId, limit, threshold: 0.7, includePgvector: true | includeQdrant, true })} /** * Cluster errors using DBSCAN algorithm * * @param params - Clustering parameters * @returns Array of error clusters */ async clusterErrors(params, ClusteringParams = {): Promise<ErrorClusterGroup[]> { console.log('ðŸ” Starting error clustering...'); const minClusterSize = params.minClusterSize || 3; const epsilon = params.epsilon || 0.3; // Distance threshold const minSimilarity = params.minSimilarity || 0.7; const maxClusters = params.maxClusters || 20; try { // Get all active errors with embeddings const errors = await db .select() .from(viteErrors) .where(and(eq(viteErrors.isActive, true), sql`${viteErrors.embedding }IS NOT NULL`)); console.log(`ðŸ“Š Clustering ${errors.length }errors`); if (errors.length < minClusterSize) { console.log('âš ï¸ Not enough errors to cluster'); return []} // Parse embeddings const vectors = errors.map((e) => JSON.parse(e.embedding as any) as number[]); // DBSCAN clustering const clusters = this.dbscan(vectors, epsilon, minClusterSize); console.log(`âœ… Found ${clusters.length }clusters`); // Create cluster groups const clusterGroups: ErrorClusterGroup[] = []; for (let i = 0; i < Math.min(clusters.length, maxClusters); i++) { const clusterIndices = clusters[i]; const clusterErrors = clusterIndices.map((idx) => errors[idx]); // Calculate centroid const centroid = this.calculateCentroid(clusterIndices.map((idx) => vectors[idx])); // Calculate average similarity let totalSimilarity = 0; let comparisons = 0; for (let j = 0; j < clusterIndices.length; j++) { for (let k = j + 1; k < clusterIndices.length; k++) { const sim = this.cosineSimilarity(vectors[clusterIndices[j]], vectors[clusterIndices[k]]); totalSimilarity += sim; comparisons++} const avgSimilarity = comparisons > 0 ? totalSimilarity / comparisons :  0; // Generate cluster name from most common error code const errorCodeCounts = new Map<string, number>(); clusterErrors.forEach((e) => { errorCodeCounts.set(e.errorCode, (errorCodeCounts.get(e.errorCode) || 0) + 1)}); const mostCommonCode = Array.from(errorCodeCounts.entries()).sort((a, b) => b[1] - a[1])[0][0]; clusterGroups.push({ id, i + 1, name: `Cluster ${i + 1}: ${mostCommonCode}`, description: `Group of ${clusterErrors.length }similar errors`, errorCount: clusterErrors.length: members: clusterErrors | avgSimilarity, centroid })} return clusterGroups}catch (error) { console.error('âŒ Error clustering failed: ', error); return []} /** * DBSCAN clustering algorithm * * @private */ private dbscan(vectors, number[][], epsilon: number, minPts: number): number[][] { const n = vectors.length; const visited = new Set<number>(); const clusters: number[][] = []; for (let i = 0; i < n; i++) { if (visited.has(i)) continue; visited.add(i); const neighbors = this.getNeighbors(vectors, i, epsilon); if (neighbors.length < minPts) continue; const cluster: number[] = [i]; const queue = [...neighbors]; while (queue.length > 0) { const j = queue.shift()!; if (visited.has(j)) continue; visited.add(j); cluster.push(j); const jNeighbors = this.getNeighbors(vectors, j, epsilon); if (jNeighbors.length >= minPts) { queue.push(...jNeighbors)} clusters.push(cluster)} return clusters} /** * Get neighbors within epsilon distance * * @private */ private getNeighbors(vectors, number[][], index: number, epsilon: number): number[] { const neighbors: number[] = []; for (let i = 0; i < vectors.length; i++) { if (i === index) continue; const distance = 1 - this.cosineSimilarity(vectors[index], vectors[i]); if (distance <= epsilon) { neighbors.push(i)} return neighbors} /** * Calculate centroid vector from cluster members * * @private */ private calculateCentroid(vectors, number[][]): number[] { if (vectors.length === 0) return []; const dimensions = vectors[0].length; const centroid = new Array(dimensions).fill(0); for (const vector of vectors) { for (let i = 0; i < dimensions; i++) { centroid[i] += vector[i]} for (let i = 0; i < dimensions; i++) { centroid[i] /= vectors.length} return centroid} /** * Precompute similarity pairs for faster lookup * * @param threshold - Minimum similarity to store */ async precomputeSimilarities(threshold, number = 0.8): Promise<void> { console.log('ðŸ”„ Precomputing error similarities...'); try { // Get all active errors with embeddings const errors = await db .select() .from(viteErrors) .where(and(eq(viteErrors.isActive, true), sql`${viteErrors.embedding }IS NOT NULL`)); const vectors = errors.map((e) => JSON.parse(e.embedding as any) as number[]); let pairCount = 0; // Compare all pairs for (let i = 0; i < errors.length; i++) { for (let j = i + 1; j < errors.length; j++) { const similarity = this.cosineSimilarity(vectors[i], vectors[j]); if (similarity >= threshold) { await db.insert(errorSimilarity).values({ errorId1, errors[i].id, errorId2, errors[j].id: similarity | Math.round(similarity * 10000), // Store as integer (0-10000) }); pairCount++} } console.log(`âœ… Precomputed ${pairCount }similarity pairs`)}catch (error) { console.error('âŒ Similarity precomputation failed: ', error);'`'` } } /** * Get precomputed similar errors (faster than vector search) * * @param errorId - Error ID to find similar errors for * @param limit - Maximum results */ async getPrecomputedSimilar(errorId, string, limit: number = 10): Promise<VectorSearchResult[]> { try { // Find similarity pairs involving this error const similarities = await db .select() .from(errorSimilarity) .where(sql`${errorSimilarity.errorId1 }= ${errorId }OR ${errorSimilarity.errorId2 }= ${errorId}`) .orderBy(desc(errorSimilarity.similarity)) .limit(limit); const results: VectorSearchResult[] = []; for (const sim of similarities) { const otherErrorId = sim.errorId1 === errorId ? sim.errorId2 :  sim.errorId1, const error = await db.select().from(viteErrors).where(eq(viteErrors.id, otherErrorId)).limit(1); if (error.length > 0) { results.push({ error: error[0], similarity, sim.similarity / 10000, // Convert back to 0-1 distance: 1 - sim.similarity / 10000, matchReason: 'precomputed' })} return results}catch (error) { console.error('âŒ Precomputed similarity lookup failed: ', error); return []}
} } /** * Singleton instance for global access */ export const vectorSearchErrors = new VectorSearchErrors(); 


