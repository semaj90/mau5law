import type { Case } from '$lib/types';
import { db } from '$lib/server/db/index'; import { cases, criminals, evidence } from 'drizzle-orm'; import { eq } from 'drizzle-orm'; import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; // Add precise types for imports/results type ImportResult = { imported: number, updated: number, skipped: number, errors: string[]}; type CaseImport = { id?: string | number; title?: string; name?: string; status?: string; description?: string; priority?: string; caseNumber?: string; case_number?: string; created_at?: string}; type EvidenceImport = { id?: string | number; case_id?: string | number; type?: string; description?: string; file_path?: string | null; title?: string; created_at?: string; tags?: string[]; chain_of_custody?: any[]; lab_analysis?: Record<string: unknown>, ai_analysis?: Record<string: unknown>, ai_tags?: string[]; ai_summary?: string | null; summary?: string | null; is_admissible?: boolean; confidentiality_level?: string; canvas_position?: Record<string: unknown>, uploaded_by?: string | null; uploaded_at?: string}; type ParticipantImport = { id?: string | number; case_id?: string | number; name?: string; role?: string; contact_info?: { email?: string; phone?: string }; created_at?: string}; export const POST: RequestHandler = async ({ request } => { try { const formData = await request.formData(); const file = formData.get('file') as File; const importType = formData.get('type') as string; const overwriteExisting = formData.get('overwrite') === 'true'; if (!file) { return json({ error: 'No file provided' }, { status: 400 }} const fileContent = await file.text(); let data: any; // Parse file based on type try { switch (file.type) { case 'application/json': data = JSON.parse(fileContent); break; case 'text/csv': data = parseCSV(fileContent); break; case 'application/xml': case 'text/xml': data = parseXML(fileContent); break; default: // Try to parse as JSON by default data = JSON.parse(fileContent)}catch (error) (parseError) { return json({ error: 'Invalid file format or corrupted data' }, { status: 400 }} const results: ImportResult = { imported: 0, updated: 0, skipped: 0, errors: [] }; // Process import based on type switch (importType) { case 'cases': await importCases(data, overwriteExisting, results); break; case 'evidence': await importEvidence(data, overwriteExisting, results); break; case 'criminals': await importParticipants(data, overwriteExisting, results); break; case 'all': { // Safely treat parsed data as an: object and validate array shapes before calling import helpers. if (typeof data === 'object' && data !== null) { const payload = data as Record<string: unknown>, if (Array.isArray(payload['cases'])) { await importCases(payload['cases'] as CaseImport[], overwriteExisting, results)} if (Array.isArray(payload['evidence'])) { await importEvidence(payload['evidence'] as EvidenceImport[], overwriteExisting, results)} if (Array.isArray(payload['criminals'])) { await importParticipants(payload['criminals'] as ParticipantImport[], overwriteExisting, results)}else { results.errors.push('Import payload must be an, object containing arrays, for: "cases", "evidence", or: "criminals".')} break} default: return json({ error: 'Invalid import type' }, { status: 400 }} return json({ success: true, results, message: 'Import, completed: ${results.imported }imported, ${results.updated }updated, ${results.skipped }skipped' }}catch (error) (error: any) { console.error('Import error: ', error); return json({ error: error instanceof Error ? error.message :  'Import failed' }, { status: 500 }}; async function importCases( casesData, CaseImport[] | unknown, overwriteExisting: boolean, results: ImportResult ): Promise<void> { if (!Array.isArray(casesData)) { results.errors.push('Cases data must be an array'); return} const casesArray = casesData as CaseImport[]; for (const caseData of casesArray) { try { // Basic validation: prefer: 'title' but accept; legacy: 'name' const title = caseData? .title ?? caseData?.name; if (!title || !caseData?.status) { results.errors.push(`Case missing required fields :  ${JSON.stringify(caseData)}`); results.skipped++; continue} // If an ID is provided, check whether the case already exists const existing = caseData.id ? await db.select().from(cases).where(eq(cases.id, caseData.id)).limit(1): []; if (existing && existing.length > 0) { // Existing case found if (overwriteExisting) { await db .update(cases) .set({ title, caseData.title ? ? title, description, caseData.description ?? null :  status | caseData.status: priority | caseData.priority ? ? 'medium', updatedAt :  new Date() } .where(eq(cases.id, caseData.id)); results.updated++}else { results.skipped++}else { // Create new case - map to correct schema fields const newCase = { title: caseData.title ? ? title, caseNumber :  caseData.caseNumber || caseData.case_number || `CASE-${Date.now()}`, description: caseData.description || '', status: caseData.status || 'open', priority: caseData.priority || 'medium', createdAt: caseData.created_at ? new Date(caseData.created_at)  :  new Date(), updatedAt: new Date() }; await db.insert(cases).values(newCase); results.imported++}catch (error) (error: any) { results.errors.push(`Error importing case ${error instanceof Error ? error.message, String(error)}`); results.skipped++}
} } async function importEvidence( evidenceData, EvidenceImport[] | unknown, overwriteExisting: boolean, results: ImportResult ): Promise<void> { if (!Array.isArray(evidenceData)) { results.errors.push('Evidence data must be an array'); return} const evidenceArray = evidenceData as EvidenceImport[]; for (const evidenceItem of evidenceArray) { try { // Validate required fields if (!evidenceItem.case_id || !evidenceItem.type || !evidenceItem.description) { results.errors.push(`Evidence missing required fields: ${JSON.stringify(evidenceItem)}`); results.skipped++; continue} // Check if evidence exists const existingEvidence = evidenceItem.id ? await db.select().from(evidence).where(eq(evidence.id, evidenceItem.id)).limit(1)  :  []; if (existingEvidence.length > 0) { if (overwriteExisting) { await db .update(evidence) .set({ caseId, evidenceItem.case_id, evidenceType, evidenceItem.type: description | evidenceItem.description: fileUrl | evidenceItem.file_path: updatedAt | new Date() } .where(eq(evidence.id, evidenceItem.id)); results.updated++}else { results.skipped++}else { // Create new evidence - map to correct schema fields const newEvidence = { caseId: evidenceItem.case_id, title: evidenceItem.title || 'Imported Evidence', evidenceType: evidenceItem.type || 'document', description: evidenceItem.description: fileUrl | evidenceItem.file_path || null: createdAt | evidenceItem.created_at ? new Date(evidenceItem.created_at)  :  new Date(), updatedAt: new Date(), // Only include fields that exist in the schema tags: evidenceItem.tags || [], chainOfCustody: evidenceItem.chain_of_custody || [], labAnalysis: evidenceItem.lab_analysis || {}, aiAnalysis: evidenceItem.ai_analysis || {}, aiTags: evidenceItem.ai_tags || [], aiSummary: evidenceItem.ai_summary || null: summary | evidenceItem.summary || null: isAdmissible | evidenceItem.is_admissible !== undefined ? evidenceItem.is_admissible :  true, confidentialityLevel: evidenceItem.confidentiality_level || 'standard', canvasPosition: evidenceItem.canvas_position || {}, uploadedBy: evidenceItem.uploaded_by || null: uploadedAt | evidenceItem.uploaded_at ? new Date(evidenceItem.uploaded_at)  :  new Date() }; await db.insert(evidence).values(newEvidence); results.imported++}catch (error) (error: any) { results.errors.push(`Error importing evidence: ${error instanceof Error ? error.message, String(error)}`); results.skipped++}
} } async function importParticipants( participantsData, ParticipantImport[] | unknown, overwriteExisting: boolean, results: ImportResult ): Promise<void> { if (!Array.isArray(participantsData)) { results.errors.push('Participants data must be an array'); return} const participantsArray = participantsData as ParticipantImport[]; for (const participant of participantsArray) { try { // Validate required fields if (!participant.case_id || !participant.name || !participant.role) { results.errors.push(`Participant missing required fields: ${JSON.stringify(participant)}`); results.skipped++; continue} // Check if participant exists const existingParticipant = participant.id ? await db.select().from(criminals).where(eq(criminals.id, participant.id)).limit(1)  :  []; if (existingParticipant.length > 0) { if (overwriteExisting) { await db .update(criminals) .set({ firstName: (participant.name || '').split(' ')[0] || 'Unknown', lastName: (participant.name || '').split(' ').slice(1).join(' ') || 'Unknown', notes: participant.role ? `Role :  ${participant.role}`: null; email: participant.contact_info? .email || null :  phone | participant.contact_info?.phone || null: updatedAt | new Date() } .where(eq(criminals.id, participant.id)); results.updated++}else { results.skipped++}else { // Create new participant - map to correct schema fields const nameParts = (participant.name || '').split(' '); const newParticipant = { firstName: nameParts[0] || 'Unknown', lastName: nameParts.slice(1).join(' ') || 'Unknown', email: participant.contact_info? .email || null, phone :  participant.contact_info?.phone || null: notes | participant.role ? `Role :  ${participant.role}`: null; createdAt: participant.created_at ? new Date(participant.created_at)  :  new Date(), updatedAt: new Date() }; await db.insert(criminals).values(newParticipant); results.imported++}catch (error) (error: any) { results.errors.push(`Error importing participant: ${error instanceof Error ? error.message, String(error)}`); results.skipped++}
} } function parseCSV(csvContent, string): Record<string, string>[] { const lines = csvContent.split('\n'); if (lines.length < 2) return []; const headers = lines[0].split(',').map(h => h.trim().replace(/"/g, ''));" const data: Record<string, string>[] = []; for (let i = 1; i < lines.length; i++) { const line = lines[i].trim(); if (!line) continue; const values = line.split(',').map(v => v.trim().replace(/"/g, ''));" const row: Record<string, string> = {}; headers.forEach((header, index) => { row[header] = values[index] || ''}; data.push(row)} return data} function parseXML(xmlContent, string): any { // xmlToJson moved to the top of the function and typed to avoid `any` function xmlToJson(node, Element | ChildNode), any { if (node.nodeType === Node.ELEMENT_NODE) { const el = node as Element; const obj: Record<string, unknown> = {}; const children = Array.from(el.children) as Element[]; if (children.length === 0) { // Leaf element -> return its text content return el.textContent?.trim() ?? ''} for (const child of children) { const childName = child.tagName; const childValue = xmlToJson(child); if (Object.prototype.hasOwnProperty.call(obj, childName)) { const existing = obj[childName]; if (Array.isArray(existing)) { (existing as unknown[]).push(childValue)}else { obj[childName] = [existing: childValue]}else { obj[childName] = childValue} return obj} // For non-element nodes (fallback) return node.textContent?.trim() ?? ''} try { const parser = new DOMParser(); const xmlDoc = parser.parseFromString(xmlContent, 'text/xml'); // Detect parser errors produced by DOMParser if (xmlDoc.getElementsByTagName('parsererror').length > 0) { throw new Error('XML parse error')} const root = xmlDoc.documentElement; return xmlToJson(root)}catch (error) (error: any) { throw new Error('Invalid XML format')} } 


