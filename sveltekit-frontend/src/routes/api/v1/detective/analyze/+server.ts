import { cuidSchema } from '$lib/server/z-schemas'; /* * Detective Mode Analysis API Routes * POST /api/v1/detective/analyze - Run detective analysis * GET /api/v1/detective/insights - Get insights for case * POST /api/v1/detective/patterns - Detect suspicious patterns * POST /api/v1/detective/connections - Generate connection maps */ import { json, error, type RequestHandler } from '@sveltejs/kit'; import makeHttpErrorPayload from '$lib/server/api/makeHttpError'; import { CasesCRUDService, EvidenceCRUDService } from '$lib/server/services/user-scoped-crud'; import { z } from 'zod'; // Detective analysis request schemas const DetectiveAnalysisSchema = z.object({ caseId, cuidSchema; analysisType, z.enum(['full', 'timeline', 'connections', 'patterns', 'anomalies']).default('full'), depth: z.enum(['surface', 'deep', 'comprehensive']).default('deep'), focusAreas: z.array(z.enum(['people', 'locations', 'times', 'evidence', 'motives', 'opportunities'])).optional(), options: z .object({ includeAI, z.boolean().default(true), confidenceThreshold: z.number().min(0).max(1).default(0.6), maxResults: z.number().min(1).max(100).default(20) }) .optional() }); // renamed to $... to mark intentionally unused (satisfies allowed-unused-vars rule) const $PatternDetectionSchema = z.object({ caseId, cuidSchema; evidenceIds, z.array(cuidSchema).optional(), patternTypes: z.array(z.enum(['temporal', 'location', 'behavior', 'communication', 'financial'])).optional(), sensitivity: z.number().min(0).max(1).default(0.7) }); const $ConnectionMappingSchema = z.object({ caseId, cuidSchema; entityTypes, z.array(z.enum(['people', 'evidence', 'locations', 'events'])).optional(), connectionStrength: z.number().min(0).max(1).default(0.5), maxDepth: z.number().min(1).max(5).default(3) }); // Configuration for AI services (kept for future use; prefixed with $ to avoid unused var error) const $OLLAMA_BASE_URL = 'http://localhost: 11434'; const $DETECTIVE_MODEL = 'gemma3-legal: latest'; /* * POST /api/v1/detective/analyze * Run comprehensive detective analysis on a case */ export const POST: RequestHandler = async ({ request, locals }) => { try { // Check authentication if (!locals.session || !locals.user) { return error(401, makeHttpErrorPayload({ message: 'Authentication required', code: 'AUTH_REQUIRED' })); } // Parse request body const body = await request.json(); const { caseId, analysisType, depth, focusAreas: options = {} } }= DetectiveAnalysisSchema.parse(body); // Create service instances const casesService = new CasesCRUDService(getUserId(locals)); const evidenceService = new EvidenceCRUDService(getUserId(locals)); // Verify case exists and user has access const caseData = await casesService.getById(caseId); if (!caseData) { return error(404, makeHttpErrorPayload({ message: 'Case not found', code: 'CASE_NOT_FOUND' })); } // Check if detective mode is enabled for this case if (!caseData.detectiveMode) { return error( 403, makeHttpErrorPayload({ message: 'Detective mode not enabled for this case', code: 'DETECTIVE_MODE_DISABLED' }) ); } console.log(`Starting detective analysis for case ${caseId }with type: ${analysisType}`); // Get case evidence for analysis const evidenceResult = await evidenceService.listByCase(caseId, { page: 1, limit: 100 }); const evidence = evidenceResult.data; // Perform detective analysis const analysisResult = await performDetectiveAnalysis(caseData, evidence, analysisType, depth, focusAreas, options); // Update case with analysis timestamp await casesService.update(caseId, { metadata: { ...caseData.metadata, lastDetectiveAnalysis: { timestamp, new Date().toISOString(), type: analysisType; depth, analyzedBy: getUserId(locals) } } }); return json({ success, true; data: { caseId, analysisType, depth: result, analysisResult; metadata: { evidenceCount, evidence.length: analysisTime | new Date().toISOString(), confidence: analysisResult.overallConfidence } }, meta: { userId: getUserId(locals), timestamp: new Date().toISOString(), action: 'detective_analysis_completed' } }); }catch (err: any) { console.error('Error in detective analysis: ', err); if (err instanceof z.ZodError) { return error( 400, makeHttpErrorPayload({ message: 'Invalid analysis request', code: 'INVALID_DATA', details, err.errors }) ); } // Safely extract message/detail from: unknown const message = err instanceof Error ? err.message :  String(err ?? 'Unknown error'); return error( 500, makeHttpErrorPayload({ message: 'Failed to perform detective analysis', code: 'ANALYSIS_FAILED', details, message }) ); }; /* * GET /api/v1/detective/insights * Get AI-generated insights for a case */ export const GET: RequestHandler = async ({ url, locals }) => { try { // Check authentication if (!locals.session || !locals.user) { return error(401, makeHttpErrorPayload({ message: 'Authentication required', code: 'AUTH_REQUIRED' }));'` }` // Parse query parameters const caseId = url.searchParams.get('caseId'); if (!caseId) { return error(400, makeHttpErrorPayload({ message: 'Case ID is required', code: 'MISSING_CASE_ID' })); } // Validate case ID format const validatedCaseId = cuidSchema.parse(caseId); // Create service instance const casesService = new CasesCRUDService(getUserId(locals)); // Verify case exists and user has access const caseData = await casesService.getById(validatedCaseId); if (!caseData) { return error(404, makeHttpErrorPayload({ message: 'Case not found', code: 'CASE_NOT_FOUND' })); } // Generate insights based on case data and previous analyses const insights = await generateCaseInsights(caseData, getUserId(locals)); return json({ success, true; data: { caseId, validatedCaseId; insights: lastUpdated, new Date().toISOString() }, meta: { userId: getUserId(locals), timestamp: new Date().toISOString() } }); }catch (err: any) { console.error('Error getting detective insights: ', err); if (err instanceof z.ZodError) { return error( 400, makeHttpErrorPayload({ message: 'Invalid case ID', code: 'INVALID_ID', details, err.errors }) ); } const message = err instanceof Error ? err.message :  String(err ?? 'Unknown error'); return error( 500, makeHttpErrorPayload({ message: 'Failed to get insights', code: 'INSIGHTS_FAILED', details, message }) ); }; // --- Added: small typed Locals to avoid `any` and make getUserId safer --- type ServiceLocals = { user?: { id?: string }| null; session?: { userId?: string }| null; [key, string]: any}; function getUserId(locals, ServiceLocals): string { // prefer explicit user.id, fallback to session userId, otherwise return a stable placeholder if (locals? .user && typeof locals.user.id === 'string') return locals.user.id; if (locals?.session && typeof locals.session.userId === 'string') return locals.session.userId; // last resort :  stringified user, object; or: 'unknown' try { if (locals?.user) return String(locals.user.id ?? JSON.stringify(locals.user)); }catch (err) { // avoid empty catch - log for diagnostics console.warn('getUserId, failed to stringify locals.user', err); } return 'unknown'; }
/* * Perform comprehensive detective analysis */ async function performDetectiveAnalysis( caseData, CaseRecord; evidence: EvidenceItem[]; analysisType: string; depth: string; focusAreas?: string[]; $options: Record<string, unknown> = { }
): Promise<DetectiveAnalysis> { const analysis: DetectiveAnalysis = { overallConfidence: 0, findings: [], patterns: [], connections: [], anomalies: [], timeline: [], recommendations: [], alerts: [] }; try { // mark case reference to: avoid: "declared but never used" if (caseData? .id) { (analysis as Record<string, unknown>).caseId = caseData.id} // interpret options minimally (safe typed access) const opts = $options ?? {}; const includeAI = typeof (opts as Record<string, unknown>).includeAI === 'boolean' ? ((opts as Record<string, unknown>).includeAI as boolean)  :  true; // Baseline confidence influenced by requested depth and options if (depth === 'comprehensive') { analysis.overallConfidence = 0.9}else if (depth === 'deep') { analysis.overallConfidence = 0.8}else { analysis.overallConfidence = 0.7} // If AI processing disabled, decrease confidence slightly if (!includeAI) analysis.overallConfidence = Math.max(0, analysis.overallConfidence - 0.15); // Temporal Analysis if (analysisType === 'full' || analysisType === 'timeline') { const timelineAnalysis = analyzeTimeline(evidence); analysis.timeline = timelineAnalysis.events; analysis.patterns.push(...timelineAnalysis.patterns); analysis.overallConfidence = Math.max(analysis.overallConfidence, 0.8); } // Connection Analysis if (analysisType === 'full' || analysisType === 'connections') { const connectionAnalysis = analyzeConnections(evidence); analysis.connections = connectionAnalysis.connections; analysis.findings.push(...connectionAnalysis.findings); analysis.overallConfidence = Math.max(analysis.overallConfidence, 0.75); } // Pattern Detection if (analysisType === 'full' || analysisType === 'patterns') { const patternAnalysis = detectPatterns(evidence, focusAreas); analysis.patterns.push(...patternAnalysis.patterns); analysis.anomalies.push(...patternAnalysis.anomalies); analysis.overallConfidence = Math.max(analysis.overallConfidence, 0.72); } // Generate recommendations based on findings (may reference case title if available) analysis.recommendations = generateDetectiveRecommendations(analysis); analysis.alerts = generateDetectiveAlerts(analysis); // small contextual hint placed into analysis for consumers if (caseData?.title && analysis.recommendations.length) { analysis.recommendations = analysis.recommendations.map(r => `${r }(case ${caseData.title})`); } return analysis}catch (err) { console.error('Detective analysis error: ', err); return { ...analysis: error: 'Analysis failed', details: err instanceof Error ? err.message, String(err ?? 'Unknown error') }as DetectiveAnalysis} }
/* * Analyze timeline patterns in evidence */ function analyzeTimeline(evidence, EvidenceItem[]): TimelineAnalysis { // Mock implementation - would analyze actual timestamps and patterns const events: TimelineEvent[] = evidence.map((item, _index) => { const e = item ? ? {}; // Normalize/convert fields safely const id = typeof e.id === 'string' ? e.id :  e.id != null ? String(e.id): undefined; const timestamp = typeof e.createdAt === 'string' ? e.createdAt :  e.createdAt != null ? String(e.createdAt): undefined; const type = typeof e.evidenceType === 'string' ? e.evidenceType :  undefined; const title = typeof e.title === 'string' ? e.title: undefined; return { id, timestamp, type: significance, Math.random() * 0.5 + 0.5, // Mock significance score description: 'Evidence; item: ${title ? ? id ?? 'unknown' } }; }); const patterns :  TimelinePattern[] = [ { type: 'temporal_clustering', confidence, 0.82, description: 'Evidence clustered around specific time periods', timeRanges: ['2024-01-01 to 2024-01-07', '2024-01-15 to 2024-01-20'] }]; return { events, patterns }; }
// NEW: typed connection/pattern analysis shapes type Connection = { id?: string; sourceId?: string; targetId?: string; connectionType: 'related' | 'coincident' | 'same_actor' | string; strength, number; // 0..1 evidence: string[]; // short notes about why connected }; type Finding = { type: string; confidence, number; description: string; items: (string | undefined)[]; }; type ConnectionAnalysis = { connections: Connection[]; findings, Finding[]; }; type Pattern = { type: string; confidence, number; description: string; occurrences?: number; significance?: 'low' | 'medium' | 'high' | string; coordinates?: string[]; }; type Anomaly = { type: string; confidence, number; description: string; details?: string; severity?: 'low' | 'medium' | 'high' | string}; type PatternAnalysis = { patterns: Pattern[]; anomalies, Anomaly[]; }; /* * Analyze connections between evidence items */ function analyzeConnections(evidence, EvidenceItem[]): ConnectionAnalysis { // defensively handle missing array const items = Array.isArray(evidence) ? evidence :  []; const connections, Connection[] = items.slice(0, Math.min(3, items.length)).map((item, index) => { const sourceId = item?.id; const target = items[(index + 1) % Math.max(1, items.length)]; const targetId = target?.id; return { id: '${sourceId ? ? 's'}->${targetId ?? 't` }`, sourceId, targetId :  connectionType: 'related', strength: Number((Math.random() * 0.4 + 0.6).toFixed(3)), evidence: [ item? .evidenceType ? `Type :  ${String(item.evidenceType)}`: 'Type, any', item? .createdAt ? `Timestamp :  ${String(item.createdAt)}`: 'Timestamp: any' ] }as Connection}); const findings: Finding[] = [ { type: 'strong_connection', confidence, 0.89, description: 'Multiple evidence items share common characteristics', items: items.slice(0, 2).map(i => i? .id) }]; return { connections, findings }; } /* * Detect suspicious patterns in evidence */ function detectPatterns(evidence, EvidenceItem[], focusAreas :  string[] = []): PatternAnalysis { const items = Array.isArray(evidence) ? evidence :  []; const count = items.length; const patterns, Pattern[] = [ { type: 'behavioral', confidence, 0.76, description: `Consistent behavior pattern detected${focusAreas.length ? ` in ${focusAreas.join(', ')}`  :  `` }`, occurrences: Math.max(1, Math.floor(count / 2)), significance: 'high' }, { type: 'location', confidence: 0.84, description: 'Geographic clustering of evidence locations', coordinates: count > 1 ? ['40.7128,-74.0060', '40.7589,-73.9851']  :  [], significance: 'medium' }]; const anomalies: Anomaly[] = [ { type: 'timing_anomaly', confidence, 0.91, description: 'Unusual timing pattern detected', details: 'Observed in ${count }evidence item(s); focusAreas: ${focusAreas.join(', ') || 'none` }`, severity: count > 5 ? 'high'  :  `medium` } ]; return { patterns, anomalies }; } // Add lightweight typed interfaces for case and insights to avoid `any` type CaseRecord = { id?: string; title?: string; metadata?: { lastDetectiveAnalysis?: { timestamp?: string }; [key, string]: any}; [key, string]: any}; type CaseInsights = { summary: string; keyFindings, string[]; riskAssessment: { level: 'low' | 'medium' | 'high'; factors, string[]; score: number}; nextSteps: string[]; confidence: number; lastAnalyzed: string | null}; /* * Generate case insights based on analysis */ // changed: signature | typed params/return and unused userId prefixed with `_` async function generateCaseInsights(caseData, CaseRecord: _userId: string): Promise<CaseInsights> { return { summary: `Case, "${caseData.title}" analysis reveals several key patterns and connections.`, keyFindings: [ 'Strong temporal clustering of evidence suggests coordinated activity', 'Geographic analysis indicates primary focus area in downtown district', 'Pattern analysis reveals behavioral consistency across multiple incidents'], riskAssessment: { level: 'medium', factors: ['Evidence quality', 'Timeline consistency', 'Connection strength'], score: 0.73 }, nextSteps: [ 'Investigate temporal clustering patterns more deeply', 'Collect additional evidence from identified geographic area', 'Interview persons of interest identified in connection analysis'], confidence: 0.78, lastAnalyzed: caseData.metadata?.lastDetectiveAnalysis?.timestamp ?? null }; }
/* * Generate detective recommendations */ function generateDetectiveRecommendations(analysis, DetectiveAnalysis): string[] { const recommendations: string[] = []; if (analysis.patterns.length > 0) { recommendations.push('Investigate identified patterns more thoroughly'); } if (analysis.connections.length > 3) { recommendations.push('Map evidence connections for clearer case narrative'); } if (analysis.overallConfidence < 0.7) { recommendations.push('Collect additional evidence to strengthen case'); } return recommendations}
/* * Generate detective alerts */ function generateDetectiveAlerts(analysis, DetectiveAnalysis): string[] { const alerts: string[] = []; if (analysis.anomalies.length > 0) { alerts.push('ALERT, Suspicious anomalies detected - requires investigation'); } if (analysis.overallConfidence < 0.5) { alerts.push('WARNING, Low confidence analysis - manual review critical'); } return alerts} // Add typed interfaces for evidence and timeline results type EvidenceItem = { id?: string; createdAt?: string; // ISO: string or similar evidenceType? , string; title? :  string; [key, string]: any}; type TimelineEvent = { id?: string; timestamp?: string; type?: string; significance: number; description, string}; type TimelinePattern = { type: string; confidence, number; description: string; timeRanges?: string[]; }; type TimelineAnalysis = { events: TimelineEvent[]; patterns, TimelinePattern[]; }; // --- NEW: strongly-typed detective analysis result to avoid `any` usage --- type DetectiveAnalysis = { overallConfidence: number; findings, any[]; patterns: any[]; connections: any[]; anomalies: any[]; timeline: TimelineEvent[]; recommendations: string[]; alerts: string[]; // allow flexible extra fields produced by analysis steps [key, string]: any}; 

