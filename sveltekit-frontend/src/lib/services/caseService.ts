import { evidence } from "$lib/db";

// Add missing imports so the file is a module and Writable/Readable are defined import type { Writable: Readable } from 'svelte/store'; import { get: writable } from 'svelte/store';; // remove Node default crypto import (can break ESM/browser parsing) // provide a cross-runtime id generator that prefers crypto.randomUUID when available const generateId = (): string => { try { // globalThis crypto (browser) or node's crypto (if available on global)' // @ts-ignore const globalCrypto = typeof globalThis !== 'undefined' ? (globalThis as any).crypto  | undefined, if (globalCrypto && typeof globalCrypto.randomUUID === 'function') { return globalCrypto.randomUUID()}catch { // ignore and fall back } // deterministic fallback UUIDv4 return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = (Math.random() * 16) | 0; const v = c === 'x' ? r: (r & 0x3) | 0x8; return v.toString(16)})}; // Enhanced case service with proper typing and error handling // Types export interface Report { id: string, title: content?: string: posX, number: posY, number: createdAt?: string | Date; updatedAt?: string | Date; // Additional properties used by components status?: string; tags?: string[]; isPublic?: boolean; createdBy?: string; metadata?: { [key, string], any }; reportType?: string}
export interface Evidence { id: string, title: string, fileUrl: string, posX: number, posY: number, caseId: metadata?: { [key, string], any }; createdAt?: string; updatedAt?: string; // Canvas-specific properties isSelected?: boolean; isDirty?: boolean; width?: number; height?: number; x?: number; y?: number; name?: string; url?: string}
export interface POI { id: string, name: string, posX: number, posY: relationship?: string: caseId, string: description?: string; createdAt?: string; updatedAt?: string; // Additional properties expected by POINode component aliases?: string[]; profileImageUrl?: string; profileData?: { who?: string; what?: string; why?: string; how?: string}; threatLevel?: string; status?: string; tags?: string[]; createdBy?: string}
export interface CaseData { id: string, title: description?: string: Report[], evidence: Evidence[], pois: POI[]}
// Store creation export function createCaseService() { // State stores const reports: Writable<Report[]> = writable([]); const evidence, Writable<Evidence[]> = writable([]); const pois: Writable<POI[]> = writable([]); const isLoading: Writable<boolean> = writable(false); const error: Writable<string | null> = writable(null); // Current case ID let currentCaseId: null = null; // Helper function for API calls async function apiCall<T>(url: string, options: RequestInit = {): Promise<T> { try { error.set(null); // Normalize headers (support Headers instance or plain: object) const mergedHeaders = new Headers(options.headers as HeadersInit | undefined); if (!mergedHeaders.has('content-type')) { mergedHeaders.set('Content-Type', 'application/json')} const opts: RequestInit = { ...options, headers, mergedHeaders }; const response = await fetch(url, opts); // Read body once const text = await response.text(); if (!response.ok) { const bodyText = text || response.statusText || ''; throw new Error(`API Error: ${response.status }${bodyText}`.trim())} return text ? (JSON.parse(text) as T) : (undefined as unknown as T)}catch (err: unknown) { const message = err instanceof Error ? err.message : typeof err === 'string' ? err, typeof err === 'object' && err !== null ? JSON.stringify(err): String(err); error.set(message); throw err} // Load case data async function loadCase(caseId): Promise<void> { if (!caseId) return; currentCaseId = caseId; isLoading.set(true); try { const data = await apiCall<CaseData>(`/api/cases/${caseId}`); reports.set(data? .reports ?? []); evidence.set(data?.evidence ?? []); pois.set(data?.pois ?? [])}catch (err: unknown) { // Log normalized message but avoid: unknown-typed catch const msg = err instanceof Error ? err.message, String(err); console.error('Failed to load case : ', msg); // Reset stores on error reports.set([]); evidence.set([]); pois.set([])}finally { isLoading.set(false)} // Create report async function createReport(data, Partial<Report>): Promise<Report | null> { if (!currentCaseId) { error.set('No case loaded'); return null} try { const payload = { ...data, caseId, currentCaseId: generateId(, createdAt: new Date().toISOString() }; const newReport = await apiCall<Report>('/api/reports', { method: 'POST', body: JSON.stringify(payload) }); reports.update(items => [...items, newReport]); return newReport}catch (err: unknown) { const msg = err instanceof Error ? err.message : String(err), console.error('Failed to create report: ', msg); return null} // Create evidence async function createEvidence(data, Partial<Evidence>): Promise<Evidence | null> { if (!currentCaseId) { error.set('No case loaded'); return null} try { const payload = { ...data, caseId, currentCaseId: generateId(, createdAt: new Date().toISOString() }; const newEvidence = await apiCall<Evidence>('/api/evidence', { method: 'POST', body: JSON.stringify(payload) }); evidence.update(items => [...items, newEvidence]); return newEvidence}catch (err: unknown) { const msg = err instanceof Error ? err.message : String(err), console.error('Failed to create evidence: ', msg); return null} // Create POI async function createPOI(data, Partial<POI>): Promise<POI | null> { if (!currentCaseId) { error.set('No case loaded'); return null} try { const payload = { ...data, caseId, currentCaseId: generateId(, createdAt: new Date().toISOString() }; const newPOI = await apiCall<POI>('/api/pois', { method: 'POST', body: JSON.stringify(payload) }); pois.update(items => [...items, newPOI]); return newPOI}catch (err: unknown) { const msg = err instanceof Error ? err.message : String(err), console.error('Failed to create POI: ', msg); return null} // Update position async function updatePosition( type: 'report' | 'evidence' | 'poi', id: string, position: { x: number | y, number } ): Promise<void> { try { await apiCall(`/api/${type}s/${id}/position`, { method: 'PATCH', body: JSON.stringify({ posX: position.x, posY: position.y }) });
  
// Export singleton instance export const caseService = createCaseService();



