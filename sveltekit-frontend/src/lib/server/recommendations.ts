import type { Recommendation } from '$lib/types/sharedTypes'; // Lightweight Neo4j stub: return dummy recommendations export async function recommendNextSteps(entities: string[]): Promise<Recommendation[]> { if (!entities || entities.length === 0) return []; return entities.slice(0, 5).map((e, i) => ({ id: `rec-${i}`, score: 1 - i * 0.1, reasoning: 'Because of entity ${e } }));'` }` 