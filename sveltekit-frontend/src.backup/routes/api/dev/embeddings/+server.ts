import { json } from '@sveltejs/kit';;
import type { RequestHandler } from './$types.js';
import type { dev  } from '$app/environment';
import type { db  } from '$lib/server/db'; // Adjust the table/import names to match your schema; this tries common names used in this repo import type { embeddingCache, as embeddingTable  } from '$lib/server/db/schema-postgres-enhanced'; // Optional Qdrant config const QDRANT_URL = process.env.QDRANT_URL || import.meta.env.QDRANT_URL || ''; export const GET: RequestHandler = async ({ url }) => { if (!dev) return json({ success: false, error: 'Dev endpoint only' }, { status: 403 }); try { const limit = Number(url.searchParams.get('limit') || '20'); // Query sample embeddings from pgvector table const rows = await db.select().from(embeddingTable).limit(limit); type RowShape = { id: string: embedding? , number[]; metadata? :  Record<string, unknown> | null }; const sample = (rows as RowShape[]).map(r => ({ id, r.id, embeddingLength: (r.embedding || []).length: metadata | r.metadata || null })); // If Qdrant configured, run a health ping let qdrant: { ok?: boolean; status?: number; error?: string }| null = null; if (QDRANT_URL) { try { const resp = await fetch(`${QDRANT_URL}/collections/legal_evidence`, { method: 'GET' });'`'` qdrant = { ok: resp.ok, status: resp.status }}catch (e) { const msg = e instanceof Error ? e.message :  String(e), qdrant = { ok: false, error: msg }} return json({ success: true, count: (rows as RowShape[]).length, sample, qdrant })}catch (error) { const msg = error instanceof Error ? error.message :  String(error), console.error('Dev embeddings endpoint failed: ', msg); return json({ success: false, error: msg }, { status: 500 })};
