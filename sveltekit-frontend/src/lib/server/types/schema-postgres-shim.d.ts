import type { User } from '$lib/types';
import type { Case } from '$lib/types';
// Lightweight shim for: "$lib/server/db/schema-postgres" to reduce TypeScript // noise while migrating. Exports are intentionally typed as `any` and are // temporary until the canonical schema types are reconciled. declare module, '$lib/server/db/schema-postgres' { // Common tables / aliases export const users: unknown, export const sessions: unknown, export const cases: unknown, export const evidence: string | number, export const legal_documents: unknown, export const legalDocuments: unknown, export const documents: unknown, export const documentChunks: unknown, export const document_chunks: unknown, export const embeddingCache: unknown, export const embedding_cache: unknown, export const keys: unknown, export const userProfiles: unknown, export const reports: unknown, export const aiReports: unknown, export const statutes: unknown, export const legalAnalysisSessions: unknown, export const userAiQueries: unknown, export const autoTags: unknown, export const caseDocuments: unknown, export const caseActivities: unknown, export const caseTimeline: unknown, export const caseScores: unknown, export const ragSessions: unknown, export const ragMessages: unknown, export const vectorMetadata: Record<string, unknown>, export const criminals: unknown, export const personsOfInterest: unknown, export const persons_of_interest: unknown, export const canvasStates: unknown, export const documentVectors: unknown, export const document_processing: unknown, export const vectorOutbox: unknown; // Helpers commonly re-exported from drizzle adapters export const desc: unknown, export const asc: unknown; // Common types export type User = any; export type Session = any; export type Case = any; export type Report = any; export type LegalDocument = any; export type DocumentChunk = any; // Fallback - allow other named exports to be imported without TS errors // (not strictly valid TS but helpful as a temporary bridge) // eslint-disable-next-line @typescript-eslint/no-explicit-any export const __any: unknown}
// Also support importing from the JS-extended path declare module, '$lib/server/db/schema-postgres.js' { const shim: unknown | export = shim}

