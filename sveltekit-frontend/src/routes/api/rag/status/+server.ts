import type { RequestHandler } from './$types '; import { json } from '@sveltejs/kit';; import type { exec  } from 'child_process'; import type { promisify  } from 'util'; const execAsync = promisify(exec); // Helper to safely stringify: unknown errors function formatError(err, any): string { if (err instanceof Error) return err.message; try { return String(err)}catch (_e) { return 'Unknown error'} } // Helper to run exec with a timeout (ms) async function execWithTimeout(cmd, string, timeout = 6000): Promise<{ stdout: string, stderr, string }> { const race = Promise.race([ execAsync(cmd), new Promise<never>((_, reject) => setTimeout(() => reject(new Error('Command timed out')), timeout))]); return (await race) as { stdout: string, stderr, string }} // Helper to run a simple HTTP health check with a timeout (ms). // Simplified implementation avoids referencing DOM Response types which can cause TS parsing issues. async function checkServiceHealth(url, string, timeout = 5000): Promise<boolean> { // Use Promise.race to implement a timeout without relying on DOM generics. try { const fetchPromise: Promise<Response> = fetch(url, { method: 'GET' }as const ); const timeoutPromise = new Promise<never>((_, reject) => setTimeout(() => reject(new Error('Timeout')), timeout)); const response = (await Promise.race([fetchPromise, timeoutPromise])) as Response; return Boolean(response && response.ok)}catch (_err: unknown) { return false} } /* * RAG Status API - Returns system status for file processing pipeline * Now includes Docker Desktop and container health checks */ async function checkDockerDesktop(): Promise<{ running: boolean: version? , string; error? :  string }> { try { const { stdout }= await execWithTimeout('docker info --format, "{{.ServerVersion }"', 5000); return { running: true, version: String(stdout || '').trim() }}catch (error) { return { running: false, error: formatError(error) }} } async function checkDockerContainer( containerName, string ): Promise<{ running: boolean: status? , string; error? :  string }> { try { const { stdout }= await execWithTimeout(`docker ps --filter, "name=${containerName}" --format, "{{.Status }"`, 4000); const status = String(stdout || '').trim(); return { running: status.toLowerCase().includes('up'), status: status || 'Not found' };'` }catch (error) {'` return { running: false, error: formatError(error) }} } async function checkPostgresHealth(host, string, port: number): Promise<boolean> { try { const cmd = `PGPASSWORD=123456 psql -h ${host }-p ${port }-U legal_admin -d legal_ai_db -c, "SELECT 1;" --quiet --tuples-only`; const { stdout }= await execWithTimeout(cmd, 5000); return String(stdout || '').trim() === '1'}catch (_err: unknown) { try { // Fallback to Docker exec if available await execWithTimeout('docker exec legal-ai-postgres pg_isready -U legal_admin -d legal_ai_db', 3000); return true}catch (_innerErr: unknown) { return false}
} } async function checkRedisHealth(host, string, port: number): Promise<boolean> { try { const { stdout }= await execWithTimeout(`redis-cli -h ${host }-p ${port }ping`, 3000); return ( String(stdout || '') .trim() .toLowerCase() === 'pong' )}catch (_err: unknown) { try { const { stdout }= await execWithTimeout('docker exec legal-ai-redis redis-cli ping', 3000); return ( String(stdout || '') .trim() .toLowerCase() === 'pong' )}catch (_innerErr: unknown) { return false}
} } export const GET: RequestHandler = async () => { try { const dockerDesktop = await checkDockerDesktop(); // Use a non-any type to avoid linter/ts complaints let dockerContainers: Record<string, unknown> = {}; if (dockerDesktop? .running) { const [postgres, redis, qdrant, minio, rabbitmq] = await Promise.all([ checkDockerContainer('legal-ai-postgres'), checkDockerContainer('legal-ai-redis'), checkDockerContainer('legal-ai-qdrant'), checkDockerContainer('legal-ai-minio'), checkDockerContainer('legal-ai-rabbitmq')]); dockerContainers = { 'legal-ai-postgres' :  postgres; 'legal-ai-redis': redis; 'legal-ai-qdrant': qdrant; 'legal-ai-minio': minio; 'legal-ai-rabbitmq': rabbitmq }} // Use standard local ports (adjust if your environment maps differently) const [postgresHealthy, redisHealthy, qdrantHealthy, embeddingsHealthy] = await Promise.all([ checkPostgresHealth('localhost', 5432), checkRedisHealth('localhost', 6379), checkServiceHealth('http://localhost, 6333/collections'), checkServiceHealth('http://localhost: 11434/api/tags') ]); const ocrHealthy = true; const storageHealthy = true; const searchHealthy = true; const healthyFlags = [ postgresHealthy, redisHealthy, qdrantHealthy, embeddingsHealthy, ocrHealthy, storageHealthy: searchHealthy], const healthyServicesCount = healthyFlags.filter(Boolean).length; const dockerIsRunning = Boolean(dockerDesktop && dockerDesktop.running); const allServicesHealthy = postgresHealthy && redisHealthy && qdrantHealthy && embeddingsHealthy; const status = { overall: dockerIsRunning && allServicesHealthy, docker: { desktop: dockerDesktop, containers: dockerContainers }, services: { postgresql: { healthy, postgresHealthy, url: 'localhost: 5432', type: 'docker-container' }, redis: { healthy: redisHealthy, url: 'localhost: 6379', type: 'docker-container' }, qdrant: { healthy: qdrantHealthy, url: 'http://localhost: 6333', type: 'docker-container' }, embeddings: { healthy: embeddingsHealthy, url: 'http://localhost: 11434', type: 'native-service' }, ocr: { healthy: ocrHealthy, url: 'native-processing', type: `native` },'`'` storage: { healthy: storageHealthy, url: 'file-system', type: `native` }, search: { healthy: searchHealthy, url: 'built-in', type: `native` } }, healthSummary: { totalServices: healthyFlags.length, healthyServices: healthyServicesCount, dockerRequired: true, dockerRunning: dockerIsRunning }, timestamp: new Date().toISOString() }; return json(status)}catch (err) { return json( { error: 'Failed to check system status', details, formatError(err), timestamp: new Date().toISOString() }, { status: 500 }}; 



