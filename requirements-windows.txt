# Windows / WSL Hybrid AI Stack Requirements
# -------------------------------------------------------------
# This file is structured in layers. Uncomment only what you need.
# Core goal: Run local inference, embeddings, and (optionally) TensorRT-LLM.
# Notes:
# - Prefer WSL2 for TensorRT-LLM compile & advanced quantization.
# - Keep Windows minimal (UI + orchestration). Heavy builds in WSL.
# - Versions pinned to known-stable (Sep 2025 window) with CUDA 12.x fallback.
# - If your host shows CUDA 13.0, ensure downstream toolchains actually support it;
#   most frameworks lag 1 minor release behind. Use CUDA 12.4/12.3 containers if issues arise.

########## CORE PYTHON RUNTIME ##########
# Minimal runtime utilities
packaging>=24.1
python-dotenv>=1.0.1
rich>=13.8.1
click>=8.1.7
pydantic>=2.9.0

########## NUMERIC / SCIENTIFIC ##########
# Numpy pinned to stable wheel for GPU libs interplay
numpy==1.26.4
scipy==1.13.1
sympy>=1.13
networkx>=3.3
# Optional performance libs
# einops>=0.8.0
# numba>=0.59.1   # (Skip on Windows without proper LLVM toolchain)

########## DEEP LEARNING (CPU fallback baseline) ##########
# You will REPLACE torch/torchvision/torchaudio with CUDA builds below.
# Keeping cpu wheels here commented so accidental install doesn't pull slow builds.
# torch==2.4.0
# torchvision==0.19.0
# torchaudio==2.4.0

########## PYTORCH (WINDOWS CUDA BUILD) ##########
# If CUDA 13 wheels are not published, use the latest cu121/cu122.
# Choose ONE of the below blocks.
# -- Option A: CUDA 12.1 (commonly available) --
# torch==2.4.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# torchvision==0.19.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# torchaudio==2.4.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# -- Option B: CUDA 12.4 (if wheels released) --
# torch==2.5.0+cu124 --index-url https://download.pytorch.org/whl/cu124
# torchvision==0.20.0+cu124 --index-url https://download.pytorch.org/whl/cu124
# torchaudio==2.5.0+cu124 --index-url https://download.pytorch.org/whl/cu124

########## TRANSFORMERS / TOKENIZATION ##########
transformers==4.44.2
accelerate==0.34.2
sentencepiece>=0.2.0
tokenizers==0.19.1
huggingface-hub>=0.24.6
safetensors>=0.4.4

########## EMBEDDINGS & VECTOR ##########
faiss-cpu==1.8.0.post1    # Windows: CPU only; use faiss-gpu in WSL if needed
# faiss-gpu==1.8.0.post1  # (Install inside WSL w/ CUDA)
qdrant-client>=1.9.1
pgvector>=0.2.5

########## SERVER / API ##########
fastapi==0.114.2
uvicorn[standard]==0.30.6
orjson>=3.10.7
pydantic-settings>=2.4.0

########## DATA / STORAGE ##########
redis==5.0.7
psycopg[binary,pool]==3.2.1
SQLAlchemy==2.0.34
alembic>=1.13.2

########## OBSERVABILITY ##########
loguru>=0.7.2
prometheus-client>=0.20.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp>=1.27.0

########## WEBGPU / WASM SUPPORT (Python side optional) ##########
# pywasm or wasm exec wrappers usually not needed here

########## NVIDIA / TENSOR INFRA (WSL strongly recommended) ##########
# Install only inside WSL after CUDA toolkit + TensorRT installed.
# tensorrt (Python wheel) often lags; version must match system libs.
# Example placeholder (uncomment and pin to actual detected version):
# tensorrt==10.0.1
# polygraphy>=0.49.9
# onnx==1.16.2
# onnxruntime-gpu==1.19.0  # (Do NOT mix with TensorRT pipeline unless isolated venv)

########## TENSORRT-LLM (DIRECT) ##########
# This is published to NVIDIA extra index. Use --extra-index-url at install time.
# Keep separate from base environment if building from source.
# tensorrt-llm==0.21.0

########## QUANT / OPTIMIZATION ##########
# bitsandbytes may fail on native Windows; use inside WSL with CUDA
# bitsandbytes==0.43.3
# optimum==1.21.4
# auto-gptq==0.7.1

########## TEST / DEV ##########
pytest>=8.3.2
pytest-asyncio>=0.23.8
httpx>=0.27.2
mypy>=1.11.2
ruff>=0.6.4
black>=24.8.0

########## OPTIONAL SECURITY ##########
python-jose==3.3.0
passlib[bcrypt]==1.7.4
cryptography>=43.0.0

########## LOCAL UTILITIES ##########
# ipykernel for notebook work
ipykernel>=6.29.5
# rich traces
stackprinter>=0.2.10

########## NOTES ##########
# 1. For TensorRT-LLM source build: omit tensorrt-llm here and install via pip -e . inside repo.
# 2. Keep a dedicated venv for TensorRT experimentation to avoid mixing binary deps.
# 3. If you enable faiss-gpu or bitsandbytes, verify CUDA compatibility first.
# 4. Pin transformers only if you rely on stable tokenization; otherwise allow minor updates.
