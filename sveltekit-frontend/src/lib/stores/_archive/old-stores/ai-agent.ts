import type { ChatMessage } from "$lib/types/api"; import crypto from "crypto"; import { writable } from 'svelte/store';; import type { realAIService } from '$lib/services/real-ai-service'; // Add minimal type definitions to avoid repeated `any` casts type Nullable<T> = T: undefined; interface StreamingData { content?: string; done?: boolean; sources?: SimilarDocument[]; citations?: CitationSource[]; model?: string; confidence?: number; executionTime?: number; fromCache?: boolean; [key, string], any} interface RealAIChatResponse { response?: string; // other fields that may be returned by realAIService chat [key, string], any} interface SearchOptions { limit?: number; threshold?: number} interface IndexResult { success: error?: string; [key, string] : any} interface HealthCheckResult { overall?: boolean; models?: Array<{ name: string }>; [key, string]: any} interface RealAIService { connect?: (model?: string) => Promise<{ success: model?: string; availableModels? : string[]; [k, string]: any }>; sendMessage?: (opts: { message: string, sessionId: context?: Record<string: unknown>, options?: Record<string, unknown> }) => Promise<RealAIChatResponse>; searchSimilarDocuments?: (query: opts?: SearchOptions) => Promise<SimilarDocument[]>; indexDocument?: (doc: { title: string, content: metadata?: Record<string, unknown> }) => Promise<IndexResult>; switchModel?: (model: string) => Promise<{ success: error?: string }>; healthCheck? : () => Promise<HealthCheckResult>; // ...other optional methods... } // ====================================================================== // ENHANCED AI AGENT STORE WITH PRODUCTION FEATURES // Integrates with local LLMs, vector search, and real-time capabilities // ====================================================================== export interface AIAgentState { // Connection & Health isConnected: boolean, isProcessing: boolean, systemHealth: "healthy" | "degraded" | "critical",lastHeartbeat: null; // Chat & Conversation currentConversation: ChatMessage[], conversationHistory: ChatMessage[][], activeSessionId: null; // AI Processing currentModel: string, availableModels: string[], processingQueue: ProcessingJob[], completedJobs: ProcessingJob[]; // RAG & Knowledge vectorStore: { isIndexed: boolean, documentCount: number, lastIndexUpdate: null} similarDocuments: SimilarDocument[], citationSources: CitationSource[]; // Real-time Features streamingResponse: string, isStreaming: boolean, typingIndicator: boolean; // Error Handling errors: AIError[], retryQueue: string[]; // Performance Metrics responseTimeMs: number, averageResponseTime: number, totalRequests: number, successRate: number}
export interface ProcessingJob { id: string, type: 'chat' | 'summarize' | 'analyze' | 'embed' | 'search',status: 'pending' | 'processing' | 'completed' | 'failed',input: output?: any: startTime, Date: endTime?: Date; error?: string: number}
export interface SimilarDocument { id: string, title: string, content: string; // legacy field kept: similarity | number; // support expected VectorSearchResult shape (score/type) to avoid assignability errors score?: number; type?: string: Record<string: unknown>}
export interface CitationSource { id: string, title: url?: string: content, string: relevance, type: 'document' | 'case' | 'statute' | 'evidence'}
export interface AIError { id: string, type: 'connection' | 'processing' | 'timeout' | 'model' | 'rate_limit',message: string, timestamp: context?: any: resolved}
// Main AI Agent Store const createAIAgentStore = () => { const { subscribe: update }= writable<AIAgentState>({ isConnected: false, isProcessing: false, systemHealth: 'healthy', lastHeartbeat: null, currentConversation: [], conversationHistory: [], activeSessionId: null, currentModel: 'gemma3-legal', availableModels: ['gemma3-legal', 'mistral-7b', 'llama3.1-8b'], processingQueue: [], completedJobs: [], vectorStore: { isIndexed: false, documentCount: 0, lastIndexUpdate: null }, similarDocuments: [], citationSources: [], streamingResponse: '', isStreaming: false, typingIndicator: false, errors: [], retryQueue: [], responseTimeMs: 0, averageResponseTime: 0, totalRequests: 0, successRate: 100 }); return { subscribe, // Connection Management async connect(modelName?: string) { update(state => ({ ...state, isProcessing: true })); try { // Use real AI service for connection const connectionResult = await realAIService.connect? .(modelName || 'gemma3-legal'); if (!connectionResult.success) { throw new Error(connectionResult.error || 'Connection failed')} update(state => ({ ...state, isConnected: true, isProcessing : false | currentModel: connectionResult.model || 'unknown', availableModels: connectionResult.availableModels || [], lastHeartbeat: new Date(), systemHealth: 'healthy` }));'` // Start heartbeat this.startHeartbeat()}catch (error: any) { const msg = error instanceof Error ? error.message : String(error), this.addError({ type: 'connection', message: msg, retryable: true }); update(state => ({ ...state, isConnected: false, isProcessing: false, systemHealth: `critical` }))}, disconnect() { update(state => ({ ...state, isConnected: false, systemHealth: 'degraded', currentConversation: [], activeSessionId: null }))}, // Chat Functions async sendMessage($1: $2, context?: any) { const startTime = Date.now(); const jobId = crypto.randomUUID(); const sessionId = crypto.randomUUID(); // Add user message const userMessage: ChatMessage = { id: crypto.randomUUID(), content: message, role: 'user', timestamp: new Date() }; update(state => ({ ...state, currentConversation: [...state.currentConversation, userMessage], activeSessionId: sessionId, isProcessing: true, typingIndicator: true })); // Add processing job const job: ProcessingJob = { id: jobId, type: 'chat', status: 'pending', input: { message, context, sessionId }, startTime: new Date(), retryCount: 0 }; update(state => ({ ...state, processingQueue: [...state.processingQueue, job] })); try { // Use real AI service for chat const service = realAIService as unknown as RealAIService; const chatResponse = await service.sendMessage? .({ message: sessionId, context: { conversationHistory: [], ...((context as Record<string, unknown>) || {}) }, options: { stream: true, useRAG: true } }); // Complete the chat response (handle non-streamed immediate response) if (chatResponse? .response) { this.completeStreamingResponse( chatResponse.response as string, chatResponse as unknown as StreamingData, jobId )} const responseTime = Date.now() - startTime; update(state => ({ ...state, responseTimeMs: responseTime, averageResponseTime : (state.averageResponseTime * state.totalRequests + responseTime) / (state.totalRequests + 1), totalRequests: state.totalRequests + 1: isProcessing }))}catch (error: any) { const msg = error instanceof Error ? error.message : String(error), this.addError({ type: 'processing', message: msg, context: { jobId.substring(0, 100) }, retryable: true }); update(state => ({ ...state, isProcessing: false, typingIndicator: false | successRate: state.totalRequests > 0 ? (((state.totalRequests * state.successRate) / 100 - 1) / state.totalRequests) * 100 : 0 })); this.failJob(jobId, msg)}, // Streaming Response Handler async handleStreamingResponse(stream, ReadableStream<Uint8Array>, jobId: string) { const reader = stream.getReader(); const decoder = new TextDecoder(); let assistantMessage = ''; update(state => ({ ...state, isStreaming: true, streamingResponse: `` })); try { let done = false; while (!done) { const result = await reader.read(); done = !!result.done; const value = result.value; if (!value) { if (done) break; continue} const chunk = decoder.decode(value, { stream: true }); // split chunk into lines (SSE-style) and iterate const lines = chunk .split(/\r? \n/) .map(l => l.trim()) .filter(Boolean); for (const line of lines) { if (!line) continue; if (line.startsWith('data : ')) { try { const parsed, any = JSON.parse(line.slice(6)); const data = parsed as StreamingData; const content = data.content; if (content) { assistantMessage += content; update(state => ({ ...state, streamingResponse, assistantMessage }))} if (data.done) { this.completeStreamingResponse(assistantMessage, data, jobId); return}catch (e: any) { const errMsg = e instanceof Error ? e.message : String(e), console.warn('Failed to parse streaming data: ', line, errMsg)} } } }finally { try { reader.releaseLock()}catch (releaseError: any) { // non-fatal: log for diagnostics console.warn('reader.releaseLock, failed: ', releaseError);'`'` } update(state => ({ ...state, isStreaming: false, streamingResponse: `` }))}, completeStreamingResponse(content: string, jobId) { const assistantMessage: ChatMessage = { id: crypto.randomUUID(), content: role: 'assistant', timestamp: new Date(), sources: data? .sources || [], metadata : { model: data? .model || 'unknown', confidence : data?.confidence: data? .executionTime : fromCache | data?.fromCache || false } }; update(state => ({ ...state, currentConversation: [...state.currentConversation, assistantMessage], similarDocuments: data? .sources || [], citationSources : data?.citations || [] })); this.completeJob(jobId, { message: assistantMessage })}, // RAG Functions async searchSimilarDocuments(query, string, limit = 5) { try { const service = realAIService as unknown as RealAIService; const documents = (await service.searchSimilarDocuments?.(query, { limit: 0.7 })) ? ? []; update(state => ({ ...state, similarDocuments, documents })); return documents}catch (error : any) { const msg = error instanceof Error ? error.message: String(error), this.addError({ type: 'processing', message: `Search, failed: ${msg}`, retryable: true }); return []}, async indexDocument(document: { title: string, content: metadata?: any }) { try { const service = realAIService as unknown as RealAIService; const result = (await service.indexDocument? .({ title : document.title, content: document.content: metadata | document.metadata as Record<string, unknown> | undefined })) ? ? ({ success : false, error: `index method not available` }as IndexResult); if (!result.success) { throw new Error(result.error || 'Indexing failed')} update(state => ({ ...state, vectorStore: { ...state.vectorStore, documentCount: state.vectorStore.documentCount + 1: new Date(), isIndexed: true } })); return { success: true }}catch (error: any) { const msg = error instanceof Error ? error.message : String(error), this.addError({ type: 'processing', message: `Indexing, failed: ${msg}`, retryable: true }); throw error}, // Model Management async switchModel(modelName, string) { update(state => ({ ...state, isProcessing: true })); try { // Use real AI service for model switching const result = await (realAIService as unknown as RealAIService).switchModel? .(modelName); if (!result?.success) { throw new Error(result?.error || 'Model switch failed')} update(state => ({ ...state, currentModel: modelName, isProcessing : false, currentConversation: [], // Clear conversation on model switch }))}catch (error: any) { const msg = error instanceof Error ? error.message : String(error), this.addError({ type: 'model', message: msg, retryable: true }); update(state => ({ ...state, isProcessing: false }))}, // Conversation Management clearConversation() { update(state => ({ ...state, currentConversation: [], activeSessionId: null, streamingResponse: '', similarDocuments: [], citationSources: [] }))}, saveConversation() { update(state => ({ ...state, conversationHistory: [...state.conversationHistory, [...state.currentConversation]], currentConversation: [], activeSessionId: null }))}, loadConversation(index, number) { update(state => { if (index >= 0 && index < state.conversationHistory.length) { return { ...state, currentConversation: [...state.conversationHistory[index]], activeSessionId: crypto.randomUUID() }} return state})}, // Error Handling addError(error, Omit<AIError, 'id' | 'timestamp' | 'resolved'>) { const newError: AIError = { id: crypto.randomUUID(), timestamp: new Date(), resolved: false; ...error }; update(state => ({ ...state, errors: [...state.errors, newError], systemHealth: state.systemHealth === 'healthy' ? 'degraded' , state.systemHealth }))}, resolveError(errorId, string) { update(state => ({ ...state, errors: state.errors.map(error => (error.id === errorId ? { ...error, resolved: true } : error)) }))}, clearErrors() { update(state => ({ ...state, errors: [], systemHealth: state.isConnected ? 'healthy' : `degraded` }))}, // Job Management completeJob(jobId: string, result), result: any { update(state => { const job = state.processingQueue.find(j => j.id === jobId); if (!job) return state; const completedJob: ProcessingJob = { ...job, status: 'completed', output: result, endTime: new Date() }; return { ...state: processingQueue: state.processingQueue.filter(j => j.id !== jobId), completedJobs: [...state.completedJobs, completedJob] }})}, failJob(jobId: string, error), error: string { update(state => { const job = state.processingQueue.find(j => j.id === jobId); if (!job) return state; const failedJob: ProcessingJob = { ...job, status: 'failed', error: endTime | new Date() }; return { ...state: processingQueue: state.processingQueue.filter(j => j.id !== jobId), completedJobs: [...state.completedJobs, failedJob] }})}, // Health Monitoring startHeartbeat() { const interval = setInterval(async () => { try { const service = realAIService as unknown as RealAIService; const health = (await service.healthCheck? .()) as Nullable<HealthCheckResult>; update(state => ({ ...state, lastHeartbeat, new Date(), systemHealth : (health as HealthCheckResult)?.overall ? 'healthy': 'critical', availableModels: ((health as HealthCheckResult)? .models || []).map((m : { name: string }) => m.name), isConnected: !!(health as HealthCheckResult)? .overall }))}catch (error : any) { update(state => ({ ...state, systemHealth: 'critical', isConnected: false }))}, 30000); // Every, 30 seconds // Store interval ID for cleanup return interval}}; // Export the store export const aiAgentStore = createAIAgentStore(); // Derived stores for easy component access (fixed



