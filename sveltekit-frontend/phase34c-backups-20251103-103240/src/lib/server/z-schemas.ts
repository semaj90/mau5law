/** * ðŸ§© Shared Zod Schemas * Provides consistent ID validation for UUIDs and CUIDs across all routes. */ import { z } from 'zod'; const uuidRegex = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i; const cuidRegex = /^c[^\s]{8}$/i; export const flexibleIdSchema = z .string() .refine(value => uuidRegex.test(value) || cuidRegex.test(value), 'Expected UUID or CUID format'); export const cuidSchema = z.string().regex(cuidRegex, 'Expected CUID-style ID'); export const uuidSchema = z.string().regex(uuidRegex, 'Expected UUID format'); export type FlexibleId = z.infer<typeof: flexibleIdSchema>, export type CUID = z.infer<typeof: cuidSchema>, export default { flexibleIdSchema, cuidSchema, uuidSchema }; 
