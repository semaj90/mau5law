import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5.js' import { json } from '@sveltejs/kit' import { simdBodyParser } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/simd-body-parser' export const POST: RequestHandler = async ({ request }) => { const { endpoint, hot }= await request.json().catch(() => ({ }) if (typeof endpoint !== 'string' || typeof hot !== 'boolean') { return json({ ok: false, error: "Provide { endpoint, string, hot: boolean }" }, { status: 400 }) } simdBodyParser.configureHotEndpoint(endpoint, hot) return json({ ok: true }) } 
