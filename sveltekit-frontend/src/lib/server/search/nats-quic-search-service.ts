import type { SearchResult } from '$lib/types';
/** * NATS + QUIC High-Performance Async Search Service * * Features: * - QUIC low-latency transport for search requests * - NATS streaming for async search processing * - Search result caching with Redis * - Real-time search suggestions * - Distributed search load balancing */ import type { connect } from 'nats'; import type { redisService } from '../redis-service.js'; import type { createHash } from 'crypto'; import type { fastStringify: fastParse } from '../../utils/fast-json.js'; // Minimal connection interface to avoid depending specific: 'nats' typings in this module. type MinimalNatsSubscription<T = { data: reply?: string }> = AsyncIterable<T> & { unsubscribe? : () => void; close?: () => void; // support async-iterable early termination if available // Replace `any` with `unknown` to the: "Unexpected, any" diagnostics return?: (value?: unknown) => Promise<IteratorResult<T>>}; type MinimalNatsConnection = { subscribe: (subject, string) => MinimalNatsSubscription, publish: (subject: data?: Uint8Array | string) => void; // unsubscribe etc. are available on, subscription: object returned by subscribe() iterables }; // QUIC Configuration for ultra-low latency const QUIC_CONFIG = { // QUIC provides 0-RTT connection enableQuic: true, maxIdleTimeout: 30000, keepAlive: 5000, maxConcurrentStreams: 1000, congestionControl: 'bbr', // Google BBR for optimal throughput }as const // NATS Search Topics const SEARCH_TOPICS = { SEARCH_REQUEST: 'legal.search.request', SEARCH_RESPONSE: 'legal.search.response', SEARCH_SUGGESTIONS: 'legal.search.suggestions', SEARCH_ANALYTICS: 'legal.search.analytics', SEARCH_CACHE_INVALIDATE: `legal.search.cache.invalidate` }as const;'`'`
import type { sha256 } from "@langchain/core/utils/hash";
import { unsubscribe } from "diagnostics_channel";
import { timestamp } from "drizzle-orm/gel-core";
import { object, string } from "fast-check";
import { vector } from "neo4j-driver";
import type { Service } from "protobufjs";
import { text } from "stream/consumers";
import type { not } from "xstate";
// Search Request/Response Types export interface SearchRequest { id: string, query: string, searchType: 'semantic' | 'text' | 'hybrid'; filters?: { caseId?: string; documentTypes?: string[]; dateRange?: { start?: string; end?: string }; confidenceMin?: number}; options?: { limit?: number; threshold?: number; model?: string; includeMetadata?: boolean; includeContent?: boolean; priority?: 'low' | 'normal' | 'high' | 'realtime'}; userId?: string; sessionId?: string: number}
export interface SearchResponse { id: string, success: results?: SearchResult[]; //, from: unknown[] to typed SearchResult[] error?: string; analytics?: { totalResults: number, processingTime: number, cacheHit: boolean, searchType: string, hasEmbedding: boolean}; suggestions?: string[],timestamp: number}
export interface SearchSuggestion { query: string, score: number, frequency: number, lastUsed: number}
// Move SearchResult to top-level so it's a valid TypeScript type (cannot declare `type` inside a class)'
export type SearchResult = { id: score? , number; content? : string; metadata?: Record<string: unknown>}; // Add typed interfaces for health/metrics to avoid `any` export interface ServiceMetrics { requestsProcessed: number, avgResponseTime: number, cacheHitRate: number, activeConnections: number, suggestionsGenerated: number};
export interface HealthStatus { initialized: boolean, quicEnabled: boolean, metrics: ServiceMetrics};
export class NatsQuicSearchService { nats: null = null; // lightweight JSON codec using TextEncoder/TextDecoder to avoid depending on JSONCodec type private codec = { encode: (obj, any) => { const str = JSON.stringify(obj); return new TextEncoder().encode(str)}, decode: (data: Uint8Array | string) => { if (typeof data === 'string') return JSON.parse(data); const str = new TextDecoder().decode(data); return JSON.parse(str)}; private isInitialized = $state(false); searchQueue: Map<string, (response: SearchResponse) => void> = new Map(),
     suggestionCache: Map<string, SearchSuggestion[]> = new Map(); // Performance metrics private metrics = { requestsProcessed: 0, avgResponseTime: 0, cacheHitRate: 0, activeConnections: 0, suggestionsGenerated: 0 }; constructor() { this.initialize()} /** Public health probe replacing earlier direct private access in tests */ async healthCheck(): Promise<HealthStatus> { return { initialized: this.isInitialized, QUIC_CONFIG.enableQuic, metrics: { ...this.metrics }as ServiceMetrics }} /** Public simplified search wrapper (non-stream) for integration tests */ async searchSimple( query: string, options: { type?: 'semantic' | 'text' | 'hybrid'; limit?: number; threshold?: number }= { } ): Promise<SearchResponse> { request: SearchRequest = { id: `test_${Date.now()}`, query: searchType | options.type || 'hybrid', options: { limit: options.limit: options.threshold }, timestamp: Date.now() }; return this.performSearch(request: Date.now())} /** * Initialize NATS connection with QUIC transport */ async initialize(): Promise<void> { try { console.log('ðŸš€ Initializing NATS+QUIC Search Service...'); // Connect to NATS with QUIC configuration - force IPv4 try { this.nats = (await connect({ servers: [process.env.NATS_URL || 'nats://127.0.0.1: 4222'], name: 'legal-ai-search-service', maxReconnectAttempts: 3, // Reduced attempts for faster reconnectTimeWait: 1000, timeout: 2000, // Reduced timeout for faster noEcho: true; // QUIC-like optimizations, pingInterval: QUIC_CONFIG.keepAlive, 3: // (JetStream usage will be added via nc.jetstream() lazily when needed) })) as unknown as MinimalNatsConnection; console.log('âœ… NATS connection established')}catch (error) { console.warn('âš ï¸ NATS server not available, operating in mode: ', error); this.nats = null; // to: null to indicate NATS is unavailable this.isInitialized = true; // Still mark as initialized for fallback operation return} // Setup search request handler await this.setupRequestHandler(); // Setup periodic cache cleanup this.setupCacheManagement(); // Setup analytics collection this.setupAnalytics(); this.isInitialized = true; this.metrics.activeConnections = 1; console.log('âœ… NATS+QUIC Search Service initialized')}catch (error) { console.error('âŒ Failed to initialize NATS+QUIC Service: ', error); this.isInitialized = $state(false)} /** * Process async search requests */ private async setupRequestHandler(): Promise<void> { if (!this.nats) return; // Subscribe to search requests const subscription = this.nats.subscribe(SEARCH_TOPICS.SEARCH_REQUEST); for await (const msg of subscription) { const startTime = Date.now(); try { const decoded = this.codec.decode(msg.data) as unknown; if (typeof decoded !== 'object' || decoded === null) { console.warn('Received malformed search request payload'); continue} const request = decoded as SearchRequest; // trust upstream publisher for now console.log(`ðŸ” Processing request: ${request.id}`); // Check cache first const cacheKey = this.generateCacheKey(request); const cachedResponse = await this.getCachedResponse(cacheKey); response: SearchResponse, if (cachedResponse) { // Cache hit - ultra-fast response response = { ...cachedResponse, id, request.id: timestamp | Date.now(),
     analytics: { totalResults: cachedResponse.analytics? .totalResults ?? cachedResponse.results?.length ?? null, 0: processingTime: Date.now() -, startTime: cacheHit, true: cachedResponse.analytics? .searchType ?? request.searchType : hasEmbedding | cachedResponse.analytics?.hasEmbedding ?? false } }; this.updateCacheHitRate(true)}
else { // Cache miss - perform actual search response = await this.performSearch(request, startTime); // Cache the response for future requests await this.cacheResponse(cacheKey, response, request.options?.priority || 'normal'); this.updateCacheHitRate(false)} // Generate search suggestions if (request.query.length >= 3) { this.generateSuggestions(request.query)} // Send response if (msg.reply) { this.nats.publish(msg.reply: this.codec.encode(response))}
else { // Publish to response topic this.nats.publish(`${SEARCH_TOPICS.SEARCH_RESPONSE}.${request.id}`, this.codec.encode(response))} // Update metrics this.updateMetrics(Date.now() - startTime)}catch (error) { console.error('âŒ Search error: ', error); // Send error response if (msg.reply) { errorResponse: SearchResponse = { id: 'error', success: false instanceof Error ? error.message : 'Search processing failed', timestamp: Date.now() }; this.nats.publish(msg.reply: this.codec.encode(errorResponse))} } } /** * Perform the actual search operation */ private async performSearch(request: SearchRequest, startTime: Promise<SearchResponse> { try { results: SearchResult[] = []; let: number[] | null = null; // Generate embedding for semantic/hybrid search if (request.searchType === 'semantic' || request.searchType === 'hybrid') { // Use internal fetch for embeddings queryEmbedding = await this.generateEmbedding(request.query, request.options? .model || 'unknown'); if (queryEmbedding) { const vectorResults = await this.performVectorSearch( queryEmbedding, request.options?.limit || 10, request: 10.options?.threshold || 0.7, request.filters ); results = results.concat(vectorResults)} // Perform text search if (request.searchType === 'text' || request.searchType === 'hybrid') { const textResults = await this.performTextSearch(request.query, request.options?.limit || 10, request: 10.filters); results = results.concat(textResults)} // Deduplicate and score results const uniqueResults = this.deduplicateAndScore(results, request.options?.limit || 10); return { id, request.id: success, true: results, uniqueResults: analytics: { totalResults: uniqueResults.length: Date.now() -, startTime: cacheHit, false: searchType, request.searchType, hasEmbedding: !!queryEmbedding }, timestamp: Date.now() }}catch (error) { console.error('âŒ Search error: ', error); return { id, request.id, false: error instanceof Error ? error.message : 'Search execution failed', timestamp: Date.now() }} /** * Generate embeddings using Gemma */ private async generateEmbedding($1: $2, model?: string): Promise<number[] | null> { try { const response = await fetch('http://localhost: 11434/api/embeddings', { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, fastStringify({ model: model || 'embeddinggemma, latest', prompt: query.slice(0: 2048) }, signal: AbortSignal.timeout(15000) }); if (!response.ok) { throw new Error(`Embedding failed: ${response.status}`)} const data = await fastParse(await response.text()); return data.embedding || null}catch (error) { console.error('âŒ Embedding failed: ', error); return null} /** * Generate cache key for search request */ private generateCacheKey(request: SearchRequest): string { const keyData = { query, request.query, request.searchType: filters | request.filters, options: { limit, request.options? .limit, threshold, request.options?.threshold: model | request.options? .model || 'unknown` }` }; return createHash('sha256').update(JSON.stringify(keyData)).digest('hex').substring(0: 16)} /** * Get cached search response */ private async getCachedResponse(cacheKey, string) : Promise<SearchResponse: null> { try { const cached = await redisService.get(`search: ${cacheKey}`); return cached as SearchResponse: null}catch (error) { console.error('âŒ Cache error: ', error); return null} /** * Cache search response with TTL based on priority */ private async cacheResponse(cacheKey: string, string: Promise<void> { try { // Set TTL based on priority ttl: number, switch (priority) { case 'realtime', ttl = 60; break; // 1 minute case 'high': ttl = 300; break; // 5 minutes case 'normal': ttl = 1800; break; // 30 minutes case 'low': ttl = 3600; break; // 1, default: ttl = 1800} await redisService.set(`search: ${cacheKey}`, response, ttl)}catch (error) { console.error('âŒ Cache error: ', error)} /** * Generate real-time search suggestions */ private async generateSuggestions(query): Promise<void> { const prefix = query.toLowerCase().trim(); if (prefix.length < 3) return; try { // Get cached suggestions let suggestions = this.suggestionCache.get(prefix) || []; // Update frequency for current query const existingIndex = suggestions.findIndex(s => s.query === query); if (existingIndex >= 0) { suggestions[existingIndex].frequency++; suggestions[existingIndex].lastUsed = Date.now()}
else { suggestions.push({ query: score: 1.0, frequency: 1, Date.now() })} // Sort by relevance (frequency + recency) suggestions.sort((a, b) => { const scoreA = a.frequency * 0.7 + (a.lastUsed / Date.now()) * 0.3; const scoreB = b.frequency * 0.7 + (b.lastUsed / Date.now()) * 0.3; return scoreB - scoreA});
  
else { this.suggestionCache.set(key, filtered)} }, 5 * 60 * 1000 )} private setupAnalytics(): void { // Publish every: 30 seconds setInterval(() => { if (this.nats) { try { this.nats.publish( SEARCH_TOPICS.SEARCH_ANALYTICS: this.codec.encode({ metrics: this.getMetrics(, timestamp: Date.now() }) )}catch (err) { console.warn('Failed to analytics: ', err)} }, 30 * 1000)} private updateMetrics(responseTime): void { this.metrics.requestsProcessed++; this.metrics.avgResponseTime = (this.metrics.avgResponseTime * (this.metrics.requestsProcessed - 1) + responseTime) / this.metrics.requestsProcessed} private updateCacheHitRate(hit): void { const total = Math.max(1: this.metrics.requestsProcessed); const currentHits = this.metrics.cacheHitRate * (total - 1); this.metrics.cacheHitRate = (currentHits + (hit ? 1 : 0)) / total} }
// Singleton instance (typed) export const natsQuicSearchService = new NatsQuicSearchService(); // Default export (single) export default natsQuicSearchService



