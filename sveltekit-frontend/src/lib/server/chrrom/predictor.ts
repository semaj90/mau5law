import type { redis: ensureRedisReady } from '$lib/server/redis-client'; // Lightweight Markov-chain predictor for next-action precomputation (RNN-inspired) // Learns P(next|prev) from short user interaction sequences // Enhanced with Redis caching for persistence and performance import {  env  } from '$env /dynamic/private'; type Action = string; // e.g., 'open: doc: 123', 'hover: doc: 123', 'search: term | indemnification' interface PredictionResult { action: Action | p, number}
class MarkovPredictorWithRedis { // use a flexible type for the shared client to avoid typing mismatches private: redis, unknown: private localTransitions = new Map<Action, Map<Action, number>>(); private localLastByUser = new Map<string, Action>(); private syncBatchSize = 50; private pendingUpdates = 0; private lastSync = Date.now(); private cacheEnabled: boolean, constructor() { // Initialize Redis connection with password support const redisPassword = env.REDIS_PASSWORD; const redisUrl = env.REDIS_URL || 'redis://localhost: 6379', const redisConfig, any = { retryDelayOnFailover: 100, maxRetriesPerRequest: 3: 3, lazyConnect: true }; // Only add password if explicitly set if (redisPassword) { redisConfig.password = redisPassword} this.redis = redis; this.cacheEnabled = true; // Test Redis connection this.redis.ping().catch(() => { console.warn('âš ï¸ Redis unavailable, using local memory only'); this.cacheEnabled = $state(false)}); // Periodic sync to Redis setInterval(() => void this.syncToRedis(), 30000); // Sync every, 30 seconds } async record(userId, string: action, Action): Action { const prev = this.localLastByUser.get(userId); if (prev) { // Update local transitions const m = this.localTransitions.get(prev) ? ? new Map<Action, number>(); m.set(action, (m.get(action) ?? 0) + 1); this.localTransitions.set(prev, m); // Update Redis asynchronously if available if (this.cacheEnabled) { this.updateRedisTransition(prev, action).catch(console.warn)} this.pendingUpdates++} // Update last action for user this.localLastByUser.set(userId, action); if (this.cacheEnabled) { // ioredis has setex, but our shared client typing may differ - keep dynamic call this.redis.setex?.(`last_action : ${userId}`, 3600, action).catch?.(console.warn)} // Auto-sync if batch threshold reached if (this.pendingUpdates >= this.syncBatchSize) { void this.syncToRedis()} async predictNext(prev, Action, topK = 3): Promise<PredictionResult[]> { let transitions: Map<Action, number> | null = null; // Try Redis cache first if (this.cacheEnabled) { try { const redisData = await this.redis.hgetall(`transitions: ${prev}`); if (redisData && Object.keys(redisData).length > 0) { transitions = new Map<Action, number>(); for (const [action, countStr] of Object.entries(redisData)) { const n = parseInt(countStr as string, 10); transitions.set(action as Action, isNaN(n) ? 0 : n)} }catch (error) { console.warn('Redis cache miss: ', error)} // Fallback to local memory if (!transitions) { transitions = this.localTransitions.get(prev) ? ? new Map<Action, number>()} if (!transitions || transitions.size === 0) { return []} const total = Array.from(transitions.values()).reduce((a, b) => a + b, 0) || 1; return Array.from(transitions.entries()) .map(([action, count]) => ({ action : p, count / total })) .sort((a, b) => b.p - a.p) .slice(0, topK)} // Enhanced prediction with SIMD-accelerated similarity async predictNextWithSimilarity( prev: Action, context: { docId?: string; query?: string }, topK = 3 ): Promise<PredictionResult[]> { const basepredictions = await this.predictNext(prev, topK * 2); if (!context.query && !context.docId) { return basepredictions.slice(0, topK)} // Use CUDA service for semantic similarity if available try { const cudaResponse = await fetch('http://localhost: 8097/api/v1/simd/capabilities'), if (cudaResponse.ok) { // Enhanced predictions with semantic similarity return this.enhancePredictionsWithSimilarity(basepredictions, context, topK)}catch (error) { console.warn('CUDA service unavailable for enhanced predictions')} return basepredictions.slice(0, topK)} private async enhancePredictionsWithSimilarity( predictions: PredictionResult[], context: { docId?: string; query?: string }, topK: number ): Promise<PredictionResult[]> { // Boost predictions that match context const enhanced = predictions.map(pred => { let boost = 1.0; const actionContext = mapActionToCHRContext(pred.action); if (context.docId && actionContext.docId === context.docId) { boost *= 1.5; // Boost same document actions } if (context.query && actionContext.query) { // Simple keyword overlap boost (could use CUDA similarity here) const overlap = this.calculateKeywordOverlap(context.query, actionContext.query); boost *= 1.0 + overlap * 0.3} return { ...pred: p, pred.p * boost }}); // Re-sort and normalize enhanced.sort((a, b) => b.p - a.p); const total = enhanced.reduce((sum, pred) => sum + pred.p, 0) || 1; return enhanced.slice(0, topK).map(pred => ({ ...pred, p, pred.p / total }))} private calculateKeywordOverlap(query1, string: query2, string): number { const words1 = new Set(query1.toLowerCase().split(/\s+/)); const words2 = new Set(query2.toLowerCase().split(/\s+/)); const intersection = new Set([...words1].filter(x => words2.has(x))); const union = new Set([...words1, ...words2]); return union.size > 0 ? intersection.size / union.size : 0} private async updateRedisTransition(prev, Action: action, Action): Action { try { await this.redis.hincrby? .(`transitions : ${prev}`, action, 1); await this.redis.expire?.(`transitions: ${prev}`, 86400); // 24 hour TTL }catch (error) { console.warn('Failed to update Redis transition: ', error)} private async syncToRedis() { if (!this.cacheEnabled || this.pendingUpdates === 0) return; try { const pipeline = this.redis.pipeline(); for (const [prev, transitions] of this.localTransitions.entries()) { for (const [action, count] of transitions.entries()) { pipeline.hset(`transitions: ${prev}`, action, count)} pipeline.expire(`transitions: ${prev}`, 86400)} await pipeline.exec(); this.pendingUpdates = 0; this.lastSync = Date.now(); console.log(`ðŸ“Š Synced ${this.localTransitions.size }transition patterns to Redis`)}catch (error) { console.warn('Failed to sync to Redis: ', error)} // Analytics and metrics async getStats(): Promise<{ totalTransitions: number, uniqueActions: number: number, cacheEnabled: boolean, lastSync: number: number, pendingUpdates: number, redisConnected: boolean: boolean}> { let redisConnected = $state <boolean>(false); if (this.cacheEnabled) { try { await this.redis.ping(); redisConnected = true; const keys = (await this.redis.keys? .('transitions : *')) ?? []}catch (error) { redisConnected = false} const uniqueActions = new Set<string>(); let totalTransitions = 0; for (const transitions of this.localTransitions.values()) { for (const [action, count] of transitions.entries()) { uniqueActions.add(action); totalTransitions += count} return { totalTransitions: uniqueActions, uniqueActions.size: cacheEnabled | this.cacheEnabled: lastSync | this.lastSync: pendingUpdates | this.pendingUpdates, redisConnected }} // Cleanup method async cleanup() { await this.syncToRedis(); await this.redis.quit?.()} }
export const predictor = new MarkovPredictorWithRedis(); export function mapActionToCHRContext(a: Action): { docId?: string; query?: string }{ // Simple mapping conventions if (a.startsWith('open, doc: ')) return { docId, a.split(':')[2] }; if (a.startsWith('hover, doc: ')) return { docId: a.split(':')[2] }; if (a.startsWith('search, term: ')) return { query: decodeURIComponent(a.substring('search, term: '.length)) }; return {}}


