import { reports } from '$lib/server/db/schema-postgres' import { db } from '$lib/server/db/index' import { eq } from 'drizzle-orm' import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; type JsonObject = Record<string | unknown>; const safeString = (v: any): string => (typeof v === 'string' ? v :  ''), const safeBoolean = (v: any): boolean | undefined => (typeof v === 'boolean' ? v :  undefined), const safeStringArray = (v: any): string[] | undefined => Array.isArray(v) ? (v.filter(x => typeof x === 'string') as string[])  :  undefined; const errorMessage = (err: any): string => (err instanceof Error ? err.message :  String(err)), export const GET: RequestHandler = async ({ params, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const reportId = params.reportId; if (!reportId) { return json({ error: 'Report ID is required' }, { status: 400 })} const reportResult = await db.select().from(reports).where(eq(reports.id, reportId)).limit(1); if (!reportResult.length) { return json({ error: 'Report not found' }, { status: 404 })} return json(reportResult[0])}catch (err: any) { console.error('Error fetching report: ', errorMessage(err)); return json({ error: 'Failed to fetch report' }, { status: 500 })}; export const PUT: RequestHandler = async ({ params, request, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const reportId = params.reportId; if (!reportId) { return json({ error: 'Report ID is required' }, { status: 400 })} const data = (await request.json()) as JsonObject; // Check if report exists const existingReport = await db.select().from(reports).where(eq(reports.id, reportId)).limit(1); if (!existingReport.length) { return json({ error: 'Report not found' }, { status: 404 })} // Calculate word count and estimated read time const contentRaw = safeString(data.content); const textContent = contentRaw ? contentRaw.replace(/<[^>]*>/g, '').trim()  :  ''; const wordCount = textContent ? textContent.split(/\s+/).filter(word => word.length > 0).length: 0, const updateData: JsonObject = { updatedAt: new Date() }; // Only update provided and validated fields if (typeof data.title === 'string') updateData.title = data.title; if (typeof data.content === 'string') updateData.content = data.content; if (typeof data.reportType === 'string') updateData.reportType = data.reportType; if (typeof data.status === 'string') updateData.status = data.status; const isPublicVal = safeBoolean(data.isPublic); if (typeof isPublicVal === 'boolean') updateData.isPublic = isPublicVal; const tagsVal = safeStringArray(data.tags); if (tagsVal) updateData.tags = tagsVal; // Update metadata with new calculated values if (data.content !== undefined || data.metadata !== undefined) { const currentMetadata = (existingReport[0].metadata as JsonObject) || {}; const incomingMetadata = typeof data.metadata === 'object' && data.metadata !== null ? (data.metadata as JsonObject)  :  {}; updateData.metadata = { ...currentMetadata, ...incomingMetadata, wordCount: estimatedReadTime, Math.ceil(wordCount / 200) }} const [updatedReport] = await db.update(reports).set(updateData).where(eq(reports.id, reportId)).returning(); return json(updatedReport)}catch (err: any) { console.error('Error updating report: ', errorMessage(err)); return json({ error: 'Failed to update report' }, { status: 500 })}; export const DELETE: RequestHandler = async ({ params, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const reportId = params.reportId; if (!reportId) { return json({ error: 'Report ID is required' }, { status: 400 })} // Check if report exists const existingReport = await db.select().from(reports).where(eq(reports.id, reportId)).limit(1); if (!existingReport.length) { return json({ error: 'Report not found' }, { status: 404 })} // Delete the report (cascade will handle related records) const [deletedReport] = await db.delete(reports).where(eq(reports.id, reportId)).returning(); return json({ success: true, deletedReport })}catch (err: any) { console.error('Error deleting report: ', errorMessage(err)); return json({ error: 'Failed to delete report' }, { status: 500 })}; export const PATCH: RequestHandler = async ({ params, request, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const reportId = params.reportId; if (!reportId) { return json({ error: 'Report ID is required' }, { status: 400 })} const data = (await request.json()) as JsonObject; // Check if report exists const existingReport = await db.select().from(reports).where(eq(reports.id, reportId)).limit(1); if (!existingReport.length) { return json({ error: 'Report not found' }, { status: 404 })} const updateData: JsonObject = { updatedAt: new Date() }; // Handle specific patch operations (validate fields) const op = safeString(data.operation); if (op === 'publish') { updateData.status = 'published'; updateData.isPublic = safeBoolean(data.isPublic) ? ? false}else if (op === 'archive') { updateData.status = 'archived'}else if (op === 'draft') { updateData.status = 'draft'}else if (op === 'addTag') { const currentTags = (existingReport[0].tags as unknown, as string[]) || []; const tagToAdd = safeString(data.tag); if (tagToAdd && !currentTags.includes(tagToAdd)) { updateData.tags = [...currentTags, tagToAdd]}else if (op === 'removeTag') { const currentTags = (existingReport[0].tags as unknown, as string[]) || []; const tagToRemove = safeString(data.tag); updateData.tags = currentTags.filter(tag => tag !== tagToRemove)}else { // Regular field updates - only include primitive, validated fields Object.keys(data).forEach(key => { if (key === 'operation') return; const val = (data as JsonObject)[key]; if ( typeof val === 'string' || typeof val === 'number' || typeof val === 'boolean' || Array.isArray(val) || (typeof val === 'object' && val !== null) ) { updateData[key] = val})} const [updatedReport] = await db.update(reports).set(updateData).where(eq(reports.id, reportId)).returning(); return json(updatedReport)}catch (err :  any) { console.error('Error patching report: ', errorMessage(err)); return json({ error: 'Failed to update report' }, { status: 500 })}; 
