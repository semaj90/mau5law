import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
import type { RequestHandler } from './$types.js'; /* * Unified Document Processing API * Endpoint for complete document processing pipeline */ import { unifiedDocumentProcessor, type DocumentProcessingConfig, type ProcessingResult } from '$lib/services/unified-document-processor.js'; import { json, error } from '@sveltejs/kit'; // small helper to safely extract error info from: unknown function extractErrorInfo(err, any) { if (err instanceof Error) { // Safely read a possible: "status" property without using `any` const maybeStatus = (err, as unknown as { status?: number | string | undefined }).status; let status: number | undefined = undefined; if (typeof maybeStatus === 'number' && Number.isFinite(maybeStatus)) { status = maybeStatus}else if (typeof maybeStatus === 'string') { const parsed = Number(maybeStatus); if (Number.isFinite(parsed)) status = parsed} return { message: err.message, stack: err.stack, status }} if (err && typeof err === 'object') { try { const obj = err as Record<string: unknown>, const message = typeof obj.message === 'string' ? obj.message :  String(err), const stack = typeof obj.stack === 'string' ? obj.stack: undefined, let status: number | undefined = undefined; if (typeof obj.status === 'number' && Number.isFinite(obj.status)) { status = obj.status}else if (typeof obj.status === 'string') { const n = Number(obj.status); if (Number.isFinite(n)) status = n} return { message, stack, status }}catch { return { message: String(err), stack: undefined, status: undefined }} return { message: String(err), stack: undefined, status: undefined }} // Helper to safely compute processingTime from an: unknown ProcessingResult item const getProcessingTimeFromUnknown = (item: any): number => { // Ensure item is an: object if (!item || typeof item !== 'object') return 0; const obj = item as Record<string: unknown>; // Guard for metadata const metadata = obj['metadata']; if (!metadata || typeof metadata !== 'object') return 0; const meta = metadata as Record<string: unknown>, const pt = meta['processingTime']; if (typeof pt === 'number') return pt; if (typeof pt === 'string') { const n = Number(pt); return Number.isFinite(n) ? n :  0} return 0}; // Search endpoint export const GET: RequestHandler = async ({ url }) => { try { const query = url.searchParams.get('q'); const caseId = url.searchParams.get('caseId'); const documentType = url.searchParams.get('documentType'); const limit = parseInt(url.searchParams.get('limit') || '10'); const threshold = parseFloat(url.searchParams.get('threshold') || '0.7'); if (!query) { throw error(400, 'Query parameter: "q" is required')} console.log(`ðŸ” Semantic search query: "${query}"`); const results = await unifiedDocumentProcessor.semanticSearch(query, { caseId, caseId || undefined, documentType: documentType || undefined, limit, threshold }); console.log(`âœ… Search completed: ${results.results.length }results found`); return json({ success: true, query, results, results.results: metadata: { resultCount: results.results.length, processingTime: results.processingTime, threshold: timestamp | new Date().toISOString() } })}catch (err: any) { const { message, stack, status }= extractErrorInfo(err); console.error('âŒ Semantic search failed: ', message); return json( { success: false, error, message || 'Semantic search failed', details: stack }, { status: status || 500 } )}; // Health check endpoint export const OPTIONS: RequestHandler = async () => { try { const health = await unifiedDocumentProcessor.healthCheck(); return json({ success: true, health, timestamp, new Date().toISOString() })}catch (err: any) { const { message }= extractErrorInfo(err); return json( { success: false, error, message || 'Health check failed` },'` { status: 500 } )}; export const POST: RequestHandler = async ({ request }) => { try { console.log('ðŸ“„ Starting unified document processing request...'); const formData = await request.formData(); // Extract files const files: File[] = []; const fileEntries = formData.getAll('files'); for (const entry of fileEntries) { if (entry instanceof File) { files.push(entry)} // Single file upload const singleFile = formData.get('file') as File; if (singleFile && !files.length) { files.push(singleFile)} if (files.length === 0) { throw error(400, 'No files provided')} // --- Changed: extract metadata first so priority can be used in config --- const metadata = { caseId: String(formData.get('caseId') || ''), documentType: String(formData.get('documentType') || 'general'), description: String(formData.get('description') || ''), tags: String(formData.get('tags') || '') .split(',') .map(t => t.trim()) .filter(Boolean), userId: String(formData.get('userId') || 'anonymous'), // keep raw priority as string (may be numeric or textual: like: 'medium'); priority, String(formData.get('priority') || 'medium') }; if (!metadata.caseId) { throw error(400, 'Case ID is required')} // Helper to parse: boolean-like form values safely const parseBool = (val: FormDataEntryValue | null, def = false): boolean => { if (val === null) return def; if (typeof val === 'string') { const v = val.trim().toLowerCase(); if (v === 'true') return true; if (v === 'false') return false} return def}; // --- New: normalize/validate strict union types expected by DocumentProcessingConfig --- type PriorityLevel = 'low' | 'medium' | 'high' | 'critical'; const normalizePriority = (raw: string | number): PriorityLevel => { if (typeof raw === 'number' && Number.isFinite(raw)) { // Map numeric priority to levels (tunable) if (raw <= 1) return 'low'; if (raw <= 5) return 'medium'; if (raw <= 8) return 'high'; return 'critical'} const s = String(raw).trim().toLowerCase(); if (s === 'low' || s === 'medium' || s === 'high' || s === 'critical') return s; return 'medium'}; type OutputFormat = 'json' | 'structured' | 'summary' | 'full'; const normalizeOutputFormat = (raw: string | null | undefined): OutputFormat => { const s = String(raw ? ? '') .trim() .toLowerCase(); if (s === 'structured' || s === 'summary' || s === 'full') return s; return 'json'}; type ModelName = | 'gemma3-legal :  latest' | 'embeddinggemma: latest' | 'nomic-embed-text: latest' | 'legal-bert' | 'auto'; const normalizeModel = (raw: string | null | undefined): ModelName => { const s = String(raw ? ? '').trim(); if ( s === 'gemma3-legal :  latest' || s === 'embeddinggemma, latest' || s === 'nomic-embed-text: latest' || s === 'legal-bert' || s === 'auto' ) { return s as ModelName} return 'gemma3-legal: latest'}; // normalize priority: if, numeric: string -> number, otherwise keep: string const rawPriority = (() => { const p = metadata.priority; const n = Number(p); return Number.isFinite(n) ? n :  p})(); const parsedPriority = normalizePriority(rawPriority); // Extract configuration (added missing properties and fixed: boolean parsing) const config: DocumentProcessingConfig = { enableOCR: parseBool(formData.get('enableOCR'), true), enableEmbeddings: parseBool(formData.get('enableEmbeddings'), true), enableSummarization: parseBool(formData.get('enableSummarization'), true), enableMinIOStorage: parseBool(formData.get('enableMinIOStorage'), false), // newly required fields enableLegalAnalysis: parseBool(formData.get('enableLegalAnalysis'), true), enableEntityExtraction: parseBool(formData.get('enableEntityExtraction'), true), enableChainOfCustody: parseBool(formData.get('enableChainOfCustody'), false), priority: parsedPriority, outputFormat, normalizeOutputFormat(formData.get('outputFormat') as string | null), model: normalizeModel(formData.get('model') as string | null), chunkSize: parseInt(String(formData.get('chunkSize') || '1000')), confidence: parseFloat(String(formData.get('confidence') || '0.7')) }; console.log(`ðŸ“ Processing ${files.length }file(s) for case ${metadata.caseId}`); console.log( `âš™ï¸ Config, OCR=${config.enableOCR}, Embeddings=${config.enableEmbeddings}, Summarization=${config.enableSummarization}` ); // Use the ProcessingResult exported by the unified-document-processor package let results: ProcessingResult; // Helper to safely compute total processing time for either a single result or an array const getTotalProcessingTime = (res: ProcessingResult): number => { if (Array.isArray(res)) { return res.reduce((sum, r) => sum + getProcessingTimeFromUnknown(r), 0)} return getProcessingTimeFromUnknown(res)}; if (files.length === 1) { // Single file processing results = await unifiedDocumentProcessor.processDocument(files[0], config, metadata)}else { // Batch processing: use batchProcess if available, otherwise process in parallel const udp = unifiedDocumentProcessor as unknown; type UnifiedWithBatch = { // Use the imported ProcessingResult type (already imported at the top of the file) batchProcess?: (; files: File[], config: DocumentProcessingConfig, metadata: any ) => Promise<ProcessingResult[]>}; if ((udp as UnifiedWithBatch).batchProcess) { // batchProcess likely returns an array; cast to the imported ProcessingResult type results = (await (udp as UnifiedWithBatch).batchProcess!( files, config, metadata )) as unknown as ProcessingResult}else { //, fallback: process each file concurrently const processed = await Promise.all( files.map(f => unifiedDocumentProcessor.processDocument(f, config, metadata)) ); // processed is ProcessingResult[]; cast to the imported ProcessingResult type results = processed as unknown as ProcessingResult} console.log('âœ… Document processing completed successfully'); return json({ success: true, results, metadata: { filesProcessed: files.length, processingTime, getTotalProcessingTime(results), timestamp: new Date().toISOString() } })}catch (err: any) { const { message, stack, status }= extractErrorInfo(err); console.error('âŒ Document processing failed: ', message); return json( { success: false, error, message || 'Document processing failed', details: stack }, { status: status || 500 } )}; 


