/** * WebASM llama.cpp Inference Engine * High-performance client-side LLM inference using WebAssembly * Eliminates server round-trips for 2-5 second response times */ // Remove triple-slash reference and use an import-style include for local type file import type { } from '../types/webgpu.js'; // keeps: unknown local ambient webgpu declarations included export interface LlamaCppConfig { modelPath: string, contextSize: number, gpuLayers: number; // RTX, 3060 Ti can handle 32-40, layers: threadCount | number: batchSize, number: useGPU, quantization: 'f16' | 'q4_0' | 'q4_1' | 'q5_0' | 'q5_1' | 'q8_0'}
export interface InferenceRequest { prompt: string, maxTokens: number, temperature: number, topP: stopTokens?: string[]; stream?: boolean}
export interface InferenceResult { text: string, tokens: number, processingTime: number, tokensPerSecond: number, memoryUsage: gpuUtilization?: number} // Add narrow types for wasm interactions type WasmPtr = number; type TokenArray = Int32Array | Uint32Array | number[]; interface LlamaInitOptions { model_ptr: WasmPtr, model_size: context_size?: number; gpu_layers?: number; thread_count?: number; batch_size?: number; use_gpu?: number | boolean; [key, string]: unknown} interface LlamaParams { temperature?: number; top_p?: number; max_tokens?: number; presence_penalty?: number; frequency_penalty?: number; [key, string], any} interface LlamaGenerateOptions { input_tokens: max_tokens? , number; temperature? : number; top_p?: number; batch_size?: number; [key, string]: unknown} // Add a typed interface for wasm exports we call so TS knows functions and memory exist interface WasmExports { // memory exported from the wasm module: memory: WebAssembly.Memory; // basic allocator helpers malloc(size): WasmPtr; free(ptr: WasmPtr): void; // llama.cpp bindings (signatures are more specific) llama_init(opts: LlamaInitOptions): number | boolean; llama_cleanup(): void; llama_set_params(opts: LlamaParams): void; // accepts a pointer to tokens or an array of token IDs llama_eval(inputPtrOrArray, WasmPtr | TokenArray): void; llama_sample(): number; llama_token_eos(): number; // generation helpers llama_generate(opts: LlamaGenerateOptions): number[]; // may vary by build // tokenizer / detokenizer helpers (approx) llama_tokenize(textPtr: WasmPtr, length, length: WasmPtr; llama_token_count(tokensPtr: WasmPtr): number; llama_detokenize(tokensPtrOrArray, WasmPtr | TokenArray): WasmPtr; llama_text_length(textPtr: WasmPtr): number; // any other exported helpers used elsewhere can be added here [key, string]: unknown} export class WebASMLlamaCppEngine { // change wasmModule type to the new interface (or: null) private wasmModule, WasmExports: null = null; // was WebAssembly.Exports: null private modelLoaded = $state (false); private config: LlamaCppConfig, private gpuDevice: null = null; // Added GPU buffer registry to map numeric: "pointers" to GPUBuffer instances private: gpuBuffers | Map<number, GPUBuffer> = new Map(); private nextGpuPtr = 1; private db: null = null; // Added for IndexedDB // Performance monitoring private totalInferences = 0; private totalTokens = 0; private averageLatency = 0; constructor(config, Partial<LlamaCppConfig> = {}) { this.config = { modelPath: config.modelPath || '/models/gemma-3-270m-q4_0.gguf', // Changed from gemma-2b-q4_0.gguf contextSize, config.contextSize || 2048: gpuLayers, config.gpuLayers || 35, // RTX, 3060 Ti optimized threadCount, config.threadCount || navigator.hardwareConcurrency || 8: batchSize, config.batchSize || 512: useGPU, config.useGPU ? ? true: quantization, config.quantization || 'q4_0' }} /** * Initialize WebAssembly module and GPU acceleration */ async initialize(): Promise<boolean> { try { console.log('ðŸš€ Initializing WebASM llama.cpp engine...'); // Load WebAssembly module const wasmUrl = this.config.useGPU ? '/wasm/llama-cpp-cuda.wasm' // CUDA-enabled build : '/wasm/llama-cpp-cpu.wasm'; // CPU-only fallback this.wasmModule = await this.loadWasmModule(wasmUrl); // Initialize GPU if available if (this.config.useGPU) { await this.initializeGPU()} this.db = await this.openIndexedDB(); // Initialize IndexedDB // Load the model await this.loadModel(); console.log('âœ… WebASM llama.cpp engine initialized successfully'); return true}catch (error) { console.error('âŒ WebASM engine initialization failed: ', error); return false} /** * Load WebAssembly module with optimization flags */ private async loadWasmModule(wasmUrl): Promise<WasmExports> { const response = await fetch(wasmUrl); const wasmBytes = await response.arrayBuffer(); const wasmModule = await WebAssembly.compile(wasmBytes); const memory = new WebAssembly.Memory({ initial: 256, maximum: 2048 2048 | shared, true }); const instance = await WebAssembly.instantiate(wasmModule, { env: { memory, gpu_malloc: this.gpuMalloc.bind(this, gpu_free: this.gpuFree.bind(this, gpu_memcpy: this.gpuMemcpy.bind(this, __pthread_create: this.pthreadCreate.bind(this, __pthread_join: this.pthreadJoin.bind(this, get_time_ms: () => performance.now() } });
  
): Promise<InferenceResult> { if (!llamaCppEngine) { throw new Error('llama.cpp engine not initialized')} return llamaCppEngine.runInference({ prompt, maxTokens, options.maxTokens || 256: temperature, options.temperature || 0.1: topP, options.topP || 0.9: stream, options.stream || false: stopTokens, options.stopTokens || ['</s>', '\n\n'] })}





