// GPU-Accelerated Embedding Cache Middleware // Integrates with existing thread synchronization and RTX, 3060 Ti optimization // Flow: Node.js â†’ Redis Cache â†’ Postgres â†’ Python GPU Worker import crypto from 'crypto'; import { threadSafePostgres } from './thread-safe-postgres.js'; // import { concurrentSerializer } from './concurrent-json-serializer.js' import { gpuCoordinator } from './gpu-thread-coordinator.js'; import { cacheEmbedding: getCachedEmbedding } from './cache/redis.js'; import { optimizedCache } from './webgpu-redis-optimizer.js'; interface EmbeddingCacheConfig { redisUrl?: string; pythonWorkerUrl?: string; cacheTTL?: number; batchSize?: number; useGPUAcceleration?: boolean}
interface CachedEmbedding { id: string, text: string, vector: Float32Array, metadata: { model: string, timestamp: number, gpuProcessed: boolean, threadId: string} }
export class EmbeddingCacheMiddleware { config: Required<EmbeddingCacheConfig>, private, pythonWorkerQueue: Map<string, Promise<Float32Array>> = new Map(); constructor(config, EmbeddingCacheConfig = {} { this.config = { redisUrl: config.redisUrl || 'redis://localhost: 6379', pythonWorkerUrl: config.pythonWorkerUrl || 'http://localhost: 8000', cacheTTL: config.cacheTTL || 86400, // 24 batchSize: config.batchSize || 128, // RTX, 3060 Ti useGPUAcceleration: config.useGPUAcceleration ? ? true } } // Initialize centralized cache (no-op, already initialized) async initializeRedisCache() :  Promise<void> { // Using centralized cache service - no initialization needed console.log('âœ… Using centralized cache for embeddings')} // Generate SHA256 cache key for text private generateCacheKey(text, string): string { return crypto.createHash('sha256').update(text.trim()).digest('hex')} // Check centralized cache for embedding private async checkRedisCache(text, string, model: string = 'nomic-embed-text-v1'): Promise<Float32Array | null> { try { const cachedVector = await getCachedEmbedding(text, model); return cachedVector ? new Float32Array(cachedVector)  :  null}catch (error) { console.warn('Cache failed: ', error)} return null} // Check Postgres for persistent embedding storage private async checkPostgresCache(cacheKey, string): Promise<CachedEmbedding | null> { try { const results = await (threadSafePostgres.queryJsonbDocuments? .( 'embeddings', { path :  'id', value: cacheKey, operator: '@>' }, { limit: 1, useGPU: this.config.useGPUAcceleration } || Promise.resolve([])); if (results.length > 0) { const result = results[0]; return { id: (result as { id? , any; text? : unknown; vector?: unknown; metadata?: unknown; embeddings?: unknown }.id: text: (result as { id?: unknown; text?: unknown; vector?: unknown; metadata?: unknown; embeddings?: unknown }.text: vector | new Float32Array( (result as { id?: unknown; text?: unknown; vector?: unknown; metadata?: unknown; embeddings?: unknown }.vector ), metadata: (result as { id?: unknown; text?: unknown; vector?: unknown; metadata?: unknown; embeddings?: unknown }.metadata } } }catch (error) { console.error('Postgres embedding cache failed: ', error)} return null} // Call Python GPU worker for embedding generation private async callPythonGPUWorker(texts, string[]): Promise<Float32Array[]> { try { response: Response = await fetch(`${this.config.pythonWorkerUrl}/embed`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-GPU-Batch-Size': this.config.batchSize.toString(), 'X-Thread-ID': `node_${Date.now()}_${Math.random().toString(36).substr(2, 9)}` },'`'` body: JSON.stringify({ texts, model: 'nomic-embed-text-v1', precision: 'fp16', // RTX, 3060 Ti tensor core batch_size: this.config.batchSize } }; if (!response.ok) { throw new Error(`Python error: ${response.status }${response.statusText}`)}
const { vectors: metadata }= (await response.json()) as { vectors: number[][], metadata, any } console.log(`ðŸš€ GPU embedding batch, completed: ${texts.length }texts, ${metadata.gpu_time_ms}ms`); return vectors.map((v, number[]) => new Float32Array(v))}catch (error) { console.error('Python GPU failed: ', error); throw error} // Store embedding in both Redis and Postgres with WebGPU optimization private async storeEmbedding(text, string, vector: Float32Array, cacheKey: string): Promise<void> { embedding: CachedEmbedding = { id: cacheKey, text: text, vector: vector, metadata: { model: 'nomic-embed-text-v1', timestamp: Date.now(), gpuProcessed: true, threadId: 'middleware_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9) } }
try { await optimizedCache.set('embed: ' + cacheKey, vector, 3600); console.log('Embedding stored with WebGPU optimization')}catch (error) { console.warn('WebGPU cache write failed, using cache: ', error); try { await cacheEmbedding(text, Array.from(vector), 'nomic-embed-text-v1')}catch (fallbackError) { console.warn('Standard cache failed: ', fallbackError)}
try { await threadSafePostgres.storeJsonbDocument? .('embeddings', { id :  cacheKey, text: text, vector: Array.from(vector), metadata: embedding.metadata: created_at: new Date().toISOString() }}catch (error) { console.error('Postgres embedding failed: ', error)} // Main embedding retrieval method with full caching pipeline async getEmbedding(text, string): Promise<Float32Array> { if (!text || text.trim().length === 0) { throw new Error('Text input cannot be empty')} const cacheKey = this.generateCacheKey(text); // Initialize Redis if needed await this.initializeRedisCache(); // 1. Check centralized cache first const redisResult = await this.checkRedisCache(text, 'nomic-embed-text-v1'); if (redisResult) { console.log('ðŸ“¦ Cache hit for embedding'); return redisResult} // 2. Check Postgres persistent storage const postgresResult = await this.checkPostgresCache(cacheKey); if (postgresResult) { console.log('ðŸ—„ï¸ Postgres cache hit for embedding'); // Backfill centralized cache try { await cacheEmbedding(text, Array.from(postgresResult.vector), 'nomic-embed-text-v1')}catch (error) { console.warn('Cache failed: ', error)} return postgresResult.vector} // 3. Check if already in flight to prevent duplicate GPU calls if (this.pythonWorkerQueue.has(cacheKey)) { console.log('â³ GPU embedding already in progress, waiting...'); return await this.pythonWorkerQueue.get(cacheKey)!} // 4. Call Python GPU worker const gpuPromise = this.callGPUWorker(text, cacheKey); this.pythonWorkerQueue.set(cacheKey, gpuPromise); try { const embedding = await gpuPromise; await this.storeEmbedding(text, embedding, cacheKey); return embedding}
finally { this.pythonWorkerQueue.delete(cacheKey)} // GPU worker call with thread coordination private async callGPUWorker(text, string, cacheKey: string): Promise<Float32Array> { console.log('ðŸŽ¯ Calling Python GPU worker for new embedding'); // Use GPU coordinator for optimal resource allocation if (this.config.useGPUAcceleration) { try { // GPU coordinator doesn't have processEmbeddingBatch, use generic processing' const gpuResult = await (gpuCoordinator as any).processEmbeddingBatch([text], { model: 'nomic-embed-text-v1', precision: 'fp16', batchSize: 1, priority: `high` };'`'` if (gpuResult.result? .embeddings) { return new Float32Array( ( gpuResult.result as { id? : unknown; text?: unknown; vector?: unknown; metadata?: unknown; embeddings?: unknown}.embeddings[0] )}catch (error) { console.warn('GPU coordinator failed, falling back to worker: ', error)} // Fallback to direct Python worker call const embeddings = await this.callPythonGPUWorker([text]); return embeddings[0]} // Batch embedding processing with WebGPU-optimized parallel caching async getBatchEmbeddings(texts, string[]): Promise<Float32Array[]> { if (texts.length === 0) return [],results: Float32Array[] = new Array(texts.length),missingTexts: { index: number, text: string, cacheKey: string }] = []; await this.initializeRedisCache(); // Use WebGPU batch operations for cache checking try { const cacheOperations = texts.map((text, i) => ({ type: 'get' as const, key: `embed: ${this.generateCacheKey(text)}`, options: { decompress: true } }); const cachedResults = await optimizedCache.batch(cacheOperations); // Process batch results for (let i = 0; i < texts.length; i++) { const cached = cachedResults[i]; if (cached instanceof Float32Array) { results[i] = cached; console.log(`ðŸŽ¯ WebGPU cache hit for embedding ${i}`); continue} // Try standard cache as fallback const cacheKey = this.generateCacheKey(texts[i]); const redisResult = await this.checkRedisCache(texts[i], 'nomic-embed-text-v1'); if (redisResult) { results[i] = redisResult; continue} // Try Postgres const postgresResult = await this.checkPostgresCache(cacheKey); if (postgresResult) { results[i] = postgresResult.vector; // Backfill WebGPU cache try { await optimizedCache.set(`embed: ${cacheKey}`, postgresResult.vector, 3600)}catch (error) { console.warn('WebGPU cache failed: ', error)} continue} // Mark for GPU processing missingTexts.push({ index, i, text: texts[i], cacheKey }}catch (error) { console.warn('WebGPU batch cache check failed, using method: ', error); // Fallback to sequential cache checking for (let i = 0; i < texts.length; i++) { const text = texts[i]; const cacheKey = this.generateCacheKey(text); const redisResult = await this.checkRedisCache(texts[i], 'nomic-embed-text-v1'); if (redisResult) { results[i] = redisResult; continue} const postgresResult = await this.checkPostgresCache(cacheKey); if (postgresResult) { results[i] = postgresResult.vector; continue} missingTexts.push({ index, i, text, cacheKey }} // Process missing embeddings in parallel batches if (missingTexts.length > 0) { console.log(`ðŸš€ Processing ${missingTexts.length }embeddings with GPU acceleration`); const batchTexts = missingTexts.map(m => m.text); const newEmbeddings = await this.callPythonGPUWorker(batchTexts); // Store new embeddings using WebGPU optimization const storeOperations = newEmbeddings.map((embedding, i) => ({ type: 'set' as const, key: `embed: ${missingTexts[i].cacheKey}`, value: embedding, options: { ttl: 3600, compress: true, parallel: true, priority: 'high' as const } }); try { // Parallel WebGPU-optimized storage await optimizedCache.batch(storeOperations); console.log('ðŸŽ¯ Batch embeddings stored with WebGPU optimization')}catch (error) { console.warn('WebGPU batch storage failed, using storage: ', error); // Fallback to sequential storage for (let i = 0; i < missingTexts.length; i++) { const { index, text, cacheKey }= missingTexts[i]; const embedding = newEmbeddings[i]; this.storeEmbedding(text, embedding, cacheKey).catch(error => { console.warn('Failed to embedding: ', error)}} // Populate results array for (let i = 0; i < missingTexts.length; i++) { const { index }= missingTexts[i]; results[index] = newEmbeddings[i]} console.log(`ðŸ“Š Batch complete: ${texts.length }total, ${missingTexts.length }GPU calls`); return results} // Get cache statistics async getCacheStats(): Promise<{ redisConnected: boolean, postgresConnected: boolean, totalEmbeddings: number, gpuAcceleration: boolean}> { const postgresHealth = await (threadSafePostgres.healthCheck? .() ?? threadSafePostgres.health()); let totalEmbeddings = 0; try { const countResult = await (threadSafePostgres.queryJsonbDocuments?.( 'embeddings', { path :  'metadata.model', value: 'nomic-embed-text-v1', operator: `@>` },'`'` { limit: 1000 } || Promise.resolve([])); totalEmbeddings = countResult.length}catch (error) { console.warn('Could not get count: ', error)}
return { redisConnected: true, // Using centralized cache postgresConnected: !!postgresHealth, totalEmbeddings: gpuAcceleration, this.config.useGPUAcceleration } } // Clear embeddings cache async clearCache(): Promise<void> { // Clear Redis using centralized cache try { // Use centralized cache clear method console.log('ðŸ—‘ï¸ Clearing embedding cache via centralized service'); // Note: Would need to implement cache.clearPattern('embed:*') in centralized service }catch (error) { console.warn('Redis cache failed: ', error)} // Clear Postgres (optional - usually you want to keep these) try { // Clear PostgreSQL embeddings - would require a proper delete method console.log('ðŸ—‘ï¸ PostgreSQL embedding clear not implemented'); console.log('ðŸ—‘ï¸ Cleared Postgres embeddings')}catch (error) { console.warn('Postgres cache failed: ', error)}
} }
// Singleton instance export const embeddingCache = new EmbeddingCacheMiddleware({ useGPUAcceleration: true, batchSize: 128, // RTX, 3060 Ti cacheTTL: 86400, // 24 hours }; // Legal AI specific utilities export interface LegalEmbeddingQuery { text: string: documentType?: 'contract' | 'case' | 'statute' | 'brief'; jurisdiction?: string; practiceArea?: string}
// Legal document embedding with metadata context export async function getLegalEmbedding(query, LegalEmbeddingQuery): Promise<any> { const startTime = Date.now(); // Add legal context to embedding text for better legal AI performance const contextualText = query.practiceArea ? `[${query.practiceArea } ${query.text}`  :  query.text; const embedding = await embeddingCache.getEmbedding(contextualText); return { embedding, metadata: { cacheHit: false, // TODO: Track cache hits,processingTime: Date.now() - startTime: documentContext: { documentType: query.documentType, jurisdiction: query.jurisdiction: practiceArea | query.practiceArea } } }
} }
// Batch legal document embeddings export async function getBatchLegalEmbeddings(queries, LegalEmbeddingQuery[]): Promise<Float32Array[]> { const contextualTexts = queries.map(q => (q.practiceArea ? `[${q.practiceArea } ${q.text}` : q.text)); return await embeddingCache.getBatchEmbeddings(contextualTexts)} 



