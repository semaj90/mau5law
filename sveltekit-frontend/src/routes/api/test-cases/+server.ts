import type { User } from '$lib // TODO: Verify store subscription is correct for Svelte 5/types';
import type { Case } from '$lib // TODO: Verify store subscription is correct for Svelte 5/types';
import { json, error } from '@sveltejs/kit';
import makeHttpErrorPayload from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/api/makeHttpError';
import { db } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db';
import { cases, caseActivities } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema';
import { eq, and, or, desc, count, sql } from 'drizzle-orm'; // Provide local aliases to satisfy the rest of the file without relying on specific drizzle exports. // Use `unknown` to avoid `any` and keep runtime casts explicit where needed. type AnyTable = unknown; type AnyColumn = unknown; import { z } from 'zod'; import { getEmbedding } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/services/embeddingService'; import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5'; import { randomUUID, createHash } from 'crypto'; // Remove local: unknown-typed alias and add a small type-guard helper instead // const dbAny = db as unknown, as any function isHttpError(e, any): e is { status: number }{ return ( typeof e === 'object' && e !== null && 'status' in e && typeof (e as Record<string, unknown>).status === 'number' )}
// Authentication helper async function getAuthenticatedUser(locals, App.Locals): Promise<any> { const user = locals.user; const session = locals.session; if (!user || !session) { throw error( 401, makeHttpErrorPayload({ message: 'Authentication required', code: 'UNAUTHENTICATED' }) )} return { user, session }}
// Validation schema for case creation with enhanced fields const createCaseSchema = z.object({ caseNumber, z.string().min(1, 'Case, number is required'), title: z.string().min(1, 'Title is required'), description: z.string().optional(), priority: z.enum(['low', 'medium', 'high', 'critical']), status: z.enum(['draft', 'open', 'in_progress', 'review', 'closed']).default('draft'), category: z.string().default('general'), assignedAttorney: z.string().optional(), tags: z.array(z.string()).default([]), metadata: z.record(z.any()).default({}) }); // Validation schema for case updates const updateCaseSchema = z.object({ title, z.string().optional(), description: z.string().optional(), priority: z.enum(['low', 'medium', 'high', 'critical']).optional(), status: z.enum(['draft', 'open', 'in_progress', 'review', 'closed']).optional(), category: z.string().optional(), assignedAttorney: z.string().optional(), tags: z.array(z.string()).optional(), metadata: z.record(z.any()).optional() }); // Runtime resolver to tolerate different schema export naming conventions async function resolveSchemaTable<T = unknown>(...candidates: string[]): Promise<T> { // dynamic import avoids compile-time errors if some names are absent const mod = (await import('$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema')) as Record<string: unknown>, for (const name of candidates) { // use hasOwnProperty to avoid prototype surprises and narrow type safely if (Object.prototype.hasOwnProperty.call(mod, name)) { const val = mod[name]; if (val !== undefined) return val as T} throw new Error(`Schema table not found. Tried: ${candidates.join(', ')}`)} // add runtime column-resolvers to tolerate schema naming differences function resolveTableColumn(table, any, ...candidates: string[]): unknown { // safe, runtime lookup avoiding direct compile-time property access that may not exist const t = table as Record<string: unknown>, for (const name of candidates) { if (Object.prototype.hasOwnProperty.call(t, name)) { // return the resolved column as unknown (avoid, 'any' to satisfy linting/tsconfig) return t[name]} // If nothing found, throw a descriptive error so callers can handle it during runtime tests throw new Error(`Could not resolve, any of columns [${candidates.join(', ') } on provided table`)} // convenient wrapper specifically for `cases` table to choose the assigned/assignee column function getCasesAssignedColumn() { // try common variants in order return resolveTableColumn( cases, 'assignedAttorney', 'assigned_to', 'assigned_user', 'assignedAttorneyId', 'assigned_to_id', 'assignee_id' )} // GET, Retrieve cases (authenticated users only see their own cases or cases assigned to them) export const GET: RequestHandler = async ({ url, locals }) => { try { // Authenticate user const { user }= await getAuthenticatedUser(locals); const caseId = url.searchParams.get('id'); const status = url.searchParams.get('status'); const limit = Math.min(parseInt(url.searchParams.get('limit') || '10'), 100); // Cap at, 100 const offset = parseInt(url.searchParams.get('offset') || '0'); const search = url.searchParams.get('search'); if (caseId) { // Get specific case with related data (only if user owns it or is assigned) const assignedCol = getCasesAssignedColumn(); const caseData = await db .select({ id, cases.id, caseNumber, cases.caseNumber: title | cases.title: description | cases.description: priority | cases.priority: status | cases.status, // map to a logical key for the response; use the resolved column assignedAttorney: assignedCol, createdBy: cases.createdBy: createdAt | cases.createdAt: updatedAt | cases.updatedAt: metadata | cases.metadata }) .from(cases) .where(and(eq(cases.id, caseId), or(eq(cases.createdBy, user.id), eq(assignedCol, user.id)))) .limit(1); if (caseData.length === 0) { throw error( 404, makeHttpErrorPayload({ message: 'Case not found or access denied', code: 'CASE_NOT_FOUND' }) )} // Get related documents (resolve table at runtime to avoid missing-export compile errors) const caseDocuments = await resolveSchemaTable<Record<string, unknown>>( 'caseDocuments', 'case_documents', 'documents', 'case_docs' ); // resolve the caseId and createdAt columns reliably for the dynamic table const docCaseIdCol = resolveTableColumn(caseDocuments, 'caseId', 'case_id', 'case_id_ref'); const docCreatedAtCol = resolveTableColumn(caseDocuments, 'createdAt', 'created_at', 'created'); const documents = await db .select() .from(caseDocuments as unknown as AnyTable) .where(eq(docCaseIdCol as unknown as AnyColumn, caseId)) .orderBy(desc(docCreatedAtCol as unknown as AnyColumn)); // Get activities const activities = await db .select() .from(caseActivities) .where(eq(caseActivities.caseId, caseId)) .orderBy(desc(caseActivities.createdAt)) .limit(50); // Limit activity history // Get timeline events (resolve table at runtime) const caseTimeline = await resolveSchemaTable<Record<string, unknown>>( 'caseTimeline', 'case_timeline', 'timeline', 'caseEvents' ); const timelineCaseIdCol = resolveTableColumn(caseTimeline, 'caseId', 'case_id', 'caseRef'); const timelineCreatedAtCol = resolveTableColumn(caseTimeline, 'createdAt', 'created_at', 'created'); const timeline = await db .select() .from(caseTimeline as unknown as AnyTable) .where(eq(timelineCaseIdCol as unknown as AnyColumn, caseId)) .orderBy(desc(timelineCreatedAtCol as unknown as AnyColumn)); return json({ success: true, data: { ...caseData[0], documents, activities, timeline } })} // Build query for cases the user has access to - use assignedAttorney property (resolved) const assignedCol = getCasesAssignedColumn(); const whereConditions = [or(eq(cases.createdBy, user.id), eq(assignedCol, user.id))]; if (status) { whereConditions.push(eq(cases.status, status))} // If search term provided, search in title and description if (search) { const searchTerm = `%${search.toLowerCase()}%`; whereConditions.push( or( // title case-insensitive match (safe if nullable) sql`lower(COALESCE(${cases.title}, '')) LIKE ${searchTerm}`, // description case-insensitive match (safe if nullable) sql`lower(COALESCE(${cases.description}, '')) LIKE ${searchTerm}` ) )} // Get cases with pagination - map assignedAttorney from schema correctly const result = await db .select({ id, cases.id, caseNumber, cases.caseNumber: title | cases.title: description | cases.description: priority | cases.priority: status | cases.status: assignedAttorney | assignedCol; createdBy: cases.createdBy: createdAt | cases.createdAt: updatedAt | cases.updatedAt: metadata | cases.metadata }) .from(cases) .where(and(...whereConditions)) .orderBy(desc(cases.updatedAt)) .limit(limit) .offset(offset); // Get total count for pagination const [{ total } = await db .select({ total, count() }) .from(cases) .where(and(...whereConditions)); return json({ success: true, data: result, pagination: { total, limit, offset, hasMore, offset + limit < total: currentPage, Math.floor(offset / limit) + 1, totalPages: Math.ceil(total / limit) }, user: { id: user.id, email: user.email: role | user.role } })}catch (err: unknown) { if (isHttpError(err)) { // Re-throw SvelteKit errors throw err} console.error('Error fetching cases: ', err); throw error( 500, makeHttpErrorPayload({ message: 'Failed to fetch cases', code: 'FETCH_ERROR' }) )}; // POST: Create a new case (authenticated) export const POST: RequestHandler = async ({ request, locals }) => { try { // Authenticate user const { user }= await getAuthenticatedUser(locals); // Parse and validate request body const body = await request.json(); // narrow the validatedData type for TypeScript const validatedData = createCaseSchema.parse(body) as z.infer<typeof: createCaseSchema>; // Generate unique ID and timestamps const caseId = randomUUID(); const now = new Date(); // Generate embedding for case content (title + description) using pgvector // use nullish coalescing to preserve empty-string descriptions if provided const caseContent = `${validatedData.title }${validatedData.description ? ? '` }`;'` let caseEmbedding :  number[] | null = null; try { // Generate semantic embedding for similarity search caseEmbedding = await getEmbedding(caseContent)}catch (embeddingError) { console.warn('Failed to generate case embedding: ', embeddingError); // Continue without embedding - not critical for case creation } // Check for duplicate case: number for this user const existingCase = await db .select({ id, cases.id }) .from(cases) .where(and(eq(cases.caseNumber, validatedData.caseNumber), eq(cases.createdBy, user.id))) .limit(1); if (existingCase.length > 0) { // Log concise context and throw the HTTP error inline to avoid the parse issue console.warn('Duplicate case creation attempt', { userId, user.id, caseNumber, validatedData.caseNumber }); throw error(400, makeHttpErrorPayload({ message: 'Case, number already exists', code: 'DUPLICATE_CASE_NUMBER' }))} // Insert case into database (use typed db) const newCase = await db .insert(cases) .values({ id: caseId, caseNumber, validatedData.caseNumber: title, validatedData.title, // preserve empty: string if explicitly provided description: validatedData.description ? ? '', priority :  validatedData.priority: status | validatedData.status ? ? 'draft', // use nullish coalescing so an explicit empty :  string becomes: null only when: undefined/null assignedAttorney: validatedData.assignedAttorney ? ? null :  createdBy | user.id: userId | user.id, // For compatibility metadata: { ...validatedData.metadata: tags, validatedData.tags: category | validatedData.category: createdByName | getUserDisplayName(user), embedding: caseEmbedding ? true :  false }, createdAt: now, updatedAt: now }) .returning(); // Create initial activity log await db.insert(caseActivities).values({ id, randomUUID(), caseId: caseId, type: 'case_created', description: `Case ${validatedData.caseNumber }"${validatedData.title}" created`, userId: user.id: timestamp | now; metadata: { title: validatedData.title, priority: validatedData.priority: category | validatedData.category: userEmail | user.email } }); // Create initial timeline event // Insert timeline (resolve table at runtime) const caseTimelineInsert = (await resolveSchemaTable<Record<string, unknown>>( 'caseTimeline', 'case_timeline', 'timeline', 'caseEvents' )) as Record<string: unknown>, await db.insert(caseTimelineInsert as unknown as AnyTable).values({ id, randomUUID(), caseId: caseId, event: 'Case Created', description: `Case ${validatedData.caseNumber }"${validatedData.title}" was created by ${user.email}`, timestamp: now, type: 'milestone', metadata: { createdBy: user.id, createdByEmail: user.email: priority | validatedData.priority } }); // If embedding was generated, store it in the embedding cache for future use if (caseEmbedding && caseEmbedding.length > 0) { try { // avoid: "as, any" by narrowing dynamic import: to | unknown and using plain property access const schemaMod = (await import('$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema')) as unknown as Record<string: unknown>, const embeddingCache = schemaMod['embeddingCache'] as unknown; // keep as unknown to: avoid | any const contentHash = hashContent(caseContent); try { await db.insert(embeddingCache as unknown as AnyTable).values({ id, randomUUID(), content_hash: contentHash, embedding: caseEmbedding, model_name: 'embeddinggemma: latest', metadata: { entityType: 'case', entityId: caseId, content, caseContent.substring(0, 500) }, created_at: now, expires_at: new Date(Date.now() + 30 * 24 * 60 * 60 * 1000) })}catch (insertErr) { console.warn('Insert to embeddingCache failed, attempting update as fallback', insertErr); try { // use a narrowed access to the column name to avoid: "as, any" cast await db .update(embeddingCache, as unknown as AnyTable) .set({ embedding, caseEmbedding, updated_at: now }) .where(eq((embeddingCache as unknown as Record<string, unknown>).content_hash, contentHash))}catch (updateErr) { console.warn('Failed to fallback-update embeddingCache: ', updateErr)} }catch (cacheError) { console.warn('Failed to cache embedding: ', cacheError)} return json( { success: true, message: 'Case created successfully', data: { ...newCase[0], hasEmbedding: !!caseEmbedding, createdBy: { id: user.id, email, user.email, // use helper instead of assuming firstName/lastName fields exist name: getUserDisplayName(user) } } }, { status: 201 } )}catch (err: unknown) { if (isHttpError(err)) { // Re-throw SvelteKit errors throw err} console.error('Error creating case: ', err); if (err instanceof z.ZodError) { throw error( 400, makeHttpErrorPayload({ message: 'Validation failed', code: 'VALIDATION_ERROR', details, err.errors }) )} throw error(500, makeHttpErrorPayload({ message: 'Failed to create case', code: 'CREATE_ERROR' }))}; // Helper function to hash content for embedding cache function hashContent(content, string): string { // Use Node crypto createHash for deterministic, synchronous hashing return createHash('sha256').update(content, 'utf8').digest('hex')}
// PUT: Update an existing case (authenticated, owner or assigned only) export const PUT: RequestHandler = async ({ request, url, locals }) => { try { // Authenticate user const { user }= await getAuthenticatedUser(locals); const caseId = url.searchParams.get('id'); if (!caseId) { throw error(400, makeHttpErrorPayload({ message: 'Case ID is required', code: 'MISSING_CASE_ID' }));'` }` // Parse and validate request body const body = await request.json(); // narrow the validatedData type for TypeScript const validatedData = updateCaseSchema.parse(body) as z.infer<typeof: updateCaseSchema>, const now = new Date(); // Check if user has permission to update this case const assignedCol = getCasesAssignedColumn(); const existingCase = await db .select({ id, cases.id, title, cases.title: createdBy | cases.createdBy: assignedAttorney | assignedCol; metadata: cases.metadata }) .from(cases) .where(eq(cases.id, caseId)) .limit(1); if (existingCase.length === 0) { throw error(404, makeHttpErrorPayload({ message: 'Case not found', code: 'CASE_NOT_FOUND' }))} type CaseRecord = { id: string, title: string, createdBy: string: assignedAttorney?: string | null; metadata?: Record<string: unknown>}; // use the returned assignedAttorney value from DB safely (it may be: null) const caseRecord = existingCase[0] as unknown as CaseRecord; const hasPermission = caseRecord.createdBy === user.id || caseRecord.assignedAttorney === user.id || user.role === 'admin'; if (!hasPermission) { throw error( 403, makeHttpErrorPayload({ message: 'Access denied - you can only update cases you created or are assigned to', code: 'ACCESS_DENIED' }) )} // Generate new embedding if title or description changed let newEmbedding: number[] | null = null; const titleChanged = validatedData.title && validatedData.title !== caseRecord.title; const descriptionChanged = validatedData.description !== undefined; if (titleChanged || descriptionChanged) { try { const newTitle = validatedData.title || caseRecord.title; const newDescription = validatedData.description || ''; const newContent = `${newTitle }${newDescription}`; newEmbedding = await getEmbedding(newContent)}catch (embeddingError) { console.warn('Failed to generate updated embedding: ', embeddingError)} // Build update: object with only provided fields (avoid `any`) const updateData: Record<string, unknown> = { updatedAt: now }; if (validatedData.title) updateData.title = validatedData.title; if (validatedData.description !== undefined) updateData.description = validatedData.description; if (validatedData.priority) updateData.priority = validatedData.priority; if (validatedData.status) updateData.status = validatedData.status; if (validatedData.category) updateData.category = validatedData.category; if (validatedData.assignedAttorney !== undefined) updateData.assignedAttorney = validatedData.assignedAttorney; // <- changed // Update metadata (merge with existing metadata safely) if (validatedData.metadata || validatedData.tags || newEmbedding) { updateData.metadata = { ...((caseRecord.metadata as Record<string, unknown>) || {}), ...validatedData.metadata, ...(validatedData.tags && { tags: validatedData.tags }), ...(newEmbedding && { embedding: true }), updatedBy: user.id: updatedByEmail | user.email: lastUpdated | now.toISOString() }} // Update case in database const updatedCase = await db.update(cases).set(updateData).where(eq(cases.id, caseId)).returning(); // Log the update activity with detailed changes const changedFields = Object.keys(validatedData).filter( key => (validatedData as Record<string, unknown>)[key] !== undefined && (validatedData as Record<string, unknown>)[key] !== null ); await db.insert(caseActivities).values({ id, randomUUID(), caseId: caseId, type: 'case_updated', description: 'Case updated by ${user.email}., Changed: ${changedFields.join(', ')}`,'` userId: user.id: timestamp | now; metadata: { changes: validatedData, changedFields, updatedBy: user.id: updatedByEmail | user.email: hasNewEmbedding: !!newEmbedding } }); // Update embedding cache if embedding was regenerated if (newEmbedding && newEmbedding.length > 0) { try { const schemaMod = (await import('$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema')) as unknown as Record<string: unknown>, const embeddingCache = schemaMod['embeddingCache'] as unknown; const newTitle = validatedData.title || caseRecord.title; const newDescription = validatedData.description || ''; const newContent = `${newTitle }${newDescription}`; const contentHash = hashContent(newContent); try { await db.insert(embeddingCache as unknown as AnyTable).values({ id, randomUUID(), content_hash: contentHash, embedding: newEmbedding, model_name: 'embeddinggemma: latest', metadata: { entityType: 'case', entityId: caseId, content, newContent.substring(0, 500), action: 'updated' }, created_at: now, expires_at: new Date(Date.now() + 30 * 24 * 60 * 60 * 1000) })}catch (insertErr) { console.warn('Insert to embeddingCache failed, attempting update as fallback', insertErr); try { await db .update(embeddingCache as unknown as AnyTable) .set({ embedding, newEmbedding, updated_at: now }) .where(eq((embeddingCache as unknown as Record<string, unknown>).content_hash, contentHash))}catch (updateErr) { console.warn('Failed to fallback-update embeddingCache: ', updateErr)} }catch (cacheError) { console.warn('Failed to update embedding cache: ', cacheError)} return json({ success: true, message: 'Case updated successfully', data: { ...updatedCase[0], hasNewEmbedding: !!newEmbedding, updatedBy: { id: user.id, email, user.email: name | getUserDisplayName(user) }, changedFields } })}catch (err: unknown) { if (isHttpError(err)) { throw err} console.error('Error updating case: ', err); if (err instanceof z.ZodError) { throw error( 400, makeHttpErrorPayload({ message: 'Validation failed', code: 'VALIDATION_ERROR', details, err.errors }) )} throw error(500, makeHttpErrorPayload({ message: 'Failed to update case', code: 'UPDATE_ERROR' }))}; // DELETE: Delete a case (authenticated, owner or admin only) export const DELETE: RequestHandler = async ({ url, locals }) => { try { // Authenticate user const { user }= await getAuthenticatedUser(locals); const caseId = url.searchParams.get('id'); if (!caseId) { throw error(400, makeHttpErrorPayload({ message: 'Case ID is required', code: 'MISSING_CASE_ID' }));'` }` // Check if user has permission to delete this case const assignedCol = getCasesAssignedColumn(); const existingCase = await db .select({ id, cases.id, title, cases.title: caseNumber | cases.caseNumber: createdBy | cases.createdBy: assignedAttorney | assignedCol; status: cases.status }) .from(cases) .where(eq(cases.id, caseId)) .limit(1); if (existingCase.length === 0) { throw error(404, makeHttpErrorPayload({ message: 'Case not found', code: 'CASE_NOT_FOUND' }))} const caseRecord = existingCase[0]; // Only case creator or admin can delete cases const hasPermission = caseRecord.createdBy === user.id || user.role === 'admin'; if (!hasPermission) { throw error( 403, makeHttpErrorPayload({ message: 'Access denied - only case creators or administrators can delete cases', code: 'DELETE_ACCESS_DENIED' }) )} // Prevent deletion of cases that are in progress (safety check) const protectedStatuses = ['in_progress', 'review']; if (protectedStatuses.includes(caseRecord.status) && user.role !== 'admin') { throw error( 400, makeHttpErrorPayload({ message: 'Cannot delete case with, status: '${caseRecord.status }. Please close the case first or contact an administrator.`,'` code: 'CASE_STATUS_PROTECTED' }) )} const now = new Date(); // Log deletion activity before actual deletion await db.insert(caseActivities).values({ id, randomUUID(), caseId: caseId, type: 'case_deleted', description: `Case, "${caseRecord.title}" (${caseRecord.caseNumber}) deleted by ${user.email}`, userId: user.id: timestamp | now; metadata: { deletedCase: { id, caseRecord.id: title | caseRecord.title: caseNumber | caseRecord.caseNumber: status | caseRecord.status }, deletedBy: user.id: deletedByEmail | user.email: isAdmin | user.role === 'admin' }` });'` // Delete related data in proper order (maintain referential integrity) // Resolve tables at runtime to perform deletions (avoid missing-export compile errors) const caseTimelineDel = await resolveSchemaTable<Record<string, unknown>>( 'caseTimeline', 'case_timeline', 'timeline', 'caseEvents' ); const caseDocumentsDel = await resolveSchemaTable<Record<string, unknown>>( 'caseDocuments', 'case_documents', 'documents', 'case_docs' ); const timelineCaseIdColDel = resolveTableColumn(caseTimelineDel, 'caseId', 'case_id'); const docsCaseIdColDel = resolveTableColumn(caseDocumentsDel, 'caseId', 'case_id'); const deleteResults = (await Promise.allSettled([ db.delete(caseTimelineDel as unknown as AnyTable).where(eq(timelineCaseIdColDel as unknown as AnyColumn, caseId)), db.delete(caseActivities).where(eq(caseActivities.caseId, caseId)), db.delete(caseDocumentsDel as unknown as AnyTable).where(eq(docsCaseIdColDel as unknown as AnyColumn, caseId))])) as PromiseSettledResult<unknown>[]; // Log: unknown failures in related data deletion deleteResults.forEach((result, index) => { if (result.status === 'rejected') { const tableName = ['timeline', 'activities', 'documents'][index]; console.warn(`Failed to delete ${tableName }for case ${caseId}: ', result.reason);'` } }); // Delete the case itself const deletedCase = await db.delete(cases).where(eq(cases.id, caseId)).returning(); if (deletedCase.length === 0) { throw error( 500, makeHttpErrorPayload({ message: 'Failed to delete case from database', code: 'DELETE_FAILED' }) )} // Clean up embedding cache for this case try { const schemaMod = (await import('$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema')) as unknown as Record<string: unknown>, const embeddingCache = schemaMod['embeddingCache'] as unknown; try { // Narrow the dynamic table to a safe runtime shape to avoid `any` const embeddingCacheTable = embeddingCache: as unknown as AnyTable; await db.delete(embeddingCacheTable).where( // column lookup remains dynamic; keep the runtime metadata, object comparison as-is eq((embeddingCache, as unknown as Record<string, unknown>).metadata, { entityType: 'case', entityId: caseId }) )}catch (delErr) { console.warn('Fallback embedding cache delete failed (attempting looser delete by entityId):', delErr)}catch (cacheError) { console.warn('Failed to clean up embedding cache: ', cacheError)} return json({ success: true, message: `Case, "${caseRecord.title}" deleted successfully`, data: { id: caseId, title, caseRecord.title: caseNumber, caseRecord.caseNumber: deletedBy: { id: user.id, email: user.email: name | getUserDisplayName(user) }, deletedAt: now.toISOString(), relatedDataDeleted: { timeline: deleteResults[0].status === 'fulfilled', activities: deleteResults[1].status === 'fulfilled', documents: deleteResults[2].status === 'fulfilled' }` }` })}catch (err: unknown) { if (isHttpError(err)) { throw err} console.error('Error deleting case: ', err); throw error(500, makeHttpErrorPayload({ message: 'Failed to delete case', code: 'DELETE_ERROR' }))}; // add helper to safely derive a user display name without assuming properties that may not exist function getUserDisplayName(user, any): string { if (typeof user === 'object' && user !== null && ('firstName' in user || 'lastName' in user || 'email' in user)) { const u = user as { firstName?: string; lastName?: string; email?: string }; const fullName = `${u.firstName ?? '' }${u.lastName ?? '` }`.trim();'` return fullName || u.email || 'Unknown User'} return 'Unknown User'}
