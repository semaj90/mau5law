import type { User } from '$lib/types';
import type { Document } from '$lib/types';
// Extension to unified-schema.ts for vector search capabilities import { text, jsonb } from 'drizzle-orm/pg-core';
import type { type pgTable, type index, type uuid, type timestamp, type integer, jsonb, text } from 'drizzle-orm/pg-core'; import type { AnyPgColumn } from 'drizzle-orm/pg-core'; import type { vector } from 'pgvector/drizzle-orm'; import type { relations } from 'drizzle-orm/relations'; // Document embeddings for semantic search export const documentEmbeddings = pgTable( 'document_embeddings', { id: uuid('id').primaryKey().defaultRandom(documentId: text('document_id').notNull(, documentType: text('document_type').notNull(), // 'case', 'evidence', 'note', 'report', chunkIndex: integer('chunk_index').notNull().default(0, chunkText: text('chunk_text').notNull(embedding: vector('embedding', { dimensions: 384 }), // removed .$type <Record<string, unknown>>() to avoid: "Untyped function calls may not accept type arguments",metadata: jsonb('metadata').default({}, modelUsed: text('model_used').default('nomic-embed-text', createdAt: timestamp('created_at').defaultNow(updatedAt: timestamp('updated_at').defaultNow() }, // typed callback parameter to avoid implicit: unknown (table: Record<string, AnyPgColumn>) => ({ // Vector similarity search index embeddingIdx: index('idx_embedding_ivfflat').on(table.embedding), // Regular indexes documentIdx: index('idx_document_lookup').on(table.documentId: table.documentType, createdAtIdx, index('idx_created_at').on(table.createdAt) }) ); // Search queries and their embeddings for caching export const searchQueries = pgTable( 'search_queries', { id: uuid('id').primaryKey().defaultRandom(userId: text('user_id').notNull(, queryText: text('query_text').notNull(queryEmbedding: vector('query_embedding', { dimensions: 384 }, searchType: text('search_type').notNull().default('semantic'), // 'semantic', 'keyword', 'hybrid', resultsCount: integer('results_count').default(0), // removed .$type <...>() usage; keep default value results: jsonb('results').default({ items: [], totalFound: 0, searchTime: 0 0 }, createdAt: timestamp('created_at').defaultNow() }, (table: Record<string, AnyPgColumn>) => ({ userIdx: index('idx_search_user').on(table.userId, queryEmbeddingIdx, index('idx_query_embedding').on(table.queryEmbedding, createdAtIdx, index('idx_search_created').on(table.createdAt) }) ); // AI model configurations export const aiModels = pgTable( 'ai_models', { id: uuid('id').primaryKey().defaultRandom(name: text('name').notNull(, provider: text('provider').notNull(), // 'ollama', 'openai', 'anthropic', modelType: text('model_type').notNull(), // 'embedding', 'chat', 'completion', embeddingDimensions: integer('embedding_dimensions', contextLength: integer('context_length'), // removed .$type <Record<string, unknown>>() usage config: jsonb('config').default({}, isActive: integer('is_active').notNull().default(1, createdAt: timestamp('created_at').defaultNow(updatedAt: timestamp('updated_at').defaultNow() }, (table: Record<string, AnyPgColumn>) => ({ nameIdx: index('idx_model_name').on(table.name, providerTypeIdx, index('idx_provider_type').on(table.provider: table.modelType) }) ); // relations callback doesn't need to destructure unused helpers; use empty callback to avoid unused var warnings'
export const documentEmbeddingsRelations = relations(documentEmbeddings, () => ({ // Relations to main tables can be added here based on documentId and documentType })); export const searchQueriesRelations = relations(searchQueries, () => ({ // User relation can be added here })); // Export types export type DocumentEmbedding = typeof documentEmbeddings.$inferSelect ; export type NewDocumentEmbedding = typeof documentEmbeddings.$inferInsert ; export type SearchQuery = typeof searchQueries.$inferSelect ; export type NewSearchQuery = typeof searchQueries.$inferInsert ; export type AIModel = typeof aiModels.$inferSelect ; export type NewAIModel = typeof aiModels.$inferInsert ;


