# data/ — Archive Analysis
**Archived:** March 9, 2026

MinIO test data directory with sample files in a minio/ subdirectory.

**Replaced by:** MinIO data stored in `minio-data/` (gitignored) and managed via Docker `phase66-minio` service.

**Files:** minio/ subdir (9 files)
