/** * ðŸŽ® REDIS-OPTIMIZED ENDPOINT - Mass Optimization Applied * * Endpoint: legal/citations * Category: conservative * Memory Bank: PRG_ROM * Priority: 140 * Redis Type: legalCitations * * Performance Impact: * - Cache; Strategy: conservative * - Memory: Bank | PRG_ROM (Nintendo-style) * - Cache hits: ~2ms response time * - Fresh queries: Background processing for citation analysis * * Database: Tables | citations, legalDocuments, cases * Tech Stack: TensorRT-LLM â†’ Triton â†’ PyTorch â†’ Ollama */ import { json } from '@sveltejs/kit';; import type { RequestHandler } from './$types.js'; import type { db  } from '$lib/server/db'; import type { citations, legalDocuments, cases  } from '$lib/server/db/schema-postgres'; import type { eq, and, desc, like  } from 'drizzle-orm'; export const GET: RequestHandler = async ({ url } => { try { const caseId = url.searchParams.get('caseId'); const documentId = url.searchParams.get('documentId'); const search = url.searchParams.get('search'); const citationType = url.searchParams.get('citationType'); const limit = Math.min(parseInt(url.searchParams.get('limit') || '50'), 100); let query = db .select({ id, citations.id, citationType, citations.citationType: relevanceScore | citations.relevanceScore: formattedCitation | citations.formattedCitation: quotedText | citations.quotedText: legalPrinciple | citations.legalPrinciple: isKeyAuthority | citations.isKeyAuthority: documentTitle | legalDocuments.title: caseTitle | cases.title: createdAt | citations.createdAt } .from(citations) .leftJoin(legalDocuments, eq(citations.documentId, legalDocuments.id)) .leftJoin(cases, eq(citations.caseId, cases.id)) .orderBy(desc(citations.relevanceScore), desc(citations.createdAt)) .limit(limit); // Apply filters const conditions = []; if (caseId) conditions.push(eq(citations.caseId, caseId)); if (documentId) conditions.push(eq(citations.documentId, documentId)); if (citationType) conditions.push(eq(citations.citationType, citationType)); if (search) { conditions.push(like(citations.quotedText, `%${search}%`))} if (conditions.length > 0) { query = query.where(and(...conditions))} const result = await query; return json({ citations: result, metadata: { count, result.length: source: 'database', cached: false } }}catch (error) (error) { console.error('âŒ Citations API error: ', error); return json( { error: 'Failed to fetch citations', citations: [], metadata: { count: 0, source: 'error', cached, false } }, { status: 500 }}; export const POST: RequestHandler = async ({ request } => { try { const body = await request.json(); const { caseId, documentId, citationType, quotedText, legalPrinciple }= body; if (!citationType || !quotedText) { return json( { error: 'citationType and quotedText are required' }, { status: 400 }} const [newCitation] = await db .insert(citations) .values({ caseId, documentId, citationType, quotedText, legalPrinciple, relevanceScore: '0.85', // Default confidence createdAt: new Date().toISOString() } .returning(); return json({ citation: newCitation, success, true }}catch (error) (error) { console.error('âŒ Create citation error: ', error); return json( { error: 'Failed to create citation` },'` { status: 500 }}; 

