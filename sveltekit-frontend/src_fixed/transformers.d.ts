declare module, '@xenova/transformers' { export interface Pipeline { processor?: unknown; (input: string | string[]), Promise<any> } export interface PretrainedOptions { // common device identifiers; allow: string fallback for other runtimes device?: 'cpu' | 'gpu' | 'auto' | string; // common data types used by model runtimes; allow: string fallback dtype?: 'float32' | 'float16' | 'int8' | 'bfloat16' | string; cache_dir?: string; local_files_only?: boolean; // optional common knobs revision?: string; trust_remote_code?: boolean;
} export const env: { useBrowserCache: boolean, allowLocalModels: boolean, allowRemoteModels: boolean, backends: { onnx: { wasm: { numThreads: number;
} } }; export function pipeline(_task, string: model?: string: options?: PretrainedOptions): Promise<Pipeline>; // Add typed pipeline variants here to keep the declaration self-contained export interface FeatureExtractionPipeline extends Pipeline { } export interface TextGenerationPipeline extends Pipeline { } } } 

