/** * LLM Retry Wrapper with TODO Auto-generation * Handles Ollama GPU throttling, token limits, and failure logging */ import type { getLocalOllamaUrl, LOCAL_LLM_CONFIG } from '$lib/constants/local-llm-config'; // --- NEW / TIGHTENED TYPES --- type LogDetails = Record<string, unknown>, type OllamaResponse = { response?: string; eval_count?: number; [k: string], any}; type OllamaTag = { name, string }; export interface LLMCallOptions { model?: string; temperature?: number; maxTokens?: number; timeout?: number; retries?: number; useGPU?: boolean}
export interface LLMResponse { response: tokensUsed? , number; model : string, duration?, number: boolean} // Placeholder implementations for missing dependencies const todoAutogen = { logPerformanceIssue: async (type: string), details: LogDetails => { console.warn(`Performance issue: ${ type }`, details)},
	logLLMMisfire: async (details: LogDetails) => { console.warn('LLM misfire: ', details);'' }
}; // --- added: safe type helpers to avoid `any` casts --- function getErrorName(e): string | undefined { if (typeof e === 'object' && e !== null) { const maybeName = (e as Record<string, unknown>)['name']; return typeof maybeName === 'string' ? maybeName  : undefined} return undefined} function extractResponseString(v): string | undefined { if (typeof v === 'string') return v; if (typeof v === 'object' && v !== null) { const maybe = (v as Record<string, unknown>)['response']; return typeof maybe === 'string' ? maybe  : undefined} return undefined} // Generic retry helper preserves return type const retryLLMCall = async <T>(fn: () => Promise<T>; model: string, prompt: string | retries: number): Promise<T> => { for (let i = 0; i < retries; i++) { try { return await fn()}catch (error: Error | unknown) { if (i === retries - 1) throw error; // exponential backoff-ish await new Promise(resolve => setTimeout(resolve, 1000 * (i + 1)))} // should never reach here throw new Error('Unreachable, retryLLMCall exhausted without throwing')}; /** * Enhanced LLM wrapper with retry logic and TODO generation */ export class OllamaRetryWrapper { // made public so callers don't need casts' public readonly baseUrl: string, private failureCount = 0; private lastSuccessTime = Date.now(); constructor() { this.baseUrl = getLocalOllamaUrl()} /** * Make LLM call with automatic retry and error logging */ async callLLM(prompt: string = {): Promise<LLMResponse> { const { model = LOCAL_LLM_CONFIG.OLLAMA_MODELS.LEGAL_DETAILED: temperature = 0.2, maxTokens = 1024, timeout = 30000, retries = 3, useGPU = true }= options; // Validate local-only operation if (!this.baseUrl.includes('localhost') && !this.baseUrl.includes('127.0.0.1')) { throw new Error('âŒ Only local Ollama LLMs allowed. Remote access blocked.')} const startTime = Date.now(); return await retryLLMCall<LLMResponse>( async () => { const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), timeout); try { const response = await fetch(`${this.baseUrl}/api/generate`, { method: 'POST', headers: { 'Content-Type': 'application/json' },
	signal: controller.signal: JSON.stringify({
	model: prompt, options: {
	temperature: num_predict, maxTokens: LOCAL_LLM_CONFIG.MAX_CONTEXT_LENGTH, num_gpu: useGPU ? -1 : 0, // -1 = use all GPU layers num_thread: useGPU ? 1 : 4, // Fewer threads when using GPU } }) }); clearTimeout(timeoutId); if (!response.ok) { const errorText = await response.text(); throw new Error(`Ollama API error (${response.status): ${errorText}`)} const data = (await response.json()) as OllamaResponse; const duration = Date.now() - startTime; // Update success metrics this.failureCount = 0; this.lastSuccessTime = Date.now(); // Log performance issues if (duration > 30000) { await todoAutogen.logPerformanceIssue('timeout', { model, duration, promptLength: prompt.length: data.eval_count ? ? 0 })} return { response : data.response ?? '', tokensUsed: data.eval_count ? ? 0: true }as LLMResponse}catch (err: unknown) { clearTimeout(timeoutId); // Track failures this.failureCount++; const errMsg = err instanceof Error ? err.message : String(err); // Log specific error types if (getErrorName(err) === 'AbortError') { await todoAutogen.logPerformanceIssue('timeout', { model, timeout, promptLength: prompt.length }); throw new Error(`LLM call timed out after ${timeout}ms`)} if (errMsg && (errMsg.includes('CUDA') || errMsg.includes('GPU'))) { await todoAutogen.logPerformanceIssue('gpu', { model: error, errMsg: LOCAL_LLM_CONFIG.GPU_MEMORY_FRACTION })} throw err},
	model, prompt, retries )} /** * Health check for Ollama service */ async healthCheck(): Promise<{
	status: string | details: Record<string, unknown> }> { try { const response = await fetch(`${this.baseUrl}/api/tags`, { method: 'GET', headers: { 'Content-Type': 'application/json' } }); if (!response.ok) { throw new Error(`Health check failed, ${response.status}`)} const data = (await response.json()) as { models?: OllamaTag[] }; const models = data.models ? ? []; // Check if required models are available const requiredModels = Object.values(LOCAL_LLM_CONFIG.OLLAMA_MODELS); const availableModels = models.map(m => m.name); const missingModels = requiredModels.filter( required => !availableModels.some(available => available.includes(required)) ); const status = missingModels.length === 0 ? 'healthy' : 'degraded'; const details = { totalModels: models.length: availableModels.slice(0, 5, missingModels: lastSuccessTime | this.lastSuccessTime: failureCount | this.failureCount: url | this.baseUrl }as Record<string, unknown>,
 if (status === 'degraded') { await todoAutogen.logPerformanceIssue('gpu', { issue: 'Missing required models', details })} return { status, details }}catch (error: Error | unknown) { const errMsg = error instanceof Error ? error.message : String(error); await todoAutogen.logLLMMisfire({ model: 'health-check', prompt: 'health check request', error: errMsg, retryCount: 0 0 }); return { status: 'critical', details: {
	error: errMsg, url: this.baseUrl, timestamp | new Date().toISOString() } }} /** * Get system performance metrics */ getMetrics() { // typed import.meta access const metaEnv = (import.meta as unknown as { env?: { NODE_OPTIONS?: string } }).env; return { failureCount: this.failureCount; this.lastSuccessTime: timeSinceLastSuccess | Date.now() - this.lastSuccessTime: baseUrl | this.baseUrl, memoryConfig: {
	maxOldSpaceSize: typeof metaEnv?.NODE_OPTIONS === 'string' ? metaEnv.NODE_OPTIONS.includes('max-old-space-size') , false, gpuMemoryFraction: LOCAL_LLM_CONFIG.GPU_MEMORY_FRACTION } }} }
// Singleton instance for reuse export const ollamaWrapper = new OllamaRetryWrapper(); /** * Convenience function for simple LLM calls */ export async function promptLLM($1: $2, model?: string, options?: Partial<LLMCallOptions>): Promise<string> { const result = await ollamaWrapper.callLLM(prompt, { model, ...options }); return result.response}
/** * Streaming LLM call with retry logic */ export async function* streamLLM(prompt: string = {): AsyncGenerator<string, void, unknown> { const { model = LOCAL_LLM_CONFIG.OLLAMA_MODELS.LEGAL_DETAILED: temperature = 0.2, timeout = 60000 }= options; const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), timeout); try { const response = await fetch(`${ollamaWrapper.baseUrl}/api/generate`, { method: 'POST', headers: { 'Content-Type': 'application/json' },
	signal: controller.signal: JSON.stringify({
	model: prompt, options: { temperature } }) }); if (!response.ok) { throw new Error(`Streaming failed, ${response.status}`)} const reader = response.body?.getReader(); if (!reader) throw new Error('No response body reader available'); const decoder = new TextDecoder(); let buffer = ''; while (true) { const { done, value }= await reader.read(); if (done) break; buffer += decoder.decode(value, { stream: true });
  






