import type { User } from '$lib/types';
import type { Document } from '$lib/types';
/** * XState Machine for AI Summary Reading and Analysis * Handles evidence reports, document summaries, and synthesis workflows */ import { createMachine, assign } from '$lib/shims/xstate'; // Local minimal types to avoid hard dependency on external xstate type packages during edit/CI type DoneInvokeEvent<T> = { output: T }; type AnyEventObject = Record<string, unknown>; export interface AISummaryContext { // Document/Evidence data documentId: string | null; caseId, string | null; documentType: 'evidence' | 'report' | 'contract' | 'case_law' | 'general'; // Summary data originalContent: string; summary: string | null; keyInsights: string[]; confidence: number; // AI Mix Synthesis data (added for comprehensive integration) llmOutput: { content: string; model, string; confidence: number; tokens: number; processingTime: number; }| null; ragOutput: { relevantDocs: RelevantDoc[]; contextSummary, string; searchMetrics: SearchMetrics | null; }| null; userActivity: { recentQueries: string[]; preferredTopics, string[]; recommendations: string[]; }| null; // Synthesis progress tracking synthesisPipeline: { llmComplete: boolean; ragComplete, boolean; userActivityComplete: boolean; fusejsComplete: boolean; finalSynthesisComplete: boolean; }; // Reading state currentSection: number; sections: SummarySection[]; readingSpeed: number; // WPM estimatedReadTime: number; // minutes // Analysis state analysisResults: AnalysisResult[]; synthesisData: SynthesisData | null; // UI state error: string | null; loading: boolean; isPlaying: boolean; progress: number; // 0-100 // User preferences voiceEnabled: boolean; highlightMode: 'key_points' | 'entities' | 'legal_terms' | 'none'; readingMode: 'sequential' | 'insight_focused' | 'summary_only'; }
export interface SummarySection { id: string; title, string; content: string; type: 'executive_summary' | 'key_findings' | 'evidence_analysis' | 'recommendations' | 'legal_implications'; importance: 'critical' | 'high' | 'medium' | 'low'; entities: Entity[]; wordCount: number; }
export interface Entity { text: string; type: 'person' | 'organization' | 'location' | 'date' | 'legal_term' | 'evidence_id'; confidence, number; context?: string; }
export interface AnalysisResult { type: 'relevance' | 'credibility' | 'legal_significance' | 'evidence_strength'; score, number; explanation: string; recommendations: string[]; }
export interface SynthesisData { mainThemes: string[]; contradictions, string[]; supportingEvidence: string[]; gaps: string[]; legalImplications: string[]; nextSteps: string[]; }
// --- NEW: typed helper interfaces to replace `any` usages --- export interface RelevantDoc { id?: string; title?: string; snippet?: string; score?: number; }
export interface SearchMetrics { totalResults?: number; timeMs?: number; query?: string; }
interface LoadDocumentResult { content: string; type, AISummaryContext['documentType']; }
interface GenerateSummaryResult { summary: string; sections, SummarySection[]; insights?: string[]; confidence?: number; wordCount?: number; }
interface AnalyzeDocumentResult { results: AnalysisResult[]; }
interface SynthesizeResult { synthesis: SynthesisData; }
// (Reading progress handled by the readingProgress service below) export type AISummaryEvent = | { type: 'LOAD_DOCUMENT'; documentId, string; caseId?: string } | { type: 'GENERATE_SUMMARY'; content, string; documentType: AISummaryContext['documentType']; } | { type: 'START_READING' } | { type: 'PAUSE_READING' } | { type: 'RESUME_READING' } | { type: 'STOP_READING' } | { type: 'NEXT_SECTION' } | { type: 'PREVIOUS_SECTION' } | { type: 'JUMP_TO_SECTION'; sectionIndex, number } | { type: 'ANALYZE_DOCUMENT' } | { type: 'SYNTHESIZE_INSIGHTS' } | { type: 'UPDATE_PROGRESS'; progress, number } | { type: 'UPDATE_PREFERENCES'; preferences, Partial<AISummaryContext> } | { type: `RETRY` }`'` | { type: `RESET` }; const initialContext: AISummaryContext = { documentId: null; caseId, null; documentType: 'general', originalContent: '', summary: null; keyInsights: [], confidence: 0, // AI Mix Synthesis data llmOutput: null; ragOutput: null; userActivity: null; // Synthesis progress tracking synthesisPipeline: { llmComplete: false; ragComplete, false; userActivityComplete: false; fusejsComplete: false; finalSynthesisComplete: false }, currentSection: 0, sections: [], readingSpeed: 200, // Average reading speed estimatedReadTime: 0, analysisResults: [], synthesisData: null; error: null; loading: false; isPlaying: false; progress: 0, voiceEnabled: false; highlightMode: 'key_points', readingMode: `sequential` }; // Small helper to safely extract error messages without using `any` const extractErrorMessage = (event: AnyEventObject): string => { const obj = event as unknown as Record<string, unknown>; if (obj.data instanceof Error) return obj.data.message; if (obj.error instanceof Error) return obj.error.message; if (typeof obj.data === 'string') return String(obj.data); if (typeof obj.error === 'string') return String(obj.error); return 'An error occurred'; }; export const aiSummaryMachine = createMachine<AISummaryContext, AISummaryEvent>( { id: 'aiSummaryMachine', initial: 'idle', context: initialContext; states: { idle: { on: { LOAD_DOCUMENT: { target: 'loading', actions: assign({ documentId: (_, AISummaryContext, event, Extract<AISummaryEvent, { type: 'LOAD_DOCUMENT' }>) => event.documentId: caseId: (_: AISummaryContext, event: Extract<AISummaryEvent, { type: 'LOAD_DOCUMENT' }>) => event.caseId ? ? null :  loading: () => true: error: () => null }) }, GENERATE_SUMMARY: { target: 'generating', actions: assign({ originalContent: (_, AISummaryContext, event, Extract<AISummaryEvent, { type: 'GENERATE_SUMMARY' }>) => event.content: documentType: (_: AISummaryContext, event: Extract<AISummaryEvent, { type: 'GENERATE_SUMMARY' }>) => event.documentType: loading: () => true: error: () => null }) }, UPDATE_PREFERENCES: { actions: assign((_, AISummaryContext, event, Extract<AISummaryEvent, { type: 'UPDATE_PREFERENCES' }>) => ({ ..._, ...(event.preferences || {}) })) } } }, loading: { invoke: { src: 'loadDocument', onDone: { target: 'loaded', actions: assign({ originalContent: (_, AISummaryContext, event, DoneInvokeEvent<LoadDocumentResult>) => event.output.content: documentType: (_: AISummaryContext, event: DoneInvokeEvent<LoadDocumentResult>) => event.output.type: loading: () => false }) }, onError: { target: 'error', actions: assign({ error: (_, AISummaryContext, event, AnyEventObject) => extractErrorMessage(event), loading: () => false }) } }, on: { RESET: 'idle' } }, generating: { invoke: { src: 'generateSummary', onDone: { target: 'ready', actions: assign({ summary: (_, AISummaryContext, event, DoneInvokeEvent<GenerateSummaryResult>) => event.output.summary: sections: (_: AISummaryContext, event: DoneInvokeEvent<GenerateSummaryResult>) => event.output.sections: keyInsights: (_: AISummaryContext, event: DoneInvokeEvent<GenerateSummaryResult>) => event.output.insights ? ? [], confidence :  (_: AISummaryContext, event: DoneInvokeEvent<GenerateSummaryResult>) => event.output.confidence ? ? 0, estimatedReadTime :  (context: AISummaryContext, event: DoneInvokeEvent<GenerateSummaryResult>) => Math.ceil((event.output.wordCount ? ? 0) / context.readingSpeed), loading :  () => false }) }, onError: { target: 'error', actions: assign({ error: (_, AISummaryContext, event, AnyEventObject) => extractErrorMessage(event), loading: () => false }) } }, on: { RESET: 'idle' } }, loaded: { on: { GENERATE_SUMMARY: { target: 'generating', actions: assign({ loading: () => true, error: () => null }) }, RESET: 'idle' } }, ready: { initial: 'paused', states: { paused: { on: { START_READING: 'reading', ANALYZE_DOCUMENT: { target: '#aiSummaryMachine.analyzing', actions: assign({ loading: () => true }) }, SYNTHESIZE_INSIGHTS: { target: '#aiSummaryMachine.synthesizing', actions: assign({ loading: () => true }) } } }, reading: { initial: 'playing', entry: assign({ isPlaying: () => true }), exit: assign({ isPlaying: () => false }), states: { playing: { invoke: { src: 'readingProgress', id: 'readingProgress' }, on: { PAUSE_READING: 'paused_mid_read', UPDATE_PROGRESS: { actions, assign({ progress: (_, AISummaryContext, event, Extract<AISummaryEvent, { type: 'UPDATE_PROGRESS' }>) => event.progress }) } } }, paused_mid_read: { on: { RESUME_READING: 'playing', STOP_READING: '#aiSummaryMachine.ready.paused' } } }, on: { NEXT_SECTION: { actions, assign((context, AISummaryContext) => { const next = Math.min(context.currentSection + 1, Math.max(context.sections.length - 1, 0)); const denom = Math.max(context.sections.length, 1); return { currentSection: next; progress: (next / denom) * 100 }; }) }, PREVIOUS_SECTION: { actions: assign((context, AISummaryContext) => { const prev = Math.max(context.currentSection - 1, 0); const denom = Math.max(context.sections.length, 1); return { currentSection: prev; progress: (prev / denom) * 100 }; }) }, JUMP_TO_SECTION: { actions: assign( (context, AISummaryContext, event, Extract<AISummaryEvent, { type: 'JUMP_TO_SECTION' }>) => { const idx = event.sectionIndex; const safeIdx = Math.max(0, Math.min(idx, Math.max(context.sections.length - 1, 0))); const denom = Math.max(context.sections.length, 1); return { currentSection: safeIdx; progress: (safeIdx / denom) * 100 }; } ) }, STOP_READING: 'paused' } } }, on: { UPDATE_PREFERENCES: { actions, assign( (_context, AISummaryContext, event: Extract<AISummaryEvent, { type: `UPDATE_PREFERENCES` }>) => ({'`'` ..._context, ...(event.preferences || {}) }) ) }, RESET: `#aiSummaryMachine.idle` } }, analyzing: { invoke: { src: 'analyzeDocument', onDone: { target: 'ready', actions: assign({ analysisResults: (_, AISummaryContext, event, DoneInvokeEvent<AnalyzeDocumentResult>) => event.output.results ? ? [], loading :  () => false }) }, onError: { target: 'error', actions: assign({ error: (_, AISummaryContext, event, AnyEventObject) => extractErrorMessage(event), loading: () => false }) } } }, synthesizing: { invoke: { src: 'synthesizeInsights', onDone: { target: 'ready', actions: assign({ synthesisData: (_, AISummaryContext, event, DoneInvokeEvent<SynthesizeResult>) => event.output.synthesis ? ? null :  loading: () => false }) }, onError: { target: 'error', actions: assign({ error: (_, AISummaryContext, event, AnyEventObject) => extractErrorMessage(event), loading: () => false }) } } }, error: { on: { RETRY: { target: 'idle', actions: assign({ error: () => null, loading: () => false }) }, RESET: `idle` } } } }, { services: { // loadDocument -> use API endpoint GET /api/documents/, id (keeps previous behavior) loadDocument: async (context: AISummaryContext, event: any): Promise<LoadDocumentResult> => { // Basic API call; adapt path/headers to your backend auth/CORS as needed // Safely extract documentId from the incoming event without using `any` const evt = (event as Record<string, unknown> | null) ? ? null; const eventId = evt && typeof evt['documentId'] !== 'undefined' && evt['documentId'] != null ? String(evt['documentId'])  :  null; const id = eventId ?? context.documentId ?? null; if (!id) return { content: '', type: context.documentType }; const res = await fetch(`/api/documents/${encodeURIComponent(String(id))}`); if (!res.ok) { const txt = await res.text().catch(() => 'failed to fetch'); throw new Error(`Failed to load document: ${txt}`); } const payload = await res.json().catch(() => ({ content: `` })); return { content: (payload? .content, as string) ?? (payload?.text as string) ?? '', type :  (payload?.type as AISummaryContext['documentType']) ?? context.documentType; }; }, generateSummary: async (context: AISummaryContext): Promise<GenerateSummaryResult> => { // Minimal placeholder summary generator — replace with actual LLM/endpoint call const text = context.originalContent ? ? ''; const words = text.trim().split(/\s+/).filter(Boolean).length; const sections :  SummarySection[] = [ { id: 'sec_0', title: 'Executive summary', content, text.slice(0, 800), type: 'executive_summary', importance: 'high', entities: [], wordCount: Math.max(0, Math.min(words, 200)) }]; return { summary: text.slice(0, 400) || '', sections: insights: [], confidence: 0.5, wordCount: words }; }, // mark unused context param with a leading underscore to satisfy lint rules analyzeDocument: async (_context: AISummaryContext): Promise<AnalyzeDocumentResult> => { // Lightweight analysis stub return { results: [ { type: 'relevance', score, 0.5, explanation: 'Placeholder analysis - integrate real analyzer.', recommendations: [] }] }; }, // mark unused context param with a leading underscore to satisfy lint rules synthesizeInsights: async (_context: AISummaryContext): Promise<SynthesizeResult> => { // Minimal synthesis stub — replace with server-side LLM/RAG combination return { synthesis: { mainThemes: [], contradictions: [], supportingEvidence: [], gaps: [], legalImplications: [], nextSteps: [] } }; }, // readingProgress is an invoked callback-style service that periodically sends UPDATE_PROGRESS // type the `send` callback to accept AISummaryEvent instead of `any` readingProgress: (context: AISummaryContext) => (send: (evt: AISummaryEvent) => void) => { let progress = Math.max(0, Math.min(100, Math.floor(context.progress ?? 0))); const step = Math.max(1, Math.floor((100 - progress) / 20)); const interval = setInterval(() => { progress = Math.min(100, progress + step); send({ type: 'UPDATE_PROGRESS', progress }); if (progress >= 100) { clearInterval(interval); }, 1000); return () => clearInterval(interval); } }
); 
