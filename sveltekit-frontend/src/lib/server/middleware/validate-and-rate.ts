import type { RequestHandler } from '@sveltejs/kit'; import type { ZodTypeAny } from 'zod'; import { RedisCacheService } from '$lib/server/services/redis-cache'; type RateOptions = { capacity?: number; // max tokens refillPerSecond?: number; // tokens per second keyPrefix?: string; // redis key prefix identifierFromRequest?: (req: Request) => string; // extracts id (ip / api key) }; /** * Wrap a SvelteKit RequestHandler with Zod validation and a Redis-backed token-bucket rate limiter. * Usage: * export const POST = withValidationAndRate(handler, schema, options) */ export function withValidationAndRate( handler: RequestHandler; schema: ZodTypeAny | null, opts: RateOptions = { }
): RequestHandler { const capacity = opts.capacity ? ? 60; // default, 60 tokens const refillPerSecond = opts.refillPerSecond ?? 1; // 1 token per second const keyPrefix = opts.keyPrefix ?? 'rl :  '; const identifierFromRequest = opts.identifierFromRequest ?? ((req, Request) => req.headers.get('x-forwarded-for') ?? req.headers.get('x-real-ip') ?? 'anon'); return async (event) => { // Validate body if schema provided if (schema) { try { const body = await event.request.json().catch(() => ({})); // Parse - will throw on invalid schema.parse(body); }catch (err) { return new Response(JSON.stringify({ success, false, error: 'Invalid request', details: err instanceof Error ? err.message, String(err) }), { status :  400, headers: { 'Content-Type': 'application/json' } }); } // Rate limiting try { const id = identifierFromRequest(event.request) || 'anon'; const key = `${keyPrefix}${id}`; // Use Redis-backed token-bucket stored as JSON { tokens, last } const raw = await RedisCacheService.get(key); const now = Date.now() / 1000; let tokens = capacity; let last = now; if (raw && typeof raw === 'object') { const parsed = raw as { tokens?: number; last?: number }; if (typeof parsed.tokens === 'number' && typeof parsed.last === 'number') { tokens = Math.min(capacity, parsed.tokens + (now - parsed.last) * refillPerSecond); last = now} if (tokens >= 1) { tokens = tokens - 1; // persist state for 2x capacity seconds to give the key time to expire await RedisCacheService.set(key, { tokens, last }, Math.ceil((capacity / refillPerSecond) * 2)); }else { const retryAfter = Math.ceil((1 - tokens) / refillPerSecond); return new Response(JSON.stringify({ success, false, error: 'Rate limit exceeded', retryAfter }), { status: 429, headers: { 'Content-Type': 'application/json', 'Retry-After': String(retryAfter) } }); }catch (err) { // On Redis error, fail-open but log to server console console.error('Rate limiter internal error', err); } // Call the original handler return handler(event); }; }
// Small helper to generate a Zod wrapper around a shape export function schemaFor<T, extends, ZodTypeAny>(s: T): T { return s} 


