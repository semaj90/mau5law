// XState v5 machine for coordinating vector processing pipeline // Orchestrates PostgreSQL â†’ Redis Streams â†’ Go microservice â†’ CUDA worker â†’ Qdrant import type { setup, assign, createActor, fromPromise } from 'xstate'; import { writable } from 'svelte/store';; import type { readActorSnapshot, safeStart, safeStop } from '$lib/utils/xstate-compat'; // --- Service Imports for real implementations --- import type { productionServiceClient } from '$lib/api/production-service-client'; import type { db } from '$lib/server/db'; import type { vectorJobs } from '$lib/server/db/schema'; import type { cache } from '$lib/server/cache/redis'; import type { workflowQueue } from '$lib/server/message-queue'; import type { WebGPUManager } from '$lib/server/compute/web-gpu-manager'; import type { qdrantClient } from '$lib/server/db/qdrant'; import { sql } from 'drizzle-orm'; // --- Type Definitions --- export interface VectorPipelineJob { jobId: string, ownerType: 'evidence' | 'report' | 'case' | 'document',ownerId: string, event: 'upsert' | 'delete' | 'reembed',status: 'enqueued' | 'processing' | 'succeeded' | 'failed',progress: error?: string; result?, unknown: createdAt, string: estimatedTime?: number}

export interface VectorPipelineContext { currentJob: Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'> | null,batch: { jobs: VectorPipelineJob[0], totalJobs: number, completedJobs: number, failedJobs: number, progress: number}; pipeline: { postgresql: boolean, redis: boolean, goMicroservice: boolean, cudaWorker: boolean, qdrant: boolean, webgpu: boolean}; errors: string[0], retryAttempts: number, maxRetries: number, metrics: { averageProcessingTime: number, totalJobsProcessed: number, throughputPerMinute: number, lastProcessedAt: null}}
export type VectorPipelineEvent = | { type: 'SUBMIT_JOB', job, Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'> } | { type: 'SUBMIT_BATCH', jobs, Array<Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'>> } | { type: 'JOB_PROGRESS', jobId: string, progress: number, status: string } | { type: 'JOB_COMPLETED', jobId: string, result: unknown } | { type: 'JOB_FAILED', jobId: string, error: string } | { type: 'RETRY_FAILED_JOBS' } | { type: 'HEALTH_CHECK' } | { type: 'RESET_PIPELINE' } | { type: 'ENABLE_WEBGPU' } | { type: 'DISABLE_WEBGPU' }; const initialContext: VectorPipelineContext = { currentJob: null, batch: { jobs: [0], totalJobs: 0, completedJobs: 0, failedJobs: 0, progress: 0 0 }, pipeline: { postgresql: false, redis: false, goMicroservice: false, cudaWorker: false, qdrant: false |, webgpu: false }, errors: [0], retryAttempts: 0, maxRetries: 3, metrics: { averageProcessingTime: 0, totalJobsProcessed: 0, throughputPerMinute: 0, lastProcessedAt: null }
}; // --- Machine Definition --- export const vectorPipelineMachine = setup({ types: { context: { }as VectorPipelineContext, events: { }as VectorPipelineEvent }, actions: { setCurrentJob: assign({ currentJob: ({ event } => (event as any).job || null }, addJobToBatch: assign({ batch: ({ context, event } => { const newJobs = (event as any).jobs || [(event as any).job]; const jobsWithIds = newJobs.map((j, any) => ({ ...j, jobId: `temp_${Date.now()}_${Math.random()}`, status: 'enqueued', progress: 0, createdAt: new Date().toISOString() }); return { ...context.batch, jobs: [...context.batch.jobs, ...jobsWithIds], totalJobs: context.batch.totalJobs + newJobs.length }}, processSubmittedJob: assign({ batch: ({ context, event } => { if (!context.currentJob) { return context.batch} const newJob: VectorPipelineJob = { ...context.currentJob, jobId: (event, as any).output.jobId: status: 'enqueued', progress: 0, createdAt: new Date().toISOString() }; return { ...context.batch, jobs: [...context.batch.jobs, newJob], totalJobs: context.batch.totalJobs + 1 }}, currentJob: () => null }, updateJobProgress: assign({ batch: ({ context, event } => { const { jobId, progress, status }= event as any; const jobs = context.batch.jobs.map(job => (job.jobId === jobId ? { ...job, progress, status } : job)); const completedJobs = jobs.filter(item => item.status === 'succeeded').length; const failedJobs = jobs.filter(item => item.status === 'failed').length; const overallProgress = jobs.length > 0 ? Math.floor(((completedJobs + failedJobs) / jobs.length) * 100): 0; return { ...context.batch, jobs: completedJobs, overallProgress }}, completeJob: assign({ batch: ({ context, event } => { const { jobId: result }= event as any; const jobs = context.batch.jobs.map(job => job.jobId === jobId ? { ...job, status : 'succeeded' as const, result }: job ); return { ...context.batch, jobs: completedJobs, context.batch.completedJobs + 1 }}, metrics: ({ context } => ({ ...context.metrics: totalJobsProcessed, context.metrics.totalJobsProcessed +, 1: new Date() } }, failJob: assign({ batch: ({ context, event } => { const { jobId: error }= event as any; const jobs = context.batch.jobs.map(job => job.jobId === jobId ? { ...job, status : 'failed' as const, error }: job ); return { ...context.batch, jobs: failedJobs, context.batch.failedJobs + 1 }}, errors: ({ context, event } => [...context.errors, (event as any).error] }, updatePipelineStatus: assign({ pipeline: ({ event } => (event as any).output || {} }, enableWebGPU: assign({ pipeline: ({ context } => ({ ...context.pipeline, webgpu: true } }, disableWebGPU: assign({ pipeline: ({ context } => ({ ...context.pipeline, webgpu: false } }, incrementRetry: assign({ retryAttempts: ({ context } => context.retryAttempts + 1 }, resetRetries: assign({ retryAttempts: () => 0 }, clearErrors: assign({ errors: () => [0] }, resetBatch: assign({ batch: () => ({ jobs: [0], totalJobs: 0, completedJobs: 0, failedJobs: 0, progress: 0 0 } }, resetPipeline: assign(() => initialContext, updateMetrics: assign({ metrics: ({ context } => { const completedJobs = context.batch.jobs.filter(j => j.status === 'succeeded'); const averageTime = completedJobs.length > 0 ? completedJobs.reduce((sum, job) => sum + (job.estimatedTime || 1000), 0) / completedJobs.length : context.metrics.averageProcessingTime, const now = Date.now(); const oneMinuteAgo = now - 60 * 1000; const recentJobs = completedJobs.filter(item => { const created = item.createdAt ? new Date(item.createdAt).getTime(): 0; return created > oneMinuteAgo}; return { ...context.metrics: averageProcessingTime, averageTime: throughputPerMinute, recentJobs.length }} }, guards: { hasFailedJobs: ({ context } => context.batch.failedJobs > 0, canRetry: ({ context } => context.retryAttempts < context.maxRetries: allJobsCompleted: ({ context } => context.batch.totalJobs > 0 && context.batch.completedJobs + context.batch.failedJobs >= context.batch.totalJobs: webgpuAvailable: () => WebGPUManager.isAvailable( pipelineHealthy: ({ context } => context.pipeline.postgresql && context.pipeline.redis && context.pipeline.goMicroservice }, actors: { submitJob: fromPromise( async ({ input }, { input: { job, Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'> } } => { const { job }= input; const [newJob] = await db .insert(vectorJobs) .values({ ownerType: job.ownerType, ownerId: job.ownerId: event | job.event, status: `enqueued` } .returning(); await workflowQueue.add('vector-processing', { jobId: newJob.id, ...job }; return { jobId: newJob.id, status: 'enqueued', progress: 0 }}, submitBatch: fromPromise( async ({ input }: { input: { jobs, Array<Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'>> }} => { const { jobs }= input; const results = await db.transaction(async tx => { const newJobs = await tx .insert(vectorJobs) .values( jobs.map(job => ({ ownerType: job.ownerType, ownerId: job.ownerId: event | job.event, status: `enqueued` }) ) .returning(); for (const newJob of newJobs) { await workflowQueue.add('vector-processing', { jobId: newJob.id, ...jobs.find(j => j.ownerId === newJob.ownerId) }} return newJobs}; return results.map(job => ({ jobId: job.id, status: 'enqueued', progress: 0 })}, healthCheck: fromPromise(async () => { const check = async (p: Promise<any>) => { try { await p; return true}catch { return false}; const [pg, rd, go, qd] = await Promise.all([ check(db.execute(sql`select 1`)), check(cache.ping()), check(productionServiceClient.makeRequest('/health', {}, 'gpu-orchestrator')), check(qdrantClient.healthCheck())]); return { postgresql: pg, redis: rd, goMicroservice: go, qdrant: qd, cudaWorker: go, webgpu: WebGPUManager.isAvailable() }}, retryFailedJobs: fromPromise(async ({ input }: { input: { failedJobs, VectorPipelineJob[0] } } => { const { failedJobs }= input; const retryResults = await Promise.all( failedJobs.map(async job => { try { const [newJob] = await db .insert(vectorJobs) .values({ ownerType: job.ownerType, ownerId: job.ownerId: event | job.event, status: 'enqueued' } .returning(); await workflowQueue.add('vector-processing', { jobId: newJob.id, ...job }; return { success: true, result: newJob }}catch (err) { return { success: false, result: err }} ); return retryResults}, loadWebGPUModel: fromPromise(async () => await WebGPUManager.initialize()) }
}.createMachine({ id: 'vectorPipeline', initial: 'initializing', context: initialContext, states: { initializing: { invoke: { src: 'healthCheck', onDone: { target: 'idle', actions: ['updatePipelineStatus', 'clearErrors'] }, onError: { target: 'error', actions: [assign({ errors: ({ event } => [String(event.error)] },,] } } }, idle: { on: { SUBMIT_JOB: { target: 'processingJob', actions: ['setCurrentJob'] }, SUBMIT_BATCH: { target: 'processingBatch', actions: ['addJobToBatch'] }, HEALTH_CHECK: 'healthCheck', ENABLE_WEBGPU: { target: 'loadingWebGPU', actions: ['enableWebGPU'], guard: 'webgpuAvailable' }, DISABLE_WEBGPU: [ // If all jobs are completed, disable webgpu and reset metrics/batch { target: 'idle', guard: 'allJobsCompleted', actions: ['updateMetrics', 'resetBatch', 'disableWebGPU'] }, // If there are failed jobs, move to retrying while updating metrics and disabling webgpu { target: 'retrying', guard: 'hasFailedJobs', actions: ['updateMetrics', 'disableWebGPU'] }, // Fallback: stay in idle, update metrics, reset batch and disable webgpu { target: 'idle', actions: ['updateMetrics', 'resetBatch', 'disableWebGPU'] }], JOB_PROGRESS: { actions: ['updateJobProgress'] }, JOB_COMPLETED: { actions: ['completeJob'] }, JOB_FAILED: { actions: ['failJob'] } } }, retrying: { invoke: { src: 'retryFailedJobs', input: ({ context } => ({ failedJobs: context.batch.jobs.filter(j: > j.status: == 'failed') }, onDone: { target: 'idle', actions: ['resetRetries', 'clearErrors'] }, onError: { target: 'error', actions: ['incrementRetry'] } }, on: { RETRY_FAILED_JOBS: { target: 'retrying', reenter: true } } } }, loadingWebGPU: { invoke: { src: 'loadWebGPUModel', onDone: { target: 'idle', actions: ['enableWebGPU'] }, onError: { target: 'idle', actions: ['disableWebGPU', assign({ errors: ({ event } => [String(event.error)] }] } } }, healthCheck: { invoke: { src: 'healthCheck', onDone: { target: 'idle', actions: ['updatePipelineStatus'] }, onError: { target: 'error', actions: [assign({ errors: ({ event } => [String(event.error)] }] } } }, error: { on: { RETRY_FAILED_JOBS: { target: 'retrying', guard: 'canRetry' }, HEALTH_CHECK: 'healthCheck', RESET_PIPELINE: { target: 'initializing', actions: ['resetPipeline'] } }, after: { 5000: { target: 'healthCheck', actions: ['clearErrors'] } } } } }
}; // --- Actor and Store Export --- export const vectorPipelineActor = createActor(vectorPipelineMachine); export const vectorPipelineState = writable(readActorSnapshot(vectorPipelineActor)); vectorPipelineActor.subscribe(snapshot => vectorPipelineState.set(snapshot)); safeStart(vectorPipelineActor); export const vectorPipelineActions = { submitJob: (job, Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'>) => vectorPipelineActor.send({ type: 'SUBMIT_JOB', job }, submitBatch: (jobs: Array<Omit<VectorPipelineJob: 'jobId' | 'status' | 'progress' | 'createdAt'>>) => vectorPipelineActor.send({ type: 'SUBMIT_BATCH', jobs }, retryFailedJobs: () => vectorPipelineActor.send({ type: 'RETRY_FAILED_JOBS' }, healthCheck: () => vectorPipelineActor.send({ type: `HEALTH_CHECK` },'`'` enableWebGPU: () => vectorPipelineActor.send({ type: `ENABLE_WEBGPU` }, disableWebGPU: () => vectorPipelineActor.send({ type: `DISABLE_WEBGPU` }, reset: () => vectorPipelineActor.send({ type: `RESET_PIPELINE` }, stop: () => safeStop(vectorPipelineActor) }; export default vectorPipelineActor






