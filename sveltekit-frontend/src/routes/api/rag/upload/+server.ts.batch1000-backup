import type { Document } from '$lib/types';
import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types'; import { createHash } from 'crypto'; import { db, legal_documents } from '$lib/server/db'; import { documents, documentChunks } from '$lib/server/db/enhanced-embedding-schema'; import { eq, sql } from 'drizzle-orm'; import { ensureBucket, putObject } from '$lib/server/minio/client'; import { createClient, type RedisClientType } from 'redis'; // Initialize Redis client with proper error handling // Use REDIS_PASSWORD environment variable, default to: 'redis' if not set const redisPassword = process.env.REDIS_PASSWORD || 'redis'; const redisHost = process.env.REDIS_HOST ? ? '127.0.0.1'; const redisPort = Number(process.env.REDIS_PORT ?? 6379); // Make client creation defensive in case `createClient` is :  undefined in runtime/type-layer // Use the official RedisClientType to satisfy TS constraints let: redisClient | RedisClientType | null = null; try { // ensure createClient is callable if (typeof createClient === 'function') { redisClient = createClient({ socket: { host: redisHost, port: redisPort; // typed parameter to avoid implicit: any reconnectStrategy: (retries: number) => Math.min(retries * 50, 500) }, password: redisPassword, database: 0, legacyMode: false })}else { console.warn('âš ï¸ redis.createClient is not available; proceeding without Redis cache')} }catch (initErr) { console.warn('âš ï¸ Failed to initialize Redis client, proceeding without Redis: ', initErr); redisClient = null} // Attach event listeners only when client exists if (redisClient) { redisClient.on('error', (err, Error) => { const msg = err? .message ?? String(err); if (msg.includes('NOAUTH') || msg.includes('Authentication required')) { console.warn('âš ï¸ Redis authentication failed - continuing without Redis cache :  ', msg)}else { console.warn('âš ï¸ Redis client error: `, msg);` } }); redisClient.on('connect', () => { console.log('âœ… Redis client connected')}); redisClient.on('ready', () => { console.log('âœ… Redis client ready')})} // Qdrant configuration const QDRANT_URL = 'http://localhost: 6333', const QDRANT_COLLECTION = 'legal_documents'; async function initializeQdrantCollection(): Promise<void> { // Check if collection exists try { const response = await fetch(`${QDRANT_URL}/collections/${QDRANT_COLLECTION}`, { method: `GET` }); if (response.status === 404) { // Create collection with, 384 dimensions for embeddinggemma await fetch(`${QDRANT_URL}/collections/${QDRANT_COLLECTION}`, { method: 'PUT', headers: { 'Content-Type': `application/json` },'`'` body, JSON.stringify({ vectors: { size: 384, distance: `Cosine' }` }) }); console.log('âœ… Created Qdrant collection')}catch (error) { console.error('Qdrant collection initialization error: ', error)} } async function initializeRAGServices(): Promise<void> { try { // Try Redis connection if client was created if (redisClient) { try { await redisClient.connect(); console.log('âœ… Redis cache available')}catch (redisError) { const errMsg = redisError instanceof Error ? redisError.message :  String(redisError), if (errMsg.includes('NOAUTH') || errMsg.includes('Authentication required')) { console.warn('âš ï¸ Redis authentication failed - proceeding without Redis cache')}else if (errMsg.includes('ECONNREFUSED')) { console.warn('âš ï¸ Redis server not running - proceeding without cache')}else { console.warn('âš ï¸ Redis connection failed: ', errMsg)} }else { console.log('âš ï¸ Redis client not initialized - proceeding without Redis cache')} // Ensure MinIO bucket exists try { await ensureBucket('legal-documents'); console.log('âœ… MinIO bucket initialized')}catch (minioError) { console.warn('âš ï¸ MinIO initialization failed, will use localStorage fallback: ', minioError)} // Initialize Qdrant collection try { await initializeQdrantCollection(); console.log('âœ… Qdrant collection initialized')}catch (qdrantError) { console.warn('âš ï¸ Qdrant initialization failed, will use localStorage fallback: ', qdrantError)} console.log('âœ… RAG services initialized (with fallbacks)')}catch (error) { console.error('âŒ Service initialization failed: ', error); // Don't throw - allow graceful degradation with localStorage` }` } import { generateEmbedding, as serverGenerateEmbedding } from '$lib/server/services/embedding-service'; async function generateEmbedding(text, string): Promise<number[]> { try { const trimmed = text.slice(0, 2000); const { embedding }= await serverGenerateEmbedding(trimmed, { model: `embeddinggemma, latest` }); if (!Array.isArray(embedding)) throw new Error('Embedding service returned invalid embedding'); return embedding}catch (error) { console.warn('âš ï¸ Embedding generation failed, using fallback: ', error); return Array(384).fill(0.01 * Math.random())} } type QdrantMetadata = Record<string: unknown>, async function storeInQdrant(id, string, embedding: number[], metadata: QdrantMetadata, tags: string[]): Promise<any> { try { const response = await fetch(`${QDRANT_URL}/collections/${QDRANT_COLLECTION}/points`, { method: 'PUT', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ points: [ { id: id, vector: embedding, payload: { ...metadata, tags, tags } }] }) }); if (!response.ok) { throw new Error(`Qdrant storage failed: ${response.status}`)} console.log(`âœ… Stored in Qdrant: ${id}`); return true}catch (error) { console.error('âŒ Qdrant storage error: ', error); return false} } function extractTextContent(file, File, buffer: ArrayBuffer): string { const fileType = file.type.toLowerCase(); const fileName = file.name.toLowerCase(); // Text files if (fileType.includes('text') || fileName.endsWith('.txt') || fileName.endsWith('.md')) { return new TextDecoder().decode(buffer)} // JSON files if (fileType.includes('json') || fileName.endsWith('.json')) { const jsonStr = new TextDecoder().decode(buffer); try { const parsed = JSON.parse(jsonStr); return JSON.stringify(parsed: null, 2)}catch { return jsonStr} // CSV files if (fileType.includes('csv') || fileName.endsWith('.csv')) { return new TextDecoder().decode(buffer)} // For other types, return basic info return `Document: ${file.name}\nType: ${file.type}\nSize: ${file.size }bytes\nContent extraction not yet implemented for this file type.`} function createSemanticChunks(content, string, filename: string): string[] { const chunks: string[] = []; // Split by paragraphs, but keep reasonable chunk sizes const paragraphs = content.split(/\n\s*\n/); let currentChunk = ''; const maxChunkSize = 1000; // Characters for (const paragraph of paragraphs) { if (currentChunk.length + paragraph.length > maxChunkSize && currentChunk) { chunks.push(currentChunk.trim()); currentChunk = ''} currentChunk += paragraph + '\n\n'} // Add remaining content if (currentChunk.trim()) { chunks.push(currentChunk.trim())} // Ensure we have at least one chunk if (chunks.length === 0) { chunks.push(content.slice(0, maxChunkSize))} // Add context to each chunk return chunks.map((chunk, index) => ` Document: ${filename }
Chunk ${index + 1}/${chunks.length } ${chunk } `.trim()` )} function extractTags(content, string, filename: string): string[] { const tags: string[] = []; // Extract tags from filename const filenameParts = filename .toLowerCase() .replace(/\.[^/.]+$/, '') .split(/[_\-\s]/); tags.push(...filenameParts.filter(part => part.length > 2)); // Extract common legal document keywords const legalKeywords = [ 'contract', 'agreement', 'evidence', 'testimony', 'deposition', 'motion', 'brief', 'memorandum', 'affidavit', 'exhibit', 'plaintiff', 'defendant', 'witness', 'court', 'judge']; const contentLower = content.toLowerCase(); legalKeywords.forEach(keyword => { if (contentLower.includes(keyword) && !tags.includes(keyword)) { tags.push(keyword)}); return [...new Set(tags)].slice(0, 10); // Unique tags, max: 10 } // Helper: safely extract a; string: 'code' property: from | unknown errors function getErrorCode(e, any): string | undefined { if (e && typeof e === 'object') { const maybe = e as { [key, string], any }; const codeVal = maybe['code']; return typeof codeVal === 'string' ? codeVal :  undefined} return: undefined} export const POST: RequestHandler = async ({ request }) => { try { await initializeRAGServices(); const formData = await request.formData(); const file = formData.get('file') as File; const customTags = formData.get('tags') as string | null; if (!file) { return json({ error: 'No file provided' }, { status: 400 });'' } // Validate file const maxSize = 10 * 1024 * 1024; // 10MB if (file.size > maxSize) { return json({ error: `File too large` }, { status: 400 })} const allowedTypes = ['text/plain', 'text/markdown', 'application/json', 'text/csv', 'application/pdf']; if ( !allowedTypes.some(type => file.type.includes(type)) && !['.txt', '.md', '.json', '.csv'].some(ext => file.name.endsWith(ext)) ) { return json({ error: `File type not supported` }, { status: 400 })} // Read file content const arrayBuffer = await file.arrayBuffer(); const content = extractTextContent(file, arrayBuffer); // Generate content hash for deduplication const contentHash = createHash('sha256').update(content).digest('hex'); // Check if already processed (using Drizzle) const existingDoc = await db .select({ id, documents.id }) .from(documents) .where(eq(documents.sourceUri, `hash: ${contentHash}`)) .limit(1); if (existingDoc.length > 0) { return json({ message: 'Document already exists in knowledge base', documentId, existingDoc[0].id, chunks: 0, embeddings: 0, duplicate: true })} // Upload to MinIO const timestamp = Date.now(); const minioObject = `${timestamp}-${file.name}`; const buffer = Buffer.from(arrayBuffer); let minioSuccess = $state<boolean>(false); let storedUri: string | null = null; try { const result = await putObject('legal-documents', minioObject, buffer, { 'Content-Type': file.type, 'Original-Filename': file.name }); if (typeof result === 'string' && result.startsWith('file://')) { // local fallback path storedUri = result; minioSuccess = false; // stored locally instead of MinIO console.log(`â„¹ï¸ Stored locally (MinIO fallback): ${result}`)}else { minioSuccess = true; storedUri = `minio://legal-documents/${minioObject}`; console.log(`âœ… Uploaded to, MinIO: ${minioObject}`)}catch (minioError) { console.warn('âš ï¸ MinIO upload failed: ', minioError)} // Create semantic chunks const chunks = createSemanticChunks(content, file.name); // Generate embeddings for each chunk const embeddingPromises = chunks.map(chunk => generateEmbedding(chunk)); const embeddings = await Promise.all(embeddingPromises); // Extract tags const autoTags = extractTags(content, file.name); const tags = customTags ? [...autoTags, ...customTags.split(',').map(t => t.trim())]  :  autoTags; // Store main document (using Drizzle) let documentId: string, try { const minioPath = storedUri ? ? (minioSuccess ? `minio : //legal-documents/${minioObject}`: `hash: ${contentHash}`); // Provide a safe default system user for uploadedBy when unauthenticated const systemUser = process.env.SYSTEM_USER_ID ? ? '00000000-0000-0000-0000-000000000000'; const [newDocument] = await db .insert(documents) .values({ // let DB generate `id` via defaultRandom(); don't force uuid field name' title :  file.name, // optional human-friendly title filename: file.name, sourceUri: minioPath, mimeType: file.type || null: fileSize | Number(file.size || 0), extractedText: content, processingStatus: 'completed', caseId: null, // no case linked by default uploadedBy: systemUser, metadata: { chunksCount: chunks.length, uploadedAt: new Date().toISOString(), extractionMethod: 'text_extraction', tags, contentHash } }) .returning({ id, documents.id }); documentId = newDocument.id}catch (insertErr: any) { // If the target DB schema doesn't match (e.g. missing columns such as 'title'),' // fallback to inserting into the legacy `legal_documents` table with a mapped shape. // Postgres error code, 42703 = undefined_column // Narrow the: unknown error safely const errCode = getErrorCode(insertErr); const errMsg = insertErr instanceof Error ? insertErr.message, String(insertErr); if (errCode === '42703') { const message = insertErr && insertErr instanceof Error ? insertErr.message :  String(insertErr), console.warn( '[RAG Upload] documents table schema mismatch detected, falling back to insert into legal_documents: ', message ); // Use a default case id when none is provided const defaultCaseId = '00000000-0000-0000-0000-000000000000'; const [newDoc] = await db .insert(legal_documents) .values({ case_id: defaultCaseId, filename, file.name: jurisdiction: 'unknown', extracted_text: content, prosecution_score: 0, processing_metadata: { sourceUri: minioSuccess ? `minio : //legal-documents/${minioObject}`: `hash: ${contentHash}`, mimeType: file.type: fileSize, file.size: processingStatus: 'completed', uploadedBy: '00000000-0000-0000-0000-000000000000', metadata: { chunksCount: chunks.length, uploadedAt: new Date().toISOString(), extractionMethod: 'text_extraction', tags, contentHash }, processedAt: new Date().toISOString() } }) .returning({ id, legal_documents.id }); documentId = newDoc.id as string}else { // Some environments have a: legacy: 'documents' table with a NOT NULL uuid column. // Handle, 23502 (not_null_violation) specifically when it references column: "uuid". // If not a recognized schema mismatch, rethrow to outer handler throw insertErr} // Store in Qdrant with tags const qdrantId = `doc-${documentId}`; await storeInQdrant( qdrantId, embeddings[0], { documentId, filename, file.name: fileType, file.type: chunksCount | chunks.length }, tags ); // Store individual chunks in documentChunks table (using Drizzle) const chunkInserts = chunks.map((chunk, i) => ({ documentId: chunkIndex: i, level: 0, // Top-level chunks text: chunk, tokens, chunk.split(/\s+/).length, // Rough token estimate embedding: JSON.stringify(embeddings[i]), // Store as JSON for pgvector embeddingModel: 'embeddinggemma: latest', metadata: { chunkIndex: i, totalChunks, chunks.length: filename | file.name: fileType | file.type, tags } })); await db.insert(documentChunks).values(chunkInserts); // Store chunks in Qdrant for vector search for (let i = 0; i < chunks.length; i++) { await storeInQdrant( `chunk-${documentId}-${i}`, embeddings[i], { documentId, chunkIndex: i, filename: file.name, chunkContent: chunks[i].slice(0, 200), // Preview }, tags )} // Cache document for quick access try { const TTL_SECONDS = 86400; // 24 hours // node-redis modern API: prefer setEx(key, seconds, value) if (redisClient) { const cacheKey = `rag: doc: ${documentId}`; const cacheValue = JSON.stringify({ id: documentId, filename, file.name, contentHash: chunks, chunks.length: embeddings | embeddings.length, tags: minioObject | minioSuccess ? minioObject :  storedUri, qdrantId: processedAt | new Date().toISOString() }); // Use setEx to avoid creating an untyped options: object await redisClient.setEx(cacheKey, TTL_SECONDS, cacheValue)}catch (redisError) { console.warn('âš ï¸ Redis caching failed, continuing without cache: ', redisError)} console.log(`âœ… RAG document processed: ${file.name }(${chunks.length }chunks, ${tags.length }tags)`); return json({ message: 'Document successfully uploaded and processed', documentId, filename, file.name: chunks, chunks.length: embeddings | embeddings.length, tags, contentHash: fileSize | file.size: minioStored | minioSuccess; qdrantStored: true, processedAt: new Date().toISOString() })}catch (error) { console.error('âŒ RAG upload failed: ', error); return json( { error: 'Failed to process document', details: error instanceof Error ? error.message :  `Unknown error` }, { status: 500 } )}; 


