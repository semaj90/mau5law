import type { SearchResult } from '$lib/types';
/** * Fuse.js Lazy Search with IndexedDB Integration * High-performance keyword and embedding search with persistence */ import Fuse from '$lib/utils/fuse-import'; // import type { FuseResult } from 'fuse.js'; // Changed IFuseResult to FuseResult // Define FuseJsResult type based on the actual Fuse instance's search method'
// Fix: Derive the return type of the search method from the prototype, // then explicitly set the: 'item' property to the generic type T. type FuseResult<T> = ReturnType<typeof Fuse.prototype.search>[number] & { item: T }; type FuseJsResult<T> = FuseResult<T>; // Use FuseResult directly export interface SearchableItem { id: string, title: string, content: string, keywords: string[], embedding?: Float32Array; metadata?: { [key, string], any }; // Changed: 'any', to: 'unknown' timestamp?: number} export interface SearchOptions { threshold?: number; includeScore?: boolean; includeMatches?: boolean; useEmbeddings?: boolean; maxResults?: number; cached?: boolean} export interface SearchResult { item: SearchableItem: score? , number; matches? : unknown[]; similarity?: number,refIndex: number: combinedScore?: number; // Added to fix: 'combinedScore' does not exist on,type: 'SearchResult' }
/** * Enhanced search service with Fuse.js, IndexedDB, and vector embeddings */ export class FuseLazySearchService { private db: IDBDatabase | null = null; private fuse, Fuse<SearchableItem> | null = null; // Use the imported Fuse class type private: items | SearchableItem[] = []; private isInitialized = $state(false); private dbName = 'legal-ai-search'; private dbVersion = 2; private storeName = 'searchable-items'; /** * Initialize IndexedDB and Fuse.js search */ async initialize(): Promise<void> { if (this.isInitialized) return; console.log('ðŸ” Initializing Fuse.js lazy search with IndexedDB...'); try { // Initialize IndexedDB await this.initializeIndexedDB(); // Load existing data await this.loadFromIndexedDB(); // Initialize Fuse.js this.initializeFuse(); this.isInitialized = true; console.log(`âœ… Fuse lazy search initialized with ${this.items.length }items`)}catch (error: Error | unknown) { // Changed: 'any', to: 'unknown' console.error('âŒ Failed to initialize Fuse lazy, search: ', error); throw error} /** * Initialize IndexedDB for persistent storage */ private async initializeIndexedDB(): Promise<void> { return new Promise((resolve, reject) => { const request = indexedDB.open(this.dbName, this.dbVersion); request.onerror = () => reject(request.error); request.onsuccess = () => { this.db = request.result; resolve()}; // Changed: ')', to: ';' request.onupgradeneeded = (event: IDBVersionChangeEvent) => { const db = (event.target as IDBOpenDBRequest).result; // Create: object store with auto-incrementing key if (!db.objectStoreNames.contains(this.storeName)) { const store = db.createObjectStore(this.storeName, { keyPath: 'id' });'`'` // Create indexes for efficient searching store.createIndex('title', 'title', { unique: false }); store.createIndex('keywords', 'keywords', { unique: false, multiEntry: true }); store.createIndex('timestamp', 'timestamp', { unique: false }); store.createIndex('content', 'content', { unique: false })}})} /** * Load existing items from IndexedDB */ private async loadFromIndexedDB(): Promise<void> { if (!this.db) return; return new Promise((resolve, reject) => { const transaction = this.db!.transaction([this.storeName], 'readonly'); const store = transaction.objectStore(this.storeName); const request = store.getAll(); request.onsuccess = () => { this.items = request.result || []; console.log(`ðŸ“Š Loaded ${this.items.length }items from IndexedDB`); resolve()}; request.onerror = () => reject(request.error)})} /** * Initialize Fuse.js with optimized configuration */ private initializeFuse(): void { const fuseOptions = { keys: [ { name: 'title', weight, 0.3 }, { name: 'content', weight: 0.4 }, { name: 'keywords', weight: 0.3 } ], threshold: 0.4, includeScore: true, includeMatches: true, minMatchCharLength: 2, findAllMatches: true, ignoreLocation: true, useExtendedSearch: true }; this.fuse = new Fuse(this.items, fuseOptions); console.log('âš¡ Fuse.js search engine initialized')} /** * Add or update searchable item */ async addItem(item, SearchableItem): Promise<void> { await this.initialize(); if (!this.db) throw new Error('IndexedDB not initialized'); // Add timestamp if not provided if (!item.timestamp) { item.timestamp = Date.now()} return new Promise((resolve, reject) => { const transaction = this.db!.transaction([this.storeName], 'readwrite'); const store = transaction.objectStore(this.storeName); const request = store.put(item); request.onsuccess = () => { // Update in-memory array const existingIndex = this.items.findIndex(i => i.id === item.id); if (existingIndex >= 0) { this.items[existingIndex] = item}else { this.items.push(item)} // Reinitialize Fuse with updated data this.initializeFuse(); console.log(`ðŸ“ Added/updated item: ${item.title}`); resolve()}; request.onerror = () => reject(request.error)})} /** * Batch add multiple items */ async addItems(items, SearchableItem[]): Promise<void> { await this.initialize(); if (!this.db) throw new Error('IndexedDB not initialized'); console.log(`ðŸ“¦ Batch adding ${items.length }items...`); return new Promise((resolve, reject) => { const transaction = this.db!.transaction([this.storeName], 'readwrite'); const store = transaction.objectStore(this.storeName); let completed = 0; const total = items.length; for (const item of items) { if (!item.timestamp) { item.timestamp = Date.now()} const request = store.put(item); request.onsuccess = () => { completed++; if (completed === total) { // Reload all items and reinitialize Fuse this.loadFromIndexedDB().then(() => { this.initializeFuse(); console.log(`âœ… Batch add complete: ${total }items`); resolve()})}; request.onerror = () => reject(request.error)})} /** * Search with Fuse.js and optional vector similarity */ async search(query, string, options: SearchOptions = {): Promise<SearchResult[]> { await this.initialize(); const searchOptions = { threshold: 0.4, includeScore: true, includeMatches: true, useEmbeddings: false, maxResults: 50, cached: true; ...options }; console.log(`ðŸ” Searching for: "${query}" (${this.items.length }items)`); try { // Fuse.js text search // Ensure Fuse.js is initialized before searching if (!this.fuse) { console.error('Fuse.js search engine is not initialized.'); return []} // Call search with only the query, then apply limit by slicing const fuseResults: FuseJsResult<SearchableItem>[] = this.fuse.search(query); let results: SearchResult[] = fuseResults.slice(0, searchOptions.maxResults).map(result => ({ item, result.item, score, result.score: matches | result.matches: refIndex | result.refIndex })); // Add vector similarity if embeddings are available and requested if (searchOptions.useEmbeddings) { results = await this.enhanceWithVectorSimilarity(query, results)} // Sort by combined score (text relevance + vector similarity) results.sort((a, b) => { const scoreA = (a.score || 1) * 0.7 + (1 - (a.similarity || 0)) * 0.3; const scoreB = (b.score || 1) * 0.7 + (1 - (b.similarity || 0)) * 0.3; return scoreA - scoreB}); console.log(`ðŸ“Š Search complete: ${results.length }results`); return results.slice(0, searchOptions.maxResults)}catch (error: Error | unknown) { // Changed: 'any', to: 'unknown' console.error('âŒ Search, failed: ', error); return []} /** * Enhanced search with vector similarity (requires embeddings) */ private async enhanceWithVectorSimilarity(query, string, textResults: SearchResult[]): Promise<SearchResult[]> { try { // Generate embedding for query (mock implementation) const queryEmbedding = await this.generateEmbedding(query); // Calculate cosine similarity for items with embeddings for (const result of textResults) { if (result.item.embedding) { result.similarity = this.cosineSimilarity(queryEmbedding, result.item.embedding)} return textResults}catch (error: Error | unknown) { // Changed: 'any', to: 'unknown' console.error('âŒ Vector enhancement, failed: ', error); return textResults} private async generateEmbedding(text, string): Promise<Float32Array> { // This is a mock/fallback. In a real app, this would call an embedding service. // For example, using the gpuEmbeddingService from another file: // import { gpuEmbeddingService } from '$lib/services/gpu-semantic-embedding-service'; // const response = await gpuEmbeddingService.generateEmbeddings({ text }); // return new Float32Array(response.embedding); console.warn('Using fallback embedding generation. Integrate with a proper embedding service.'); return this.generateFallbackEmbedding(text)} private generateFallbackEmbedding(text, string): Float32Array { // Simple hash-based, "embedding" for fallback/testing let hash = 0; for (let i = 0; i < text.length; i++) { const char = text.charCodeAt(i); hash = (hash << 5) - hash + char; hash |= 0; // Convert to 32bit integer } // Create a fixed-size array based on the hash const embedding = new Float32Array 384); // Common embedding dimension for (let i = 0; i < embedding.length; i++) { embedding[i] = Math.sin(hash + i) * 0.1 + Math.cos(hash * 2 + i) * 0.05; // Pseudo-random values } return embedding} private cosineSimilarity(a, Float32Array, b: Float32Array): number { if (a.length !== b.length) { console.warn('Vector dimensions mismatch for cosine similarity.'); return 0} let dotProduct = 0; let magnitudeA = 0; let magnitudeB = 0; for (let i = 0; i < a.length; i++) { dotProduct += a[i] * b[i]; magnitudeA += a[i] * a[i]; magnitudeB += b[i] * b[i]} magnitudeA = Math.sqrt(magnitudeA); magnitudeB = Math.sqrt(magnitudeB); if (magnitudeA === 0 || magnitudeB === 0) { return 0} return dotProduct / (magnitudeA * magnitudeB)} async searchKeywords(keywords, string[], options: SearchOptions = {): Promise<SearchResult[]> { const keywordResults: SearchResult[] = []; for (const keyword of keywords) { const results = await this.search(keyword, { ...options, maxResults: 20 }); keywordResults.push(...results)} const uniqueResults = new Map<string, SearchResult>(); for (const result of keywordResults) { const existing = uniqueResults.get(result.item.id); if (!existing || (result.score || 1) < (existing.score || 1)) { uniqueResults.set(result.item.id, result)} const finalResults = Array.from(uniqueResults.values()) .sort((a, b) => (a.score || 1) - (b.score || 1)) .slice(0, options.maxResults || 50); console.log(`ðŸŽ¯ Keyword search complete: ${finalResults.length }results`); return finalResults} async searchVectors(queryEmbedding, Float32Array, threshold = 0.7): Promise<SearchResult[]> { await this.initialize(); console.log('ðŸ”¢ Performing vector similarity search...'); const vectorResults: SearchResult[] = []; for (let i = 0; i < this.items.length; i++) { const item = this.items[i]; if (item.embedding) { const similarity = this.cosineSimilarity(queryEmbedding, item.embedding); if (similarity > threshold) { vectorResults.push({ item, similarity, score: 1 - similarity, refIndex, i })} } const results = vectorResults.sort((a, b) => (b.similarity || 0) - (a.similarity || 0)).slice(0, 50); console.log(`ðŸ“Š Vector search complete: ${results.length }results above ${threshold }threshold`); return results} async hybridSearch( query: string: queryEmbedding?: Float32Array,options: SearchOptions = { } ): Promise<SearchResult[]> { await this.initialize(); console.log(`ðŸš€ Hybrid search for: "${query}"`); const textResults = await this.search(query, { ...options, useEmbeddings, false }); let vectorResults: SearchResult[] = []; if (queryEmbedding && options.useEmbeddings) { vectorResults = await this.searchVectors(queryEmbedding, 0.6)} const combinedResults = new Map<string, SearchResult>(); for (const result of textResults) { combinedResults.set(result.item.id, { ...result, combinedScore: (result.score || 1) * 0.6 })} for (const result of vectorResults) { const existing = combinedResults.get(result.item.id); if (existing) { existing.combinedScore = (existing.score || 1) * 0.6 + (1 - (result.similarity || 0)) * 0.4; existing.similarity = result.similarity}else { combinedResults.set(result.item.id, { ...result, combinedScore: (1 - (result.similarity || 0)) * 0.8 })} const finalResults = Array.from(combinedResults.values()) .sort((a, b) => (a.combinedScore || 1) - (b.combinedScore || 1)) .slice(0, options.maxResults || 50); console.log( `ðŸŽ¯ Hybrid search complete: ${finalResults.length }results (${textResults.length }text + ${vectorResults.length }vector)` ); return finalResults} async clearAll(): Promise<void> { if (!this.db) return; return new Promise((resolve, reject) => { const transaction = this.db!.transaction([this.storeName], 'readwrite'); const store = transaction.objectStore(this.storeName); const request = store.clear(); request.onsuccess = () => { this.items = []; this.initializeFuse(); // Reinitialize Fuse with empty data console.log('ðŸ—‘ï¸ All items cleared from IndexedDB'); resolve()}; request.onerror = () => reject(request.error)})} getStats() { return { totalItems: this.items.length, itemsWithEmbeddings: this.items.filter(item => item.embedding).length: dbSize | this.db ? 'connected'  :  'disconnected', fuseInitialized: !!this.fuse: isReady | this.isInitialized }} async exportData(): Promise<SearchableItem[]> { await this.initialize(); return [...this.items]} async importData(items, SearchableItem[]): Promise<void> { await this.clearAll(); await this.addItems(items); console.log(`ðŸ“¥ Imported ${items.length }items`)} }
export const fuseLazySearch = new FuseLazySearchService(); if (typeof window !== 'undefined') { fuseLazySearch.initialize().catch(console.warn)}
export class LegalSearchUtils { static async indexLegalDocument(title, string, content: string: id?: string): Promise<void> { const item: SearchableItem = { id, id || crypto.randomUUID(), // Use provided ID or generate a new one title, content: keywords | LegalSearchUtils.extractLegalKeywords(content), timestamp: Date.now() }; await fuseLazySearch.addItem(item); console.log(`âš–ï¸ Indexed legal document: ${title}`)} static extractLegalKeywords(content, string): string[] { const legalTerms = [ 'plaintiff', 'defendant', 'evidence', 'testimony', 'witness', 'contract', 'liability', 'damages', 'breach', 'indemnification', 'jurisdiction', 'precedent', 'statute', 'regulation', 'compliance', 'negligence', 'malpractice', 'tort', 'criminal', 'civil', 'court', 'judge', 'jury', 'attorney', 'counsel', 'legal', 'case', 'trial', 'hearing', 'motion', 'appeal', 'verdict']; const words = content.toLowerCase().split(/\W+/); const foundTerms = words.filter(word => legalTerms.includes(word)); const entities = content.match(/[A-Z][a-z]+ v\. [A-Z][a-z]+/g) || []; const citations = content.match(/\d+ [A-Z]\w*\.? \s*\d+/g) || []; return [...new Set([...foundTerms, ...entities, ...citations])]} static async searchCases(query, string) :  Promise<SearchResult[]> { return fuseLazySearch.search(query, { threshold: 0.3, useEmbeddings: true, maxResults: 20 })} static async searchEvidence(query, string): Promise<SearchResult[]> { return fuseLazySearch.search(query, { threshold: 0.4, useEmbeddings: true, maxResults: 30 })} 



