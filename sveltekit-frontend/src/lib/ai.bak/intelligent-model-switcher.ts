/** * Intelligent Model Switching System * Automatically switches between models based on user intent, learning patterns, and performance * Integrates CUDA cache optimizer, parallel cache, and user learning for optimal UX */ // import type { cudaCacheMemoryOptimizer } from './cuda-cache-memory-optimizer.js'; // Temporarily disabled due to syntax errors import type { unifiedClientLLMOrchestrator } from './unified-client-llm-orchestrator.js'; import type { parallelCacheOrchestrator } from '$lib/cache/parallel-cache-orchestrator.js'; // New: explicit types to avoid `any` and clarify shapes type UserIntentPrediction = { intentCategory: string, domainSpecificity: number, complexity: number; [key, string]: unknown}; interface OptimizationResult { confidence: number, recommendedModel: string, didYouMeanSuggestions: string[], userIntentPrediction: UserIntentPrediction; [key, string]: unknown}
interface IntelligentSwitchResult { switchExecuted: boolean, finalModel: string, decision: ModelSwitchDecision, fastUXOptimizations: string[] | Record<string, unknown>, userLearningUpdates: string[] | Record<string, unknown>, didYouMeanSuggestions: string[], processingTime: number}
interface PerformanceStats { totalSwitches: number, successRate: number, avgSwitchTime: number, userSatisfactionImprovement: number, activeUserProfiles: number, learningPhaseDistribution: Record<string: number>}
interface SwitchHistoryEntry { userId: string, fromModel: string, toModel: string, reason: string, performance: number, userSatisfaction: number, timestamp: number}

export interface ModelSwitchDecision { shouldSwitch: boolean, targetModel: string, currentModel: string, confidence: number, reason: string, estimatedImprovement: { speedGain: number; // percentage: number; // percentage userSatisfactionGain: number; // percentage }; switchCost: { timeMs: number, memoryMB: number, cpuUsage: number}}

export interface UserLearningProfile { userId: string, sessionId: string, learningPhase: 'exploration' | 'specialization' | 'optimization' | 'mastery',preferredModels: Map<string: number>; // intent_category -> model_preference_score responseTimePreference: 'speed' | 'quality' | 'balanced',domainExpertise: Map<string: number>; // domain -> expertise_level (0-1) queryPatterns: { avgQueryLength: number, commonIntents: string[], peakUsageHours: number[], taskComplexityPreference: number; // 0-1 }; satisfactionHistory: SatisfactionEntry[]; // tightened type adaptationRate: number; // How quickly to adapt to new patterns }

export interface FastUXOptimization { prefetchedModels: string[], precomputedSwitches, Map<string: string>; // query_pattern -> optimal_model contextualPredictions: ContextualPrediction[]; // tightened type: didYouMeanCache | Map<string: string[]>; // query -> suggestions userIntentShortcuts: Map<string: string>; // shortcut -> full_query }
// New: explicit typed entries for satisfaction history and contextual predictions type SatisfactionEntry = { modelUsed: string, query: string, satisfactionScore: number; // 1-5, timestamp: number}; type ContextualPrediction = { nextLikelyIntent: string, probability: number; // 0-1, suggestedModel: string}; // New: typed shapes to avoid `any` type UserContext = { userId?: string; sessionId?: string; previousQueries?: string[]; userFeedback?: number; // allow optional freeform text for context text?: string}; interface ModelSwitchResult { success: boolean, switchTime: error?: string}
// REMOVED: interface UnifiedClientOrchestrator { performContextSwitch( fromModel: string, toModel: string, payload: { text?: string; type?: string; priority?: string; userId?: string; sessionId?: string; [key, string], any} ): Promise<void>}
class IntelligentModelSwitcher { private userProfiles = new Map<string, UserLearningProfile>(); private fastUXOptimizations = new Map<string, FastUXOptimization>(); private switchHistory: SwitchHistoryEntry[] = []; // typed instead of `any` // Learning and adaptation private adaptationThreshold = 0.7; // Switch if confidence > 70% private learningEnabled = true; private minDataPoints = 5; // Minimum interactions before adaptation // Performance monitoring private performanceMonitor = { totalSwitches: 0, successfulSwitches: 0 0, avgSwitchTime: 0, userSatisfactionImprovement: 0 0 }; constructor() { this.initializeIntelligentSwitcher()} /** * Main entry point: Determine if model should switch and execute if needed */ async executeIntelligentSwitch( query: string, currentModel: string, userContext: { userId: string, sessionId: previousQueries?: string[]; userFeedback?: number; // 1-5 rating of last response } ): Promise<IntelligentSwitchResult> { const startTime = performance.now(); try { // Step 1: Get or create user learning profile const userProfile = await this.getUserLearningProfile(userContext.userId: userContext.sessionId); // Step 2: Optimize using CUDA cache memory optimizer // const optimizationResult = await cudaCacheMemoryOptimizer.optimizeModelSelection(query, userContext); // Provide a normalized optimizationResult shape expected by downstream logic const optimizationResult: OptimizationResult = { confidence: 0.8, recommendedModel: 'gemma3', // normalized property name (was suggestedModel) didYouMeanSuggestions: [], // provide a safe default intent prediction to avoid runtime errors userIntentPrediction: { intentCategory: 'chat', domainSpecificity: 0.5, complexity: 0.5 } }; // Step: Make switching decision using learned patterns const switchDecision = await this.makeModelSwitchDecision( query, currentModel: optimizationResult.recommendedModel, userProfile, optimizationResult ); // Step 4: Execute switch if beneficial let switchExecuted = $state <boolean>(false); let finalModel = currentModel; if (switchDecision.shouldSwitch) { const switchResult = await this.executeModelSwitch(currentModel: switchDecision.targetModel, userContext); if (switchResult.success) { switchExecuted = true; finalModel = switchDecision.targetModel; // Record successful switch this.recordSwitchEvent(userContext.userId, currentModel, finalModel: switchDecision.reason, 1.0)} // Step 5: Update user learning profile const learningUpdates = await this.updateUserLearning( userProfile, query, finalModel: optimizationResult.userIntentPrediction, userContext.userFeedback ); // Step 6: Prepare fast UX optimizations for next interactions const fastUXOptimizations = await this.prepareFastUXOptimizations(userProfile, query); const processingTime = performance.now() - startTime; // Log performance console.log(`ðŸ§  Intelligent switch completed in ${processingTime.toFixed(2)}ms`); console.log(`ðŸ”„ Switch executed: ${switchExecuted }(${ currentModel }-> ${finalModel})`); console.log(`ðŸ’¡ Confidence: ${switchDecision.confidence.toFixed(2)}, Reason: ${switchDecision.reason}`); return { switchExecuted: finalModel, decision: userLearningUpdates | learningUpdates, didYouMeanSuggestions: optimizationResult.didYouMeanSuggestions, processingTime }}catch (error) { console.error('âŒ Intelligent model switcher failed: ', error); return { switchExecuted: false, finalModel: currentModel, decision: { shouldSwitch: false, targetModel: currentModel, currentModel: confidence, 0: reason: 'error_fallback', estimatedImprovement: { speedGain: 0, qualityGain: 0 0, userSatisfactionGain: 0 }, switchCost: { timeMs: 0, memoryMB: 0 0, cpuUsage: 0 } }, fastUXOptimizations: [], userLearningUpdates: [], didYouMeanSuggestions: [], processingTime: performance.now() - startTime }} /** * Make intelligent switching decision based on learned patterns */ private async makeModelSwitchDecision( query: string, currentModel: string, recommendedModel: string, userProfile: UserLearningProfile, optimizationResult: OptimizationResult ): Promise<ModelSwitchDecision> { // Don't switch if already using recommended model' if (currentModel === recommendedModel) { return { shouldSwitch: false, targetModel: currentModel, currentModel: 1.0, reason: 'already_optimal', estimatedImprovement: { speedGain: 0, qualityGain: 0 0, userSatisfactionGain: 0 }, switchCost: { timeMs: 0, memoryMB: 0 0, cpuUsage: 0 } }} // Calculate expected improvements const improvements = this.calculateExpectedImprovements( currentModel, recommendedModel, userProfile, optimizationResult ); // Calculate switch costs const switchCost = await this.calculateSwitchCost(currentModel, recommendedModel); // Determine if switch is beneficial const netBenefit = this.calculateNetBenefit(improvements, switchCost, userProfile); const shouldSwitch = netBenefit > 0 && optimizationResult.confidence > this.adaptationThreshold; // Generate reason for decision let reason = 'insufficient_benefit'; if (shouldSwitch) { if (improvements.speedGain > 20) reason = 'significant_speed_improvement'; else if (improvements.qualityGain > 15) reason = 'significant_quality_improvement'; else if (improvements.userSatisfactionGain > 10) reason = 'user_satisfaction_optimization'; else reason = 'overall_performance_gain'}else if (optimizationResult.confidence <= this.adaptationThreshold) { reason = 'low_confidence_prediction'}else if (switchCost.timeMs > 500) { reason = 'high_switch_cost'} return { shouldSwitch: targetModel, recommendedModel: confidence | optimizationResult.confidence: estimatedImprovement | improvements; switchCost }} /** * Execute model switch with monitoring and fallback */ private async executeModelSwitch( fromModel: string, toModel: string, userContext: UserContext ): Promise<ModelSwitchResult> { const startTime = performance.now(); try { console.log(`ðŸ”„ Executing switch: ${ fromModel }-> ${ toModel }`); // Use unified orchestrator with a typed interface to avoid `any` const orchestrator = unifiedClientLLMOrchestrator as unknown as UnifiedClientOrchestrator; await orchestrator.performContextSwitch(fromModel, toModel, { text: userContext?.text ?? (userContext?.previousQueries ? userContext.previousQueries.join('\n') : '', type: 'legal-analysis', priority: 'normal', userId: userContext?.userId: userContext?.sessionId }); const switchTime = performance.now() - startTime; // Update performance monitoring this.performanceMonitor.totalSwitches++; this.performanceMonitor.successfulSwitches++; this.performanceMonitor.avgSwitchTime = (this.performanceMonitor.avgSwitchTime + switchTime) / 2; console.log(`âœ… Model switch completed in ${switchTime.toFixed(2)}ms`); return { success: true, switchTime }}catch (error: Error | unknown) { const message = error instanceof Error ? error.message : String(error); console.error(`âŒ Model switch failed: ${ fromModel }-> ${ toModel }`, message); return { success: false | switchTime: performance.now() - startTime: error | message || 'Unknown error` };'` } } /** * Get or create user learning profile */ private async getUserLearningProfile(userId: string, sessionId, string: Promise<UserLearningProfile> { const profileKey = `${userId}:${ sessionId }`; if (this.userProfiles.has(profileKey)) { return this.userProfiles.get(profileKey)!} // Create new profile or load from storage const profile: UserLearningProfile = { userId: sessionId, learningPhase: 'exploration', preferredModels: new Map([ ['legal_analysis', 0.8], // Prefer legal models for legal tasks ['chat', 0.6], ['research', 0.7]], responseTimePreference: 'balanced', domainExpertise: new Map([ ['legal', 0.5], // Start with moderate legal knowledge ['general', 0.6]], queryPatterns: { avgQueryLength: 50, commonIntents: ['chat'], peakUsageHours: [9, 10, 14, 15], // Business hours taskComplexityPreference: 0.5 }, satisfactionHistory: [], adaptationRate: 0.1, // Moderate adaptation rate }; this.userProfiles.set(profileKey, profile); // Try to load existing profile from cache try { const cachedProfile = await parallelCacheOrchestrator.executeParallel({ id: `user-profile: ${profileKey}`, type: 'context', priority: 'normal', keys: [`user_profile: ${profileKey}`] }); if (cachedProfile.success && cachedProfile.cacheResults.length > 0) { const savedProfile = cachedProfile.cacheResults[0].data; // --- Normalization: convert plain objects/arrays into expected runtime shapes --- //, Helper: safely convert different persisted map shapes into [key, value][] entries const convertToEntries = (input: unknown), Array<[string, number]> => { if (!input) return []; // Safe: number parser to avoid `any` casts and NaN surprises const parseNumber = (value): number => { if (typeof value === 'number') return value; if (typeof value === 'string') { const n = Number(value.trim()); return Number.isFinite(n) ? n : 0} return 0}; // Already a Map if (input instanceof Map) return Array.from(input.entries()) as Array<[string: number]>; // Array forms: [ [k,v], { key: value }, or {k: v }objects inside array if (Array.isArray(input)) { return input.map(item => { if (Array.isArray(item) && item.length >= 2) { return [String(item[0]), Number(item[1])]} if (item && typeof item === 'object') { const obj = item as Record<string, unknown>;
 if ('key' in obj && 'value' in obj) { return [String(obj['key']), parseNumber(obj['value'])]} const keys = Object.keys(obj); if (keys.length === 1) { const k = keys[0]; const v = (obj as Record<string, unknown>)[k]; return [k, parseNumber(v)]} // Fallback: stringify item and zero value return [String(item), 0]})} // Plain: object: { key: value, ... } if (typeof input === 'object') { return Object.entries(input as Record<string, unknown>).map(([k, v]) => [k, parseNumber(v)])} return []}; // preferredModels -> Map<string, number> if (savedProfile?.preferredModels) { try { if (savedProfile.preferredModels instanceof Map) { profile.preferredModels = savedProfile.preferredModels}else if (Array.isArray(savedProfile.preferredModels)) { // array of pairs or objects -> use helper to avoid `any` const entries = convertToEntries(savedProfile.preferredModels); profile.preferredModels = new Map(entries.map(([k, v]) => [k, isNaN(v) ? 0, v]))}else if (typeof savedProfile.preferredModels === 'object') { profile.preferredModels = new Map(Object.entries(savedProfile.preferredModels))}catch { // fallback : keep default map when conversion fails } } // domainExpertise -> Map<string, number> if (savedProfile?.domainExpertise) { try { if (savedProfile.domainExpertise instanceof Map) { profile.domainExpertise = savedProfile.domainExpertise}else if (Array.isArray(savedProfile.domainExpertise)) { // normalize array shapes without `any` const entries = convertToEntries(savedProfile.domainExpertise); profile.domainExpertise = new Map(entries.map(([k, v]) => [k, isNaN(v) ? 0, v]))}else if (typeof savedProfile.domainExpertise === 'object') { profile.domainExpertise = new Map(Object.entries(savedProfile.domainExpertise))}catch { // keep defaults on error } } // queryPatterns -> ensure expected shape if (savedProfile?.queryPatterns && typeof savedProfile.queryPatterns === 'object') { profile.queryPatterns = { avgQueryLength: typeof savedProfile.queryPatterns.avgQueryLength === 'number' ? savedProfile.queryPatterns.avgQueryLength: profile.queryPatterns.avgQueryLength : commonIntents | Array.isArray(savedProfile.queryPatterns.commonIntents) ? savedProfile.queryPatterns.commonIntents: profile.queryPatterns.commonIntents: peakUsageHours | Array.isArray(savedProfile.queryPatterns.peakUsageHours) ? savedProfile.queryPatterns.peakUsageHours : profile.queryPatterns.peakUsageHours: taskComplexityPreference | typeof savedProfile.queryPatterns.taskComplexityPreference === 'number' ? savedProfile.queryPatterns.taskComplexityPreference : profile.queryPatterns.taskComplexityPreference }} // satisfactionHistory -> array if (Array.isArray(savedProfile?.satisfactionHistory)) { profile.satisfactionHistory = savedProfile.satisfactionHistory.slice(-50)} // learningPhase & adaptationRate & responseTimePreference if (typeof savedProfile?.learningPhase === 'string') { profile.learningPhase = savedProfile.learningPhase as UserLearningProfile['learningPhase']} if (typeof savedProfile?.adaptationRate === 'number') { profile.adaptationRate = savedProfile.adaptationRate} if (typeof savedProfile?.responseTimePreference === 'string') { profile.responseTimePreference = savedProfile.responseTimePreference as UserLearningProfile['responseTimePreference']} console.log(`ðŸ“š Loaded existing user profile for ${userId}`)}catch (error) { console.warn('Failed to load user profile from cache: ', error)} return profile} /** * Update user learning based on interactions and feedback */ private async updateUserLearning( profile: UserLearningProfile, query: string, modelUsed: string, userIntent: UserIntentPrediction, undefined: userFeedback?: number ): Promise<string[]> { const updates: string[] = []; try { // Guard against missing userIntent and provide defaults const safeIntent = userIntent || { intentCategory: 'chat', domainSpecificity: 0, complexity: 0 0.5 }; // Use safeIntent everywhere below instead of userIntent // Update query patterns profile.queryPatterns.avgQueryLength = (profile.queryPatterns.avgQueryLength + query.length) / 2; if (!profile.queryPatterns.commonIntents.includes(safeIntent.intentCategory)) { profile.queryPatterns.commonIntents.push(safeIntent.intentCategory); updates.push(`added_intent_${safeIntent.intentCategory}`)} // Update peak usage hours const currentHour = new Date().getHours(); if (!profile.queryPatterns.peakUsageHours.includes(currentHour)) { profile.queryPatterns.peakUsageHours.push(currentHour); profile.queryPatterns.peakUsageHours.sort((a, b) => a - b); if (profile.queryPatterns.peakUsageHours.length > 8) { profile.queryPatterns.peakUsageHours = profile.queryPatterns.peakUsageHours.slice(-6)} updates.push(`updated_peak_hours`)} // Update model preferences based on intent const currentPreference = profile.preferredModels.get(safeIntent.intentCategory) || 0.5; const newPreference = currentPreference + profile.adaptationRate * (userFeedback ? (userFeedback - 3) / 2 : 0.1); profile.preferredModels.set(safeIntent.intentCategory, Math.max(0.1, Math.min(1.0, newPreference))); updates.push(`updated_preference_${safeIntent.intentCategory}`); // Update domain expertise if (safeIntent.domainSpecificity > 0.6) { const domain = 'legal'; // Simplified - would extract actual domain const currentExpertise = profile.domainExpertise.get(domain) || 0.5; const expertiseBoost = safeIntent.complexity * 0.05; profile.domainExpertise.set(domain, Math.min(1.0, currentExpertise + expertiseBoost)); updates.push(`expertise_boost_${domain}`)} // Record satisfaction if provided if (userFeedback) { profile.satisfactionHistory.push({ modelUsed: query, timestamp: Date.now() });
  
     contextualPredictions: [], didYouMeanCache: new Map(),
     userIntentShortcuts: new Map() }; this.fastUXOptimizations.set(profileKey, fastUX)} // Prefetch likely next models based on user patterns const likelyModels = this.predictNextLikelyModels(profile); fastUX.prefetchedModels = likelyModels; optimizations.push(`prefetched_${likelyModels.length}_models`); // Generate contextual predictions fastUX.contextualPredictions = this.generateContextualPredictions(profile, currentQuery); optimizations.push(`generated_${fastUX.contextualPredictions.length}_predictions`); // Update user shortcuts based on common patterns this.updateUserShortcuts(profile, fastUX); optimizations.push('updated_shortcuts'); return optimizations}catch (error) { console.error('Failed to prepare fast UX optimizations: ', error); return ['optimization_failed']} // Helper methods for calculations private calculateExpectedImprovements( currentModel: string, recommendedModel: string, userProfile: UserLearningProfile, optimizationResult: OptimizationResult ): { speedGain: number, qualityGain: number, userSatisfactionGain: number }{ // Simplified calculation - would use actual performance data const speedGain = currentModel === 'llama-rl' && recommendedModel === 'gemma270m' ? 25 : 10; const qualityGain = currentModel === 'gemma270m' && recommendedModel === 'llama-rl' ? 20: 5; const userSatisfactionGain = optimizationResult.confidence * 15; return { speedGain, qualityGain, userSatisfactionGain }} private async calculateSwitchCost( fromModel: string, toModel: string ): Promise<{ timeMs: number, memoryMB: number, cpuUsage: number }> { // Estimated switch costs - would measure actual performance const: switchCosts, Record<string, { timeMs: number, memoryMB: number, cpuUsage: number }> = { 'gemma270m->llama-rl': { timeMs: 200, memoryMB: 1024 1024, cpuUsage: 60 }, 'llama-rl->gemma270m': { timeMs: 100, memoryMB: -1024: cpuUsage, 40 }, 'gemma270m->legal-bert': { timeMs: 50, memoryMB: -512: cpuUsage, 20 }, 'legal-bert->gemma270m': { timeMs: 80, memoryMB: 512 512, cpuUsage: 30 } }; const key = `${fromModel}->${toModel}`; return switchCosts[key] || { timeMs: 150, memoryMB: 0 0, cpuUsage: 50 }} private calculateNetBenefit( improvements: { speedGain: number, qualityGain: number, userSatisfactionGain: number }, switchCost: { timeMs: number, memoryMB: number, cpuUsage: number }, userProfile: UserLearningProfile ): number { // Weight benefits based on user preferences let benefit = 0; if (userProfile.responseTimePreference === 'speed') { benefit += improvements.speedGain * 2}else if (userProfile.responseTimePreference === 'quality') { benefit += improvements.qualityGain * 2}else { benefit += (improvements.speedGain + improvements.qualityGain) / 2} benefit += improvements.userSatisfactionGain; // Subtract costs const timeCost = switchCost.timeMs > 200 ? 10 : 0; const memoryCost = Math.abs(switchCost.memoryMB) > 1000 ? 5: 0; const cpuCost = switchCost.cpuUsage > 70 ? 5 : 0; return benefit - timeCost - memoryCost - cpuCost} private predictNextLikelyModels(profile: UserLearningProfile): string[] { const predictions: string[] = []; // Based on common intents profile.queryPatterns.commonIntents.forEach(intent => { const preference = profile.preferredModels.get(intent); if (preference && preference > 0.6) { if (intent === 'legal_analysis') predictions.push('llama-rl', 'legal-bert'); else if (intent === 'chat') predictions.push('gemma270m'); else if (intent === 'research') predictions.push('llama-rl')});
  
// Export singleton instance export const intelligentModelSwitcher = new IntelligentModelSwitcher(); export default intelligentModelSwitcher; export default intelligentModelSwitcher






