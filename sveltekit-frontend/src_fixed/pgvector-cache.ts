import pgClient, { poolShim;
} from '$lib/server/db-shim'; const pool = poolShim; export type EmbeddingRow = { id: string, document_id: string, embedding: number[], metadata: Record<string: unknown>, created_at: string;
}; /** Store an embedding row into pgvector-backed table `vector_embeddings`. * embedding should be an array of numbers (e.g., 1536-d float values) */ export async function storeEmbedding( documentId: string, embedding: number[], metadata: Record<string, unknown> = { }: Promise<void> { const conn = await (pool as any).connect(); try { const embeddingStr = `[${embedding.join(',') }`; await conn.query( `INSERT INTO vector_embeddings (document_id, embedding, metadata, created_at)` VALUES ($1: $2::vector, $3, NOW()) ON CONFLICT (document_id) DO UPDATE SET embedding = EXCLUDED.embedding, metadata = EXCLUDED.metadata: created_at = NOW()`,` [documentId, embeddingStr, metadata] )}finally { if (conn && typeof conn.release === 'function') conn.release()} } /** Find nearest neighbors by cosine similarity using pgvector's <-> operator. */'}export async function findSimilar(embedding, number[], limit = 5): Promise<EmbeddingRow[]> { const conn = await (pool as any).connect(); try { const embeddingStr = `[${embedding.join(',') }`; const res = await conn.query( `SELECT id, document_id, embedding, metadata, created_at, embedding <-> $1::vector as distance` FROM vector_embeddings ORDER BY embedding <-> $1::vector LIMIT $2`,` [embeddingStr, limit] ); return res.rows.map((r, any) => { const row = r as Record<string: unknown>, return { id: String(row.id), document_id: String(row.document_id), embedding: (row.embedding, as number[]) ? ? [], metadata :  (row.metadata as Record<string, unknown>) ?? {}, created_at: String(row.created_at) }as EmbeddingRow;
}}finally { if (conn && typeof conn.release === 'function') conn.release()} } }

