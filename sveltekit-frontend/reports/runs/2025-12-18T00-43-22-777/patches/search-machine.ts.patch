--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\machines\search-machine.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\machines\search-machine.ts (fixed)

@@ Line 2, Col 69 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type SearchContext =

@@ Line 2, Col 79 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: '(' expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type SearchContext =

@@ Line 2, Col 179 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: '>' expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type SearchContext =

@@ Line 2, Col 181 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Property or signature expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export ty

@@ Line 2, Col 188 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: An element access expression should take an argument. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "exten

@@ Line 2, Col 190 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expression expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type SearchCo

@@ Line 2, Col 197 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type SearchContext =

@@ Line 2, Col 212 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Declaration or statement expected. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export

@@ Line 2, Col 47 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'filters'. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type S

@@ Line 2, Col 63 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Type parameter name cannot be 'string'. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' e

@@ Line 2, Col 71 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Type parameter name cannot be 'unknown'. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' 

@@ Line 2, Col 137 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'Analytics'. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' export type

@@ Line 2, Col 166 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Generic type 'Record' requires 2 type argument(s). import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended

@@ Line 2, Col 181 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: 'string' only refers to a type, but is being used as a value here. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context

@@ Line 2, Col 199 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: 'string' only refers to a type, but is being used as a value here. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context

@@ Line 2, Col 208 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The value 'null' cannot be used here. import type { createMachine, assign, setup, fromPromise  } from 'xstate'; // removed unused sendParent // ============================================================================ // 1. TYPES - Define a single source of truth for all types // ============================================================================ type Analytics = { totalResults: number, searchTime: number, relevanceScore: number}; // The machine's context (its: "extended state")' exp

