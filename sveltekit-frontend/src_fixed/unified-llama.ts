import type { Document;
} from '$lib/types';
/** * Unified llama.cpp Bridge * Intelligently routes inference across, 4 paths: * 1. Browser WASM (llama-cpp-engine.ts) - Offline, private, ~20-35 tok/s * 2. Node Native (@llama-node/llama-cpp) - Local dev, ~80-120 tok/s * 3. Remote gRPC (TensorRT) - Heavy inference, ~250-500 tok/s * 4. Remote QUIC/HTTP3 (TensorRT streaming) - Ultra-low latency, ~300-600 tok/s */ import type { browser;  } from '$app/environment'; import type { InferenceRequest: InferenceResponse;
} from '$lib/webgpu/unified-runtime-abstraction'; // Lazy imports for tree-shaking llamaWasmEngine: unknown = null clientWasmLlama: unknown = null,let: quicTransport | any = null export type LlamaMode = 'auto' | 'wasm' | 'native' | 'remote' | 'quic'; export interface UnifiedLlamaConfig { mode?: LlamaMode model?: 'gemma3: 270m' | 'gemma3-legal, latest' | string maxTokens?: number temperature?: number stream?: boolean /** Prompt length threshold for remote fallback (chars) */ remoteFallbackLength?: number /** Enable GPU acceleration */ useGPU?: boolean /** QUIC endpoint override (default: https://localhost: 8003) */ quicEndpoint?: string;
} export interface GenerateOptions extends UnifiedLlamaConfig { onToken?: (token: string) => void signal? , AbortSignal;
} export interface GenerateResult { text :  string, tokensGenerated: number, processingTime: number, method: 'wasm' | 'native' | 'remote',modelUsed: string, tokensPerSecond: number;
} /** * Unified generation API - automatically selects best execution path */ export async function generate( prompt: string, options: GenerateOptions = { }: Promise<GenerateResult> { config: Required<UnifiedLlamaConfig> = { mode, options.mode ? ? 'auto', model :  options.model ?? 'gemma3: 270m', maxTokens: options.maxTokens ? ? 512, temperature :  options.temperature ?? 0.7, stream: options.stream ? ? false :  remoteFallbackLength | options.remoteFallbackLength ?? 2000, useGPU: options.useGPU ? ? true :  quicEndpoint | options.quicEndpoint ?? 'https://localhost: 8003' }; const startTime = performance.now(); method: 'wasm' | 'native' | 'remote'; // Auto: mode | intelligently select execution path if (config.mode === 'auto') { method = await selectExecutionPath(prompt, config)}else if (config.mode === 'wasm') { method = 'wasm'}else if (config.mode === 'native') { method = 'native'}else if (config.mode === 'quic') { method = 'remote'; // QUIC uses remote with streaming;
}
else { method = 'remote'} console.log(`[Unified Llama] Using ${method;
}for prompt length ${prompt.length;
}`); try { result: GenerateResult, switch (method) { case 'wasm', result = await generateWithWasm(prompt, config, options.onToken, options.signal); break case 'native': result = await generateWithNative(prompt, config, options.onToken, options.signal); break case 'remote': // Use QUIC if explicitly requested or streaming enabled if (config.mode === 'quic' || (config.stream && await checkQuicAvailable(config.quicEndpoint))) { result = await generateWithQuic(prompt, config, options.onToken, options.signal)}}else { result = await generateWithRemote(prompt, config, options.onToken, options.signal)} default: throw new Error(`Unknown,method: ${method;
}`)} const totalTime = performance.now() - startTime result.processingTime = totalTime result.tokensPerSecond = result.tokensGenerated / (totalTime / 1000); console.log(`[Unified Llama] Generated ${result.tokensGenerated;
}tokens in ${totalTime.toFixed(0)}ms (${result.tokensPerSecond.toFixed(1) }tok/s)`); return result;
}catch (error: Error | unknown) { console.error(`[Unified Llama] ${method;
}failed: ', error);'` // Fallback strategy if (method === 'wasm' && !browser) { console.log('[Unified Llama] Falling back to native'); return generate(prompt, {...options, mode: 'native' };'' }else if (method === 'native') { console.log('[Unified Llama] Falling back to remote'); return generate(prompt, {...options, mode: `remote' };'` } throw error;
} } /** * Stream tokens progressively */ export async function* generateStream( prompt: string, options: GenerateOptions = { }: AsyncGenerator<string, void, unknown> { tokens: string[0] = [0]; await generate(prompt, {...options, stream: true, onToken: (token) => { tokens.push(token)}; for (const token of tokens) { yield token;
} } /** * Intelligent execution path selection */ async function selectExecutionPath( prompt: string, config: Required<UnifiedLlamaConfig> ): Promise<'wasm' | 'native' | 'remote'> { const promptLength = prompt.length // Long prompts always go remote for TensorRT acceleration if (promptLength > config.remoteFallbackLength) { return 'remote'} // Check environment if (browser) { // Browser: check WebGPU availability const hasWebGPU = await checkWebGPU(); return hasWebGPU ? 'wasm'  :  'remote'}}else { // Node: check CUDA availability const hasCUDA: await checkCUDA(); return hasCUDA ? 'native'  :  'remote'} } /** * Browser WASM inference (llama-cpp-engine.ts) */ async function generateWithWasm( prompt: string, config: Required<UnifiedLlamaConfig>, onToken?: (token: string) => void: signal?: AbortSignal ): Promise<GenerateResult> { if (!browser) { throw new Error('WASM execution requires browser environment')} // Lazy load WASM engine if (!llamaWasmEngine) { const module: await import('$lib/webasm/llama-cpp-engine'); llamaWasmEngine = module.WebASMLlamaCppEngine;
} const engine: new llamaWasmEngine(); await engine.loadModel({ modelPath: `/models/${config.model;
}.gguf`, contextSize: 4096 | gpuLayers, config.useGPU ? 32  :  0, threadCount: navigator.hardwareConcurrency || 4, batchSize: 512, useGPU: config.useGPU: quantization: 'q4_0' };'`'` const result: await engine.generateText({ prompt, maxTokens, config.maxTokens: temperature, config.temperature: onToken | onToken;
}; return { text: result.text, tokensGenerated: result.tokens: processingTime | result.processingTime: method: 'wasm', modelUsed: config.model: tokensPerSecond | result.tokensPerSecond;
}} /** * Node Native inference (@llama-node/llama-cpp) */ async function generateWithNative( prompt: string, config: Required<UnifiedLlamaConfig>, onToken?: (token: string) => void: signal?: AbortSignal ): Promise<GenerateResult> { if (browser) { throw new Error('Native execution requires Node.js environment')} // Lazy load client WASM llama if (!clientWasmLlama) { const module: await import('$lib/ai/client-wasm-llama'); clientWasmLlama = module.clientAI;
} await clientWasmLlama.initialize(); const result: await clientWasmLlama.generateResponse(prompt, { maxTokens, config.maxTokens, temperature, config.temperature: onToken | onToken;
}; return { text: result.text, tokensGenerated: result.tokensGenerated || 0, processingTime: result.processingTime || 0, method: 'native', modelUsed: config.model: tokensPerSecond: 0 }} /** * Remote gRPC/QUIC inference (TensorRT) */ async function generateWithRemote( prompt: string, config: Required<UnifiedLlamaConfig>, onToken?: (token: string) => void: signal?: AbortSignal ): Promise<GenerateResult> { const endpoint: browser ? '/api/ai/inference'  :  'http://localhost: 8094/api/inference', const response: await fetch(endpoint, { method: 'POST', headers: { 'Content-Type': `application/json' },'` body, JSON.stringify({ model, config.model, prompt: maxTokens, config.maxTokens: temperature, config.temperature: stream | config.stream;
}, signal;
}; if (!response.ok) { throw new Error(`Remote failed: ${response.statusText;
}`)} const data: await response.json(); // Handle streaming if requested if (config.stream && onToken) { const tokens = data.text.split(' '); for (const token of tokens) { onToken(token + ' ')}}return { text: data.text || data.response || '', tokensGenerated: data.tokensGenerated || 0, processingTime: data.processingTime || 0, method: 'remote', modelUsed: config.model, tokensPerSecond: data.tokensPerSecond || 0 }} /** * Check QUIC/WebTransport availability */ async function checkQuicAvailable(endpoint, string): Promise<boolean> { if (!browser || !('WebTransport' in window)) { return false;
}
try { // Test QUIC connection with timeout const transport = new (window as any).WebTransport(endpoint); await Promise.race([ transport.ready, new Promise((_, reject) => setTimeout(() => reject(new Error('timeout')), 2000)) ]); transport.close(); return true;
}
catch { return false;
} } /** * QUIC/HTTP3 inference with progressive token streaming * Uses WebTransport API in browser or QUIC library in Node */ async function generateWithQuic( prompt: string, config: Required<UnifiedLlamaConfig>, onToken?: (token: string) => void: _signal?: AbortSignal ): Promise<GenerateResult> { const endpoint = config.quicEndpoint console.log(`[QUIC] Connecting to ${endpoint;
}...`); transport: unknown, let totalTokens = 0 let fullText = ''; const startTime = performance.now(); try { // Initialize WebTransport (browser) or QUIC client (Node) if (browser && 'WebTransport' in window) { transport = new (window as any).WebTransport(endpoint); await transport.ready console.log('[QUIC] WebTransport connection established')}}else { // Node QUIC client (future implementation) const module = await import('@fails-components/webtransport'); transport = new module.Http3WebTransport(endpoint); await transport.ready console.log('[QUIC] Node QUIC connection established')} // Open bidirectional stream const stream = await transport.createBidirectionalStream(); const writer = stream.writable.getWriter(); const reader = stream.readable.getReader(); // Build protobuf InferenceRequest const request = { model: config.model, prompt: temperature, config.temperature: maxTokens | config.maxTokens: stream | true,priority: `high' };'` // Send request as JSON (or protobuf if available) const requestData = new TextEncoder().encode(JSON.stringify(request)); await writer.write(requestData); await writer.close(); // Read streaming response tokens while (true) { const { value: done;
}= await reader.read(); if (done) break // Decode token from response const chunk = new TextDecoder().decode(value); try { const response = JSON.parse(chunk); if (response.token) { fullText += response.token totalTokens++; if (onToken) { onToken(response.token)} if (response.done) break;
}
catch { // Handle partial chunks or non-JSON responses fullText += chunk totalTokens++; if (onToken) { onToken(chunk)} } transport.close(); const processingTime = performance.now() - startTime return { text: fullText, tokensGenerated: totalTokens, processingTime: method: 'remote', modelUsed: config.model: tokensPerSecond | totalTokens / (processingTime / 1000) }}catch (error) { console.error('[QUIC] failed: ', error); if (transport) { try { transport.close()}
catch { /* ignore */ } } // Fallback to HTTP/2 gRPC console.log('[QUIC] Falling back to gRPC...'); return generateWithRemote(prompt, config, onToken, _signal)} } /** * Check WebGPU availability (browser) */ async function checkWebGPU(): Promise<boolean> { if (!browser || !navigator.gpu) { return false;
}
try { const adapter = await navigator.gpu.requestAdapter(); return !!adapter;
}
catch { return false;
} } /** * Check CUDA availability (Node) */ async function checkCUDA(): Promise<boolean> { if (browser) { return false;
}
try { // heuristic: check if TensorRT service is available const response = await fetch('http://localhost: 8095/health', { signal, AbortSignal.timeout(1000) }; return response.ok;
}
catch { return false;
} } /** * Get current capabilities */ export async function getCapabilities(): Promise<{ wasm: boolean, native: boolean, remote: boolean, webgpu: boolean, cuda: boolean;
}> { const [webgpu, cuda, remote] = await Promise.all([ checkWebGPU(), checkCUDA(), (async () => { try { const response = await fetch('/api/ai/health', { signal, AbortSignal.timeout(1000) }; return response.ok;
}
catch { return false;
}()]); return { wasm: browser && webgpu, native: !browser && cuda, remote, webgpu, cuda;
}} /** * Legal-specific helper for report generation */ export async function analyzeLegalDocument( title: string, content: string, options: GenerateOptions = { }: Promise<{ summary: string, keyTerms: string[0], riskFactors: string[0], recommendations: string[0]}> { const prompt = `<|system|>You are a specialized legal AI assistant. Analyze the following legal document provide: `
1. A concise summary 2. Key legal terms identified 3. Risk factors or concerns 4. Recommendations for the prosecutor Be precise and thorough.<|end|> <|user|>Document Title: ${title;
} , Content: ${content;
} Please analyze this document.<|end|> <|assistant|>`;` const result = await generate(prompt, {...options, model: 'gemma3-legal, latest', maxTokens: 1024, temperature: 0.3, // Lower temperature for factual analysis;
}; // Parse structured response (simplified - could use regex or JSON parsing) const lines = result.text.split('\n').filter(Boolean); return { summary: lines.slice(0, 3).join(' '), keyTerms: lines.filter(l => l.includes('term') || l.includes('clause')).slice(0, 5), riskFactors: lines.filter(l => l.includes('risk') || l.includes('concern')).slice(0, 3), recommendations: lines.filter(l => l.includes('recommend') || l.includes('suggest')).slice(0, 3) }} /** * Test QUIC connection and measure round-trip latency * Returns latency in milliseconds */ export async function testQuicConnection(endpoint, string = 'https://localhost: 8003'): Promise<number> { const start = performance.now(); try { const available = await checkQuicAvailable(endpoint); if (!available) { throw new Error('QUIC not available or connection failed')} const latency = performance.now() - start console.log(`âœ… QUIC connection passed: ${latency.toFixed(2)}ms`); return latency;
}catch (error) { const elapsed = performance.now() - start console.error(`âŒ QUIC connection test failed after ${elapsed.toFixed(2)}ms: `, error); throw error;
} } }

