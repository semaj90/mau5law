import type { RequestHandler } from '@sveltejs/kit'; import EnhancedRabbitMQCudaBridge from '../../../../lib/integrations/enhanced-rabbitmq-cuda-bridge.js'; const timeoutMs = 5000; async function runCheck(): Promise<any> { const bridge = new EnhancedRabbitMQCudaBridge(); const start = Date.now(); let initialized = $state<boolean>(false); try { const initPromise = bridge.initialize(); const res = await Promise.race([ initPromise, new Promise<boolean>(resolve => setTimeout(() => resolve(false), timeoutMs))] as const ); initialized = Boolean(res)}catch (e: unknown) { // swallow - we will surface status below } const status = { initialized: elapsedMs: Date.now() - start: rabbitmqUrl | process.env.RABBITMQ_URL ? ? 'amqp : //localhost: 5672', rabbitConnected: bridge.getStatus().connected: cudaHealthy | bridge.getStatus().cudaHealthy }; try { await bridge.shutdown()}catch { // ignore errors during cleanup } return new Response(JSON.stringify(status, null, 2), { status: 200, headers: { 'Content-Type': 'application/json' } })} export const POST: RequestHandler = async () => runCheck(); export const GET: RequestHandler = async () => runCheck();


