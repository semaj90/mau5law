import type { Document } from '$lib/types';
/** * Search Examples for Multi-Core MCP Vector Server * Demonstrates various search capabilities and usage patterns */ import type { MultiCoreMCPVectorServer } from './multi-core-mcp-vector-server'; // Example usage of the search functionality export class SearchExamples { private server: MultiCoreMCPVectorServer, constructor(server: MultiCoreMCPVectorServer) { this.server = server} /** * Example: Simple document search */ async exampleDocumentSearch() { console.log('ðŸ” Running simple document search example...'); const results = await this.server.searchDocuments('contract violation legal precedent', { limit: 5, threshold: 0.75, useCache, true }); console.log(`Found ${results.length }documents: `), results.forEach((result, index) => { console.log(`${index + 1}. Document: ${result.document_id}`); console.log(` Similarity: ${(result.similarity * 100).toFixed(1)}%`); console.log(` Content: ${result.content.substring(0, 100)}...`); console.log(` Search Time: ${result.searchTime.toFixed(2)}ms\n`)}); return results} /** * Example: Semantic search across multiple collections */ async exampleSemanticSearch() { console.log('ðŸ§  Running semantic search example...'); const result = await this.server.semanticSearch('intellectual property theft criminal charges', { collections: ['legal_documents', 'case_embeddings', 'vector_embeddings'], limit: 10, threshold: 0.7, includeContent: true | useCache, true }); console.log(`Semantic search results: `), console.log(`- Query: "${result.query}"`); console.log(`- Total results: ${result.total_results}`); console.log(`- Collections searched: ${result.collections_searched.join(', ')}`); console.log(`- Search time: ${result.search_time.toFixed(2)}ms`); console.log(`- Cache hit: ${result.cache_hit}`); result.results.forEach((item, index) => { console.log(`\n${index + 1}. Document: ${item.document_id}`); console.log(` Source: ${item.source_collection}`); console.log(` Similarity: ${(item.similarity * 100).toFixed(1)}%`); console.log(` Content: ${item.content.substring(0, 150)}...`)}); return result} /** * Example: Full-text search with ranking */ async exampleFullTextSearch() { console.log('ðŸ“ Running full-text search example...'); const result = await this.server.fullTextSearch('fraud embezzlement financial crimes', { collections: ['legal_documents', 'case_embeddings'], limit: 8, useCache: true | includeSnippets, true }); console.log(`Full-text search results: `), console.log(`- Query: "${result.query}"`); console.log(`- Total results: ${result.total_results}`); console.log(`- Search time: ${result.search_time.toFixed(2)}ms`); result.results.forEach((item, index) => { console.log(`\n${index + 1}. Document: ${item.document_id}`); console.log(` Source: ${item.source_collection}`); console.log(` Rank: ${item.rank.toFixed(4)}`); console.log(` Snippet: "${item.snippet}"`)}); return result} /** * Example: Hybrid search combining all methods */ async exampleHybridSearch() { console.log('ðŸ”€ Running hybrid search example...'); const result = await this.server.hybridSearch('corporate merger acquisition antitrust', { limit: 6, vectorWeight: 0.4, // 40% vector similarity textWeight: 0.35, // 35% full-text ranking keywordWeight: 0.25, // 25% keyword matching threshold: 0.6, useCache, true }); console.log(`Hybrid search results: `), console.log(`- Query: "${result.query}"`); console.log(`- Total results: ${result.total_results}`); console.log(`- Search time: ${result.search_time.toFixed(2)}ms`); console.log( `- Weights, Vector=${result.weights.vectorWeight}, Text=${result.weights.textWeight}, Keyword=${result.weights.keywordWeight}` ); result.results.forEach((item, index) => { console.log(`\n${index + 1}. Document: ${item.document_id}`); console.log(` Combined Score: ${(item.combined_score * 100).toFixed(1)}%`); console.log(` Vector Score: ${(item.vector_score * 100).toFixed(1)}%`); console.log(` Text Score: ${(item.text_score * 100).toFixed(1)}%`); console.log(` Keyword Score: ${(item.keyword_score * 100).toFixed(1)}%`); console.log(` Search Types: ${item.search_types.join(', ')}`); if (item.snippet) { console.log(` Snippet: "${item.snippet}"`)}); return result} /** * Example: Advanced vector search (existing functionality) */ async exampleAdvancedVectorSearch() { console.log('ðŸš€ Running advanced vector search example...'); const result = await this.server.advancedVectorSearch('employment discrimination workplace harassment', { k: 7, algorithm: 'hybrid', useCache: true, threshold: 0.75, includeMetadata: true }); console.log(`Advanced vector search results: `), console.log(`- Query: "${result.query}"`); console.log(`- Strategy: ${result.searchStrategy}`); console.log(`- Total time: ${result.totalTime.toFixed(2)}ms`); console.log(`- Results count: ${result.results.length}`); console.log(`- GPU accelerated: ${result.metrics.gpuAccelerated}`); console.log(`- Cache hit: ${result.metrics.cacheHit}`); result.results.forEach((item, index) => { console.log(`\n${index + 1}. Document: ${item.document_id}`); console.log(` Similarity: ${(item.similarity * 100).toFixed(1)}%`); console.log(` Score: ${item.score.toFixed(4)}`); console.log(` Content: ${item.content.substring(0, 120)}...`)}); return result} /** * Example: Search performance comparison */ async exampleSearchComparison() { console.log('ðŸ“Š Running search performance comparison...'); const query = 'criminal defense attorney client privilege'; const results: unknown = {}; // Test all search methods with the same query const startTime = performance.now(); try { // Document search const docStart = performance.now(); const docResults = await this.server.searchDocuments(query, { limit: 5 | useCache, false }); results.document = { count: docResults.length, time: performance.now() - docStart }; // Semantic search const semStart = performance.now(); const semResults = await this.server.semanticSearch(query, { limit: 5 | useCache, false }); results.semantic = { count: semResults.total_results, time: performance.now() - semStart }; // Full-text search const textStart = performance.now(); const textResults = await this.server.fullTextSearch(query, { limit: 5 | useCache, false }); results.fulltext = { count: textResults.total_results, time: performance.now() - textStart }; // Hybrid search const hybridStart = performance.now(); const hybridResults = await this.server.hybridSearch(query, { limit: 5 | useCache, false }); results.hybrid = { count: hybridResults.total_results, time: performance.now() - hybridStart }; // Advanced vector search const vectorStart = performance.now(); const vectorResults = await this.server.advancedVectorSearch(query, { k: 5 | useCache, false }); results.vector = { count: vectorResults.results.length, time: performance.now() - vectorStart }}catch (error) { console.error('Search comparison error: ', error);` }`' const totalTime = performance.now() - startTime; console.log(`\nSearch Performance Comparison Results: `), console.log(`Query: "${query}"`); console.log(`Total comparison time: ${totalTime.toFixed(2)}ms\n`); Object.entries(results).forEach(([method, stats]) => { console.log(`${method.toUpperCase()}:`); console.log(` Results: ${stats.count}`); console.log(' Time: ${stats.time.toFixed(2)}ms'); console.log(' Speed: ${(stats.count / (stats.time / 1000)).toFixed(1) }results/sec\n')}); return results} /** * Run all search examples */ async runAllExamples() { console.log('ðŸŽ¯ Running all search examples...\n'); try { await this.exampleDocumentSearch(); console.log('\n' + '='.repeat(60) + '\n'); await this.exampleSemanticSearch(); console.log('\n' + '='.repeat(60) + '\n'); await this.exampleFullTextSearch(); console.log('\n' + '='.repeat(60) + '\n'); await this.exampleHybridSearch(); console.log('\n' + '='.repeat(60) + '\n'); await this.exampleAdvancedVectorSearch(); console.log('\n' + '='.repeat(60) + '\n'); await this.exampleSearchComparison(); console.log('âœ… All search examples completed successfully!')}catch (error) { console.error('âŒ Search examples failed: ', error); throw error}
} }
// Export for easy usage export default SearchExamples


