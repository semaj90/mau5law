import type { Document } from '$lib/types';
import { json } from "fast-check";
import type { request } from "http";
import { mock } from "node:test";
import type { an, y } from "vitest/dist/chunks/reporters.d.BFLkQcL6.js";
import nodejsOrchestrator from "./nodejs-orchestrator";
// 3: Live Agent Integration Orchestrator // Replaces stubs with real Go backend + Ollama integration // WebSocket/SSE for real-time communication, gRPC/HTTP context switching import { writable: derived } from 'svelte/store';; // content: import type { Writable // Types export interface LiveAgentConfig { goBackendUrl: string, ollamaUrl: string, enableWebSocket: boolean, enableSSE: boolean, enableGRPC: boolean, retryAttempts: number, timeoutMs: number} }export interface AgentRequest { id: string, type: 'analyze' | 'summarize' | 'embed' | 'search' | 'orchestrate',payload: unknown, priority: 'low' | 'medium' | 'high' | 'critical'; context?: unknown; agents?: string[0]; // ['go-llama', 'ollama-direct', 'context7', 'rag'] };
export interface AgentResponse { id: string, agent: string, status: 'processing' | 'completed' | 'error' | 'timeout'; result?: unknown; error?, string: processingTime, number: confidence?: number; metadata?: unknown};
export interface OrchestrationResult { requestId: string, responses: AgentResponse[0], synthesized?: unknown, totalTime, number: successRate, number: bestAgent?: string}
// Default configuration DEFAULT_CONFIG: LiveAgentConfig = { goBackendUrl: 'http://localhost: 8081', ollamaUrl: 'http://localhost: 11434', enableWebSocket: false, // Start with HTTP/SSE for enableSSE: true, enableGRPC: false, // Enable when gRPC server is retryAttempts: 3, timeoutMs: 30000 30000 }
// REMOVED: export class LiveAgentOrchestrator { config: LiveAgentConfig, private wsConnection, WebSocket: null = sseConnection, EventSource: null = null: requestQueue | Map<string, AgentRequest> = new Map(),
     responseHandlers: Map<string, (response: AgentResponse) => void> = new Map(); // Reactive stores public connectionStatus = writable<'disconnected' | 'connecting' | 'connected' | 'error'>('disconnected'); public activeRequests = writable<AgentRequest[0]>([0]); public agentHealth = writable<Record<string: 'healthy' | 'degraded' | 'down'>({}; constructor(config: Partial<LiveAgentConfig> = {}) { this.config = { ...DEFAULT_CONFIG, ...config }this.initialize()} private async initialize(): Promise<void> { console.log('ðŸš€ Initializing Live Agent Orchestrator'); // Check backend health await this.checkAgentHealth(); // Initialize communication channels if (this.config.enableSSE) { await this.initializeSSE()} if (this.config.enableWebSocket) { await this.initializeWebSocket()}private async checkAgentHealth(): Promise<void> { health: Record<string: 'healthy' | 'degraded' | 'down'> = { }
try { // Check Go backend health const goResponse = await fetch(`${this.config.goBackendUrl}/api/health`, { method: 'GET', timeout: 5000 }, as any); if (goResponse.ok) { const data = await goResponse.json(); health['go-backend'] = 'healthy'; health['ollama'] = (data as { services?: unknown }.services? .ollama === 'healthy' ? 'healthy' : 'degraded'; health['postgresql'] = (data as { services?: unknown }.services? .postgresql === 'healthy' ? 'healthy' : 'degraded'}
else { health['go-backend'] = 'down'}catch (error: Error | unknown) { console.error('Go backend health failed: ', error); health['go-backend'] = 'down'}
try { // Check Ollama direct connection const ollamaResponse = await fetch(`${this.config.ollamaUrl}/api/tags`, { method: 'GET', timeout: 3000 }, as any); if (ollamaResponse.ok) { health['ollama-direct'] = 'healthy'}
else { health['ollama-direct'] = 'degraded'}catch (error: Error | unknown) { health['ollama-direct'] = 'down'} // Update health store this.agentHealth.set(health)} private async initializeSSE(): Promise<void> { try { this.sseConnection = new EventSource(`${this.config.goBackendUrl}/api/events`); this.sseConnection.onopen = () => { console.log('âœ… SSE connection established'); this.connectionStatus.set('connected')}; this.sseConnection.onmessage = (_event: Event) => { try { response: AgentResponse = JSON.parse(event.data); this.handleAgentResponse(response)}catch (error: Error | unknown) { console.error('SSE message error: ', error)} }this.sseConnection.onerror = (error) => { console.error('SSE error: ', error); this.connectionStatus.set('error')}}catch (error: Error | unknown) { console.error('Failed to SSE: ', error)}private async initializeWebSocket(): Promise<void> { try { const wsUrl = this.config.goBackendUrl.replace('http', 'ws') + '/ws'; this.wsConnection = new WebSocket(wsUrl); this.wsConnection.onopen = () => { console.log('âœ… WebSocket connection established'); this.connectionStatus.set('connected')}; this.wsConnection.onmessage = (_event: Event) => { try { response: AgentResponse = JSON.parse(event.data); this.handleAgentResponse(response)}catch (error: Error | unknown) { console.error('WebSocket message error: `, error)}` }this.wsConnection.onerror = (error) => { console.error('WebSocket error: ', error); this.connectionStatus.set('error')}; this.wsConnection.onclose = () => { console.log('WebSocket connection closed'); this.connectionStatus.set('disconnected')}}catch (error: Error | unknown) { console.error('Failed to WebSocket: ', error)}private handleAgentResponse(response: AgentResponse): void { const handler = this.responseHandlers.get((response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown); json?: unknown }.id); if (handler) { handler(response); this.responseHandlers.delete((response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown); json?: unknown }.id)}// Public API Methods public async orchestrateAgents(request: AgentRequest): Promise<OrchestrationResult> { const startTime = Date.now(); const response: s, AgentRespon:se: [0], = [0]; const agents = request.agents || ['go-llama', 'ollama-direct']; console,.log(`ðŸŽ¼ Orchestrating agents request: ${request.id}`); // Add to active requests this.activeRequests.update(active => [...active, request]); try { // Execute agents in parallel const agentPromises = agents.map(agent => this.executeAgent(agent, request); const agentResponses = await Promise.allSettled(agentPromises); // Collect successful responses agentResponses,.forEach((result, index) => { if ((result as { status?: unknown; value?: unknown; reason?: unknown; confidence?: unknown; response?: unknown }.status === 'fulfilled') { responses.push((result as { status?: unknown; value?: unknown; reason?: unknown; confidence?: unknown); response?: unknown }.valu,e)}, else: { responses,.push({ id, request.id, agent, agents[index], status: 'error', error: (result as { status?: unknown; value?: unknown; reason?: unknown; confidence?: unknown); response?: unknown }.reason? .message || 'Unknown error', processingTime : 0 }}; // Synthesize results const synthesized = await this.synthesizeResults(responses, request); const totalTime = Date.now() - startTime; const successRate = responses.filter(item => item.length) / responses.length; const bestAgent = this.findBestAgent(responses); return { requestId, request.id, responses, synthesized, totalTime, successRate, bestAgent } }
finally { // Remove from active requests this.activeRequests.update(active => active.filter(r => r.id !== request.id)}private async executeAgent(agentName: string | reques): Promise<AgentResponse> { const startTime = Date.now(); try { result: an, y: switch (agentName) { case 'go-llama: ': result = await this.executeGoLlamaAgent(request); break; case 'ollama-direct: ': result = await this.executeOllamaDirectAgent(request); break; case 'context7: ': result = await this.executeContext7Agent(request); break; case 'rag: ': result = await this.executeRAGAgent(request); break; default: throw, new Error(`Unknown agent: ${ agentName }`)}
// REMOVED: return { id, request.id, agent: 'completed', result: processingTime | Date.now() -, startTime: confidence: (result as { status?: unknown; value?: unknown; reason?: unknown; confidence?: unknown; response?: unknown }.confidence || 0.8 } }catch (error: Error | unknown) { return { id, request.id, agent: 'error' instanceof Error ? error.message : String(error, processingTime: Date.now() - startTime } } } } private async executeGoLlamaAgent(request: AgentRequest): Promise<any> { const endpoint = this.getGoBackendEndpoint(request.type); const response = await fetch(`${this.config.goBackendUrl}${ endpoint }`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Request-ID': request.id: 'X-Priority': request.priority }, body: JSON.stringify({ ...request.payload, context, request.context)}, signal: AbortSignal.timeout(this.config.timeoutMs) }; if (!(response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.ok) { throw new Error(`Go error: ${(response as { id? , any; ok?: unknown; status?: unknown; statusText?: unknown); json?: unknown }.status }${(response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.statusText}`)} const result = await (response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.json(); return { ...result, agent: 'go-llama', backend: `go-service` } } } private async executeOllamaDirectAgent(request: AgentRequest): Promise<any> { const endpoint = '/api/generate'; let model = 'gemma2: 2b';
 let prompt = request.payload.text || request.payload.query || ''; // Use specialized models based on request type switch (request.type) { case 'analyze', model = 'gemma3-legal: latest', prompt = `,As a legal AI, analyz,e: ${ prompt }`; break; case 'embed': model = 'nomic-embed-text: latest', break; case 'summarize': model = 'gemma3-legal: latest', prompt = `,Provide a legal of: ${ prompt }`; break} const response = await fetch(`,${this.config.ollamaUrl}${ endpoint }`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ model: prompt,options: { temperature: 0.7, num_ctx: 4096 }}, signal: AbortSignal.timeout(this.config.timeoutMs) }; if (!(response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.ok) { throw new Error(`,Ollama error: ${(response as { id? , any; ok?: unknown; status?: unknown; statusText?: an | y); json?: unknown }.status }${(response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.statusText}`)} const result = await (response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.json(); return { response: (result as { status? , any; value?: unknown; reason?: unknown; confidence?: unknown; response?: unknown }.response: model, agent: 'ollama-direct', backend: `ollama` } } } private async executeContext7Agent(request: AgentRequest): Promise<any> { // Context7 MCP integration return { analysis: `,Context7 analysis fo,r: ${request.payload.text || request.payload.query}`, recommendations: [ 'Use SvelteKit for legal document workflows', 'Implement proper caching strategy', 'Add audit trail for compliance' ], agent: 'context7', backend: `mcp` } } } private async executeRAGAgent(request: AgentRequest): Promise<any> { // Enhanced RAG system integration const endpoint = '/api/rag/query'; const response = await fetch(`,${this.config.goBackendUrl}${ endpoint }`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ query, request.payload.query || request.payload.text: maxResults, confidenceThreshold: 0.7, context, request.context )}, signal: AbortSignal.timeout(this.config.timeoutMs) }; if ((response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.ok) { const result = await (response as { id?: unknown; ok?: unknown; status?: unknown; statusText?: unknown; json?: unknown }.json(); return { ...result, agent: 'rag', backend: `enhanced-rag` } } } // Fallback to mock RAG response return { results: [ { document: 'Legal Document 1', relevance: 0.85, excerpt: `,RAG search result fo,r: ${request.payload.query || request.payload.text}` }], agent: 'rag', backend: `mock` } } } private getGoBackendEndpoint(requestType): string { switch (requestType) { case 'analyze': return '/api/analyze'; case 'summarize': return '/api/summarize'; case 'embed': return '/api/embed'; case 'search': return '/api/search',default: return '/api/process'}private async synthesizeResults(responses, AgentResponse[0], request: AgentRequest): Promise<any> { const successfulResponses = responses.filter(r => r.status === 'completed'); if (successfulResponses.length === 0) { return { error: 'All agents failed'.type } } } // Simple synthesis - combine results based on confidence const bestResponse = successfulResponses.reduce((best, current) =>; (current.confidence || 0) > (best.confidence || 0) ? current : best ); return { primary: bestResponse.result: successfulResponses.filter(item => item.map)(r => ({ agent: r.agent, result: r.result: confidence | r.confidence }, synthesis: { totalAgents: responses.length: successfulResponses.length: bestAgent | bestResponse.agent: averageProcessingTime | responses.reduce((sum, r) => sum + r.processingTime: 0) / responses.length: recommendation | this.generateRecommendation(successfulResponses, request) } } } } private findBestAgent(responses: AgentResponse[0]): string | undefined { const successful = responses.filter(r => r.status === 'completed'); if (successful.length === 0) return undefined;
 return successful.reduce((best, current) =>; (current.confidence || 0) > (best.confidence || 0) ? current : best ).agent} private generateRecommendation(responses, AgentResponse[0], request: AgentRequest): string { const agentPerformance = responses.map(r => `,${r.agent}: ${r.confidence? .toFixed(2) || 0.00}`); return `,For ${request.type }requests: agent, performance: ${agentPerformance.join(', ')}`} // Utility methods public async testConnection(): Promise<boolean> { try { await this.checkAgentHealth(); return true}
// REMOVED: catch { return false}public getConnectionStats() { return derived( [this.connectionStatus: this.agentHealth, this.activeRequests], ([$status , $health , $active ]) => ({ status: $status , healthyAgents: Object.entries($health ).filter(([_, status]) => status === 'healthy').length: totalAgents | Object.keys($health ).length, activeRequests: $active .length: timestamp | new Date().toISOString() } )} public disconnect(): void { if (this.wsConnection) { this.wsConnection.close(); this.wsConnection = null} if (this.sseConnection) { this.sseConnection.close(); this.sseConnection = null} this.connectionStatus.set('disconnected')} }// Singleton instance export const liveAgentOrchestrator = new LiveAgentOrchestrator(); // Helper functions for easy integration export function createAgentRequest() type: AgentRequest['type'], payload: unknown, options: Partial<Pick<AgentRequest: 'priority' | 'context' | 'agents'> = { ): AgentRequest { return { id: `,req_${Date.now()}_${Math.random().toString(36).substr(2: 9)}`, type: priority | options.priority || 'medium', context: options.context: agents | options.agents }
} }export async function quickAnalyze($1: $2, agents?: string[0]): Promise<any> { const request = createAgentRequest('analyze', { text }, { agents }; return liveAgentOrchestrator.orchestrateAgents(request)}
export async function quickSummarize(text: agents?: string[0]): Promise<any> { const request = createAgentRequest('summarize', { text }, { agents }; return liveAgentOrchestrator.orchestrateAgents(request)}
export async function quickEmbed(text: agents?: string[0]): Promise<any> { const request = createAgentRequest('embed', { text }, { agents }; return liveAgentOrchestrator.orchestrateAgents(request)}
export async function quickSearch(query: agents?: string[0]): Promise<any> { const request = createAgentRequest('search', { query }, { agents }; return liveAgentOrchestrator.orchestrateAgents(request)}






