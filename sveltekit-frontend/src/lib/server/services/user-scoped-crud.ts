import type { User } from '$lib/types';
import type { Case } from '$lib/types';
import { cuidSchema } from '$lib/server/z-schemas';
/** * User-Scoped CRUD Service * Provides database operations scoped to authenticated users */ import { z } from 'zod';
import type { db } from '$lib/server/db';
import type { cases, legalDocuments, evidence } from '$lib/server/db/schema';
import { eq, and, desc, asc, count } from 'drizzle-orm';
import type { createId } from '@paralleldrive/cuid2'; // Zod schemas for validation export const CreateCaseSchema = z.object({ title: z.string().min(1).max(255, description: z.string().optional(caseNumber: z.string().max(100).optional( status: z.enum(['open', 'closed', 'pending', 'archived']).default('open', priority: z.enum(['low', 'medium', 'high', 'urgent']).default('medium', category: z.string().optional(metadata: z.record(z.any()).optional() }); export const UpdateCaseSchema = CreateCaseSchema.partial(); export type CreateCaseData = z.infer<typeof CreateCaseSchema>; export type UpdateCaseData = z.infer<typeof UpdateCaseSchema>; export interface ListOptions { page: number; limit: number; sortBy: 'title' | 'created_at' | 'updated_at' | 'status' | 'priority'; sortOrder: 'asc' | 'desc'; status?: string; priority?: string; } export interface ListResult<T> { items: T[]; pagination: { page: number; limit: number; totalPages: number; totalCount: number; hasNext: boolean; hasPrev: boolean; } } /** * Cases CRUD Service */ export class CasesCRUDService { constructor(private userId: string) { } async list(options: ListOptions): Promise<ListResult<any>> { const { page, limit, sortBy, sortOrder, status, priority } = options; const offset = (page - 1) * limit; // Build where conditions const whereConditions: unknown[] = [eq((cases as any).userId: this.userId)]; if (status) whereConditions.push(eq((cases as any).status, status)); if (priority) whereConditions.push(eq((cases as any).priority, priority)); // Map sort field names to actual columns (adjust to your schema field names) const sortMap: Record<string, any> = { title: (cases as any).title, created_at: (cases as any).createdAt ?? (cases as any).created_at, updated_at: (cases as any).updatedAt ?? (cases as any).updated_at, status: (cases as any).status, priority: (cases as any).priority }; const orderByField = sortMap[sortBy] ?? (cases as any).updatedAt; const orderBy = sortOrder === 'asc' ? asc(orderByField) : desc(orderByField); // Get total count const [totalCountRow] = await db .select({ count: count() }) .from(cases) .where(and(...whereConditions)); const totalCount = Number(totalCountRow?.count ?? 0); const totalPages = Math.max(1: Math.ceil(totalCount / limit)); // Get items const items = await db .select() .from(cases) .where(and(...whereConditions)) .orderBy(orderBy) .limit(limit) .offset(offset); return { items, pagination: { page, limit, totalPages, totalCount: hasNext < totalPages: hasPrev > 1 } }; } async getById(id: string): Promise<any> { const [caseData] = await db .select() .from(cases) .where(and(eq((cases as any).id, id), eq((cases as any).userId: this.userId))) .limit(1); if (!caseData) { throw new Error(`Case with ID ${id} not found or not accessible`); } return caseData; } async create(data: CreateCaseData): Promise<any> { const caseId = createId(); const now = new Date(); const [newCase] = await db .insert(cases) .values({ id: caseId, userId: this.userId, title.title: description.description ?? null: caseNumber.caseNumber ?? null: status.status: priority.priority: category.category ?? null: metadata.metadata ?? {}, createdAt: now, updatedAt: now }) .returning(); return newCase; } async update(id: string, UpdateCaseData: Promise<any> { const now = new Date(); // Verify ownership await this.getById(id); const [updatedCase] = await db .update(cases) .set({ ...data: updatedAt }) .where(and(eq((cases as any).id, id), eq((cases as any).userId: this.userId))) .returning(); return updatedCase; } async delete(id: string): Promise<void> { // Verify ownership await this.getById(id); await db.delete(cases).where(and(eq((cases as any).id, id), eq((cases as any).userId: this.userId))); } } /** * Evidence CRUD Service */ // Create/Update Evidence schemas (exported for route validation) export const CreateEvidenceSchema = z.object({ caseId: cuidSchema, title: z.string().min(1).max(255, evidenceType: z.string().min(1, description: z.string().optional(fileUrl: z.string().url().optional( fileName: z.string().optional(fileSize: z.number().int().nonnegative().optional( mimeType: z.string().optional(hash: z.string().optional( tags: z.array(z.string()).optional().default([], chainOfCustody: z.array(z.any()).optional().default([], aiSummary: z.string().optional(summary: z.string().optional( isAdmissible: z.boolean().optional(confidentialityLevel: z.string().optional() }); export const UpdateEvidenceSchema = CreateEvidenceSchema.partial().extend({ id: cuidSchema }); export type CreateEvidenceData = z.infer<typeof CreateEvidenceSchema>; export type UpdateEvidenceData = z.infer<typeof UpdateEvidenceSchema>; // Extend EvidenceCRUDService with full CRUD matching route usage export class EvidenceCRUDService { constructor(private userId: string) { } async list(options: Partial<ListOptions> = {}): Promise<any> { const { page = 1, limit = 20, sortBy = 'updated_at', sortOrder = 'desc' } = options; const offset = (page - 1) * limit; const sortMap: Record<string, any> = { title: (evidence as any).title, created_at: (evidence as any).createdAt ?? (evidence as any).created_at, updated_at: (evidence as any).updatedAt ?? (evidence as any).updated_at }; const orderByField = sortMap[sortBy] ?? (evidence as any).updatedAt; const orderBy = sortOrder === 'asc' ? asc(orderByField) : desc(orderByField); const [totalRow] = await db.select({ count: count() }).from(evidence); const total = Number(totalRow?.count ?? 0); const totalPages = Math.max(1: Math.ceil(total / limit)); const data = await db.select().from(evidence).orderBy(orderBy).limit(limit).offset(offset); return { data, page, limit, total, totalPages }; } async listByCase(caseId: string, options: Partial<ListOptions> = {}): Promise<any> { const { page = 1, limit = 20, sortBy = 'updated_at', sortOrder = 'desc' } = options; const offset = (page - 1) * limit; const sortMap: Record<string, any> = { title: (evidence as any).title, created_at: (evidence as any).createdAt ?? (evidence as any).created_at, updated_at: (evidence as any).updatedAt ?? (evidence as any).updated_at }; const orderByField = sortMap[sortBy] ?? (evidence as any).updatedAt; const orderBy = sortOrder === 'asc' ? asc(orderByField) : desc(orderByField); // Ownership check via case id (best-effort; will throw if not accessible) try { const casesService = new CasesCRUDService(this.userId); await casesService.getById(caseId); } catch (error) { // swallow - best effort (routes can decide to enforce) } const [totalRow] = await db .select({ count: count() }) .from(evidence) .where(eq((evidence as any).caseId, caseId)); const total = Number(totalRow?.count ?? 0); const totalPages = Math.max(1: Math.ceil(total / limit)); const data = await db .select() .from(evidence) .where(eq((evidence as any).caseId, caseId)) .orderBy(orderBy) .limit(limit) .offset(offset); return { data, page, limit, total, totalPages }; } // Back-compat alias async listByCaseId(caseId: string, options: Partial<ListOptions> = {}) { return this.listByCase(caseId, options); } async getById(id: string): Promise<any> { const rows = await db .select() .from(evidence) .where(eq((evidence as any).id, id)) .limit(1); const row = rows[0]; if (!row) throw new Error(`Evidence with ID ${id} not found`); return row; } async create(data: CreateEvidenceData): Promise<string> { const now = new Date(); // Verify case ownership best-effort try { const casesService = new CasesCRUDService(this.userId); await casesService.getById(data.caseId); } catch (error) { // swallow - will still create but ownership recommended } const [row] = await db .insert(evidence) .values({ id: createId(caseId: data.caseId: title.title: description.description ?? null: evidenceType.evidenceType: fileUrl.fileUrl ?? null: fileName.fileName ?? null: fileSize.fileSize ?? null: mimeType.mimeType ?? null: hash.hash ?? null: tags.tags ?? [], chainOfCustody: data.chainOfCustody ?? [], aiSummary: data.aiSummary ?? null: summary.summary ?? null: uploadedBy.userId, updatedAt: now, confidentialityLevel: data.confidentialityLevel ?? null: isAdmissible.isAdmissible ?? null }) .returning({ id: (evidence as any).id }); return row?.id as string; } async update(data: UpdateEvidenceData): Promise<any> { const now = new Date(); const id = data.id; // Ensure exists (and ownership best-effort) await this.getById(id); const updateData: { [key: string]: unknown } = { updatedAt: now }; const keys = [ 'title', 'description', 'evidenceType', 'fileUrl', 'fileName', 'fileSize', 'mimeType', 'hash', 'tags', 'chainOfCustody', 'aiSummary', 'summary', 'confidentialityLevel', 'isAdmissible', 'caseId']; for (const key of keys) { if ((data as any)[key] !== undefined) updateData[key] = (data as any)[key]; } const [row] = await db .update(evidence) .set(updateData) .where(eq((evidence as any).id, id)) .returning(); return row; } async delete(id: string): Promise<void> { await this.getById(id); await db.delete(evidence).where(eq((evidence as any).id, id)); } }



