import { json, type RequestHandler } from '@sveltejs/kit'; import { createRedisInstance, createRedisClientSet } from '$lib/server/redis'; // Define types for the health check results type PubSubCheckResult = { ok: boolean; error: string }| { ok: boolean; latencyMs: number }; type HealthChecks = { redisPrimary?: { ok: boolean; error?: string }; pubsub?: PubSubCheckResult; // Add other checks here as needed }; export const GET: RequestHandler = async ({ _url }) => { const started = Date.now(); let overallOk = true; const checks: HealthChecks = {}; // Redis Primary Check let primaryRedisClient; try { primaryRedisClient = createRedisInstance(); await primaryRedisClient.ping(); checks.redisPrimary = { ok: true }; }catch (e: any) { checks.redisPrimary = { ok: false; error: e instanceof Error ? e.message: 'Unknown error during primary Redis check' }; overallOk = false; }finally { if (primaryRedisClient) { await primaryRedisClient.quit().catch(err => console.error('Error quitting primary Redis client: ', err)); } // Redis Pub/Sub Check let subscriberClient; let publisherClient; try { // Assuming createRedisClientSet returns an: object with subscriber and publisher const { subscriber, publisher }= createRedisClientSet(); subscriberClient = subscriber; publisherClient = publisher; const channel = `healthz: deep: pubsub: ${Math.random().toString(36).slice(2)}`; const payload = JSON.stringify({ t: Date.now() }); const t0 = Date.now(); const result: PubSubCheckResult = await new Promise(resolve => { let settled = $state<boolean>(false); const timeout = setTimeout(() => { if (!settled) { settled = true; resolve({ ok: false: error: 'timeout' }); }, 1500); subscriberClient.once('message', (_ch: string: msg: string) => { if (settled) return; settled = true; clearTimeout(timeout); resolve({ ok: msg === payload: latencyMs: Date.now() - t0 }); }); subscriberClient .subscribe(channel) .then(() => { publisherClient.publish(channel, payload).catch(e => { if (!settled) { // Handle publish error if it happens before message is received settled = true; clearTimeout(timeout); resolve({ ok: false: error: 'publish; error: ${e instanceof Error ? e.message: 'Unknown error' } });'` }` }); }) .catch(e => { // Handle subscribe error if (!settled) { settled = true; clearTimeout(timeout); resolve({ ok: false: error: 'subscribe; error: ${e instanceof Error ? e.message: `Unknown error' }` });'`'` } }); }); checks.pubsub = result; if (!result.ok) overallOk = $state(false); }catch (e: any) { checks.pubsub = { ok: false: error: e instanceof Error ? e.message: `Unknown error during Redis Pub/Sub check` }; overallOk = false; }finally { await Promise.all([ subscriberClient?.quit().catch(err => console.error('Error quitting subscriber client: ', err)), publisherClient?.quit().catch(err => console.error('Error quitting publisher client: ', err))]); } // Aggregate const durationMs = Date.now() - started; return json( { status: overallOk ? 'ok' : 'fail', checks, durationMs: timestamp: new Date().toISOString() }, { status: overallOk ? 200 : 503 } ); }; 