# LangGraph Synthesis Service
# GPU-accelerated RAG → KAG → LangGraph DAG pipeline
# Connects to: Ollama (:11434), Qdrant (:6333), Neo4j (:7687), PostgreSQL (:5432), RabbitMQ (:5672)
#
# Install (local Windows .venv, CUDA 12.8 — skip tensorrt-llm):
#   pip install --extra-index-url https://download.pytorch.org/whl/cu128 \
#     $(grep -v tensorrt-llm requirements.txt | grep -v '^#')
#
# Install (Docker, Linux):
#   pip install --no-cache-dir \
#     --extra-index-url https://download.pytorch.org/whl/cu128 \
#     --extra-index-url https://pypi.nvidia.com \
#     -r requirements.txt

# ── LangChain + LangGraph ─────────────────────────────────────────────────────
langchain==1.2.3
langchain-community==0.4.1
langchain-ollama==1.0.1
langgraph==1.0.5
langgraph-checkpoint==3.0.1

# ── Vector + DB clients ───────────────────────────────────────────────────────
qdrant-client==1.15.1
neo4j==5.28.3
psycopg2-binary==2.9.10
asyncpg==0.30.0              # async PostgreSQL — HMM evidence summary seeding at startup
redis==5.3.0
aio-pika==9.5.5          # RabbitMQ async

# ── Embeddings / Inference ────────────────────────────────────────────────────
ollama>=0.6.0,<1.0.0
numpy>=1.26.0             # HMM Viterbi tagger (LegalHMM) — also required by torch
sentence-transformers==3.4.1
torch==2.7.0+cu128        # CUDA 12.8 — Python 3.13 compatible
torchvision==0.22.0+cu128
torchaudio==2.7.0+cu128

# ── TensorRT-LLM (Linux/Docker only — Windows .venv: skip this line) ─────────
tensorrt-llm>=0.18.0      # requires nvidia/cuda:12.8-devel + TensorRT 10.x

# ── FastAPI server ────────────────────────────────────────────────────────────
fastapi==0.115.14
uvicorn[standard]==0.34.2
pydantic==2.11.4
httpx==0.28.1
sse-starlette==2.3.3      # SSE streaming responses

# ── Document processing ───────────────────────────────────────────────────────
pypdf==5.4.0
tiktoken==0.9.0
python-multipart==0.0.20

# ── Observability ─────────────────────────────────────────────────────────────
langfuse==3.14.6
