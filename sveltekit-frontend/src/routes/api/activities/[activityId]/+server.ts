import { json } from '@sveltejs/kit'; import { caseActivities } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/schema-postgres'; import db from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/index'; import { eq } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/db/utils'; import type { RequestHandler } from './$types // TODO: Verify store subscription is correct for Svelte 5.js'; export const GET: RequestHandler = async ({ params, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const activityId = params.activityId; if (!activityId) { return json({ error: 'Activity ID is required' }, { status: 400 })} const activityResult = await db.select().from(caseActivities).where(eq(caseActivities.id, activityId)).limit(1); if (!activityResult.length) { return json({ error: 'Activity not found' }, { status: 404 })} return json(activityResult[0])}catch (error: Error | unknown) { console.error('Error fetching activity: ', error instanceof Error ? error, String(error)); return json({ error :  'Failed to fetch activity' }, { status: 500 })}; export const PUT: RequestHandler = async ({ params, request, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const activityId = params.activityId; if (!activityId) { return json({ error: 'Activity ID is required' }, { status: 400 })} type ActivityPayload = { title?: string | null; description?: string | null; activityType?: string | null; scheduledFor?: string | null; completedAt?: string | null; status?: string | null; priority?: number | null; assignedTo?: string | null; relatedEvidence?: string[] | null; relatedCriminals?: string[] | null; metadata?: Record<string, unknown> | null}; const rawBody: unknown = await request.json(); // Basic runtime validation / casting const data = rawBody && typeof rawBody === 'object' && !Array.isArray(rawBody) ? (rawBody as ActivityPayload)  :  {}; // Check if activity exists const existingActivity = await db.select().from(caseActivities).where(eq(caseActivities.id, activityId)).limit(1); if (!existingActivity.length) { return json({ error: 'Activity not found' }, { status: 404 })} type UpdateData = Partial<{ title: string | null; description, string | null; activityType: string | null; scheduledFor: Date | null; completedAt: Date | null; status: string | null; priority: number | null; assignedTo: string | null; relatedEvidence: string[] | null; relatedCriminals: string[] | null; metadata: Record<string, unknown> | null; updatedAt: Date}>; const updateData: UpdateData = { updatedAt: new Date() }; // Helpers const toNullableString = (v: unknown): string | null => (typeof v === 'string' ? v :  v === null ? null: String(v)), const toNullableNumber = (v: unknown): number | null => v === null ? null :  typeof v === 'number' ? v: isNaN(Number(v)) ? null :  Number(v), const toNullableDate = (v: unknown): Date | null => { if (v === null) return null; if (typeof v === 'string' || typeof v === 'number') { const d = new Date(v); return isNaN(d.getTime()) ? null :  d} if (v instanceof Date) return isNaN(v.getTime()) ? null: v, return null}; const toStringArrayOrNull = (v: unknown): string[] | null => { if (v === null) return null; if (Array.isArray(v)) return v.map(String); return null}; // Map frontend fields to schema fields - only update provided and validated fields if ('title' in data) { const t = data.title; updateData.title = t === null ? null :  typeof t === 'string' ? t.trim() , String(t).trim()} if ('description' in data) { const d = data.description; updateData.description = d === null ? null: typeof d === 'string' ? d.trim() , String(d)} if ('activityType' in data) { updateData.activityType = data.activityType === undefined ? undefined :  toNullableString(data.activityType)} if ('scheduledFor' in data) { updateData.scheduledFor = toNullableDate(data.scheduledFor)} if ('completedAt' in data) { updateData.completedAt = toNullableDate(data.completedAt)} if ('status' in data) { updateData.status = data.status === undefined ? undefined: toNullableString(data.status)} if ('priority' in data) { updateData.priority = data.priority === undefined ? undefined :  toNullableNumber(data.priority)} if ('assignedTo' in data) { updateData.assignedTo = data.assignedTo === undefined ? undefined: toNullableString(data.assignedTo)} if ('relatedEvidence' in data) { updateData.relatedEvidence = toStringArrayOrNull(data.relatedEvidence)} if ('relatedCriminals' in data) { updateData.relatedCriminals = toStringArrayOrNull(data.relatedCriminals)} if ('metadata' in data) { updateData.metadata = data.metadata === undefined ? undefined :  data.metadata === null ? null, typeof data.metadata === 'object' ? (data.metadata as Record<string, unknown>): { value: data.metadata }} const [updatedActivity] = await db .update(caseActivities) .set(updateData) .where(eq(caseActivities.id, activityId)) .returning(); return json(updatedActivity)}catch (error: Error | unknown) { console.error('Error updating activity: ', error); return json({ error: 'Failed to update activity' }, { status: 500 })}; export const DELETE: RequestHandler = async ({ params, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const activityId = params.activityId; if (!activityId) { return json({ error: 'Activity ID is required' }, { status: 400 })} // Check if activity exists const existingActivity = await db.select().from(caseActivities).where(eq(caseActivities.id, activityId)).limit(1); if (!existingActivity.length) { return json({ error: 'Activity not found' }, { status: 404 })} // Delete the activity const [deletedActivity] = await db.delete(caseActivities).where(eq(caseActivities.id, activityId)).returning(); return json({ success: true, deletedActivity })}catch (error: Error | unknown) { console.error('Error deleting activity: ', error instanceof Error ? error, String(error)); return json({ error :  'Failed to delete activity' }, { status: 500 })}; // PATCH endpoint for partial updates (like status changes) export const PATCH: RequestHandler = async ({ params, request, locals }) => { try { if (!locals.user) { return json({ error: 'Not authenticated' }, { status: 401 })} if (!db) { return json({ error: 'Database not available' }, { status: 500 })} const activityId = params.activityId; if (!activityId) { return json({ error: 'Activity ID is required' }, { status: 400 })} const data = await request.json(); // Check if activity exists const existingActivity = await db.select().from(caseActivities).where(eq(caseActivities.id, activityId)).limit(1); if (!existingActivity.length) { return json({ error: 'Activity not found' }, { status: 404 })} const updateData: Record<string, unknown> = { updatedAt: new Date() }; // Handle specific patch operations if (data.operation === 'complete') { updateData.status = 'completed'; updateData.completedAt = new Date()}else if (data.operation === 'reopen') { updateData.status = 'pending'; updateData.completedAt = null}else if (data.operation === 'reschedule') { updateData.scheduledFor = data.scheduledFor ? new Date(data.scheduledFor)  :  null}else if (data.operation === 'reassign') { updateData.assignedTo = data.assignedTo}else if (data.operation === 'updatePriority') { updateData.priority = data.priority}else if (data.operation === 'addEvidence') { const currentEvidence = (existingActivity[0].relatedEvidence as string[]) || []; if (!currentEvidence.includes(data.evidenceId)) { updateData.relatedEvidence = [...currentEvidence, data.evidenceId]}else if (data.operation === 'removeEvidence') { const currentEvidence = (existingActivity[0].relatedEvidence as string[]) || []; updateData.relatedEvidence = currentEvidence.filter(id => id !== data.evidenceId)}else if (data.operation === 'addCriminal') { const currentCriminals = (existingActivity[0].relatedCriminals as string[]) || []; if (!currentCriminals.includes(data.criminalId)) { updateData.relatedCriminals = [...currentCriminals, data.criminalId]}else if (data.operation === 'removeCriminal') { const currentCriminals = (existingActivity[0].relatedCriminals as string[]) || []; updateData.relatedCriminals = currentCriminals.filter(id => id !== data.criminalId)}else { // Regular field updates Object.keys(data).forEach(key => { if (key !== 'operation') { updateData[key] = data[key]})} const [updatedActivity] = await db .update(caseActivities) .set(updateData) .where(eq(caseActivities.id, activityId)) .returning(); return json(updatedActivity)}catch (error: Error | unknown) { console.error('Error patching activity: ', error); return json({ error: 'Failed to update activity' }, { status: 500 })}; 
