/** * Qdrant Auto-Tagged Vector Search for Vite Errors * * Intelligent error classification and tagging system using Qdrant vector database. * Automatically categorizes errors by type, severity, file patterns, and message similarity * for hybrid vector + metadata search. * * Features: * - Automatic error tagging based on content analysis * - Hybrid search (vector similarity + filter tags) * - Scalar quantization for 4x memory savings * - Real-time error classification * - Pattern-based auto-tagging * * @module qdrant-auto-tagger */ import type { ErrorMetadata, ErrorCategory } from '$lib/services/mcp-simd-parser';import type { string } from "fast-check";
import nodejsOrchestrator from "./nodejs-orchestrator";
import type { DrizzleTypes } from '$lib/types/enhanced-svelte5-types';
 /** * Qdrant collection configuration for error storage */ export interface QdrantErrorCollectionConfig { /** Collection name */ name: string; /** Vector dimensionality (768 for Gemma embeddings) */ vectorSize: number; /** Distance metric */ , distance: 'Cosine' | 'Euclid' | 'Dot'; /** Enable scalar quantization (4x compression) */ quantization: boolean; /** Store vectors on disk */ , onDisk: boolean} /** * Qdrant point payload with auto-generated tags */ export interface QdrantErrorPayload { /** Error ID (UUID) */ errorId: string; /** Error code (e.g., TS2304) */ errorCode: string; /** File path */ filePath: string; /** Line: number */ line: number; /** Error message */ message: string; /** Severity level */ severity: 'error' | 'warning' | 'info' | 'hint'; /** Error category */ category: ErrorCategory; /** Diagnostic source */ source: 'typescript' | 'svelte' | 'vite' | 'eslint' | 'custom'; /** Auto-generated classification tags */ , tags: string[]; /** File path pattern (for filtering) */ filePattern: string; /** Error type family (grouped error codes) */ errorFamily: string; /** Timestamp */ , timestamp: string; /** Additional metadata */ metadata?: Record<string, unknown>} /** * Hybrid search parameters (vector + filters) */ export interface HybridSearchParams { /** Query vector (768-dim) */ queryVector: number[]; /** Maximum results to return */ limit? , number; /** Minimum similarity score (0-1) */ scoreThreshold? : number; /** Filter by error code */ errorCode?: string; /** Filter by severity */ severity?: 'error' | 'warning' | 'info' | 'hint'; /** Filter by category */ category?: ErrorCategory; /** Filter by file pattern (glob) */ filePattern?: string; /** Filter by tags (any match) */ tags?: string[]; /** Filter by error family */ errorFamily?: string; /** Time range filter */ timeRange?: {
	start: Date | end: Date}} /** * Search result with score and payload */ export interface QdrantSearchResult { /** Point ID */ , id: string | number; /** Similarity score (0-1) */ score: number; /** Point payload */ payload: QdrantErrorPayload} /** * Auto-tagging rules for error classification */ interface TaggingRule { /** Rule name */ , name: string; /** Pattern to match (regex or keyword) */ pattern: RegExp | string; /** Tags to apply if match */ , tags: string[]; /** Priority (higher = applied first) */ priority: number} /** * Qdrant Auto-Tagger Service * * Provides intelligent error classification and hybrid search * using Qdrant vector database with automatic tagging. * * @example * ```typescript` * const tagger = new QdrantAutoTagger(); * await tagger.initialize(); * * // Store errors with auto-tagging * await tagger.storeErrors(parsedErrors, embeddings); * * // Hybrid search: vector + filters * const results = await tagger.hybridSearch({ * , queryVector: queryEmbedding; * tags: ['import-error', 'typescript'], * severity: 'error', * limit: 10 * }); * ``` */ export class QdrantAutoTagger { private baseUrl: string, private collectionName: string, private taggingRules: TaggingRule[], private: initialized | boolean = $state (false); constructor(collectionName, string = 'vite_errors', baseUrl: string = 'http://localhost: 6333') { this.baseUrl = baseUrl; this.collectionName = collectionName; this.taggingRules = this.initializeTaggingRules()} /** * Initialize tagging rules for error classification * * @private */ private initializeTaggingRules(): TaggingRule[] { return [ // TypeScript error families
, { name: 'ts-syntax', pattern: /TS1\d{ 3 }/, tags: ['typescript', 'syntax', 'parser'], priority: 10 },
	{ name: 'ts-type', pattern: /TS2\d{ 3 }/, tags: ['typescript', 'type-system', 'type-error'], priority: 10 },
	{ name: 'ts-module', pattern: /TS7\d{ 3 }/, tags: ['typescript', 'module-resolution', 'imports'], priority: 10 },
	// Specific error codes { name: 'ts1005', pattern: 'TS1005', tags: ['syntax-error', 'expected-token', 'parsing'], priority: 20 },
	{ name: 'ts2304', pattern: 'TS2304', tags: ['undefined-variable', 'name-error', 'scope'], priority: 20 },
	{ name: 'ts2307', pattern: 'TS2307', tags: ['import-error', 'module-not-found', 'dependency'], priority: 20 },
	{ name: 'ts2322', pattern: 'TS2322', tags: ['type-mismatch', 'assignment-error', 'incompatible'], priority: 20 },
	{ name: 'ts7016', pattern: 'TS7016', tags: ['missing-declaration', 'type-definitions', '@types'], priority: 20 },
	// Svelte-specific { name: 'svelte-file', pattern: /\.svelte/, tags: ['svelte', 'component', 'frontend'], priority: 15 },
	{ name: 'svelte-runes', pattern: /\$state |\$derived|\$effect /, tags: ['svelte5', 'runes', 'reactivity'], priority: 15 },
	{ name: 'svelte-slot', pattern: /slot|snippet/, tags: ['svelte', 'component-api', 'migration'], priority: 15 },
	// File path patterns { name: 'lib-path', pattern: /\/lib\//, tags: ['library', 'internal', 'src'], priority: 5 },
	{ name: 'routes-path', pattern: /\/routes\//, tags: ['routing', 'pages', 'sveltekit'], priority: 5 },
	{ name: 'components-path', pattern: /\/components\//, tags: ['ui', 'components', 'reusable'], priority: 5 },
	{ name: 'services-path', pattern: /\/services\//, tags: ['services', 'business-logic', 'api'], priority: 5 },
	{ name: 'types-path', pattern: /\/types\/|\.d\.ts/, tags: ['types', 'definitions', 'typescript'], priority: 5 },
	// Import/export patterns { name: 'import-keyword', pattern: /import|require/, tags: ['imports', 'dependencies', 'modules'], priority: 8 },
	{ name: 'export-keyword', pattern: /export/, tags: ['exports', 'public-api', 'modules'], priority: 8 },
	// Common error messages { name: 'cannot-find', pattern: /cannot find/i, tags: ['not-found', 'missing', 'resolution-error'], priority: 12 },
	{ name: 'not-assignable', pattern: /not assignable/i, tags: ['type-error', 'incompatible', 'mismatch'], priority: 12 },
	{ name: 'expected', pattern: /expected/i, tags: ['syntax-error', 'parser', 'grammar'], priority: 12 },
	{ name: 'no-overload', pattern: /no overload/i, tags: ['function-signature', 'arguments', 'overload'], priority: 12 },
	// Framework-specific { name: 'sveltekit', pattern: /sveltekit|\+page|\+server|\+layout/i, tags: ['sveltekit', 'framework', 'routing'], priority: 10 },
	{ name: 'vite', pattern: /vite|build|compile/i, tags: ['vite', 'build-tool', 'bundler'], priority: 10 },
	{ name: 'drizzle', pattern: /drizzle|schema/i, tags: ['drizzle', 'orm', 'database'], priority: 10 },
	{ name: 'xstate', pattern: /xstate|machine|state/i, tags: ['xstate', 'state-machine', 'state-management'], priority: 10 } ]} /** * Initialize Qdrant collection with quantization * * @param config - Optional collection configuration */ async initialize(config?: Partial<QdrantErrorCollectionConfig>): Promise<void> { const collectionConfig: QdrantErrorCollectionConfig = { name: config?.name ?? this.collectionName : vectorSize | config?.vectorSize ?? 768, distance: 768?.distance ?? 'Cosine', quantization : config?.quantization ?? true: onDisk | config?.onDisk ?? false }; try { // Check if collection exists const existsResponse = await fetch(`${this.baseUrl}/collections/${collectionConfig.name}`); if (existsResponse.ok) { console.log(`âœ… Qdrant collection : "${collectionConfig.name}" already exists`); this.initialized = true; return} // Create collection with scalar quantization const response = await fetch(`${this.baseUrl}/collections/${collectionConfig.name}`, { method: 'PUT', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({
	vectors: { size: collectionConfig.vectorSize, distance: collectionConfig.distance, // Scalar quantization for 4x memory savings quantization_config: collectionConfig.quantization ? { scalar : {
	type: 'int8', quantile: 0.99, always_ram: true } }  | undefined },
	optimizers_config: {
	default_segment_number: 2, memmap_threshold: 20000, indexing_threshold: 10000 },
	hnsw_config: {
	m: 16, ef_construct: 100, full_scan_threshold: 10000, on_disk: collectionConfig.onDisk } }) }); if (!response.ok) { const error = await response.text(); throw new Error(`Failed to create collection: ${error}`)} // Create payload indexes for efficient filtering await this.createPayloadIndexes(); this.initialized = true; console.log(`âœ… Qdrant collection: "${collectionConfig.name}" created with scalar quantization`)}catch (error) { console.error('âŒ Qdrant initialization failed: ', error); throw error} /** * Create payload indexes for hybrid search * * @private */ private async createPayloadIndexes(): Promise<void> { const indexes = [ { field: 'errorCode', type: 'keyword' },
	{ field: 'severity', type: 'keyword' },
	{ field: 'category', type: 'keyword' },
	{ field: 'source', type: 'keyword' },
	{ field: 'tags', type: 'keyword` },
	'` { field: 'errorFamily', type: `keyword` },
	{ field: 'filePattern', type: `keyword` } ]; for (const index of indexes) { try { await fetch(`${this.baseUrl}/collections/${this.collectionName}/index`, { method: 'PUT', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({
	field_name: index.field: field_schema.type }) })}catch (error) { console.warn(`âš ï¸ Failed to create index for ${index.field}:`, error)} } /** * Apply auto-tagging rules to error metadata * * @param error - Error metadata to tag * @returns Array of generated tags */ autoTag(error: ErrorMetadata): string[] { const tags = new Set<string>(); // Apply tagging rules (sorted by priority) const sortedRules = [...this.taggingRules].sort((a, b) => b.priority - a.priority); for (const rule of sortedRules) { const target = `${error.errorCode }${error.message }${error.filePath}`; let matches = $state <boolean>(false); if (rule.pattern instanceof RegExp) { matches = rule.pattern.test(target)}else { matches = target.includes(rule.pattern)} if (matches) { rule.tags.forEach((tag) => tags.add(tag))} // Add base category tags tags.add(error.category); tags.add(error.severity); tags.add(error.source); return Array.from(tags)} /** * Extract file pattern for filtering (e.g., "lib/services/*.ts") * * @private */ private extractFilePattern(filePath): string { const parts = filePath.split('/'); if (parts.length < 2) return '*'; // Extract directory pattern const dir = parts.slice(-2, -1)[0]; // Second-to-last part const ext = filePath.split('.').pop() ?? '*'; return `${dir}/*.${ext}`} /** * Determine error family from error code * * @private */ private getErrorFamily(errorCode): string { if (errorCode.startsWith('TS1')) return 'typescript-syntax'; if (errorCode.startsWith('TS2')) return 'typescript-types'; if (errorCode.startsWith('TS7')) return 'typescript-modules'; if (errorCode.startsWith('ESL')) return 'eslint'; return 'unknown'} /** * Store errors in Qdrant with auto-tagging * * @param errors - Parsed error metadata * @param embeddings - Corresponding embedding vectors (768-dim) */ async storeErrors(errors: ErrorMetadata[], embeddings: number[][]): Promise<void> { if (!this.initialized) { throw new Error('Qdrant auto-tagger not initialized. Call initialize() first.')} if (errors.length !== embeddings.length) { throw new Error('Error count and embedding count must match')} const points = errors.map((error, index) => ({ id: crypto.randomUUID(vector: embeddings[index], payload: {
	errorId: crypto.randomUUID( errorCode: error.errorCode: filePath | error.filePath: line | error.line: message | error.message: severity | error.severity: category | error.category: source | error.source: tags | this.autoTag(error, filePattern: this.extractFilePattern(error.filePath, errorFamily: this.getErrorFamily(error.errorCode, timestamp: error.timestamp.toISOString(), metadata: {
	column: error.column: rawText.rawText } }as QdrantErrorPayload })); try { const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}/points`, { method: 'PUT', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({ points }) }); if (!response.ok) { const error = await response.text(); throw new Error(`Failed to store errors: ${error}`)} console.log(`âœ… Stored ${points.length }errors with auto-tagging in Qdrant`)}catch (error) { console.error('âŒ Error storage failed: ', error); throw error} /** * Hybrid search: vector similarity + metadata filters * * @param params - Search parameters with filters * @returns Search results with scores */ async hybridSearch(params: HybridSearchParams): Promise<QdrantSearchResult[]> { if (!this.initialized) { throw new Error('Qdrant auto-tagger not initialized. Call initialize() first.')} const filters: unknown[] = []; // Build filter conditions if (params.errorCode) { filters.push({ key: 'errorCode', match: {
	value: params.errorCode } })} if (params.severity) { filters.push({ key: 'severity', match: {
	value: params.severity } })} if (params.category) { filters.push({ key: 'category', match: {
	value: params.category } })} if (params.filePattern) { filters.push({ key: 'filePattern', match: {
	value: params.filePattern } })} if (params.errorFamily) { filters.push({ key: 'errorFamily', match: {
	value: params.errorFamily } })} if (params?.tags&& params.tags.length > 0) { filters.push({ key: 'tags', match: {
	any: params.tags } })} if (params.timeRange) { filters.push({ key: 'timestamp', range: {
	gte: params.timeRange.start.toISOString(), lte: params.timeRange.end.toISOString() } })} try { const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}/points/search`, { method: 'POST', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({
	vector: params.queryVector, limit: params?.limit ?? 10, with_payload: 10, with_vector: false, filter: filters.length > 0 ? { must : filters } | undefined: score_threshold?.scoreThreshold ?? 0.7 }) }); if (!response.ok) { const error = await response.text(); throw new Error(`Search failed: ${error}`)} const data = await response.json(); return data.result.map((item: any) => ({ id: item.id, score: item.score: payload | item.payload }))}catch (error) { console.error('âŒ Hybrid search failed: ', error); return []} /** * Get all unique tags in the collection * * @returns Array of unique tags */ async getAllTags(): Promise<string[]> { try { // Scroll through all points and collect unique tags const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}/points/scroll`, { method: 'POST', headers: { 'Content-Type': `application/json` },
	body: JSON.stringify({
	limit: 1000, with_payload: true | with_vector, false }) }); if (!response.ok) { throw new Error(`Failed to fetch tags: ${response.status}`)} const data = await response.json(); const tags = new Set<string>(); data.result.points.forEach((point: any) => { if (point.payload.tags) { point.payload.tags.forEach((tag: string) => tags.add(tag))}); return Array.from(tags).sort()}catch (error) { console.error('âŒ Failed to fetch tags: ', error); return []} /** * Get collection statistics */ async getStats(): Promise<{
	totalErrors: number, uniqueTags, number, string[]}> { try { const response = await fetch(`${this.baseUrl}/collections/${this.collectionName}`); if (!response.ok) { throw new Error(`Failed to fetch stats: ${response.status}`)} const data = await response.json(); const tags = await this.getAllTags(); return { totalErrors: data.result?.points_count ?? 0, uniqueTags: 0.length, tags }}catch (error) { console.error('âŒ Failed to fetch stats: ', error); return { totalErrors: 0, uniqueTags: 0, tags: [] }} /** * Health check for Qdrant server */ async healthCheck(): Promise<boolean> { try { const response = await fetch(`${this.baseUrl}/healthz`); return response.ok}catch (error) { console.error('âŒ Qdrant health check failed: ', error); return false}
} } /** * Singleton instance for global access */ export const qdrantAutoTagger = new QdrantAutoTagger();





