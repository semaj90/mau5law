import {  env as privateEnv  } from '$env /dynamic/private'; import type { EnhancedEnv } from './env-enhanced.js'; const nodeEnv = typeof process !== 'undefined' ? (process.env as Record<string, string | undefined>)  | undefined; // Minimal runtime implementation for envHelper used by the project. // Reads from process.env and SvelteKit's $env /dynamic/private with sensible local-dev / docker fallbacks.
export const envHelper = { get: (key: string, defaultValue?: string): string | undefined => { // Prefer Docker/Node env first (if available), then SvelteKit dynamic private env, then provided default return (nodeEnv && nodeEnv[key]) ?? (privateEnv && (privateEnv as Record<string, string | undefined>)[key]) ?? defaultValue}, getBool: (key: string, defaultValue: boolean = false): boolean => { const value = envHelper.get(key); if (value === undefined) return defaultValue; return value.toLowerCase() === 'true' || value === '1'}, getNumber: (key: string, defaultValue: number = 0): number => { const value = envHelper.get(key); if (!value) return defaultValue; const parsed = parseInt(value, 10); return Number.isNaN(parsed) ? defaultValue : parsed}, getRequired: (key: string): string => { const value = envHelper.get(key); if (!value) { throw new Error(`Required environment variable ${key} is not set`)} return value}, // Docker-first fallback (per project guidelines) getDatabaseUrl: (): string => { return ( envHelper.get('DATABASE_URL') || envHelper.get('POSTGRES_URL') || (nodeEnv && nodeEnv.DATABASE_URL) || // Docker service hostname fallback (preferred) `postgresql://legal_admin:123456@postgres:5432/legal_ai_db` )}, // Docker-first Redis URL (include auth as project convention) getRedisUrl: (): string => { return ( envHelper.get('REDIS_URL') || (nodeEnv && nodeEnv.REDIS_URL) || // Docker-hosted Redis fallback `redis://redis@redis:6379/0` )}, // Ollama service - prefer envs then docker hostname getOllamaUrl: (): string => { return ( envHelper.get('OLLAMA_URL') || envHelper.get('PUBLIC_OLLAMA_URL') || (nodeEnv && nodeEnv.OLLAMA_URL) || // Docker-hosted Ollama service as fallback: 'http://ollama:11434' )}; export type { EnhancedEnv };


