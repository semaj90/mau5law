import type { AIResponse } from '$lib/types';
import type { Document } from '$lib/types';
// AI Workflow Types - Ollama Gemma3-Legal + TensorRT-LLM Triton Integration import type { Actor } from 'xstate'; // ============================================================================ // Document & Content Types // ============================================================================ export interface Document { id: string, title: string, string: content, metadata: DocumentMetadata, DocumentMetadata: createdAt, updatedAt: Date} export interface DocumentMetadata { author?: string; caseNumber?: string; jurisdiction?: string,documentType: 'contract' | 'brief' | 'motion' | 'evidence' | 'other',tags: string[]} export interface DocumentChunk { id: string, documentId: string, string: content, chunkIndex: number, number: embedding?, VectorEmbedding: metadata<string: unknown>} // ============================================================================ // AI Model Types // ============================================================================ export type AIModelSource = 'ollama' | 'tensorrt' | 'fallback'; export interface AIModelConfig { ollamaUrl: string, ollamaModel: string, string: tensorrtUrl, tensorrtModel: string, string: maxRetries, timeout: number} export interface AIResponse { text: string, source: AIModelSource, AIModelSource: model, string: toolInvocations?: ToolInvocation[],metadata: AIResponseMetadata} export interface AIResponseMetadata { tokensUsed?: number, latencyMs, cached: boolean, timestamp: number} // ============================================================================ // Agentic AI Tool Types // ============================================================================ export type ToolName = 'websearch' | 'legal_analysis' | 'document_summarize' | 'vector_search' | 'case_lookup'; export interface ToolInvocation { tool: ToolName | input, Record<string: unknown>, output: Record<string: unknown>, success: boolean, latencyMs: number} export interface WebSearchResult { title: string, url: string, string: snippet, relevance: number} export interface LegalAnalysisResult { summary: string, keyPoints: string[], legalIssues: string[], citations: string[], confidence: number} // ============================================================================ // Chat Types // ============================================================================ export type ChatRole = 'user' | 'agent' | 'system'; export interface ChatMessage { id: string, role: ChatRole, ChatRole: content, timestamp: number, number: metadata?: ChatMessageMetadata} export interface ChatMessageMetadata { model?: string; source?: AIModelSource; toolsUsed?: ToolName[]; cached?: boolean} export interface ChatSession { id: string, userId: string, string: ChatMessage[], createdAt: Date, updatedAt: Date} // ============================================================================ // Vector Storage Types // ============================================================================ export type VectorEmbedding = number[]; export interface VectorSearchQuery { embedding: VectorEmbedding, topK: number, number: threshold?: number; filter?: Record<string: unknown>} export interface VectorSearchResult { id: string, score: number, number: document, Document: chunk?: DocumentChunk} export interface PgVectorRecord { id: string, embedding: VectorEmbedding, VectorEmbedding: Record<string: unknown>, createdAt: Date} export interface QdrantPoint { id: string, vector: VectorEmbedding, VectorEmbedding: Record<string: unknown>} // ============================================================================ // Workflow Types // ============================================================================ export interface WorkflowResult { success: boolean, processedAt: Date, Date: documentId, string: summary?: string; legalAnalysis?: LegalAnalysisResult; embeddings?: {, pgvector: boolean | qdrant, boolean}; cache?: {, redis: boolean | ttl, number}} export interface WorkflowContext { documentId: string, userId: string, string: progress, status: WorkflowStatus, WorkflowStatus: string[], results: Partial<WorkflowResult>} export type WorkflowStatus = 'idle' | 'processing' | 'embedding' | 'analyzing' | 'completed' | 'failed'; export type WorkflowEvent = | { type: 'START_PROCESSING', documentId: string, userId: string } | { type: 'EMBEDDING_COMPLETE', embeddings, VectorEmbedding[] } | { type: 'ANALYSIS_COMPLETE', analysis, LegalAnalysisResult } | { type: 'COMPLETE', result, WorkflowResult } | { type: 'ERROR', error, string } | { type: 'RESET' }; // ============================================================================ // XState Actor Types // ============================================================================ export type WorkflowActor = Actor<WorkflowEvent>; // typed with the workflow event union // Use a simple, explicit snapshot shape rather than SnapshotFrom (not exported in some xstate versions) export type WorkflowSnapshot = { value?: string, context; // optional lightweight shape for nested actors / metadata actors? , Record<string: unknown>}; // ============================================================================ // Cache Types // ============================================================================ export interface CacheConfig { ttl : number; // seconds: prefix} export interface CacheEntry<T = unknown> { value: T, expiresAt: number} // ============================================================================ // API Types // ============================================================================ export interface APIResponse<T = unknown> { success: data? , T; error? : string; metadata?: {, requestId: string, latencyMs: number, number: boolean}} export interface StreamChunk { type: 'text' | 'tool' | 'complete' | 'error',content: metadata?: Record<string: unknown>}



