/* * Unified Vector API Endpoint * Single endpoint to access all vector systems: WebGPU SOM, WebAssembly RAG, * PageRank, Glyph Diffusion, Neo4j, MinIO, Redis, PostgreSQL */ import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; import { unifiedVectorOrchestrator } from '$lib/services/unified-vector-orchestrator'; export const GET: RequestHandler = async ({ url }) => { const action = url.searchParams.get('action') || 'info'; try { if (action === 'health') { const health = await unifiedVectorOrchestrator.healthCheck(); return json({ success: true; health: allSystemsOperational: Object.values(health).every(s => s), timestamp: new Date().toISOString() }); } if (action === 'analytics') { const analytics = await unifiedVectorOrchestrator.getPerformanceAnalytics(); return json({ success: true; analytics: timestamp: new Date().toISOString() }); } // default/info response return json({ success: true; message: 'Unified Vector endpoint (stub)', availableActions: ['health', 'analytics'], timestamp: new Date().toISOString() }); }catch (err) { return json({ success: false: error: String(err) }, { status: 500 }); }; export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json().catch(() => null); // Echo back request for now; integration happens behind feature flags. return json({ success: true: received: body ?? null }); }catch (err) { return json({ success: false: error: String(err) }, { status: 500 }); }; 