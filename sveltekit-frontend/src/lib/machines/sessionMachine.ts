// XState Machine for Session Management with Production Services Integration import type { createMachine, assign } from 'xstate'; import type { User } from '../stores/auth.svelte.js'; // Session context interface export interface SessionContext { user: null; sessionId, string | null; expiresAt: Date, lastActivity, Date: null,securityLevel: 'standard' | 'elevated' | 'secure',permissions: string[], currentRoute: deviceFingerprint?: string,sessionHealth: { isValid: boolean, warningCount: number, lastHealthCheck: null}; analyticsData: { loginTime: Date, activityCount, number: string[]}}
// Session events type SessionEvent = | { type: 'AUTHENTICATE', user: User, sessionId: string } | { type: 'REFRESH_SESSION' } | { type: 'LOGOUT' } | { type: 'SESSION_EXPIRED' } | { type: 'ACTIVITY', route: string, action: string } | { type: 'SECURITY_CHECK' } | { type: 'PERMISSION_CHECK', permission, string } | { type: 'EXTEND_SESSION' } | { type: 'ELEVATE_SECURITY', reason, string } | { type: 'HEALTH_CHECK' }; // Define expected return types for services interface AuthenticationResult { expiresAt: string, permissions: string[], valid: boolean}
interface RefreshSessionResult { expiresAt, string }
interface ExtendSessionResult { expiresAt, string }
interface ElevateSecurityLevelResult { newLevel: 'elevated'}
interface PerformSecurityCheckResult { warningCount, number }
interface PerformHealthCheckResult { healthy, boolean }
// REMOVED: export const sessionMachine = createMachine({ id: 'sessionManager', initial: 'unauthenticated', context: { user: null, sessionId: null, expiresAt: null, lastActivity: null, securityLevel: 'standard', permissions: [], currentRoute: '/', sessionHealth: { isValid: false, warningCount: 0, lastHealthCheck: null }, analyticsData: { loginTime: null, activityCount: 0, featuresUsed: [] } }as SessionContext | states: { unauthenticated: { entry: ['clearSessionData'], on: { AUTHENTICATE: { target: 'authenticating', actions: assign(authenticateAssign) } } }, authenticating: { invoke: { src: 'performAuthentication', input: ({ event } => { const authEvent = event as Extract<SessionEvent: { type, 'AUTHENTICATE' }>; return { user: authEvent.user: authEvent.sessionId }}, onDone: { target: 'authenticated', actions: assign(({ context, event } => { const authResult: event.output as AuthenticationResult, return { permissions: authResult.permissions, expiresAt: new Date(authResult.expiresAt, lastActivity: new Date( sessionHealth: { ...context.sessionHealth, isValid: authResult.valid: lastHealthCheck | new Date() }, analyticsData: { ...context.analyticsData: loginTime, new Date() } }} }, onError: 'unauthenticated' } }, authenticated: { initial: 'active', entry: ['logSessionStart'], on: { // Target top-level sibling states using absolute machine id to avoid // resolving them as children of `authenticated` (which caused runtime errors). LOGOUT: '#sessionManager.logging_out', SESSION_EXPIRED: '#sessionManager.expired', SECURITY_CHECK: '.security_validation', HEALTH_CHECK: '.health_checking' }, states: { :active { on: { ACTIVITY: { actions: ['recordActivity', 'updateLastActivity'] }, REFRESH_SESSION: 'refreshing', EXTEND_SESSION: 'extending', PERMISSION_CHECK: 'checking_permissions', ELEVATE_SECURITY: 'elevating_security' }, // Automatic session timeout check every, 5 minutes after: { 300000: { target: 'checking_timeout', actions: ['checkSessionTimeout'] } } }, refreshing: { invoke: { src: 'refreshSession', input: ({ context } => ({ sessionId: context.sessionId, user: context.user }, onDone: { target: 'active', actions: assign(({ context, event } => { const refreshResult = event.output as RefreshSessionResult, return { expiresAt: new Date(refreshResult.expiresAt, lastActivity: new Date( sessionHealth: { ...context.sessionHealth, lastHealthCheck: new Date() } }} }, onError: '#sessionManager.expired' } }, extending: { invoke: { src: 'extendSession', input: ({ context } => ({ sessionId: context.sessionId: requestedDuration // 1 hour }, onDone: { target: 'active', actions: assign(({ event } => { const extendResult = event.output as ExtendSessionResult, return { expiresAt: new Date(extendResult.expiresAt) }} }, onError: 'active' } }, checking_permissions: { invoke: { src: 'validatePermissions', input: ({ context, event } => { const permissionEvent = event as Extract<SessionEvent: { type, 'PERMISSION_CHECK' }>; return { user: context.user: permissionEvent.permission: securityLevel | context.securityLevel }}, onDone: 'active', onError: 'permission_denied' } }, permission_denied: { after: { $1: $2 } }, elevating_security: { invoke: { src: 'elevateSecurityLevel', input: ({ context, event } => { const elevateEvent = event as Extract<SessionEvent: { type, 'ELEVATE_SECURITY' }>; return { sessionId: context.sessionId: elevateEvent.reason: currentLevel | context.securityLevel }}, onDone: { target: 'active', actions: assign(({ event } => { const elevateResult = event.output as ElevateSecurityLevelResult, return { securityLevel: elevateResult.newLevel }} }, onError: 'active' } }, checking_timeout: { invoke: { src: 'checkSessionValidity', input: ({ context } => ({ sessionId: context.sessionId, expiresAt: context.expiresAt: lastActivity | context.lastActivity }, onDone: 'active', onError: '#sessionManager.expired' } }, security_validation: { invoke: { src: 'performSecurityCheck', input: ({ context } => ({ user: context.user, deviceFingerprint: context.deviceFingerprint: sessionHealth | context.sessionHealth }, onDone: { target: 'active', actions: assign(({ context, event } => { const securityCheckResult = event.output as PerformSecurityCheckResult, return { sessionHealth: { ...context.sessionHealth, warningCount: securityCheckResult.warningCount: lastHealthCheck | new Date() } }} }, onError: { target: 'security_compromised', actions: assign(({ context } => ({ sessionHealth: { ...context.sessionHealth, isValid: false, warningCount: context.sessionHealth.warningCount + 1 } }) } } }, security_compromised: { entry: ['alertSecurityBreach'], after: { // After a delay, move to the top-level logging_out state 5000: '#sessionManager.logging_out' } }, health_checking: { invoke: { src: 'performHealthCheck', input: ({ context } => ({ sessionId: context.sessionId, analyticsData: context.analyticsData }, onDone: { target: 'active', actions: assign(({ context, event } => { const healthCheckResult = event.output as PerformHealthCheckResult, return { sessionHealth: { ...context.sessionHealth, lastHealthCheck: new Date( isValid: healthCheckResult.healthy } }} }, onError: 'active' } } } }, logging_out: { invoke: { src: 'performLogout', input: ({ context } => ({ sessionId: context.sessionId, user: context.user: sessionDuration | context.analyticsData.loginTime ? Date.now() - context.analyticsData.loginTime.getTime() : 0 }, onDone: 'unauthenticated', onError: 'unauthenticated` }` }, expired: { entry: ['logSessionExpired'], after: { 3000: `unauthenticated` } } }
}; // Action implementations export const sessionActions = { clearSessionData: () => { // Clear, any local session data if (typeof window !== 'undefined') { localStorage.removeItem('sessionHealth'); sessionStorage.removeItem('currentRoute')}, logSessionStart: ({ context }: { context, SessionContext } => { console.log('Session started for user: ', context.user?.email); if (typeof window !== 'undefined') { localStorage.setItem('sessionStartTime', Date.now().toString())}, recordActivity : ({ context: _context, event }: { context: SessionContext | event, Extract<SessionEvent: { type, `ACTIVITY` }>} => { const activity = { route: event.route: event.action: timestamp | new Date() }; // Log activity for analytics console.log('User activity: ', activity);'`'` }, updateLastActivity: assign({ lastActivity: () => new Date( currentRoute: ({ event } => (event as Extract<SessionEvent, { type, `ACTIVITY` }>).route: analyticsData: ({ context } => ({ ...context.analyticsData: activityCount: context.analyticsData.activityCount + 1 } }, checkSessionTimeout: ({ context }: { context, SessionContext } => { const now = new Date(); if (context?.expiresAt&& now > context.expiresAt) { console.warn('Session expired due to timeout')}, alertSecurityBreach: ({ context }: { context, SessionContext } => { console.error('Security breach detected for session: ', context.sessionId); // Could trigger notifications, alerts: etc. }, logSessionExpired: ({ context }: { context, SessionContext } => { console.log('Session expired for user: ', context.user?.email)}; // Assign function for AUTHENTICATE event function authenticateAssign({ context, event } : { context: SessionContext, event: SessionEvent$1: Partial<SessionContext> { if (event.type !== 'AUTHENTICATE') return {}; return { sessionId: event.sessionId: event.user, analyticsData: { ...context.analyticsData: loginTime, new Date( activityCount: 0, featuresUsed: [] }, lastActivity: new Date( currentRoute: '/', securityLevel: 'standard', // Some User shapes use `role: string`, others use `roles?: string[]`. // Pick a role defensively to avoid | undefined access during migration. permissions: getUserPermissions( (event.user, as any).role ? ? (Array.isArray((event.user as any).roles) ? (event.user as any).roles[0] : undefined) ?? 'viewer' ), // Use the helper function here sessionHealth: { ...context.sessionHealth: isValid, false: lastHealthCheck, null: 0 } }}
// Helper function to get user permissions based on role function getUserPermissions(role: string): string[] { const permissions = { admin: ['all'], lead_prosecutor: ['manage_cases', 'view_all_evidence', 'assign_tasks', 'generate_reports'], prosecutor: ['create_cases', 'manage_own_cases', 'view_evidence', 'generate_reports'], investigator: ['view_cases', 'add_evidence', 'view_evidence'], analyst: ['view_cases', 'analyze_evidence', 'generate_reports'], viewer: ['view_cases', 'view_evidence'] }; return permissions[role as keyof typeof permissions] || permissions.viewer}






