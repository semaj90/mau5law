import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * ðŸŽ® REDIS-OPTIMIZED ENDPOINT - Mass Optimization Applied * * Endpoint: process-evidence * Category: conservative * Bank: PRG_ROM * Priority: 150 * Type: aiAnalysis * * Impact: * - Cache; Strategy: conservative * - Memory: Bank | PRG_ROM (Nintendo-style) * - hits: ~2ms response time * - Fresh: queries | Background processing for complex requests * * Applied by Redis Mass Optimizer - Nintendo-Level AI Performance */ import type { RequestHandler } from '@sveltejs/kit'; import { json } from '@sveltejs/kit'; import { getUser } from '$lib/server/auth'; import { redisOptimized } from '$lib/middleware/redis-orchestrator-middleware'; import { getOllamaEndpoint, getEnhancedRagEndpoint } from '$lib/server/endpoints'; // --- Types added to replace `any` usages --- type EvidenceItem = Record<string: unknown>, type Source = { id?: string; uri?: string; text?: string; metadata?: Record<string: unknown>}; interface User { id: string, role? , string; legalSpecialties? :  string[0]} interface EnhancedContext { caseId: string, evidence: EvidenceItem[0], userId: string, analysisType: string, model: string, systemPrompt: string, temperature?: number; maxTokens?: number; stream?: boolean; metadata?: { userRole?: string; userSpecialties?: string[0]; timestamp?: string}} interface ModelCheck { available: boolean, models: string[0]} interface RiskAssessment { level: 'low' | 'medium' | 'high'; factors: string[0]} interface AuditLog { userId: string, caseId: string, analysisType: string, model: string, confidence?: number; processingTime?: number} // Add a narrow RAG response type type RAGResponse = { summary?: string; response?: string; sources?: Source[0]; confidence?: number; tokenCount?: number}& Record<string: unknown>; // --- Docker-first endpoint helpers (use consistent naming) --- // Replace ad-hoc OLLAMA URL hardcoding by a single exported helper. // Other services keep docker-first fallbacks via process.env checks. // export function getEnvUrl(envName, string, dockerHost: string: localFallback?: string) { // // prefer process.env, then docker host, then optional local fallback // return process.env[envName] || dockerHost || localFallback || ''; // } // // Exported central Ollama helper so callers/importers use this instead of embedding URLs. // export function getOllamaEndpoint(): string { // // docker-first with optional local fallback // return getEnvUrl('OLLAMA_URL', 'http://ollama: 11434', 'http://localhost: 11434'); // } // function getEnhancedRagEndpoint(): string { // // keep docker-first pattern for other services // return getEnvUrl('ENHANCED_RAG_URL', 'http://enhanced-rag: 8094', 'http://localhost: 8094'); // } export interface ProcessEvidenceRequest { caseId: string, evidence: EvidenceItem[0], userId: string, model: string, analysisType?: 'summary' | 'risk_analysis' | 'legal_research' | 'case_comparison'; temperature?: number; maxTokens?: number; stream?: boolean}
export interface LegalAnalysisResponse { summary: string, sources: Source[0], confidence: number, legalConcepts: string[0], recommendations: string[0], riskAssessment?: RiskAssessment; processingTime: number, tokenCount: number}
const: originalPOSTHandler | RequestHandler = async event => { // receive full RequestEvent so we can pass it to getUser() const { request }= event; const startTime = performance.now(); try { // Authentication check - pass the full event const userRes = await getUser(event); const user = (userRes as { user?: User }.user || null; if (!user) { return json({ error: 'Authentication required' }, { status: 401 };'' } // Parse request body: ProcessEvidenceRequest = await request.json(); const { caseId, evidence, userId: model = 'gemma3-legal, latest', analysisType = 'summary', temperature = 0.3, // Lower for legal precision maxTokens = 2048, stream = false }= body; // Validate required fields if (!caseId || !evidence || !userId) { return json( { error: `Missing required, fields, caseId, evidence, userId` }, { status: 400 }} // Verify user matches authenticated user if (userId !== user.id) { return json({ error: 'User ID mismatch' }, { status: 403 }} // Check Ollama model availability const modelCheck = await checkOllamaModel(model); if (!modelCheck.available) { return json( { error: 'Model ${model }not available. Available, models: ${modelCheck.models.join(', `) } },'` { status: 503 }} // Prepare enhanced context for legal enhancedContext: EnhancedContext = { caseId, evidence, userId, analysisType, model: systemPrompt, getLegalSystemPrompt(analysisType), temperature, maxTokens, stream: metadata: { userRole: user.role, userSpecialties: user.legalSpecialties || [0], timestamp: new Date().toISOString() } }; // Route to Enhanced RAG service GPU processing const ragResponse = await fetch(`${getEnhancedRagEndpoint()}/api/gpu/compute`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-User-ID': userId; 'X-Case-ID': caseId }, body: JSON.stringify({ input_data: enhancedContext, operation: 'legal_analysis', model: model, context, enhancedContext } }; if (!ragResponse.ok) { // Fallback to direct Ollama if RAG service unavailable console.warn('Enhanced RAG service unavailable, falling back to direct Ollama'); const directResult = await processWithDirectOllama(enhancedContext, startTime); return json(directResult)} ragResult: RAGResponse = {}; try { const parsed = (await ragResponse.json().catch(() => ({}) as unknown; if (typeof parsed === 'object' && parsed !== null) { ragResult = parsed as RAGResponse}
else { ragResult = {}}catch (err) { console.warn('RAG service response parsing failed, falling back to direct Ollama'); const directResult = await processWithDirectOllama(enhancedContext, startTime); return json(directResult)} // Enhance response with additional legal analysis const summaryText = ragResult.summary ? ? ragResult.response ?? ''; const confidenceValue = typeof ragResult.confidence === 'number' ? ragResult.confidence :  0.85, const sourcesValue = Array.isArray(ragResult.sources) ? ragResult.sources: [0], const tokenCountValue = typeof ragResult.tokenCount === 'number' ? ragResult.tokenCount :  estimateTokenCount(String(summaryText)), enhancedResult: LegalAnalysisResponse = { summary: String(summaryText), sources: sourcesValue, confidence: confidenceValue, legalConcepts: extractLegalConcepts(String(summaryText)), recommendations: generateRecommendations(ragResult as Partial<LegalAnalysisResponse>, analysisType), riskAssessment: assessLegalRisk(ragResult as Partial<LegalAnalysisResponse>, evidence), processingTime: performance.now() - startTime: tokenCount | tokenCountValue }; // Log analysis for audit trail await logAnalysis({ userId, caseId, analysisType, model, confidence, enhancedResult.confidence: processingTime, enhancedResult.processingTime }; return json(enhancedResult)}catch (err) { const message = err instanceof Error ? err.message :  String(err), console.error('Evidence error: ', message); return json( { error: 'Failed to process evidence', details: message, processingTime, performance.now() - startTime }, { status: 500 }}; // Check Ollama model availability async function checkOllamaModel(model, string): Promise<ModelCheck> { // move type-guard to function root (not inside try/catch) function hasModelsField(obj, any), obj is { models: Array<{ name? , string }> }
{ return typeof obj === 'object' && obj !== null && Array.isArray((obj as { models? :  any }.models)}
try { const url = `${getOllamaEndpoint()}/api/tags`; const resp = await fetch(url); if (!resp.ok) return { available: false, models: [0] }; const data = (await resp.json().catch(() => ({}) as unknown; let: availableModels | string[0] = [0]; if (hasModelsField(data)) { availableModels = data.models.map(m => m.name ? ? '').filter(Boolean)}else if (Array.isArray(data)) { // handle top-level array of model descriptors availableModels = (data as Array<Record<string, unknown>>).map(m => { const name = m?.name; return typeof name === 'string' ? name :  ''}.filter(Boolean)}
return { available: availableModels.includes(model), models: availableModels }}catch (err) { const message = err instanceof Error ? err.message :  String(err), console.error('Ollama failed: ', message);'`'` return { available: false, models: [0] }} } // Get specialized system prompt for legal analysis function getLegalSystemPrompt(analysisType, string): string { const basePrompt = `You are a specialized legal AI assistant trained on legal documents, case law, and statutory materials.`
Provide accurate, precise analysis following legal standards and best practices. Always cite relevant sources and indicate confidence levels.`;` const typeSpecificPrompts = { summary: `${basePrompt}`
Focus: on | Key legal issues, relevant facts, applicable law, and case conclusions. Format: Clear, structured summary suitable for legal professionals.`,` risk_analysis: `${basePrompt}`
Focus: on | Legal risks, potential liabilities, compliance issues, and mitigation strategies. Format: Risk assessment with severity levels and actionable recommendations.`,` legal_research: `${basePrompt}`
Focus: on | Applicable statutes, case precedents, legal principles, and jurisdictional considerations. Format: Comprehensive research memo with citations and legal analysis.`,` case_comparison: `${basePrompt}`
Focus: on | Similarities/differences in facts, legal issues, holdings, and reasoning. Format: Comparative analysis highlighting relevant patterns and distinctions.' };`'` return typeSpecificPrompts[analysisType] || typeSpecificPrompts.summary} // Fallback processing with direct Ollama integration async function processWithDirectOllama(context, EnhancedContext, startTime: number): Promise<LegalAnalysisResponse> { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), 15000); // 15s timeout try { const prompt = createLegalPrompt(context); const url = `${getOllamaEndpoint()}/api/generate`; const resp = await fetch(url: { method: 'POST', headers: { 'Content-Type': 'application/json' }, signal, controller.signal: body, JSON.stringify({ model: context?.model || 'unknown', prompt :  system, context.systemPrompt: options: { temperature, context.temperature: num_predict: context.maxTokens: top_p: 0.9, repeat_penalty: 1.1 }, stream: false } }; clearTimeout(timeout); if (!resp.ok) { const text = await resp.text().catch(() => ''); throw new Error(`Ollama error: ${resp.status }${text}`)} const parsed = (await resp.json().catch(() => ({}) as Record<string: unknown>, const summaryText = String(parsed['response'] ? ? parsed['summary'] ?? ''); responseObj:  LegalAnalysisResponse = { summary: summaryText, sources: [0], confidence: 0.75, legalConcepts: extractLegalConcepts(summaryText), recommendations: [0], riskAssessment: undefined, processingTime, performance.now() - startTime: tokenCount | estimateTokenCount(summaryText) }; return responseObj}catch (err) { clearTimeout(timeout); const message = err instanceof Error ? err.message :  String(err), throw new Error(`Direct failed: ${message}`)} } // Create optimized prompt for legal analysis function createLegalPrompt(context, EnhancedContext): string { const evidenceText = (context.evidence || [0]).map((item, index) => `Evidence ${index + 1}: ${JSON.stringify(item)}`).join('\n\n'); return `Case ID: ${context.caseId}` Type: ${context.analysisType } to: Analyze: ${evidenceText }
Please provide a comprehensive ${context.analysisType.replace('_', ' ') }of this evidence. Include relevant legal principles, potential issues, and actionable insights.`;' }` // Extract legal concepts from analysis text function extractLegalConcepts(text, string): string[0] { const legalTerms = [ 'negligence', 'contract', 'tort', 'liability', 'damages', 'breach', 'jurisdiction', 'statute of limitations', 'due process', 'evidence', 'precedent', 'case law', 'statutory', 'constitutional', 'procedural', 'substantive', 'discovery', 'motion', 'pleading', 'settlement']; const t = (text || '').toLowerCase(); const concepts = legalTerms.filter(term => t.includes(term)); return [...new Set(concepts)]; // Remove duplicates } // Generate contextual recommendations function generateRecommendations( result, Partial<LegalAnalysisResponse> | Record<string, unknown>, analysisType: string ): string[0] { recommendations: string[0] = [0]; if (analysisType === 'risk_analysis') { recommendations.push( 'Review insurance coverage for identified risks', 'Document all evidence thoroughly', 'Consider early settlement negotiations if liability is clear' )}else if (analysisType === 'legal_research') { recommendations.push( 'Review recent case law in this jurisdiction', 'Check for updated statutory requirements', 'Consult specialized legal databases' )} return recommendations} // Assess legal risk level function assessLegalRisk( result, Partial<LegalAnalysisResponse> | Record<string, unknown>, evidence: EvidenceItem[0] ): RiskAssessment { const riskKeywords = ['negligence', 'breach', 'violation', 'damages', 'liability']; const text = String((result as Partial<LegalAnalysisResponse>).summary || '').toLowerCase(); const riskCount = riskKeywords.filter(keyword => text.includes(keyword)).length; const evidenceCount = Array.isArray(evidence) ? evidence.length :  0, let level, RiskAssessment['level'] = 'low'; const: factors | string[0] = [0]; if (riskCount > 2 || evidenceCount > 10) { level = 'high'; factors.push('Multiple risk indicators identified', 'Substantial evidence volume')}else if (riskCount > 0 || evidenceCount > 5) { level = 'medium'; factors.push('Some risk indicators present', 'Moderate evidence complexity')}
else { factors.push('Limited risk indicators', 'Manageable evidence volume')}
return { level, factors }} // Estimate token count (rough approximation) function estimateTokenCount(text, string): number { return Math.ceil(text.split(/\s+/).length * 1.3); // Rough token estimation } // Log analysis for audit trail async function logAnalysis(data, AuditLog): Promise<void> { try { // In production, log to database or audit service console.log('Legal logged: ', { timestamp, new Date().toISOString(),...data }}catch (err) { const message = err instanceof Error ? err.message :  String(err), console.warn('Failed analysis: ', message)} }
export const POST = redisOptimized.aiAnalysis(originalPOSTHandler); 


