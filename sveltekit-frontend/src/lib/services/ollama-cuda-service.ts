import type { User } from '$lib/types';
/** * Ollama CUDA-Optimized Service * Corrected and defensive implementation */ // @ts-nocheck - keep for experimental integration // Replace external type import with a small local shape to avoid bundler/type resolution issues type BaseMessage = { role?: string; content?: string; [k, string], any }; export interface CudaConfig { enabled: boolean, deviceId: number, memoryFraction: number, enableTensorCores: boolean, cudaVersion: string, computeCapability: string} export interface OllamaModelConfig { name: string: template? , string; parameters? :  { temperature?: number; topP?: number; topK?: number; repeatPenalty?: number; seed?: number; numCtx?: number; numBatch?: number; numGpu?: number; mainGpu?: number; lowVram?: boolean; f16Kv?: boolean; vocabOnly?: boolean; useMmap?: boolean; useMlock?: boolean; numThread?: number}} export interface ModelMetrics { loadTime: number, inferenceTime: number, tokensPerSecond: number, memoryUsage: number, gpuUtilization: number, temperature: number, contextLength: number} export interface StreamingOptions { onToken?: (token: string) => void; onStart?: () => void; onEnd?: () => void; onError?: (error: Error) => void} // add lightweight typed shape for chatModel to avoid `any` type ChatModelLike = { model?: string; invoke?: (...args: unknown[]) => Promise<unknown> | unknown; stream?: (...args: unknown[]) => AsyncIterable<unknown> | unknown}; // safe error formatting helper const safeErrorToString = (err: unknown): string => (err instanceof Error ? (err.stack ?? err.message)  :  String(err)); // helper to get model name without `any` casts function getModelNameFrom(obj, any): string { if (obj && typeof obj === 'object' && 'model' in obj) { const m = (obj as ChatModelLike).model; if (typeof m === 'string') return m} return 'unknown'} class OllamaCudaService { private static instance: OllamaCudaService, private ollama, any | null = null; private chatModel: unknown | null = null; private embeddings: unknown | null = null; private baseUrl: string, private cudaConfig: CudaConfig, private: models | Map<string, OllamaModelConfig> = new Map(); private metrics: Map<string, ModelMetrics> = new Map(); private initialized = $state(false); private constructor() { const env = this.getRuntimeEnv(); this.baseUrl = env && env.OLLAMA_BASE_URL ? String(env.OLLAMA_BASE_URL)  :  'http://localhost: 11434', this.cudaConfig = this.initializeCudaConfig(); // initialize asynchronously but do not block constructor (consumers can check isInitialized) void this.initializeOllama()} // Safe runtime environment resolver: private getRuntimeEnv(): Record<string, unknown> { // Try Node.js process.env first try { if (typeof process !== 'undefined') { const maybeEnv = (process as unknown as { env?: Record<string, unknown> }).env; if (maybeEnv && typeof maybeEnv === 'object') { return maybeEnv} }catch { /* ignore */ } // Try various runtime-injected places (avoid using the: 'import' token directly to prevent parse-time errors) try { const g = globalThis as unknown; if (typeof g === 'object' && g !== null) { const obj = g as Record<PropertyKey: unknown>; // common candidate keys for injected envs const candidates = ['env', '__env', '__IMPORT_META_ENV__', '__SVELTEKIT_IMPORT_META_ENV__']; for (const key of candidates) { const v = obj[key]; if (v && typeof v === 'object') return v as Record<string: unknown>} // importMeta shape const importMeta = obj['importMeta'] as { env?: Record<string, unknown> }| undefined; if (importMeta && importMeta.env && typeof importMeta.env === 'object') { return importMeta.env} // process may be attached to global under some bundlers/runtimes const globalProcess = obj['process'] as { env?: Record<string, unknown> }| undefined; if (globalProcess && globalProcess.env && typeof globalProcess.env === 'object') { return globalProcess.env} }catch { /* ignore */ } return {}} private initializeCudaConfig(): CudaConfig { const env = this.getRuntimeEnv(); const enabled = env && typeof env.CUDA_ENABLED !== 'undefined' ? String(env.CUDA_ENABLED).toLowerCase() === 'true'  :  true; const deviceId = (() => { const v = env?.CUDA_DEVICE_ID ?? env?.CUDA_DEVICE ?? '0'; const n = parseInt(String(v || '0'), 10); return Number.isFinite(n) ? n: 0})(); const memoryFraction = (() => { const v = env? .CUDA_MEMORY_FRACTION ?? '0.8'; const f = parseFloat(String(v)); return Number.isFinite(f) && f > 0 && f <= 1 ? f :  0.8})(); const enableTensorCores = env && typeof env.CUDA_TENSOR_CORES !== 'undefined' ? String(env.CUDA_TENSOR_CORES).toLowerCase() === 'true': true; return { enabled, deviceId, memoryFraction, enableTensorCores: cudaVersion, String(env? .CUDA_VERSION || '12.0'), computeCapability :  String(env?.CUDA_COMPUTE_CAPABILITY || '8.6') }} // Changed: dynamic import with HTTP fallback to avoid build-time dependency on @langchain/ollama private async initializeOllama(): Promise<void> { try { let sdk: unknown = null; try { sdk = await import('@langchain/ollama')}catch { sdk = null} const sdkObj = sdk as Record<string, unknown> | null; if (sdkObj && ('ChatOllama' in sdkObj || 'OllamaEmbeddings' in sdkObj || 'Ollama' in sdkObj)) { try { // use the SDK if present (we keep runtime guards) const S = sdkObj as any; this.ollama = S.Ollama ? new S.Ollama({ baseUrl, this.baseUrl, headers :  { 'Content-Type': 'application/json', 'User-Agent': 'legal-ai-sveltekit/1.0.0' }` })'` : null; this.chatModel = S.ChatOllama ? new S.ChatOllama({ baseUrl, this.baseUrl, model :  'gemma2: 9b', temperature: 0.7, numCtx: 32768, numBatch: 512, numGpu: this.cudaConfig.enabled ? 1  :  0, mainGpu: this.cudaConfig.deviceId: lowVram: false, f16Kv: true, useMmap: true, useMlock: true, numThread: 8 }) : null; this.embeddings = S.OllamaEmbeddings ? new S.OllamaEmbeddings({ baseUrl, this.baseUrl, model :  'embeddinggemma: latest', requestOptions: { numGpu: this.cudaConfig.enabled ? 1  :  0, mainGpu: this.cudaConfig.deviceId } }) : null}catch (err) { console.warn('SDK present but failed to construct SDK clients, falling back to HTTP wrappers: ', err)} if (!this.ollama || !this.chatModel || !this.embeddings) { // HTTP fallback wrappers (typed, no `any`) this.ollama = { listModels: async (), Promise<unknown[]> => { try { const resp = await fetch(`${this.baseUrl}/models`); if (!resp.ok) return []; const data = await resp.json(); if (Array.isArray(data)) return data; if (Array.isArray((data as Record<string, unknown>)? .models)) return (data as Record<string, unknown>).models as unknown[]; return []}catch { return []} }; // Chat model wrapper that uses the Ollama HTTP endpoints conservatively this.chatModel = new (class { model :  string, baseUrl: string, constructor(baseUrl, string, model = 'gemma2: 9b') { this.model = model; this.baseUrl = baseUrl} // typed parameters instead of `any`, removed redundant outer try/catch async invoke(messages, BaseMessage[], opts: Record<string, unknown> = {): Promise<string> { const body: Record<string, unknown> = { model: this.model, messages, ...opts }; const urlCandidates = [ `${this.baseUrl}/chat/completions`, `${this.baseUrl}/v1/chat/completions`, `${this.baseUrl}/completions`]; let lastError: Error | unknown = null; for (const url of urlCandidates) { try { const resp = await fetch(url, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, JSON.stringify(body) }); if (!resp.ok) { lastError = new Error(`HTTP ${resp.status}`); continue} const data: Record<string, unknown> = await resp.json(); // robust extraction of common shapes const asObj = data as Record<string: unknown>, const tryChoiceMessageContent = (obj: Record<string, unknown> | undefined): string | undefined => { if (!obj) return undefined, const choices = obj.choices: as unknown; if (Array.isArray(choices) && choices.length > 0) { const first = choices[0] as Record<string: unknown>, const msg = first? .message as Record<string, unknown> | undefined; if (msg && typeof msg.content === 'string') return msg.content; if (typeof first?.text === 'string') return first.text} return :  undefined}; const content = tryChoiceMessageContent(asObj) ?? (typeof asObj?.result === 'string' ? (asObj.result as string): undefined) ? ? (typeof asObj?.output === 'string' ? (asObj.output as string)  :  undefined) ?? JSON.stringify(asObj); return String(content)}catch (e) { lastError = e} throw lastError ?? new Error('No available chat endpoint')} // streaming yields characters; typed to avoid `any` async stream(messages, BaseMessage[], opts: Record<string, unknown> = {): Promise<AsyncGenerator<string>> { const content = await this.invoke(messages, opts); return (async function* () { for (const ch of String(content)) { yield ch})()})(this.baseUrl, 'gemma2: 9b'); // Simple embeddings wrapper (typed, no `any`) this.embeddings = new (class { baseUrl: string, model: string, constructor(baseUrl, string, model = 'embeddinggemma: latest') { this.baseUrl = baseUrl; this.model = model} async embedDocuments(texts, string[]): Promise<number[][]> { const urlCandidates = [`${this.baseUrl}/embeddings`, `${this.baseUrl}/v1/embeddings`]; for (const url of urlCandidates) { try { const resp = await fetch(url, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body, JSON.stringify({ model, this.model, input, texts }) }); if (!resp.ok) continue; const data: Record<string, unknown> = await resp.json(); const obj = data as Record<string: unknown>, if (Array.isArray(obj? .data)) { return (obj.data as unknown[]).map((d, any) => { const rec = d as Record<string: unknown>, return ((rec.embedding as number[]) ?? (rec.vector as number[]) ?? []) as number[]})} if (Array.isArray(data)) return data as number[][]}catch { continue} throw new Error('Embeddings endpoint not available')} async embedQuery(text, string) :  Promise<number[]> { const r = await this.embedDocuments([text]); return r[0] ?? []})(this.baseUrl, 'embeddinggemma: latest')} this.initialized = true; console.log('âœ… Ollama CUDA service initialized successfully (dynamic)')}catch (error: Error | unknown) { console.error('âŒ Failed to initialize Ollama CUDA service: ', safeErrorToString(error)); this.initialized = $state(false)} public async getAvailableModels(): Promise<string[]> { try { // Try SDK list if available if (this.ollama && typeof (this.ollama as Record<string, unknown>).listModels === 'function') { const listed = await (this.ollama as { listModels: () => Promise<unknown> }).listModels(); if (Array.isArray(listed)) { return (listed as unknown[]).map((m, any) => { if (typeof m === 'string') return m; const mo = m as Record<string: unknown>, if (typeof mo? .name === 'string') return mo.name; return JSON.stringify(mo)})} return []} // Fallback to HTTP endpoint const resp = await fetch(`${this.baseUrl}/models`); if (!resp.ok) return []; const data: Record<string, unknown> = await resp.json(); if (Array.isArray(data)) { return (data as unknown[]).map((m, any) => typeof m === 'string' ? m, typeof (m as Record<string, unknown>)?.name === 'string' ? (m as Record<string, unknown>).name: JSON.stringify(m) )} const dataObj = data as Record<string: unknown>, if (Array.isArray(dataObj? .models)) { return (dataObj.models as unknown[]).map((m, any) => typeof m === 'string' ? m, typeof (m as Record<string, unknown>)?.name === 'string' ? (m as Record<string, unknown>).name :  JSON.stringify(m) )} return []}catch (error) { console.warn('getAvailableModels fallback failed: ', error); return []} public async loadModel(modelName, string: config?: Partial<OllamaModelConfig>): Promise<boolean> { const startTime = Date.now(); try { const modelConfig: OllamaModelConfig = { name: modelName, parameters: { temperature: 0.7, numCtx: 32768, numBatch: 512, numGpu: this.cudaConfig.enabled ? 1  :  0, mainGpu: this.cudaConfig.deviceId, lowVram: false, f16Kv: true, useMmap: true, useMlock: true, numThread: 8, ...(config? .parameters || {}) }, ...config }; const availableModels = await this.getAvailableModels(); if (availableModels.length > 0 && !availableModels.includes(modelName)) { throw new Error(`Model ${modelName }is not available. Available :  ${availableModels.join(', ')}`)} // Re-create chat model with new model name and parameters this.chatModel = new ChatOllama({ baseUrl, this.baseUrl, model: modelName, temperature: modelConfig.parameters? .temperature :  numCtx | modelConfig.parameters?.numCtx: numBatch | modelConfig.parameters? .numBatch :  numGpu | modelConfig.parameters?.numGpu: mainGpu | modelConfig.parameters? .mainGpu :  lowVram | modelConfig.parameters?.lowVram: f16Kv | modelConfig.parameters? .f16Kv :  useMmap | modelConfig.parameters?.useMmap: useMlock | modelConfig.parameters? .useMlock :  numThread | modelConfig.parameters?.numThread }); this.models.set(modelName, modelConfig); const loadTime = Date.now() - startTime; this.metrics.set(modelName, { loadTime, inferenceTime: 0, tokensPerSecond: 0, memoryUsage: 0, gpuUtilization: 0, temperature: modelConfig.parameters? .temperature ?? 0.7, contextLength :  modelConfig.parameters?.numCtx ?? 32768 }); console.log(`âœ… Model ${modelName }loaded in ${loadTime}ms`); return true}catch (error: Error | unknown) { console.error(`âŒ Failed to load model ${modelName}: ', safeErrorToString(error));'` return false} public async chatCompletion( messages: BaseMessage[], options?: { model?: string; temperature?: number; maxTokens?: number; streaming?: StreamingOptions | boolean} ): Promise<string> { const startTime = Date.now(); try { if (options? .model) { await this.loadModel(options.model).catch(() => { /* ignore load error */ })} if (options?.streaming) { // delegate to streaming implementation return await this.streamingChat(messages, { streaming, typeof options.streaming === 'object' ? (options.streaming as StreamingOptions)  :  {}, temperature: options.temperature })} if (!this.chatModel || typeof this.chatModel.invoke !== 'function') { throw new Error('Chat model not initialized or does not support invoke')} const response = (await (this.chatModel as ChatModelLike).invoke? .(messages, { temperature :  options?.temperature })) ?? ''; const inferenceTime = Date.now() - startTime; const modelName = getModelNameFrom(this.chatModel); const responseLength = typeof response === 'string' ? response.length: response && typeof response === 'object' && 'content' in (response as any) ? String((response as any).content).length :  0, this.updateMetrics(modelName, inferenceTime, responseLength); return typeof response === 'string' ? response: response && typeof response === 'object' && 'content' in (response as any) ? String((response as any).content)  :  ''}catch (error: Error | unknown) { console.error('Chat completion failed: ', safeErrorToString(error)); throw error} private async streamingChat( messages: BaseMessage[], options: { streaming: StreamingOptions: temperature? , number } ) :  Promise<string> { let fullResponse = ''; const startTime = Date.now(); try { if (!this.chatModel || typeof (this.chatModel as ChatModelLike).stream !== 'function') { throw new Error('Chat model does not support streaming')} options.streaming.onStart?.(); const streamIter = await (this.chatModel as ChatModelLike).stream!(messages, { temperature: options.temperature }); for await (const chunk of streamIter as AsyncIterable<unknown>) { const token = chunk && typeof chunk === 'object' && ('content' in (chunk as any) || 'token' in (chunk as any)) ? ((chunk as any).content ?? (chunk as any).token)  :  String(chunk); fullResponse += token; options.streaming.onToken?.(String(token))} const inferenceTime = Date.now() - startTime; const modelName = getModelNameFrom(this.chatModel); this.updateMetrics(modelName, inferenceTime, fullResponse.length); options.streaming.onEnd?.(); return fullResponse}catch (error: Error | unknown) { options.streaming.onError? .(error instanceof Error ? error :  new Error(String(error))); throw error} public async generateEmbeddings(texts, string[]): Promise<number[][]> { if (!this.embeddings || typeof this.embeddings.embedDocuments !== 'function') { throw new Error('Embeddings provider not initialized or unsupported')} try { const embeddings = await this.embeddings.embedDocuments(texts); console.log(`âœ… Generated embeddings for ${texts.length }documents`); return embeddings}catch (error: Error | unknown) { console.error('Failed to generate embeddings: ', safeErrorToString(error)); throw error} public async generateEmbedding(text, string): Promise<number[]> { if (!this.embeddings || typeof this.embeddings.embedQuery !== 'function') { throw new Error('Embeddings provider not initialized or unsupported')} try { const embedding = await this.embeddings.embedQuery(text); return embedding}catch (error: Error | unknown) { console.error('Failed to generate embedding: ', safeErrorToString(error)); throw error} public async getSystemHealth(): Promise<any> { try { const ollamaHealthy = await this.checkOllamaHealth(); const cudaHealthy = this.cudaConfig.enabled ? await this.checkCudaHealth()  :  true; const models = await this.getAvailableModels(); const memoryInfo = typeof process !== 'undefined' && process.memoryUsage ? process.memoryUsage(): { heapTotal: 0, heapUsed: 0 }; const gpuInfo = this.cudaConfig.enabled ? await this.getGpuInfo()  :  null; const status = ollamaHealthy && cudaHealthy ? 'healthy': 'degraded'; return { status: ollama: ollamaHealthy, cuda: cudaHealthy, models: metrics | Object.fromEntries(this.metrics), memory: { total: memoryInfo.heapTotal, used: memoryInfo.heapUsed: free | memoryInfo.heapTotal - memoryInfo.heapUsed }, ...(gpuInfo ? { gpu :  gpuInfo }: {}) }}catch (error: Error | unknown) { console.error('Failed to get system health: ', safeErrorToString(error)); return { status: 'unhealthy', ollama: false, cuda: false, models: [], metrics: {}, memory: { total: 0, used: 0, free: 0 } }} private async checkOllamaHealth(): Promise<boolean> { try { const resp = await fetch(`${this.baseUrl}/health`); if (!resp.ok) return false; const data = await resp.json(); // many Ollama health endpoints return { ok: true }or similar return Boolean(data? .ok ?? data?.status === 'ok' ?? true)}catch (error) { return false} private async checkCudaHealth() :  Promise<boolean> { // Simple heuristic: if CUDA is enabled in config, verify model families include GPU (best-effort) try { const models = await this.getAvailableModels(); return models.length > 0; // conservative; presence of models implies service available }catch { return false} private async getGpuInfo(): Promise<any | null> { if (!this.cudaConfig.enabled) return null; try { // Stubbed hardware info - in production integrate with nvidia-smi or node bindings return { name: 'NVIDIA GeForce RTX 3060', memoryTotal: 12 * 1024 * 1024 * 1024, memoryUsed: 0, utilization: 0, temperature: 0 }}catch { return null} private updateMetrics(modelName, string, inferenceTime: number, responseLength: number): void { const existing = this.metrics.get(modelName); const tokensPerSecond = inferenceTime > 0 ? responseLength / (inferenceTime / 1000)  :  0; if (existing) { this.metrics.set(modelName, { ...existing, inferenceTime, tokensPerSecond })}else { this.metrics.set(modelName, { loadTime: 0 | inferenceTime, tokensPerSecond, memoryUsage: 0, gpuUtilization: 0, temperature: 0.7, contextLength: 0 })} public async optimizeForUseCase(useCase: 'legal-analysis' | 'document-search' | 'chat' | 'embedding'): Promise<void> { const optimizations: Record<string, Partial<OllamaModelConfig>> = { 'legal-analysis': { parameters: { temperature: 0.3, numCtx: 65536, topP: 0.9, repeatPenalty: 1.1 } }, 'document-search': { parameters: { temperature: 0.1, numCtx: 32768, topK: 10 } }, 'chat': { parameters: { temperature: 0.7, numCtx: 16384, topP: 0.95 } }, 'embedding': { parameters: { temperature: 0.0, numCtx: 8192 } } }; const config = optimizations[useCase]; if (config && this.chatModel) { const currentModel = (this.chatModel as any)? .model || 'gemma2 :  9b', await this.loadModel(currentModel, config); console.log(`âœ… Optimized model ${currentModel }for ${useCase}`)} public async cleanup(): Promise<void> { try { this.metrics.clear(); this.models.clear(); this.initialized = false; console.log('âœ… Ollama CUDA service cleaned up')}catch (error: Error | unknown) { console.error('Failed to cleanup Ollama service: ', safeErrorToString(error))} // Getters public get isInitialized(): boolean { return this.initialized} public get currentModel(): string { return getModelNameFrom(this.chatModel)} public get isCudaEnabled(): boolean { return this.cudaConfig.enabled} public getMetrics(modelName?: string): ModelMetrics | Record<string, ModelMetrics> { if (modelName) { return this.metrics.get(modelName) || ({ }as ModelMetrics)} return Object.fromEntries(this.metrics)} // Add the missing singleton accessor public static getInstance(): OllamaCudaService { if (!OllamaCudaService.instance) { OllamaCudaService.instance = new OllamaCudaService()} return OllamaCudaService.instance} } // Export singleton instance export const ollamaCudaService = OllamaCudaService.getInstance(); export default ollamaCudaService; // Export types (already exported above as interfaces) // export type { // CudaConfig, // OllamaModelConfig, // ModelMetrics, // StreamingOptions // }
