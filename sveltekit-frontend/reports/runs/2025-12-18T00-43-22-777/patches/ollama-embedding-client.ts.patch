--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\services\ollama-embedding-client.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\services\ollama-embedding-client.ts (fixed)

@@ Line 1, Col 152 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 154 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 155 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expression expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel 

@@ Line 1, Col 191 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ';' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 208 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Declaration or statement expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.e

@@ Line 1, Col 293 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expected '=' for property initializer. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; th

@@ Line 1, Col 317 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expected '=' for property initializer. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; th

@@ Line 1, Col 816 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ':' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 965 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 972 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 981 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 990 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1006 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1506 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1507 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1512 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ':' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1607 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1906 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1914 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1925 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1934 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1939 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 1945 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2026 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2027 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Invalid character. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = 

@@ Line 1, Col 2030 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2041 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2047 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2068 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2074 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2085 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 1, Col 2096 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Identifier expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel 

@@ Line 1, Col 2098 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Unterminated string literal. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddi

@@ Line 2, Col 1 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingModel = embed

@@ Line 2, Col 3 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: 'catch' or 'finally' expected. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embed

@@ Line 1, Col 41 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: File 'c:/Users/james/Videos/deeds-web-app/sveltekit-frontend/src/lib/utils/api-endpoints.ts' is not a module. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddin

@@ Line 1, Col 146 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Type parameter name cannot be 'number'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; t

@@ Line 1, Col 157 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'generateCompletion'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; th

@@ Line 1, Col 184 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: 'string' only refers to a type, but is being used as a value here. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { 

@@ Line 1, Col 328 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'completionModel'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.

@@ Line 1, Col 503 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Property 'completionModel' does not exist on type 'OllamaEmbeddingService'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: s

@@ Line 1, Col 656 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: A function whose declared type is neither 'undefined', 'void', nor 'any' must return a value. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, 

@@ Line 1, Col 965 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: No value exists in scope for the shorthand property 'Ollama'. Either declare one or provide an initializer. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingM

@@ Line 1, Col 980 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name '$'. Do you need to install type definitions for jQuery? Try `npm i --save-dev @types/jquery` and then add 'jquery' to the types field in your tsconfig. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: com

@@ Line 1, Col 981 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingMode

@@ Line 1, Col 981 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Expected 1-2 arguments, but got 22. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.

@@ Line 1, Col 982 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Block-scoped variable 'response' used before its declaration. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.

@@ Line 1, Col 1005 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name '$'. Do you need to install type definitions for jQuery? Try `npm i --save-dev @types/jquery` and then add 'jquery' to the types field in your tsconfig. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: com

@@ Line 1, Col 1007 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: No value exists in scope for the shorthand property 'errorText'. Either declare one or provide an initializer. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddi

@@ Line 1, Col 1506 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name '$'. Do you need to install type definitions for jQuery? Try `npm i --save-dev @types/jquery` and then add 'jquery' to the types field in your tsconfig. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: com

@@ Line 1, Col 1507 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingMode

@@ Line 1, Col 1524 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'api'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingMod

@@ Line 1, Col 1528 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'generate'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddi

@@ Line 1, Col 1906 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'Invalid'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddin

@@ Line 1, Col 1914 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'completion'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embed

@@ Line 1, Col 1925 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Block-scoped variable 'response' used before its declaration. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.

@@ Line 1, Col 1934 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'from'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingMo

@@ Line 1, Col 1939 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'Ollama'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embedding

@@ Line 1, Col 2026 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name '├ó'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingMode

@@ Line 1, Col 2028 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name '┼Æ'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embeddingMode

@@ Line 1, Col 2030 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'Completion'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embed

@@ Line 1, Col 2041 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'failed'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embedding

@@ Line 1, Col 2074 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'generating'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embed

@@ Line 1, Col 2085 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'completion'. import type { getOllamaEndpoint  } from '$lib/utils/api-endpoints'; export interface EmbeddingService { generateEmbedding(text, string), Promise<number[]>; generateCompletion(prompt, string): Promise<string>} export class OllamaEmbeddingService implements EmbeddingService { ollamaUrl: string, embeddingModel: string, private: completionModel | string; constructor(baseUrl, string, embeddingModel: string, completionModel: string) { this.ollamaUrl = baseUrl; this.embed

