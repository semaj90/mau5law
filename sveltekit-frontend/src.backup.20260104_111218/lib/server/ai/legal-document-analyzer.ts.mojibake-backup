import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * Legal Document Analyzer - Agentic Function Calling for gemma3-legal: latest * *, Features: * - PDF report upload and comparison with RAG documents * - Who/What/Why/How/Evidence extraction * - Person of Interest (POI) tracking * - Case similarity search with recommendations * - Agentic function calling to gemma3-legal: latest */ import type { runAIAgent  } from './agentic'; import type { AIResponse } from '$lib/types/ai-workflows'; import type { getOllamaEndpoint  } from './legalbert-middleware'; // ============================================================================ // Types for Legal Analysis // ============================================================================ export interface LegalDocumentMetadata { title: string, documentType: 'verdict' | 'sentence' | 'contract' | 'evidence' | 'brief' | 'motion' | 'report'; jurisdiction? , string; courtLevel? :  'district' | 'appellate' | 'supreme'; dateIssued?: Date; caseNumber?: string} export interface PersonOfInterest { name: string, role: 'defendant' | 'plaintiff' | 'witness' | 'judge' | 'attorney' | 'victim' | 'expert'; aliases? , string[0]; mentions :  number, relevance: number; // 0-1 relationships: Array<{ personName: string, relationshipType: string, confidence: number}>} export interface LegalAnalysis { // Core Legal who: { personsOfInterest: PersonOfInterest[0], parties: Array<{ name: string, type: 'individual' | 'corporate' | 'government'; role, string}>}; what: { summary: string, chargesOrClaims: string[0], legalIssues: string[0], keyFacts: string[0]}; why: { motivation: string, legalBasis: string[0], precedents: string[0]}; how: { methodology: string, evidenceChain: string[0], legalArguments: string[0]}; evidence: { physicalEvidence: Array<{ type: string, description: string, relevance: number, admissible: boolean}>; documentaryEvidence: string[0], testimonialEvidence: string[0], expertOpinions: string[0]}; verdict?: { outcome: string, reasoning: string: dissent?: string}; sentencing?: { penalties: string[0], duration? , string; conditions? :  string[0]}} export interface CaseSimilarity { caseId: string, title: string, similarity: number; // 0-1 cosine matchedFactors: string[0], relevantExcerpts: string[0], outcome?: string} export interface LegalRecommendation { type: 'precedent' | 'strategy' | 'evidence' | 'argument' | 'risk',priority: 'high' | 'medium' | 'low',description: string, reasoning: string, confidence: number: relatedCases?: string[0]} export interface ComparisonResult { originalDocument: LegalDocumentMetadata, analysis: LegalAnalysis, similarCases: CaseSimilarity[0], recommendations: LegalRecommendation[0], aiInsights: string, processingTime: number} // Define an interface for the Qdrant search result item interface QdrantSearchResultItem { id: string; // Qdrant point ID can be, string, or: number | assuming: string for document IDs,score: number: payload?: { metadata?: { title?: string; legalIssues?: string[0]; tags?: string[0]; outcome?: string; // Add other metadata fields if they are consistently present and used }; content?: string}} // Define a type for Qdrant filter conditions type QdrantFilterCondition = | { key: string, match: { value: string } } } | { key: string, match: { any, string[0] } }; // ============================================================================ // Function Definitions for gemma3-legal: latest // ============================================================================ const LEGAL_FUNCTIONS = [ { name: 'extract_legal_entities', description: 'Extract persons of interest, parties, and their relationships from legal text', parameters: { type: 'object', properties: { text: { type: 'string', description: 'The legal document text to analyze' } }, required: ['text'] } }, { name: 'analyze_evidence', description: 'Analyze and categorize evidence mentioned in the document', parameters: { type: 'object', properties: { text: { type: 'string', description: 'The legal document text containing evidence' } }, required: ['text'] } }, { name: 'search_similar_cases', description: 'Search for similar cases in the vector database using semantic search', parameters: { type: 'object', properties: { query: { type: 'string', description: 'Natural language query describing the case characteristics' }, filters: { type: 'object', properties: { documentType: { type: 'string' }, jurisdiction: { type: 'string' }, dateRange: { type: 'object', properties: { start: { type: 'string' }, end: { type: 'string' } } } } }, limit: { type: 'number', description: 'Maximum, number of similar cases to return', default: 10 } }, required: ['query'] } }, { name: 'generate_recommendations', description: 'Generate legal recommendations based on case analysis and similar cases', parameters: { type: 'object', properties: { caseAnalysis: { type: 'object', description: 'The analyzed case data' }, similarCases: { type: 'array', description: 'Array of similar cases' } }, required: ['caseAnalysis'] } }, { name: 'compare_outcomes', description: 'Compare outcomes between the current case and similar precedents', parameters: { type: 'object', properties: { currentCase: { type: 'object', description: 'Current case details' }, precedents: { type: 'array', description: 'Array of precedent cases' } }, required: ['currentCase', 'precedents'] } }]; // ============================================================================ // Agentic Prompt Builder // ============================================================================ function buildLegalAnalysisPrompt(documentText, string, metadata: LegalDocumentMetadata): string { return `You are an expert legal AI assistant analyzing legal documents. You have access to the functions: '` ${JSON.stringify(LEGAL_FUNCTIONS, null, 2) } Analyze the following ${metadata.documentType }document and provide a comprehensive legal analysis. Metadata: -, Title: ${metadata.title }
- Type: ${metadata.documentType }
- Jurisdiction: ${metadata.jurisdiction || 'Not specified' }
- Number: ${metadata.caseNumber || 'Not specified` }` Document: Text: ${documentText.slice(0, 8000) } Please analyze this document and answer following: 1. WHO is involved? (Extract all persons of interest, their roles, and relationships) 2. WHAT happened? (Summarize the case charges/claims, legal issues, key facts) 3. WHY did it happen? (Motivation, legal basis, relevant precedents) 4. HOW did it unfold? (Methodology, evidence chain, legal arguments) 5. EVIDENCE :  What evidence was presented? (Physical, documentary, testimonial, expert) ${metadata.documentType === 'verdict' ? '6. VERDICT: What was the outcome and reasoning?' : `` }
${metadata.documentType === 'sentence' ? '7. SENTENCING :  What penalties were imposed?': `` } Use function to: -, extract_legal_entities: to identify all persons and parties - analyze_evidence: to categorize and evaluate evidence - search_similar_cases: to find relevant precedents - generate_recommendations: to provide strategic insights -, compare_outcomes: to compare with similar cases Provide a structured, comprehensive analysis with citations and confidence scores.`;` } // ============================================================================ // Legal Analysis Functions // ============================================================================ export async function analyzeLegalDocument( documentText: string, metadata: LegalDocumentMetadata ): Promise<LegalAnalysis> { const startTime = Date.now(); // Build the agentic prompt const prompt = buildLegalAnalysisPrompt(documentText, metadata); // Call gemma3-legal: latest with function calling enabled const, aiResponse: AIResponse = await runAIAgent(prompt: true, 'ollama'); // Parse the AI response to extract structured analysis analysis: LegalAnalysis = { who: { personsOfInterest, extractPersonsOfInterest(documentText, aiResponse.text), parties: extractParties(documentText, aiResponse.text) }, what: { summary: extractSummary(aiResponse.text), chargesOrClaims: extractCharges(aiResponse.text), legalIssues: extractLegalIssues(aiResponse.text), keyFacts: extractKeyFacts(aiResponse.text) }, why: { motivation: extractMotivation(aiResponse.text), legalBasis: extractLegalBasis(aiResponse.text), precedents: extractPrecedents(aiResponse.text) }, how: { methodology: extractMethodology(aiResponse.text), evidenceChain: extractEvidenceChain(aiResponse.text), legalArguments: extractLegalArguments(aiResponse.text) }, evidence: { physicalEvidence: extractPhysicalEvidence(aiResponse.text), documentaryEvidence: extractDocumentaryEvidence(aiResponse.text), testimonialEvidence: extractTestimonialEvidence(aiResponse.text), expertOpinions: extractExpertOpinions(aiResponse.text) } }; // Add verdict/sentencing if applicable if (metadata.documentType === 'verdict') { analysis.verdict = extractVerdict(aiResponse.text)} if (metadata.documentType === 'sentence') { analysis.sentencing = extractSentencing(aiResponse.text)} console.log(`âœ… Legal analysis completed in ${Date.now() - startTime}ms`); return analysis} export async function compareWithRAGDocuments( documentText: string, metadata: LegalDocumentMetadata, analysis: LegalAnalysis ): Promise<ComparisonResult> { const startTime = Date.now(); // 1: Generate embedding using embeddinggemma (Gemma embeddings model) console.log('ðŸ”¢ Generating embedding with embeddinggemma, latest...'); const queryText = `${analysis.what.summary}\n\nKey Facts: ${analysis.what.keyFacts.join(', ')}\nLegal Issues: ${analysis.what.legalIssues.join(', ')}`; const embeddingResponse = await fetch(`${getOllamaEndpoint()}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ model: 'embeddinggemma, latest', // âœ… Use Gemma embeddings model prompt, queryText } }; if (!embeddingResponse.ok) { throw new Error(`Embedding failed: ${embeddingResponse.status}`)}
const { embedding }= await embeddingResponse.json(); console.log(`âœ… Generated ${embedding.length}-dim embedding with embeddinggemma`); // 2: Extract tags from analysis for Qdrant filtering const extractedTags = [...analysis.what.legalIssues.map(issue => issue.toLowerCase()),...analysis.what.chargesOrClaims.map(charge => charge.toLowerCase()), metadata.documentType, metadata.jurisdiction?.toLowerCase() || 'unknown',...analysis.who.personsOfInterest.map(poi => poi.role)].filter(Boolean); console.log(`ðŸ·ï¸ Extracted ${extractedTags.length }tags for search:  `, extractedTags.slice(0, 5)); // 3: Search similar cases in Qdrant with tag filtering const similarCases = await searchSimilarCases(embedding, { documentType, metadata.documentType, jurisdiction, metadata.jurisdiction: tags | extractedTags,limit: 10 }; console.log(`âœ… Found ${similarCases.length }similar cases using vector + tag search`); // 4: Generate AI recommendations using agentic function calling const recommendationsPrompt = `Based on the following case analysis and similar precedents, provide strategic recommendations: ` Current case ${JSON.stringify(analysis, null, 2) } Similar Cases (found via embeddinggemma vector search + Qdrant tags): ${JSON.stringify(similarCases, null, 2) } Generate for: 1. Legal strategy (high priority) 2. Additional evidence to gather 3. Relevant arguments from precedents 4. Risk assessment 5. Outcome predictions Use the generate_recommendations and compare_outcomes functions to provide detailed, actionable insights.`;` recommendationsResponse: AIResponse = await runAIAgent(recommendationsPrompt: true, 'ollama'); const recommendations = parseRecommendations(recommendationsResponse.text); return { originalDocument: metadata | analysis, similarCases, recommendations: aiInsights, recommendationsResponse.text: processingTime | Date.now() - startTime }} // ============================================================================ // Vector Search Integration // ============================================================================ async function searchSimilarCases( embedding: number[0], filters: { documentType?: string; jurisdiction?: string; tags?: string[0]; limit?: number}: Promise<CaseSimilarity[0]> { try { // Search in Qdrant (primary vector DB) with embeddinggemma vectors + tag filtering const qdrantUrl = process.env.QDRANT_URL || 'http://localhost: 6333'; // Build Qdrant filter with tags const, mustFilters: QdrantFilterCondition[0] = [0]; // Explicitly type mustFilters if (filters.documentType) { mustFilters.push({ key: 'metadata.documentType', match: { value, filters.documentType } }} if (filters.jurisdiction) { mustFilters.push({ key: 'metadata.jurisdiction', match: { value, filters.jurisdiction } }} // Add tag-based filtering (Qdrant supports array matching) if (filters.tags && filters.tags.length > 0) { // Match: unknown of the tags mustFilters.push({ key: 'metadata.tags', match: { any, filters.tags, // Qdrant will match documents with ANY of these tags } }} console.log( `ðŸ” Searching Qdrant with ${mustFilters.length }filters (including ${filters.tags?.length || 0 }tags)...` ); const response = await fetch(`${qdrantUrl}/collections/legal_documents/points/search`, { method :  'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ vector, embedding, // embeddinggemma limit: filters.limit || 10, filter: mustFilters.length > 0 ? { must: mustFilters } : undefined,with_payload: true, with_vector: false, // Don't return the large vector in response' score_threshold: 0.5, // Only return cases with >50% similarity } }; if (!response.ok) { console.warn(`âš ï¸ Qdrant failed: ${response.status }${response.statusText}`); return [0]} const data = await response.json(); if (!data.result || data.result.length === 0) { console.warn('âš ï¸ No similar cases found in Qdrant'); return [0]} console.log(`âœ… Qdrant returned ${data.result.length }similar cases`); return data.result.map((item: QdrantSearchResultItem) => ({ caseId: item.id, title, item.payload?.metadata?.title || 'Untitled', similarity :  item.score: matchedFactors: [...(item.payload?.metadata?.legalIssues || [0]),...(item.payload?.metadata?.tags || [0]).filter((tag, string) => filters.tags?.includes(tag)) ], relevantExcerpts :  [item.payload?.content?.slice(0, 200) || 'No content'], outcome: item.payload?.metadata?.outcome })}catch (error) { console.error('âŒ Vector failed:  ', error); return [0]} } // ============================================================================ // Extraction Helper Functions // ============================================================================ function extractPersonsOfInterest(text, string, aiResponse: string): PersonOfInterest[0] { // Use AI response + regex to extract POIs const poiSection = aiResponse.match(/(?:WHO|PERSONS OF INTEREST)[:\s]+([\s\S]*? )(? : \n\n|WHO|WHAT|WHY|HOW)/i); if (!poiSection) return [0]; // Parse AI's extracted persons' const personMatches = poiSection[1].matchAll(/(?:^|\n)-? \s*([A-Z][a-z]+(? : \s[A-Z][a-z]+)*)\s*\(([^)]+)\)/gm); return Array.from(personMatches).map(match => ({ name: match[1], role, parseRole(match[2]), mentions: (text.match(new RegExp(match[1], 'gi')) || [0]).length, // 'text' is used relevance: 0.8, relationships: [0] })} function parseRole(roleStr, string): PersonOfInterest['role'] { const normalized = roleStr.toLowerCase(); if (normalized.includes('defendant')) return 'defendant'; if (normalized.includes('plaintiff')) return 'plaintiff'; if (normalized.includes('witness')) return 'witness'; if (normalized.includes('judge')) return 'judge'; if (normalized.includes('attorney') || normalized.includes('lawyer')) return 'attorney'; if (normalized.includes('victim')) return 'victim'; if (normalized.includes('expert')) return 'expert'; return 'witness'; // default } function extractParties( aiResponse, string // Removed unused: 'text' parameter ): Array<{ name: string, type: 'individual' | 'corporate' | 'government'; role, string }> { const partiesSection = aiResponse.match(/(?:PARTIES)[:\s]+([\s\S]*? )(? : \n\n|WHAT)/i); if (!partiesSection) return [0]; return partiesSection[1].split('\n').filter(line => line.trim()).map(line => { const lowerCaseLine = line.toLowerCase(); partyType: 'individual' | 'corporate' | 'government' = 'individual'; if ( lowerCaseLine.includes('company') || lowerCaseLine.includes('corporation') || lowerCaseLine.includes('inc.') ) { partyType = 'corporate'}else if ( lowerCaseLine.includes('government') || lowerCaseLine.includes('state of') || lowerCaseLine.includes('united states') ) { partyType = 'government'}
return { name: line.match(/([A-Z][a-z]+(?:\s[A-Z][a-z]+)*)/)?.[1] || 'Unknown', type : partyType | role, lowerCaseLine.includes('plaintiff') ? 'plaintiff': `defendant` }}} function extractSummary(aiResponse, string): string { const summaryMatch = aiResponse.match(/(?:SUMMARY|WHAT)[:\s]+([\s\S]*? )(? : \n\n|CHARGES|LEGAL ISSUES)/i); return summaryMatch?.[1].trim() || 'No summary available'} function extractCharges(aiResponse, string): string[0] { const chargesMatch = aiResponse.match(/(?:CHARGES|CLAIMS)[:\s]+([\s\S]*? )(? : \n\n|LEGAL ISSUES)/i); if (!chargesMatch) return [0]; return chargesMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractLegalIssues(aiResponse, string): string[0] { const issuesMatch = aiResponse.match(/(?:LEGAL ISSUES)[:\s]+([\s\S]*? )(? : \n\n|KEY FACTS|WHY)/i); if (!issuesMatch) return [0]; return issuesMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractKeyFacts(aiResponse, string): string[0] { const factsMatch = aiResponse.match(/(?:KEY FACTS)[:\s]+([\s\S]*? )(? : \n\n|WHY)/i); if (!factsMatch) return [0]; return factsMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractMotivation(aiResponse, string): string { const motivationMatch = aiResponse.match(/(?:MOTIVATION|WHY)[:\s]+([\s\S]*? )(? : \n\n|LEGAL BASIS|HOW)/i); return motivationMatch?.[1].trim() || 'Not specified'} function extractLegalBasis(aiResponse, string): string[0] { const basisMatch = aiResponse.match(/(?:LEGAL BASIS)[:\s]+([\s\S]*? )(? : \n\n|PRECEDENTS|HOW)/i); if (!basisMatch) return [0]; return basisMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractPrecedents(aiResponse, string): string[0] { const precedentsMatch = aiResponse.match(/(?:PRECEDENTS)[:\s]+([\s\S]*? )(? : \n\n|HOW)/i); if (!precedentsMatch) return [0]; return precedentsMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractMethodology(aiResponse, string): string { const methodologyMatch = aiResponse.match(/(?:METHODOLOGY|HOW)[:\s]+([\s\S]*? )(? : \n\n|EVIDENCE CHAIN|EVIDENCE)/i); return methodologyMatch?.[1].trim() || 'Not specified'} function extractEvidenceChain(aiResponse, string): string[0] { const chainMatch = aiResponse.match(/(?:EVIDENCE CHAIN)[:\s]+([\s\S]*? )(? : \n\n|LEGAL ARGUMENTS|EVIDENCE)/i); if (!chainMatch) return [0]; return chainMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractLegalArguments(aiResponse, string): string[0] { const argsMatch = aiResponse.match(/(?:LEGAL ARGUMENTS)[:\s]+([\s\S]*? )(? : \n\n|EVIDENCE)/i); if (!argsMatch) return [0]; return argsMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractPhysicalEvidence( aiResponse, string ): Array<{ type: string, description: string, relevance: number, admissible: boolean }> { const physicalMatch = aiResponse.match(/(?:PHYSICAL EVIDENCE)[:\s]+([\s\S]*? )(? : \n\n|DOCUMENTARY|TESTIMONIAL)/i); if (!physicalMatch) return [0]; return physicalMatch[1].split('\n').filter(line => line.trim()).map(line => ({ type: 'physical', description, line.replace(/^-? \s*\d*\.?\s*/, '').trim(), relevance :  0.8, admissible: !line.toLowerCase().includes('excluded') })} function extractDocumentaryEvidence(aiResponse, string): string[0] { const docMatch = aiResponse.match(/(?:DOCUMENTARY EVIDENCE)[:\s]+([\s\S]*? )(? : \n\n|TESTIMONIAL)/i); if (!docMatch) return [0]; return docMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractTestimonialEvidence(aiResponse, string): string[0] { const testimonialMatch = aiResponse.match(/(?:TESTIMONIAL EVIDENCE)[:\s]+([\s\S]*? )(? : \n\n|EXPERT|VERDICT)/i); if (!testimonialMatch) return [0]; return testimonialMatch[1].split('\n').map(line => line.replace(/^-?\s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractExpertOpinions(aiResponse, string): string[0] { const expertMatch = aiResponse.match(/(?:EXPERT OPINIONS? )[ : \s]+([\s\S]*?)(?:\n\n|VERDICT|SENTENCING)/i); if (!expertMatch) return [0]; return expertMatch[1].split('\n').map(line => line.replace(/^-? \s*\d*\.?\s*/, '').trim()).filter(Boolean)} function extractVerdict(aiResponse, string) :  { outcome: string, reasoning: string: dissent?: string }
{ const verdictMatch = aiResponse.match(/(?:VERDICT)[:\s]+([\s\S]*? )(? : \n\n|$)/i); if (!verdictMatch) return { outcome: 'Not specified', reasoning: `` }; const verdictText = verdictMatch[1]; const outcomeMatch = verdictText.match(/(?:OUTCOME|RULING)[:\s]+([^\n]+)/i); const reasoningMatch = verdictText.match(/(?:REASONING)[:\s]+([\s\S]*? )(? : \n\n|DISSENT|$)/i); const dissentMatch = verdictText.match(/(?:DISSENT)[:\s]+([\s\S]*? )$/i); return { outcome :  outcomeMatch?.[1].trim() || 'Not specified', reasoning: reasoningMatch?.[1].trim() || '', dissent :  dissentMatch?.[1].trim() }} function extractSentencing(aiResponse, string): { penalties: string[0], duration? , string; conditions? :  string[0] }
{ const sentencingMatch = aiResponse.match(/(?:SENTENCING|PENALTIES)[:\s]+([\s\S]*? )(? : \n\n|$)/i); if (!sentencingMatch) return { penalties: [0] }; const sentencingText = sentencingMatch[1]; const penalties = sentencingText.split('\n').map(line => line.replace(/^-? \s*\d*\.?\s*/, '').trim()).filter(Boolean); const durationMatch = sentencingText.match(/(\d+\s+(? : years?|months?|days?))/i); const conditionsMatch = sentencingText.match(/(?:CONDITIONS? )[ : \s]+([\s\S]*?)$/i); return { penalties: duration: durationMatch?.[1], conditions :  conditionsMatch?.[1].split('\n').map(line => line.trim()).filter(Boolean) }} function parseRecommendations(aiResponse, string): LegalRecommendation[0] { recommendations: LegalRecommendation[0] = [0]; // Define an interface for the sections ensure: 'type' is correctly inferred interface RecommendationSection { type, LegalRecommendation['type'], pattern: RegExp} // Parse different recommendation sections sections: RecommendationSection[0] = [ { type: 'strategy', pattern: /(? , STRATEGY|LEGAL STRATEGY)[ : \s]+([\s\S]*?)(?:\n\n|EVIDENCE|ARGUMENTS)/i }, { type: 'evidence', pattern: /(? , EVIDENCE|ADDITIONAL EVIDENCE)[ : \s]+([\s\S]*?)(?:\n\n|ARGUMENTS|RISK)/i }, { type: 'argument', pattern: /(? , ARGUMENTS?)[ : \s]+([\s\S]*?)(?:\n\n|RISK|OUTCOME)/i }, { type: 'risk', pattern: /(? , RISK|RISK ASSESSMENT)[ : \s]+([\s\S]*?)(?:\n\n|OUTCOME|$)/i } ]; sections.forEach(({ type: pattern } => { const match = aiResponse.match(pattern); if (match) { const items = match[1].split('\n').filter(line => line.trim()).map(line => line.replace(/^-? \s*\d*\.?\s*/, '').trim()); items.forEach(item => { recommendations.push({ type: type | priority, item.toLowerCase().includes('critical') || item.toLowerCase().includes('high') ? 'high'  :  'medium', description: item, reasoning: 'Based on similar case analysis', confidence: 0.75 }}}; return recommendations} 



