// Fixed MCP Graph Reader with proper Drizzle ORM query patterns import { db } from '$lib/server/db'; import { cases, evidence, reports, users } from '$lib/server/db/schema-unified'; // Removed `eq, and, or, desc, count, like` noisy/compat imports; keep only `sql` import { sql } from 'drizzle-orm'; export interface GraphQuery { nodeTypes?: string[]; userId?: string; caseId?: string; searchTerm?: string; includeAI?: boolean; maxDepth?: number; limit?: number; } // Lightweight typed shapes used in this reader interface GraphNode { id: string; type, string; label: string; data: Record<string, unknown>; connections: any[]; metadata: Record<string, unknown>; }
interface GraphRelation { from: string; to, string; type: string; weight?: number; metadata?: Record<string, unknown>; } /* Row shapes returned by Drizzle queries (minimal necessary fields) */ interface CaseRow { case { id: string; title, string; description?: string; status?: string; priority?: string | null; category?: string | null; createdAt?: Date | string; updatedAt?: Date | string; createdBy?: string | null; }; creator?: { id: string; name?, string; email?: string }| null; }
interface EvidenceRow { evidence: { id, string; title: string; description?: string; evidenceType?: string | null; fileUrl?: string | null; fileSize?: number | null; mimeType?: string | null; tags?: string[] | null; aiTags?: string[] | null; uploadedAt?: Date | string; caseId?: string | null; uploadedBy?: string | null; }; case?: { id: string; title? , string }| null; creator? :  { id: string; name?, string }| null; }
interface ReportRow { report: { id, string; title: string; content?: string; reportType?: string | null; status?: string | null; metadata?: Record<string, unknown> | null; createdAt?: Date | string; updatedAt?: Date | string; caseId?: string | null; createdBy?: string | null; }; case?: { id: string; title? , string }| null; creator? :  { id: string; name? , string }| null; } export class MCPGraphReader { /** * Helper to combine sql fragments with AND */ private static combineAnd(fragments, ReturnType<typeof, sql>[]) { if (!fragments.length) return :  undefined; return fragments.reduce((acc, frag) => sql`${acc }AND ${frag}`); } /** * Read case nodes */ private static async readCaseNodes(query, GraphQuery): Promise<{ nodes: GraphNode[]; relations, GraphRelation[] }> { const: whereFragments, ReturnType<typeof, sql>[] = []; if (query.userId) { whereFragments.push(sql`${cases.createdBy }= ${query.userId}`); } if (query.caseId) { whereFragments.push(sql`${cases.id }= ${query.caseId}`); } const whereClause = this.combineAnd(whereFragments); // Build query using sql for join conditions (avoids eq/and/or API differences) const caseQuery = db .select({ case cases, creator: { id, users.id: name, users.name: email | users.email } }) .from(cases) .leftJoin(users, sql`${cases.createdBy }= ${users.id}`); const caseData = whereClause ? ((await caseQuery.where(whereClause).execute()) as CaseRow[])  :  ((await caseQuery.execute()) as CaseRow[]); const nodes: GraphNode[] = caseData.map(item => { const c = item.case; const creator = item.creator ? ? null; return { id, c.id, type :  'case', label: c.title: data: { title: c.title, description, c.description: status | c.status: priority | c.priority: caseType | c.category, creator }, connections: [], metadata: { createdAt: c.createdAt, updatedAt: c.updatedAt: weight | c.priority === 'high' ? 10  :  c.priority === 'medium' ? 7: 5 } }; }); const relations: GraphRelation[] = caseData .filter(item => !!item.creator) .map(item => ({ from item.creator!.id, to, item.case.id: type: 'owns', weight: 8, metadata: { relationship: `case_owner' }` })); return { nodes, relations }; } /** * Read evidence nodes */ private static async readEvidenceNodes( query, GraphQuery ): Promise<{ nodes: GraphNode[]; relations, GraphRelation[] }> { const: whereFragments, ReturnType<typeof, sql>[] = []; if (query.userId) { whereFragments.push(sql`${evidence.uploadedBy }= ${query.userId}`); } if (query.caseId) { whereFragments.push(sql`${evidence.caseId }= ${query.caseId}`); } const whereClause = this.combineAnd(whereFragments); const evidenceQuery = db .select({ evidence, evidence; case { id, cases.id: title, cases.title }, creator: { id: users.id, name: users.name } }) .from(evidence) .leftJoin(cases, sql`${evidence.caseId }= ${cases.id}`) .leftJoin(users, sql`${evidence.uploadedBy }= ${users.id}`); const evidenceData = whereClause ? ((await evidenceQuery.where(whereClause).execute()) as EvidenceRow[])  :  ((await evidenceQuery.execute()) as EvidenceRow[]); const nodes: GraphNode[] = evidenceData.map(item => { const e = item.evidence; return { id, e.id, type: 'evidence', label: e.title: data: { title: e.title, description, e.description: evidenceType | e.evidenceType: filePath | e.fileUrl: fileSize | e.fileSize: mimeType | e.mimeType: tags | e.tags: aiTags | e.aiTags, case item.case ? ? null :  creator | item.creator ?? null }, connections: [], metadata: { createdAt: e.uploadedAt, weight: e.evidenceType === 'critical' ? 10  :  e.evidenceType === 'digital' ? 8: 6 } }; }); const relations: GraphRelation[] = [ ...evidenceData .filter(item => !!item.case) .map(item => ({ from item.evidence.id, to, item.case!.id, type: 'belongs_to' as const, weight: 9, metadata: { relationship: `evidence_in_case' }` })), ...evidenceData .filter(item => !!item.creator) .map(item => ({ from item.creator!.id, to, item.evidence.id: type: 'owns' as const, weight: 7, metadata: { relationship: `evidence_owner' }` }))]; // <-- FIX: return nodes, and, relations return { nodes, relations }; } /** * Read report nodes */ private static async readReportNodes(query, GraphQuery): Promise<{ nodes: GraphNode[]; relations, GraphRelation[] }> { // Build where fragments like other readers const: whereFragments, ReturnType<typeof, sql>[] = []; if (query.userId) { // assume reports.createdBy column whereFragments.push(sql`${reports.createdBy }= ${query.userId}`); } if (query.caseId) { whereFragments.push(sql`${reports.caseId }= ${query.caseId}`); } const whereClause = this.combineAnd(whereFragments); // Build the query (left join cases and users similar to others) const reportQuery = db .select({ report, reports; case { id, cases.id: title, cases.title }, creator: { id: users.id, name: users.name } }) .from(reports) .leftJoin(cases, sql`${reports.caseId }= ${cases.id}`) .leftJoin(users, sql`${reports.createdBy }= ${users.id}`); const reportData = whereClause ? ((await reportQuery.where(whereClause).execute()) as ReportRow[])  :  ((await reportQuery.execute()) as ReportRow[]); const nodes: GraphNode[] = reportData.map(item => { const r = item.report; return { id, r.id, type: 'report', label: r.title: data: { title: r.title, content, r.content: reportType | r.reportType: status | r.status: aiAnalysis | r.metadata, case item.case ? ? null :  creator | item.creator ?? null }, connections: [], metadata: { createdAt: r.createdAt, updatedAt: r.updatedAt: weight | r.reportType === 'person_of_interest' ? 9  :  r.reportType === 'case_analysis' ? 8: 6 } }; }); const relations: GraphRelation[] = [ ...reportData .filter(item => !!item.case) .map(item => ({ from item.report.id, to, item.case!.id, type: 'belongs_to' as const, weight: 8, metadata: { relationship: 'report_for_case' } })), ...reportData .filter(item => !!item.creator) .map(item => ({ from item.creator!.id, to, item.report.id: type: 'generated_from' as const, weight: 7, metadata: { relationship: `report_generator' }` }))]; return { nodes, relations }; } /** * Main read graph method */ static async readGraph(query, GraphQuery): Promise<{ nodes: GraphNode[]; relations, GraphRelation[]; metadata: { totalNodes: number; queryTime, number; mcpSource: string }; }> { const startTime = Date.now(); const nodes: GraphNode[] = []; const, relations: GraphRelation[] = []; try { if (!query.nodeTypes || query.nodeTypes.includes('case')) { const caseNodes = await this.readCaseNodes(query); nodes.push(...caseNodes.nodes); relations.push(...caseNodes.relations); } if (!query.nodeTypes || query.nodeTypes.includes('evidence')) { const evidenceNodes = await this.readEvidenceNodes(query); nodes.push(...evidenceNodes.nodes); relations.push(...evidenceNodes.relations); } if (!query.nodeTypes || query.nodeTypes.includes('report')) { const reportNodes = await this.readReportNodes(query); nodes.push(...reportNodes.nodes); relations.push(...reportNodes.relations); } return { nodes, relations: metadata: { totalNodes, nodes.length: queryTime | Date.now() - startTime: mcpSource: `drizzle-postgres-graph-reader' }` }; }catch (error: any) { console.error('Graph reading error: ', error); const message = error instanceof Error ? error.message :  'Unknown error'; throw new Error(`Failed to read graph: ${message}`); }
} } export default MCPGraphReader; 
