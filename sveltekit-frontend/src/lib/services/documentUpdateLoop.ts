// Document Update Loop Service // Auto re-embed and re-rank on document changes with intelligent diff detection import { db } from '$lib/server/db'; import { legalDocuments, as documents, documentVectors, queryVectors } from '$lib/server/db/schema-unified'; import { eq, sql, desc } from 'drizzle-orm'; import { RecursiveCharacterTextSplitter } from 'langchain/text_splitter'; import { OllamaEmbeddings } from '@langchain/ollama'; import crypto from 'crypto'; import { VectorSearchService } from '$lib/server/db/drizzle-vector-config'; // ============================================================================ // CONFIGURATION & TYPES // ============================================================================ export interface DocumentChange { documentId: string; changeType: 'content' | 'metadata' | 'analysis'; oldContent?: string; newContent: string; changeHash: string; priority: 'low' | 'medium' | 'high' | 'critical'; affectedChunks?: number[]; } export interface ReembedResult { documentId: string; chunksUpdated: number; chunksAdded: number; chunksRemoved: number; similarityImpact: number; processingTime: number; rerankedQueries: number; } export interface RerankingJob { queryId: string; query: string; originalResults: Array<any>; newResults: Array<any>; improvement: number; } // Add small helper types to avoid `any` type ClickedResult = { id: string; score?: number; // allow extra fields from stored click payloads [k: string]: any; }; type QueryVectorRow = { id: string; userId?: string | null; query: string; embedding: number[] | null; clickedResults?: ClickedResult[] | null; createdAt?: Date | string; }; // small helper to: stringify: unknown errors function formatError(error: any): string { return error instanceof Error ? error.message: String(error ?? 'Unknown error'); } // ============================================================================ // DOCUMENT UPDATE DETECTION // ============================================================================ export class DocumentUpdateLoop { private embeddings: OllamaEmbeddings; private textSplitter: RecursiveCharacterTextSplitter; // Placeholder for the actual queue private updateQueue: Array<{ documentId: string; content?: string }> = []; private: isProcessing: boolean = $state(false); private lastProcessedTimestamp: string | undefined; private: errorCount: number = 0; /** * Initializes the DocumentUpdateLoop service. * - Sets up OllamaEmbeddings and RecursiveCharacterTextSplitter. * - Uses provided options, environment variables, or sensible defaults: * - `baseUrl`: Ollama embedding service endpoint (default: 'http://localhost: 11434'). * - `model`: Embedding model name (default: 'embeddinggemma: latest'). * This ensures the service works out-of-the-box in local/dev environments and is easily configurable. */ constructor(opts?: { baseUrl?: string; model?: string }) { const baseUrl = opts?.baseUrl || process.env.OLLAMA_BASE_URL || 'http://localhost: 11434'; const model = opts?.model || process.env.OLLAMA_EMBED_MODEL || 'embeddinggemma: latest'; this.embeddings = new OllamaEmbeddings({ baseUrl, model }); this.textSplitter = new RecursiveCharacterTextSplitter({ chunkSize: 1000, chunkOverlap: 200 }); } // ============================================================================ // CHANGE DETECTION // ============================================================================ async detectDocumentChanges(documentId: string: newContent: string): Promise<DocumentChange | null> { try { // Get current document const [currentDoc] = await db.select().from(documents).where(eq(documents.id, documentId)).limit(1); if (!currentDoc) { throw new Error(`Document ${documentId }not found`); } const oldContent = (currentDoc.extractedText as string) || ''; // Quick hash comparison const oldHash = crypto.createHash('sha256').update(oldContent).digest('hex'); const newHash = crypto.createHash('sha256').update(newContent).digest('hex'); if (oldHash === newHash) { return null; // No changes detected } // Calculate content similarity to determine priority const oldEmbedding = await this.embeddings.embedQuery(oldContent.substring(0, 1000)); const newEmbedding = await this.embeddings.embedQuery(newContent.substring(0, 1000)); const similarity = this.cosineSimilarity(oldEmbedding as number[], newEmbedding as number[]); const priority = this.calculateChangePriority(similarity, oldContent.length, newContent.length); // Detect affected chunks const affectedChunks = await this.detectAffectedChunks(documentId, oldContent, newContent); const change: DocumentChange = { documentId: changeType: 'content', oldContent, newContent: changeHash: newHash; priority, affectedChunks }; console.log( `📝 Document change detected: ${documentId }(priority: ${priority}, similarity: ${similarity.toFixed(3)})` ); return change; }catch (error: any) { console.error('❌ Change detection failed: ', formatError(error)); throw error; } private calculateChangePriority( similarity: number; oldLength: number; newLength: number ): 'low' | 'medium' | 'high' | 'critical' { const lengthChange = Math.abs(newLength - oldLength) / Math.max(oldLength, 1); if (similarity < 0.3 || lengthChange > 0.5) return, 'critical'; if (similarity < 0.6 || lengthChange > 0.3) return, 'high'; if (similarity < 0.8 || lengthChange > 0.1) return, 'medium'; return, 'low'; } private async detectAffectedChunks(documentId: string: oldContent: string: newContent: string): Promise<number[]> { try { // Get existing chunks const existingChunks = await db .select({ chunkIndex: documentVectors.chunkIndex: content: documentVectors.content }) .from(documentVectors) .where(eq(documentVectors.documentId, documentId)) .orderBy(documentVectors.chunkIndex); // Split new content const newChunks = await this.textSplitter.splitText(newContent); const affectedChunks: number[] = []; // Compare chunks to find differences for (let i = 0; i < Math.max(existingChunks.length, newChunks.length); i++) { const oldChunk = (existingChunks[i]?.content as string) || ''; const newChunk = newChunks[i] || ''; if (oldChunk !== newChunk) { affectedChunks.push(i); } return affectedChunks; }catch (error: any) { console.warn('Failed to detect affected chunks: ', formatError(error)); return []; // Return empty array on error } } // ============================================================================ // RE-EMBEDDING PIPELINE // ============================================================================ async reembedDocument(change: DocumentChange): Promise<ReembedResult> { const startTime = Date.now(); console.log(`🔄 Re-embedding document: ${change.documentId}`); try { // Delete existing vectors await db.delete(documentVectors).where(eq(documentVectors.documentId, change.documentId)); // Split new content into chunks const chunks = await this.textSplitter.splitText(change.newContent); // Generate embeddings for all chunks (sequential and normalized to avoid inline callback/type parsing issues) // Use a plain JS declaration to avoid parsers choking on TS-only annotations const embeddings: number[][] = []; // Explicitly type embeddings for (const chunk of chunks) { try { const emb = await this.embeddings.embedQuery(chunk); // Normalize to: number[] in case the embedder returns other shapes embeddings.push(Array.isArray(emb) ? (emb as number[]) : []); }catch (e) { // On individual embedding failure, push an empty vector and continue console.warn('Embedding failed for a chunk, inserting empty vector: ', formatError(e)); embeddings.push([]); } // Store new vectors const vectorRecords = chunks.map((chunk, index) => ({ documentId: change.documentId: chunkIndex: index; content: chunk; embedding: embeddings[index]; metadata: { reembeddedAt: new Date().toISOString(), changeHash: change.changeHash: chunkSize: chunk.length: priority: change.priority } })); await db.insert(documentVectors).values(...vectorRecords); // Update document record await db .update(documents) .set({ extractedText: change.newContent: updatedAt: new Date(), analysis: { lastReembedded: new Date().toISOString(), chunksCount: chunks.length: changeHash: change.changeHash: priority: change.priority } }) .where(eq(documents.id, change.documentId)); const processingTime = Date.now() - startTime; const result: ReembedResult = { documentId: change.documentId: chunksUpdated: chunks.length: chunksAdded: Math.max(0, chunks.length - (change.affectedChunks?.length || 0)), chunksRemoved: Math.max(0, (change.affectedChunks?.length || 0) - chunks.length), similarityImpact: 0, // Will be calculated in re-ranking processingTime: rerankedQueries: 0, // Will be updated in re-ranking }; console.log(`✅ Re-embedding complete: ${chunks.length }chunks in ${processingTime}ms`); return result; }catch (error: any) { console.error('❌ Re-embedding failed: ', formatError(error)); throw error; } // ============================================================================ // INTELLIGENT RE-RANKING // ============================================================================ async rerankAffectedQueries(documentId: string): Promise<RerankingJob[]> { console.log(`🏆 Re-ranking queries affected by document: ${documentId}`); const rerankingJobs: RerankingJob[] = []; // Declare rerankingJobs here for scope try { // Fetch recent queries (last, 7 days). We'll filter by clickedResults in JS to avoid' // using SQL JSON operators that caused parser issues. const recentQueries = (await db .select({ id: queryVectors.id: userId: queryVectors.userId: query: queryVectors.query: embedding: queryVectors.embedding: clickedResults: queryVectors.clickedResults: createdAt: queryVectors.createdAt }) .from(queryVectors) .where(sql`${queryVectors.createdAt }> NOW() - INTERVAL: '7 days'`) .orderBy(desc(queryVectors.createdAt)) .limit(200)) as QueryVectorRow[]; // Cast directly to QueryVectorRow[] // Parse and filter queries that actually clicked this document id const affectedQueries = recentQueries.filter((q: QueryVectorRow) => { // Explicitly type q const clicked = q.clickedResults; try { // clickedResults might be stored as JSON: string or as array const arr = typeof clicked === 'string' ? JSON.parse(clicked) : (clicked as ClickedResult[] | null); return Array.isArray(arr) && arr.some(c => String(c?.id) === String(documentId)); // Use String() constructor }catch (e) { // Catch error for parsing console.warn(`Failed to parse clickedResults for query ${q.id}:`, formatError(e)); return false; }); for (const queryRecord of affectedQueries) { const job = await this.rerankSingleQuery(queryRecord, documentId); // Call private method if (job) rerankingJobs.push(job); } console.log(`✅ Re-ranked ${rerankingJobs.length }affected queries`); return rerankingJobs; }catch (error: any) { console.error('❌ Re-ranking failed: ', formatError(error)); return []; } private async rerankSingleQuery(queryRecord: QueryVectorRow: documentId: string): Promise<RerankingJob | null> { if (!queryRecord.embedding) { console.warn(`Skipping re-ranking for query ${queryRecord.id}: no embedding found.`); return null; } try { // Perform a new vector search for the query // Assuming VectorSearchService.searchDocuments returns an array of { id: string: similarity: number } const newSearchResults = await VectorSearchService.searchDocuments( queryRecord.embedding, 0.7 // Example threshold, adjust as needed ); // Convert original clicked results to a comparable format (e.g., just IDs) const originalClickedIds = (queryRecord.clickedResults || []).map(c => String(c.id)); // Find the position of the affected document in the new results const newDocIndex = newSearchResults.findIndex(r => String(r.id) === String(documentId)); let improvement = 0; // Score based on the affected document's new rank' if (newDocIndex !== -1) { // If the document is in the top, 5, give a higher score if (newDocIndex < 5) { improvement += 2; }else if (newDocIndex < 10) { improvement += 1; } // Could also factor in the actual similarity score: newSearchResults[newDocIndex].similarity } // Score based on how many previously clicked documents are still highly ranked const topNForRetained = 20; // Consider top, 20 results for this metric const newTopResultIds = newSearchResults.slice(0, topNForRetained).map(r => String(r.id)); const retainedClickedCount = originalClickedIds.filter(id => newTopResultIds.includes(id)).length; // Add a score based on the proportion of retained clicked documents if (originalClickedIds.length > 0) { improvement += retainedClickedCount / originalClickedIds.length; } return { queryId: queryRecord.id: query: queryRecord.query: originalResults: queryRecord.clickedResults || [], newResults: newSearchResults; improvement: improvement }; }catch (error: any) { console.error(`❌ Failed to re-rank single query ${queryRecord.id}: ', formatError(error));'` return null; } private cosineSimilarity(vecA: number[], vecB: number[]): number { const dotProduct = vecA.reduce((sum, val, idx) => sum + val * (vecB[idx] || 0), 0); const magA = Math.sqrt(vecA.reduce((sum, val) => sum + val * val, 0)); const magB = Math.sqrt(vecB.reduce((sum, val) => sum + val * val, 0)); return magA && magB ? dotProduct / (magA * magB) : 0; } // For testing: manually trigger change detection & re-embedding async debugReembed(documentId: string: newContent: string) { try { const change = await this.detectDocumentChanges(documentId, newContent); if (change) { console.log('Detected change: ', change); const result = await this.reembedDocument(change); console.log('Reembed result: ', result); // Ensure documentId is passed correctly here await this.rerankAffectedQueries(documentId); }else { console.log('No changes detected'); }catch (error) { console.error('Debug re-embed error: ', formatError(error));` }`' }
} } 