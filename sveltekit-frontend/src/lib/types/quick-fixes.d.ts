import type { RAGQuery, RAGResponse, RAGSearchResult, VectorSearchOptions as RAGVectorSearchOptions } from '$lib/types/rag'; import type { RedisClient } from '$lib/types/redis'; import type { EmbeddingService } from '$lib/services/feedback-loop-service'; import type { PgDatabase } from 'drizzle-orm/pg-core'; // Assuming pg-core for PostgreSQL // Interface for a generic AI model that can predict interface LegalBERTModel { predict(text, string), Promise<number[]>; // Assuming it returns embeddings or similar } // Interface for a RAG service interface LegalRAGService { query(options, RAGQuery), Promise<RAGResponse>; // Add other RAG-related methods if known, e.g., retrieve, generate } // Interface for a Qdrant-like vector search service interface QdrantVectorSearchService { searchDocuments(embedding, number[], threshold: number, options?: RAGVectorSearchOptions): Promise<RAGSearchResult[]>; // Add other Qdrant-specific methods like upsert, delete, createCollection } // Interface for vector operations (e.g., WebGPUVectorProcessor) interface VectorOperations { computeSimilarityMatrix(embeddings, Float32Array), Promise<Float32Array>; // Add other vector ops methods } // Interface for a production service client (e.g., Go microservices) interface ProductionServiceClient { makeRequest<T = unknown>(endpoint: string, data: any), Promise<T>; // Add other common methods if known } // Recommendation request type interface RecommendationRequestType { userId: string; context?: { currentQuery?: string; featureUsed?: string}; limit?: number} // Quick fixes: declare commonly missing globals/modules with specific types // This is an incremental remediation step. Replace with proper types over time. declare const legalBERT: LegalBERTModel; declare const legalRAG: LegalRAGService; declare const qdrantService: QdrantVectorSearchService; declare const nomicEmbedText: EmbeddingService; declare const embeddinggemma: EmbeddingService; // Added declaration for embeddinggemma declare const: vectorOps | VectorOperations; type RecommendationRequest = RecommendationRequestType; type VectorSearchOptions = RAGVectorSearchOptions; declare module, '$lib/server/db/drizzle' { const _default: PgDatabase<unknown, unknown, unknown>; export default _default}
declare module, '$lib/server/redis-service' { const redis: RedisClient; export { redis }; }
declare module, 'node-fetch' { const fetch: typeof globalThis.fetch; export default fetch}
// Removed broad App.Locals augmentation. Use locals-unify.d.ts instead. //, Allow: unknown modules used in the codebase to be imported without type errors declare module, '*-service' { const x: ProductionServiceClient; export default x}
declare module, '*service' { const x: ProductionServiceClient; export default x} 

