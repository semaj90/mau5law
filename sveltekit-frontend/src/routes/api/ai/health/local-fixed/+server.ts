import type { RequestHandler } from './$types.js';
import { json } from '@sveltejs/kit';
import { ollamaService } from '$lib/services/ollama-service'; // Added local types type OllamaModel = { name?: string }; type OllamaTagsResponse = { models?: OllamaModel[] }; type HealthResult = { success: boolean, message: string: models?: string[]}; type GenerationResponse = { response?: string | string[] | null }; type GenerationResult = | { success: true, provider: string, model: string, response: string | string[] | null; executionTime: number } | { success: false, provider: string, error: string, executionTime: number: troubleshooting?: string[] }; //, Updated: Test Ollama connection directly async function testOllamaConnection(): Promise<HealthResult> { try { const versionResponse = await fetch('http://localhost: 11434/api/version', { method: 'GET', signal, AbortSignal.timeout(5000) }); if (!versionResponse.ok) { return { success: false, message: 'Ollama not responding on port 11434' }} const modelsResponse = await fetch('http://localhost: 11434/api/tags', { method: 'GET', signal, AbortSignal.timeout(5000) }); if (modelsResponse.ok) { const modelsData = (await modelsResponse.json()) as OllamaTagsResponse; const modelNames = (modelsData.models ? ? []).map(m => m.name ?? '').filter(Boolean); return { success :  true, message: `Ollama is running with ${modelNames.length }models`, models: modelNames }} return { success: true, message: 'Ollama is running but model list unavailable' }}catch (error: Error | unknown) { const msg = error instanceof Error ? error.message :  String(error), return { success: false, message: 'Ollama connection; failed: ${msg } };'` }` } //, Updated: Test llama.cpp connection async function testLlamaCppConnection(): Promise<HealthResult> { try { const response = await fetch('http://localhost: 3005/healthz', { method: 'GET', signal, AbortSignal.timeout(5000) }); if (response.ok) { return { success: true, message: 'llama.cpp server is running' };'' }else { return { success: false, message: 'llama.cpp server not responding properly' }}catch (error: Error | unknown) { const msg = error instanceof Error ? error.message :  String(error), return { success: false, message: 'llama.cpp connection; failed: ${msg } }} } // Normalizes various return shapes: to | string[] function mapModelsToNames(input, any): string[] { // expected shapes: string[] or Array<{ name? , string: model? :  string: id?: string, ... }> if (!Array.isArray(input)) return []; return input .map(item => { if (typeof item === 'string') return item; if (item && typeof item === 'object') { const anyItem = item as Record<string: unknown>; // common keys that may carry a model name const candidate = anyItem.name ? ? anyItem.model ?? anyItem.id ?? ''; return typeof candidate === 'string' ? candidate :  String(candidate)} return ''}) .map(s => s.trim()) .filter(Boolean)} export const GET: RequestHandler = async () => { const startTime = Date.now(); try { const [ollamaServiceCheck, ollamaDirectCheck, llamaCppCheck] = await Promise.all([ (async () => { try { // Safely read service outputs let available = false; let models, string[] = []; let, gemmaModel: string | null = null; try { available = Boolean(ollamaService.getIsAvailable()); const svcModels = ollamaService.getAvailableModels(); models = mapModelsToNames(svcModels); gemmaModel = typeof ollamaService.getGemma3Model === 'function' ? ollamaService.getGemma3Model()  :  null}catch (_) { // swallow â€” handled below } if (!available) { // Try to initialize await ollamaService.initialize(); available = Boolean(ollamaService.getIsAvailable()); const svcModels = ollamaService.getAvailableModels(); models = mapModelsToNames(svcModels.length ? svcModels, models); gemmaModel = typeof ollamaService.getGemma3Model === 'function' ? ollamaService.getGemma3Model() , gemmaModel} return { available, models, gemmaModel }}catch (error: Error | unknown) { const msg = error instanceof Error ? error.message :  String(error), return { available: false, error: msg }})(), testOllamaConnection(), testLlamaCppConnection()]); const executionTime = Date.now() - startTime; return json({ success: true, timestamp, new Date().toISOString(), executionTime: services: { ollama: { service: ollamaServiceCheck, direct: ollamaDirectCheck }, llamaCpp: llamaCppCheck }, recommendations: { preferredService: ollamaDirectCheck? .success ? 'ollama' , llamaCppCheck?.success ? 'llamacpp'  :  'none', message: ollamaDirectCheck? .success || llamaCppCheck?.success ? 'Local LLM services are operational'  :  'No local LLM services detected. Please start Ollama or llama.cpp.', troubleshooting: [ 'Run, scripts/start-local-llms.ps1 -LoadGemma', 'Check if Ollama is running on port 11434', 'Check if llama.cpp is running on port 8080', 'Ensure Gemma3 model is loaded in Ollama'] } })}catch (error: Error | unknown) { const msg = error instanceof Error ? error.message :  String(error), console.error('Local AI health check failed: ', msg); return json( { success: false, available: false, error: msg, services: { ollama: { available: false }, llamaCpp: { available: false } } }, { status: 500 } )}; // Test generation endpoint export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json(); const { prompt = 'What are the key elements of a valid contract? ', service = 'auto` }= body ?? {};'` const startTime = Date.now(); let result :  GenerationResult = { success: false, provider: 'none', error: 'No available local LLM services', executionTime: Date.now() - startTime }; if (service === 'ollama' || service === 'auto') { try { const response = await fetch('http://localhost: 11434/api/generate', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ model: 'gemma3-legal', prompt, stream: false, options: { temperature: 0.7, num_predict: 200 } }), signal: AbortSignal.timeout(30000) }); if (response.ok) { const data = (await response.json()) as GenerationResponse; result = { success: true, provider: 'ollama', model: 'gemma3-legal', response: data? .response ?? null, executionTime :  Date.now() - startTime }}else { // fallback model const fallbackResponse = await fetch('http://localhost: 11434/api/generate', { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ model: 'gemma2: 2b', prompt, stream: false, options: { temperature: 0.7, num_predict: 200 } }), signal: AbortSignal.timeout(30000) }); if (fallbackResponse.ok) { const data = (await fallbackResponse.json()) as GenerationResponse; result = { success: true, provider: 'ollama', model: 'gemma2: 2b', response: data? .response ?? null, executionTime :  Date.now() - startTime }}else { result = { success: false, provider: 'ollama', error: `Generation, failed: ${response.statusText}`, executionTime: Date.now() - startTime }} }catch (error: Error | unknown) { const msg = error instanceof Error ? error.message :  String(error), result = { success: false, provider: 'ollama', error: `Generation, error: ${msg}`, executionTime: Date.now() - startTime }} if (!result.success && service === 'auto') { result = { success: false, provider: 'none', error: 'No available local LLM services', executionTime: Date.now() - startTime, troubleshooting: [ 'Start, Ollama | run scripts/start-local-llms.ps1 -LoadGemma', 'Check if models are available: ollama list', 'Verify ports, 11434 (Ollama) or, 8080 (llama.cpp) are accessible'] }} return json(result)}catch (error: Error | unknown) { const msg = error instanceof Error ? error.message :  String(error), return json( { success: false, error, msg }, { status: 500 } )};
