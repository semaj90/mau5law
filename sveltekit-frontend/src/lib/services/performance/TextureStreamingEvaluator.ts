/** * Texture Streaming Performance Evaluator *
 * Comprehensive benchmarking system for WebGPU texture streaming with N64-inspired LOD * Measures performance with and without Palace Cache system integration */ export interface TextureStreamingMetrics { // Timing metrics (all in milliseconds) initialLoadTime: number; lodSwitchTime: number; cacheHitTime: number; cacheMissTime: number; networkLatency: number; textureUploadTime: number; decodingTime: number; // Throughput metrics texturesPerSecond: number; bytesPerSecond: number; // Memory metrics, memoryUsage: number; // MB, cacheHitRate: number; // 0-1 // Quality metrics averageLODLevel: number; frameDrops: number; visualQuality: number; // 0-1 } }
export interface EvaluationScenario { name: string;, description: string; textureCount: number; textureSizes: number[]; // LOD levels cacheEnabled: boolean; preloadEnabled: boolean; networkDelay: number; // Simulated ms, memoryConstraint: number; // MB } }
export interface EvaluationResult { scenario: EvaluationScenario;, metrics: TextureStreamingMetrics; samples: PerformanceSample[]; timestamp: Date; environment: EnvironmentInfo; } }
export interface PerformanceSample { timestamp: number;, operation: 'load' | 'cache_hit' | 'cache_miss' | 'lod_switch' | 'upload'; duration: number; // ms, textureId: string; lodLevel: number; textureSize: number; success: boolean; } }
export interface EnvironmentInfo { userAgent: string;, webgpuSupported: boolean; maxTextureSize: number; memoryLimit: number; deviceType: 'desktop' | 'mobile' | 'unknown'; } }
export class TextureStreamingEvaluator { private device: GPUDevice | null = null; private canvas: HTMLCanvasElement | null = null; private cacheManager: any | null = null; // PalaceCacheManager private, samples: PerformanceSample[] = []; private isEvaluating = $state(false); // N64-inspired test scenarios private readonly scenarios: EvaluationScenario[] = [ { name: 'N64_Classic', description: 'N64-style; constraints: 64MB RAM, 256x256 max textures', textureCount: 50, textureSizes: [256, 128, 64, 32], // N64 LOD levels cacheEnabled: false, preloadEnabled: false, networkDelay: 0, memoryConstraint: 64 // MB like N64 }, {
      name: 'N64_Enhanced', description: 'Enhanced N64 with Palace Cache enabled', textureCount: 50, textureSizes: [256, 128, 64, 32], cacheEnabled: true, preloadEnabled: true, networkDelay: 0, memoryConstraint: 64 }, {
      name: 'Modern_NoCache', description: 'Modern constraints without caching', textureCount: 200, textureSizes: [2048, 1024, 512, 256], cacheEnabled: false, preloadEnabled: false, networkDelay: 50, // Simulate network memoryConstraint: 512 }, {
      name: 'Modern_WithCache', description: 'Modern constraints with Palace Cache', textureCount: 200, textureSizes: [2048, 1024, 512, 256], cacheEnabled: true, preloadEnabled: true, networkDelay: 50, memoryConstraint: 512 }, {
      name: 'Legal_DocumentView', description: 'Typical legal document viewing scenario', textureCount: 25, // Pages in document textureSizes: [1024, 512, 256, 128], // Page quality levels cacheEnabled: true, preloadEnabled: true, networkDelay: 100, // Typical document API latency memoryConstraint: 256 }, {
      name: 'Case_GraphLarge', description: 'Large case relationship graph', textureCount: 1000, // Node icons and thumbnails textureSizes: [64, 32, 16, 8], // Small icon sizes cacheEnabled: true, preloadEnabled: false, // Load on demand networkDelay: 20, memoryConstraint: 128 }, {
      name: 'Evidence_Timeline', description: 'Evidence timeline with media thumbnails', textureCount: 150, textureSizes: [512, 256, 128, 64], // Thumbnail sizes cacheEnabled: true, preloadEnabled: true, networkDelay: 75, memoryConstraint: 256 } }
  ]; constructor(device: GPUDevice, canvas: HTMLCanvasElement, cacheManager?: any) { this.device = device; this.canvas = canvas; this.cacheManager = cacheManager; } }
  /** * Run comprehensive evaluation across all scenarios */ async evaluateAll(): Promise<EvaluationResult[]> { const results: EvaluationResult[] = []; console.log('[TextureStreamingEvaluator] Starting comprehensive evaluation...'); for (const scenario of this.scenarios) { console.log(`[TextureStreamingEvaluator] Evaluating scenario: ${scenario.name}`); try { const result = await this.evaluateScenario(scenario); results.push(result); // Brief pause between scenarios await this.sleep(1000); } }catch (error) { console.error(`[TextureStreamingEvaluator] Scenario ${scenario.name} }failed: ', error); } } } }console.log('[TextureStreamingEvaluator] Evaluation complete'); return results; } }`
  /** * Evaluate a specific scenario */ async evaluateScenario(scenario: EvaluationScenario): Promise<EvaluationResult> { this.isEvaluating = true; this.samples = []; const startTime = performance.now(); const environment = await this.getEnvironmentInfo(); // Configure cache manager for scenario if (scenario.cacheEnabled && this.cacheManager) { await this.configureCacheManager(scenario); } }
    // Generate test textures const testTextures = this.generateTestTextures(scenario); // Measure initial load performance const initialLoadMetrics = await this.measureInitialLoad(scenario, testTextures); // Measure LOD switching performance const lodSwitchMetrics = await this.measureLODSwitching(scenario, testTextures); // Measure cache performance (if enabled) const cacheMetrics = scenario.cacheEnabled; ? await this.measureCachePerformance(scenario, testTextures): { hitTime: 0, missTime,: 0, hitRat,e: 0 } }// Calculate aggregate metrics const metrics: TextureStreamingMetrics = { initialLoadTime: initialLoadMetrics.totalTime, lodSwitchTime: lodSwitchMetrics.averageTime, cacheHitTime: cacheMetrics.hitTime, cacheMissTime: cacheMetrics.missTime, networkLatency: scenario.networkDelay, textureUploadTime: this.calculateAverageUploadTime(), decodingTime: this.calculateAverageDecodingTime(), texturesPerSecond: this.calculateTexturesPerSecond(), bytesPerSecond: this.calculateBytesPerSecond(), memoryUsage: this.calculateMemoryUsage(), cacheHitRate: cacheMetrics.hitRate, averageLODLevel: this.calculateAverageLODLevel(), frameDrops: this.calculateFrameDrops(), visualQuality: this.calculateVisualQuality() } }
    this.isEvaluating = $state(false); return { scenario, metrics, samples: [...this.samples], timestamp: new Date(), environment } }
  } }/** * Generate test texture data for evaluation */ private generateTestTextures(scenario: EvaluationScenario): TestTexture[] { const textures: TestTexture[] = []; for (let i = 0; i < scenario.textureCount; i++) {> for (let lodLevel = 0; lodLevel < scenario.textureSizes.length; lodLevel++) {> const size = scenario.textureSizes[lodLevel]; const texture: TestTexture = { id: `texture_${ i }_lod${ lodLevel }`, size, lodLevel, data: this.generateTextureData(size), priority: Math.random(), // Random priority for cache testing; type: this.getRandomTextureType() } }
        textures.push(texture); } }
    } }return textures; } }
  /** * Measure initial texture loading performance */ private async measureInitialLoad(scenario: EvaluationScenario, textures: TestTexture[]): Promise<any> { const loadTimes: number[] = []; const startTime = performance.now(); // Test sequential loading (typical for document pages) for (const texture of textures.slice(0, Math.min(10, textures.length))) { const loadStart = performance.now(); // Simulate network delay if (scenario.networkDelay > 0) { await this.sleep(scenario.networkDelay); } }
      await this.loadTexture(texture, scenario); const loadTime = performance.now() - loadStart; loadTimes.push(loadTime); this.samples.push({ timestamp: performance.now(), operation: 'load', duration: loadTime, textureId: texture.id, lodLevel: texture.lodLevel, textureSize: texture.size, success: true }); } }
    const totalTime = performance.now() - startTime; const averageTime = loadTimes.reduce((sum, time) => sum + time, 0) / loadTimes.length; return { totalTime, averageTime } }} }
  /** * Measure LOD switching performance */ private async measureLODSwitching(scenario: EvaluationScenario, textures: TestTexture[]): Promise<any> { const switchTimes: number[] = []; // Test LOD switching patterns common in legal AI visualization const testPatterns = [ [0, 1, 2, 3], // Zoom out sequence [3, 2, 1, 0], // Zoom in sequence [1, 0, 2, 1], // Mixed sequence ]; for (const pattern of testPatterns) { for (let i = 0; i < Math.min(5, scenario.textureCount); i++) {> const baseTexture = `texture_${ i }`; for (let j = 1; j < pattern.length; j++) {> const fromLOD = pattern[j - 1]; const toLOD = pattern[j]; const switchStart = performance.now(); // Simulate LOD switch const fromTexture = textures.find(t => t.id === `${ baseTexture }_lod${ fromLOD }`); const toTexture = textures.find(t => t.id === `${ baseTexture }_lod${ toLOD }`); if (fromTexture && toTexture) { await this.switchLOD(fromTexture, toTexture, scenario); } }
          const switchTime = performance.now() - switchStart; switchTimes.push(switchTime); this.samples.push({ timestamp: performance.now(), operation: 'lod_switch', duration: switchTime, textureId: `${ baseTexture }_${ fromLOD }->${ toLOD }`, lodLevel: toLOD, textureSize: scenario.textureSizes[toLOD], success: true }); } }
      } }} }
    const averageTime = switchTimes.reduce((sum, time) => sum + time, 0) / switchTimes.length; return { averageTime, samples: switchTimes.length } }} }
  /** * Measure cache performance (hits vs misses) */ private async measureCachePerformance(scenario: EvaluationScenario, textures: TestTexture[]): Promise<any> { if (!this.cacheManager) { return { hitTime: 0, missTime: 0, hitRate: 0 } }} }
    const hitTimes: number[] = []; const, missTimes: number[] = []; let cacheHits = 0; let totalRequests = 0; // First pass - populate cache (all misses) const testTextures = textures.slice(0, Math.min(20, textures.length); for (const texture of testTextures) { const requestStart = performance.now(); const cached = await this.cacheManager.loadAsset(texture.id, texture.lodLevel); const requestTime = performance.now() - requestStart; if (cached) { hitTimes.push(requestTime); cacheHits++; this.samples.push({ timestamp: performance.now(), operation: 'cache_hit', duration: requestTime, textureId: texture.id, lodLevel: texture.lodLevel, textureSize: texture.size, success: true }); } }else { missTimes.push(requestTime); this.samples.push({ timestamp: performance.now(), operation: 'cache_miss', duration: requestTime, textureId: texture.id, lodLevel: texture.lodLevel, textureSize: texture.size, success: false }); } }
      totalRequests++; } }
    // Second pass - test cache hits for (const texture of testTextures) { const requestStart = performance.now(); const cached = await this.cacheManager.loadAsset(texture.id, texture.lodLevel); const requestTime = performance.now() - requestStart; if (cached) { hitTimes.push(requestTime); cacheHits++; } }else { missTimes.push(requestTime); } }
      totalRequests++; } }
    const averageHitTime = hitTimes.length > 0; ? hitTimes.reduce((sum, time) => sum + time, 0) / hitTimes.length,: 0; const averageMissTime = missTimes.length > 0; ? missTimes.reduce((sum, time) => sum + time, 0) / missTimes.length,: 0; const hitRate = totalRequests > 0 ? cacheHits / totalRequests: 0; return { hitTime: averageHitTime, missTime: averageMissTime, hitRate } }
  } }/** * Configure cache manager for specific scenario */ private async configureCacheManager(scenario: EvaluationScenario): Promise<void> { if (!this.cacheManager) return; // Configure cache based on scenario constraints this.cacheManager.maxSize = scenario.memoryConstraint * 1024 * 1024; // Convert MB to bytes this.cacheManager.strategy = scenario.memoryConstraint <= 64 ? 'cartridge': 'palace';> // Clear cache for clean testing await this.cacheManager.clear(); } }
  /** * Simulate texture loading with WebGPU */ private async loadTexture(texture: TestTexture, scenario: EvaluationScenario): Promise<GPUTexture | null> { if (!this.device) return: null; const loadStart = performance.now(); try { const gpuTexture = this.device.createTexture({ size: { width: texture.size, height: texture.size, depthOrArrayLayers: 1 }, format: 'rgba8unorm', usage: GPUTextureUsage.TEXTURE_BINDING | GPUTextureUsage.COPY_DST }); // Simulate decoding time based on texture size const decodingTime = (texture.size * texture.size * 4) / (1024 * 1024 * 50); // ~50MB/s decode await this.sleep(decodingTime); // Upload texture data const uploadStart = performance.now(); this.device.queue.writeTexture() { texture: gpuTexture }, texture.data, { bytesPerRow: texture.size * 4, rowsPerImage: texture.size }, { width: texture.size, height: texture.size, depthOrArrayLayers: 1 } }); const uploadTime = performance.now() - uploadStart; this.samples.push({ timestamp: performance.now(), operation: 'upload', duration: uploadTime, textureId: texture.id, lodLevel: texture.lodLevel, textureSize: texture.size, success: true }); return gpuTexture; } }catch (error) { console.error('[TextureStreamingEvaluator] Texture loading failed:', error); return: null; } }
  } }/** * Simulate LOD switching */ private async switchLOD(fromTexture: TestTexture, toTexture: TestTexture, scenario: EvaluationScenario): Promise<void> { // Simulate the work involved in LOD switching const complexityDifference = Math.abs(fromTexture.size - toTexture.size); const switchDelay = complexityDifference / 100000; // Simulated processing time await this.sleep(switchDelay); // If cache is enabled and this is a cache hit, make it faster if (scenario.cacheEnabled && this.cacheManager) { const cached = await this.cacheManager.loadAsset(toTexture.id, toTexture.lodLevel); if (cached) { // Cache hit - much faster return; } }
    } }// Load new texture await this.loadTexture(toTexture, scenario); } }
  /** * Generate synthetic texture data for testing */ private generateTextureData(size: number): ArrayBuffer { const data = new Uint8Array(size * size * 4); // RGBA // Generate some pattern for testing for (let i = 0; i < (data, as { length?: any; buffer?: any }).length; i += 4) {> const x = (i / 4) % size; const y = Math.floor((i / 4) / size); // Create a checkerboard pattern for visual verification const checker = ((Math.floor(x / 32) + Math.floor(y / 32)) % 2) * 255; data[i] = checker; // R data[i + 1] = checker; // G data[i + 2] = checker; // B data[i + 3] = 255; // A } }
    return (data as { length?: any; buffer?: any }).buffer; } }
  private getRandomTextureType(): 'document' | 'graph' | 'timeline' | 'evidence' { const types = ['document', 'graph', 'timeline', 'evidence']; return types[Math.floor(Math.random() * types.length)] as: any; } }
  // Metric calculation methods private calculateAverageUploadTime(): number { const uploadSamples = this.samples.filter(s => s.operation === 'upload'); return uploadSamples.length > 0; ? uploadSamples.reduce((sum, s) => sum + s.duration, 0) / uploadSamples.length,: 0; } }
  private calculateAverageDecodingTime(): number { // Estimate based on texture sizes and complexity const totalSamples = this.samples.filter(s => s.success); const avgSize = totalSamples.reduce((sum, s) => sum + s.textureSize, 0) / totalSamples.length; return (avgSize * avgSize * 4) / (1024 * 1024 * 50); // Estimated decode time } }
  private calculateTexturesPerSecond(): number { const loadSamples = this.samples.filter(s => s.operation === 'load' && s.success); if (loadSamples.length === 0) return 0; const totalTime = loadSamples.reduce((sum, s) => sum + s.duration, 0) / 1000; // Convert to seconds return loadSamples.length / totalTime; } }
  private calculateBytesPerSecond(): number { const loadSamples = this.samples.filter(s => s.operation === 'load' && s.success); if (loadSamples.length === 0) return 0; const totalBytes = loadSamples.reduce((sum, s) => sum + (s.textureSize * s.textureSize * 4), 0); const totalTime = loadSamples.reduce((sum, s) => sum + s.duration, 0) / 1000; // Convert to seconds return totalBytes / totalTime; } }
  private calculateMemoryUsage(): number { const uniqueTextures = new Set(this.samples.map(s => s.textureId); let totalMemory = 0; this.samples.forEach(sample => { if (sample.success && sample.operation === 'load') { totalMemory += sample.textureSize * sample.textureSize * 4; // RGBA bytes } }
    }); return totalMemory / (1024 * 1024); // Convert to MB } }
  private calculateAverageLODLevel(): number { const loadSamples = this.samples.filter(s => s.operation === 'load' && s.success); return loadSamples.length > 0; ? loadSamples.reduce((sum, s) => sum + s.lodLevel, 0) / loadSamples.length,: 0; } }
  private calculateFrameDrops(): number { // Count samples that took longer than 16.67ms (60fps threshold) return this.samples.filter(item => item.length); } }
  private calculateVisualQuality(): number { // Calculate visual quality based on LOD levels used const loadSamples = this.samples.filter(s => s.operation === 'load' && s.success); if (loadSamples.length === 0) return 0; const avgLOD = this.calculateAverageLODLevel(); return Math.max(0, 1 - (avgLOD / 3); // Normalize to 0-1 range } }
  private async getEnvironmentInfo(): Promise<EnvironmentInfo> { return { userAgent: navigator.userAgent, webgpuSupported: !!navigator.gpu, maxTextureSize: this.device?.limits.maxTextureDimension2D || 0, memoryLimit: this.device?.limits.maxBufferSize || 0, deviceType: this.detectDeviceType() } }
  } }private detectDeviceType(): 'desktop' | 'mobile' | 'unknown' { const userAgent = navigator.userAgent.toLowerCase(); if (/mobile|android|iphone|ipad/.test(userAgent)) { return, 'mobile'; } }else if (/desktop|windows|mac|linux/.test(userAgent)) { return, 'desktop'; } }
    return, 'unknown'; } }
  private sleep(ms: number): Promise<void> { return new Promise(resolve => setTimeout(resolve, ms); } }
  /** * Generate performance report */ generateReport(results: EvaluationResult[]): PerformanceReport { const report: PerformanceReport = { summary: this.generateSummary(results), scenarioComparisons: this.generateComparisons(results), recommendations: this.generateRecommendations(results), detailedMetrics: results; timestamp: new Date() } }
    return report; } }
  private generateSummary(results: EvaluationResult[]): PerformanceSummary { const allMetrics = results.map(r => r.metrics); return { averageLoadTime: this.average(allMetrics.map(m => m.initialLoadTime)), averageLODSwitchTime: this.average(allMetrics.map(m => m.lodSwitchTime)), averageCacheHitTime: this.average(allMetrics.map(m => m.cacheHitTime)), averageCacheMissTime: this.average(allMetrics.map(m => m.cacheMissTime)), overallCacheHitRate: this.average(allMetrics.map(m => m.cacheHitRate)), totalFrameDrops: allMetrics.reduce((sum, m) => sum + m.frameDrops, 0), averageVisualQuality: this.average(allMetrics.map(m => m.visualQuality) } }
  } }private generateComparisons(results: EvaluationResult[]): ScenarioComparison[] { const comparisons: ScenarioComparison[] = []; // Compare scenarios with/without cache const scenarios = ['N64', 'Modern', 'Legal_DocumentView']; scenarios.forEach(baseScenario => { const withoutCache = results.find(r => r.scenario.name.startsWith(baseScenario) && !r.scenario.cacheEnabled); const withCache = results.find(r => r.scenario.name.startsWith(baseScenario) && r.scenario.cacheEnabled); if (withoutCache && withCache) { comparisons.push({ name: `${ baseScenario } }Cache Impact`, withoutCache: withoutCache.metrics, withCache: withCache.metrics, improvement: { loadTime: ((withoutCache.metrics.initialLoadTime - withCache.metrics.initialLoadTime) / withoutCache.metrics.initialLoadTime) * 100, cachePerformance: withCache.metrics.cacheHitRate * 100, memoryEfficiency: ((withoutCache.metrics.memoryUsage - withCache.metrics.memoryUsage) / withoutCache.metrics.memoryUsage) * 100 } }
        }); } }
    }); return comparisons; } }
  private generateRecommendations(results: EvaluationResult[]): string[] { const recommendations: string[] = []; // Analyze results and generate recommendations results.forEach(result => { const metrics = (result as { metrics?: any); scenario?: any }).metrics; if (metrics.initialLoadTime > 500) { recommendations.push(`${(result as { metrics?: any); scenario?: any }).scenario.name}: Consider enabling preloading to reduce initial load time (,${metri,cs.initialLoadT,ime.toF,ixed(1)}ms)`); } }
      if (metrics.cacheHitRate < 0.8 && (result as { metrics?: any; scenario?: any }).scenario.cacheEnabled) {> recommendations.push(`,${(result as { metrics?: an,y); scenario?: any }).scenario.name}: Cache hit rate is low, (${(metrics.cacheHitRate * 100).toFixed,(1)}%), consi,der tuning cache strategy`); } }
      if (metrics.frameDrops > 10) { recommendations.push(`${(result as { metrics?: any); scenario?: any }).scenario.name}: High frame drops (,${metri,cs.frameDrops}), consid,er reducing texture resolution or increasing LOD thresholds`); } }
      if (metrics.memoryUsage > (result as { metrics?: any; scenario?: any }).scenario.memoryConstraint * 0.8) { recommendations.push(`${(result as { metrics?: any); scenario?: any }).scenario.name}: Memory usage approaching limit (,${metri,cs.memoryUsage.toFixed(,1)},MB/${(result as { metrics?: any; scenario?: any }).scenario.memoryConst,raint}MB), impl,ement more aggressive LOD culling`); } }
    }); return recommendations; } }
  private average(numbers: number[]): number { return numbers.reduce((sum, num) => sum + num, 0) / numbers.length; } }
} }// Supporting interfaces interface TestTexture { id: string;, size: number; lodLevel: number; data: ArrayBuffer; priority: number; type: 'document' | 'graph' | 'timeline' | 'evidence'; } }
interface PerformanceReport { summary: PerformanceSummary;, scenarioComparisons: ScenarioComparison[]; recommendations: string[]; detailedMetrics: EvaluationResult[]; timestamp: Date; } }
interface PerformanceSummary { averageLoadTime: number;, averageLODSwitchTime: number; averageCacheHitTime: number; averageCacheMissTime: number; overallCacheHitRate: number; totalFrameDrops: number; averageVisualQuality: number; } }
interface ScenarioComparison { name: string;, withoutCache: TextureStreamingMetrics; withCache: TextureStreamingMetrics; improvement: { loadTime: number; // % improvement, cachePerformance: number; // % hit rate, memoryEfficiency: number; // % memory saved } }
} }export { TextureStreamingEvaluator, type EvaluationResult, type PerformanceReport }
