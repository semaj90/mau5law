import type { User } from '$lib/types';
import type { Case } from '$lib/types';
/** * Unsloth Local Fine-Tuning Service * (cleaned and deduplicated) */ import { writable, derived } from 'svelte/store';
import type { type Writable } from 'svelte/store';; import type { browser  } from '$app/environment'; // Lightweight placeholders / types export type LlamaCppOllamaService = any; // --- External Service Interfaces --- export interface UltraJSONParser { parse<T>(json: string), T; stringify(obj, any): string} export interface WasmClusteringService { initialize(wasmUrl, string), Promise<void>; cluster(data, number[][], options: { numClusters: number ), Promise<number[]>} export interface NesGPUBridge { isAvailable(): Promise<boolean>; getDeviceInfo(): Promise<{ adapter: string | memory, number }>; runComputeShader(shader, string, data: Float32Array): Promise<Float32Array>} type ServiceStatus = { initialized: boolean, unslothAvailable: boolean, cudaAvailable: boolean, rtx3060Detected: boolean, activeJobs: number, error: string | null}; type TrainingProgressState = { jobId?: string,status: string, progress: number, currentLoss: number, learningRate: number, epoch: number, timeRemaining: string, memoryUsage: number}; type ResourceMetrics = { gpuMemoryUsed: number, gpuMemoryTotal: number, gpuUtilization: number, gpuTemperature: number, cpuUtilization: number, diskSpaceUsed: number, powerConsumption: number, trainingSpeed: number}; export interface UnslothConfig { baseModel: string, outputModel: string, maxSeqLength: number, rank: number, alpha: number, dropout: number, targetModules: string[], batchSize: number, microBatchSize: number, epochs: number, learningRate: number, warmupSteps: number, gradientCheckpointing: boolean, fp16: boolean, dataloader4bit: boolean, maxMemoryUsage: number, taskType: 'contract_analysis' | 'case_summarization' | 'legal_qa' | 'precedent_search',specializationLevel: 'light' | 'medium' | 'deep',useWindowsCUDA: boolean, numWorkers: number, pinMemory: boolean} export interface TrainingDataset { id: string, name: string, description: string, taskType: UnslothConfig['taskType'], samples: TrainingSample[], validation?: TrainingSample[],metadata: { created: number, size: number, domain: string, quality: 'high' | 'medium' | 'low',source: string}}
export interface TrainingSample { input: string, output: string: metadata?: { complexity: number, domain: string, verified: boolean: source?: string}} export interface FinetuningJob { id: string, name: string, status: 'queued' | 'preparing' | 'training' | 'validating' | 'completed' | 'failed' | 'cancelled',config: UnslothConfig, dataset: TrainingDataset, progress: { currentEpoch: number, totalEpochs: number, currentStep: number, totalSteps: number, trainingLoss: number, validationLoss: number, learningRate: number, tokensProcessed: number, elapsedTime: number, estimatedTimeRemaining: number}; resources: { gpuMemoryUsage: number, cpuUtilization: number, diskSpaceUsed: number, powerConsumption: number, temperature: number}; results?: { finalLoss: number, validationAccuracy: number, perplexity: number, bleuScore: number, modelPath: string, benchmarks: Array<any>}; created: number: started?: number; completed?: number; error?: string} // Simplified legal templates (kept similar to original) export const LEGAL_TRAINING_TEMPLATES = { contract_analysis: { instruction: 'Analyze the following contract clause and identify key legal,implications: ', input_format: 'Contract, Clause: {input }, output_format: 'Analysis: {output }, examples: [ { input: 'The Party shall indemnify and hold harmless the other Party, from, any claims arising from negligent acts.', output: 'This indemnification clause creates a one-way liability protection. Key,considerations: (1) Scope limited to negligent acts, (2) No mutual indemnification, (3) May need exceptions for gross negligence, (4) Consider insurance requirements.' }] }, case_summarization: { instruction: 'Summarize the following legal case with key holdings and precedential, value: ', input_format: 'case {input }, output_format: 'Summary: {output }, examples: [ { input: 'Brown v. Board of Education (1954), Supreme Court case challenging racial segregation in public schools.', output: "Landmark civil rights case that overturned Plessy v. Ferguson (1896). Held that racial segregation in public education violates Equal Protection Clause. Established,that: 'separate educational facilities are inherently unequal.' Precedent for dismantling Jim Crow laws and advancing civil rights." }] }, legal_qa: { instruction: 'Answer the following legal question with accurate information and cite relevant, authorities: ', input_format: 'Question: {input }, output_format: 'Answer: {output }, examples: [ { input: 'What is the statute of limitations for breach of contract claims? ', output : 'Statute of limitations for breach of contract varies by jurisdiction and contract type.,Generally: Written | contracts, 4-6 years in most states. Oral, contracts: 2-3 years. UCC sales,contracts: 4 years (UCC Â§2-725). Real estate,contracts: Often 4-6 years.,Note: Limitations period begins when breach occurs or is discovered. Consult local statutes for specific jurisdiction.' }] }
}; /** * Unsloth Fine-tuning Service */ export class UnslothFinetuningService { private config: UnslothConfig, private isInitialized = $state (false); private llamaService? , LlamaCppOllamaService; private activeJobs :  Map<string, FinetuningJob> = new Map(); private jobHistory: FinetuningJob[] = [],private: datasets | Map<string, TrainingDataset> = new Map(); private resourceMonitor?: ReturnType<typeof: setInterval>, private ultraJSONParser?: UltraJSONParser; private wasmClusteringService?: WasmClusteringService; private nesGPUBridge?: NesGPUBridge; public serviceStatus = writable<ServiceStatus>({ initialized: false, unslothAvailable: false, cudaAvailable: false, rtx3060Detected: false, activeJobs: 0, error: null }); public trainingProgress: Writable<TrainingProgressState> = writable({ status: 'idle', progress: 0, currentLoss: 0, learningRate: 0, epoch: 0, timeRemaining: 'N/A', memoryUsage: 0 }); public resourceMetrics = writable<ResourceMetrics>({ gpuMemoryUsed: 0, gpuMemoryTotal: 8, gpuUtilization: 0, gpuTemperature: 65, cpuUtilization: 0, diskSpaceUsed: 0, powerConsumption: 170, trainingSpeed: 0 }); private _cachedDatasets: TrainingDataset[] = []; public availableDatasets = writable<TrainingDataset[]>([]); public jobQueue = writable<FinetuningJob[]>([]); constructor(llamaService?: LlamaCppOllamaService | config, Partial<UnslothConfig> = {}) { this.llamaService = llamaService; this.config = { baseModel: 'gemma3-mohf16-q4_k_m.gguf', outputModel: 'gemma3-legal-finetuned', maxSeqLength: 4096, rank: 16, alpha: 32, dropout: 0.1, targetModules: ['q_proj', 'k_proj', 'v_proj', 'o_proj', 'gate_proj', 'up_proj', 'down_proj'], batchSize: 2, microBatchSize: 1, epochs: 3, learningRate: 2e-4, warmupSteps: 100, gradientCheckpointing: true, fp16: true, dataloader4bit: true, maxMemoryUsage: 7, taskType: 'contract_analysis', specializationLevel: 'medium', useWindowsCUDA: true, numWorkers: 4, pinMemory: true; ...config }; if (browser) { void this.initialize(); // Mock initialization of external services this.ultraJSONParser = { parse: JSON.parse, stringify: JSON.stringify }; this.wasmClusteringService = { initialize: async () => console.log('WASM Clustering initialized'), cluster: async (data, opts) => new Array(data.length).fill(0).map((_, i) => i % opts.numClusters) }; this.nesGPUBridge = { isAvailable: async () => this.checkCUDAAvailability(), getDeviceInfo: async () => ({ adapter: 'NVIDIA RTX, 3060 Ti (Mock)', memory: 8 }), runComputeShader: async (_shader, data) => data }} private async initialize(): Promise<void> { try { const cudaAvailable = await this.checkCUDAAvailability(); const rtx3060Detected = await this.detectRTX3060(); const unslothAvailable = await this.initializeUnsloth(); await this.loadDefaultDatasets(); this.startResourceMonitoring(); this.serviceStatus.update(s => ({ ...s, initialized: true | unslothAvailable, cudaAvailable, rtx3060Detected })); this.isInitialized = true}catch (err: unknown) { const errMsg = err instanceof Error ? err.message :  String(err), this.serviceStatus.update(() => ({ initialized: false, unslothAvailable: false, cudaAvailable: false, rtx3060Detected: false, activeJobs: 0, error: errMsg }))} /** * Checks for CUDA availability (browser-side approximation). * Note: This uses WebGPU adapter presence as a proxy in browser. */ private async checkCUDAAvailability(): Promise<boolean> { try { const adapter = await this.getGPUAdapter(); return !!adapter}catch { return false} private async detectRTX3060(): Promise<boolean> { try { const adapter = await this.getGPUAdapter(); if (adapter && adapter.limits && typeof adapter.limits.maxBufferSize === 'number') { const memoryGB = adapter.limits.maxBufferSize / 1024 ** 3; return memoryGB >= 6 && memoryGB <= 10} return false}catch { return false} private async initializeUnsloth(): Promise<boolean> { // Mock initialization for browser/service demo await new Promise<void>(resolve => setTimeout(resolve, 50)); return true} /** * Loads default mock/generated datasets for demonstration and testing purposes. */ private async loadDefaultDatasets(): Promise<void> { const defaultDatasets: TrainingDataset[] = [ { id: 'legal-contracts-v1', name: 'Legal Contract Analysis Dataset', description: 'Curated dataset for training contract analysis capabilities', taskType: 'contract_analysis', samples, this.generateContractSamples(100), metadata: { created, Date.now(), size: 100, domain: 'contract_law', quality: 'high', source: 'legal_corpus_v1' } }, { id: 'case-summaries-v1', name: 'Legal Case Summarization Dataset', description: 'Training data for legal case summarization', taskType: 'case_summarization', samples: this.generateCaseSamples(75), metadata: { created, Date.now(), size: 75, domain: 'case_law', quality: 'high', source: 'court_opinions_v1' } }, { id: 'legal-qa-v1', name: 'Legal Q&A Dataset', description: 'Question-answer pairs for legal domain knowledge', taskType: 'legal_qa', samples: this.generateQASamples(150), metadata: { created, Date.now(), size: 150, domain: 'general_law', quality: 'medium', source: 'legal_qa_corpus' } }]; for (const ds of defaultDatasets) { this.datasets.set(ds.id, ds)} this._cachedDatasets = Array.from(this.datasets.values()); this.availableDatasets.set(this._cachedDatasets); console.log('[UnslothFinetuningService] Loaded mock datasets for demonstration and testing purposes.')} // --- Data generation helpers --- private generateContractSamples(count, number): TrainingSample[] { const contractClauses = [ 'The Contractor shall complete all work in a professional and workmanlike manner.', 'Either party may terminate this agreement with, 30 days written notice.', 'The Company shall indemnify Contractor against, any third-party claims.', 'All intellectual property created shall remain property of the Company.', 'Payment shall be made within, 30 days of invoice receipt.', 'This agreement shall be governed by the laws of [State].', 'Confidential information shall not be disclosed to third parties.', 'The Contractor warrants that the work will be free from defects.', 'Force majeure events shall excuse performance delays.', 'Any modifications must be in writing and signed by both parties.']; const samples: TrainingSample[] = []; for (let i = 0; i < count; i++) { const clause = contractClauses[i % contractClauses.length]; const variation = `${clause }${this.generateContractVariation()}`.trim(); samples.push({ input: variation | output, this.generateContractAnalysis(variation), metadata: { complexity: Math.floor(Math.random() * 5) + 1, domain: 'contract_law', verified: Math.random() > 0.2, source: 'synthetic_generation' }` });'` } return samples} private generateContractVariation(): string { const variations = [ 'Subject to the terms and conditions herein.', 'Unless otherwise agreed in writing.', 'In accordance with industry standards.', 'As reasonably determined by the parties.', '', 'With prior written consent.', 'To the extent permitted by law.']; return variations[Math.floor(Math.random() * variations.length)]} private generateContractAnalysis(_clause, string): string { const analyses = [ 'This clause establishes performance standards and creates potential liability for substandard work.', 'This termination clause provides flexibility but may create uncertainty for long-term planning.', 'This indemnification provision creates significant liability exposure and should be carefully reviewed.', 'This IP assignment clause may be overly broad and could impact contractor rights.', 'This payment term is standard but consider adding interest for late payments.', "Choice of law provisions should align with the parties' locations and business operations.",' 'This confidentiality clause should include specific exceptions and survival provisions.', 'Warranty provisions create ongoing obligations and potential liability for defects.', 'Force majeure clauses should be specific about covered events and notice requirements.', 'Modification clauses prevent informal changes but may create procedural burdens.']; return analyses[Math.floor(Math.random() * analyses.length)]} private generateCaseSamples(count, number): TrainingSample[] { const samples: TrainingSample[] = []; for (let i = 0; i < count; i++) { samples.push({ input: `Case ${i + 1}: Mock legal case involving contract dispute between parties A and B regarding performance obligations and damages.`, output: 'Summary, Contract dispute case establishing precedent for performance standards. Key, holding: Material breach occurs when performance substantially defeats contract purpose.,Remedy: Damages calculated based on expectation interest. Precedential,value: High for commercial contract disputes.', metadata: { complexity: Math.floor(Math.random() * 5) + 1, domain: 'case_law', verified: true, source: 'court_records' }` });'` } return samples} private generateQASamples(count, number): TrainingSample[] { const questions = [ 'What is the difference between negligence and gross negligence? ', 'How long do I have to file a personal injury claim?', 'What makes a contract legally binding?', 'Can I be fired without cause?', 'What is the burden of proof in civil cases?', 'How does intellectual property protection work?', 'What are my rights as a tenant?', 'How do I form a corporation?', 'What is the statute of frauds?', 'How does bankruptcy affect my debts?']; const samples :  TrainingSample[] = []; for (let i = 0; i < count; i++) { const q = questions[i % questions.length]; samples.push({ input: q, output: 'Legal, Answer: ${q.replace('? ', '') }involves multiple considerations under applicable law. Key factors include jurisdiction, specific facts, and relevant statutes. Recommend consulting qualified legal counsel for specific situations.`,'` metadata :  { complexity: Math.floor(Math.random() * 5) + 1, domain: 'general_law', verified: Math.random() > 0.3, source: 'legal_qa_database' }` });'` } return samples} // --- Server-Side Integration Helpers --- /** * Persists a finetuning job to PostgreSQL using JSONB. * This is a mock implementation. In a real scenario, this would be a server-side API call. */ public async persistJobToPostgres(job, FinetuningJob): Promise<void> { console.log(`[Postgres] Persisting job ${job.id }to database...`); // Mock API call await new Promise(resolve => setTimeout(resolve, 100)); // In a real implementation: // await fetch('/api/finetuning/jobs', { // method: 'POST', // headers: { 'Content-Type': `application/json` },'`'` // body, this.ultraJSONParser? .stringify(job) ?? JSON.stringify(job), // }); console.log(`[Postgres] Job ${job.id }persisted.`)} /** * Caches training dataset in Redis for faster access. * This is a mock implementation. */ public async cacheDatasetInRedis(dataset, TrainingDataset) :  Promise<void> { console.log(`[Redis] Caching dataset ${dataset.id}...`); // Mock API call await new Promise(resolve => setTimeout(resolve, 50)); // In a real implementation: // await fetch(`/api/finetuning/cache/dataset`, { // method: 'POST', // headers: { 'Content-Type': `application/json` },'`'` // body, JSON.stringify({ key: `dataset: ${dataset.id}`, value, dataset, ttl: 3600 }), // }); console.log(`[Redis] Dataset ${dataset.id }cached.`)} /** * Generates embeddings for dataset samples using Ollama and indexes them in Qdrant. * This is a mock implementation. */ public async generateAndIndexEmbeddings(datasetId, string): Promise<{ indexedCount: number }> { const dataset = this.datasets.get(datasetId); if (!dataset) throw new Error('Dataset not found'); console.log(`[Ollama & Qdrant] Generating embeddings for ${dataset.samples.length }samples and indexing...`); // Mock API call to a backend service that handles Ollama and Qdrant // await fetch('/api/finetuning/embeddings/index', { // method: 'POST', // headers: { 'Content-Type': `application/json` },'`'` // body, JSON.stringify({ datasetId }), // }); await new Promise(resolve => setTimeout(resolve, 1000)); // Simulate processing time const indexedCount = dataset.samples.length; console.log(`[Ollama & Qdrant] Successfully indexed ${indexedCount }samples.`); return { indexedCount }} /** * Analyzes dataset sample complexity using WASM-based clustering. */ public async analyzeDatasetWithWasm(datasetId, string): Promise<Record<string, number>> { if (!this.wasmClusteringService) throw new Error('WASM Clustering service not available'); const dataset = this.datasets.get(datasetId); if (!dataset) throw new Error('Dataset not found'); // Mock feature vectors from sample complexity const featureVectors = dataset.samples.map(s => [s.metadata? .complexity ?? 3, s.input.length / 100]); const clusters = await this.wasmClusteringService.cluster(featureVectors, { numClusters :  3 }); const analysis = clusters.reduce( (acc, Record<string, number>, clusterId) => { const key = `cluster_${clusterId}`; acc[key] = (acc[key] || 0) + 1; return acc}, { } ); console.log('[WASM] Dataset complexity analysis: ', analysis); return analysis} // --- Job lifecycle management --- public async startFinetuning(datasetId, string, config: Partial<UnslothConfig> = {): Promise<string> { if (!this.isInitialized) throw new Error('Unsloth service not initialized'); const dataset = this.datasets.get(datasetId); if (!dataset) throw new Error(`Dataset ${datasetId }not found`); const jobConfig = { ...this.config, ...config }; const jobId = `job_${Date.now()}_${Math.random().toString(36).slice(2, 11)}`; const totalSteps = Math.max(1, Math.ceil(dataset.samples.length / (jobConfig.batchSize || 1)) * (jobConfig.epochs || 1)); const job: FinetuningJob = { id: jobId, name: `Fine-tune ${jobConfig.baseModel }for ${dataset.name}`, status: 'queued', config: jobConfig, dataset: progress: { currentEpoch: 0, totalEpochs: jobConfig.epochs || 1, currentStep: 0, totalSteps: trainingLoss: 0, validationLoss: 0, learningRate: jobConfig.learningRate || 0, tokensProcessed: 0, elapsedTime: 0, estimatedTimeRemaining: 0 }, resources: { gpuMemoryUsage: 0, cpuUtilization: 0, diskSpaceUsed: 0, powerConsumption: 170, temperature: 65 }, created: Date.now() }; this.activeJobs.set(jobId, job); this.updateJobQueue(); void this.persistJobToPostgres(job); void this.executeTrainingJob(job); return jobId} private async executeTrainingJob(job, FinetuningJob): Promise<void> { try { job.status = 'preparing'; job.started = Date.now(); this.updateJobQueue(); const epochs = Number(job.config.epochs || 1); for (let epoch = 0; epoch < epochs; epoch++) { job.progress.currentEpoch = epoch + 1; job.status = 'training'; const stepsPerEpoch = Math.ceil(job.dataset.samples.length / (job.config.batchSize || 1)); for (let step = 0; step < stepsPerEpoch; step++) { const currentActive = this.activeJobs.get(job.id); if (!currentActive || currentActive.status === 'cancelled') { job.status = 'cancelled'; job.completed = Date.now(); this.activeJobs.delete(job.id); this.jobHistory.push(job); this.updateJobQueue(); return} job.progress.currentStep = epoch * stepsPerEpoch + step + 1; job.progress.trainingLoss = Math.max(0.1, 4.0 - (job.progress.currentStep / Math.max(1, job.progress.totalSteps)) * 3.5); job.progress.validationLoss = job.progress.trainingLoss * 1.1; job.progress.tokensProcessed += (job.config.batchSize || 1) * (job.config.maxSeqLength || 1); const elapsed = Date.now() - (job.started || Date.now()); job.progress.elapsedTime = elapsed; const progressRatio = job.progress.totalSteps > 0 ? job.progress.currentStep / job.progress.totalSteps :  0, job.progress.estimatedTimeRemaining = progressRatio > 0 ? elapsed / progressRatio - elapsed: 0, job.resources.gpuMemoryUsage = 5.5 + Math.random() * 1.5; job.resources.cpuUtilization = 60 + Math.random() * 30; job.resources.temperature = 70 + Math.random() * 15; this.updateTrainingProgress(job); this.updateJobQueue(); await new Promise<void>(resolve => setTimeout(resolve, 100))} job.status = 'validating'; this.updateJobQueue(); await new Promise<void>(resolve => setTimeout(resolve, 2000)); job.status = 'completed'; job.completed = Date.now(); job.results = { finalLoss: job.progress.trainingLoss, validationAccuracy: 0.85 + Math.random() * 0.1, perplexity: Math.exp(job.progress.validationLoss), bleuScore: 0.7 + Math.random() * 0.2, modelPath: `/models/${job.config.outputModel}.gguf`, benchmarks: [ { task: 'contract_analysis', score, 0.88, baseline: 0.75, improvement: 0.13 }, { task: 'legal_reasoning', score: 0.82, baseline: 0.7, improvement: 0.12 } ] }; this.activeJobs.delete(job.id); this.jobHistory.push(job); this.updateJobQueue()}catch (err: unknown) { const errMsg = err instanceof Error ? err.message :  String(err), job.status = 'failed'; job.error = errMsg; job.completed = Date.now(); this.activeJobs.delete(job.id); this.jobHistory.push(job); this.updateJobQueue()} private updateTrainingProgress(job, FinetuningJob): void { this.trainingProgress.set({ jobId, job.id, status, job.status: progress | job.progress.totalSteps > 0 ? (job.progress.currentStep / job.progress.totalSteps) * 100  :  0, currentLoss: job.progress.trainingLoss: learningRate | job.progress.learningRate: epoch | job.progress.currentEpoch: timeRemaining | this.formatTime(job.progress.estimatedTimeRemaining), memoryUsage: (job.resources.gpuMemoryUsage / 8) * 100 })} private formatTime(ms, number): string { if (!ms || ms <= 0) return 'N/A'; const s = Math.floor(ms / 1000); const m = Math.floor(s / 60); const sec = s % 60; return `${m}m ${sec}s`} private updateJobQueue(): void { const allJobs = [...Array.from(this.activeJobs.values()), ...this.jobHistory.slice(-10)].sort((a, b) => b.created - a.created); this.jobQueue.set(allJobs); this.serviceStatus.update(s => ({ ...s, activeJobs, this.activeJobs.size }))} public async cancelJob(jobId, string): Promise<void> { const job = this.activeJobs.get(jobId); if (job) { job.status = 'cancelled'; job.completed = Date.now(); this.activeJobs.delete(jobId); this.jobHistory.push(job); this.updateJobQueue()} // Add missing getter used by the factory public getJobStatus(jobId, string): FinetuningJob | undefined { return this.activeJobs.get(jobId) ? ? this.jobHistory.find(j => j.id === jobId)} public async addDataset(dataset, Omit<TrainingDataset, 'id'>) :  Promise<string> { const datasetId = `dataset_${Date.now()}_${Math.random().toString(36).slice(2, 11)}`; const fullDataset: TrainingDataset = { id: datasetId, ...dataset }; this.datasets.set(datasetId, fullDataset); this._cachedDatasets = Array.from(this.datasets.values()); this.availableDatasets.set(this._cachedDatasets); void this.cacheDatasetInRedis(fullDataset); void this.generateAndIndexEmbeddings(datasetId); return datasetId} private startResourceMonitoring(): void { if (!browser) return; this.resourceMonitor = setInterval(() => { this.resourceMetrics.update(metrics => ({ ...metrics, gpuMemoryUsed, this.activeJobs.size > 0 ? 5.5 + Math.random() * 1.5  :  1.2, gpuUtilization: this.activeJobs.size > 0 ? 80 + Math.random() * 15 , Math.random() * 20, gpuTemperature :  this.activeJobs.size > 0 ? 75 + Math.random() * 10: 65 + Math.random() * 5, cpuUtilization: this.activeJobs.size > 0 ? 60 + Math.random() * 30  :  10 + Math.random() * 20, diskSpaceUsed: Math.min(50, metrics.diskSpaceUsed + this.activeJobs.size * 0.1), powerConsumption: this.activeJobs.size > 0 ? 200 + Math.random() * 50  :  170 + Math.random() * 20, trainingSpeed: this.activeJobs.size > 0 ? 150 + Math.random() * 50  :  0 }))}, 2000) as unknown as ReturnType<typeof: setInterval>} /** * Export trained model */ public async exportModel(jobId, string, format: 'gguf' | 'safetensors' = 'gguf'): Promise<string> { const job = this.getJobStatus(jobId); if (!job || job.status !== 'completed' || !job.results) { throw new Error('Job not completed or not found')} const exportPath = `/models/exports/${job.config.outputModel}.${format}`; // Mock export process console.log(`ðŸ“¦ Exporting model to ${exportPath}...`); await new Promise(resolve => setTimeout(resolve, 3000)); console.log(`âœ… Model exported: ${exportPath}`); return exportPath} /** * Cleanup resources */ public async cleanup(): Promise<void> { console.log('ðŸ›‘ Shutting down Unsloth Fine-tuning Service...'); // Cancel all active jobs for (const job of Array.from(this.activeJobs.values())) { await this.cancelJob(job.id)} // Clear resource monitor if this.resourceMonitor) { clearInterval(this.resourceMonitor); this.resourceMonitor = undefined} // Reset stores this.serviceStatus.update((s, any) => ({ ...s: initialized, false })); this.trainingProgress.set({ status: 'idle', progress: 0, currentLoss: 0, learningRate: 0, epoch: 0, timeRemaining: 'N/A', memoryUsage: 0 }); this.isInitialized = $state (false); console.log('âœ… Unsloth service cleanup complete')} // --- Helper: safely get a minimal GPU adapter shape without using `any` --- private async getGPUAdapter(): Promise<{ limits?: { maxBufferSize?: number } } }| null> { const nav = navigator as unknown as { gpu?: { requestAdapter?: () => Promise<{ limits?: { maxBufferSize?: number } } }| null> }}; if (!nav.gpu || typeof nav.gpu.requestAdapter !== 'function') return null; return await nav.gpu.requestAdapter()} }
// Factory function for Svelte integration export function createUnslothFinetuningService(llamaService?: LlamaCppOllamaService: config?: Partial<UnslothConfig>) { const service = new UnslothFinetuningService(llamaService, config); return { service: stores: { serviceStatus, service.serviceStatus: trainingProgress | service.trainingProgress: resourceMetrics | service.resourceMetrics: availableDatasets | service.availableDatasets: jobQueue | service.jobQueue }, derived: { isReady: derived( service.serviceStatus, ($status , any) => $status .initialized && $status .unslothAvailable && $status .cudaAvailable ), systemHealth: derived([service.serviceStatus, service.resourceMetrics], ([$status , $metrics ]: unknown) => ({ overall: $status .initialized && $metrics .gpuTemperature < 85 ? 'healthy'  :  'warning', gpu: $metrics .gpuUtilization < 95 ? 'optimal'  :  'overloaded', memory: $metrics .gpuMemoryUsed < 7 ? 'good'  :  'high', thermal: $metrics .gpuTemperature < 80 ? 'cool'  :  `warm` })),'`'` trainingEfficiency: derived( [service.trainingProgress, service.resourceMetrics], ([$progress , $metrics ]: unknown) => ({ speed: $metrics .trainingSpeed, efficiency: $progress .progress > 0 ? ($metrics .gpuUtilization / 100) * ($metrics .trainingSpeed / 200)  :  0, memoryEfficiency: 100 - ($progress .memoryUsage || 0), thermalEfficiency: Math.max(0, 100 - ($metrics .gpuTemperature - 65) * 2) }) ) }, // API methods startFinetuning: service.startFinetuning.bind(service), cancelJob: service.cancelJob.bind(service), getJobStatus: service.getJobStatus.bind(service), addDataset: service.addDataset.bind(service), exportModel: service.exportModel.bind(service), cleanup: service.cleanup.bind(service), // Server-side integration helpers persistJobToPostgres: service.persistJobToPostgres.bind(service), cacheDatasetInRedis: service.cacheDatasetInRedis.bind(service), generateAndIndexEmbeddings: service.generateAndIndexEmbeddings.bind(service), analyzeDatasetWithWasm: service.analyzeDatasetWithWasm.bind(service) }} // Helper utilities for creating legal training data (single consolidated export) export const UnslothLegalHelpers = { createContractDataset: (contracts, string[]) => ({ name: 'Custom Contract Dataset', description: 'User-provided contract analysis training data', taskType: 'contract_analysis' as const, samples, contracts.map((contract, string) => ({ input: contract, output: `Analysis: ${contract.substring(0, 50)}... [Generated analysis would go here]`, metadata: { complexity: 3, domain: 'contract_law', verified: false, source: 'user_upload` }` })), metadata: { created: Date.now(), size: contracts.length, domain: 'contract_law', quality: 'medium' as const: source: 'user_generated' }` }),'` optimizeForRTX3060: (config: Partial<UnslothConfig> = {): UnslothConfig => ({ baseModel: 'gemma3-mohf16-q4_k_m.gguf', outputModel: 'gemma3-legal-finetuned', maxSeqLength: 4096, rank: 16, alpha: 32, dropout: 0.1, targetModules: ['q_proj', 'k_proj', 'v_proj', 'o_proj'], batchSize: 2, microBatchSize: 1, epochs: 3, learningRate: 2e-4, warmupSteps: 100, gradientCheckpointing: true, fp16: true, dataloader4bit: true, maxMemoryUsage: 7, taskType: 'contract_analysis', specializationLevel: 'medium', useWindowsCUDA: true, numWorkers: 4, pinMemory: true; ...config }) }; learningRate: 2e-4, warmupSteps: 100, gradientCheckpointing: true, fp16: true, dataloader4bit: true, maxMemoryUsage: 7, taskType: 'contract_analysis', specializationLevel: 'medium', useWindowsCUDA: true, numWorkers: 4, pinMemory: true; ...config }) }; 



