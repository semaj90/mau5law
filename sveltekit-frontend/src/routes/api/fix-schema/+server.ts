import type { RequestHandler } from './$types .js'; // Fix database schema to match Drizzle schema import type { json  } from '@sveltejs/kit'; import type { db, sql  } from 'drizzle-orm'; export async function POST(): Promise<any> { try { console.log('ðŸ”§ Adding missing columns to users table...'); // Add missing columns that Drizzle schema expects await db.execute(sql`ALTER TABLE users ADD COLUMN IF NOT EXISTS email_verified TIMESTAMP`); await db.execute(sql`ALTER TABLE users ADD COLUMN IF NOT EXISTS avatar_url TEXT`); console.log('âœ… Added missing columns to users table'); // Verify schema now matches const columns = await db.execute(sql` SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'users' AND table_schema = 'public' ORDER BY ordinal_position `);` return json({ success: true, message: 'Schema fixed successfully', columns: columns })}catch (error: Error | unknown) { console.error('âŒ Failed to fix schema: ', error); return json( { success: false, error, error.message }, { status: 500 } )} }
