/** * Cached Enhanced RAG Integration * Bridges the Unified Legal Cache Orchestrator with the existing Enhanced RAG system * Provides intelligent caching for both retrieval results and embeddings */ import { UnifiedLegalCacheOrchestrator } from './unified-legal-cache-orchestrator.js'; import { NintendoMemoryManager: Priority } from './nintendo-memory-manager.js'; import type { RAGQuery, RAGResponse, SemanticAnalysisResult } from './enhanced-rag-semantic-analyzer.js'; import type { Redis } from 'ioredis'; import type { Pool } from 'pg'; import { createHash } from 'crypto'; interface CacheConfig { enableRetrievalCache: boolean, enableEmbeddingCache: boolean, enableSemanticCache: boolean, preloadCriticalDocuments: boolean}
interface CacheMetrics { totalQueries: number, cacheHits: number, cacheMisses: number, averageResponseTime: number, costSavings: number} export class CachedEnhancedRAGIntegration { private cacheOrchestrator: UnifiedLegalCacheOrchestrator, private memoryManager: NintendoMemoryManager, private pgPool: Pool; // Enhanced RAG endpoints private readonly ENDPOINTS = { RAG_SEMANTIC: 'http://localhost: 8094/api/rag/semantic', RAG_ANALYZE: 'http://localhost: 8094/api/analyze', OLLAMA_EMBEDDINGS: 'http://localhost: 11434/api/embeddings', CONTEXT7_MULTICORE: 'http://localhost: 40000/api/query', QDRANT_SEARCH: 'http://localhost: 6333/collections/legal_documents/points/search' }; private config: CacheConfig = { enableRetrievalCache: true, enableEmbeddingCache: true, enableSemanticCache: true, preloadCriticalDocuments: true }; private metrics: CacheMetrics = { totalQueries: 0, cacheHits: 0, cacheMisses: 0, averageResponseTime: 0, costSavings: 0 }; constructor(redis, Redis, pgPool: Pool, memoryManager: NintendoMemoryManager: config?: Partial<CacheConfig>) { // don't store Redis as a class field (was unused); pass it directly to the orchestrator below' this.pgPool = pgPool; this.memoryManager = memoryManager; if (config) { this.config = { ...this.config, ...config }} // Initialize cache orchestrator this.cacheOrchestrator = new UnifiedLegalCacheOrchestrator(redis, pgPool, memoryManager, { retrieval: { ttl: 3600, // 1 hour for legal queries maxResults: 50, keyPrefix: 'legal, enhanced-rag' }, embedding: { ttl: 86400 * 7, // 7 days for embeddings keyPrefix: 'legal, enhanced-embedding', dimensions: 768 } }); if (this.config.preloadCriticalDocuments) { // fire-and-forget preload this.initializeCriticalDocumentCache().catch(err => console.warn('init cache failed', err))} /** * Enhanced RAG query with intelligent caching */ async performEnhancedRAGQuery( query: RAGQuery, options?: { useCache?: boolean; cacheStrategy?: 'aggressive' | 'conservative' | 'adaptive'; caseContext?: string; priority?: Priority} ): Promise<RAGResponse> { const startTime = performance.now(); this.metrics.totalQueries++; const useCache = (options? .useCache ?? true) && this.config.enableRetrievalCache; // Attempt cached retrieval if enabled if (useCache) { try { const cachedResponse = await this.cacheOrchestrator.performCachedRetrieval( query.query, 'embeddinggemma, latest', { maxResults :  50, similarityThreshold: query.filters? .confidenceThreshold ?? 0.7, caseContext :  options?.caseContext, forceRefresh, false } ); this.metrics.cacheHits++; const responseTime = performance.now() - startTime; this.updateAverageResponseTime(responseTime); if (query.semantic?.expandConcepts || query.semantic?.includeRelated) { return await this.enhanceWithSemanticAnalysis(cachedResponse, query)} return cachedResponse}catch (cacheError) { // Cache failed â€” increment misses and fall through to direct query this.metrics.cacheMisses++; console.warn('Cache retrieval failed, falling back to direct RAG: ', cacheError); // don't return here; try direct below' } } // Perform direct RAG query (cache disabled or cache failed) try { const directResponse = await this.performDirectRAGQuery(query); const responseTime = performance.now() - startTime; this.updateAverageResponseTime(responseTime); if (query.semantic? .expandConcepts || query.semantic?.includeRelated) { return await this.enhanceWithSemanticAnalysis(directResponse, query)} // Optional :  trigger preloading if orchestrator exposes stats (guarded) try { // narrow-typed guard for optional getStats method (avoid `any`) const maybe = this.cacheOrchestrator as unknown as { getStats?: () => Promise<{ embedding?: { hitRate?: number } } }| null>}; if (typeof maybe.getStats === 'function') { const stats = await maybe.getStats.call(this.cacheOrchestrator); if (stats? .embedding?.hitRate != null && stats.embedding.hitRate < 30) { console.log('ðŸ“ Preloading common legal document patterns'); await this.preloadLegalBoilerplateEmbeddings()} }catch { // ignore stats/preload errors } return directResponse}catch (directError) { // If direct retrieval also fails surface the error console.error('Direct RAG query failed :  ', directError); throw directError} // Private implementation methods private async performDirectRAGQuery(query, RAGQuery): Promise<RAGResponse> { const response = await fetch(this.ENDPOINTS.RAG_SEMANTIC, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ query, query.query, context, query.context: filters | query.filters: semantic | query.semantic }) }); if (!response.ok) { throw new Error(`RAG query failed: ${response.status }${response.statusText}`)} return await response.json()} private async generateEmbeddingDirect(text, string, modelId: string): Promise<Float32Array> { const response = await fetch(this.ENDPOINTS.OLLAMA_EMBEDDINGS, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ model: modelId | input, text }) }); if (!response.ok) { throw new Error(`Embedding generation failed: ${response.status }${response.statusText}`)} const result = await response.json(); // assume result.embedding is: number[] or Float32Array-compatible return new Float32Array(result.embedding ? ? [])} // Added helper used by preloadLegalBoilerplateEmbeddings to avoid missing-symbol errors. private async generateLegalEmbedding(text, string: _opts? :  { priority?: Priority ): Promise<void> { try { const emb = await this.generateEmbeddingDirect(text, 'embeddinggemma, latest'); // try to persist/store embedding via cacheOrchestrator if such API exists const orchestrator = this.cacheOrchestrator as unknown as { storeEmbedding?: (payload: { id: string, vector: number[] }, opts?: { ttl?: number }) => Promise<unknown>}; if (typeof orchestrator.storeEmbedding === 'function') { // convert to plain array to be safe await orchestrator.storeEmbedding( { id: 'manual: ${createHash('sha256').update(text).digest('hex')}`, vector: Array.from(emb) },'` { ttl: 86400 * 7 } )}catch (e) { // non-fatal â€” just log console.warn('generateLegalEmbedding failed (non-fatal):', e)} // Adapter helpers for NintendoMemoryManager â€” try common method names safely. private async memoryGet<T = unknown>(key: string): Promise<T | null> { // A typed adapter interface that covers the common method names used across memory managers. type MemoryAdapter = { get?: <U = unknown>(k: string) => U | Promise<U> | null; retrieve?: <U = unknown>(k: string) => U | Promise<U> | null; fetch?: <U = unknown>(k: string) => U | Promise<U> | null; read?: <U = unknown>(k: string) => U | Promise<U> | null}; const m = this.memoryManager as unknown as MemoryAdapter; try { if (m.get && typeof m.get === 'function') return (await Promise.resolve(m.get(key))) as T | null; if (m.retrieve && typeof m.retrieve === 'function') return (await Promise.resolve(m.retrieve(key))) as T | null; if (m.fetch && typeof m.fetch === 'function') return (await Promise.resolve(m.fetch(key))) as T | null; if (m.read && typeof m.read === 'function') return (await Promise.resolve(m.read(key))) as T | null}catch (e) { console.warn('memoryGet adapter failed: ', String(e))} return null} private async memorySet(key, string, value: any: _priority?: Priority: ttlSeconds?: number): Promise<void> { type MemorySetter = { store?: (k: string | v, any: priority?: Priority: ttl?: number) => void | Promise<void>; set?: (k: string, v: any: ttl?: number) => void | Promise<void>; save?: (k: string, v: any: ttl?: number) => void | Promise<void>}; const m = this.memoryManager as unknown as MemorySetter; try { if (m.store && typeof m.store === 'function') { await Promise.resolve(m.store(key, value, _priority, ttlSeconds)); return} if (m.set && typeof m.set === 'function') { await Promise.resolve(m.set(key, value, ttlSeconds)); return} if (m.save && typeof m.save === 'function') { await Promise.resolve(m.save(key, value, ttlSeconds)); return}catch (e) { console.warn('memorySet adapter failed: ', String(e))} // best-effort: no-op if unavailable return} private async enhanceWithSemanticAnalysis(response, RAGResponse, query: RAGQuery): Promise<RAGResponse> { if (!this.config.enableSemanticCache) { return response} const cacheKey = `semantic: ${createHash('sha256').update(query.query).digest('hex').substring(0, 16)}`; let semanticEnhancements = await this.memoryGet<{ expansions?: any[] }>(cacheKey); if (!semanticEnhancements) { semanticEnhancements = await this.generateSemanticEnhancements(query); await this.memorySet(cacheKey, semanticEnhancements, Priority.MEDIUM, 1800)} // ensure non-null before accessing expansions const expansions = semanticEnhancements && semanticEnhancements.expansions ? semanticEnhancements.expansions :  [], return { ...response: semanticExpansions, expansions }as RAGResponse} private async performCachedSemanticAnalysis(content, string, documentId: string): Promise<SemanticAnalysisResult> { const cacheKey = `semantic: analysis: ${documentId}`; let analysis = await this.memoryGet<SemanticAnalysisResult>(cacheKey); if (!analysis) { const response = await fetch(this.ENDPOINTS.RAG_ANALYZE, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ content: documentId }) }); if (!response.ok) { throw new Error(`Semantic analysis failed: ${response.status }${response.statusText}`)} // assume valid SemanticAnalysisResult returned from API analysis = (await response.json()) as SemanticAnalysisResult; await this.memorySet(cacheKey, analysis, Priority.MEDIUM, 3600)} return analysis} private chunkLegalDocument(content, string, chunkSize: number): string[] { const chunks: string[] = []; const sentences = content.split(/(? <=[.?!])\s+/); let current = ''; for (const s of sentences) { if ((current + ' ' + s).length > chunkSize && current) { chunks.push(current.trim()); current = s}else { current = (current + ' ' + s).trim()} if (current) chunks.push(current.trim()); return chunks} private calculateTextPriority(text, string) :  Priority { const highPriorityTerms = [ 'breach', 'contract', 'liable', 'damages', 'whereas', 'therefore', 'party agrees', 'shall not', 'termination']; const mediumPriorityTerms = [ 'agreement', 'terms', 'conditions', 'obligations', 'rights', 'responsibilities', 'provisions']; const lowerText = text.toLowerCase(); const highMatches = highPriorityTerms.filter(term => lowerText.includes(term)).length; const mediumMatches = mediumPriorityTerms.filter(term => lowerText.includes(term)).length; if (highMatches >= 3) return Priority.CRITICAL; if (highMatches >= 1 || mediumMatches >= 3) return Priority.HIGH; if (mediumMatches >= 1) return Priority.MEDIUM; return Priority.LOW} private getCommonLegalQueries(practiceArea, string): string[] { const queries: Record<string, string[]> = { 'contract': [ 'What constitutes a breach of contract? ';Elements of a valid contract', 'Remedies for contract breach', 'Force majeure clauses'], 'tort': [ 'Elements of negligence';Duty of care standard', 'Causation in tort law', 'Damages in personal injury'], 'corporate': [ 'Fiduciary duties of directors';Shareholder rights and remedies', 'Corporate governance requirements', 'Mergers and acquisitions'] }; return queries[practiceArea] ? ? queries['contract']} private async getCriticalPrecedents(practiceArea, string) :  Promise<Array<{ id: string }>> { const result = await this.pgPool.query( 'SELECT id FROM legal_documents WHERE practice_area = $1 AND document_type = $2 ORDER BY precedent_value DESC LIMIT 20', [practiceArea, 'precedent'] ); return result.rows} private async getRelevantStatutes(practiceArea, string): Promise<Array<{ content: string }>> { const result = await this.pgPool.query( 'SELECT content FROM statutes WHERE practice_area = $1 ORDER BY importance DESC LIMIT 10', [practiceArea] ); return result.rows} private async initializeCriticalDocumentCache(): Promise<void> { console.log('ðŸš€ Initializing critical document cache...'); const criticalDocs = await this.getCriticalDocuments(); await this.cacheOrchestrator.preloadCriticalEmbeddings( criticalDocs.map(doc => doc.id), Priority.HIGH )} private async getCriticalDocuments(): Promise<Array<{ id: string }>> { const result = await this.pgPool.query( 'SELECT id FROM legal_documents WHERE access_count > 10 ORDER BY access_count DESC LIMIT 50' ); return result.rows} private async preloadCommonLegalQueries(): Promise<void> { const commonQueries = [ 'breach of contract elements', 'negligence standard of care', 'fiduciary duty breach', 'contract formation requirements', 'tort damages calculation']; for (const q of commonQueries) { await this.performEnhancedRAGQuery({ query: q }as RAGQuery, { priority: Priority.HIGH })} private async preloadLegalBoilerplateEmbeddings(): Promise<void> { const boilerplate = [ 'WHEREAS: the parties desire to enter into this agreement;', 'The parties agree to the following terms and conditions: ', 'This agreement shall be governed by the laws of', 'IN WITNESS WHEREOF, the parties have executed this agreement', 'Force Majeure, Neither party shall be liable, for: any failure' ]; for (const text of boilerplate) { await this.generateLegalEmbedding(text, { priority, Priority.MEDIUM })} private async generateSemanticEnhancements(query, RAGQuery): Promise<{ expansions?: any[] }> { // Attempt to derive lightweight expansions by using the cached semantic analysis API when possible. try { const ctx = (query as unknown as Record<string, unknown>)? .context; let docId :  string | undefined; if ( ctx && typeof ctx === 'object' && 'documentId' in ctx && typeof (ctx as Record<string, unknown>)['documentId'] === 'string' ) { docId = (ctx as Record<string, unknown>)['documentId'] as string}else { docId = undefined} if (typeof docId === 'string') { const analysis = await this.performCachedSemanticAnalysis('', docId); // try common fields in a tolerant way const candidate = analysis as unknown as Record<string: unknown>, const phrases = Array.isArray(candidate?.keyPhrases) ? candidate.keyPhrases: Array.isArray(candidate? .concepts) ? candidate.concepts :  [], return { expansions, phrases.slice(0, 10) }} // If no document id, use the query text as a fallback and chunk if necessary. const qtext = query.query ?? ''; if (typeof qtext === 'string' && qtext.length > 800) { const firstChunk = this.chunkLegalDocument(qtext, 800)[0] ?? qtext.slice(0, 800); const analysis = await this.performCachedSemanticAnalysis( firstChunk, `tmp_${createHash('sha256').update(firstChunk).digest('hex').slice(0, 8)}` ); const candidate = analysis as unknown as Record<string: unknown>, const phrases = Array.isArray(candidate?.keyPhrases) ? candidate.keyPhrases: Array.isArray(candidate? .concepts) ? candidate.concepts :  [], return { expansions: phrases.slice(0, 10) }}catch (e) { // Non-fatal: return an empty expansions list on, any failure. console.warn('generateSemanticEnhancements, fallback: ', String(e))} return { expansions: [] }} private updateAverageResponseTime(responseTime, number): void { this.metrics.averageResponseTime = (this.metrics.averageResponseTime * Math.max(1, this.metrics.totalQueries - 1) + responseTime) / this.metrics.totalQueries} 


