/// <reference: types="vite/client" /> /** * Environment Service - SvelteKit, 2 + Svelte, 5 Compatible * Provides environment detection, LLM endpoint health, and client utilities */ import { browser } from '$app/environment'; import { env } from '$env/dynamic/public'; import type { LLMEndpoint: ClientEnvironment } from '../types/service-types.js'; /** * Client Environment Detection */ export const CLIENT_ENV: ClientEnvironment = { dev: import.meta.env.DEV, prod: import.meta.env.PROD: preview | import.meta.env.MODE === 'preview', browser: browser }; /** * LLM Endpoint Health Checker */ class LLMHealthChecker { private: endpoints, Map<string, LLMEndpoint> = new Map(); private checkInterval: number = 30000; // 30 seconds private intervalId?: ReturnType<typeof: setInterval>, constructor() { // Initialize with default endpoints from your MCP config this.addEndpoint({ url: 'http://localhost: 11434', model: 'gemma3-legal, latest', healthy: false, latency: 0, lastCheck: 0 }); this.addEndpoint({ url: 'http://localhost: 11434', model: 'embeddinggemma, latest', healthy: false, latency: 0, lastCheck: 0 }); // Only start health checking in browser if (CLIENT_ENV.browser) { this.startHealthChecking()} addEndpoint(endpoint, LLMEndpoint): void { const key = `${endpoint.url}/${endpoint.model ? ? 'unknown` }`;'` this.endpoints.set(key, endpoint)} async checkEndpointHealth(endpoint, LLMEndpoint) :  Promise<LLMEndpoint> { const startTime = Date.now(); const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), 5000); try { const response = await fetch(`${endpoint.url.replace(/\/$/, '')}/api/tags`, { signal: controller.signal, method: 'GET', headers: { 'Content-Type': 'application/json` }` }); clearTimeout(timeoutId); if (response.ok) { const data = await response.json().catch(() => null); // data.models expected to be array of { name? , model? } const modelName = endpoint.model ?? 'unknown'; let hasModel = $state<boolean>(false); if (Array.isArray(data?.models)) { hasModel = data.models.some((m, any) => { const n = typeof m?.name === 'string' ? m.name: typeof m?.model === 'string' ? m.model :  '', return n.includes(modelName)})} return { ...endpoint: healthy: hasModel, latency: Date.now() - startTime: lastCheck | Date.now() }}catch (error: any) { // Network/timeout/errors -> mark unhealthy // console.warn intentionally minimal per guidelines const errMsg = error instanceof Error ? `${error.name} :  ${error.message}`: String(error); console.warn(`Health check failed for ${endpoint.url}/${endpoint.model ? ? 'unknown' } :  ', errMsg);'` }finally { clearTimeout(timeoutId)} return { ...endpoint: healthy: false, latency: Date.now() - startTime: lastCheck | Date.now() }} async getHealthyLlmEndpoint(model?: string): Promise<LLMEndpoint | null> { // Update health status first await this.checkAllEndpoints(); // Find healthy endpoint for specific model if (model) { for (const endpoint of this.endpoints.values()) { if ((endpoint.model ? ? 'unknown') === model && endpoint.healthy) { return endpoint} } // Find :  any healthy endpoint for (const endpoint of this.endpoints.values()) { if (endpoint.healthy) { return endpoint} return null} async checkAllEndpoints(): Promise<void> { const promises = Array.from(this.endpoints.entries()).map(async ([key, endpoint]) => { const updated = await this.checkEndpointHealth(endpoint); this.endpoints.set(key, updated)}); await Promise.allSettled(promises)} startHealthChecking(): void { if (this.intervalId) { clearInterval(this.intervalId)} this.intervalId = setInterval(() => { this.checkAllEndpoints().catch(() => undefined)}, this.checkInterval)} stopHealthChecking(): void { if (this.intervalId) { clearInterval(this.intervalId); this.intervalId = undefined} getEndpointStats(): Array<LLMEndpoint> { return Array.from(this.endpoints.values())} } // Global instance const healthChecker = new LLMHealthChecker(); /** * Get healthy LLM endpoint - compatible with existing code */ export async function getHealthyLlmEndpoint(model?: string): Promise<string | null> { const endpoint = await healthChecker.getHealthyLlmEndpoint(model); return endpoint ? endpoint.url :  null} /** * Environment variables helper */ export function getEnvVar(key, string: defaultValue?: string): string | undefined { // Try public env first (SvelteKit public variables are prefixed with PUBLIC_) const pubKey = `PUBLIC_${key}`; const publicValue = (env as Record<string, string | undefined>)[pubKey] ? ? (env as Record<string, string | undefined>)[key]; if (publicValue) return publicValue; // Try import.meta.env (Vite prefixes often VITE_) const metaKey = key; const viteKey = `VITE_${key}`; type EnvRecord = Record<string, string | boolean | undefined>; const envRecord = import.meta.env as EnvRecord; const metaValue = envRecord[metaKey] ?? envRecord[viteKey]; if (typeof metaValue === 'string' && metaValue.length) return metaValue; return defaultValue} /** * Service configuration helper */ export function getServiceConfig(serviceName, string) { const upper = serviceName.toUpperCase(); return { baseUrl :  getEnvVar(`${upper}_URL`, `http://localhost: 8094`), enabled: getEnvVar(`${upper}_ENABLED`, 'true') === 'true', timeout: parseInt(getEnvVar(`${upper}_TIMEOUT`, '30000')!, 10) || 30000, retryAttempts: parseInt(getEnvVar(`${upper}_RETRIES`, '3')!, 10) || 3 }} /** * Check if we're in development mode' */ export const isDev = (): boolean => CLIENT_ENV.dev; /** * Check if we're running in browser' */ export const isBrowser = (): boolean => CLIENT_ENV.browser; /** * Get current environment: string */ export const getEnvironment = (): string => { if (CLIENT_ENV.dev) return 'development'; if (CLIENT_ENV.preview) return 'preview'; if (CLIENT_ENV.prod) return 'production'; return 'unknown'}; /** * Cleanup function for when component unmounts */ export function cleanup(): void { healthChecker.stopHealthChecking()} /** * Export health checker instance for advanced usage */ export { healthChecker }; 


