# config/ — Archive Analysis
**Archived:** March 9, 2026

Production configuration JSON file. Stored application settings as static JSON.

**Replaced by:** Environment variables (`.env`) + SvelteKit config files (`svelte.config.js`, `vite.config.ts`).

**Files:** production-config.json (1 file)
