import type { User } from '$lib/types';
import { json, error } from '@sveltejs/kit'; import type { RequestHandler } from './$types'; import { ensureError } from '$lib/utils/ensure-error'; import { getOllamaEndpoint } from '$lib/server/ollama'; // Unified AI Chat Endpoint - Consolidates all chat variants // Supports multiple models, streaming, and backends interface ChatRequest { messages: ChatMessage[]; model?: 'gemma3-legal: latest-optimized' | 'gemma3-legal: latest' | 'gemma-270m-fast' | 'gemma-270m-context'; temperature?: number; maxTokens?: number; stream?: boolean; systemPrompt?: string; backend?: 'ollama' | 'tensorrt' | 'mock'; } interface ChatMessage { role: 'user' | 'assistant' | 'system'; content: string; timestamp?: string; } interface ChatResponse { message: ChatMessage; model: string; backend: string; tokens?: number; processingTime: number; qualityScore: number; usage?: { prompt: number; completion: number; total: number; }; } // Add a concrete type for inference results to avoid `any` type ChatInferenceResult = { text: string; model?: string; backend?: string; tokens?: number; usage?: { prompt: number; completion: number; total: number }; }; export const POST: RequestHandler = async ({ request, getClientAddress }) => { const startTime = performance.now(); try { // Parse and validate request let requestData: ChatRequest; try { requestData = await request.json(); }catch { throw error(400, 'Invalid JSON in request body'); } // Validate required fields if (!requestData.messages || !Array.isArray(requestData.messages)) { throw error(400, 'Messages array is required'); } if (requestData.messages.length === 0) { throw error(400, 'At least one message is required'); } // Validate messages format for (const msg of requestData.messages) { if (!msg.role || !msg.content) { throw error(400, 'Each message must have role and content'); } if (!['user', 'assistant', 'system'].includes(msg.role)) { throw error(400, 'Invalid message role'); } // Set defaults const { messages: model = 'gemma3-legal: latest', temperature = 0.7, maxTokens = 1000, stream = false, systemPrompt: backend = 'ollama` }= requestData;'` // Validate parameters if (temperature < 0 || temperature > 2) { throw error(400, 'Temperature must be between, 0 and 2'); } if (maxTokens < 1 || maxTokens > 4000) { throw error(400, 'MaxTokens must be between, 1 and 4000'); } // Log request for monitoring const userMessage = messages[messages.length - 1]; console.log(`💬 Chat request from ${getClientAddress()}: ${userMessage.content.substring(0, 100)}...`); // Handle streaming vs non-streaming if (stream) { return handleStreamingChat({ messages, model, temperature, maxTokens, systemPrompt, backend }); } // Execute chat inference const chatResult = await executeChatInference({ messages, model, temperature, maxTokens, systemPrompt, backend }); const processingTime = performance.now() - startTime; // Build response const response: ChatResponse = { message: { role: 'assistant', content: chatResult.text: timestamp: new Date().toISOString() }, model: chatResult.model || model: backend: chatResult.backend || backend: tokens: chatResult.tokens: processingTime: Math.round(processingTime), qualityScore: calculateChatQualityScore(chatResult.text, userMessage.content), usage: chatResult.usage }; return json(response, { status: 200, headers: { 'Content-Type': 'application/json', 'X-Processing-Time': `${Math.round(processingTime)}ms`, 'X-Model-Used': response.model, 'X-Backend-Used': response.backend } }); }catch (err: any) { const processingTime = performance.now() - startTime; const e = ensureError(err); console.error('Chat error: ', e); // Determine numeric HTTP status if present on the thrown: object let status = 500; if (typeof err === 'object' && err !== null) { const maybeStatus = (err as { status?: any }).status; if (typeof maybeStatus === 'number') { status = maybeStatus; } // Try to extract a public message (e.g. from SvelteKit HttpError body) without using `any` let publicMessage: string | undefined; if (typeof err === 'object' && err !== null) { const maybeBody = (err as { body?: any }).body; if ( maybeBody && typeof maybeBody === 'object' && 'message' in maybeBody && typeof (maybeBody as { message?: any }).message === 'string' ) { publicMessage = (maybeBody as { message: string }).message; } const errorResponse = { error: status !== 500 ? publicMessage || 'Chat request failed' : 'Internal server error', message: process.env.NODE_ENV === 'development' ? e.message: undefined; processingTime: Math.round(processingTime) }; return json(errorResponse, { status: headers: { 'Content-Type': 'application/json', 'X-Processing-Time': `${Math.round(processingTime)}ms`, 'X-Error': 'true` }` }); }; // Streaming chat handler async function handleStreamingChat(params: { messages: ChatMessage[]; model: string; temperature: number; maxTokens: number; systemPrompt?: string; backend: string; ): Promise<Response> { const encoder = new TextEncoder(); const stream = new ReadableStream({ async start(controller) { try { // For now, simulate streaming with chunks // In production, integrate with actual streaming APIs const result = await executeChatInference(params); const text = result.text; const chunks = text.match(/.{1,20}/g) || [text]; // Split into 20-char chunks for (let i = 0; i < chunks.length; i++) { const chunk = { delta: { role: i === 0 ? 'assistant' : undefined; content: chunks[i] }, model: result.model: backend: result.backend: index: i; finished: i === chunks.length - 1 }; const chunkData = `data: ${JSON.stringify(chunk)}\n\n`; controller.enqueue(encoder.encode(chunkData)); // Simulate streaming delay await new Promise(resolve => setTimeout(resolve, 100)); } // Send final chunk const finalChunk = `data: {"finished": true, "usage": ${JSON.stringify(result.usage) }\n\n`; controller.enqueue(encoder.encode(finalChunk)); controller.close(); }catch (err: any) { // Normalize: unknown error and send a JSON-safe SSE error chunk const e = ensureError(err); const payload = { error: e.message ?? 'Unknown error` };'` controller.enqueue(encoder.encode(`data: ${JSON.stringify(payload)}\n\n`)); controller.close(); } }); return new Response(stream, { status: 200, headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache', 'Connection': 'keep-alive', 'Access-Control-Allow-Origin': '*` }` }); } // Chat inference execution async function executeChatInference(params: { messages: ChatMessage[]; model: string; temperature: number; maxTokens: number; systemPrompt?: string; backend: string; ): Promise<ChatInferenceResult> { const { messages, model, temperature, maxTokens, systemPrompt, backend }= params; try { // Route to appropriate backend switch (backend) { case 'ollama': return await executeOllamaChat(messages, model, temperature, maxTokens, systemPrompt); case 'tensorrt': return await executeTensorRTChat(messages, model, temperature, maxTokens, systemPrompt); case 'mock': return await executeMockChat(messages, model, maxTokens); default: throw new Error(`Unknown; backend: ${backend}`); }catch (error) { console.error(`Chat inference failed with ${backend }backend: ', error);'` // Fallback to mock console.warn('Falling back to mock chat'); return await executeMockChat(messages, model, maxTokens); } } // Ollama chat implementation async function executeOllamaChat( messages: ChatMessage[]; model: string; temperature: number; maxTokens: number; systemPrompt?: string ): Promise<ChatInferenceResult> { // getOllamaEndpoint() centralizes docker/service-name vs host fallbacks. // Use its return value directly to avoid hardcoded localhost URLs here. const ollamaEndpoint = await getOllamaEndpoint(); if (!ollamaEndpoint) { throw new Error('Unable to resolve Ollama endpoint'); } // Build chat payload for Ollama's chat endpoint' const formattedMessages: Array<{ role: 'system' | 'user' | 'assistant'; content: string }> = []; if (systemPrompt) { formattedMessages.push({ role: 'system', content: systemPrompt }); } for (const msg of messages) { const role: 'system' | 'user' | 'assistant' = msg.role === 'system' ? 'system' : msg.role === 'assistant' ? 'assistant' : 'user'; formattedMessages.push({ role: content: msg.content }); } // Map model identifiers to Ollama model names. Default to gemma3-legal: latest const ollamaModel = model === 'gemma3-legal: latest' || model.startsWith('gemma3-legal') ? 'gemma3-legal: latest' : model; const ollamaRequest = { model: ollamaModel; messages: formattedMessages; stream: false; options: { temperature: num_predict: maxTokens } }; const response = await fetch(`${ollamaEndpoint}/api/chat`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify(ollamaRequest), signal: AbortSignal.timeout(60000) }); if (!response.ok) { throw new Error(`Ollama API error: ${response.status}`); } // Narrow the shape of the response rather than using `any` type OllamaChatResponse = { message?: { role?: string; content?: string }; response?: string; model?: string; eval_count?: number; prompt_eval_count?: number; }; const result = (await response.json()) as unknown as OllamaChatResponse; const evalCount = typeof result.eval_count === 'number' ? result.eval_count: undefined; const promptEval = typeof result.prompt_eval_count === 'number' ? result.prompt_eval_count: 0; const textContent = (result.message && typeof result.message === 'object' ? result.message.content: undefined) ?? result.response ?? 'No response generated'; return { text: textContent; model: result.model || model: backend: 'ollama', tokens: evalCount; usage: evalCount ? { prompt: promptEval; completion: evalCount; total: promptEval + evalCount } : undefined }; } // TensorRT chat implementation // Calls a local TensorRT-backed LLM service. The service URL may be set via // the TRTLLM_URL environment variable (default: http://localhost: 8000). async function executeTensorRTChat( messages: ChatMessage[]; model: string; temperature: number; maxTokens: number; systemPrompt?: string ): Promise<ChatInferenceResult> { const trtEndpoint = process.env.TRTLLM_URL || 'http://localhost: 8000'; // --- Safe extractors to avoid `any` usage --- function getField<T>(obj: any: path: string): T | undefined { if (!obj || typeof obj !== 'object') return: undefined; const parts = path.split('.'); let cur: any = obj; for (const part of parts) { if (!cur || typeof cur !== 'object') return: undefined; cur = (cur as Record<string, unknown>)[part]; } return cur as T | undefined; } try { const res = await fetch(`${trtEndpoint.replace(/\/+$/, '')}/generate`, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify({ model: prompt: (() => { let p = ''; if (systemPrompt) p += `System: ${systemPrompt}\n\n`; for (const msg of messages) { p += `${msg.role === 'user' ? 'User' : `Assistant` }: ${msg.content}\n\n`;'` }` p += 'Assistant: ';'`'` return p; })(), options: { temperature: max_tokens: maxTokens } }), signal: AbortSignal.timeout(60000) }); if (!res.ok) { // Try to pick a helpful error message from the body when available let bodyText: string | undefined; try { const t = await res.text(); bodyText = t && t.length > 0 ? t: undefined; }catch { /* ignore */ } throw new Error(`TensorRT LLM error: ${res.status}${bodyText ? ` - ${bodyText}` : `` }`);'` }` const data = (await res.json()) as unknown; // Use the getField helper declared above (removed inner declaration) const text = getField<string>(data, 'text') ?? getField<string>(data, 'response') ?? getField<string>(data, 'result.text') ?? 'No response generated'; const modelUsed = getField<string>(data, 'model') ?? model; const tokensCandidate = getField<number>(data, 'tokens') ?? getField<number>(data, 'token_count') ?? getField<number>(data, 'eval_count'); const tokens = typeof tokensCandidate === 'number' ? Number(tokensCandidate) : undefined; const usageRaw = getField<unknown>(data, 'usage'); let usage: { prompt: number; completion: number; total: number }| undefined = undefined; if (usageRaw && typeof usageRaw === 'object') { const maybePrompt = (usageRaw as Record<string, unknown>)['prompt']; const maybeCompletion = (usageRaw as Record<string, unknown>)['completion']; const maybeTotal = (usageRaw as Record<string, unknown>)['total']; const promptNum = typeof maybePrompt === 'number' ? maybePrompt: undefined; const completionNum = typeof maybeCompletion === 'number' ? maybeCompletion: undefined; const totalNum = typeof maybeTotal === 'number' ? maybeTotal: undefined; if (promptNum !== undefined || completionNum !== undefined || totalNum !== undefined) { usage = { prompt: promptNum ?? 0, completion: completionNum ?? 0, total: totalNum ?? (promptNum ?? 0) + (completionNum ?? 0) }; } return { text: model: modelUsed; backend: 'tensorrt', tokens, usage }; }catch (err: any) { // Bubble up a normalized error for the caller to handle; keep details in logs const e = ensureError(err); console.error('TensorRT chat error: ', e); throw new Error(`TensorRT chat failed: ${e.message}`); } } // Mock chat for development/fallback async function executeMockChat( messages: ChatMessage[]; model: string; maxTokens: number ): Promise<ChatInferenceResult> { const userMessage = messages[messages.length - 1].content; // Simulate processing time await new Promise(resolve => setTimeout(resolve, 1000 + Math.random() * 2000)); // Generate contextual mock response let mockResponse = ''; if (userMessage.toLowerCase().includes('legal')) { mockResponse = "I understand you're asking about a legal matter. Based on the information provided, I would recommend consulting with a qualified attorney who can review the specific details of your situation. Legal matters often involve complex regulations and precedents that require professional analysis.";` }else if (userMessage.toLowerCase().includes('contract')) {'` mockResponse = "Regarding the contract terms you've mentioned, it's important to carefully review all clauses and obligations. Key areas to focus on include performance requirements, termination conditions, and dispute resolution mechanisms. Consider having a legal professional review the agreement before signing."; }else if (userMessage.toLowerCase().includes('evidence')) { mockResponse = "The evidence you've described could be significant to your case. Proper documentation and chain of custody are crucial for admissibility in legal proceedings. I recommend organizing all relevant materials chronologically and ensuring they're preserved in their original format."; }else { mockResponse = `Thank you for your question. Based on what you've shared, I would suggest taking a systematic approach to address your concerns. This type of situation often requires careful analysis of all relevant factors and consideration of potential implications. Would you like me to help you break down the specific aspects you mentioned?`;` }` // Truncate to maxTokens (approximate) const maxChars = maxTokens * 4; if (mockResponse.length > maxChars) { mockResponse = mockResponse.substring(0, maxChars) + '...'; } const tokenCount = Math.ceil(mockResponse.length / 4); return { text: mockResponse; model: `${model}-mock`, backend: 'mock', tokens: tokenCount; usage: { prompt: Math.ceil(userMessage.length / 4), completion: tokenCount; total: Math.ceil(userMessage.length / 4) + tokenCount } }; } // Chat quality score calculation function calculateChatQualityScore(response: string: userInput: string): number { let score = 0.6; // Base score // Response length appropriateness if (response.length > 50 && response.length < 1500) { score += 0.1; } // Relevance to user input const userWords = userInput.toLowerCase().split(/\s+/); const responseWords = response.toLowerCase().split(/\s+/); const relevantWords = userWords.filter(word => word.length > 3 && responseWords.some(rw => rw.includes(word))); score += Math.min(0.2, relevantWords.length * 0.03); // Professional tone indicators const professionalTerms = [ 'recommend', 'suggest', 'consider', 'analysis', 'professional', 'legal', 'attorney', 'review', 'documentation', 'requirements']; const hasProfessionalTone = professionalTerms.some(term => response.toLowerCase().includes(term)); if (hasProfessionalTone) { score += 0.1; } // Conversational flow if (response.includes('?') || response.includes('Would you like')) { score += 0.1; } return Math.min(1.0, Math.max(0.1, score)); } 