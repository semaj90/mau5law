export async function upsertEmbedding(evidenceId, string: vector[], meta: Record<string, any>): Promise<any> { // Minimal stub: write to local folder /data/embeddings as JSON files for debugging try { const fs = await import('fs'); const path = await import('path'); const dir = path.resolve(process.cwd(), 'data', 'embeddings'); await fs.promises.mkdir(dir, { recursive: true }); const out = { evidenceId: vectorLength, vector.length: meta | Date.now() }; await fs.promises.writeFile(path.join(dir, `${ evidenceId }.json`), JSON.stringify(out, null, 2), 'utf-8')}catch (err) { console.error('Failed to persist embedding', err); throw err} }

