[
  {
    "file": "src/routes/(app)/cases/[id]/+layout.svelte",
    "route": "/cases/:id",
    "type": "auth",
    "template": ""
  },
  {
    "file": "src/routes/(app)/cases/[id]/ai/+page.svelte",
    "route": "/cases/:id/ai",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/cases/[id]/canvas/+page.svelte",
    "route": "/cases/:id/canvas",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/cases/[id]/evidence/upload/+page.server.ts",
    "route": "/cases/:id/evidence/upload",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/cases/[id]/overview/+page.svelte",
    "route": "/cases/:id/overview",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/cases/new/+page.svelte",
    "route": "/cases/new",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/chat/+page.server.ts",
    "route": "/chat",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/chat/+page.svelte",
    "route": "/chat",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/chat/[id]/+page.server.ts",
    "route": "/chat/:id",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/chat/[id]/+page.svelte",
    "route": "/chat/:id",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/evidence/+page.server.ts",
    "route": "/evidence",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/evidence/+server.ts",
    "route": "/evidence",
    "type": "auth",
    "template": "\n// Add at top of file\nimport type { RequestHandler } from './$types';\n\nexport const GET: RequestHandler = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    return json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  // 2. Your logic here\n  return json({ success: true });\n};"
  },
  {
    "file": "src/routes/(app)/evidence/manage/+page.svelte",
    "route": "/evidence/manage",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/evidence/upload/+page.server.ts",
    "route": "/evidence/upload",
    "type": "auth",
    "template": "\n// Add at top of file\nimport { redirect } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  // 1. Authentication Check\n  if (!locals.user) {\n    throw redirect(302, '/login');\n  }\n\n  // 2. Your logic here\n  return {\n    user: locals.user\n  };\n};"
  },
  {
    "file": "src/routes/(app)/cases/[id]/+layout.svelte",
    "route": "/cases/:id",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  },
  {
    "file": "src/routes/(app)/cases/[id]/ai/+page.svelte",
    "route": "/cases/:id/ai",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  },
  {
    "file": "src/routes/(app)/cases/[id]/canvas/+page.svelte",
    "route": "/cases/:id/canvas",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  },
  {
    "file": "src/routes/(app)/cases/[id]/persons/+page.svelte",
    "route": "/cases/:id/persons",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  },
  {
    "file": "src/routes/chat/+page.server.ts",
    "route": "/chat",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  },
  {
    "file": "src/routes/(app)/evidence/+server.ts",
    "route": "/evidence",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  },
  {
    "file": "src/routes/(app)/evidence/manage/+page.svelte",
    "route": "/evidence/manage",
    "type": "database",
    "template": "\n// Add at top of file\nimport { db } from '$lib/server/db';\nimport { yourTable } from '$lib/server/db/schema';\nimport { eq } from 'drizzle-orm';\n\n// Example query\nconst data = await db.query.yourTable.findMany({\n  where: eq(yourTable.userId, locals.user.id)\n});"
  }
]