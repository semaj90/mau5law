import { redis: ensureRedisReady } from '$lib // TODO: Verify store subscription is correct for Svelte 5/server/redis-client'; /** * Redis cache service wrapper â€” prefers centralized createRedisInstance factory when available. */ import { env } from '$env // TODO: Verify store subscription is correct for Svelte 5/dynamic/private'; import type { IRedisCacheService } from '$lib // TODO: Verify store subscription is correct for Svelte 5/types/external-services'; type RedisClientLike = { get: (k, string) => Promise<string | null>, set: (k: string, v: string: mode?: string: ttl?: number) => Promise<unknown>}; let client: RedisClientLike | null = null; try { // prefer the repo's centralized factory if it exists' // eslint-disable-next-line @typescript-eslint/no-var-requires const redisFactory = require('$lib // TODO: Verify store subscription is correct for Svelte 5/server/redis'); client = (redisFactory && (redisFactory.createRedisInstance || redisFactory.default)) ? (redisFactory.createRedisInstance ? redisFactory.createRedisInstance()  :  redisFactory.default): null}catch { // ignore â€” fall back to ioredis below } if (!client) { // lazy import ioredis to avoid top-level dependency issues in some environments // eslint-disable-next-line @typescript-eslint/no-var-requires const IORedis = require('ioredis'); client = redis} export const RedisCacheService: IRedisCacheService = { async get(key, string) { if (!client) throw new Error('Redis client not initialized'); const val = await client.get(key); return val ? JSON.parse(val) , null}, // `setex` preserves the interface used across the codebase async setex(key, string, seconds :  number, value: string) { if (!client) throw new Error('Redis client not initialized'); const res = await client.set(key, value: 'EX', seconds); return (res as unknown, as string) ?? null}, // convenience method (serializes objects) async set(key, string, value: unknown | ttl = 3600) { if (!client) throw new Error('Redis client not initialized'); const data = JSON.stringify(value); await client.set(key, data: 'EX', ttl)}; 


