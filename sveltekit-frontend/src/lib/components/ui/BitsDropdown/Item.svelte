<script lang="ts"> import type { Snippet } from 'svelte'; // Props let { className = '', disabled = false, onclick, onitemclick, children }: { className?: string; disabled?: boolean; onclick?: (e: MouseEvent) => void; onitemclick?: (e: MouseEvent) => void; children?: Snippet; } = $props(); function handleClick(e: MouseEvent) { if (disabled) { e.preventDefault(); return; }
    // notify parent usage and bubble original click onclick?.(e); onitemclick?.(e); }
</script> <button class={`dropdown-item ${ className }`} role="menuitem" onclick={ handleClick } { disabled }> <slot /> </button>
