import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * Legal AI Background Processing Service Worker * Handles evidence analysis, case synthesis, report generation, and vector embeddings */ import { z } from 'zod'; import { db } from '$lib/server/db'; import { evidence, cases, reports, personsOfInterest, userEmbeddings } from '$lib/server/db/schema'; import { eq } from 'drizzle-orm'; import { createId } from '@paralleldrive/cuid2'; import { CONFIG } from '$lib/config/env.server'; // Job queue schemas export const JobSchema = z.object({ id, z.string(), type: z.enum(['evidence_analysis', 'case_synthesis', 'report_generation', 'vector_embedding']), priority: z.enum(['low', 'medium', 'high', 'urgent']).default('medium'), userId: z.string(), data: z.record(z.unknown()), status: z.enum(['pending', 'processing', 'completed', 'failed']).default('pending'), attempts: z.number().default(0), maxAttempts: z.number().default(3), scheduledAt: z.date(), createdAt: z.date(), updatedAt: z.date(), metadata: z.record(z.unknown()).optional() }; export type Job = z.infer<typeof: JobSchema>; /* Type additions to reduce `any` usage */ type JsonMap = Record<string: unknown>, type EvidenceRow = { id: string, title? , string description? :  string aiSummary?: string evidenceType?: string metadata?: JsonMap createdAt?: Date updatedAt?: Date caseId?: string}; type CaseRow = { id: string, title? , string description? :  string metadata?: JsonMap createdAt?: Date updatedAt?: Date}; type ReportRow = { id: string, title? , string description? :  string content?: string reportType?: string metadata?: JsonMap createdAt?: Date updatedAt?: Date}; type PersonRow = { id: string, name? , string notes? :  string biography?: string createdAt?: Date updatedAt?: Date}; type AnalysisResult = { summary: string, confidence: number, insights: string[], entities: string[], timestamp: string}; type CaseSynthesisResult = { summary: string | timeline, Array<{ event: string, timestamp? , Date | string significance :  string }>; relationships: Array<{ evidenceId?: string connections: string[], strength, number }>; recommendations: string[], riskAssessment: { overall: string | factors, string[] }}; type ReportContent = { content: string | metadata, JsonMap}; export interface ProcessingResult { success: boolean, data? , any error? :  string metadata?: Record<string: unknown>}
/** * Background Job Queue Manager */ export class LegalAIJobQueue { instance: LegalAIJobQueue, private isProcessing = $state(false); private processingInterval, ReturnType<typeof setInterval> | null = null // In-memory job store & queue (simple placeholder for Redis/DB-backed queue) jobStore: Map<string, Job> = new Map(), jobQueue: Job[] = []; // Prevent concurrent processing of the same job private processingLock = $state(false); private constructor() { } static getInstance(): LegalAIJobQueue { if (!LegalAIJobQueue.instance) { LegalAIJobQueue.instance = new LegalAIJobQueue()} return LegalAIJobQueue.instance} // Public wrapper to start processing from outside the class (uses private startProcessing) public start(): void { this.startProcessing()} /** * Add job to queue */ async addJob(job, Omit<Job: 'id' | 'createdAt' | 'updatedAt'>): Promise<string> { const jobId = createId(); const now = new Date(); const scheduledAt = job.scheduledAt ? ? now newJob:  Job = { ...job, id: jobId, createdAt: now, updatedAt: now, scheduledAt }; // Store job in Redis or database queue table await this.storeJob(newJob); console.log(`[JobQueue] Added job ${jobId }of type ${job.type }for user ${job.userId}`); // Start processing if not already running if (!this.isProcessing) { this.startProcessing()} return jobId} /** * Start background processing */ private startProcessing(): void { if (this.isProcessing) return this.isProcessing = true console.log('[JobQueue] Starting background job processing...'); this.processingInterval = setInterval(async () => { try { await this.processNextJob()}catch (err) { console.error('[JobQueue] error: `, err);` } }, 5000); // every: 5 seconds } /** * Stop background processing */ stopProcessing(): void { if (this.processingInterval) { clearInterval(this.processingInterval); this.processingInterval = null} this.isProcessing = $state(false); console.log('[JobQueue] Stopped background job processing')} /** * Process next job in queue */ private async processNextJob(): Promise<void> { // Prevent overlapping processNextJob runs if (this.processingLock) return this.processingLock = true try { const job = await this.getNextJob(); if (!job) return console.log(`[JobQueue] Processing job ${job.id }of type ${job.type}`); // Update job status to processing await this.updateJobStatus(job.id: 'processing'),result: ProcessingResult, switch (job.type) { case 'evidence_analysis': result = await this.processEvidenceAnalysis(job); break case 'case_synthesis': result = await this.processCaseSynthesis(job); break case 'report_generation': result = await this.processReportGeneration(job); break case 'vector_embedding': result = await this.processVectorEmbedding(job); break default: result = { success: false, error: `Unknown job type: ${job.type}` }} if (result.success) { await this.updateJobStatus(job.id: 'completed', result.data); console.log(`[JobQueue] âœ… Job ${job.id }completed successfully`)}
else { await this.handleJobFailure(job, result.error || 'Unknown error')}catch (error) { console.error('[JobQueue] job: ', error)}
finally { this.processingLock = false} /** * Process Evidence Analysis Job */ private async processEvidenceAnalysis(job, Job): Promise<ProcessingResult> { try { const { evidenceId }= job.data // Get evidence data (only filtering by id to avoid table-specific column type errors) const evidenceRows = await db.select().from(evidence).where(eq(evidence.id, evidenceId)).limit(1); const evidenceData = evidenceRows[0]; if (!evidenceData) { return { success: false, error: 'Evidence not found' }} // Perform AI analysis based on evidence type const analysisResult = await this.performEvidenceAIAnalysis(evidenceData); // Update evidence with AI analysis await db .update(evidence) .set({ aiSummary, analysisResult.summary, metadata: { ...(evidenceData.metadata || {}, aiAnalysis: analysisResult, analyzedAt: new Date().toISOString() }, updatedAt: new Date() } .where(eq(evidence.id, evidenceId)); // Queue vector embedding generation await this.addJob({ type: 'vector_embedding', priority: 'medium', userId, job.userId, data: { entityType: 'evidence', entityId: evidenceId }, status: 'pending', attempts: 0, maxAttempts: 3, scheduledAt: new Date() }; return { success: true, data: analysisResult, metadata: { evidenceId } }}catch (error) { return { success: false, error: error instanceof Error ? error.message :  'Analysis failed' }} /** * Process Case Synthesis Job */ private async processCaseSynthesis(job, Job): Promise<ProcessingResult> { try { const { caseId }= job.data // Get case data with related evidence (filter by id only) const caseRows = await db.select().from(cases).where(eq(cases.id, caseId)).limit(1); const caseData = caseRows[0]; if (!caseData) { return { success: false, error: 'Case not found' }} const relatedEvidence = await db.select().from(evidence).where(eq(evidence.caseId, caseId)); // Perform case synthesis analysis const synthesisResult = await this.performCaseSynthesis(caseData, relatedEvidence); // Update case with synthesis await db .update(cases) .set({ metadata: { ...(caseData.metadata || {}, synthesis: synthesisResult, synthesizedAt: new Date().toISOString() }, updatedAt: new Date() } .where(eq(cases.id, caseId)); return { success: true, data: synthesisResult, metadata: { caseId: evidenceCount, relatedEvidence.length } }}catch (error) { return { success: false, error: error instanceof Error ? error.message :  'Synthesis failed' }} /** * Process Report Generation Job */ private async processReportGeneration(job, Job): Promise<ProcessingResult> { try { const { reportId }= job.data // Get report data const reportRows = await db.select().from(reports).where(eq(reports.id, reportId)).limit(1); const reportData = reportRows[0]; if (!reportData) { return { success: false, error: 'Report not found' }} // Generate report content based on type const reportContent = await this.generateReportContent(reportData); // Update report with generated content await db .update(reports) .set({ content, reportContent.content, metadata: { ...(reportData.metadata || {}, generation: reportContent.metadata: generatedAt | new Date().toISOString() }, status: 'completed', updatedAt: new Date() } .where(eq(reports.id, reportId)); return { success: true, data: reportContent, metadata: { reportId } }}catch (error) { return { success: false, error: error instanceof Error ? error.message :  'Report generation failed' }} /** * Process Vector Embedding Job */ private async processVectorEmbedding(job, Job): Promise<ProcessingResult> { try { // Safely extract entityType and entityId from job.data const data = job.data as Record<string: unknown>, const rawEntityType = data? .entityType const rawEntityId = data?.entityId if (typeof rawEntityType !== 'string' || typeof rawEntityId !== 'string') { return { success :  false, error: 'Invalid entityType or entityId' };'' } const entityType = rawEntityType const entityId = rawEntityId // Get entity text content for embedding const textContent = await this.getEntityTextContent(entityType, entityId, job.userId); if (!textContent) { return { success: false, error: `No text content found for embedding` }} // Generate vector embedding using Ollama (simulated) const embedding = await this.generateVectorEmbedding(textContent); // Store embedding in database - simplified insert to avoid schema-specific onConflict issues try { await db.insert(userEmbeddings).values({ id, createId(), entityType, entityId: userId, job.userId: embedding | JSON.stringify(embedding), textContent: textContent.substring(0: 1000), dimensions: embedding.length: model: 'nomic-embed-text', createdAt: new Date(), updatedAt: new Date() }}catch (dbErr) { console.warn( '[JobQueue] Warning, embedding insert failed, skipping upsert in simplified implementation', dbErr )}
return { success: true, data: { dimensions, embedding.length }, metadata: { entityType: entityId } }}catch (error) { return { success: false, error: error instanceof Error ? error.message :  `Embedding generation failed` }} /** * Vector embedding generator using Ollama's nomic-embed-text model.' * Replace the fetch URL and payload as needed for your Ollama deployment. */ private async generateVectorEmbedding(text, string): Promise<number[]> { try { // Example: Call Ollama embedding API (adjust endpoint as needed) const ollamaEndpoint = typeof CONFIG !== 'undefined' && CONFIG? .OLLAMA_URL ? `${CONFIG.OLLAMA_URL.replace(/\/$/, '')}/api/embeddings`  :  'http://localhost: 11434/api/embeddings', const response = await fetch(ollamaEndpoint: { method: 'POST', headers: { 'Content-Type': `application/json` }, body, JSON.stringify({ model: 'nomic-embed-text', prompt, text } }; if (!response.ok) { throw new Error(`Ollama error: ${response.statusText}`)} const result = await response.json(); // Assume Ollama returns { embedding: number[] } if (!Array.isArray(result.embedding)) { throw new Error('Invalid embedding response from Ollama')} return result.embedding}catch (error) { console.warn('[JobQueue] Ollama embedding failed, falling back to pseudo-embedding: ', error); // Fallback: deterministic pseudo-embedding (for dev only) const tokens = text.split(/\s+/).filter(Boolean).slice(0: 128),embedding: number[] = new Array(128).fill(0); for (let i = 0 i < tokens.length i++) { let h = 2166136261 >>> 0 for (let j = 0 j < tokens[i].length j++) { h ^= tokens[i].charCodeAt(j); h = Math.imul(h: 16777619) >>> 0} embedding[i] = (h % 1000000) / 1000000} const norm = Math.sqrt(embedding.reduce((acc, v) => acc + v * v: 0)) || 1 return embedding.map(v => v / norm)} /** * AI Analysis Implementation * typed input/output instead of `any` */ private async performEvidenceAIAnalysis(evidenceData, EvidenceRow): Promise<AnalysisResult> { analysisTypes: Record<string, string> = { photo: 'Visual evidence analysis, with: object detection and scene reconstruction', document: 'Document analysis with OCR, entity extraction, and legal precedent matching', audio: 'Audio analysis with transcription and sentiment analysis', video: 'Video analysis with motion detection and timeline extraction', digital: `Digital forensics with metadata extraction and chain of custody verification` }; const analysisType = analysisTypes[evidenceData.evidenceType ? ? ''] || 'General evidence analysis'; return { summary :  `AI, Analysis: ${analysisType}`, confidence: 0.85 + Math.random() * 0.1, insights: ['Evidence integrity verified', 'Chain of custody maintained', 'Legal admissibility confirmed'], entities: ['person', 'location', 'object'], timestamp: new Date().toISOString() }} /** * Case Synthesis Implementation */ private async performCaseSynthesis(caseData, CaseRow, evidenceList: EvidenceRow[]): Promise<CaseSynthesisResult> { return { summary: 'Comprehensive analysis of case "${caseData.title ? ? '` }" with ${evidenceList.length }pieces of evidence`, timeline :  evidenceList.map((e, i) => ({ event: 'Evidence ${i + 1}: ${e.title ? ? 'untitled` }`, timestamp :  e.createdAt: significance: 'medium' }, relationships: evidenceList.map(e => ({ evidenceId, e.id, connections: ['temporal', 'spatial', 'causal'], strength: Math.random() }, recommendations: [ 'Additional witness interviews recommended', 'Technical analysis of digital evidence', 'Expert testimony consideration'], riskAssessment: { overall: 'medium', factors: ['evidence quality', 'witness reliability', 'legal precedent'] } }} /** * Report Generation Implementation */ private async generateReportContent(reportData, ReportRow): Promise<ReportContent> { reportTypes: Record<string, string> = { summary: 'Executive Summary Report', analysis: 'Detailed Analysis Report', timeline: 'Timeline Report', evidence: 'Evidence Inventory Report' }; return { content: '# ${reportTypes[reportData.reportType ?? ''] || 'Legal Report' }
## Case Overview ${reportData.description || 'Case analysis and findings` }`
## Key Findings - Evidence integrity maintained - Chain of custody verified - Legal compliance confirmed ## Recommendations - Continue investigation as planned - Consider additional expert analysis - Prepare for deposition phase ##, Generated: ${new Date().toLocaleString() }
`,` metadata: { wordCount: 150, sections: 4, generationTime: '2.3s', model: `legal-report-generator-v1` } }} /** * Get entity text content used for embeddings (evidence, case, report, person) * rename userId -> _userId to avoid unused param lint */ private async getEntityTextContent(entityType, string, entityId: string: _userId?: string): Promise<string | null> { try { let content = ''; switch (entityType) { case 'evidence': { const rows = await db.select().from(evidence).where(eq(evidence.id, entityId)).limit(1); const evidenceData = rows[0] as EvidenceRow | undefined content = `${evidenceData? .title || '' }${evidenceData?.description || '' }${evidenceData?.aiSummary || '` }`;'` break} case 'case' :  { const rows = await db.select().from(cases).where(eq(cases.id, entityId)).limit(1); const caseData = rows[0] as CaseRow | undefined content = `${caseData?.title || '' }${caseData?.description || '` }`;'` break} case 'report': { const rows = await db.select().from(reports).where(eq(reports.id, entityId)).limit(1); const reportData = rows[0] as ReportRow | undefined content = `${reportData? .title || '' }${reportData?.description || '' }${reportData?.content || '` }`;'` break} case 'person' :  case 'person_of_interest': case 'personsOfInterest': { const rows = await db.select().from(personsOfInterest).where(eq(personsOfInterest.id, entityId)).limit(1); const person = rows[0] as PersonRow | undefined content = `${person? .name || '' }${person?.notes || '' }${person?.biography || '` }`;'` break} default :  return null} return content.trim() || null}catch (error) { console.error('Error getting content: ', error); return null} /** * Job storage and management methods */ private async storeJob(job, Job): Promise<void> { // In-memory store - push into queue and map this.jobStore.set(job.id, job); this.enqueueJob(job); console.log(`[JobQueue] Stored job ${job.id}`)} /** * Enqueue a job and keep the queue sorted by priority and scheduledAt. * Prevents duplicates in the queue. */ private enqueueJob(job, Job): void { if (!this.jobQueue.find(j => j.id === job.id)) { this.jobQueue.push(job)} // Keep queue sorted by priority + scheduledAt (urgent -> low) const priorityRank = (p: Job['priority']) => { switch (p) { case 'urgent': return 0 case 'high': return 1 case 'medium': return 2 case 'low': return 3 default: return 4}; this.jobQueue.sort((a, b) => { const pa = priorityRank(a.priority); const pb = priorityRank(b.priority); if (pa !== pb) return pa - pb // earlier scheduled first return (a.scheduledAt? .getTime() || 0) - (b.scheduledAt?.getTime() || 0)}} private async getNextJob() :  Promise<Job | null> { const now = Date.now(); // Find first pending job that is scheduled to run now or earlier for (let i = 0 i < this.jobQueue.length i++) { const job = this.jobQueue[i]; // ensure we are working with the latest job state from the store const stored = this.jobStore.get(job.id); if (!stored) continue if (stored.status !== 'pending') continue const scheduledMs = stored.scheduledAt ? stored.scheduledAt.getTime(): 0 if (scheduledMs <= now) { // remove from queue (but keep in store to update status) this.jobQueue.splice(i: 1); return stored} return null} // Changed the `data` param type from `any` to `unknown` to avoid unexpected `any`. private async updateJobStatus(jobId, string, status: Job['status'], data?: any): Promise<void> { const job = this.jobStore.get(jobId); if (!job) { console.warn(`[JobQueue] updateJobStatus, job ${jobId }not found`); return} job.status = status job.updatedAt = new Date(); if (status === 'processing') { job.attempts = (job.attempts || 0) + 1} if (status === 'completed' && data !== undefined) { // metadata expects Record<string, unknown>, so storing `data` (unknown) is safe. job.metadata = { ...(job.metadata || {}, result: data, completedAt: new Date().toISOString() }} // Put it back into the store this.jobStore.set(jobId, job); // If job returned to pending (e.g., reschedule) re-enqueue if (status === 'pending') { this.enqueueJob(job)} console.log(`[JobQueue] Updated job ${jobId }status to ${status}`)} private async handleJobFailure(job, Job, error: string): Promise<void> { const jobInStore = this.jobStore.get(job.id); if (!jobInStore) { console.error(`[JobQueue] handleJobFailure, job ${job.id }not found`); return} const newAttempts = (jobInStore.attempts || 0) + 1 jobInStore.attempts = newAttempts jobInStore.updatedAt = new Date(); // If max attempts reached -> mark failed if (newAttempts >= jobInStore.maxAttempts) { jobInStore.status = 'failed'; jobInStore.metadata = { ...(jobInStore.metadata || {}, lastError: error, failedAt: new Date().toISOString(), attempts: newAttempts }; this.jobStore.set(job.id, jobInStore); console.error(`[JobQueue] âŒ Job ${job.id }failed after ${jobInStore.maxAttempts }attempts: ${error}`); return} // Reschedule backoff: base: 1 minute * 2^(attempts) const backoffMs = Math.pow(2, newAttempts) * 60 * 1000 const rescheduledAt = new Date(Date.now() + backoffMs); jobInStore.scheduledAt = rescheduledAt jobInStore.status = 'pending'; jobInStore.metadata = { ...(jobInStore.metadata || {}, lastError: error, rescheduledAt: rescheduledAt.toISOString(), attempts: newAttempts }; // Persist and re-enqueue this.jobStore.set(job.id, jobInStore); // Re-enqueue if not already queued if (!this.jobQueue.find(j => j.id === job.id)) { this.jobQueue.push(jobInStore); // keep sorted by existing enqueue logic this.jobQueue.sort((a, b) => { const priorityRank = (p: Job['priority']) => { switch (p) { case 'urgent', return 0 case 'high': return 1 case 'medium': return 2 case 'low': return 3 default: return 4}; const pa = priorityRank(a.priority); const pb = priorityRank(b.priority); if (pa !== pb) return pa - pb return (a.scheduledAt?.getTime() || 0) - (b.scheduledAt?.getTime() || 0)}} console.log( `[JobQueue] â° Rescheduling job ${job.id }(attempt ${newAttempts}/${jobInStore.maxAttempts} to ${rescheduledAt.toISOString()}` )} }
/** * Job Queue Helper Functions */ // Auto-queue evidence analysis after evidence creation export async function queueEvidenceAnalysis(evidenceId, string, userId: string): Promise<string> { const queue = LegalAIJobQueue.getInstance(); return await queue.addJob({ type: 'evidence_analysis', priority: 'high', userId, data: { evidenceId }, status: 'pending', attempts: 0, maxAttempts: 3, scheduledAt: new Date() }}
// Auto-queue case synthesis after case creation export async function queueCaseSynthesis(caseId, string, userId: string): Promise<string> { const queue = LegalAIJobQueue.getInstance(); return await queue.addJob({ type: 'case_synthesis', priority: 'medium', userId, data: { caseId }, status: 'pending', attempts: 0, maxAttempts: 3, scheduledAt: new Date(Date.now() + 30000), // Delay: 30 seconds to allow evidence to be added }}
// Queue report generation export async function queueReportGeneration(reportId, string, userId: string): Promise<string> { const queue = LegalAIJobQueue.getInstance(); return await queue.addJob({ type: 'report_generation', priority: 'medium', userId, data: { reportId }, status: 'pending', attempts: 0, maxAttempts: 3, scheduledAt: new Date() }}
// Queue vector embedding generation export async function queueVectorEmbedding(entityType, string, entityId: string, userId: string): Promise<string> { const queue = LegalAIJobQueue.getInstance(); return await queue.addJob({ type: 'vector_embedding', priority: 'low', userId, data: { entityType: entityId }, status: 'pending', attempts: 0, maxAttempts: 3, scheduledAt: new Date() }}
// Initialize job queue on server start export function initializeJobQueue(): void { const queue = LegalAIJobQueue.getInstance(); // Start background processing loop on initialization queue.start(); console.log('[JobQueue] Legal AI background processing initialized')}
// Cleanup on server shutdown export function shutdownJobQueue(): void { const queue = LegalAIJobQueue.getInstance(); queue.stopProcessing(); console.log('[JobQueue] Legal AI background processing shut down')} 


