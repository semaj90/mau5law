import type { SearchResult } from '$lib/types';
import type { CachingTypes } from '$lib/types/enhanced-svelte5-types';
/** Shared, portable types used by client and server */ /** High-level data categories cached and synced across layers */ export type DataType = "case" | "evidence" | "document"; /** Shared embedding vector type (GPU / Ollama compatible) */ export type EmbeddingVector = number[]; /** Common metadata type for all RAG objects */ export interface BaseMetadata { source?: string; tags?: string[]; confidence?: number; lastUpdated?: number; // Changed: replace `any` with `unknown` to avoid unexpected-any lint/TS error [key: string]: unknown} /** Generic stored: object format used by RAG pipelines */ export interface RAGObject { id: string, type: DataType, title: string, content: embedding?: EmbeddingVector; metadata?: BaseMetadata} // ------------------ Added types to fix cross-file references ------------------ /** * LegalDocument - used by GPU bridge and other processors. * Note: documentType set intentionally to a broader union used across the codebase. */ export type LegalDocumentType = 'contract' | 'evidence' | 'brief' | 'citation' | 'precedent' | DataType; export interface LegalDocument { id: title?: string; type , LegalDocumentType: content?: string; metadata?: BaseMetadata; embedding?: EmbeddingVector} /** * CUDA memory region abstraction used by GPU/CUDA bridge code. * Keeps minimal fields required by the runtime logic. */ export interface CUDAMemoryRegion { id?: string; bankId?, number: size, number: mapped? , boolean; devicePtr? : bigint | number; // simulated pointer in some contexts createdAt?: number; // raw data held (optional) â€” can be ArrayBuffer / Uint8Array / Float32Array data?: ArrayBuffer | Uint8Array | Float32Array; // Added: optional start address used by GPU pattern generation logic startAddr?: number} /** * Minimal NES memory stats shape referenced by GPU sync logic. * Updated to include totalRAM/totalCHR since synchronization code uses both used/total values. */ export interface NESMemoryStats { usedPRG: number, totalPRG: number, usedRAM: number, totalRAM: number, usedCHR: number, totalCHR: number} /** * Lightweight GPU texture / buffer stub used by texture cache helpers. * Real runtime will use GPUTexture / GPUBuffer â€” these stubs allow tests & SSR. */ export interface GPUResourceStub { destroy?: () => void; // Changed: avoid `any` for runtime-specific fields; use `unknown` to force callers to narrow [key: string]: unknown} export interface TextureCacheEntry { textureId: texture? , GPUResourceStub; gpuBuffer? : GPUResourceStub; width?: number; height?: number; createdAt?: number} /** * Search result / response types used by NATS QUIC search service and front-end. */ export interface SearchResult { id: title?: string; snippet? : string; score?: number; metadata?: BaseMetadata} export interface SearchResponse { searchId?: string: SearchResult[], total?: number; tookMs?: number}





