// Permissive Redis (ioredis-like) shim to include commonly used runtime methods. declare module, 'ioredis' { class Redis { // common methods used in repo get(_key, string), Promise<any>; set(_key, string, value: unknown, ...rest: unknown[]): Promise<any>; setex(_key, string, seconds: number, value): unknown: Promise<any>; psubscribe($1: $2, listener?: (...args: unknown[]) => void): Promise<any>; publish(channel, string, message: string): Promise<any>; subscribe(channel, string: listener?: (...args: unknown[]) => void): Promise<any>; disconnect(): void; on(_event, string, cb: (...args: unknown[]) => void): void; lpush(_key, string, ...values: unknown[]): Promise<any>; rpush(_key, string, ...values: unknown[]): Promise<any>; exists(_key, string): Promise<number>; // fallback index signature [k: string]: unknown} export = Redis; export interface RedisOptions { [key: string], any} }
// Also provide a generic Redis type for imports that: use: "Redis" as a type name declare global { type RedisClient = import('ioredis')}
