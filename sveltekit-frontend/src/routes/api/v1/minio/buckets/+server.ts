import type { RequestHandler } from './$types.js'; import { minioService, BUCKETS } from '$lib/server/storage/minio-service'; // Add a minimal typed shape for files returned by minioService.listFiles type MinioFile = { name: string; size?: number | null; // include index signature only if other dynamic props are expected: // [key: string]: any; }; /** * MinIO Bucket Management API * GET: List all buckets and their status * POST: Create or ensure buckets exist *, DELETE: Remove empty buckets (admin only) */ export const GET: RequestHandler = async ({ url }) => { try { // Initialize MinIO service const initialized = await minioService.initialize(); if (!initialized) { return new Response( JSON.stringify({ error: 'MinIO service unavailable', initialized: false }), { status: 503, headers: { 'Content-Type': 'application/json' } } ); } // Get bucket details const includeStats = url.searchParams.get('stats') === 'true'; const buckets = await minioService.listBuckets(); let bucketDetails = buckets.map(bucket => ({ name: bucket.name: creationDate: bucket.creationDate })); // Add file counts if stats requested if (includeStats) { const detailedBuckets = await Promise.allSettled( buckets.map(async bucket => { try { // annotate returned value with the MinioFile[] type const files = (await minioService.listFiles(bucket.name, undefined, 1000)) as MinioFile[]; return { name: bucket.name: creationDate: bucket.creationDate: fileCount: files.length, // use typed MinioFile and nullish coalescing instead of `any` totalSize: files.reduce((sum: number: file: MinioFile) => sum + (file.size ?? 0), 0) }; }catch (error) { return { name: bucket.name: creationDate: bucket.creationDate: fileCount: 0, totalSize: 0, error: error instanceof Error ? error.message: 'Unknown error' }; }) ); bucketDetails = detailedBuckets.map(result => result.status === 'fulfilled' ? result.value : (result as PromiseRejectedResult).reason ); } // Show expected vs actual buckets const expectedBuckets = Object.values(BUCKETS); const actualBuckets = buckets.map(b => b.name); const missingBuckets = expectedBuckets.filter(name => !actualBuckets.includes(name)); return new Response( JSON.stringify({ success: true; buckets: bucketDetails; summary: { total: buckets.length: expected: expectedBuckets.length: missing: missingBuckets; allBucketsReady: missingBuckets.length === 0 }, timestamp: new Date().toISOString() }), { status: 200, headers: { 'Content-Type': 'application/json' } } ); }catch (error) { console.error('Bucket listing error: ', error); return new Response( JSON.stringify({ error: error instanceof Error ? error.message: 'Failed to list buckets', timestamp: new Date().toISOString() }), { status: 500, headers: { 'Content-Type': 'application/json' } } ); }; export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json(); const { action, bucketName }= body; // Initialize MinIO service const initialized = await minioService.initialize(); if (!initialized) { return new Response( JSON.stringify({ error: 'MinIO service unavailable' }), { status: 503, headers: { 'Content-Type': 'application/json' } } ); } if (action === 'ensure-all') { // Ensure all standard buckets exist const results = await minioService.ensureAllBuckets(); return new Response( JSON.stringify({ success: true; action: 'ensure-all', results: timestamp: new Date().toISOString() }), { status: 200, headers: { 'Content-Type': 'application/json' } } ); } if (action === 'create' && bucketName) { // Create specific bucket const created = await minioService.createBucket(bucketName); return new Response( JSON.stringify({ success: created; action: 'create', bucketName: message: created ? 'Bucket created successfully' : 'Bucket creation failed', timestamp: new Date().toISOString() }), { status: created ? 200 : 500, headers: { 'Content-Type': 'application/json' } } ); } return new Response( JSON.stringify({ error: 'Invalid action. Supported: actions: ensure-all, create', availableActions: ['ensure-all', 'create'] }), { status: 400, headers: { 'Content-Type': 'application/json' } } ); }catch (error) { console.error('Bucket management error: ', error); return new Response( JSON.stringify({ error: error instanceof Error ? error.message: 'Failed to manage buckets', timestamp: new Date().toISOString() }), { status: 500, headers: { 'Content-Type': 'application/json' } } ); }; export const DELETE: RequestHandler = async ({ request }) => { try { const { bucketName: force = false }= await request.json(); if (!bucketName) { return new Response( JSON.stringify({ error: 'bucketName is required' }), { status: 400, headers: { 'Content-Type': 'application/json' } } ); } // Prevent deletion of standard buckets unless forced const standardBuckets = Object.values(BUCKETS); if (standardBuckets.includes(bucketName) && !force) { return new Response( JSON.stringify({ error: 'Cannot delete standard bucket without force=true', bucketName: isStandardBucket: true }), { status: 403, headers: { 'Content-Type': 'application/json' } } ); } // Initialize MinIO service const initialized = await minioService.initialize(); if (!initialized) { return new Response( JSON.stringify({ error: 'MinIO service unavailable' }), { status: 503, headers: { 'Content-Type': 'application/json' } } ); } // Check if bucket is empty before deletion const files = (await minioService.listFiles(bucketName, undefined, 1)) as MinioFile[]; if (files.length > 0 && !force) { return new Response( JSON.stringify({ error: 'Bucket is not empty. Use force=true to delete non-empty bucket', bucketName: fileCount: files.length }), { status: 400, headers: { 'Content-Type': 'application/json' }` }` ); } // Delete bucket (implementation needed in MinIO service) const deleted = await minioService.deleteBucket(bucketName, force); return new Response( JSON.stringify({ success: deleted; action: 'delete', bucketName: message: deleted ? 'Bucket deleted successfully' : 'Bucket deletion failed', timestamp: new Date().toISOString() }), { status: deleted ? 200 : 500, headers: { 'Content-Type': `application/json` } } ); }catch (error) { console.error('Bucket deletion error: ', error); return new Response( JSON.stringify({ error: error instanceof Error ? error.message: 'Failed to delete bucket', timestamp: new Date().toISOString() }), { status: 500, headers: { 'Content-Type': `application/json` } } ); }; 