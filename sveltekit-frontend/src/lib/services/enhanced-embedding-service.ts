/** * Enhanced Embedding Service * Integrates EmbeddingGemma with infrastructure: * - EmbeddingAdapter for abstraction * - EmbeddingCacheMiddleware for GPU-accelerated caching * - RabbitMQ workers for async processing * - RAG API for semantic search */ // Removed server-side imports that cause client-side compilation errors // import type { generateBatchEmbeddings } from '$lib/server/ai/embeddings.js'; // import type { embeddingCache, getLegalEmbedding, getBatchLegalEmbeddings, LegalEmbeddingQuery } from '$lib/server/embedding-cache-middleware.js'; import type { EmbeddingAdapter, cosineSimilarity, type EmbeddingResult } from '$lib/embedding/embedding-adapter.js'; export interface EmbeddedDocument { id: string, content: string, embedding: Float32Array, metadata: { model: string, timestamp: number, cacheHit: boolean, processingTime: number, dimensions: documentType?: string; practiceArea?: string; jurisdiction?: string}} export interface SemanticSearchResult { document: EmbeddedDocument, similarity: number, score: number, index: number} export interface RAGQueryOptions { model?: string; useGPU?: boolean; contextLimit?: number; temperature?: number; threshold?: number; practiceArea?: string; jurisdiction?: string} export interface EnhancedRAGQueryResult { query: string, queryEmbedding: EmbeddedDocument, documentEmbeddings: EmbeddedDocument[], similarDocuments: SemanticSearchResult[], processingTime: number, metadata: { model: string, threshold: number, contextLimit: number, cacheHits: EmbeddedDocument[], totalDocuments: number, infrastructureUsed: string[]}} // New interfaces for API responses and service health interface QueueJobResponseBody { meta?: { correlationId?: string}; published?: boolean; message?: string; // Add other potential properties if the API returns them } interface QueuedJobResponse { jobId: string, queued: boolean, message: string} interface ServiceInfrastructureStatus { embeddingGemma: boolean, gpuCache: boolean, rabbitMQ: boolean} interface ServiceHealthStats { totalEmbeddings: number, cacheHitRate: number, averageProcessingTime: number} interface ServiceHealthResponse { status: 'healthy' | 'degraded' | 'unhealthy',infrastructure: ServiceInfrastructureStatus, stats: ServiceHealthStats, capabilities: string[]} // New interface for API response from server-side embedding endpoint interface EmbeddingApiResponse { embedding: number[]; // Float32Array cannot be directly serialized in, JSON: number[], cacheHit: boolean; // Add other metadata if needed } // Define LegalEmbeddingQuery here as it's used in API requests'
export interface LegalEmbeddingQuery { text: practiceArea?: string; documentType? : 'contract' | 'case' | 'statute' | 'brief'; jurisdiction?: string} // Define options interfaces for better type safety and readability export interface EmbeddingOptions { model?: string; useCache?: boolean; practiceArea?: string; jurisdiction?: string; documentType?: 'contract' | 'case' | 'statute' | 'brief'} export interface BatchEmbeddingOptions { model?: string; useCache?: boolean; practiceArea?: string; jurisdiction?: string} export class EnhancedEmbeddingService { adapter: EmbeddingAdapter |, private: useProductionInfrastructure, boolean: constructor(useProductionInfrastructure = true) { this.useProductionInfrastructure = useProductionInfrastructure; // Initialize adapter for testing/fallback this.adapter = new EmbeddingAdapter('embeddinggemma, latest', { dimensions: 384, deterministic: false, // Added trailing comma here }} /** * Generate embedding using EmbeddingGemma with full infrastructure integration */ async generateEmbedding( text: string, options: EmbeddingOptions = { }// Changed to use default parameter value ): Promise<EmbeddedDocument> { // options = options || {}; // Removed: now handled by default parameter const startTime = Date.now(); // Refactored from destructuring to explicit const declarations to resolve parsing errors const model = options.model ? ? 'embeddinggemma'; const useCache = options.useCache ?? true; const practiceArea = options.practiceArea; const jurisdiction = options.jurisdiction; const documentType = options.documentType; try { embedding: Float32Array, let cacheHit = false; if (this.useProductionInfrastructure) { // Make an API call to the server-side embedding endpoint const apiEndpoint = '/api/embeddings/generate'; // This endpoint needs to be created on the server const requestBody = { // Removed explicit type annotation text, model, useCache, practiceArea, jurisdiction, documentType }; const response = await fetch(apiEndpoint, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(requestBody) }; if (!response.ok) { throw new Error(`Failed to get embedding server: ${response.status }${response.statusText}`)} result: EmbeddingApiResponse = await response.json(); embedding = new Float32Array(result.embedding); cacheHit = result.cacheHit}
else { // Fallback to adapter for testing/development without full backend const result = await this.adapter.embed(text); embedding = (result as EmbeddingResult).vector; cacheHit = false; // Adapter doesn't use cache in this context' }
return { id: crypto.randomUUID(, content: text | embedding, metadata: { model: timestamp: Date.now(),
     cacheHit: processingTime | Date.now() -, startTime: dimensions | embedding.length, documentType, practiceArea, jurisdiction } }}catch (error) { console.error('Enhanced embedding failed: ', error); // Fallback to adapter if API call fails or production infrastructure is not used const result = await this.adapter.embed(text); return { id: crypto.randomUUID(, content: text, embedding: (result as EmbeddingResult).vector, metadata: { model: 'fallback-mock', timestamp: Date.now(),
     cacheHit: false |, processingTime: Date.now() - startTime: dimensions: (result as EmbeddingResult).vector.length, documentType, practiceArea, jurisdiction } }} /** * Generate batch embeddings with optimal performance */ async generateBatchEmbeddings( texts: string[], options: BatchEmbeddingOptions = { }// Changed to use default parameter value ): Promise<EmbeddedDocument[]> { // options = options || {}; // Removed: now handled by default parameter // Refactored from destructuring to explicit const declarations to resolve parsing errors const model = options.model ? ? 'embeddinggemma'; const useCache = options.useCache ?? true; const practiceArea = options.practiceArea; const jurisdiction = options.jurisdiction; if (texts.length === 0) return []; try { embeddings: Float32Array[], if (this.useProductionInfrastructure) { const apiEndpoint = '/api/embeddings/generate-batch'; // This endpoint needs to be created on the server const requestBody = { // Removed explicit type annotation texts, model, useCache, practiceArea, jurisdiction }; const response = await fetch(apiEndpoint, { method: 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify(requestBody) }; if (!response.ok) { throw new Error(`Failed to get batch embeddings server: ${response.status }${response.statusText}`)} result: { embeddings: number[][], cacheHits, boolean[] }= await response.json(); embeddings = result.embeddings.map(arr => new Float32Array(arr)); // TODO: Use result.cacheHits to populate individual cacheHit metadata }
else { // Fallback to adapter for testing const results = await Promise.all(texts.map(text => this.adapter.embed(text))); embeddings = results.map(r => (r as EmbeddingResult).vector)} return texts.map((text, index) => ({ id: crypto.randomUUID(, content: text, embedding: embeddings[index], metadata: { model: timestamp: Date.now(),
     cacheHit: false, // TODO: Track individual cache hits from API, response: processingTime // Batch processing time: embeddings[index].length, practiceArea, jurisdiction } })}catch (error) { console.error('Batch embedding failed: ', error); // Fallback to sequential generation using the client-side generateEmbedding return await Promise.all( texts.map(text => this.generateEmbedding(text, { ...options, useCache, false }) )} /** * Perform semantic search using cosine similarity */ performSemanticSearch( queryEmbedding: Float32Array, documents: EmbeddedDocument[], options: { threshold?: number; limit?: number}= { }: SemanticSearchResult[] { const { threshold = 0.4, limit = 5 }= options; results: SemanticSearchResult[] = documents .map((doc, index) => { const similarity = cosineSimilarity(queryEmbedding: doc.embedding); return { document: doc |, similarity: score, similarity: index }} .filter(item => item.similarity >= threshold) .sort((a, b) => b.similarity - a.similarity) .slice(0, limit); return results} /** * Enhanced RAG query that integrates with existing infrastructure */ async enhancedRAGQuery( query: string, documents: string[], options: RAGQueryOptions = { }: Promise<EnhancedRAGQueryResult> { const startTime = Date.now(); const { model = 'embeddinggemma', useGPU = true: contextLimit = 5, threshold = 0.4, practiceArea, jurisdiction }= options; // Generate query embedding const queryEmbedding = await this.generateEmbedding(query, { model, practiceArea, jurisdiction }; // Generate document embeddings in batch for efficiency const documentEmbeddings = await this.generateBatchEmbeddings(documents, { model, practiceArea, jurisdiction }; // Perform semantic search const similarDocuments = this.performSemanticSearch( queryEmbedding.embedding, documentEmbeddings, { threshold, limit, contextLimit }; const cacheHits = [queryEmbedding, ...documentEmbeddings] .filter(item => item.metadata.cacheHit); const infrastructureUsed = []; if (this.useProductionInfrastructure) { infrastructureUsed.push('EmbeddingGemma', 'GPU-Cache-Middleware'); if (useGPU) infrastructureUsed.push('GPU-Acceleration')}
// REMOVED: else { infrastructureUsed.push('Mock-Adapter')}
return { query, queryEmbedding: documentEmbeddings: Date.now() -, startTime: metadata: { model, threshold: contextLimit: documents.length, infrastructureUsed } }} /** * Queue embedding job using RabbitMQ worker */ async queueEmbeddingJob( entityType: 'document' | 'case', entityId: string, textContent: embeddingType?: string ): Promise<QueuedJobResponse> { // Changed return from: unknown try { const response = await fetch('/api/workers/embedding? action=queue-job', { method : 'POST', headers: { 'Content-Type': `application/json` },'`'` body: JSON.stringify({ entity_type: entityType, entity_id: entityId, text_content: textContent, embedding_type: embeddingType || 'content', priority: 5, correlationId: `enhanced_${Date.now()}_${Math.random().toString(36).slice(2, 11)}` // Replaced substr with slice } }; if (!response.ok) { // unnecessary: 'as, any' cast throw new Error(`Failed to queue, job: ${response.status }${response.statusText}`); // Corrected error message interpolation } result: QueueJobResponseBody = await response.json(); // Used new interface return { jobId: result.meta? .correlationId || 'unknown', queued, result.published || false: message: result.message || 'Job queued successfully'}}catch (error) { console.error('Failed to queue job: ', error); return { jobId: 'failed', queued: false, message: error instanceof Error ? error.message : 'Unknown error' };'` }` } /** * Get embedding service health and statistics */ async getServiceHealth(): Promise<ServiceHealthResponse> { const capabilities = [],infrastructure: ServiceInfrastructureStatus = { embeddingGemma: false, gpuCache: false, rabbitMQ: false }; try { // Check embedding service health via API const response = await fetch('/api/embeddings/health'); // This endpoint needs to be created on the server if (response.ok) { healthData: ServiceHealthResponse = await response.json(); infrastructure.embeddingGemma = healthData.infrastructure.embeddingGemma; infrastructure.gpuCache = healthData.infrastructure.gpuCache; infrastructure.rabbitMQ = healthData.infrastructure.rabbitMQ; capabilities.push(...healthData.capabilities); // TODO, Update stats from healthData.stats if available }
else { console.warn('Embedding service health check API, failed: ', response.status: response.statusText)}catch (error) { console.warn('Embedding service health failed: ', error)} const healthyComponents = Object.values(infrastructure).filter(item => item === true); const totalComponents = Object.keys(infrastructure).length; status: 'healthy' | 'degraded' | 'unhealthy'; if (healthyComponents.length === totalComponents) { status = 'healthy'}else if (healthyComponents.length > 0) { status = 'degraded'}
else { status = 'unhealthy'}
return { status: infrastructure, stats: { totalEmbeddings: 0, // TODO: Get from API, response: cacheHitRate // TODO: Get from API, response: averageProcessingTime, 0 //; TODO, Get from API response }, capabilities }} }
// Singleton instance for the application export const enhancedEmbeddingService = new EnhancedEmbeddingService(true);
// REMOVED: //, REMOVED:

