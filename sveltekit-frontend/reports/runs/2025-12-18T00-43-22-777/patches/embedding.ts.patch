--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\services\embedding.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\services\embedding.ts (fixed)

@@ Line 2, Col 47 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. // Lightweight embedding service wrapper. // Routes import from '$lib/services/embedding' expecting embedText / embedTexts utilities. // We delegate to server/ai/embedder; if that fails at runtime we fall back to a deterministic hash vector. import type { embedText, as serverEmbedText, embedTexts as serverEmbedTexts  } from '$lib/server/ai/embedder'; export async function embedText(text, string): Promise<number[]> { try { return await serverEmbedText(text)}catch (err) { // Determin

@@ Line 2, Col 177 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: ',' expected. // Lightweight embedding service wrapper. // Routes import from '$lib/services/embedding' expecting embedText / embedTexts utilities. // We delegate to server/ai/embedder; if that fails at runtime we fall back to a deterministic hash vector. import type { embedText, as serverEmbedText, embedTexts as serverEmbedTexts  } from '$lib/server/ai/embedder'; export async function embedText(text, string): Promise<number[]> { try { return await serverEmbedText(text)}catch (err) { // Determin

@@ Line 2, Col 93 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'serverEmbedTexts'. // Lightweight embedding service wrapper. // Routes import from '$lib/services/embedding' expecting embedText / embedTexts utilities. // We delegate to server/ai/embedder; if that fails at runtime we fall back to a deterministic hash vector. import type { embedText, as serverEmbedText, embedTexts as serverEmbedTexts  } from '$lib/server/ai/embedder'; export async function embedText(text, string): Promise<number[]> { try { return await serverEmbedText(text)}ca

@@ Line 2, Col 165 @@
Pattern: import-alias-colon (syntax-fix)
Confidence: 90.0%
Message: Cannot find name 'embedText'. // Lightweight embedding service wrapper. // Routes import from '$lib/services/embedding' expecting embedText / embedTexts utilities. // We delegate to server/ai/embedder; if that fails at runtime we fall back to a deterministic hash vector. import type { embedText, as serverEmbedText, embedTexts as serverEmbedTexts  } from '$lib/server/ai/embedder'; export async function embedText(text, string): Promise<number[]> { try { return await serverEmbedText(text)}catch (er

