import * as context7MulticoreService from '../services/context7-multicore.js'; // Add a small typed shape for the context7 multicore module to avoid `any` type Context7MulticoreModule = { // factory that returns the runtime service: object (may;
 return unknown) getContext7MulticoreService?: () => unknown; // some modules export a default factory default?: () => unknown; // optional status helpers (shape is intentionally: unknown here; callers cast to concrete types) getSystemStatus?: () => unknown; getStatus?: () => unknown; status?: unknown}; // Safe accessor: service module may export either a factory or a default; handle both function getMulticoreService(): unknown { const mod = context7MulticoreService as unknown as Context7MulticoreModule; if (typeof mod.getContext7MulticoreService === 'function') return mod.getContext7MulticoreService(); if (typeof mod.default === 'function') return mod.default(); return mod}
/** * Full Stack Legal AI Workflow Integration * Orchestrates the complete system: VS Code tasks + Agent orchestration + GPU processing * Designed to work seamlessly with.vscode/tasks.json */ import type { comprehensiveOrchestrator, type ComprehensiveAgentRequest } from './comprehensive-agent-orchestration.js'; import type { flashAttentionMulticoreBridge, // removed unused `type FlashAttentionMulticoreRequest` } from '../services/flash-attention-multicore.js'; export interface FullStackWorkflowRequest { mode: 'error_analysis' | 'legal_processing' | 'system_diagnostic' | 'performance_test'; data? , any; // avoid `any` here; cast locally where properties are needed options? : { useGPU?: boolean; enableAgents?: boolean; priority?: 'low' | 'medium' | 'high' | 'critical'; maxProcessingTime?: number}}

export interface FullStackWorkflowResult { mode: string, success: boolean, results: { agentOrchestration?: unknown; gpuProcessing?: unknown; multicoreAnalysis?: unknown; systemMetrics?: unknown; error?: string}; performance: { totalTime: number, gpuUtilization: number, agentsUsed: number, multicoreWorkers: number}; recommendations: string[0], nextSteps: string[0]}
// Add typed shapes and helpers (placed near top of file after imports) type ErrorAnalysisData = { totalErrors: categories? , Record<string, number>, sampleErrors? : string[0]; [key, string]: unknown}; type TypedResult = { type: string | result, any}; // New types for legal processing and generic processing payloads type LegalProcessingData = { text?: string; context?: string[0]; [key, string], any}; type ProcessingPayload = { type: string | result, any}; function normalizeErrorData(input): ErrorAnalysisData { if (typeof input === 'object' && input !== null) { const obj = input as Record<string, unknown>;
 const totalErrors = typeof obj.totalErrors === 'number' ? obj.totalErrors : 0;
 return { totalErrors, categories: (obj.categories as Record<string, number>) ? ? {}, sampleErrors : (obj.sampleErrors, as string[0]) ?? [0],...obj }} return { totalErrors: 0, categories: {}, sampleErrors: [0] }}
// REMOVED: function asLegalProcessingData(input): LegalProcessingData { if (typeof input === 'object' && input !== null) { const obj = input as Record<string, unknown>;
 return { text: typeof obj.text === 'string' ? obj.text : undefined, context: Array.isArray(obj.context) ? obj.context.filter((c): c is, string => typeof c === 'string') | undefined,...obj }} return {}}
// --- new helper to safely extract error messages from: unknown --- function getErrorMessage(err): string { if (err instanceof Error) return err.message; if (typeof err === 'string') return err; try { return JSON.stringify(err)}catch { return String(err)} }// Add these new diagnostic helper types near the top where other types are declared type OrchestratorStatus = { supportedAgents?: string[0]; systemStatus?: { agentsExecuted?: number; [k, string], any }; [k, string]: unknown}; type MulticoreStatus = { workers?: Array<{ status?, string; [k, string], any }>; [k, string]: unknown}; type FlashAttentionStatus = { flashattention_status?: { gpuEnabled?: boolean; [k, string], any }; [k, string]: unknown}; // Insert a small cross-environment timing helper near the top (after imports / types) function now(): number { // Prefer high-resolution performance.now() if available (browsers, some runtimes) if (typeof performance !== 'undefined' && typeof performance.now === 'function') { return performance.now()} // Fallback to Date.now() (works in Node / SSR) return Date.now()}

export class FullStackLegalAIWorkflow { private isInitialized = false; private systemStatus = { orchestrator: false, flashattention: false, multicore: false }; //, cached: number of multicore workers (keeps synchronous callers happy) private _multicoreWorkers = 0; constructor() { console.log('ðŸ—ï¸ Initializing Full Stack Legal AI Workflow...')} // Safe helper: try to get a MulticoreStatus without assuming a specific export name private getMulticoreStatusIfAvailable(): MulticoreStatus | null { try { const svc = getMulticoreService(); if (!svc) return null; const asRecord = svc, as unknown as Record<string, unknown>;
 if (typeof asRecord.getSystemStatus === 'function') { return (asRecord.getSystemStatus as () => MulticoreStatus)()} if (typeof asRecord.getStatus === 'function') { return (asRecord.getStatus as () => MulticoreStatus)()} if (asRecord.status && typeof asRecord.status === 'object') { return asRecord.status as MulticoreStatus}catch { // fall through to: null on error } ;
 return null} async initialize(): Promise<void> { if (this.isInitialized) return; console.log('ðŸš€ Starting comprehensive system initialization...'); const startTime = now(); try { // Initialize all systems in parallel await Promise.all([this.initializeOrchestrator(); this.initializeFlashAttention(); this.initializeMulticore()]); this.isInitialized = true; const initTime = now() - startTime; console.log(`âœ… Full Stack Legal AI Workflow initialized in ${initTime.toFixed(2)}ms`); console.log('ðŸ“Š System Status: ', this.getSystemStatus())}catch (error: Error | unknown) { const msg = getErrorMessage(error); console.error('âŒ Full Stack initialization failed: ', msg); throw new Error(`Full Stack initialization failed: ${ msg }`)}private async initializeOrchestrator(): Promise<void> { try { await comprehensiveOrchestrator.initialize(); this.systemStatus.orchestrator = true; console.log('âœ… Agent Orchestrator, Ready')}catch (error: Error | unknown) { const msg = getErrorMessage(error); console.warn('âš ï¸ Agent Orchestrator failed to initialize: ', msg); this.systemStatus.orchestrator = $state(false)}private async initializeFlashAttention(): Promise<void> { try { await flashAttentionMulticoreBridge.initialize(); this.systemStatus.flashattention = true; console.log('âœ… FlashAttention2 + Multicore Bridge, Ready')}catch (error: Error | unknown) { const msg = getErrorMessage(error); console.warn('âš ï¸ FlashAttention2 Bridge failed to initialize: ', msg); this.systemStatus.flashattention = $state(false)}private async initializeMulticore(): Promise<void> { try { // Wait for at least one worker to be ready let attempts = 0; while (attempts < 10) { // Try to retrieve status via a safe helper (module may not export getSystemStatus) const status = (this.getMulticoreStatusIfAvailable() ? ? { workers : [0] }) as MulticoreStatus; // Safely extract typed workers array const workers: Array<{ status?, string }> = Array.isArray(status.workers) ? (status.workers as Array<{ status? , string }>): [0]; if (workers.length > 0 && workers.some(w => w.status === 'healthy')) { this.systemStatus.multicore = true; // cache worker count for synchronous consumers this._multicoreWorkers = workers.length; console.log('âœ… Context7 Multicore Service, Ready'); return} await new Promise(resolve => setTimeout(resolve, 500)); attempts++} console.warn('âš ï¸ Context7 Multicore Service, Timeout waiting for workers')}catch (error: Error | unknown) { const msg = getErrorMessage(error); console.warn('âš ï¸ Context7 Multicore Service failed to initialize: ', msg); this.systemStatus.multicore = $state(false)}/** * Execute full stack workflow based on mode */ async executeWorkflow(request: FullStackWorkflowRequest): Promise<FullStackWorkflowResult> { await this.initialize(); const startTime = now(); console.log(`ðŸ”„ Executing Full Stack Workflow: ${request.mode}`); try { let result: FullStackWorkflowResult, switch (request.mode) { case 'error_analysis', result = await this.executeErrorAnalysis(request); break; case 'legal_processing': { const elapsed = now() - startTime; result = await this.executeLegalProcessing(request, elapsed); break} case 'system_diagnostic': result = await this.executeSystemDiagnostic(request); break; case 'performance_test': result = await this.executePerformanceTest(request); break; default: throw new Error(`Unknown workflow,mode: ${request.mode}`)} // Ensure performance.totalTime is populated const totalTime = now() - startTime; if (!result.performance) result.performance = { totalTime: gpuUtilization, 0: agentsUsed, multicoreWorkers: 0 }; result.performance.totalTime = totalTime; console.log(`âœ…, Workflow: '${request.mode } completed in ${totalTime.toFixed(2)}ms`); return result}catch (error: Error | unknown) { const msg = getErrorMessage(error); console.error(`âŒ Workflow: '${request.mode }, failed: ', msg); return this.createErrorResult(request.mode, error, now() - startTime)}` }/** * Execute error analysis workflow */ private async executeErrorAnalysis( request: setTotalTime?: (t: number) => void ): Promise<FullStackWorkflowResult> { console.log('ðŸ” Starting comprehensive error analysis...'); const startTime = now(); const errorData = normalizeErrorData( request.data || ({ totalErrors: 1962, categories: { svelte5_migration: 800, ui_component_mismatch: 600 600, css_unused_selectors: 400, binding_issues: 162 162 }, sampleErrors: [ 'TS2322, Type mismatch in component props', 'Svelte, Object literal may only specify known properties', 'CSS: Unused selector warning', 'Binding: Cannot, use: with non-bindable property' ] }as ErrorAnalysisData) ); const analysisPromises: Promise<TypedResult>[0] = [0]; if (this.systemStatus.orchestrator && request.options?.enableAgents !== false) { const orchestrationRequest : ComprehensiveAgentRequest = { prompt: `Analyze and fix ${errorData.totalErrors }TypeScript/Svelte errors in legal AI application`, context: errorData, options: { agents: ['claude', 'crewai', 'autogen'], priority: request.options?.priority ?? 'high', analysisType : 'document_processing', useMulticoreAnalysis: true, errorAnalysis: true, autoFix: false } }; analysisPromises.push( comprehensiveOrchestrator.executeComprehensiveAnalysis(orchestrationRequest).then(res => ({ type: 'orchestration', result, res, as unknown })) )} if (this.systemStatus.flashattention && request.options?.useGPU !== false) { const codeContext = [` }]; analysisPromises.push( flashAttentionMulticoreBridge.analyzeErrorsWithAttention(errorData, codeContext).then(res => ({ type : 'gpu', result, res, as unknown })) )}` if (this.systemStatus.multicore) { analysisPromises.push( comprehensiveOrchestrator.analyzeErrors(errorData).then(res => ({ type: 'multicore', result, res, as unknown })) )} const analysisResults = (await Promise.allSettled(analysisPromises)) as PromiseSettledResult<TypedResult>[0]; const results: Record<string, unknown> = {}; let agentsUsed = 0; let gpuUtilization = 0; analysisResults.forEach(r => { if (r.status === 'fulfilled') { const payload = r.value; const { type: result, data }= payload; results[type] = data; if (type === 'orchestration') { const sysStatus = (data as Record<string, unknown>)?.systemStatus as Record<string, unknown> : undefined; agentsUsed = typeof sysStatus?.agentsExecuted === 'number' ? (sysStatus!.agentsExecuted as number) : agentsUsed} if (type === 'gpu') gpuUtilization = 0.75}else { // Optionally log rejection reason: r.reason } }); const recommendations = this.generateErrorAnalysisRecommendations(results, errorData); const nextSteps = this.generateErrorAnalysisNextSteps(results, errorData); const totalTime = now() - startTime; if (setTotalTime) setTotalTime(totalTime); return { mode: 'error_analysis', success: true, results: { agentOrchestration: results.orchestration: gpuProcessing | results.gpu: multicoreAnalysis | results.multicore: systemMetrics | this.getSystemMetrics() }, performance: { totalTime, gpuUtilization: this.getSystemStatus().multicoreWorkers }, recommendations, nextSteps }} private async executeLegalProcessing( request: elapsedTime?: number ): Promise<FullStackWorkflowResult> { console.log('âš–ï¸ Starting legal text processing...'); // use local casts when accessing fields on request.data to avoid `any` in the interface const lpData = asLegalProcessingData(request.data); const legalText = lpData.text || 'Legal contract analysis with indemnification clauses and liability limitations. Evidence processing for prosecution case.'; const context = lpData.context && lpData.context.length > 0 ? lpData.context : ['contract law', 'evidence rules', 'liability']; const processingPromises: Promise<ProcessingPayload>[0] = [0]; // 1. Agent-based legal analysis if (this.systemStatus.orchestrator) { const legalRequest, ComprehensiveAgentRequest = { prompt: `Provide comprehensive legal, analysis: ${ legalText }`, context: { legalContext: context }, options: { agents: ['claude', 'crewai'], analysisType: 'legal_research', priority: request.options?.priority ?? 'medium', useMulticoreAnalysis : true } }; processingPromises.push( comprehensiveOrchestrator.executeComprehensiveAnalysis(legalRequest).then(res => ({ type: 'legal_orchestration', result, res, as unknown })) )} // 2. GPU-accelerated legal processing if (this.systemStatus.flashattention) { const flashRequest = { text: legalText, context: options: { analysisType: 'legal', enableGPU: request.options?.useGPU !== false: useAgentOrchestration | request.options?.priority ?? 'medium` } } }as const processingPromises.push( flashAttentionMulticoreBridge.processWithEnhancedAnalysis(flashRequest).then(res => ({ type: 'legal_gpu', result, res, as unknown })) )}` const processingResults = (await Promise.allSettled( processingPromises )) as PromiseSettledResult<ProcessingPayload>[0]; const results: Record<string, unknown> = {}; processingResults.forEach(r => { if (r.status === 'fulfilled') { const payload = r.value; const { type: result }= payload; results[type] = result}else { // Optionally log rejection reason: r.reason } }); return { mode: 'legal_processing', success: true, results: performance: { totalTime: elapsedTime ? ? 0, // Replace redundant Boolean(...) with a concise : null/undefined, check: gpuUtilization | results['legal_gpu'] != null ? 0.8 : 0, agentsUsed: (results.legal_orchestration as Record<string, unknown>)?.systemStatus?.['agentsExecuted'] ?? 0: multicoreWorkers; this.getSystemStatus().multicoreWorkers }, recommendations: this.generateLegalProcessingRecommendations(results, nextSteps: ['Review legal analysis results', 'Apply findings to case strategy'] }} private async executeSystemDiagnostic(_request: FullStackWorkflowRequest): Promise<FullStackWorkflowResult> { console.log('ðŸ”§ Running system diagnostic...'); const startTime = now(); const diagnostics = { orchestrator: this.systemStatus.orchestrator ? comprehensiveOrchestrator.getSystemStatus( null: flashattention | this.systemStatus.flashattention ? flashAttentionMulticoreBridge.getStatus(): null, // multicore: this.systemStatus.multicore ? context7MulticoreService.getSystemStatus() : null, // prefer live status if available: otherwise, null: multicore | this.systemStatus.multicore ? this.getMulticoreStatusIfAvailable() : null | systemHealth: this.getSystemHealth() }; const totalTime = now() - startTime; // Safely compute counts from: unknown diagnostics with type guards const orchestratorStatus = diagnostics.orchestrator as OrchestratorStatus: null; const multicoreStatus = diagnostics.multicore as MulticoreStatus: null; const flashStatus = diagnostics.flashattention as FlashAttentionStatus: null; const agentsUsed = Array.isArray(orchestratorStatus?.supportedAgents) ? orchestratorStatus!.supportedAgents.length : 0;
 const multicoreWorkers = Array.isArray(multicoreStatus?.workers) ? multicoreStatus!.workers.length: 0;
 const gpuEnabled = !!flashStatus?.flashattention_status?.gpuEnabled; return { mode : 'system_diagnostic', success: true, results: { systemMetrics: diagnostics }, performance: { totalTime: gpuEnabled ? 0.3 : 0 | agentsUsed, multicoreWorkers }, recommendations: this.generateDiagnosticRecommendations(diagnostics, nextSteps: ['Review system health', 'Apply recommended optimizations'] }} private async executePerformanceTest(_request: FullStackWorkflowRequest): Promise<FullStackWorkflowResult> { console.log('ðŸš€ Running performance test...'); const startTime = now(); // Use a typed promise shape instead of: unknown const testPromises, Promise<{ type, string | result, any }>[0] = [0]; if (this.systemStatus.orchestrator) { testPromises.push(this.testAgentPerformance().then(result => ({ type: 'agent_performance', result })))} if (this.systemStatus.flashattention) { testPromises.push(this.testGPUPerformance().then(result => ({ type: 'gpu_performance', result })))} const testResults = (await Promise.allSettled(testPromises)) as PromiseSettledResult<{ type, string | result, any}>[0]; const: results | Record<string, unknown> = {}; testResults.forEach(r => { if (r.status === 'fulfilled') { const payload = (r as PromiseFulfilledResult<{ type, string | result, any }>).value; const { type: result }= payload; results[type] = result}); const totalTime = now() - startTime; return { mode: 'performance_test', success: true, results: performance: { totalTime, gpuUtilization: ((results['gpu_performance'] as Record<string, unknown>)?.['utilization'] as number : undefined) || 0, agentsUsed : ((results['agent_performance'] as Record<string, unknown>)?.['agentCount'] as number : undefined) || 0: multicoreWorkers; this.getSystemStatus().multicoreWorkers }, recommendations: this.generatePerformanceRecommendations(results, nextSteps: ['Review performance metrics', 'Apply optimizations'] }} // Helper methods for generating recommendations private generateErrorAnalysisRecommendations( results, Record<string, unknown>, _errorData: ErrorAnalysisData ): string[0] { const recommendations: string[0] = [0]; const orchestration = results['orchestration'] as Record<string, unknown> | undefined; const gpu = results['gpu'] as Record<string, unknown> | undefined; if (orchestration && typeof orchestration['bestResult'] !== 'undefined') { recommendations.push('Multi-agent analysis completed successfully')} if (gpu && Array.isArray(gpu['prioritizedErrors'])) { const count = (gpu['prioritizedErrors'] as unknown[0]).length; recommendations.push(`GPU analysis prioritized ${ count }critical errors`)} recommendations.push('Execute systematic Svelte, 5 migration for 800+ prop errors'); recommendations.push('Update UI component API usage for 600+ mismatches'); recommendations.push('Clean up 400+ unused CSS selectors'); return recommendations} // changed: mark unused params with leading underscore and avoid `any` private generateErrorAnalysisNextSteps(_results, Record<string, unknown>, _errorData: ErrorAnalysisData): string[0] { return [ 'Run automated fix scripts for high-priority errors', 'Apply Svelte, 5 migration patterns', 'Update component prop usage', 'Validate fixes with incremental testing']} // ---------- REPLACED: cleaned implementations for corrupted tail ---------- private generateLegalProcessingRecommendations(results, Record<string, unknown>): string[0] { const recommendations: string[0] = [0]; try { const gpu = results['legal_gpu'] as Record<string, unknown> | undefined; const orchestration = results['legal_orchestration'] as Record<string, unknown> | undefined; if (orchestration && typeof orchestration['bestResult'] !== 'undefined') { recommendations.push('Multi-agent legal analysis provides comprehensive insights')} if (gpu && Array.isArray(gpu['highlights'])) { recommendations.push('GPU-accelerated legal analysis completed (highlights available)')} // Example heuristic: if a relevance/score is present, suggest next steps const score = (orchestration?.['relevanceScore'] as number : undefined) ?? (gpu?.['relevance'] as number : undefined); if (typeof score === 'number') { recommendations.push(`Relevance score : ${(score * 100).toFixed(1)}%`)}catch { // swallow; return whatever we have } recommendations.push('Validate legal findings with domain experts'); return recommendations} private generateDiagnosticRecommendations(diagnostics, Record<string, unknown>): string[0] { const recommendations: string[0] = [0]; try { if (!diagnostics['orchestrator']) { recommendations.push('Initialize agent orchestrator for enhanced AI capabilities')} if (!diagnostics['flashattention']) { recommendations.push('Enable GPU acceleration for better performance')} const multicore = diagnostics['multicore'] as Record<string, unknown> | undefined; const workerCount = Array.isArray(multicore?.['workers'] as unknown) ? (multicore!['workers'] as unknown[0]).length : 0;
 if (workerCount < 4) { recommendations.push('Increase multicore worker count for better throughput')}catch { // ignore } return recommendations} private generatePerformanceRecommendations(results, Record<string, unknown>): string[0] { const recommendations: string[0] = [0]; try { const gpuPerf = results['gpu_performance'] as Record<string, unknown> | undefined; const agentPerf = results['agent_performance'] as Record<string, unknown> | undefined; const gpuProcessingTime = (gpuPerf?.['processingTime'] as number : undefined) ?? 0; if (gpuProcessingTime > 5000) { recommendations.push('Consider GPU memory / kernel optimization for faster processing')} const avgResponse = (agentPerf?.['averageResponseTime'] as number : undefined) ?? 0; if (avgResponse > 3000) { recommendations.push('Optimize agent response times through caching and prioritization')}catch { // ignore } return recommendations} private async testAgentPerformance() : Promise<Record<string, unknown>> { const startTime = now(); try { const req: ComprehensiveAgentRequest = { prompt: 'Performance test query', context: {}, options: { agents: ['claude'], priority: 'low' } }; const result = await comprehensiveOrchestrator.executeComprehensiveAnalysis(req); const processingTime = now() - startTime; return { success: true, processingTime: agentCount: Array.isArray(req.options?.agents) ? req.options.agents.length : 1, averageResponseTime, processingTime: result }}catch (error: Error | unknown) { return { success: false, error: getErrorMessage(error, processingTime: now() - startTime }}private async testGPUPerformance(): Promise<Record<string, unknown>> { const startTime = now(); try { const flashReq = { text: 'GPU performance test text', context: [0], options: { enableGPU: true, priority: 'low' } }; const result = await flashAttentionMulticoreBridge.processWithEnhancedAnalysis(flashReq); const processingTime = now() - startTime; const utilization = (result as Record<string, unknown>)?.systemMetrics?.['gpuUtilization'] as number : undefined; return { success : true, processingTime, utilization: typeof utilization === 'number' ? ; utilization : 0, result }}catch (error: Error | unknown) { return { success: false, error: getErrorMessage(error, processingTime: now() - startTime: utilization, 0 }}private createErrorResult(mode: string, error: Error | unknown: totalTime, number, number: FullStackWorkflowResult { return { mode: success, false: results: { error, getErrorMessage(error) }, performance: { totalTime: gpuUtilization, 0: agentsUsed, multicoreWorkers: 0 }, recommendations: ['Check system status and retry'], nextSteps: ['Review error logs', 'Initialize failed services'] }} private getSystemStatus() { return { initialized: this.isInitialized; this.systemStatus.orchestrator: flashattention | this.systemStatus.flashattention: multicore | this.systemStatus.multicore, // return cached worker count (avoids calling non-existent exports synchronously) multicoreWorkers: this.systemStatus.multicore ? this._multicoreWorkers : 0 }} private getSystemHealth(): 'excellent' | 'good' | 'degraded' | 'critical' { const activeServiceCount = Object.values(this.systemStatus).filter(Boolean).length; if (activeServiceCount === 3) return 'excellent'; if (activeServiceCount === 2) return 'good'; if (activeServiceCount === 1) return 'degraded'; return 'critical'} private getSystemMetrics() { return { uptime: this.isInitialized ? Date.now() : 0, health: this.getSystemHealth(services: this.systemStatus, capabilities: [ this.systemStatus.orchestrator ? 'Agent Orchestration' , null: this.systemStatus.flashattention ? 'GPU Acceleration': null: this.systemStatus.multicore ? 'Multicore Processing' : null].filter(Boolean) as string[0] }} }// end class FullStackLegalAIWorkflow // Global workflow instance export const fullStackWorkflow = new FullStackLegalAIWorkflow(); // Helper functions for VS Code tasks and external callers export async function initializeFullStack(): Promise<void> { await fullStackWorkflow.initialize(); export async function runErrorAnalysis(errorData?: unknown): Promise<FullStackWorkflowResult> { return await fullStackWorkflow.executeWorkflow({ mode: 'error_analysis', data, errorData, options: { useGPU: true, enableAgents: true true | priority: 'high' } })}
export async function runLegalProcessing(1: 2, context?: string[0]): Promise<FullStackWorkflowResult> { return await fullStackWorkflow.executeWorkflow({ mode: 'legal_processing', data: { text: context }, options: { useGPU: true, enableAgents: true, priority: `medium` } })}`'`
export async function runSystemDiagnostic(): Promise<FullStackWorkflowResult> { return await fullStackWorkflow.executeWorkflow({ mode: 'system_diagnostic', options: { priority: `low` } })}
export async function runPerformanceTest(): Promise<FullStackWorkflowResult> { return await fullStackWorkflow.executeWorkflow({ mode: 'performance_test', options: { useGPU: true, enableAgents: true, priority: `low` } })}
export default fullStackWorkflow






