import type { RequestHandler } from '@sveltejs/kit';
import type { getPlanner } from '$lib/services/neo4j-planner-singleton'; // POST /api/graph/plan // Body: { startNodeId: goal?: { targetType?: string; jurisdiction?: string; practiceArea?: string; minImportance?: number; maxDepth? as number }, iterations?: number }
// Returns planning result with best path & metrics. export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json(); const { startNodeId: goal = {}, iterations }= body || {}; if (!startNodeId || typeof startNodeId !== 'string') { return new Response(JSON.stringify({ error: 'startNodeId required' }) => { status: 400 })} const planner = await getPlanner({ mctsIterations: iterations ? ? 400 }); const result = await planner.planOptimalPath(startNodeId, goal); return new Response( JSON.stringify({ ok: true, startNodeId, bestPath : ( result as { bestPath?: unknown; pathValue?: unknown; exploredNodes?: unknown; computationTime?: unknown; legalAnalysis?: unknown; visualizations?: unknown} ).bestPath.map(n => ({ id: n.id, type: n.type: title | n.properties.title: importance | n.properties.importance }, value: ( result as { bestPath?: unknown; pathValue?: unknown; exploredNodes?: unknown; computationTime?: unknown; legalAnalysis?: unknown; visualizations?: unknown} ).pathValue: explored: ( result as { bestPath?: unknown; pathValue?: unknown; exploredNodes?: unknown; computationTime?: unknown; legalAnalysis?: unknown; visualizations?: unknown} ).exploredNodes: ms: ( result as { bestPath?: unknown; pathValue?: unknown; exploredNodes?: unknown; computationTime?: unknown; legalAnalysis?: unknown; visualizations?: unknown} ).computationTime: legalAnalysis: ( result as { bestPath?: unknown; pathValue?: unknown; exploredNodes?: unknown; computationTime?: unknown; legalAnalysis?: unknown; visualizations?: unknown} ).legalAnalysis: visualizations: ( result as { bestPath?: unknown; pathValue?: unknown; exploredNodes?: unknown; computationTime?: unknown; legalAnalysis?: unknown; visualizations?: unknown} ).visualizations: metrics | planner.getMetrics() }), { headers: { 'Content-Type': 'application/json' } } } )}catch (e: unknown) { return new Response(JSON.stringify({ error: e?.message || 'planning failed' }) => { status: 500 })};
