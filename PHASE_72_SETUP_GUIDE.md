# Phase 72: Complete Setup Guide\n\n**Date:** December 2, 2025  \n**Status:** Ready to Deploy  \n**Architecture:** Shared legal_ai_db + Phase 72/73/78 Brain\n\n---\n\n## 🎯 What You're Setting Up\n\nA complete error-learning brain that:\n- **Learns** from your codebase errors (Phase 72)\n- **Clusters** similar errors (Phase 73)\n- **Suggests** fixes via RAG (Phase 78)\n- **Integrates** with your SvelteKit app via API routes\n- **Powers** a real-time NES Command Center UI\n\n---\n\n## 📋 Prerequisites\n\n### Infrastructure (Already Running)\n\n✅ Postgres (phase66-postgres) with legal_ai_db  \n✅ Redis (phase66-redis)  \n✅ Qdrant (phase66-qdrant)  \n✅ Ollama with embeddinggemma + gemma3-legal  \n\n### Verify Status\n\n```powershell\n# Check Postgres\ndocker exec phase66-postgres psql -U postgres -d legal_ai_db -c \"\\dt\"\n\n# Check Redis\ndocker exec phase66-redis redis-cli PING\n\n# Check Qdrant\nInvoke-RestMethod -Uri \"http://localhost:6333/health\"\n\n# Check Ollama\ncurl http://127.0.0.1:11434/api/tags\n```\n\n---\n\n## 🚀 Setup Steps\n\n### Step 1: Initialize Phase 72 Tables\n\n```powershell\ncd sveltekit-frontend\n.\\scripts\\init-phase72-db.ps1 -Verify\n```\n\n**Expected output:**\n```\n✓ Schema file found\n✓ Schema applied successfully\n✓ Table exists: phase72_error\n✓ Table exists: phase72_error_vector\n✓ Table exists: phase72_cluster\n✓ Table exists: phase72_cluster_summary\n```\n\n### Step 2: Configure Environment\n\nUpdate `sveltekit-frontend/.env.local`:\n\n```bash\n# Database (shared with app)\nDATABASE_URL=postgresql://postgres:postgres@localhost:5432/legal_ai_db\n\n# Services\nREDIS_URL=redis://127.0.0.1:6379\nQDRANT_URL=http://127.0.0.1:6333\nOLLAMA_ENDPOINT=http://127.0.0.1:11434\n\n# Phase 72 Configuration\nPHASE72_CYCLE=1\nPHASE73_SIM_THRESHOLD=0.92\nPHASE73_MAX_CLUSTERS=100\nPHASE73_SAMPLES_PER_CLUSTER=6\n\n# Go Ingest Service\nGO_INGEST_URL=http://127.0.0.1:8089\n```\n\n### Step 3: Start Go Ingest Service\n\n```powershell\n# In a separate terminal\ncd go-services/phase72-ingest\ngo run .\n\n# Verify it's running\nInvoke-WebRequest -Uri \"http://127.0.0.1:8089/health\"\n```\n\n### Step 4: Run Phase 72 Topology\n\n```powershell\ncd sveltekit-frontend\n\n# Scan and vectorize errors\nnpm run phase72:topology\n\n# Or with specific cycle\nPHASE72_CYCLE=1 npm run phase72:topology\n```\n\n**Expected output:**\n```\n[phase72-topology] Scanning for errors...\n[phase72-topology] Found 42 errors\n[phase72-topology] Vectorizing with embeddinggemma:latest...\n[phase72-topology] Persisting to Postgres + Redis + Qdrant...\n[phase72-topology] ✓ Phase 72 complete\n```\n\n### Step 5: Run Phase 73 Clustering\n\n```powershell\n# Cluster similar errors\nPHASE72_CYCLE=1 npm run phase73:cluster\n```\n\n**Expected output:**\n```\n[phase73-cluster] Loading 42 errors from Postgres...\n[phase73-cluster] Clustering with similarity threshold 0.92...\n[phase73-cluster] Created 8 clusters\n[phase73-cluster] ✓ Phase 73 clustering complete\n```\n\n### Step 6: Run Phase 73 Summarization\n\n```powershell\n# Generate summaries\nPHASE72_CYCLE=1 npm run phase73:summarize\n```\n\n**Expected output:**\n```\n[phase73-summary] Finding clusters without summaries...\n[phase73-summary] Summarizing cluster 1/8...\n[phase73-summary] ✓ Phase 73 summarization complete\n```\n\n### Step 7: Test API Routes\n\n```powershell\n# Get errors for a route\nInvoke-RestMethod -Uri \"http://localhost:5173/api/phase72/errors?route=/cases\" -Method Get\n\n# Get fix suggestions\n$body = @{\n    route = \"/cases/[id]\"\n    code = \"TS2304\"\n    message = \"Cannot find name 'CardTitle'\"\n} | ConvertTo-Json\n\nInvoke-RestMethod -Uri \"http://localhost:5173/api/phase72/suggest-fix\" -Method Post -Body $body -ContentType \"application/json\"\n```\n\n---\n\n## 📊 Verify Data in Database\n\n```powershell\n# Check error count\ndocker exec phase66-postgres psql -U postgres -d legal_ai_db -c \"\n    SELECT COUNT(*) as total_errors FROM phase72_error;\n\"\n\n# Check cluster count\ndocker exec phase66-postgres psql -U postgres -d legal_ai_db -c \"\n    SELECT COUNT(*) as total_clusters FROM phase72_cluster;\n\"\n\n# Check error statistics\ndocker exec phase66-postgres psql -U postgres -d legal_ai_db -c \"\n    SELECT code, COUNT(*) as count\n    FROM phase72_error\n    GROUP BY code\n    ORDER BY count DESC\n    LIMIT 10;\n\"\n\n# Check route errors\ndocker exec phase66-postgres psql -U postgres -d legal_ai_db -c \"\n    SELECT file_path, COUNT(*) as error_count\n    FROM phase72_error\n    GROUP BY file_path\n    ORDER BY error_count DESC\n    LIMIT 10;\n\"\n```\n\n---\n\n## 🔗 API Routes\n\n### GET /api/phase72/errors\n\nGet errors for a specific route.\n\n**Query Parameters:**\n- `route` (required): Route path pattern (e.g., `/cases`, `/evidence`)\n- `limit` (optional): Max results (default: 200, max: 500)\n\n**Response:**\n```json\n{\n  \"errors\": [\n    {\n      \"id\": \"uuid\",\n      \"file_path\": \"src/routes/cases/+page.svelte\",\n      \"line\": 42,\n      \"column\": 13,\n      \"code\": \"TS2304\",\n      \"severity\": \"error\",\n      \"message\": \"Cannot find name 'CardTitle'\",\n      \"cycle\": 1,\n      \"created_at\": \"2025-12-02T13:05:01Z\"\n    }\n  ],\n  \"stats\": [\n    {\n      \"code\": \"TS2304\",\n      \"count\": 15,\n      \"first_seen\": \"2025-12-02T13:00:00Z\",\n      \"last_seen\": \"2025-12-02T13:05:00Z\"\n    }\n  ],\n  \"total\": 42\n}\n```\n\n### POST /api/phase72/suggest-fix\n\nGet fix suggestions for a route.\n\n**Request Body:**\n```json\n{\n  \"route\": \"/cases/[id]\",\n  \"code\": \"TS2304\",\n  \"message\": \"Cannot find name 'CardTitle'\",\n  \"file_path\": \"src/routes/cases/+page.svelte\"\n}\n```\n\n**Response:**\n```json\n{\n  \"plan\": \"### Error Analysis for /cases/[id]\\n\\n**Error Summary:** TS2304: 15 occurrences\\n\\n**Recommendations:**\\n1. Missing import: Check if component/type is imported from correct path\\n2. Verify Bits-UI v2 imports: import { Card } from \\\"$lib/components/ui/card\\\"\",\n  \"suggestions\": [\n    \"Missing import: Check if component/type is imported from correct path\",\n    \"Verify Bits-UI v2 imports: import { Card } from \\\"$lib/components/ui/card\\\"\"\n  ],\n  \"related_routes\": [\n    \"src/routes/evidence/+page.svelte\",\n    \"src/routes/poi/+page.svelte\"\n  ]\n}\n```\n\n---\n\n## 🧠 Brain Pass (Complete Pipeline)\n\nRun all phases in sequence:\n\n```powershell\nPHASE72_CYCLE=1 npm run phase78:brain-pass\n```\n\n**Expected output:**\n```\n[phase78-brain] Starting Phase 78 Brain Pass for cycle 1...\n[phase78-brain] ✓ Phase 72 Fast Scan complete\n[phase78-brain] ✓ Phase 73 Clustering complete\n[phase78-brain] ✓ Phase 73 Summarization complete\n[phase78-brain] Errors: 42\n[phase78-brain] Clusters: 8\n[phase78-brain] Summaries: 8\n[phase78-brain] ✓ Phase 78 Brain Pass complete in 45.2s\n```\n\n---\n\n## 📈 Monitoring\n\n### Query Error Trends\n\n```sql\n-- Errors by cycle\nSELECT cycle, COUNT(*) as error_count\nFROM phase72_error\nGROUP BY cycle\nORDER BY cycle DESC;\n\n-- Error reduction\nSELECT\n  cycle,\n  COUNT(*) as error_count,\n  LAG(COUNT(*)) OVER (ORDER BY cycle) as prev_cycle,\n  ROUND(100.0 * (LAG(COUNT(*)) OVER (ORDER BY cycle) - COUNT(*)) / LAG(COUNT(*)) OVER (ORDER BY cycle), 2) as reduction_pct\nFROM phase72_error\nGROUP BY cycle\nORDER BY cycle DESC;\n```\n\n### Query Cluster Quality\n\n```sql\nSELECT\n  label,\n  size,\n  COUNT(DISTINCT code) as unique_codes,\n  COUNT(DISTINCT file_path) as unique_files\nFROM phase72_cluster c\nLEFT JOIN phase72_error e ON e.cluster_id = c.id\nGROUP BY c.id, c.label, c.size\nORDER BY size DESC;\n```\n\n---\n\n## ❌ Troubleshooting\n\n### \"No Phase 72 tables found\"\n\n```powershell\n# Re-run initialization\n.\\scripts\\init-phase72-db.ps1 -Verify\n\n# Or manually create tables\nGet-Content backend/sql/phase72_topology_schema.sql | docker exec -i phase66-postgres psql -U postgres -d legal_ai_db -f -\n```\n\n### \"Go ingest service offline\"\n\n```powershell\n# Start the service\ncd go-services/phase72-ingest\ngo run .\n\n# Verify it's running\nInvoke-WebRequest -Uri \"http://127.0.0.1:8089/health\"\n```\n\n### \"Redis connection failed\"\n\n```powershell\n# Check Redis is running\ndocker exec phase66-redis redis-cli PING\n\n# If not, start it\ndocker compose up -d phase66-redis\n```\n\n### \"Qdrant unhealthy\"\n\n```powershell\n# Check Qdrant logs\ndocker logs --tail 50 phase66-qdrant\n\n# Restart if needed\ndocker restart phase66-qdrant\n```\n\n### \"API routes return 500\"\n\n```powershell\n# Check SvelteKit logs\n# Verify DATABASE_URL is set\necho $env:DATABASE_URL\n\n# Test database connection\npsql \"postgresql://postgres:postgres@localhost:5432/legal_ai_db\" -c \"SELECT COUNT(*) FROM phase72_error;\"\n```\n\n---\n\n## 📚 Next Steps\n\n### Immediate\n1. Run `init-phase72-db.ps1` to create tables\n2. Run `npm run phase72:topology` to scan errors\n3. Run `npm run phase73:cluster` to cluster errors\n4. Test API routes\n\n### Short Term\n1. Build NES Command Center UI\n2. Integrate with /all-routes page\n3. Add real-time polling\n4. Show error graphs\n\n### Medium Term\n1. Phase 79: Autonomous fixing\n2. Phase 80: Multi-cycle analysis\n3. Phase 81: Adaptive learning\n\n---\n\n## 📊 Architecture\n\n```\nSvelteKit App (legal_ai_db)\n    ↓\nAPI Routes (/api/phase72/*)\n    ↓\nPostgres (phase72_* tables)\n    ↓\nPhase 72: Vectorization\n    ↓\nPhase 73: Clustering\n    ↓\nPhase 73: Summarization\n    ↓\nPhase 78: Fix Suggestions\n    ↓\nNES Command Center UI\n```\n\n---\n\n**Status:** ✅ Ready to Deploy  \n**Next:** Run `init-phase72-db.ps1` to begin\n"}
