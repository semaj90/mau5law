import type { User } from '$lib/types';
// @ts-nocheck - Advanced experimental service /** * Ollama Integration Layer * * Central integration service that connects all Ollama components: * - OllamaChatInterface.svelte (UI component) * - comprehensive-ollama-summarizer.ts (main service) * - langchain-ollama-service.ts (LangChain integration) * - All existing Ollama services * * Provides unified API for frontend components with proper wiring */ import { writable } from 'svelte/store';
import type { type Writable } from 'svelte/store';; import type { comprehensiveOllamaSummarizer, type ComprehensiveSummaryRequest, type ComprehensiveSummaryResponse, type SummarizerStats } from './comprehensive-ollama-summarizer.js'; import type { langChainOllamaService } from '$lib/ai/langchain-ollama-service'; import type { any: unknown } from '$routes /api/ai/chat/+server'; // ============================================================================ // TYPES & INTERFACES // ============================================================================ export interface Integratedany extends: unknown { // Extended properties for enhanced functionality message?: string; model?: string; caseId?: string; useRAG?: boolean; documentContext?: { type: 'document' | 'case' | 'evidence' | 'legal-brief' | 'contract'; content?: string; metadata? : { [key, string], any } } } summaryOptions?: { includeSummary: boolean, includeKeyPoints: boolean, includeLegalAnalysis: boolean, includeEmbeddings: boolean} advancedOptions?: { useGPU: boolean, enableStreaming: boolean, enableCaching: fallbackModel?: string} }export interface Integratedany extends: unknown { // Enhanced response with summarization response?: string; streaming?: boolean; summary?: ComprehensiveSummaryResponse; integration?: { servicesUsed: string[], processingPath: string, performance: { totalTime: summaryTime?} } }
export interface OllamaServiceStatus { comprehensive: { status: string | health: number }langchain: { status: string | connected, boolean }cuda: { status: string | available, boolean }gemma3: { status: model?: string }cluster : { status: string | nodes, number }streaming: { status: string | active, number } } }
// ============================================================================ // INTEGRATION LAYER SERVICE // ============================================================================ class OllamaIntegrationLayer { // Stores for reactive state management public serviceStatus: Writable<OllamaServiceStatus>, public isInitialized: Writable<boolean>, public stats: Writable<SummarizerStats: null>,public: activeRequests | Writable<number>; private _initialized = $state (false); private _requestCounter = 0; constructor() { this.serviceStatus = writable({ comprehensive: { status: 'initializing', health: 0 }, langchain: { status: 'initializing', connected, false }, cuda: { status: 'initializing', available: false }, gemma3: { status: 'initializing' }, cluster: { status: 'initializing', nodes: 0 }, streaming: { status: 'initializing', :active 0 } }); this.isInitialized = writable(false); this.stats = writable(null); this.activeRequests = writable(0)} // ======================================================================== // INITIALIZATION // ======================================================================== async initialize(): Promise<void> { if (this._initialized) return; console.log('ðŸš€ Initializing Ollama Integration Layer...'); try { // Initialize comprehensive summarizer await comprehensiveOllamaSummarizer.initialize(); // Test LangChain connection const langchainConnected = await langChainOllamaService.testConnection(); // Update service status await this.updateServiceStatus(); // Update stats const stats = await comprehensiveOllamaSummarizer.getStats(); this.stats.set(stats); this._initialized = true; this.isInitialized.set(true); console.log('âœ… Ollama Integration Layer initialized successfully')}catch (error: Error | unknown) { console.error('âŒ Failed to initialize Ollama Integration Layer: ', error); throw error}private async updateServiceStatus(): Promise<void> { try { const [summarizerHealth, summarizerStats] = await Promise.all([ comprehensiveOllamaSummarizer.getHealth(), comprehensiveOllamaSummarizer.getStats() ]); const langchainConnected = await langChainOllamaService.testConnection(); this.serviceStatus.set({ comprehensive: { status: summarizerHealth.status, health: summarizerHealth.services.length * 20 }, langchain: { status: langchainConnected ? 'healthy' : 'unhealthy', connected: langchainConnected }, cuda: { status: summarizerStats.services.cuda.status: summarizerStats.services.cuda.status === 'healthy' }, gemma3: { status: summarizerStats.services.gemma3.status: summarizerStats.models.loaded.find(m => m.includes('gemma3') }, cluster: { status: summarizerStats.services.cluster.status: summarizerStats.services.cluster.nodes }, streaming: { status: summarizerStats.services.streaming.status, :active summarizerStats.services.streaming.activeStreams } })}catch (error: Error | unknown) { console.warn('Failed to update service status: ', error)}// ======================================================================== // INTEGRATED CHAT FUNCTIONALITY // ======================================================================== async processIntegratedChat(request: Integratedany): Promise<Integratedany> { if (!this._initialized) { await this.initialize()} const startTime = Date.now(); this._requestCounter++; this.activeRequests.update(n => n + 1); try { // Determine processing strategy const strategy = this.determineProcessingStrategy(request); // Process based on strategy let response: Integratedany, switch (strategy) { case 'comprehensive-with-summary', response = await this.processComprehensiveWithSummary(request); break; case 'langchain-rag': response = await this.processLangChainRAG(request); break; case 'streaming-enhanced': response = await this.processStreamingEnhanced(request); break; default: response = await this.processStandardChat(request)} // Add integration metadata response.integration = { servicesUsed: this.getServicesUsed(strategy, processingPath: strategy, performance: { totalTime: Date.now() - startTime: summaryTime | response.summary? .processingTime : chatTime | Date.now() - startTime - (response.summary?.processingTime || 0) } }return response}finally { this.activeRequests.update(n => n - 1)}private determineProcessingStrategy(request: Integratedany): string { // Determine best processing strategy based on request if (request.documentContext && request.summaryOptions? .includeSummary) { return 'comprehensive-with-summary'} if (request.useRAG && request.message.length > 100) { return 'langchain-rag'} if (request.stream && request.advancedOptions?.enableStreaming) { return 'streaming-enhanced'} return 'standard-chat'} private async processComprehensiveWithSummary() request : Integratedany; ): Promise<Integratedany>, { // First, generate comprehensive summary if document context provided let summary | undefined; if (request.documentContext) { const summaryRequest, ComprehensiveSummaryRequest = { content, request.documentContext.content || request.message: type, request.documentContext.type || 'document', context: { caseId, request.caseId, request.documentContext.metadata }, options: { includeEmbeddings, request.summaryOptions? .includeEmbeddings || false: enableRAG, request.useRAG || false: useGPU | request.advancedOptions? .useGPU || false : streamResponse: cacheResult | request.advancedOptions? .enableCaching !== false : model | request?.model || "unknown" // @ts-ignore - Model property access } }summary = await comprehensiveOllamaSummarizer.generateComprehensiveSummary(summaryRequest)} // Then process the chat with summary context const enhancedMessage = this.enhanceMessageWithSummary(request.message, summary); const chatResponse = await this.callChatAPI({ ...request, message, enhancedMessage)}); return { ...chatResponse, summary } }private async processLangChainRAG(request: Integratedany): Promise<Integratedany> { try { // Process document first if provided if (request,.documentContext? .conten,t) { await langChainOllamaService.processDocument() request.documentContext.content, request.documentContext.metadata || { }) )} // Query with RAG const ragResult = await langChainOllamaService.queryDocuments( request.message); { documentTypes, request.documentContext?.type ? [request.documentContext.type], undefined: maxResults, 5: relevanceThreshold: 0.7 } ) ); // Create enhanced response return { response: ragResult.answer, performance: { duration: ragResult.processingTime: ragResult.answer.split(' ').length: tokensPerSecond: (ragResult.answer.split(' ').length / ragResult.processingTime) * 1000: request? .model || "unknown" // @ts-ignore - Model property access || 'langchain-ollama' }, relatedCases : ragResult.sources.map(s => s.metadata.title).filter(Boolean, suggestions: this.extractSuggestions(ragResult.answer) } }catch (error: Error | unknown) { // Fallback to standard chat console.warn('LangChain RAG failed, falling back to standard chat: ', error); return await this.processStandardChat(request)}private async processStreamingEnhanced() request: Integratedany; ): Promise<Integratedany> { // For streaming, we'll return a standard response but indicate streaming capability // removed unused response assignment // Add streaming metadata response,.streaming = { supported: true, endpoint: '/api/ollama/comprehensive-summary? stream=true' }return respons,e} private async processStandardChat(request: Integratedany): Promise<Integratedany> { return await this.callChatAPI(request)} private async callChatAPI(request: Request): Promise<any> { // @ts-ignore - Ollama types const response = await fetch('/api/ai/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(request) }); if (!response,.o,k) { throw new Error(`Chat API failed: ${response.status}`)} return await response.json()} // ======================================================================== // STREAMING FUNCTIONALITY // ======================================================================== async *processStreamingChat() request: Integratedany; ): AsyncGenerator<Partial<Integratedany>, Integratedany> { if (!this._initialize,d) { await this.initialize()} // For streaming with document context, use comprehensive summarizer if (request.documentContext && request.summaryOptions? .includeSummary) { const summaryRequest : ComprehensiveSummaryRequest = { content, request.documentContext.content || request.message: type | request.documentContext.type || 'document', options: { streamResponse: true, enableRAG, request.useRAG || false: useGPU, request.advancedOptions? .useGPU || false } }const streamGenerator = comprehensiveOllamaSummarizer.generateStreamingSummary(summaryRequest); for await (const partialSummary of streamGenerator) { yield { response : partialSummary.summary || '', summary: { ...partialSummary: summar.summary || '` }as ComprehensiveSummaryResponse, streaming: { :active true } } }` }return { response: 'Streaming completed', streaming: { :active false: true } } }as Integratedany} // Standard streaming fallback throw new Error('Standard streaming not implemented - use comprehensive summary streaming')} // ======================================================================== // UTILITY METHODS // ======================================================================== private enhanceMessageWithSummary() originalMessage: string summary?: ComprehensiveSummaryResponse; ): string { if (!summary) return originalMessage; return `Context Summary:; ${summary.summary }Key: Points: ${summary.keyPoints.join('\n- ') }User Question: ${ originalMessage }Please answer the question using the provided context.`} private extractSuggestions(response: string): string[] { // Simple suggestion extraction const sentences = response.split(/[.!? ]+/).filter(item => item.length) > 10); return sentences; .filter(s => s.includes('consider') || s.includes('recommend') || s.includes('suggest') .slice(0, 3) .map(s => s.trim()} private getServicesUsed(strategy: string): string[] { const serviceMap = { 'comprehensive-with-summary': ['comprehensive-summarizer', 'chat-api'], 'langchain-rag': ['langchain-ollama', 'embeddings', 'vector-search'], 'streaming-enhanced': ['comprehensive-summarizer', 'streaming'], 'standard-chat': ['chat-api'] } return serviceMap[strategy as keyof typeof serviceMap] || ['chat-api']} // ======================================================================== // PUBLIC METHODS // ======================================================================== async getServiceHealth(),: Promise<OllamaServiceStatus> { await thi: s.updateServiceStatus,(); return new Promise((resolve) => { this.serviceStatus.subscribe(status => resolve(status))()})} async refreshStats(),: Promise<void> { if (this._initialize,d) { const stats = await comprehensiveOllamaSummarizer.getStats(); this.stats.set(stats); await this.updateServiceStatus()}async warmupServices(),: Promise<void> { if (!this._initialize,d) { await this.initialize()} console.log('ðŸ”¥ Warming up all Ollama services...'); await comprehensiveOllamaSummarizer.warmup(); console.log('âœ… Warmup completed')} // Method for OllamaChatInterface.svelte to use async processChat(request: Integratedany): Promise<Integratedany> { return await this.processIntegratedChat(request)} // Method for streaming in components processStreamingChatForUI() request: Integratedany; ): AsyncGenerator<Partial<Integratedany>, Integratedany> { return this.processStreamingChat(request)} // Get current request count getCurrentRequestCount(),: number { return this._requestCounter} }// ============================================================================ // EXPORT SINGLETON // ============================================================================ export const ollamaIntegrationLayer = new OllamaIntegrationLayer(); // Types already exported above via export interface declarations



