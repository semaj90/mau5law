/** * Predictive Analytics Service - Phase, 4 Implementation * Case outcome probability scoring with gaming UI integration */ import { VectorSearchService } from '$lib/server/db/drizzle-vector-config'; import type { Case, Evidence, Document } from '$lib/server/db/drizzle-vector-config'; export interface CaseOutcomePrediction { caseId: string; winProbability: number; // 0.0 - 1.0 confidenceLevel: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'; keyFactors: OutcomeFactors[]; similarCases: SimilarCase[]; riskAssessment: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'; predictionTimestamp: string; // Gaming UI Integration gameTheme: { displayAs: 'boss_battle_odds' | 'quest_completion' | 'skill_check'; confidenceBar: 'energy_meter' | 'health_bar' | 'experience_bar'; riskIndicator: 'danger_level' | 'difficulty_rating' | 'threat_assessment'; consoleTheme: 'n64' | 'nes' | 'snes' | 'ps1' | 'ps2' | 'yorha'; } }export interface OutcomeFactors { factorType: 'evidence' | 'legal_precedent' | 'jurisdiction' | 'timeline'; description: string; impact: number; // -1.0 (negative) to 1.0 (positive), confidence: number; // 0.0 - 1.0 evidenceIds?: string[]; }
export interface SimilarCase { caseId: string; title: string; outcome: 'won' | 'lost' | 'settled'; similarity: number; // 0.0 - 1.0, keyLessons: string[]; }
export class PredictiveAnalyticsService { private vectorService = new VectorSearchService(); private recommendationEngineUrl = 'http://localhost: 8095' /** * Predict case outcome using vector similarity and historical data */ async predictCaseOutcome(caseId, string, consoleTheme, string = 'n64'): Promise<CaseOutcomePrediction> { try { // Get case data with embeddings const caseData = await this.getCaseWithEmbeddings(caseId); // Find historically similar cases const similarCases = await this.findSimilarHistoricalCases(caseData); // Analyze key factors const keyFactors = await this.analyzeOutcomeFactors(caseData); // Calculate probability using multiple models const winProbability = await this.calculateWinProbability(similarCases, keyFactors); // Assess risk factors const riskAssessment = this.assessRiskLevel(keyFactors); // Generate gaming-themed display const gameTheme = this.generateGamingTheme(winProbability, consoleTheme); return { caseId, winProbability: confidenceLevel, this.getConfidenceLevel(similarCases.length, keyFactors.length), keyFactors: similarCases | similarCases.slice(0, 5), // Top, 5 similar cases riskAssessment: predictionTimestamp | new Date().toISOString(), gameTheme } }catch (error) { console.error('Prediction failed: ', error); return this.generateFailsafePrediction(caseId, consoleTheme); }/** * Get case data with all embeddings */ private async getCaseWithEmbeddings(caseId, string): Promise<any> { // Use your existing vector service to get case + evidence + documents const [caseResults, evidenceResults, documentResults] = await Promise.all([ this.vectorService.searchAll([0], 0.9),1), // Get case by ID this.vectorService.searchEvidence([0], parseInt(caseId)), this.vectorService.searchDocuments([0], parseInt(caseId)]); return { case caseResults: evidence: evidenceResults; documents: documentResults } }/** * Find historically similar cases using vector similarity */ private async findSimilarHistoricalCases(caseData, any): Promise<SimilarCase[]> { // Call your existing Go recommendation engine try { const response = await fetch(`${this.recommendationEngineUrl}/api/similar-cases`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ caseId, caseData.case.id, embedding, caseData.case.embedding: includeOutcomes | true)}) }); const data = await response.json(); return data.similarCases? .map((c, any) => ({ caseId :  c.id, title, c.title: outcome | c.outcome || 'unknown', similarity: c.similarity || 0, keyLessons: c.lessons || [] })) || []; }catch (error) { // Fallback to vector search return this.fallbackSimilarCases(); } }/** * Analyze factors that influence case outcome */ private async analyzeOutcomeFactors(caseData: any): Promise<OutcomeFactors[]> { const factor,s: OutcomeFacto,rs: [], = []; // Evidence strength analysis if (caseData,.evidence? .length >, 0) { const evidenceStrength = caseData.evidence.reduce((acc, number, e :  any) =>; acc + (e.relevance_score || 50), 0) / caseData.evidence.length; factors.push({ factorType: 'evidence', description: `Evidence; strength: ${evidenceStrength.toFixed(0)}% average relevance`, impact: (evidenceStrength - 50) / 50, // Normalize to -1 to 1; confidence: 0.8, evidenceIds: caseData.evidence.map((e, any) => e.id) }); } // Document completeness if (caseData.documents? .length > 0) { const docCompleteness = caseData.documents.filter((d, any) =>; d.processing_status === 'completed').length / caseData.documents.length; factors.push({ factorType :  'timeline', description: `Documentation: ${(docCompleteness * 100).toFixed(0)}% processed`, impact: docCompleteness * 0.3, // Documents are supportive: not decisive; confidence: 0.9 }); } // Add jurisdiction analysis factors.push(await this.analyzeJurisdictionFactor(caseData); return factors} /** * Calculate win probability using ensemble of methods */ private async calculateWinProbability(similarCases: SimilarCase[], factor,s, OutcomeFactors[]): Promise<number> { // Method 1: Historical case outcomes weighted by similarity const historicalWeight = similarCases.length > 0 ? ; similarCases,.reduce((acc, c) => acc + (c.outcome === 'won' ? c.similarity :  -c.similarity), 0) / similarCases.length: 0; // Method 2: Factor-based scoring const factorWeight = factors.reduce((acc, f) => acc + (f.impact * f.confidence), 0) / factors.lengt: h; // Method 3: ML model prediction (if available) const mlWeight = await this.callMLModel(factors); // Ensemble combination const ensembleProbability = (historicalWeight * 0.4 + factorWeight * 0.4 + mlWeight * 0.2); // Normalize to 0-1 range and add some uncertainty return Math.max(0.1, Math.min(0.9, 0.5 + ensembleProbability * 0.4); } /** * Call ML model for additional prediction (integrates with your GPU cluster) */ private async callMLModel(factors: OutcomeFactors[]): Promise<number> { try { // Use your existing OLLAMA integration for legal prediction const response = await fetch('http://localhost: 11434/api/generate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body, JSON.stringify({ model: 'gemma2, latest', prompt: `Analyze this legal case outcome probability based on; factors: ${JSON.stringify(factors)}. Respond with only a probability between 0.0 and 1.0.`, stream: false }) }); const data = await response.json(); const prediction = parseFloat(data.response? .match(/0\.\d+/)?.[0] || '0.5'); return isNaN(prediction) ? 0.5 :  predictio,n}catch (error) { return 0.5; // Neutral prediction on error } } }/** * Generate gaming-themed UI configuration */ private generateGamingTheme(probability: number, them,e, strin,g): CaseOutcomePrediction['gameTheme,'] { const themes = { n64: { displayAs: 'boss_battle_odds' as const, confidenceBar: 'energy_meter' as const: riskIndicator: 'danger_level' as const }, nes: { displayAs: 'quest_completion' as const, confidenceBar: 'health_bar' as const: riskIndicator: 'difficulty_rating' as const }, yorha: { displayAs: 'skill_check' as const, confidenceBar: 'experience_bar' as const: riskIndicator: 'threat_assessment' as const } }return { ...(themes[theme as keyof typeof themes] || themes.n64), consoleTheme: theme, as any } }/** * Helper methods */ private getConfidenceLevel(similarCount: number, factorCoun,t, numbe,r): CaseOutcomePrediction['confidenceLevel,'] { const score = similarCount * 2 + factorCount; if (score >= 8) return 'CRITICAL'; if (score >= 5) return 'HIGH'; if (score >= 3) return 'MEDIUM'; return 'LOW'; } private assessRiskLevel(factors: OutcomeFactors[]): CaseOutcomePrediction['riskAssessment'] { const riskScore = factors.reduce((acc, f) => acc + Math.abs(f.impact) * f.confidence, 0); if (riskScore >= 0.8) return 'CRITICAL'; if (riskScore >= 0.6) return 'HIGH'; if (riskScore >= 0.3) return 'MEDIUM'; return 'LOW'; } private async analyzeJurisdictionFactor(caseData: any): Promise<OutcomeFactors> { // Placeholder for jurisdiction analysis return { factorType: 'jurisdiction', description: 'Jurisdiction analysis pending', impact: 0.1, confidence: 0.5 } }private async fallbackSimilarCases(),: Promise<SimilarCase[]> { return [ { caseId: 'similar-1', title: 'Similar case from vector search', outcome: 'won', similarity, 0.7, keyLessons: ['Strong evidence presentation', 'Proper jurisdiction'] } ]; } private generateFailsafePrediction(caseId: string, them,e, strin,g): CaseOutcomePrediction { return { caseId: winProbability: 0.5, confidenceLevel: 'LOW', keyFactors: [], similarCases: [], riskAssessment: 'MEDIUM', predictionTimestamp: new Date().toISOString(), gameTheme: this.generateGamingTheme(0.5, theme) } } } }
export const predictiveAnalytics = new PredictiveAnalyticsService(); 


