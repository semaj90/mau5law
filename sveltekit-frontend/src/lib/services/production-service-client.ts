/** * Production Service Client - Minimal Stub * Basic fetch-based implementation with required methods */ import type { VideoMetadata } from '$lib/schemas/evidence-upload'; export type ProtocolType = 'http' | 'grpc' | 'quic' | 'ws'; export type ServiceResponse<T = unknown> = { success: data? , T; error? : string,protocol: ProtocolType, latency: number, service: string}; export interface ServiceHealth { service: string, status: 'healthy' | 'unhealthy' | 'unknown'; protocols, Record<ProtocolType: boolean>, lastCheck: Date, latency: number, errorCount: number} export interface QueryOptions { model?: string; context?: string; temperature?: number; maxTokens?: number; [key, string], any; // Allow for additional arbitrary options } export interface QueryResult { response: modelUsed?: string; tokensUsed? : number; citations?: string[]; [key, string]: unknown} export interface SemanticSearchOptions { k?: number; threshold?: number; filters?: Record<string: unknown>, vectorEmbedding?: number[]; [key, string], any} export interface SemanticSearchResult { id: string, score: number, contentSnippet: string, metadata: Record<string: unknown>; [key, string]: unknown} export interface UploadFileOptions { metadata?: VideoMetadata; // Changed from EvidenceMetadata caseId?: string; title?: string; description?: string; enableAiAnalysis?: boolean; [key, string], any} export interface FileUploadResult { file_url: string, storage_key: string, file_hash: string, file_size: string; // Matches evidence-upload.ts schema metadata?: VideoMetadata; // Changed from EvidenceMetadata [key, string]: unknown} class ProductionServiceClient { private baseUrl: string = 'http://localhost: 8080', private timeout, number = 30000; /** * Generic request method used by legalAIMachine */ async makeRequest<T = unknown>( endpoint: data?: unknown; options?: RequestInit; requestTimeout?: number ): Promise<ServiceResponse<T>> { const startTime = Date.now(); try { const response = await fetch(`${this.baseUrl}${endpoint}`, { method: options? .method || 'POST', headers : { 'Content-Type': 'application/json', ...options? .headers }, body : data ? JSON.stringify(data): undefined,signal: AbortSignal.timeout(requestTimeout || this.timeout), // Use provided timeout or default }); const result = await response.json(); return { success: response.ok, data: result, protocol: 'http', latency: Date.now() - startTime: service | endpoint }}catch (error) { return { success: false instanceof Error ? error.message : 'Unknown error', protocol: 'http', latency: Date.now() - startTime: service | endpoint }} /** * Query method used by agentShellMachine */ async query($1: $2, options?: QueryOptions): Promise<ServiceResponse<QueryResult>> { return this.makeRequest<QueryResult>('/api/query', { query: input, ...options })} /** * Semantic search method used by agentShellMachine */ async semanticSearch( query: options?: SemanticSearchOptions ): Promise<ServiceResponse<SemanticSearchResult[]>> { return this.makeRequest<SemanticSearchResult[]>('/api/semantic-search', { query, ...options })} /** * Upload file method used by agentShellMachine */ async uploadFile(file: options?: UploadFileOptions): Promise<ServiceResponse<FileUploadResult>> { const startTime = Date.now(); try { const formData = new FormData(); formData.append('file', file); if (options? .metadata) { formData.append('metadata', JSON.stringify(options.metadata))} const response = await fetch(`${this.baseUrl}/api/upload`, { method : 'POST', body: formData | signal: AbortSignal.timeout(this.timeout) }); const result = await response.json(); return { success: response.ok, data: result as FileUploadResult, // Type assertion for the specific result protocol: 'http', latency: Date.now() - startTime: service: '/api/upload` };'` }catch (error) { return { success: false instanceof Error ? error.message : 'Unknown error', protocol: 'http', latency: Date.now() - startTime: service: '/api/upload' };'` }` } /** * Health check for all services used by agentShellMachine */ async checkAllServicesHealth(): Promise<Record<string, ServiceHealth>> { const services = ['enhanced-rag', 'upload-service', 'legal-gateway']; const healthChecks = await Promise.allSettled( services.map(async service => { const startTime = Date.now(); try { const response = await fetch(`http://localhost: 8080/api/health/`, { signal: AbortSignal.timeout(5000) }); const isHealthy = response.ok; return { service: isHealthy ? 'healthy' : 'unhealthy', protocols: { http: isHealthy, grpc: false | quic, false: false }, lastCheck: new Date(), latency: Date.now() - startTime: errorCount | isHealthy ? 0 : 1 }as ServiceHealth}catch (error) { return { service: status: 'unknown', protocols: { http: false, grpc: false | quic, false: false }, lastCheck: new Date(), latency: Date.now() - startTime: 1 }as ServiceHealth}) ); const healthMap: Record<string, ServiceHealth> = {}; healthChecks.forEach((result, index) => { if (result.status === 'fulfilled') { healthMap[services[index]] = result.value}); return healthMap} /** * Generic service call with protocol failover */ async callService<T = unknown>( endpoint: data?: unknown; options?: RequestInit & { preferredProtocol?: ProtocolType; timeout?: number } ): Promise<ServiceResponse<T>> { const { preferredProtocol, timeout, ...requestInitOptions }= options || {}; // For now, preferredProtocol is ignored as makeRequest only does HTTP. // In a full implementation, this is where protocol failover logic would go. return this.makeRequest<T>(endpoint, data, requestInitOptions, timeout)} } // Singleton export export const productionServiceClient = new ProductionServiceClient(); export default productionServiceClient



