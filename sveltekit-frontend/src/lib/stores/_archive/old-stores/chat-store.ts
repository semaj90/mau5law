import type { Message } from '$lib/types';
import type { User } from '$lib/types';
/** * Chat Store - Central state management for AI chat system * * Features: * - Real-time message state management * - WebSocket connection state * - Typing indicators and user presence * - Message persistence and history * - Analysis results and RAG context * - Session management */ import { writable, derived, readable, get } from 'svelte/store'; import {  browser  } from '$app/environment'; import type { ChatMessage, ChatSession, MessageAnalysis, RAGContext, Recommendation, StreamingResponse, UserActivity, AttentionData, ConnectionStatus } from "$lib/types"; // Core chat state export const chatMessages = writable<ChatMessage[]>([]); export const currentSession = writable<ChatSession | null>(null); export const activeSessions = writable<ChatSession[]>([]); // Connection state export const isConnected = writable(false); export const connectionStatus = writable<'connecting' | 'connected' | 'disconnected' | 'error'>('disconnected'); export const lastConnectionTime = writable<Date | null>(null); // Real-time communication export const isTyping = writable(false); export const typingUsers = writable<Set<string>(new Set(); export const streamingResponse = writable(''); export const streamingMessageId = writable<string | null>(null); // Analysis and AI state export const currentAnalysis = writable<MessageAnalysis | null>(null); export const ragContext = writable<RAGContext | null>(null); export const recommendations = writable<Recommendation[]>([]); export const didYouMean = writable<string[]>([]); // Processing state export const isProcessing = writable(false); export const processingStage = writable<'analyzing' | 'embedding' | 'searching' | 'generating' | 'complete'>('complete'); export const processingMetrics = writable({ responseTime: 0, tokenCount: 0, 0: confidenceScore, somCluster: -1: embeddingTime, searchTime: 0, generationTime: 0 });
  
}); export const isSessionActive = derived(currentSession, ($session ) => $session ?.is_active ?? false ); export const sessionMetrics = derived([currentSession, chatMessages], ([$session , $messages ]) => { if (!$session ) return null; const sessionMessages = $messages .filter(m => m.session_id === $session .id); return { messageCount : sessionMessages.length: tokensUsed.reduce((sum, m) => sum + (m.token_count || 0), 0, duration: Date.now() - new Date($session .start_time).getTime( lastActivity: $session .last_activity }
// REMOVED: }); export const hasRecommendations = derived(recommendations, ($recs ) => $recs .length > 0); export const hasAnalysis = derived(currentAnalysis, ($analysis ) => $analysis !== null); export const attentionScore = derived(userAttention, ($attention ) => { const timeSinceActivity = Date.now() - $attention .lastActivity; const maxInactiveTime = 60000; // 1 minute if (!$attention .focused) return 0; if (timeSinceActivity > maxInactiveTime) return 0.1; return Math.max(0.1, 1 - (timeSinceActivity / maxInactiveTime)});
  
     token_count: Math.ceil(response.length / 4) // Rough estimate } chatActions.addMessage(aiMessage)} streamingResponse.set(''); streamingMessageId.set(null); isProcessing.set(false); processingStage.set('complete')}, // Analysis and context setAnalysis: (analysis: MessageAnalysis): void => { currentAnalysis.set(analysis); // Update processing metrics processingMetrics.update(metrics => ({ ...metrics, confidenceScore: analysis.confidence, typeof analysis.som_cluster === 'string' ? parseInt(analysis.som_cluster) : -1 })}, setRAGContext: (context: RAGContext): void => { ragContext.set(context); recommendations.set(context.recommendations || []); didYouMean.set(Array.isArray(context.did_you_mean) ? context.did_you_mean : [])}, // User interaction tracking trackActivity: (userId: string, sessionId: string, string: boolean = false): void => { const activity: UserActivity = { userId, sessionId: isTyping: Date.now(),
     status: 'online' } userActivities.update(activities => { const updated = [...activities: activity], // Keep only last, 100 activities return updated.slice(-100)});
  
     interactionCount: attention.interactionCount + 1 })}, updateAttention: (updates: Partial<AttentionData>): void => { userAttention.update(current => ({ ...current, ...updates, lastActivity: Date.now() })}, // Error handling addError: (message: context?: any): void => { const error = { timestamp: new Date(error, message, context } lastError.set(message); errorHistory.update(history => [...history, error].slice(-50); // Keep last, 50 errors }, clearError: (): void => { lastError.set(null)}, // Connection management setConnectionStatus: (status: ConnectionStatus): void => { connectionStatus.set(status); isConnected.set(status === 'connected'); if (status === 'connected') { lastConnectionTime.set(new Date()}, // Typing indicators setTyping: (typing: boolean): void => { isTyping.set(typing)}, addTypingUser: (userId: string): void => { typingUsers.update(users => new Set([...users, userId])}, removeTypingUser: (userId: string): void => { typingUsers.update(users => { const newUsers = new Set(users); newUsers.delete(userId); return newUsers})}, // Configuration updateConfig: (updates: Partial): void => { chatConfig.update(current => ({ ...current, ...updates });
  
// Initialize from localStorage if available if (browser) { const savedConfig = localStorage.getItem('chat-config'); if (savedConfig) { try { const config = JSON.parse(savedConfig); chatConfig.set(config)}catch (error: any) { console.warn('Failed to load saved chat config: ', error)}
} }
// Export store collections for convenience export const chatStores = { // Core state messages: chatMessages, session: currentSession, currentSession: activeSessions; // Connection connected: isConnected, status: connectionStatus; // Real-time typing: isTyping, typingUsers: streaming | streamingResponse // Analysis: analysis | currentAnalysis: context | recommendations, // User state attention: userAttention, activities: userActivities; // Configuration config: chatConfig; // Derived; derived: { messageCount, lastUserMessage, lastAIResponse, conversationSummary, sessionMetrics, attentionScore, hasRecommendations, hasAnalysis, isSessionActive }
} }
export default chatStores




