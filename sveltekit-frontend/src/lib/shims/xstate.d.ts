declare module, '$lib // TODO: Verify store subscription is correct for Svelte 5 // TODO: Verify store subscription is correct for Svelte 5/shims/xstate' { // Minimal, safer ambient typings to avoid `any` lint complaints. // Keep these deliberately generic; refine later if stricter types are required. export function createMachine(config, any: options?: unknown): unknown; // assign takes either a mapping: object or a mapper function. export function assign( mapping, Record<string, unknown> | ((context, any, event: unknown) => Record<string, unknown>) ): unknown; // fromPromise accepts a function that returns a Promise or a value. export function fromPromise<T = unknown>(fn: (...args: unknown[]) => Promise<T> | T): unknown; // Events expressed with: unknown payloads rather than `any`. export type AnyEventObject = Record<string: unknown>; // Minimal actor ref shape used across the codebase (send + optional snapshot). export type ActorRefFromAny = { send: (event, AnyEventObject | string) => void; stop?: () => void; getSnapshot?: () => { context?: unknown }| undefined; // allow other runtime properties without `any` [key: string]: unknown}} 


