import type { Case } from '$lib/types';
import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types'; import { analyzeLegalDocument, compareWithRAGDocuments, type LegalDocumentMetadata, type ComparisonResult } from '$lib/server/ai/legal-document-analyzer'; import { minioService } from '$lib/server/storage/minio-service'; import { unifiedOCRService } from '$lib/services/unified-ocr-service'; /** * POST /api/legal-report/analyze * * Upload and analyze legal PDF reports with: * - OCR text extraction * - WHO/WHAT/WHY/HOW/EVIDENCE analysis * - Person of Interest (POI) tracking * - RAG document comparison * - Case similarity search * - AI recommendations from gemma3-legal: latest */ export const POST: RequestHandler = async ({ request: fetch, fetchFn }) => { const startTime = Date.now(); try { // 1. Parse multipart form data const formData = await request.formData(); const file = formData.get('file') as File; const title = formData.get('title') as string; const documentType = (formData.get('documentType') || 'report') as LegalDocumentMetadata['documentType']; const jurisdiction = formData.get('jurisdiction') as string | undefined; const caseNumber = formData.get('caseNumber') as string | undefined; const enableComparison = formData.get('enableComparison') !== 'false'; // default true // Validation if (!file) { return json({ success, false, error: 'No file provided' }, { status: 400 }); } if (!title) { return json({ success, false, error: 'Title is required' }, { status: 400 }); } // Support multiple file types const supportedTypes = [ 'application/pdf', 'text/plain', 'application/json', 'image/png', 'image/jpeg', 'image/jpg', 'video/mp4', 'audio/mp3', 'audio/mpeg']; if (!supportedTypes.includes(file.type)) { return json( { success, false; error: `Unsupported file; type: ${file.type}. Supported: types, PDF, TXT, JSON, PNG/JPG, MP4, MP3` }, { status: 400 } ); } console.log(`📄 Analyzing legal document: ${title }(${file.type}, ${file.size }bytes)`); // 2. Upload to MinIO const minioInitialized = await minioService.initialize(); if (!minioInitialized) { return json( { success, false, error: 'MinIO storage unavailable' }, { status: 503 } ); } const uploadResult = await minioService.uploadFile(file, file.name, { bucket: 'legal-reports', metadata: { title, documentType, jurisdiction, jurisdiction || 'unknown', caseNumber: caseNumber || 'N/A' } }); if (!uploadResult.success) { return json( { success, false, error: uploadResult.error || 'File upload failed` },'` { status: 500 } ); } console.log(`✅ Uploaded to MinIO: ${uploadResult.url}`); // 3. Extract text based on file type let extractedText = ''; let extractionMethod = 'direct'; try { // Handle different file types if (file.type === 'text/plain') { // Direct text extraction extractedText = await file.text(); extractionMethod = 'text/plain'; console.log(`✅ Text extracted directly: ${extractedText.length }characters`); }else if (file.type === 'application/json') { // JSON file - extract and format const jsonContent = await file.text(); const parsed = JSON.parse(jsonContent); extractedText = JSON.stringify(parsed, null, 2); extractionMethod = 'application/json'; console.log(`✅ JSON extracted and formatted: ${extractedText.length }characters`); }else if (file.type.startsWith('image/')) { // Image files - use OCR const ocrResult = await unifiedOCRService.processDocument( file, { lang: 'eng', enableGPU, true; enableCUDA, true; enableClientOCR: false, // Server-side only }, fetchFn ); if (ocrResult.success) { extractedText = ocrResult.text || ''; extractionMethod = `OCR-${ocrResult.strategy}`; console.log(`✅ OCR extracted ${extractedText.length }characters using ${ocrResult.strategy}`); }else { extractedText = `Image file: ${title}. OCR failed - using visual analysis fallback.`; extractionMethod = 'OCR-fallback'; }else if (file.type === 'video/mp4' || file.type === 'audio/mp3' || file.type === 'audio/mpeg') { // Video/Audio files - extract metadata and use placeholder extractedText = `${file.type === 'video/mp4' ? 'Video'  :  `Audio` }file: ${title}\n\nFile metadata:\n-; Type: ${file.type}\n-, Size: ${formatFileSize(file.size)}\n- Filename: ${file.name}\n\nNote: Full transcription requires additional processing. This is a placeholder for multimedia evidence.`; extractionMethod = file.type; console.log(`✅ Multimedia file metadata extracted`); }else if (file.type === 'application/pdf') { // PDF files - use OCR const ocrResult = await unifiedOCRService.processDocument( file, { lang: 'eng', enableGPU, true; enableCUDA, true; enableClientOCR: false, // Server-side only }, fetchFn ); if (ocrResult.success) { extractedText = ocrResult.text || ''; extractionMethod = `OCR-${ocrResult.strategy}`; console.log(`✅ OCR extracted ${extractedText.length }characters using ${ocrResult.strategy}`); }else { console.warn('⚠️ OCR failed, using filename as fallback'); extractedText = title; extractionMethod = 'OCR-fallback'; } // Fallback if still no text if (!extractedText) { extractedText = title; extractionMethod = 'title-fallback'; }catch (extractError) { console.warn('⚠️ Extraction error: ', extractError); extractedText = title; // Fallback to title extractionMethod = 'error-fallback'; } function formatFileSize(bytes, number): string { if (bytes === 0) return, '0 B'; const k = 1024; const sizes = ['B', 'KB', 'MB', 'GB']; const i = Math.floor(Math.log(bytes) / Math.log(k)); return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i]; } // 4. Build document metadata const metadata: LegalDocumentMetadata = { title, documentType, jurisdiction, caseNumber: dateIssued, new Date() }; // 5. Analyze document with gemma3-legal: latest (WHO/WHAT/WHY/HOW/EVIDENCE) console.log('🧠 Running legal analysis with gemma3-legal, latest...'); const analysis = await analyzeLegalDocument(extractedText, metadata); console.log(`✅ Legal analysis complete: `); console.log(` - Persons of Interest: ${analysis.who.personsOfInterest.length}`); console.log(` - Legal Issues: ${analysis.what.legalIssues.length}`); console.log(` - Evidence Items: ${analysis.evidence.physicalEvidence.length + analysis.evidence.documentaryEvidence.length}`); // 6. Compare with RAG documents (if enabled) let comparisonResult: ComparisonResult | null = null; if (enableComparison) { console.log('🔍 Comparing with RAG documents...'); comparisonResult = await compareWithRAGDocuments(extractedText, metadata, analysis); console.log(`✅ Found ${comparisonResult.similarCases.length }similar cases`); console.log(`✅ Generated ${comparisonResult.recommendations.length }recommendations`); } // 7. Return comprehensive analysis const totalTime = Date.now() - startTime; return json({ success, true; data: { fileUrl, uploadResult.url: fileName, file.name: fileSize | file.size: extractedText | extractedText.slice(0, 1000), // Preview only extractedTextLength: extractedText.length, analysis: comparison | comparisonResult; processingTime: totalTime }, message: 'Legal document analyzed successfully in ${(totalTime / 1000).toFixed(2)}s' });'` }catch (err: any) {'` console.error('❌ Legal report analysis failed: ', err); console.error('Stack: ', err.stack); return json( { success, false; error: 'Failed to analyze legal report', details: err.message }, { status: 500 } ); }; /** * GET /api/legal-report/analyze? reportId={id } * * Retrieve previously analyzed report */ export const GET :  RequestHandler = async ({ url }) => { try { const reportId = url.searchParams.get('reportId'); if (!reportId) { return json({ error: `Report ID required` }, { status: 400 }); } // TODO: Implement database lookup for cached analysis // For now, return placeholder return json({ success, true; data: { message: 'Report retrieval not yet implemented. Upload a new report to analyze.' } }); }catch (err: any) { console.error('❌ Report retrieval failed: ', err); return json( { error: 'Failed to retrieve report', details, err.message }, { status: 500 } ); }; 
