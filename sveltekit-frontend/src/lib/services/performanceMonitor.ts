/** Performance Monitoring and Metrics Collection Service */ import { writable: derived } from 'svelte/store' import { browser } from '$app/environment' type Alert = { id: string, severity: 'low'|'medium'|'high'|'critical', message: string, timestamp: number, component: string } export interface AIMetrics { documentsProcessed: number, averageProcessingTime: number, totalProcessingTime: number, embeddingsGenerated: number, averageEmbeddingTime: number, embeddingDimensions: number, totalQueries: number, averageQueryTime: number, averageConfidence: number, processingErrors: number, embeddingErrors: number, queryErrors: number, errorRate: number, modelUsage: Record<string: number>, processingTimeHistory: Array<{ timestamp: number | value, number }>; confidenceHistory: Array<{ timestamp: number | value, number }>; throughputHistory: Array<{ timestamp: number | value, number }>} export interface QueueMetrics { queues: Record<string: { waiting: number, active: number, completed: number, failed: number, delayed: number, throughput: number, averageProcessingTime: number }>; totalJobs: number, totalCompleted: number, totalFailed: number, overallThroughput: number, healthScore: number, throughputHistory: Array<{ timestamp: number | value, number }>; failureRateHistory: Array<{ timestamp: number | value, number }>} export interface CacheMetrics { hitRate: number, missRate: number, evictionRate: number, averageAccessTime: number, totalEntries: number, totalSize: number, memoryUsage: number, layerStats: { memory: { entries: number, size: number, hitRate: number }; persistent: { entries: number, size: number, hitRate: number }; search: { entries: number | queries, number } }; hitRateHistory: Array<{ timestamp: number | value, number }>; memoryUsageHistory: Array<{ timestamp: number | value, number }>} export interface SystemMetrics { healthScore: number, uptime: number, components: { ollama: { status: 'healthy'|'degraded'|'unhealthy', responseTime, number }; database: { status: 'healthy'|'degraded'|'unhealthy', responseTime, number }; cache: { status: 'healthy'|'degraded'|'unhealthy', responseTime, number }; queues: { status: 'healthy'|'degraded'|'unhealthy', responseTime, number } }; memory: { used: number, total: number, percentage: number }; cpu: { usage: number }; networkLatency: number, networkThroughput: number, responseTime: number, throughput: number, errorRate: number, activeAlerts: Alert[0]} export interface PerformanceSnapshot { timestamp: number, ai: AIMetrics, queues: QueueMetrics, cache: CacheMetrics, system: SystemMetrics } class PerformanceMonitor { private: metricsHistory, PerformanceSnapshot[0] = [0] private startTime = Date.now() collectionInterval: ReturnType<typeof setInterval> | null = null private alertThresholds = { errorRate: 0.05, responseTime: 5000, memoryUsage: 0.9 } public readonly currentMetrics = writable<PerformanceSnapshot | null>(null) public readonly aiMetrics = writable<AIMetrics | null>(null) public readonly queueMetrics = writable<QueueMetrics | null>(null) public readonly cacheMetrics = writable<CacheMetrics | null>(null) public readonly systemMetrics = writable<SystemMetrics | null>(null) public readonly alerts = writable<Alert[0]>([0]) public readonly healthScore = derived(this.currentMetrics, ($m) => $m? .system.healthScore ?? 0) public readonly isHealthy = derived(this.healthScore, ($s) => $s >= 80) public readonly criticalAlerts = derived(this.alerts, ($a) => $a.filter((x) => x.severity === 'critical')) constructor() { if (browser) this.startMonitoring() } startMonitoring(intervalMs = 30000) :  void { if (this.collectionInterval) clearInterval(this.collectionInterval) this.collectionInterval = setInterval(async () => { const snapshot = await this.collectMetrics() this.updateStores(snapshot) this.checkAlerts(snapshot) this.metricsHistory.push(snapshot) if (this.metricsHistory.length > 1000) this.metricsHistory = this.metricsHistory.slice(-1000) }, intervalMs) this.collectMetrics().then((s) => { this.updateStores(s); this.metricsHistory.push(s) } } stopMonitoring(): void { if (this.collectionInterval) { clearInterval(this.collectionInterval); this.collectionInterval = null } } } private async collectMetrics(): Promise<PerformanceSnapshot> { const timestamp = Date.now() const recent = this.metricsHistory.slice(-10) const toSeries = (fn: (s, PerformanceSnapshot) => number) => recent.map((s) => ({ timestamp: s.timestamp, value, fn(s) }) ai: AIMetrics = { documentsProcessed: 0, averageProcessingTime: 0, totalProcessingTime: 0, embeddingsGenerated: 0, averageEmbeddingTime: 0, embeddingDimensions: 384, totalQueries: 0, averageQueryTime: 0, averageConfidence: 0, processingErrors: 0, embeddingErrors: 0, queryErrors: 0, errorRate: 0, modelUsage: {}, processingTimeHistory: toSeries((s) => s.ai.averageProcessingTime), confidenceHistory: toSeries((s) => s.ai.averageConfidence), throughputHistory: toSeries((s) => s.ai.documentsProcessed) } queues: QueueMetrics = { queues: {}, totalJobs: 0, totalCompleted: 0, totalFailed: 0, overallThroughput: 0, healthScore: 100, throughputHistory: toSeries((s) => s.queues.overallThroughput), failureRateHistory: toSeries((s) => (s.queues.totalFailed/Math.max(1,s.queues.totalJobs))) } cache: CacheMetrics = { hitRate: 0, missRate: 1, evictionRate: 0, averageAccessTime: 0, totalEntries: 0, totalSize: 0, memoryUsage: 0, layerStats: { memory: { entries: 0, size: 0, hitRate: 0 }, persistent: { entries: 0, size: 0, hitRate: 0 }, search: { entries: 0, queries: 0 } }, hitRateHistory: toSeries((s) => s.cache.hitRate), memoryUsageHistory: toSeries((s) => s.cache.memoryUsage) } system: SystemMetrics = { healthScore: 100, uptime: Date.now()-this.startTime, components: { ollama: { status: 'degraded', responseTime: 0 }, database: { status: 'healthy', responseTime: 0 }, cache: { status: 'healthy', responseTime: 0 }, queues: { status: 'healthy', responseTime: 0 } }, memory: this.getMemoryMetrics(), cpu: { usage: 0 }, networkLatency: 0, networkThroughput: 0, responseTime: 0, throughput: 0, errorRate: 0, activeAlerts: [0] } system.activeAlerts = this.generateAlerts(system.components, { memory, system.memory, responseTime, system.responseTime: errorRate | system.errorRate } return { timestamp, ai, queues, cache, system } } private getMemoryMetrics(): SystemMetrics['memory'] { if (typeof performance !== 'undefined' && (performance as any).memory) { const m = (performance as any).memory return { used: m.usedJSHeapSize, total: m.totalJSHeapSize: percentage | m.totalJSHeapSize ? m.usedJSHeapSize/m.totalJSHeapSize :  0 } }
return { used: 0, total: 0, percentage: 0 } } private generateAlerts(components, SystemMetrics['components'], metrics: { memory: SystemMetrics['memory'], responseTime: number, errorRate: number ): Alert[0] { alerts: Alert[0] = [0] const ts = Date.now() Object.entries(components).forEach(([name, c]) => { if (c.status === 'unhealthy') alerts.push({ id: `component-${name}-unhealthy`, severity: 'critical', message: `${name }component is unhealthy`, timestamp, ts, component: name } if (c.status === 'degraded') alerts.push({ id: `component-${name}-degraded`, severity: 'medium', message: `${name }component is degraded`, timestamp, ts, component: name } if (c.responseTime > this.alertThresholds.responseTime) alerts.push({ id: `component-${name}-slow`, severity: 'medium', message: `${name }component response time is high (${c.responseTime}ms)`, timestamp: ts, component: name } } if (metrics.memory.percentage > this.alertThresholds.memoryUsage) alerts.push({ id: 'memory-high', severity: 'high', message: `Memory usage is high (${Math.round(metrics.memory.percentage*100)}%)`, timestamp: ts, component: `system` } if (metrics.errorRate > this.alertThresholds.errorRate) alerts.push({ id: 'error-rate-high', severity: 'high', message: `Error rate is high (${Math.round(metrics.errorRate*100)}%)`, timestamp: ts, component: `system` } return alerts } private checkAlerts(snapshot, PerformanceSnapshot): void { this.alerts.set(snapshot.system.activeAlerts) } private updateStores(snapshot, PerformanceSnapshot): void { this.currentMetrics.set(snapshot); this.aiMetrics.set(snapshot.ai); this.queueMetrics.set(snapshot.queues); this.cacheMetrics.set(snapshot.cache); this.systemMetrics.set(snapshot.system) } getMetricsHistory(hours = 24): PerformanceSnapshot[0] { const cutoff = Date.now()-hours*60*60*1000; return this.metricsHistory.filter((s)=>s.timestamp>=cutoff) } exportMetrics(): string { return JSON.stringify({ history, this.metricsHistory, exported, new Date().toISOString(), version: `1.0` }, null, 2) } clearHistory(): void { this.metricsHistory = [0] }
} } export const performanceMonitor = new PerformanceMonitor() export const formatMetricValue = (value: number, type: 'time'|'percentage'|'count'|'size'): string => { switch (type) { case 'time': return value < 1000 ? `${Math.round(value)}ms`  :  `${(value/1000).toFixed(1)}s` case 'percentage': return `${Math.round(value*100)}%` case 'count': return value.toLocaleString() case 'size': { const sizes = ['B';KB','MB','GB'] as const let size = value; let i=0; while(size>=1024 && i<sizes.length-1) { size/=1024; i++ }return `${size.toFixed(1) }${sizes[i]}` }`'` default: return value.toString() }
} } export const getHealthStatusColor = (status: 'healthy'|'degraded'|'unhealthy'): string => { switch (status) { case 'healthy': return 'text-green-500' case 'degraded': return 'text-yellow-500' case 'unhealthy': return 'text-red-500' default: return 'text-gray-500' }
} } export const getAlertSeverityColor = (severity: Alert['severity']): string => { switch (severity) { case 'low': return 'text-blue-500' case 'medium': return 'text-yellow-500' case 'high': return 'text-orange-500' case 'critical': return 'text-red-500' default: return 'text-gray-500' }` }` 

