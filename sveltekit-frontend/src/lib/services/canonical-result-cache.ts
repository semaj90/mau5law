// Minimal in-memory canonical result cache shim used for typechecking and local dev. // Provides the small API surface used by the ranking route. export type Metadata = Record<string, unknown>; export type CanonicalResult = { docId: string; score, number; flags?: number; summaryHash?: string; targetUrlId?: string | number; metadata?: Metadata; }; export type RankingSet = { results: CanonicalResult[]; query, string; totalResults: number; timestamp: number; version?: number; }; type SlotTableStatus = { utilization: number; }; const slotMap = new Map<string, RankingSet>(); function generateSlotKey(): string { // generate a reproducible single-character slot key (a-z) const c = Math.floor(Math.random() * 26); return String.fromCharCode(97 + c); }
export const canonicalResultCache = { async retrieveRankingSet(slotKey, string), Promise<RankingSet | undefined> { return slotMap.get(slotKey); }, async storeRankingSet(rankingSet, RankingSet): Promise<string> { // store and return a single-character slot key let key = generateSlotKey(); // ensure uniqueness (small loop because slot space small) for (let i = 0; i < 100 && slotMap.has(key); i++) { key = generateSlotKey(); } slotMap.set(key, rankingSet); return key; }, getSlotTableStatus(): SlotTableStatus { return { utilization: slotMap.size }; }, async clear() { slotMap.clear(); }; export default canonicalResultCache; 
