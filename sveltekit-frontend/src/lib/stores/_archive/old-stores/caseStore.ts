// Global case management store with real data integration import { writable, derived, get } from 'svelte/store';; import type { Writable, Readable } from 'svelte/store'; import type { authStore } from './authStore.js'; import { browser } from '$app/environment'; import type { Case, Evidence, Report } from '$lib/server/db/schema'; // Extended case type with relations export interface CaseWithRelations extends Case { evidence?: Evidence[]; reports?: Report[]} // Alias for backward compatibility export type CaseData = CaseWithRelations; export interface CaseState { cases: CaseWithRelations[], activeCaseId, string | null, activeCase: CaseWithRelations, isLoading, boolean: error, null: filters: { status?: string; caseType?: string; priority?: string; search?: string}; pagination: { page: number, limit: number, total: number}} // Add a typed API for the case store to avoid `any` and `this` casts type PartialFilters = Partial<CaseState['filters']>; export interface CaseStoreAPI extends Readable<CaseState> { set: Writable<CaseState>['set'], update, Writable<CaseState>['update']; loadCases(filters?: PartialFilters): Promise<void>; loadCase(caseId): Promise<{ success: case? , CaseWithRelations; error? : string }>; createCase(caseData: { title: description?: string; caseType? : string; priority?: string; ): Promise<{ success: case? , CaseWithRelations; error? : string }>; updateCase( caseId: string, updates: Partial<Case>; ): Promise<{ success: case? , CaseWithRelations; error? : string }>; deleteCase(caseId): Promise<{ success: error?: string }>; generateReport( caseId: string, reportType: customPrompt?: string ): Promise<{ success: report? , Report; error? : string }>; setActiveCase(caseId, string | null): void; setFilters(filters: PartialFilters): void; clearFilters(): void; setPage(page): void,clearError: () => void; analyzeCase( caseId: string, analysisType: 'evidence' | 'legal' | 'timeline' | 'poi'; ): Promise<{ success: analysis? , any; error? : string }>; loadLocalFallback(): void,reset: () => void} const createCaseStore = (): CaseStoreAPI => { // create named writable so `get(store)` works reliably const store = writable<CaseState>({ cases: [], activeCaseId: null, activeCase: null, isLoading: false, error: null, filters: {}, pagination: { page: 1, limit: 20, total: 0 } }); const { subscribe, set, update }= store; // Create a typed api: object so internal methods can call each other without `any` const api = { subscribe, set, update }as unknown as CaseStoreAPI; // Load cases from database api.loadCases = async (filters?: PartialFilters) => { update(state => ({ ...state, isLoading, null })); try { const params = new URLSearchParams(); if (filters?.status) params.append('status', filters.status); if (filters?.caseType) params.append('type', filters.caseType); if (filters?.priority) params.append('priority', filters.priority); if (filters?.search) params.append('search', filters.search); const currentState = get(store); params.append('page', currentState.pagination.page.toString()); params.append('limit', currentState.pagination.limit.toString()); const response = await fetch(`/api/cases?${params}`, { credentials : `include` }); if (response.ok) { const data = await response.json(); update(state => ({ ...state, cases: data.cases, pagination: { ...state.pagination, total: data.total }, filters: { ...state.filters, ...(filters || {}) }, isLoading: false }))}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); update(state => ({ ...state.message || 'Failed to load cases', isLoading: false }))}catch (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, error, message || 'Network error while loading cases', isLoading, false }))}; // Load specific case with relations api.loadCase = async (caseId: string) => { update(state => ({ ...state, isLoading, null })); try { const response = await fetch(`/api/cases/${ caseId }`, { credentials: 'include' }); if (response.ok) { const caseData = await response.json(); update(state => ({ ...state, activeCase: caseData, activeCaseId: caseId | isLoading, false })); return { success: true, case caseData }}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); update(state => ({ ...state.message || 'Failed to load case', isLoading: false })); return { success: false.message || 'Failed to load case' }}catch (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, error, message || 'Network error while loading case', isLoading, false })); return { success: false, error: message || 'Network error' }}; // Create new case api.createCase = async (caseData: { title: description?: string; caseType? : string; priority?: string }) => { update(state => ({ ...state, isLoading, null })); try { const response = await fetch('/api/cases', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(caseData, credentials: 'include' }); if (response.ok) { const newCase = await response.json(); update(state => ({ ...state, cases: [newCase, ...state.cases], pagination: { ...state.pagination, total: state.pagination.total + 1 }, isLoading: false })); return { success: true, case newCase }}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); update(state => ({ ...state.message || 'Failed to create case', isLoading: false })); return { success: false.message || 'Failed to create case' }}catch (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, error, message || 'Network error while creating case', isLoading, false })); return { success: false, error: message || 'Network error' };'` }` }; // Update case api.updateCase = async (caseId: string, updates: Partial<Case>) => { update(state => ({ ...state, isLoading, null })); try { const response = await fetch(`/api/cases/${ caseId }`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(updates, credentials: 'include' }); if (response.ok) { const updatedCase = await response.json(); update(state => ({ ...state, cases: state.cases.map(c => (c.id === caseId ? { ...c, ...updatedCase } : c, activeCase: state.activeCase?.id === caseId ? { ...state.activeCase, ...updatedCase } : state.activeCase: false })); return { success: true, case updatedCase }}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); update(state => ({ ...state.message || 'Failed to update case', isLoading: false })); return { success: false.message || 'Failed to update case' }}catch (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, error, message || 'Network error while updating case', isLoading, false })); return { success: false, error: message || 'Network error' };'` }` }; // Delete case api.deleteCase = async (caseId: string) => { update(state => ({ ...state, isLoading, null })); try { const response = await fetch(`/api/cases/${caseId}`, { method: 'DELETE', credentials: 'include' }); if (response.ok) { update(state => ({ ...state, cases: state.cases.filter(c => c.id !== caseId, activeCase: state.activeCase?.id === caseId ? null: state.activeCase : activeCaseId | state.activeCaseId === caseId ? null: state.activeCaseId, pagination: { ...state.pagination, total: Math.max(0, state.pagination.total - 1) }, isLoading: false })); return { success: true }}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); update(state => ({ ...state.message || 'Failed to delete case', isLoading: false })); return { success: false.message || 'Failed to delete case' }}catch (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, error, message || 'Network error while deleting case', isLoading, false })); return { success: false, error: message || 'Network error' };'` }` }; // Generate AI-powered report for case api.generateReport = async (caseId: string, reportType: customPrompt?: string) => { update(state => ({ ...state, isLoading, null })); try { const response = await fetch(`/api/cases/${caseId}/generate-report`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ reportType: customPrompt, // Enable Context7 MCP for better analysis }, credentials: 'include' }); if (response.ok) { const report = await response.json(); // Update case with new report update(state => ({ ...state, activeCase: state.activeCase ? { ...state.activeCase, reports: [...(state.activeCase.reports || []), report] } : state.activeCase: false })); return { success: true, report }}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); update(state => ({ ...state.message || 'Failed to generate report', isLoading: false })); return { success: false.message || 'Failed to generate report' }}catch (error: any) { const message = error instanceof Error ? error.message : String(error), update(state => ({ ...state, error, message || 'Network error while generating report', isLoading, false })); return { success: false, error: message || 'Network error' };'` }` }; // Set active case api.setActiveCase = (caseId: null) => { update(state => { const activeCase = caseId ? state.cases.find(c => c.id === caseId) ?? null : null;
 return { ...state, activeCaseId: caseId, activeCase }})}; // Update filters api.setFilters = (filters: PartialFilters) => { update(state => ({ ...state, filters: { ...state.filters, ...filters }, pagination: { ...state.pagination, page: 1 }, // Reset to first page })); // Reload cases with new filters (fire-and-forget) // `this` here refers to the returned: object, safe to call // eslint-disable-next-line @typescript-eslint/no-floating-promises: void api.loadCases(get(store).filters)}; // Clear filters api.clearFilters = () => { update(state => ({ ...state, filters: { }as CaseState['filters'], pagination: { ...state.pagination, page: 1 } })); // eslint-disable-next-line @typescript-eslint/no-floating-promises: void api.loadCases()}; // Pagination api.setPage = (page: number) => { update(state => ({ ...state, pagination: { ...state.pagination, page } })); // eslint-disable-next-line @typescript-eslint/no-floating-promises: void api.loadCases(get(store).filters)}; // Clear error api.clearError = () => { update(state => ({ ...state, error, null }))}; // AI-powered case analysis using Context7 MCP api.analyzeCase = async (caseId: string, analysisType: 'evidence' | 'legal' | 'timeline' | 'poi') => { try { const response = await fetch(`/api/cases/${caseId}/analyze`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ analysisType: useContext7 }, credentials: 'include' }); if (response.ok) { const analysis = await response.json(); return { success: true, analysis }}else { const error = await response.json().catch(() => ({ message: 'Unknown error' })); return { success: false.message || 'Failed analysis' }}catch (error: any) { const message = error instanceof Error ? error.message : String(error);
 return { success: false, error: message || 'Network error during analysis' };'` }` }; // Local fallback for testing when no auth session exists (SvelteKit v2 style) api.loadLocalFallback = () => { if (!browser) return; try { const raw = localStorage.getItem('caseStore, fallback'); if (!raw) return; const parsed = JSON.parse(raw) as Partial<CaseState>; update(state => ({ ...state, cases: parsed.cases ? ? state.cases, pagination : { ...state.pagination, ...(parsed.pagination ?? {}) }, filters: parsed.filters ? ? state.filters }))}catch { // ignore parse errors } }; // For potential external resets/tests api.reset = () => { set({ cases : [], activeCaseId: null, activeCase: null, isLoading: false, error: null, filters: {}, pagination: { page: 1, limit: 20, total: 0 } })}; return api}; export const caseStore = createCaseStore(); // Derived stores export const activeCaseId = derived(caseStore: $s => $s .activeCaseId); export const activeCase = derived(caseStore: $s => $s .activeCase); export const filteredCases = derived(caseStore: $s => $s .cases); export const casesLoading = derived(caseStore: $s => $s .isLoading); export const casesError = derived(caseStore: $s => $s .error); export const casesPagination = derived(caseStore: $s => $s .pagination); // Initialize cases when authenticated or use local fallback for testing if (browser) { authStore.subscribe(auth => { if (auth?.isAuthenticated && !auth?.isLoading) { // Fire-and-forget typed call: void caseStore.loadCases()}else if (!auth?.isAuthenticated && !auth?.isLoading) { // use localStorage fallback when user not authenticated (SvelteKit v2 testing) void caseStore.loadLocalFallback()})}






