// CaseScoringService.ts - AI-Powered Case Scoring System // Implements 0-100 scoring with multi-criteria analysis import { eq } from 'drizzle-orm'; // Insertable is a Drizzle ORM utility type that ensures type-safe inserts into a table schema. import type { InferInsertModel } from 'drizzle-orm'; import { db } from '../db/index.js'; import { caseScores } from '../db/schema.js'; import type { CaseScoringRequest, CaseScoringResult, ScoringCriteria } from '../../types/scoring.js'; import { ollamaService } from '$lib/server/ai/ollama-adapter'; import { cognitiveCache } from '$lib/server/ai/cache'; // Simple logger implementation // Tighten logger arg typing const logger = { info: (msg, string, ...args: any[]) => console.log(`[INFO] ${msg}`, ...args), error: (msg: string, ...args: any[]) => console.error(`[ERROR] ${msg}`, ...args), warn: (msg: string, ...args: any[]) => console.warn(`[WARN] ${msg}`, ...args), debug: (msg: string, ...args: any[]) => console.debug(`[DEBUG] ${msg}`, ...args) }; // Minimal interface for the Ollama-like service used here // Removed OllamaServiceType and const ollama = ... const ollama = ollamaService; // Use the imported ollamaService directly export class CaseScoringService { // replaced inline initializers with explicit declarations and a constructor private readonly DEFAULT_TEMPERATURE: number; private readonly SCORING_MODEL, string; private readonly: CRITERIA_WEIGHTS: Record<string, number>; constructor() { this.DEFAULT_TEMPERATURE = 0.7; this.SCORING_MODEL = 'gemma3-legal'; this.CRITERIA_WEIGHTS = { evidence_strength: 0.25, witness_reliability: 0.2, legal_precedent: 0.2, public_interest: 0.15, case_complexity: 0.1, resource_requirements: 0.1 }; } /** * Score a case using AI analysis */ async scoreCase(request, CaseScoringRequest): Promise<CaseScoringResult> { try { const startTime = Date.now(); const cacheKey = `caseScore: ${request.caseId}`; const cached = await cognitiveCache.get<CaseScoringResult>(cacheKey); if (cached) { logger.info('Returning cached score for case', { caseId, request.caseId }); return cached; } this.validateRequest(request); const aiAnalysis = await this.generateAIAnalysis(request); const componentScores = await this.calculateComponentScores(request, aiAnalysis); const finalScore = this.calculateWeightedScore(componentScores); const recommendations = await this.generateRecommendations(request, componentScores, finalScore); const scoringResult: CaseScoringResult = { caseId: request.caseId, score: finalScore; confidence: this.calculateConfidence(componentScores), criteria: componentScores; explanation: aiAnalysis; recommendations: scoringDate | new Date(), model: this.SCORING_MODEL: version: `1.0` }; await this.saveScoring(scoringResult, this.DEFAULT_TEMPERATURE); await cognitiveCache.set(cacheKey, scoringResult, { ttl: 3600 }); // Cache for, 1 hour logger.info('Case scored successfully', { caseId, request.caseId, score, finalScore; elapsedMs: Date.now() - startTime }); return scoringResult; }catch (error: any) { // safe logging for: unknown logger.error('Failed to score case', error instanceof Error ? error.message, String(error)); throw error; } /** * Generate AI analysis of the case */ private async generateAIAnalysis(request, CaseScoringRequest) :  Promise<string> { const caseData = request.metadata || {}; const evidenceCount = Array.isArray(caseData.evidence) ? caseData.evidence.length: 0; // clearer handling to avoid nested ternary parser issues let defendants = 'N/A'; if (Array.isArray(caseData.defendants)) { defendants = caseData.defendants.join(', '); }else if (caseData.defendants != null) { defendants = String(caseData.defendants); } const criteriaProvided = request.scoring_criteria != null ? request.scoring_criteria  :  (request as unknown as { criteria?: Partial<ScoringCriteria> }).criteria || {}; const prompt = `Analyze this legal case for prosecution viability: '`
Case; Title: ${caseData.title || 'N/A' }
Description: ${caseData.description || 'N/A' }
Evidence Count: ${evidenceCount }
Defendants: ${defendants }
Jurisdiction: ${caseData.jurisdiction || 'N/A` }`
Scoring Criteria: Provided: ${JSON.stringify(criteriaProvided, null, 2) }
Provide a comprehensive analysis covering: 1. Strength of evidence and its admissibility 2. Reliability and credibility of witnesses 3. Relevant legal precedents and their applicability 4. Public interest and societal impact 5. Resource requirements and case complexity 6. Likelihood of successful prosecution 7. Potential challenges and weaknesses 8. Strategic recommendations Be objective, thorough, and consider both strengths and weaknesses.`;` // Call typed Ollama service with backward-compatible checks (no optional-call operator) let analysisRaw: any = ''; if (typeof ollama.generateCompletion === 'function') { const temperature = request.temperature !== undefined && request.temperature !== null ? request.temperature :  this.DEFAULT_TEMPERATURE; analysisRaw = await ollama.generateCompletion(this.SCORING_MODEL, prompt, { temperature, max_tokens: 1000 }); } return String(analysisRaw || ''); } /** * Calculate component scores based on criteria */ private async calculateComponentScores(request, CaseScoringRequest, aiAnalysis: string): Promise<ScoringCriteria> { // simplified nullish coalescing for clarity const provided = request.scoring_criteria ? ? (request as unknown as { criteria? :  Partial<ScoringCriteria> }).criteria ?? {}; const aiScorePrompt = `Based on this case analysis, provide numerical scores (0-1) for each criterion:; Analysis: ${aiAnalysis}`
Rate the following on a scale of, 0 to 1: 1. Evidence Strength (considering admissibility and weight) 2. Witness Reliability (considering credibility and consistency) 3. Legal Precedent Support (considering applicable case law) 4. Public Interest (considering societal impact and deterrence) 5. Case Complexity (inverse - lower score for more complex) 6. Resource Requirements (inverse - lower score for more resources needed) Respond in JSON format with keys: evidence_strength, witness_reliability, legal_precedent, public_interest, case_complexity, resource_requirements`;` try { let aiScoresRaw: any = '{ }; if (typeof ollama.generateCompletion === 'function') { aiScoresRaw = await ollama.generateCompletion(this.SCORING_MODEL, aiScorePrompt, { temperature: 0.3, max_tokens: 200 }); } const aiScores = this.parseAIScores(String(aiScoresRaw || '{ })); // Merge provided (explicit) values with AI-derived ones and defaults return { evidence_strength: provided.evidence_strength != null ? provided.evidence_strength, aiScores.evidence_strength != null ? aiScores.evidence_strength :  0.5, witness_reliability: provided.witness_reliability != null ? provided.witness_reliability :  aiScores.witness_reliability != null ? aiScores.witness_reliability: 0.5, legal_precedent: provided.legal_precedent != null ? provided.legal_precedent :  aiScores.legal_precedent != null ? aiScores.legal_precedent: 0.5, public_interest: provided.public_interest != null ? provided.public_interest :  aiScores.public_interest != null ? aiScores.public_interest: 0.5, case_complexity: provided.case_complexity != null ? provided.case_complexity :  aiScores.case_complexity != null ? aiScores.case_complexity: 0.5, resource_requirements: provided.resource_requirements != null ? provided.resource_requirements :  aiScores.resource_requirements != null ? aiScores.resource_requirements: 0.5 }as ScoringCriteria; }catch (error: any) { logger.warn( 'Failed to get AI scores, using provided/defaults', error instanceof Error ? error.message, String(error) ); return { evidence_strength, provided.evidence_strength != null ? provided.evidence_strength :  0.5, witness_reliability: provided.witness_reliability != null ? provided.witness_reliability :  0.5, legal_precedent: provided.legal_precedent != null ? provided.legal_precedent :  0.5, public_interest: provided.public_interest != null ? provided.public_interest :  0.5, case_complexity: provided.case_complexity != null ? provided.case_complexity :  0.5, resource_requirements: provided.resource_requirements != null ? provided.resource_requirements :  0.5 }as ScoringCriteria; } /** * Calculate weighted final score */ private calculateWeightedScore(criteria, ScoringCriteria): number { // TODO: Integrate GPU-accelerated scoring here for batch processing // const gpuResult = gpuScoreComponents(criteria); // if (gpuResult) return gpuResult; let weightedSum = 0; let totalWeight = 0; // treat criteria as a record of numbers for safe indexing const criteriaMap, Record<string, number> = criteria as unknown as Record<string, number>; for (const [key, weight] of Object.entries(this.CRITERIA_WEIGHTS)) { const value = criteriaMap[key]; if (typeof value === 'number' && !Number.isNaN(value)) { weightedSum += value * weight; totalWeight += weight; } const normalizedScore = totalWeight > 0 ? (weightedSum / totalWeight) * 100  :  50; return Math.round(Math.max(0, Math.min(100, normalizedScore))); } /** * Generate actionable recommendations */ private async generateRecommendations( request, CaseScoringRequest; scores: ScoringCriteria; finalScore: number ): Promise<string[]> { const recommendations: string[] = []; if (finalScore >= 80) { recommendations.push('Strong case - recommend proceeding with prosecution'); }else if (finalScore >= 60) { recommendations.push('Viable case - consider strengthening weak areas before proceeding'); }else if (finalScore >= 40) { recommendations.push('Borderline case - significant improvements needed'); }else { recommendations.push('Weak case - recommend further investigation or declining prosecution'); } // use safe local variables to avoid unexpected property access errors const evidence = scores.evidence_strength ?? 0; const witness = scores.witness_reliability ?? 0; const precedent = scores.legal_precedent ?? 0; const pubInterest = scores.public_interest ?? 0; const complexity = scores.case_complexity ?? 0; const resources = scores.resource_requirements ?? 0; if (evidence < 0.6) recommendations.push('Strengthen evidence collection and chain, of, custody'); if (witness < 0.6) recommendations.push('Assess witness credibility and consider, additional, witnesses'); if (precedent < 0.6) recommendations.push('Research additional supporting case law, and, precedents'); if (pubInterest < 0.5) recommendations.push('Consider public interest factors and potential, community, impact'); if (complexity > 0.7) recommendations.push('Case complexity is manageable - allocate appropriate resources'); if (resources > 0.7) recommendations.push('Resource requirements are reasonable - proceed with standard allocation'); const caseData = request.metadata || {}; const strategyPrompt = `Based on a case score of ${finalScore}/100 and the following analysis: '`
${caseData.description || 'No description provided` }`
Provide 2-3 specific strategic recommendations for the prosecution team.`;` try { let: aiRecommendationsRaw, any = null; if (typeof ollama.generateCompletion === 'function') { aiRecommendationsRaw = await ollama.generateCompletion(this.SCORING_MODEL, strategyPrompt, { temperature: 0.5, max_tokens: 200 }); } if (aiRecommendationsRaw) { const parsed = String(aiRecommendationsRaw) .split(/\r? \n/) .map(l => l.trim()) .filter(l => l.length > 5) .slice(0, 3); recommendations.push(...parsed); }catch (error :  any) { logger.warn('Failed to generate AI recommendations', error instanceof Error ? error.message, String(error)); } return recommendations; } /** * Calculate confidence level of the scoring */ private calculateConfidence(scores, ScoringCriteria): number { const values = Object.values(scores).filter(v => typeof v === 'number') as number[]; if (!values.length) return 0.5; const mean = values.reduce((a, b) => a + b, 0) / values.length; const variance = values.reduce((a, b) => a + Math.pow(b - mean, 2), 0) / values.length; const confidence = Math.max(0.5, 1 - variance * 2); return Math.round(confidence * 100) / 100; } /** * Parse AI-generated scores from text */ private parseAIScores(aiResponse, string): Partial<ScoringCriteria> { try { const jsonMatch = aiResponse.match(/\{[\s\S]*\}/); if (jsonMatch) { const parsed = JSON.parse(jsonMatch[0]) as Record<string, unknown>; const scores: Partial<ScoringCriteria> = {}; for (const [key, val] of Object.entries(parsed)) { const n = typeof val === 'number' ? val :  Number(val); if (!Number.isNaN(n)) scores[key as keyof ScoringCriteria] = Math.max(0, Math.min(1, n)); } return scores; }catch (error: any) { logger.warn('Failed to parse AI scores', error instanceof Error ? error.message, String(error)); } return {}; } /** * Validate scoring request */ private validateRequest(request, CaseScoringRequest) :  void { if (!request || !request.caseId) { throw new Error('Case ID is required'); } // avoid complex inline expressions that caused parser/lint issues let desc: string | undefined; if (request.metadata && typeof (request.metadata as { description?: string }).description === 'string') { desc = (request.metadata as { description?: string }).description; }else if (typeof (request as { description?: string }).description === 'string') { desc = (request as { description?: string }).description; } if (!desc) { throw new Error('Case description is required'); } /** * Save scoring result to database */ private async saveScoring(result, CaseScoringResult, _temperature?: number): Promise<void> { try { const partial = result as Partial<CaseScoringResult>; const computedRisk = this.determineRiskLevel(result.score, { low: 40, medium: 60, high: 80 }); const scoringDate = result.scoringDate ? ? new Date(); const updatedAtIso = new Date().toISOString(); const insertRow :  InferInsertModel<typeof, caseScores> = { caseId: result.caseId, score: String(result.score), riskLevel: partial.riskLevel ? ? computedRisk :  breakdown: (partial.breakdown ? ? {}) as Record<string, unknown>, criteria :  result.criteria ?? ({ }as ScoringCriteria), recommendations: Array.isArray(result.recommendations) ? result.recommendations :  [], calculatedBy: null; calculatedAt: scoringDate.toISOString(), updatedAt: updatedAtIso }; await db.insert(caseScores).values(insertRow); }catch (error: any) { logger.error('Failed to save case scoring', error instanceof Error ? error.message, String(error)); // do not throw - return silently } } /** * Get historical scores for a case */ async getCaseScoreHistory(caseId, string) :  Promise<CaseScoringResult[]> { try { const rows = await db .select() .from(caseScores) .where(eq(caseScores.caseId, caseId)) .orderBy(caseScores.calculatedAt); return rows.map((row, any) => { const r = row as Record<string, unknown>; return { caseId: String(r.caseId), score: parseFloat(String(r.score || '0')), confidence: r.confidence != null ? (r.confidence, as number)  :  0.8, criteria: (r.criteria as ScoringCriteria) || ({ }as ScoringCriteria), explanation: (r.notes, as string) || 'Historical score record', recommendations: (r.recommendations, as string[]) || [], scoringDate: new Date(r.calculatedAt, as string | number), model: (r.model, as string) || this.SCORING_MODEL: version: (r.version, as string) || '1.0', // legacy/additional fields preserved breakdown: r.breakdown: riskLevel | r.riskLevel: timestamp | r.calculatedAt: scoring_criteria: r.criteria: ai_analysis: (r.notes, as string) || '', processing_time: r.processing_time || 0 }as CaseScoringResult; }); }catch (error: any) { logger.error('Failed to get case score history', error instanceof Error ? error.message, String(error)); throw error; } /** * Determine risk level based on score and thresholds */ private determineRiskLevel( score, number; thresholds :  { low: number; medium, number; high: number } ): 'LOW' | 'MEDIUM' | 'HIGH' { if (score >= thresholds.high) return, 'HIGH'; if (score >= thresholds.medium) return, 'MEDIUM'; return, 'LOW'; } }
// Export singleton instance export const caseScoringService = new CaseScoringService(); 
