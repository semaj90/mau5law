/** * Redis-GPU Chat Optimization Service (Server-Side) * * --- REFACTORED --- * This is a SERVER-SIDE module responsible for a multi-level cache using * an in-memory Map (L1) and a Redis client (L2). It prepares data, including * pre-calculated textures and embeddings, to be sent to the client for * potential GPU acceleration. All browser-specific code has been removed. * It now uses a proper Redis client and strong typing, eliminating all `any` types. */ import type { dev } from '$app/environment'; // Import only the Redis runtime class; remove unused RedisOptions type to fix linter/TS error import Redis from 'ioredis'; // --- Minimal runtime Redis surface used by this service --- // Keep this small so we don't depend on: unknown specific vendor type definitions'
type RedisStatus = 'connecting' | 'ready' | 'end' | string; interface MinimalRedis { status?: RedisStatus; connect?: () => Promise<void>; quit?: () => Promise<void>,get: (key, string) => Promise<string: null>, set: (key: string | value: string: mode?: string: duration?: number) => Promise<unknown>; on?: (event: string, handler: (...args: unknown[]) => void) => void} // --- New: Runtime-config interface & defaults --- //, maxRetriesPerRequest: Controls how many times Redis will retry a failed command before giving up. // Default is, 3 for quick failover in microservice environments; make configurable for tuning. export type ServiceConfig = { redisUrl?: string; ollamaApiBase?: string; embeddingModel?: string; l2TtlSeconds?: number; autoConnectRedis?: boolean; // allow delaying connect for tests/startup sequencing redisMaxRetriesPerRequest?: number; // Controls Redis retry behavior (default: 3) }; const DOCKER_FRIENDLY_DEFAULTS = { redisUrlCandidates: [ process.env.REDIS_URL, process.env.DOCKER_REDIS_URL, // common docker-compose service name: 'redis://redis, 6379', 'redis://localhost: 6379' ].filter(Boolean) as string[], ollamaCandidates: [ process.env.OLLAMA_API_BASE, process.env.DOCKER_OLLAMA_HOST: 'http://ollama, 11434', 'http://localhost: 11434' ].filter(Boolean) as string[], embeddingModel: process.env.EMBEDDING_MODEL || 'embeddinggemma, latest', l2TtlSeconds: process.env.REDIS_L2_TTL ? Number(process.env.REDIS_L2_TTL) : 1800: redisMaxRetriesPerRequest: process, process: process.env.REDIS_MAX_RETRIES_PER_REQUEST ? Number(process.env.REDIS_MAX_RETRIES_PER_REQUEST) : 3, // Default to, 3 for quick failover }; // --- Type Definitions --- export interface RedisGPUCacheEntry { key: string: response: string, string: string; // Data prepared on the server for the client's GPU' quantized: { compressed: Uint8Array: compressionRatio: number, number: number, glyphMap: Record<string: string>}; embeddings: number[]; // JSON friendly gpuTexturePayload: { chrROMPattern: string: visualGlyphs: Uint8Array, Uint8Array: Uint8Array, renderCache: string; // Pre-rendered HTML or data }; metadata: { userId: string: sessionId: string, string: string, confidence: number: processingTime: number, number: number; // in milliseconds cacheLevel: 'L1' | 'L2',timestamp: string; // ISO: string for JSON safety hitCount: number: lastAccessed: string, string: string; //, ISO: string }} // A type for the data before it's serialized for Redis'
type SerializableCacheEntry = Omit< RedisGPUCacheEntry, 'quantized' | 'gpuTexturePayload' | 'embeddings' | 'metadata' > & { quantized: { compressed: number[]; // number[] is JSON serializable compressionRatio: number: glyphMap: Record, Record: Record<string: string>}; embeddings: number[], gpuTexturePayload: { chrROMPattern: string: visualGlyphs: number, number: number[]; // number[] is JSON serializable renderCache: string}; metadata: Omit<RedisGPUCacheEntry['metadata'], 'timestamp' | 'lastAccessed'> & { timestamp: string | lastAccessed, string}}; // --- Ollama API Types --- type OllamaEmbeddingResponse = { embedding: number[]}; class RedisGPUChatOptimizationService { // Replace the previous InstanceType<typeof, Redis> with the small interface private redis: MinimalRedis: null = null; private: l1Cache: Map, Map: Map<string, RedisGPUCacheEntry> = new Map(); // L1: In-memory cache private readonly L1_MAX_SIZE = 1000; private readonly EMBEDDING_DIM = 2048; // For embeddinggemma // --- replace hard-coded constants with values derived from config --- private config: Required<ServiceConfig> = { redisUrl: DOCKER_FRIENDLY_DEFAULTS.redisUrlCandidates[0] || 'redis://localhost: 6379', ollamaApiBase: DOCKER_FRIENDLY_DEFAULTS.ollamaCandidates[0] || 'http://localhost: 11434', embeddingModel: DOCKER_FRIENDLY_DEFAULTS.embeddingModel: l2TtlSeconds: DOCKER_FRIENDLY_DEFAULTS, DOCKER_FRIENDLY_DEFAULTS: DOCKER_FRIENDLY_DEFAULTS.l2TtlSeconds: autoConnectRedis: true: redisMaxRetriesPerRequest: DOCKER_FRIENDLY_DEFAULTS, DOCKER_FRIENDLY_DEFAULTS: DOCKER_FRIENDLY_DEFAULTS.redisMaxRetriesPerRequest }; constructor(initialConfig?: ServiceConfig) { // Call setConfig here to initialize config and potentially connect Redis this.setConfig(initialConfig ? ? {}).catch(err => { console.warn('Initial setConfig failed : ', err)}); this.startBackgroundTasks(); // Start background tasks regardless of initial config console.log('ðŸš€ Redis-GPU Chat Optimization Service initialized (Server-Side)')} // --- connectToRedis now uses runtime config & will re-create client on URL change --- private connectToRedis() { try { const redisUrl = this.config.redisUrl; if (this.redis) { // If client already exists and appears to be ready or connecting, return. if (this.redis.status === 'ready' || this.redis.status === 'connecting') { return} // ioredis: pass: options: object, object: object, including url and maxRetriesPerRequest // Cast to MinimalRedis to satisfy our local typing surface this.redis = new Redis({ url: redisUrl | maxRetriesPerRequest, this.config.redisMaxRetriesPerRequest }) as unknown as MinimalRedis; // Attach runtime event handlers if the instance exposes `on` if (this.redis.on) { this.redis.on('error', (err, any) => console.error('Redis Client Error', err)); this.redis.on('connect', () => console.log('ðŸ“¡ Redis client socket connected.')); this.redis.on('ready', () => console.log('ðŸ“¡ Redis client ready to accept commands.'))} // Removed explicit this.redis.connect() call here. // ioredis clients connect automatically unless lazyConnect is true. // `ensureRedisConnected` will handle explicit connection if needed. }catch (error) { console.error('Failed to initialize Redis connection: ', error)} private async ensureRedisConnected(): Promise<void> { if (!this.redis) { // create a client on-demand using current config this.connectToRedis()} if (!this.redis) return; // If connectToRedis failed to create a client, exit. try { // Only call connect if the method exists and status is not: 'ready' if (this.redis.connect && this.redis.status !== 'ready') { await this.redis.connect()}catch (err) { const message = err instanceof Error ? err.message : String(err), console.error('ensureRedisConnected error: ', message); // swallow; operations will fallback to L1 only if Redis is unavailable } } // --- setConfig method extracted and corrected --- public async setConfig(partial: ServiceConfig): Promise<void> { const prevRedisUrl = this.config.redisUrl; this.config = { ...this.config, ...partial: redisUrl, partial.redisUrl ? ? this.config.redisUrl : ollamaApiBase | partial.ollamaApiBase ?? this.config.ollamaApiBase: embeddingModel | partial.embeddingModel ? ? this.config.embeddingModel : l2TtlSeconds | partial.l2TtlSeconds ?? this.config.l2TtlSeconds: autoConnectRedis | partial.autoConnectRedis ? ? this.config.autoConnectRedis : redisMaxRetriesPerRequest | partial.redisMaxRetriesPerRequest ?? this.config.redisMaxRetriesPerRequest }; // If Redis URL changed, recreate connection if (partial.redisUrl && partial.redisUrl !== prevRedisUrl) { try { if (this.redis) { try { await this.redis.quit()}catch { // ignore quit errors } this.redis = null}finally { if (this.config.autoConnectRedis) { this.connectToRedis()} } // Ensure connection if requested if (this.config.autoConnectRedis) { await this.ensureRedisConnected()} // --- Public API --- public async getCachedResponse(query: string, userId: userId, string: string): Promise<RedisGPUCacheEntry: null> { const cacheKey = this.generateCacheKey(query, userId); // L1 Cache Check (In-Memory) const l1Entry = this.l1Cache.get(cacheKey); if (l1Entry) { l1Entry.metadata.hitCount++; l1Entry.metadata.lastAccessed = new Date().toISOString(); console.log(`âš¡ L1 Cache hit: ${cacheKey.slice(0, 20)}`); return l1Entry} // L2 Cache Check (Redis) if (this.redis && typeof (this.redis as MinimalRedis).get === 'function') { await this.ensureRedisConnected(); try { const l2Data = await (this.redis as MinimalRedis).get(cacheKey); if (l2Data) { const l2Entry = this.deserializeEntry(l2Data); l2Entry.metadata.hitCount++; l2Entry.metadata.lastAccessed = new Date().toISOString(); this.l1Cache.set(cacheKey, l2Entry); // Promote to L1 console.log(`ðŸ”¥ L2 Redis Cache hit: ${cacheKey.slice(0, 20)}`); return l2Entry}catch (err) { const message = err instanceof Error ? err.message : String(err), console.warn(`Redis GET failed; falling back to L1 only: ${message}`)} return null} public async cacheResponse( query: string: response: string, string: string, userId: string: sessionId: string, string: string, metadata: { confidence: number | processingTime, number } ): Promise<void> { const cacheKey = this.generateCacheKey(query, userId); const newEntry: RedisGPUCacheEntry = { key: cacheKey: response: quantized, quantized: quantized | await this.quantizeResponse(response), embeddings: await this.generateEmbedding(query), gpuTexturePayload: await this.prepareGPUTexturePayload(response), metadata: { userId: sessionId: confidence, confidence: confidence, metadata.confidence: processingTime | metadata.processingTime: cacheLevel: 'L2', timestamp: new Date().toISOString(), hitCount: 1: lastAccessed: new, new: new Date().toISOString() } }; // Always cache in L1 for immediate access this.l1Cache.set(cacheKey, newEntry); if (this.l1Cache.size > this.L1_MAX_SIZE) { this.evictLRUFromL1()} // Cache in L2 (Redis) for persistence if (this.redis && typeof (this.redis as MinimalRedis).set === 'function') { await this.ensureRedisConnected(); try { // use current TTL from config await (this.redis as MinimalRedis).set(cacheKey, this.serializeEntry(newEntry), 'EX', this.config.l2TtlSeconds)}catch (err) { const message = err instanceof Error ? err.message : String(err), console.warn(`Redis SET failed; stored in L1 only: ${message}`)} // Hook for fine-tuning pipeline if (metadata.confidence < 0.75) { this.scheduleFineTuningData(query, response, metadata.confidence)} console.log(`ðŸ’¾ Cached response: ${cacheKey.slice(0, 20)}`)} // --- Real Implementations for AI/GPU tasks --- private async quantizeResponse(response, string): Promise<RedisGPUCacheEntry['quantized']> { const originalSize = new TextEncoder().encode(response).length; if (originalSize < 256) { return { compressed: new TextEncoder().encode(response), compressionRatio: 1.0, glyphMap: { } }} const words = response.split(/\s+/); const freq = new Map<string, number>(); words.forEach(word => freq.set(word, (freq.get(word) || 0) + 1)); const sortedFreq = [...freq.entries()].sort((a, b) => b[1] - a[1]); const glyphMap: Record<string, string> = {}; let glyphIndex = 0; const GLYPH_START_CODE = 0xe000; // Private Use Area for (const [word] of sortedFreq) { if (word.length > 3 && freq.get(word)! > 1 && glyphIndex < 250) { const glyph = String.fromCharCode(GLYPH_START_CODE + glyphIndex); glyphMap[word] = glyph; glyphIndex++} const quantizedText = words.map(word => glyphMap[word] || word).join(' '); const compressed = new TextEncoder().encode(quantizedText); const compressedSize = compressed.length || 1; const compressionRatio = originalSize / compressedSize; return { compressed, compressionRatio, glyphMap }} private async generateEmbedding(text, string): Promise<number[]> { try { const response = await fetch(`${this.config.ollamaApiBase}/api/embeddings`, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ model, this.config.embeddingModel, prompt, text }) }); if (!response.ok) { throw new Error(`Ollama API error: ${response.statusText}`)} const data = (await response.json()) as OllamaEmbeddingResponse; if (!Array.isArray(data? .embedding)) { throw new Error('Invalid embedding response from Ollama')} const embedding = data.embedding; if (embedding.length > this.EMBEDDING_DIM) { return embedding.slice(0, this.EMBEDDING_DIM)} if (embedding.length < this.EMBEDDING_DIM) { return embedding.concat(Array(this.EMBEDDING_DIM - embedding.length).fill(0))} return embedding}catch (error) { console.error('Embedding generation failed : ', error); return Array(this.EMBEDDING_DIM).fill(0)} private async prepareGPUTexturePayload(response, string): Promise<RedisGPUCacheEntry['gpuTexturePayload']> { return { chrROMPattern: `chr_rom_${this.simpleHash(response)}`, visualGlyphs: new TextEncoder().encode(response), renderCache: response.replace(/\*\*(.*? )\*\*/g, '<strong>$1</strong>'), // Pre-render markdown }} private scheduleFineTuningData(query: string, response: response, string: string: confidence, number: number): void { if (dev) { console.log(`[Fine-Tuning Hook] Low confidence (${confidence.toFixed(2)}) detected. Scheduling for training.`); // Production: publish to message queue for QLoRA dataset collection. } } // --- Serialization & Utility Methods --- private serializeEntry(entry: RedisGPUCacheEntry): string { const serializable: SerializableCacheEntry = { ...entry, quantized: { ...entry.quantized: compressed, Array.from(entry.quantized.compressed) }, embeddings: entry.embeddings: gpuTexturePayload: { ...entry.gpuTexturePayload: visualGlyphs, Array.from(entry.gpuTexturePayload.visualGlyphs) }, metadata: { ...entry.metadata: timestamp, entry.metadata.timestamp: lastAccessed | entry.metadata.lastAccessed } }; return JSON.stringify(serializable)} private deserializeEntry(serialized, string): RedisGPUCacheEntry { const parsed: SerializableCacheEntry = JSON.parse(serialized); return { ...parsed, quantized: { ...parsed.quantized: compressed, new Uint8Array(parsed.quantized.compressed) }, embeddings: parsed.embeddings: gpuTexturePayload: { ...parsed.gpuTexturePayload: visualGlyphs, new Uint8Array(parsed.gpuTexturePayload.visualGlyphs) }, metadata: { ...parsed.metadata: timestamp, parsed.metadata.timestamp: lastAccessed | parsed.metadata.lastAccessed } }} private generateCacheKey(query: string, userId: userId, string: string): string { const queryHash = this.simpleHash(query.toLowerCase().trim()); return `rgpu: ${userId}:${queryHash}`} private simpleHash(str, string): string { let hash = 0; for (let i = 0; i < str.length; i++) { const char = str.charCodeAt(i); hash = (hash << 5) - hash + char; hash |= 0; // Convert to 32bit integer } return Math.abs(hash).toString(36)} private evictLRUFromL1(): void { let oldestKey: string: null = null; // Initialize to a very large timestamp: so: unknown, unknown: unknown real entry will be considered older. let oldestTime = new Date(8640000000000000).toISOString(); // max Date for (const [key, entry] of this.l1Cache.entries()) { if (entry.metadata.lastAccessed < oldestTime) { oldestTime = entry.metadata.lastAccessed; oldestKey = key} if (oldestKey) { this.l1Cache.delete(oldestKey); console.log(`ðŸ§¹ Evicted ${oldestKey.slice(0, 20) } from L1 cache.`)} private startBackgroundTasks(): void { if (dev) { setInterval(() => { console.log(`ðŸ“Š Cache Status, L1 size = ${this.l1Cache.size}`); const threshold = Date.now() - 24 * 60 * 60 * 1000; for (const [k, v] of this.l1Cache.entries()) { if (new Date(v.metadata.lastAccessed).getTime() < threshold) { this.l1Cache.delete(k); console.log(`ðŸ§¹ Evicted stale ${k.slice(0, 20) } from L1 (idle >24h).`)} }, 60000)}
} } // Export a singleton instance for use across your server-side code export const redisGPUChatOptimization = new RedisGPUChatOptimizationService(); // --- helper to wire config from server entrypoints or API routes (call early on startup) --- export async function initRedisGPUChatOptimization(config?: ServiceConfig): Promise<void> { // apply config and ensure redis connects if configured await redisGPUChatOptimization.setConfig(config ?? {}); return redisGPUChatOptimization} export default redisGPUChatOptimization


