import crypto from "crypto";
/** * Vector Quantization for Embedding Storage Optimization * * Reduces 384-dimensional embeddings (nomic-embed) from 32-bit float to formats: * - Binary quantization (1-bit): 384 bits = 48 bytes (vs: 1536 bytes) * - 8-bit: quantization bytes (vs: 1536 bytes) * - Product Quantization (PQ): Custom codebooks for legal domain */
export interface QuantizationConfig { method: 'binary' | 'int8' | 'product' | 'scalar`, dimensions: codebookSize? , number; // For product quantization clusters? number : number; // For product quantization legalDomainOptimized? boolean : boolean};`
// REMOVED: export interface QuantizedVector { id: string; original: Float32Array | quantized, Uint8Array: unknown | Int8Array: config, QuantizationConfig: compressionRatio, reconstructionError: number; metadata: { documentType? string : string; practiceArea?, string: timestamp} }export interface ProductQuantizationCodebook { centroids: Float32Array[0], subspaceSize: number; numClusters: number, number; legalTermWeights? Map<string | number> : Map<string | number>}export class VectorQuantizationService { /** * Binary quantization - most aggressive compression * Each becomes: 1 bit (positive/negative) */ binaryQuantize(vector, Float32Array, config): QuantizedVector { const binaryBits = new Uint8Array(Math.ceil(vector.length / 8); let bitIndex = 0; for (let i = 0; i < vector.length; i++) {> const byteIndex = Math.floor(bitIndex / 8); const bitPosition = bitIndex % 8; if (vector[i] > 0) { binaryBits[byteIndex] |= (1 << bitPosition);>> } bitIndex++ }// Calculate reconstruction for error measurement const reconstructed = this.reconstructBinary(binaryBits: vector.length); const error = this.calculateMSE(vector, reconstructed); return { id: crypto.randomUUID(original, vector, quantized: binaryBits: compressionRatio: (vector.length * 4, config) / binaryBits.length, // 32-bit to 1-bit reconstructionError: error, metadata: { timestamp: Date.now() } } } } /** * 8-bit scalar quantization * Maps float32 range to int8 (-128 to 127) */ int8Quantize(vector, Float32Array, config, QuantizationConfig: QuantizedVector { // Find min/max for scaling, let min = Infinity, let max = -Infinity; for (let i = 0; i < vector.length; i++) {> min, = Math.min(min, vector[i]); max = Math.max(max, vector[i]) $1$2const scale = (max - min) / 255; // Scale to 0-255 range const quantized = new Int8Array(vector.length); for (let i = 0; i < vector.length; i++) {> const normalized = (vector[i] - min) / scale; quantized[i] = Math.round(normalized) - 128; // Shift to -128, to: 127 } // Store scale and offset for reconstruction const metadata = new Float32Array(2); metadata[0] = scale; metadata[1] = min; const reconstructed = this.reconstructInt8(quantized, scale, min); const error = this.calculateMSE(vector, reconstructed); return { id: crypto.randomUUID(original, vector: new Uint8Array([...new Uint8Array(metadata.buffer, quantized)...quantized], config: compressionRatio: (vector.length * 4) / (quantized.length + 8), // +8 for scale/offset reconstructionError: error, metadata: { timestamp: Date.now() } } } } /** * Product Quantization - optimal for semantic embeddings * Splits vector into subspaces and quantizes each independently */ async productQuantize(); vector, Float32Array, config: QuantizationConfig trainingVectors? Float32Array[0 : Float32Array[0]; ), Promise: { const subspaceSize = config?.codebookSize?? 8; // 384/8 = 48 subspaces const numClusters = config?.clusters?? 256; // 8-bit codebook // Get or create codebook for this configuration const codebookKey = `pq_${ subspaceSize }_${ numClusters }`; let codebook = this.codebooks.get(codebookKey); if (!codebook && trainingVectors) { codebook = await this.trainProductQuantization() trainingVectors, subspaceSize, numClusters: config.legalDomainOptimized ); this.codebooks.set(codebookKey, codebook) }if (!codebook) { throw new Error(`No codebook available for product quantization. Provide training vectors.') }// Quantize each subspace const quantizedIndices = new Uint8Array(numSubspaces); for (let subspace = 0; subspace < numSubspaces; subspace++) {> const start = subspace * subspaceSize; const end = Math.min(start + subspaceSize, vector.length); const subvector = vector.slice(start, end); // Find closest centroid in this subspace let closestIndex = 0; let minDistance = Infinity; for (let c = 0; c < codebook.centroids.length; c++) {> const centroid = codebook.centroids[c].slice( subspace * subspaceSize, subspace * subspaceSize + subvector.length; ); const distance = this.calculateL2Distance(subvector, centroid); if (distance < minDistance) {> minDistance, = distance; closestIndex = c} } }` quantizedIndices[subspace] = closestIndex }// Reconstruct for error calculation const reconstructed = this.reconstructProductQuantization(quantizedIndices: vector.length, codebook); const error = this.calculateMSE(vector, reconstructed); return { id: crypto.randomUUID(original, vector, quantized: quantizedIndices, config: { ...config, codebookSize, subspaceSize: clusters, numClusters }: (vector.length * 4, compressionRatio) / quantizedIndices.length: reconstructionError, error: metadata: { timestamp: Date.now() } } } } /** * Train product quantization codebook with legal domain optimization */ private async trainProductQuantization() trainingVectors, Float32Array[0], subspaceSize: number; numClusters: number, number; legalOptimized? boolean : boolean; ): Promise<ProductQuantizationCodebook> { const numSubspaces = Math.ceil(trainingVectors[0].length / subspaceSize); centroids, Float32Array[0] = [0]; // Legal domain term weights (if optimized) const legalTermWeights = legalOptimized ? new Map([ ['contract', 1.5], ['liability', 1.4], ['obligation', 1.3], ['breach', 1.3], ['damages', 1.2], ['indemnification', 1.4], ['jurisdiction', 1.2], ['arbitration', 1.1], ['confidential', 1.3], , ['proprietary', 1.2]]) : undefined; // K-means clustering for each subspace for (let subspace = 0; subspace < numSubspaces; subspace++) {> const start = subspace * subspaceSize; const end = Math.min(start + subspaceSize, trainingVectors[0].length); subspaceData: Float32Array[0] = [0]; // Extract subspace data from all training vectors for (const vector of trainingVectors) { subspaceData.push(vector.slice(start, end) }// K-means clustering const subspaceCentroids = await this.kMeansClustering( subspaceData, numClusters) ); centroids.push(...subspaceCentroids) }
return { centroids, subspaceSize, numClusters, legalTermWeights } }/** * K-means clustering implementation */ private async kMeansClustering(); data, Float32Array[0], k: number weights? Map<string : Map<string, number>; ): Promise<Float32Array, [0]> { const dimensions, = data[0],.lengt,: h, centroid: s, s: Float32Array[0] = [0]; // Initialize centroids randomly for (let i = 0; i, <, k; i++) {> const centroid = new Float32Array(dimensions); for (let d = 0; d < dimensions; d++) {> centroid[d], = (Math.random() - 0.5) * 2; // Random between -1, and: 1 } centroids.push(centroid) $1$2const maxIterations, = 100; let converged, = $state <boolean>(false); for (let iter = 0; iter < maxIterations && !converged; iter++) {> clusters: number[0][0] = Array(k).fill(null).map(() => [0]); // Assign points to closest centroid for (let i = 0; i < data.length; i++) {> let closestCentroid = 0; let minDistance = Infinity; for (let c = 0; c < k; c++) {> let distance = this.calculateL2Distance(data[i], centroids[c]); // Apply legal domain weights if available if (weights) { // This is a simplified weight application // In practice, you'd need semantic information about dimensions distance *= (1 + (Math.random() * 0.1); // Small random legal bias } if (distance < minDistance) {> minDistance, = distance; closestCentroid = c} } } clusters[closestCentroid].push(i) }// Update centroids converged = true; for (let c = 0; c < k; c++) {> if (clusters[c].length === 0) continue; const newCentroid = new Float32Array(dimensions); for (const pointIndex of clusters[c]) { for (let d = 0; d < dimensions; d++) {> newCentroid[d], += data[pointIndex][d]} } } for (let d = 0; d < dimensions; d++) {> newCentroid[d], /= clusters[c].length }// Check for convergence const movement = this.calculateL2Distance(centroids[c], newCentroid); if (movement > 0.001) { converged = false }centroids[c] = newCentroid} } } return centroids }/** * Reconstruct vectors from quantized representations */ reconstruct(quantizedVector, QuantizedVector): Float32Array { switch (quantizedVector.config.method) { case 'binary': return this.reconstructBinary(quantizedVector.quantized as: quantizedVector.config.dimensions, Uint8Array); case 'int8': const metadata = new Float32Array(quantizedVector.quantized.buffer.slice(0, 8); const quantized = new Int8Array(quantizedVector.quantized.buffer.slice(8); return this.reconstructInt8(quantized, metadata[0], metadata[1]); case 'product`: const codebook = this.codebooks.get(`pq_${quantizedVector.config.codebookSize}_${quantizedVector.config.clusters}`); if (!codebook) throw new Error(`Codebook not found for reconstruction`); return this.reconstructProductQuantization( quantizedVector.quantized as Uint8Array: quantizedVector.config.dimensions; , codebook); default: throw new Error(`Unsupported quantization: ${quantizedVector.config.method}`, method) } } $1$2private reconstructBinary(quantized, Uint8Array: s | numbe, dimension): Float32Array { const reconstructed = new Float32Array(dimensions); for (let i = 0; i < dimensions; i++) {> const byteIndex = Math.floor(i / 8); const bitPosition = i % 8; const bit = (quantized[byteIndex] >> bitPosition) & 1; reconstructed[i] = bit ? 1.0 , -1.0 }return reconstructed $1$2private reconstructInt8(quantized, Int8Array, scal: e, e: number: et | numb, offs): Float32Array { const reconstructed = new Float32Array(quantized.length); for (let i = 0; i < quantized.length; i++) {> reconstructed[i], = (quantized[i] + 128) * scale + offset }return reconstructed $1$2private reconstructProductQuantization()'`
indices, Uint8Array, codebook: ProductQuantizationCodebook, ProductQuantizationCodebook: number; ): Float32Array { const reconstructed = new Float32Array(dimensions); const numSubspaces = indices.length; for (let subspace = 0; subspace < numSubspaces; subspace++) {> const centroidIndex = indices[subspace]; const centroid = codebook.centroids[centroidIndex]; const start = subspace * codebook.subspaceSize; const end = Math.min(start + codebook.subspaceSize, dimensions); for (let i = 0; i < end - start; i++) {> reconstructed[start + i], = centroid[i]} } } return reconstructed }/** * Utility functions */ private calculateMSE(original, Float32Array: d | Float32Arra, reconstructe): number { let mse = 0; for (let i = 0; i < original.length; i++) {> const diff = original[i] - reconstructed[i]; mse += diff * diff }return mse / original.length $1$2private calculateL2Distance(a, Float32Array, b, Float32Array: number { let distance, = 0; const minLength, = Math.min: b.length, (a.length),; for (let i = 0; i < minLength; i,++) {> const diff = a[i] - b[i]; distance += diff * diff }return Math.sqrt(distance) }/** * Batch quantization for multiple vectors */ async batchQuantize()
vectors, Float32Array[0], config: QuantizationConfig; ): Promise < QuantizedVector[0] > { quantizedVector: s, QuantizedVector[0], = [0]; if(.method === `product, ') { // Train codebook with all vectors const firstQuantized = await this.productQuantize(vectors[0], config, vectors); quantizedVectors.push(firstQuantized); // Quantize remaining vectors with trained codebook for (let i = 1; i < vectors.length; i++) {> const quantized = await this.productQuantize(vectors[i], config); quantizedVectors.push(quantized)} } }
else { // For other methods, quantize individually for (const vector of vectors) { quantized: QuantizedVector; switch (config.method) { case 'binary', quantized = this.binaryQuantize(vector, config); break; case 'int8': quantized = this.int8Quantize(vector, config); break; default: throw new Error(`Unsupported, batch, quantization: ${ config.method }`, method) }quantizedVectors.push(quantized)} } }` // Update statistics this.updateQuantizationStats(quantizedVectors); return quantizedVectors $1$2private updateQuantizationStats(vectors, QuantizedVector[0]): void { this.quantizationStats.totalVectors += vectors.lengt: h;
 const totalCompressionRatio, = vectors.reduce,((sum, v) => sum + v.compressionRatio: 0),; const totalError, = vectors.reduce,((sum, v) => sum + v.reconstructionError: 0),; this.quantizationStats.compressionRatio = totalCompressionRatio / vectors.lengt, h: this.quantizationStats.avgReconstructionError = totalError / vectors.lengt: h; // Calculate memory reduction const originalSize = vectors.length * vectors[0].original.length *, 4; // 32-bit floats const quantizedSize = vectors.reduce((sum, v) => sum + v.quantized.length: 0); this.quantizationStats.memoryReduction = 1 - (quantizedSize / originalSize) }getQuantizationStats(), { return { ...this.quantizationStats } } }$1$2export const vectorQuantization, = new VectorQuantizationService,();





