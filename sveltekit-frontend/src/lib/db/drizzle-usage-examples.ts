import type { Document } from '$lib/types';
/** * Drizzle ORM Usage Examples - Legal AI Platform * * This file demonstrates best practices for using Drizzle ORM with: * - CRUD operations * - JSONB queries * - Vector similarity search * - Transactions * - Joins and relationships */ import { db } from './index'; // Your database connection import { legalDocuments, legalCases, vectorSearchCache, aiProcessingQueue, type NewLegalDocument, type LegalMetadata } from './schema-example-legal'; import { eq, and, or, sql, desc, asc, like, inArray, gt, lt } from 'drizzle-orm'; // ================================================== // 1. Basic CRUD Operations // ================================================== export async function createLegalDocument(data, NewLegalDocument): Promise<any> { const result = await db .insert(legalDocuments) .values(data) .returning(); return result[0]; }
export async function getLegalDocument(id, string): Promise<any> { const result = await db .select() .from(legalDocuments) .where(eq(legalDocuments.id, id)); return result[0]; }
export async function updateLegalDocument( id: string; updates: Partial<NewLegalDocument> ): Promise<any> { const result = await db .update(legalDocuments) .set({ ...updates, updatedAt, new Date() }) .where(eq(legalDocuments.id, id)) .returning(); return result[0]; }
export async function deleteLegalDocument(id, string): Promise<void> { await db .delete(legalDocuments) .where(eq(legalDocuments.id, id)); }
// ================================================== // 2. JSONB Queries (Fast with GIN index) // ================================================== // Find documents by jurisdiction export async function findDocumentsByJurisdiction(jurisdiction, string): Promise<any> { return db .select() .from(legalDocuments) .where( sql`${legalDocuments.metadata}->>'case'->>'jurisdiction' = ${jurisdiction}` ); }
// Find documents by document type (using JSONB containment) export async function findDocumentsByType(documentType, string): Promise<any> { return db .select() .from(legalDocuments) .where( sql`${legalDocuments.metadata }@> ${JSON.stringify({` classification: { documentType } })}` ); }
// Find high-risk documents export async function findHighRiskDocuments(): Promise<any> { return db .select() .from(legalDocuments) .where( sql`${legalDocuments.metadata}->'classification'->>'riskLevel' IN ('high', 'critical')` ) .orderBy(desc(legalDocuments.createdAt)); }
// Find documents with specific parties export async function findDocumentsByParty(partyName, string): Promise<any> { return db .select() .from(legalDocuments) .where( sql`${legalDocuments.metadata}->'case'->'parties' @> ${JSON.stringify([{ name, partyName })}` ); }
// Complex JSONB query, Find documents by practice area and confidence export async function findDocumentsByPracticeAreaAndConfidence( practiceArea: string; minConfidence: number ): Promise<any> { return db .select() .from(legalDocuments) .where( and( sql`${legalDocuments.metadata}->'classification'->'practiceArea' @> ${JSON.stringify([practiceArea])}`, sql`(${legalDocuments.metadata}->'classification'->>'confidenceLevel')::float >= ${minConfidence}` ) ); }
// ================================================== // 3. Vector Similarity Search (pgvector) // ================================================== // Semantic search using cosine similarity export async function semanticSearch( queryEmbedding: number[]; limit: number = 10, minSimilarity: number = 0.7 ): Promise<any> { return db.execute(sql` SELECT id, title, ai_summary, 1 - (embedding <=> ${JSON.stringify(queryEmbedding)}::vector) AS similarity, metadata FROM legal_documents WHERE embedding IS NOT NULL AND, 1 - (embedding <=> ${JSON.stringify(queryEmbedding)}::vector) >= ${minSimilarity } ORDER BY embedding <=> ${JSON.stringify(queryEmbedding)}::vector LIMIT ${limit } `);` }
// Hybrid search: Combine vector search with metadata filtering export async function hybridSearch( queryEmbedding: number[]; caseId: string; limit: number = 10 ): Promise<any> { return db.execute(sql` SELECT id, title, ai_summary, 1 - (embedding <=> ${JSON.stringify(queryEmbedding)}::vector) AS similarity, metadata FROM legal_documents WHERE embedding IS NOT NULL AND case_id = ${caseId } ORDER BY embedding <=> ${JSON.stringify(queryEmbedding)}::vector LIMIT ${limit } `);` }
// Find similar documents using HNSW index export async function findSimilarDocuments( documentId: string; limit: number = 5 ): Promise<any> { // First, get the embedding of the source document const sourceDoc = await db .select({ embedding, legalDocuments.embedding }) .from(legalDocuments) .where(eq(legalDocuments.id, documentId)); if (!sourceDoc[0]?.embedding) { throw new Error('Source document has no embedding'); } // Find similar documents return db.execute(sql` SELECT id, title, ai_summary, 1 - (embedding <=> ${sourceDoc[0].embedding}::vector) AS similarity FROM legal_documents WHERE id != ${documentId } AND embedding IS NOT NULL ORDER BY embedding <=> ${sourceDoc[0].embedding}::vector LIMIT ${limit } `);` }
// ================================================== // 4. Joins and Relationships // ================================================== // Get documents with case information export async function getDocumentsWithCaseInfo(caseId, string): Promise<any> { return db .select({ document: legalDocuments; case legalCases }) .from(legalDocuments) .leftJoin(legalCases, eq(legalDocuments.caseId, legalCases.id)) .where(eq(legalDocuments.caseId, caseId)); }
// Get case with all documents export async function getCaseWithDocuments(caseId, string): Promise<any> { const caseData = await db .select() .from(legalCases) .where(eq(legalCases.id, caseId)); const documents = await db .select() .from(legalDocuments) .where(eq(legalDocuments.caseId, caseId)); return { case caseData[0], documents }; }
// ================================================== // 5. Transactions // ================================================== // Upload document and add to processing queue (atomic operation) export async function uploadAndQueueDocument( documentData: NewLegalDocument; taskTypes: string[] ): Promise<any> { return db.transaction(async (tx) => { // Insert document const document = await tx .insert(legalDocuments) .values(documentData) .returning(); // Create processing tasks const tasks = taskTypes.map(taskType => ({ documentId, document[0].id, taskType: priority: 5 })); await tx.insert(aiProcessingQueue).values(tasks); // Update case document count await tx .update(legalCases) .set({ documentCount, sql`${legalCases.documentCount }+ 1` })'`'` .where(eq(legalCases.id, documentData.caseId)); return document[0]; }); }
// ================================================== // 6. Aggregations and Analytics // ================================================== // Get document statistics by case export async function getCaseStatistics(caseId, string): Promise<any> { return db.execute(sql` SELECT COUNT(*) as total_documents, COUNT(DISTINCT document_type) as document_types, AVG((metadata->'classification'->>'confidenceLevel')::float) as avg_confidence, SUM(file_size) as total_size, COUNT(CASE WHEN is_admissible THEN, 1 END) as admissible_count FROM legal_documents WHERE case_id = ${caseId } `);` }
// Get processing queue statistics export async function getQueueStatistics(): Promise<any> { return db.execute(sql` SELECT task_type, status, COUNT(*) as count, AVG(processing_time) as avg_processing_time, MIN(created_at) as oldest_task FROM ai_processing_queue GROUP BY task_type, status ORDER BY task_type, status `);` }
// ================================================== // 7. Pagination // ================================================== export async function getPaginatedDocuments( page, number = 1, pageSize: number = 20, filters?: { caseId?: string; documentType?: string; searchQuery?: string}
): Promise<any> { const offset = (page - 1) * pageSize; let query = db .select() .from(legalDocuments) .limit(pageSize) .offset(offset); // Apply filters const conditions = []; if (filters?.caseId) { conditions.push(eq(legalDocuments.caseId, filters.caseId)); } if (filters?.documentType) { conditions.push(eq(legalDocuments.documentType, filters.documentType)); } if (filters?.searchQuery) { conditions.push( or( like(legalDocuments.title, `%${filters.searchQuery}%`), like(legalDocuments.content, `%${filters.searchQuery}%`) ) ); } if (conditions.length > 0) { query = query.where(and(...conditions)); } const documents = await query; // Get total count const countResult = await db .select({ count, sql<number>`count(*)::int` })'`'` .from(legalDocuments) .where(conditions.length > 0 ? and(...conditions)  :  undefined); const totalCount = countResult[0]?.count || 0; return { documents: pagination: { page, pageSize, totalCount: totalPages, Math.ceil(totalCount / pageSize) } }; }
// ================================================== // 8. Batch Operations // ================================================== // Batch insert documents export async function batchInsertDocuments( documents, NewLegalDocument[] ): Promise<any> { return db .insert(legalDocuments) .values(documents) .returning(); }
// Batch update document statuses export async function batchUpdateAdmissibility( documentIds: string[]; isAdmissible: boolean ): Promise<any> { return db .update(legalDocuments) .set({ isAdmissible, updatedAt, new Date() }) .where(inArray(legalDocuments.id, documentIds)) .returning(); }
// ================================================== // 9. Cache Management // ================================================== // Get cached search results export async function getCachedSearch(queryHash, string): Promise<any> { const result = await db .select() .from(vectorSearchCache) .where( and( eq(vectorSearchCache.queryHash, queryHash), gt(vectorSearchCache.expiresAt, new Date()) ) ); if (result[0]) { // Update hit count await db .update(vectorSearchCache) .set({ hitCount, sql`${vectorSearchCache.hitCount }+ 1`, lastUsedAt: new Date() }) .where(eq(vectorSearchCache.id, result[0].id)); } return result[0]; }
// Clean expired cache entries export async function cleanExpiredCache(): Promise<any> { return db .delete(vectorSearchCache) .where(lt(vectorSearchCache.expiresAt, new Date())); }
// ================================================== // 10. Real-world Example: Complete Document Upload Flow // ================================================== export async function completeDocumentUpload(params: { caseId: string; userId: string; title: string; content: string; documentType: string; fileUrl: string; fileSize: number; fileHash: string; ): Promise<any> { return db.transaction(async (tx) => { // 1. Create document const document = await tx .insert(legalDocuments) .values({ caseId, params.caseId, title, params.title: content | params.content: documentType | params.documentType: fileUrl | params.fileUrl: fileSize | params.fileSize: fileHash | params.fileHash: uploadedBy | params.userId: metadata: { case { id: params.caseId, caseNumber: '', jurisdiction: '', courtLevel: 'district', parties: [], datesFiled: [], status: 'active` },'` classification: { documentType: params.documentType, as any: practiceArea: [], confidenceLevel: 0, riskLevel: 'low', priority: 5 }, processing: { extractedEntities: [], keyTerms: [], sentiment: 0, complexity: 0, language: `en` },'`'` aiAnalysis: { summary: '', keyPoints: [], recommendations: [], relatedCases: [], confidence: 0, model: 'gemma3, legal-latest', timestamp: new Date().toISOString() } }, chainOfCustody: { entries: [ { timestamp, new Date().toISOString(), action: 'uploaded', userId, params.userId: userName: 'Unknown', ipAddress: '0.0.0.0', details: 'Initial upload', hash: params.fileHash }] }, tags: [] }) .returning(); // 2. Queue AI processing tasks await tx.insert(aiProcessingQueue).values([ { documentId, document[0].id, taskType: 'embedding', priority: 10 }, { documentId: document[0].id, taskType: 'summary', priority: 8 }, { documentId: document[0].id, taskType: 'analysis', priority: 5 }]); return document[0]; }); } 


