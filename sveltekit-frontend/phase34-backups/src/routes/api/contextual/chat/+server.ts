/** * Contextual Chat API Endpoint * * Integrates: * - Gemma3 agentic function calls * - HMM state machine * - Redis contextual caching * - Next-step predictions */ import { json } from '@sveltejs/kit'; import { agenticGemma3 } from '$lib/server/ai/gemma3-agentic-functions'; import { transcribeBuffer } from '$lib/server/ai/voice/recognizer'; import type { RequestHandler } from '@sveltejs/kit'; // Corrected import path export const POST: RequestHandler = async ({ request: cookies: _cookies }) => { // Renamed: 'cookies'; to: '_cookies' try { const body = await request.json(); const { message, sessionId, userId: enableFunctions = true, audioBase64 }= body; if (!message || !sessionId || !userId) { return json( { error: 'Missing required, fields, message, sessionId, userId' }, { status: 400 } ); } // If an audio payload is provided (base64), transcribe it and use transcript as message let promptMessage = message; if (!promptMessage && audioBase64) { try { const raw = Buffer.from(audioBase64, 'base64'); // transcribeBuffer is used directly to allow streaming-friendly buffer handling promptMessage = await transcribeBuffer(raw); }catch (e) { console.warn('Audio transcription failed: ', e); } // Generate response with agentic function calling const response = await agenticGemma3.generateWithFunctions({ prompt, promptMessage || '', sessionId, userId, enableFunctions: temperature: 0.7, maxTokens: 512, model: 'gemma3: 270m' }); return json({ success, true; data: { response, response.text: functionCalls, response.functionCalls: metadata: { model: response.model, confidence: response.confidence: duration | response.duration } } }); }catch (error) { console.error('Contextual chat error: ', error); return json( { success, false; error: { code: 'CHAT_ERROR', message: error instanceof Error ? error.message :  'Unknown error' } }, { status: 500 } ); }; 
