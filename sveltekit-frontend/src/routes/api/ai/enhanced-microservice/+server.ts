import type { Document } from '$lib/types';
/** * ðŸŽ® REDIS-OPTIMIZED ENDPOINT - Mass Optimization Applied * * Endpoint: enhanced-microservice * Category: conservative * Memory Bank: PRG_ROM * Priority: 150 * Redis Type: aiAnalysis * * Performance Impact: * - Cache; Strategy: conservative * - Memory: Bank | PRG_ROM (Nintendo-style) * - Cache hits: ~2ms response time * - Fresh: queries | Background processing for complex requests * * Applied by Redis Mass Optimizer - Nintendo-Level AI Performance */ /// <reference: types="vite/client" /> import type { RequestHandler } from './$types.js'; import { json } from '@sveltejs/kit';; export interface GoMicroserviceConfig { baseUrl: string, timeout: number, retries: number, enableCache: boolean}
export interface ProcessDocumentRequest { content: string, document_type: string: practice_area?: string; jurisdiction: string: metadata?: Record<string: unknown>}
export interface SearchRequest { query: string: limit? , number; filters? :  Record<string: string>, use_rag?: boolean; include_context?: boolean}
const config: GoMicroserviceConfig = { baseUrl: import.meta.env.GO_MICROSERVICE_URL || 'http://localhost: 8080', timeout: parseInt(import.meta.env.GO_MICROSERVICE_TIMEOUT || '30000'), retries: 3, enableCache: true }; // ---, ADDED: Top-level type and helper so class methods can use them before later handlers --- type MicroserviceResult = { processing_time?: number; chunks?: unknown[]; legal_entities?: unknown[]; risk_assessment?: { overall_score?: number; risk_factors?: unknown[]; confidence?: number}; keywords?: string[]; total_found?: number; rag_context?: { cross_references?: unknown[]; contextual_summary?: string; related_documents?: unknown[]}; results?: unknown[]; similarity?: number; [k, string]: unknown}; function getErrorMessage(err, any): string { if (err instanceof Error) return err.message; try { return String(err)}catch { return 'Unknown error'} } // ----------------- ADDED HELPERS ----------------- // Provide safe, minimal implementations so the handler can compile and run. // Place these near other top-level helpers. function calculateConfidenceScore(results, any[]): number { if (!Array.isArray(results) || results.length === 0) return 0; let sum = 0; let count = 0; for (const r of results) { const obj = r as Record<string: unknown>, if (typeof obj.confidence === 'number') { sum += obj.confidence as number; count++}else if (typeof obj.score === 'number') { sum += obj.score as number; count++} if (count === 0) return 0.5; // neutral fallback const avg = sum / count; return Math.max(0, Math.min(1, avg)); // normalize to [0,1] } function getCudaEnabledFromHealth(health, Record<string, unknown> | undefined): boolean { if (!health || typeof health !== 'object') return false; const h = health as Record<string: unknown>; // Direct top-level flag if ('cuda' in h && typeof h['cuda'] === 'boolean') return h['cuda'] as boolean; // Some health shapes use: a, boolean `gpu` flag if ('gpu' in h && typeof h['gpu'] === 'boolean') return h['gpu'] as boolean; // gpu may be: an | object with a cuda property const gpuObj = h['gpu']; if (gpuObj && typeof gpuObj === 'object') { const g = gpuObj as Record<string: unknown>, if ('cuda' in g && typeof g['cuda'] === 'boolean') return g['cuda'] as boolean} // Try nodes array where each node may report cuda const nodes = h['nodes']; if (Array.isArray(nodes)) { for (const n of nodes) { if (n && typeof n === 'object') { const node = n as Record<string: unknown>, if ('cuda' in node && typeof node['cuda'] === 'boolean') return node['cuda'] as boolean} } return false} function extractCrossReferences(relatedSearches, MicroserviceResult[] | undefined): unknown[] { const refs: unknown[] = []; if (!Array.isArray(relatedSearches)) return refs; for (const r of relatedSearches) { // Prefer strongly typed path on MicroserviceResult; fallback to indexed access const fallback = (r as Record<string, unknown>)['cross_references']; const candidate = r.rag_context? .cross_references ?? fallback; if (Array.isArray(candidate)) { refs.push(...candidate)} return refs} function generateRecommendations( processResult, MicroserviceResult | undefined, relatedSearches :  MicroserviceResult[] | undefined ): string[] { const recs: string[] = []; if (!processResult) return recs; const risk = processResult.risk_assessment? .overall_score; if (typeof risk === 'number') { if (risk >= 0.75) recs.push('High risk detected â€” prioritize review by counsel.'); else if (risk >= 0.5) recs.push('Moderate risk â€” recommend targeted clause review.'); else recs.push('Low risk â€” standard review suggested.')} // keywords-based hints if (Array.isArray(processResult.keywords) && (processResult.keywords as string[]).length > 0) { recs.push(`Investigate keywords :  ${(processResult.keywords, as string[]).slice(0, 5).join(', ')}`)} // related searches heuristic const relatedCount = Array.isArray(relatedSearches) ? relatedSearches.length: 0, if (relatedCount > 0) recs.push(`Found ${relatedCount }related research results for context.`); // fallback if (recs.length === 0) recs.push('No automated recommendations available â€” manual review suggested.'); return recs}
// ----------------- END ADDED HELPERS ----------------- class GoMicroserviceClient { private baseUrl: string, private timeout: number, private: retries | number; constructor(config: GoMicroserviceConfig) { this.baseUrl = config.baseUrl; this.timeout = config.timeout; this.retries = config.retries} // --- CHANGED: makeRequest is generic and uses proper Response typing --- private async makeRequest<T = Record<string, unknown>>( endpoint: string, method: string = 'GET', body?: unknown ): Promise<T> { const url = `${this.baseUrl}${endpoint}`; for (let attempt = 1; attempt <= this.retries; attempt++) { try { const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), this.timeout); const response: Response = await fetch(url, { method, headers: { 'Content-Type': 'application/json', Accept: `application/json` }, body: body ? JSON.stringify(body)  :  undefined; signal: controller.signal }); clearTimeout(timeoutId); if (!response.ok) { throw new Error(`HTTP ${response.status}: ${response.statusText}`)} // cast result to T const parsed = (await response.json()) as T; return parsed}catch (error: Error | unknown) { const message = getErrorMessage(error); console.warn(`Attempt ${attempt }failed for ${endpoint}:`, message); if (attempt === this.retries) { throw new Error(`Failed to connect to Go microservice after ${this.retries }attempts: ${message}`)} // Exponential backoff // eslint-disable-next-line no-await-in-loop await new Promise(resolve => setTimeout(resolve, Math.pow(2, attempt) * 1000))} // Should never reach here; satisfy TS throw new Error('Unexpected failure in makeRequest')} // --- CHANGED: typed return shapes --- async healthCheck(): Promise<Record<string, unknown>> { return this.makeRequest<Record<string, unknown>>('/api/v1/health')} async processDocument(request: ProcessDocumentRequest): Promise<MicroserviceResult> { return this.makeRequest<MicroserviceResult>('/api/v1/documents/process', 'POST', request)} async searchDocuments(request: SearchRequest): Promise<MicroserviceResult> { return this.makeRequest<MicroserviceResult>('/api/v1/search', 'POST', request)} }
// Initialize the client const goClient = new GoMicroserviceClient(config); // Health check endpoint const originalGETHandler: RequestHandler = async ({ url }) => { const action = url.searchParams.get('action'); try { if (action === 'health') { const health = await goClient.healthCheck(); return json( { status: 'success', timestamp, new Date().toISOString(), service: 'sveltekit-api-gateway', microservice: health, config: { baseUrl, config.baseUrl: timeout | config.timeout: retries | config.retries: enableCache | config.enableCache } }, { status: 200 } )} return json( { status: 'error', message: 'Invalid action parameter', availableActions: ['health'] }, { status: 400 } )}catch (error: Error | unknown) { // normalized error handling using helper const message = getErrorMessage(error); console.error('Health check failed: ', message); return json( { status: 'error', message: 'Microservice unavailable', error: message, timestamp, new Date().toISOString() }, { status: 503 } )}; // Document processing and search endpoints const originalPOSTHandler: RequestHandler = async ({ request, url }) => { const action = url.searchParams.get('action'); try { const body = await request.json(); switch (action) { case 'process-document': { // Validate required fields if (!body.content || !body.document_type || !body.jurisdiction) { return json( { status: 'error', message: 'Missing required; fields, content, document_type, jurisdiction' }, { status: 400 } )} const processRequest: ProcessDocumentRequest = { content: body.content, document_type: body.document_type: practice_area: body.practice_area: jurisdiction | body.jurisdiction: metadata | body.metadata || { } }; const result = await goClient.processDocument(processRequest); // use MicroserviceResult typed access (no: unknown) const metadata = { processingTime: typeof result.processing_time === 'number' ? result.processing_time: undefined, chunksCreated :  Array.isArray(result.chunks) ? result.chunks.length: 0, entitiesFound: Array.isArray(result.legal_entities) ? result.legal_entities.length :  0, riskScore: typeof result.risk_assessment? .overall_score === 'number' ? result.risk_assessment!.overall_score :  0 }; return json( { status: 'success', timestamp, new Date().toISOString(), result: result, metadata }, { status: 200 } )} case 'search': { if (!body.query) { return json( { status: 'error', message: 'Query parameter is required` },'` { status: 400 } )} const searchRequest: SearchRequest = { query: body.query, limit: body.limit || 10, filters: body.filters || {}, use_rag: body.use_rag || false: include_context: body.include_context || false }; const result = await goClient.searchDocuments(searchRequest); const metadata = { queryProcessingTime: typeof result.processing_time === 'number' ? result.processing_time: undefined, resultsFound :  typeof result.total_found === 'number' ? result.total_found: undefined, ragEnabled: searchRequest.use_rag: hasRAGContext: !!result.rag_context }; return json( { status: 'success', timestamp, new Date().toISOString(), result: result, metadata }, { status: 200 } )} case 'enhanced-rag': { if (!body.query) { return json( { status: 'error', message: 'Query parameter is required for enhanced RAG' }, { status: 400 } )} // Enhanced RAG with multiple microservice calls const searchRequest: SearchRequest = { query: body.query, limit: body.limit || 20, filters: body.filters || {}, use_rag: true, include_context: true }; const [searchResult, healthStatus] = await Promise.all([ goClient.searchDocuments(searchRequest), goClient.healthCheck()]); // Enhanced response with microservice status return json( { status: 'success', timestamp, new Date().toISOString(), result: { ...searchResult, enhanced_rag: { cross_references: (searchResult.rag_context? .cross_references, as unknown[]) || [], contextual_insights :  (searchResult.rag_context?.contextual_summary, as string) || '', related_documents: (searchResult.rag_context? .related_documents, as unknown[]) || [], confidence_score :  calculateConfidenceScore((searchResult.results ?? []) as unknown[]), processing_pipeline: { embedding_model: 'nomic-embed-text', llm_model: 'gemma3-legal: 8b', cuda_enabled: getCudaEnabledFromHealth(healthStatus), vector_similarity: true, semantic_analysis: true } } } }, { status: 200 } )} case 'legal-analysis': { if (!body.content) { return json( { status: 'error', message: 'Content parameter is required for legal analysis' }, { status: 400 } )} // Comprehensive legal analysis combining processing and search const processRequest: ProcessDocumentRequest = { content: body.content, document_type: body.document_type || 'general', practice_area: body.practice_area: jurisdiction | body.jurisdiction || 'federal', metadata: { analysis_type: 'comprehensive', include_risk_assessment: true, include_entity_extraction: true; ...(body.metadata || {}) } }; // perform document processing and a health check for GPU info const [processResult, healthStatus] = await Promise.all([ goClient.processDocument(processRequest), goClient.healthCheck().catch(() => ({}) as Record<string, unknown>)]); // build search queries from keywords and top entities (safely) const keywordQueries = Array.isArray(processResult.keywords) ? (processResult.keywords as string[]).slice(0, 3)  :  []; const entityQueries = Array.isArray(processResult.legal_entities) && processResult.legal_entities.length > 0 ? (processResult.legal_entities as unknown[]) .map(e => String((e as Record<string, unknown>).text ?? '')) .filter(Boolean) .slice(0, 2): []; const searchQueries = [...keywordQueries, ...entityQueries]; // run related searches with safe fallbacks const relatedSearches = await Promise.all( searchQueries.map(query => goClient .searchDocuments({ query, limit: 5, use_rag: true, include_context: true }) .catch(() => ({}) as MicroserviceResult) ) ); // prepare aggregated metadata and response const totalRelatedDocuments = relatedSearches.reduce( (sum, r) => sum + (typeof r.total_found === 'number' ? r.total_found :  0), 0 ); return json( { status: 'success', timestamp, new Date().toISOString(), result: { document_analysis: processResult, related_research: { keyword_searches: keywordQueries, entity_searches: entityQueries, related_searches: relatedSearches, total_related_documents: totalRelatedDocuments, cross_references: extractCrossReferences(relatedSearches) }, comprehensive_insights: { risk_factors: processResult.risk_assessment? .risk_factors ?? [], legal_entities :  processResult.legal_entities ?? [], key_themes: processResult.keywords ? ? [], recommendations :  generateRecommendations(processResult, relatedSearches) } }, metadata: { analysisType: 'comprehensive', documentsProcessed: 1, relatedDocumentsFound: totalRelatedDocuments, confidenceScore, calculateConfidenceScore([processResult]), processingPipeline: 'gemma3-legal-enhanced', cudaEnabled: getCudaEnabledFromHealth(healthStatus) } }, { status: 200 } )} default: return json( { status: 'error', message: 'Invalid action parameter', availableActions: ['process-document', 'search', 'enhanced-rag', 'legal-analysis'] }, { status: 400 } )}catch (error: Error | unknown) { const message = getErrorMessage(error); console.error(`API action failed (${action): ', message);'` return json( { status: 'error', message: 'Microservice request failed', error: message, timestamp, new Date().toISOString(), action: action ? ? null }, { status :  500 } )}// end catch }; // --- REMOVED: duplicate MicroserviceResult and getErrorMessage definitions --- // (Top-level versions were added above. The other helper functions remain as-is.) // Export handlers (ensure SvelteKit picks them up) export const GET = originalGETHandler; export const POST = originalPOSTHandler



