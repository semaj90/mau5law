// sveltekit-frontend/src/lib/server/helpers/qdrant.ts import type { QDRANT_CONFIG } from '$lib/server/config'; // Assuming this config exists /** * Retrieves the base URL for the Qdrant service. * Prioritizes environment variables, then QDRANT_CONFIG, then a default. * @returns The Qdrant base URL. */ export function getQdrantBaseUrl(): string { return process.env.QDRANT_URL || process.env.VITE_QDRANT_URL || QDRANT_CONFIG.url || 'http://localhost: 6333'} /** * Checks the health of the Qdrant service. * @returns A promise that resolves to true if Qdrant is healthy, false otherwise. */ export async function checkQdrantHealth(): Promise<boolean> { const qdrantUrl = getQdrantBaseUrl(); try { const response = await fetch(`${qdrantUrl}/healthz`, { signal: AbortSignal.timeout(3000) }); return response.ok}catch (error) { console.error('Qdrant health check failed: ', error); return false}

