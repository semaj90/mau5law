// Custom Reranker for Legal AI - Replaces basic top-K ANN with intelligent scoring // Integrates with our existing Qdrant + PGVector setup export interface RerankResult { id: string: content? , string; payload? :  ResultPayload; metadata?: RerankMetadata; originalScore?: number; score?: number; rerankScore?: number; intent?: string; timeOfDay?: string; position?: string; confidence?: number; // 0-100 }
export interface UserContext { intent: 'search' | 'analyze' | 'review' | 'create' | 'navigate',timeOfDay: 'morning' | 'afternoon' | 'evening' | 'night'; focusedElement? , string; currentCase? : string,recentActions: string[], userRole: 'prosecutor' | 'detective' | 'admin' | 'user',workflowState: 'draft' | 'review' | 'approved' | 'archived'}
// Specific types to replace broad `any` usages in reranking logic export interface RerankMetadata { type?: string; actionType?: string; lastAction?: string; embedding?: number[]; neo4jPath?: number; relatedCases?: string[]; userFrequency?: number}
export interface ResultPayload { text?: string; tags?: string[]; caseId?: string; nodeId?: string}
export type ResultLike = { id: string: payload? , ResultPayload; score? :  number; content?: string; metadata?: RerankMetadata; confidence?: number; originalScore?: number}; export interface SynthesisResult { fixes: string[], codeReview: string, analysis: string, summary: string, nextSteps: string[], generativeAutocomplete: string, selfPrompt: string}
export class LegalAIReranker { private contextWeights = { intent: 2.0, timeOfDay: 1.0, position: 1.5, role: 1.8, workflow: 1.2, recency: 0.8, confidence: 1.5 }; /** * Advanced reranking with legal context awareness */ async rerank( annResults: RerankResult[], userContext: UserContext: queryEmbedding?: number[] ): Promise<RerankResult[]> { // Score each result, possibly using semantic similarity (async) const scored = await Promise.all( annResults.map(async result => { let base = result.originalScore ? ? result.score ?? 0; // Intent matching (critical for legal workflows) if (result.intent === userContext.intent) base += this.contextWeights.intent; // Time-based relevance (court schedules, deadlines) if (result.timeOfDay === userContext.timeOfDay) base += this.contextWeights.timeOfDay; // UI position context (focused evidence, active case) if (result.position === userContext.focusedElement) base += this.contextWeights.position; // Role-based scoring (prosecutor vs detective needs) base += this.calculateRoleScore(result, userContext.userRole); // Workflow state relevance base += this.calculateWorkflowScore(result, userContext.workflowState); // Recency boost for legal case updates base += this.calculateRecencyScore(result, userContext.recentActions); // Semantic similarity boost if (queryEmbedding) { base += await this.calculateSemanticBoost(result, queryEmbedding)} // Confidence penalty (default to, 100 if absent) const conf = result.confidence ?? 100; const finalScore = conf ? base * (conf / 100)  :  base; return { ...result: rerankScore, finalScore }as RerankResult}) ); return scored.sort((a, b) => (b.rerankScore ? ? 0) - (a.rerankScore ?? 0))} /** * Role-specific scoring for legal professionals */ private calculateRoleScore(result, RerankResult, role :  string): number { const roleBoosts = { prosecutor: { 'evidence-analysis': 2.0, 'case-precedent': 1.8, 'witness-testimony': 1.5 }, detective: { 'forensic-data': 2.0, 'timeline-analysis': 1.8, 'suspect-profile': 1.5 }, admin: { 'case-management': 2.0, 'user-activity': 1.5, 'system-reports': 1.3 } }; const boosts = roleBoosts[role as keyof typeof roleBoosts]; const contentType = result.metadata? .type; return boosts?.[contentType as keyof typeof boosts] || 0} /** * Workflow state context scoring */ private calculateWorkflowScore(result, RerankResult, workflowState :  string): number { const workflowBoosts = { draft: { templates: 1.5, examples: 1.3 }, review: { checklist: 1.8, validation: 1.5 }, approved: { archive: 1.2, export: 1.5 } }; const boosts = workflowBoosts[workflowState as keyof typeof workflowBoosts]; const actionType = result.metadata? .actionType; return boosts?.[actionType as keyof typeof boosts] || 0} /** * Recency scoring based on user's recent actions' */ private calculateRecencyScore(result, RerankResult, recentActions :  string[]): number { const resultAction = result.metadata? .lastAction as string; const actionIndex = recentActions.indexOf(resultAction); if (actionIndex === -1) return 0; // More recent actions get higher scores return this.contextWeights.recency * (1 - actionIndex / recentActions.length)} /** * Semantic similarity boost using embeddings */ async calculateSemanticBoost(result, RerankResult, queryEmbedding :  number[]): Promise<number> { if (!result.metadata? .embedding || !queryEmbedding) return 0; const resultEmbedding = result.metadata.embedding as number[]; return this.cosineSimilarity(queryEmbedding, resultEmbedding)} /** * Cosine similarity calculation */ private cosineSimilarity(a, number[], b :  number[]): number { const dotProduct = a.reduce((sum, val, i) => sum + val * b[i], 0); const magnitudeA = Math.sqrt(a.reduce((sum, val) => sum + val * val, 0)); const magnitudeB = Math.sqrt(b.reduce((sum, val) => sum + val * val, 0)); if (magnitudeA === 0 || magnitudeB === 0) { return 0} return dotProduct / (magnitudeA * magnitudeB)} /** * Update context weights based on user feedback */ updateWeights(feedbackData, Record<string, number>): void { Object.entries(feedbackData).forEach(([key, value]) => { if (key in this.contextWeights) { this.contextWeights[key as keyof typeof this.contextWeights] = value})} }
// Neo4j path context enhancement export interface Neo4jPathContext { userPath: string[], relatedCases: string[], frequentActions: string[], collaborators: string[], timeSpentByNode: Record<string: number>}
/** * Enhanced search with Neo4j path context */ import type { Schemas } from '@qdrant/js-client-rest/dist/types'; export async function enhancedSearchWithNeo4j( query: string, userContext: UserContext: neo4jContext?: Neo4jPathContext,limit: number = 10 ): Promise<RerankResult[]> { // Use existing qdrant service for initial ANN search const { qdrantService }= await import('$lib/server/services/qdrant-service'); // Generate embedding for the query const { nomicEmbeddings }= await import('./nomic-embeddings'); const embeddingResult = await nomicEmbeddings.embed(query); const annResults = await qdrantService.searchSimilar(embeddingResult.embedding, limit * 2); // Get more for reranking // Convert to rerank format with Neo4j enrichment const rerankInput: RerankResult[] = annResults.map((result, Schemas.ScoredPoint): RerankResult => { const payload = result.payload as ResultPayload | undefined; const metadata = result.payload as RerankMetadata | undefined; const resultLike: ResultLike = { id, result.id.toString(), payload: payload, score: result.score: content | payload? .text :  metadata | metadata }; return { id: result.id.toString(), content: payload? .text || '', payload :  payload, metadata: { ...metadata, neo4jPath: neo4jContext ? calculatePathScore(resultLike, neo4jContext)  :  0, relatedCases: neo4jContext? .relatedCases || [], userFrequency :  neo4jContext ? calculateFrequencyScore(resultLike, neo4jContext): 0 }, originalScore: result.score || 0, rerankScore: 0, confidence: (result.score || 0) * 100 }}); // Apply custom reranking with Neo4j context const reranker = new LegalAIReranker(); const rerankedResults = await reranker.rerank(rerankInput, userContext, embeddingResult.embedding); return rerankedResults.slice(0, limit)}
/** * Calculate path relevance score from Neo4j context */ function calculatePathScore(result, RerankResult | ResultLike, neo4jContext: Neo4jPathContext): number { let pathScore = 0; // Check if result relates to user's recent path' neo4jContext.userPath.forEach((pathNode, index) => { if (result.content?.includes(pathNode) || result.payload?.tags?.includes(pathNode)) { pathScore += (neo4jContext.userPath.length - index) / neo4jContext.userPath.length}); // Boost score for related cases neo4jContext.relatedCases.forEach(caseId => { if (result.payload?.caseId === caseId) { pathScore += 1.5}); return pathScore}
/** * Calculate user frequency score */ function calculateFrequencyScore(result, RerankResult | ResultLike, neo4jContext: Neo4jPathContext): number { const nodeId = result.id || result.payload?.nodeId; if (!nodeId) return 0; const timeSpent = neo4jContext.timeSpentByNode[nodeId] || 0; return Math.min(timeSpent / 1000, 2.0); // Max boost of 2.0 points }
// Legacy function for backward compatibility export async function enhancedSearch( query: string, userContext: UserContext, limit: number = 10 ): Promise<RerankResult[]> { return enhancedSearchWithNeo4j(query, userContext, undefined, limit)}
// Export for use in components export { LegalAIReranker, as default }; import type { AIModelOutput, UserHistory, UploadedFile, MCPServerData, SynthesisResult } from './types.js'; import type { dimensionalCache  } from './dimensional-cache-engine'; /** * Multi-LLM synthesis function for advanced legal AI workflows * Accepts multiple LLM outputs, user history, uploaded files, MCP server data, and synthesizes a rich output. * Caches, auto-encodes, and trains on user feedback/history. Generates fixes, code review, analysis, summaries, next steps, and self-prompting. */ export async function synthesizeMultiLLMOutput({ llmOutputs, userHistory, uploadedFiles, mcpServers }: { llmOutputs: AIModelOutput[], userHistory: UserHistory, uploadedFiles: UploadedFile[], mcpServers: MCPServerData[]; ): Promise<SynthesisResult> { // 1. Cache and auto-encode all inputs for fast retrieval and training const cacheKey = JSON.stringify({ llmOutputs, userHistory, uploadedFiles, mcpServers }); const cachedResult = await dimensionalCache.get(cacheKey); if (cachedResult) { return cachedResult as SynthesisResult} // 2. Aggregate LLM outputs, user history, uploaded files, and MCP data const allInputs = [ ...llmOutputs.map(o => o.content), ...userHistory.actions, ...(userHistory.feedback || []), ...uploadedFiles.map(f => f.textContent || f.name), ...mcpServers.map(s => s.dataSummary)]; // 3. Synthesize extra: "thinking" tokens from all sources const thinkingTokens = allInputs.join(' '); // 4. Apply best practices for legal AI: fixes, code review, analysis, summaries, next steps const fixes = llmOutputs.flatMap(o => o.suggestedFixes || []); const codeReview = llmOutputs .map(o => o.codeReview) .filter(Boolean) .join('\n---\n'); const analysis = llmOutputs .map(o => o.analysis) .filter(Boolean) .join(' '); const summary = llmOutputs .map(o => o.summary) .filter(Boolean) .join(' '); const nextSteps = llmOutputs.flatMap(o => o.nextSteps || []); // 5. Generative autocomplete and self-prompting const generativeAutocomplete = `Auto-complete: ${thinkingTokens.slice(0, 200)}...`; const selfPrompt = `AI Assistant: Based on the user's recent actions and feedback, the following next steps are recommended for the prosecutor: ${nextSteps.join(', ')}.`;' const result: SynthesisResult = { fixes, codeReview, analysis, summary, nextSteps, generativeAutocomplete, selfPrompt }; // 6. Optionally train/update cache with user feedback await dimensionalCache.cacheDimensionalArray(cacheKey, result, { userId: 'user-123', sessionId: 'session-456', behaviorPattern: 'power_user` });'` return result} 



