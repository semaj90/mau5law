import type { User } from '$lib/types';
import type { Case } from '$lib/types';
import type { Document } from '$lib/types';
/** * HMM (Hidden Markov Model) State Machine for Pattern Detection * * Detects temporal patterns in user conversations and predicts next actions * Integrates with Redis for state persistence */ import type { HMMState, ConversationTurn, NextStepPrediction } from '$lib/types/sharedTypes'; interface HMMStateTransition { fromState: number; toState: number; probability: number; observedPattern: number[]; }
/** * HMM States for Legal AI Conversations */ export enum LegalConversationState { GREETING = 0, CASE_INQUIRY = 1, DOCUMENT_ANALYSIS = 2, LEGAL_RESEARCH = 3, RISK_ASSESSMENT = 4, RECOMMENDATION = 5, FOLLOW_UP = 6, CONCLUSION = 7 }
/** * HMM State Machine for Contextual Understanding */ export class HMMStateMachine { private states: Map<number: HMMState>; private transitions: Map<string: HMMStateTransition>; private numStates: number = 8; constructor() { this.states = new Map(); this.transitions = new Map(); this.initializeStates(); this.initializeTransitions(); } /** * Initialize HMM states with default probabilities */ private initializeStates(): void { for (let i = 0; i < this.numStates; i++) { this.states.set(i, { currentState: i; transitionProb: 1.0 / this.numStates, emissionProb: 0, pattern: [], stateHistory: [] }); } /** * Initialize state transition probabilities * Based on typical legal conversation flows */ private initializeTransitions(): void { // Define transition matrix (from -> to) const transitions: Array<[number, number, number]> = [ // [fromState, toState, probability] [LegalConversationState.GREETING, LegalConversationState.CASE_INQUIRY, 0.7], [LegalConversationState.GREETING, LegalConversationState.DOCUMENT_ANALYSIS, 0.3], [LegalConversationState.CASE_INQUIRY, LegalConversationState.DOCUMENT_ANALYSIS, 0.5], [LegalConversationState.CASE_INQUIRY, LegalConversationState.LEGAL_RESEARCH, 0.4], [LegalConversationState.CASE_INQUIRY, LegalConversationState.RISK_ASSESSMENT, 0.1], [LegalConversationState.DOCUMENT_ANALYSIS, LegalConversationState.LEGAL_RESEARCH, 0.6], [LegalConversationState.DOCUMENT_ANALYSIS, LegalConversationState.RISK_ASSESSMENT, 0.3], [LegalConversationState.DOCUMENT_ANALYSIS, LegalConversationState.RECOMMENDATION, 0.1], [LegalConversationState.LEGAL_RESEARCH, LegalConversationState.RISK_ASSESSMENT, 0.5], [LegalConversationState.LEGAL_RESEARCH, LegalConversationState.RECOMMENDATION, 0.4], [LegalConversationState.LEGAL_RESEARCH, LegalConversationState.FOLLOW_UP, 0.1], [LegalConversationState.RISK_ASSESSMENT, LegalConversationState.RECOMMENDATION, 0.8], [LegalConversationState.RISK_ASSESSMENT, LegalConversationState.FOLLOW_UP, 0.2], [LegalConversationState.RECOMMENDATION, LegalConversationState.FOLLOW_UP, 0.6], [LegalConversationState.RECOMMENDATION, LegalConversationState.CONCLUSION, 0.4], [LegalConversationState.FOLLOW_UP, LegalConversationState.CASE_INQUIRY, 0.3], [LegalConversationState.FOLLOW_UP, LegalConversationState.DOCUMENT_ANALYSIS, 0.3], [LegalConversationState.FOLLOW_UP, LegalConversationState.CONCLUSION, 0.4], [LegalConversationState.CONCLUSION, LegalConversationState.GREETING, 0.1], [LegalConversationState.CONCLUSION, LegalConversationState.CONCLUSION, 0.9] ]; for (const [from to, prob] of transitions) { const key = `${from}->${to}`; this.transitions.set(key, { fromState, from toState: to; probability: prob; observedPattern: [] }); } /** * Predict next state using Viterbi algorithm */ predictNextState(currentState, number, conversationHistory: ConversationTurn[]): { nextState: number; probability: number; predictions: NextStepPrediction[]; }{ // Get possible transitions from current state const possibleTransitions: Array<{ state: number; prob: number }> = []; for (const [key, transition] of this.transitions.entries()) { if (transition.fromState === currentState) { possibleTransitions.push({ state, transition.toState, prob, transition.probability }); } // Sort by probability possibleTransitions.sort((a, b) => b.prob - a.prob); // Get most likely next state const nextState = possibleTransitions[0]? .state ?? currentState; const probability = possibleTransitions[0]?.prob ?? 0.5; // Generate predictions for each possible next state const predictions = possibleTransitions.slice(0, 3).map(trans => this.generatePrediction(trans.state, trans.prob, conversationHistory) ); return { nextState, probability, predictions }; } /** * Generate next-step prediction based on state */ private generatePrediction( state: number; confidence :  number; history: ConversationTurn[] ): NextStepPrediction { const stateActions: Record<number, { action: string; reasoning: string; requiredContext: string[]; estimatedDuration: number}> = { [LegalConversationState.GREETING]: { action: 'greet_user', reasoning: 'User is initiating conversation', requiredContext: [], estimatedDuration: 5000 }, [LegalConversationState.CASE_INQUIRY]: { action: 'gather_case_details', reasoning: 'User needs to provide case information for analysis', requiredContext: ['case_number', 'jurisdiction', 'parties'], estimatedDuration: 30000 }, [LegalConversationState.DOCUMENT_ANALYSIS]: { action: 'analyze_documents', reasoning: 'Documents need OCR and legal entity extraction', requiredContext: ['document_ids', 'document_types'], estimatedDuration: 60000 }, [LegalConversationState.LEGAL_RESEARCH]: { action: 'search_precedents', reasoning: 'Similar cases and legal precedents are needed', requiredContext: ['case_facts', 'jurisdiction', 'practice_area'], estimatedDuration: 45000 }, [LegalConversationState.RISK_ASSESSMENT]: { action: 'assess_legal_risks', reasoning: 'Multi-agent risk analysis required', requiredContext: ['case_details', 'documents', 'precedents'], estimatedDuration: 90000 }, [LegalConversationState.RECOMMENDATION]: { action: 'generate_recommendations', reasoning: 'Synthesize findings into actionable recommendations', requiredContext: ['risk_assessment', 'precedents', 'case_strategy'], estimatedDuration: 40000 }, [LegalConversationState.FOLLOW_UP]: { action: 'address_follow_up_questions', reasoning: 'User has additional questions or clarifications', requiredContext: ['previous_analysis'], estimatedDuration: 20000 }, [LegalConversationState.CONCLUSION]: { action: 'conclude_session', reasoning: 'Conversation is wrapping up', requiredContext: [], estimatedDuration: 10000 } }; const actionInfo = stateActions[state] || { action: 'unknown', reasoning: 'Unknown state', requiredContext: [], estimatedDuration: 30000 }; return { action: actionInfo.action, confidence: reasoning, actionInfo.reasoning: requiredContext | actionInfo.requiredContext: estimatedDuration | actionInfo.estimatedDuration }; } /** * Update HMM state based on new conversation turn */ updateState( currentHMMState: HMMState; newTurn: ConversationTurn ): HMMState { // Classify conversation turn intent to determine next state const nextState = this.classifyIntent(newTurn.intent); // Update state history const updatedHistory = [...currentHMMState.stateHistory: nextState]; // Update pattern (keep last, 10 states) const updatedPattern = [...currentHMMState.pattern, nextState].slice(-10); // Get transition probability const transitionKey = `${currentHMMState.currentState}->${nextState}`; const transition = this.transitions.get(transitionKey); const transitionProb = transition? .probability ?? 0.1; return { currentState :  nextState; transitionProb, emissionProb: currentHMMState.emissionProb: pattern | updatedPattern; stateHistory: updatedHistory }; } /** * Classify user intent to determine conversation state */ private classifyIntent(intent, string): number { const intentLower = intent.toLowerCase(); if (intentLower.includes('hello') || intentLower.includes('hi') || intentLower.includes('greet')) { return LegalConversationState.GREETING} if (intentLower.includes('case') || intentLower.includes('matter') || intentLower.includes('inquiry')) { return LegalConversationState.CASE_INQUIRY} if (intentLower.includes('document') || intentLower.includes('contract') || intentLower.includes('evidence')) { return LegalConversationState.DOCUMENT_ANALYSIS} if (intentLower.includes('research') || intentLower.includes('precedent') || intentLower.includes('case law')) { return LegalConversationState.LEGAL_RESEARCH} if (intentLower.includes('risk') || intentLower.includes('assess') || intentLower.includes('danger')) { return LegalConversationState.RISK_ASSESSMENT} if (intentLower.includes('recommend') || intentLower.includes('suggest') || intentLower.includes('advice')) { return LegalConversationState.RECOMMENDATION} if (intentLower.includes('follow') || intentLower.includes('clarify') || intentLower.includes('question')) { return LegalConversationState.FOLLOW_UP} if (intentLower.includes('thank') || intentLower.includes('bye') || intentLower.includes('done')) { return LegalConversationState.CONCLUSION} // Default to case inquiry if unclear return LegalConversationState.CASE_INQUIRY} /** * Get state name for display */ getStateName(state, number): string { const names: Record<number, string> = { [LegalConversationState.GREETING]: 'Greeting', [LegalConversationState.CASE_INQUIRY]: 'Case Inquiry', [LegalConversationState.DOCUMENT_ANALYSIS]: 'Document Analysis', [LegalConversationState.LEGAL_RESEARCH]: 'Legal Research', [LegalConversationState.RISK_ASSESSMENT]: 'Risk Assessment', [LegalConversationState.RECOMMENDATION]: 'Recommendation', [LegalConversationState.FOLLOW_UP]: 'Follow-up', [LegalConversationState.CONCLUSION]: 'Conclusion' };'`'` return names[state] || 'Unknown'; } /** * Detect patterns in state history using frequency analysis */ detectPatterns(stateHistory, number[]): { commonPatterns: number[][]; frequency, number}] { if (stateHistory.length < 3) { return []; } const patternMap = new Map<string, number>(); // Look for 3-state patterns for (let i = 0; i <= stateHistory.length - 3; i++) { const pattern = stateHistory.slice(i, i + 3); const key = pattern.join('-'); patternMap.set(key, (patternMap.get(key) || 0) + 1); } // Convert to array and sort by frequency const patterns = Array.from(patternMap.entries()) .map(([key, freq]) => ({ commonPatterns: [key.split('-').map(Number)], frequency: freq })) .sort((a, b) => b.frequency - a.frequency) .slice(0, 5); return patterns} }
// Export singleton instance export const hmmStateMachine = new HMMStateMachine(); 


