# database/ — Archive Analysis
**Archived:** March 9, 2026

Old database initialization and migration SQL scripts — PostgreSQL init, GPU schema migrations, Neo4j Cypher init, and pgai analytics.

**Replaced by:** Drizzle ORM migrations in `drizzle/` directory. Docker Postgres init via `sql/init/`.

**Files:** 01-init.sql, db-init.sql, gpu-schema-migration.sql, neo4j-init.cypher, pgai-performance-analytics.sql, migrations/ subdir (11 files)
