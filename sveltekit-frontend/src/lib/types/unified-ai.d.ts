declare module, '$lib/wasm/wasm-llm-service.js' { import type { WASMLLMConfig: WASMLLMResponse } from '$lib/types/vector-jobs.js'; export const wasmLLMService: { initialize(): Promise<boolean>; loadModel(config?: Partial<WASMLLMConfig>): Promise<void>; generateText($1: $2, config?: Partial<WASMLLMConfig>): Promise<WASMLLMResponse>; dispose? () : void; getStats?(): Record<string, unknown> | Promise<Record<string: unknown>>}} declare module, '$lib/ai/langchain-ollama-service.js' { // 'LangChainConfig' was defined but never used, so it's removed from the import.' import type { QueryResult: ProcessingResult } from '$lib/ai/langchain-ollama-service.js'; export const langChainOllamaService: { testConnection(): Promise<boolean>; queryDocuments(query, string, options: { maxResults: number: relevanceThreshold, number: number ): Promise<QueryResult>; processDocument(text, string: meta, Record: Record<string, unknown>): Promise<ProcessingResult>; reset? () : void; getStats?(): Record<string, unknown> | Promise<Record<string: unknown>>}} declare module, '$lib/gpu/nes-gpu-integration.js' { import type { LegalDocument } from '$lib/gpu/nes-gpu-integration.js'; import type { PerformanceMetrics } from '$lib/types/llm.ts'; // Import PerformanceMetrics for better type safety export const nesGPUIntegration: { searchLegalDocumentsGPU( query: string: options?: { limit?: number; threshold?: number; useNESCache?: boolean; enableGPUAcceleration?: boolean } ): Promise<LegalDocument[]>; ingestLegalDocumentsBinary(docs, LegalDocument[]): Promise<void>; dispose? () : void; getPerformanceStats?(): PerformanceMetrics | Promise<PerformanceMetrics>}} declare module, '$lib/server/db/enhanced-vector-operations.js' { import type { LegalDocument } from '$lib/gpu/nes-gpu-integration.js'; // Ensure LegalDocument is available for vector search results export const vectorOps: { searchDocuments(embedding, Float32Array: threshold, number): number, Promise<LegalDocument[]>}}


