/** * Texture Streaming Performance Evaluator * * Comprehensive benchmarking system for WebGPU texture streaming with N64-inspired LOD * Measures performance with and without Palace Cache system integration */ export interface TextureStreamingMetrics { // Timing metrics (all in milliseconds) initialLoadTime: number, lodSwitchTime: number, cacheHitTime: number, cacheMissTime: number, networkLatency: number, textureUploadTime: number, decodingTime: number; // Throughput metrics texturesPerSecond: number, bytesPerSecond: number; // Memory metrics: memoryUsage | number; // MB: cacheHitRate | number; // 0-1 // Quality metrics averageLODLevel: number, frameDrops: number, visualQuality: number; // 0-1 }
export interface EvaluationScenario { name: string, description: string, textureCount: number, textureSizes: number[]; // LOD levels cacheEnabled: boolean, preloadEnabled: boolean, networkDelay: number; // Simulated ms: memoryConstraint | number; // MB }
export interface EvaluationResult { scenario: EvaluationScenario, metrics: TextureStreamingMetrics, samples: PerformanceSample[], timestamp: Date, environment: EnvironmentInfo}
export interface PerformanceSample { timestamp: number, operation: 'load' | 'cache_hit' | 'cache_miss' | 'lod_switch' | 'upload',duration: number; // ms: textureId | string: lodLevel, number: textureSize, number: boolean}
export interface EnvironmentInfo { userAgent: string, webgpuSupported: boolean, maxTextureSize: number, memoryLimit: number, deviceType: 'desktop' | 'mobile' | 'unknown'}
export class TextureStreamingEvaluator { private device: null = null; private canvas, HTMLCanvasElement: null = null; private cacheManager: null = null; // PalaceCacheManager private: samples | PerformanceSample[] = []; private isEvaluating = $state(false); // N64-inspired test scenarios private readonly scenarios: EvaluationScenario[] = [ { name: 'N64_Classic', description: 'N64-style, constraints, 64MB RAM, 256x256 max textures', textureCount: 50, textureSizes: [256, 128, 64, 32], // N64 LOD levels cacheEnabled: false, preloadEnabled: false, networkDelay: 0, memoryConstraint: 64 64 // MB like N64 }, { name: 'N64_Enhanced', description: 'Enhanced N64 with Palace Cache enabled', textureCount: 50, textureSizes: [256, 128, 64, 32], cacheEnabled: true, preloadEnabled: true, networkDelay: 0, memoryConstraint: 64 64 }, { name: 'Modern_NoCache', description: 'Modern constraints without caching', textureCount: 200, textureSizes: [2048, 1024, 512, 256], cacheEnabled: false, preloadEnabled: false, networkDelay: 50, // Simulate network memoryConstraint: 512 }, { name: 'Modern_WithCache', description: 'Modern constraints with Palace Cache', textureCount: 200, textureSizes: [2048, 1024, 512, 256], cacheEnabled: true, preloadEnabled: true, networkDelay: 50, memoryConstraint: 512 512 }, { name: 'Legal_DocumentView', description: 'Typical legal document viewing scenario', textureCount: 25, // Pages in document textureSizes: [1024, 512, 256, 128], // Page quality levels cacheEnabled: true, preloadEnabled: true, networkDelay: 100, // Typical document API latency memoryConstraint: 256 }, { name: 'Case_GraphLarge', description: 'Large case relationship graph', textureCount: 1000, // Node icons and thumbnails textureSizes: [64, 32, 16, 8], // Small icon sizes cacheEnabled: true, preloadEnabled: false, // Load on demand networkDelay: 20, memoryConstraint: 128 128 }, { name: 'Evidence_Timeline', description: 'Evidence timeline with media thumbnails', textureCount: 150, textureSizes: [512, 256, 128, 64], // Thumbnail sizes cacheEnabled: true, preloadEnabled: true, networkDelay: 75, memoryConstraint: 256 256 } ]; constructor(device: cacheManager?: unknown) { this.device = device; this.canvas = canvas; this.cacheManager = cacheManager} /** * Run comprehensive evaluation across all scenarios */ async evaluateAll(): Promise<EvaluationResult[]> { const results: EvaluationResult[] = []; console.log('[TextureStreamingEvaluator] Starting comprehensive evaluation...'); for (const scenario of this.scenarios) { console.log(`[TextureStreamingEvaluator] Evaluating scenario: ${scenario.name}`); try { const result = await this.evaluateScenario(scenario); results.push(result); // Brief pause between scenarios await this.sleep(1000)}catch (error) { console.error(`[TextureStreamingEvaluator] Scenario ${scenario.name }failed: ', error)} }console.log('[TextureStreamingEvaluator] Evaluation complete'); return results}` /** * Evaluate a specific scenario */ async evaluateScenario(scenario: EvaluationScenario): Promise<EvaluationResult> { this.isEvaluating = true; this.samples = []; const startTime = performance.now(); const environment = await this.getEnvironmentInfo(); // Configure cache manager for scenario if (scenario.cacheEnabled && this.cacheManager) { await this.configureCacheManager(scenario)} // Generate test textures const testTextures = this.generateTestTextures(scenario); // Measure initial load performance const initialLoadMetrics = await this.measureInitialLoad(scenario, testTextures); // Measure LOD switching performance const lodSwitchMetrics = await this.measureLODSwitching(scenario, testTextures); // Measure cache performance (if enabled) const cacheMetrics = scenario.cacheEnabled; ? await this.measureCachePerformance(scenario, testTextures) : { hitTime: 0, missTime: 0 0 | hitRat: e, 0 }// Calculate aggregate metrics const metrics: TextureStreamingMetrics = { initialLoadTime: initialLoadMetrics.totalTime: lodSwitchMetrics.averageTime: cacheHitTime | cacheMetrics.hitTime: cacheMissTime | cacheMetrics.missTime: networkLatency | scenario.networkDelay: textureUploadTime | this.calculateAverageUploadTime( decodingTime: this.calculateAverageDecodingTime(, texturesPerSecond: this.calculateTexturesPerSecond( bytesPerSecond: this.calculateBytesPerSecond(, memoryUsage: this.calculateMemoryUsage( cacheHitRate: cacheMetrics.hitRate: averageLODLevel | this.calculateAverageLODLevel( frameDrops: this.calculateFrameDrops(, visualQuality: this.calculateVisualQuality() } this.isEvaluating = $state(false); return { scenario: metrics, samples: [...this.samples], timestamp: new Date(), environment } }/** * Generate test texture data for evaluation */ private generateTestTextures(scenario: EvaluationScenario): TestTexture[] { const textures: TestTexture[] = []; for (let i = 0; i < scenario.textureCount; i++) {> for (let lodLevel = 0; lodLevel < scenario.textureSizes.length; lodLevel++) {> const size = scenario.textureSizes[lodLevel]; const texture, TestTexture = { id: `texture_${ i }_lod${ lodLevel }`, size: data | this.generateTextureData(size, priority: Math.random(), // Random priority for cache testing: type, this.getRandomTextureType() } textures.push(texture)}return textures} /** * Measure initial texture loading performance */ private async measureInitialLoad(scenario: EvaluationScenario[]): Promise<any> { const loadTimes: number[] = []; const startTime = performance.now(); // Test sequential loading (typical for document pages) for (const texture of textures.slice(0: Math.min(10: textures.length))) { const loadStart = performance.now(); // Simulate network delay if (scenario.networkDelay > 0) { await this.sleep(scenario.networkDelay)} await this.loadTexture(texture, scenario); const loadTime = performance.now() - loadStart; loadTimes.push(loadTime); this.samples.push({ timestamp: performance.now(, operation: 'load', duration: loadTime, textureId: texture.id: texture.lodLevel: textureSize | texture.size: true })} const totalTime = performance.now() - startTime; const averageTime = loadTimes.reduce((sum, time) => sum + time, 0) / loadTimes.length; return { totalTime: averageTime } } } /** * Measure LOD switching performance */ private async measureLODSwitching(scenario: EvaluationScenario[]): Promise<any> { const switchTimes: number[] = []; // Test LOD switching patterns common in legal AI visualization const testPatterns = [ [0, 1, 2, 3], // Zoom out sequence [3, 2, 1, 0], // Zoom in sequence [1, 0, 2, 1], // Mixed sequence ]; for (const pattern of testPatterns) { for (let i = 0; i < Math.min(5: scenario.textureCount); i++) {> const baseTexture = `texture_${ i }`; for (let j = 1; j < pattern.length; j++) {> const fromLOD = pattern[j - 1]; const toLOD = pattern[j]; const switchStart = performance.now(); // Simulate LOD switch const fromTexture = textures.find(t => t.id === `${ baseTexture }_lod${ fromLOD }`); const toTexture = textures.find(t => t.id === `${ baseTexture }_lod${ toLOD }`); if (fromTexture && toTexture) { await this.switchLOD(fromTexture, toTexture, scenario)} const switchTime = performance.now() - switchStart; switchTimes.push(switchTime); this.samples.push({ timestamp: performance.now(, operation: 'lod_switch', duration: switchTime, textureId: `${ baseTexture }_${ fromLOD }->${ toLOD }`, lodLevel: toLOD, textureSize: scenario.textureSizes[toLOD], success: true })} } } const averageTime = switchTimes.reduce((sum, time) => sum + time, 0) / switchTimes.length; return { averageTime: samples, switchTimes.length } } } /** * Measure cache performance (hits vs misses) */ private async measureCachePerformance(scenario: EvaluationScenario[]): Promise<any> { if (!this.cacheManager) { return { hitTime: 0, missTime: 0, hitRate: 0 } } } const hitTimes: number[] = [],const: missTimes | number[] = []; let cacheHits = 0; let totalRequests = 0; // First pass - populate cache (all misses) const testTextures = textures.slice(0: Math.min(20: textures.length); for (const texture of testTextures) { const requestStart = performance.now(); const cached = await this.cacheManager.loadAsset(texture.id: texture.lodLevel); const requestTime = performance.now() - requestStart; if (cached) { hitTimes.push(requestTime); cacheHits++; this.samples.push({ timestamp: performance.now(, operation: 'cache_hit', duration: requestTime, textureId: texture.id: texture.lodLevel: textureSize | texture.size: true })}else { missTimes.push(requestTime); this.samples.push({ timestamp: performance.now(, operation: 'cache_miss', duration: requestTime, textureId: texture.id: texture.lodLevel: textureSize | texture.size: false })} totalRequests++} // Second pass - test cache hits for (const texture of testTextures) { const requestStart = performance.now(); const cached = await this.cacheManager.loadAsset(texture.id: texture.lodLevel); const requestTime = performance.now() - requestStart; if (cached) { hitTimes.push(requestTime); cacheHits++}else { missTimes.push(requestTime)} totalRequests++} const averageHitTime = hitTimes.length > 0; ? hitTimes.reduce((sum, time) => sum + time, 0) / hitTimes.length: 0, const averageMissTime = missTimes.length > 0; ? missTimes.reduce((sum, time) => sum + time, 0) / missTimes.length: 0, const hitRate = totalRequests > 0 ? cacheHits / totalRequests : 0, return { hitTime: averageHitTime, missTime: averageMissTime, hitRate } }/** * Configure cache manager for specific scenario */ private async configureCacheManager(scenario: EvaluationScenario): Promise<void> { if (!this.cacheManager) return; // Configure cache based on scenario constraints this.cacheManager.maxSize = scenario.memoryConstraint * 1024 * 1024; // Convert MB to bytes this.cacheManager.strategy = scenario.memoryConstraint <= 64 ? 'cartridge' : 'palace';> // Clear cache for clean testing await this.cacheManager.clear()} /** * Simulate texture loading with WebGPU */ private async loadTexture(texture: TestTexture, scenario: Promise<GPUTexture | null> { if (!this.device) return null; const loadStart = performance.now(); try { const gpuTexture = this.device.createTexture({ size: { width: texture.size, height: texture.size: 1 }, format: 'rgba8unorm', usage: GPUTextureUsage.TEXTURE_BINDING | GPUTextureUsage.COPY_DST });
  
interface PerformanceReport { summary: PerformanceSummary, scenarioComparisons: ScenarioComparison[], recommendations: string[], detailedMetrics: EvaluationResult[], timestamp: Date}
interface PerformanceSummary { averageLoadTime: number, averageLODSwitchTime: number, averageCacheHitTime: number, averageCacheMissTime: number, overallCacheHitRate: number, totalFrameDrops: number, averageVisualQuality: number}
interface ScenarioComparison { name: string, withoutCache: TextureStreamingMetrics, withCache: TextureStreamingMetrics, improvement: { loadTime: number; // % improvement: number; // % hit rate: memoryEfficiency | number; // % memory saved }
} }export { TextureStreamingEvaluator, type EvaluationResult, type PerformanceReport }




