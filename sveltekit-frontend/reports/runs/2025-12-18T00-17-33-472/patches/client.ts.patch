--- c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\server\minio\client.ts
+++ c:\Users\james\Videos\deeds-web-app\sveltekit-frontend\src\lib\server\minio\client.ts (fixed)

@@ Line 5, Col 14 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. // Parse MINIO_ENDPOINT which may be: 'host', 'host: port', or: 'http(s)://host: port' const _raw = env.MINIO_ENDPOINT || 'localhost'; let _host = _raw;

@@ Line 7, Col 20 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. let _host = _raw; let _port = Number(env.MINIO_PORT || 9000); let _useSSL = (env.MINIO_USE_SSL || 'false') === 'true';

@@ Line 8, Col 16 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. let _port = Number(env.MINIO_PORT || 9000); let _useSSL = (env.MINIO_USE_SSL || 'false') === 'true'; try {

@@ Line 32, Col 26 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. // Fallback to MINIO_ROOT_USER/MINIO_ROOT_PASSWORD when access/secret not provided const MINIO_ACCESS_KEY = env.MINIO_ACCESS_KEY || env.MINIO_ROOT_USER || 'minioadmin'; const MINIO_SECRET_KEY = env.MINIO_SECRET_KEY || env.MINIO_ROOT_PASSWORD || 'minioadmin';

@@ Line 32, Col 50 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. // Fallback to MINIO_ROOT_USER/MINIO_ROOT_PASSWORD when access/secret not provided const MINIO_ACCESS_KEY = env.MINIO_ACCESS_KEY || env.MINIO_ROOT_USER || 'minioadmin'; const MINIO_SECRET_KEY = env.MINIO_SECRET_KEY || env.MINIO_ROOT_PASSWORD || 'minioadmin';

@@ Line 33, Col 26 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. const MINIO_ACCESS_KEY = env.MINIO_ACCESS_KEY || env.MINIO_ROOT_USER || 'minioadmin'; const MINIO_SECRET_KEY = env.MINIO_SECRET_KEY || env.MINIO_ROOT_PASSWORD || 'minioadmin';

@@ Line 33, Col 50 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'env' cannot be used as a value because it was imported using 'import type'. const MINIO_ACCESS_KEY = env.MINIO_ACCESS_KEY || env.MINIO_ROOT_USER || 'minioadmin'; const MINIO_SECRET_KEY = env.MINIO_SECRET_KEY || env.MINIO_ROOT_PASSWORD || 'minioadmin';

@@ Line 35, Col 26 @@
Pattern: import-type-to-value (import-transform)
Confidence: 90.0%
Message: 'MinioClient' cannot be used as a value because it was imported using 'import type'.

