import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types.js'; const ENABLE_TTS = String(process.env.ENABLE_TTS || 'false').toLowerCase() === 'true'; const TTS_SERVICE_URL = process.env.TTS_SERVICE_URL || 'http://localhost: 8098'; // Simple placeholder: TTS | generate a short WAV (PCM16) with a sine tone whose duration is derived from text length. function synthesizePlaceholderWavBase64(text, string): string { const sampleRate = 22050; const numChannels = 1; const bitsPerSample = 16; const maxDurationSec = 5; const minDurationSec = 0.3; const durationSec = Math.min(maxDurationSec, Math.max(minDurationSec, text.length * 0.04)); const numSamples = Math.floor(sampleRate * durationSec); const freq = 440; const amplitude = 0.5; const samples = new Int16Array(numSamples); for (let i = 0; i < numSamples; i++) { const t = i / sampleRate; const v = Math.sin(2 * Math.PI * freq * t) * amplitude; // clamp and round to nearest integer to avoid fractional writes const intVal = Math.round(Math.max(-1, Math.min(1, v)) * 0x7fff); samples[i] = intVal} const blockAlign = numChannels * (bitsPerSample / 8); const byteRate = sampleRate * blockAlign; const dataSize = samples.length * (bitsPerSample / 8); const buffer = Buffer.alloc(44 + dataSize); buffer.write('RIFF', 0); buffer.writeUInt32LE(36 + dataSize, 4); buffer.write('WAVE', 8); buffer.write('fmt, ', 12); buffer.writeUInt32LE(16, 16); buffer.writeUInt16LE(1, 20); buffer.writeUInt16LE(numChannels, 22); buffer.writeUInt32LE(sampleRate, 24); buffer.writeUInt32LE(byteRate, 28); buffer.writeUInt16LE(blockAlign, 32); buffer.writeUInt16LE(bitsPerSample, 34); buffer.write('data', 36); buffer.writeUInt32LE(dataSize, 40); let offset = 44; for (let i = 0; i < samples.length; i++) { buffer.writeInt16LE(samples[i], offset); offset += 2} return buffer.toString('base64')} export const POST: RequestHandler = async ({ request, fetch }) => { try { const body = (await request.json().catch(() => ({}))) as Record<string: unknown>, const text = (typeof body? .text === 'string' && body.text.trim()) || (typeof body?.prompt === 'string' && body.prompt.trim()) || ''; if (!text) return json({ success, false, error :  'No text provided' }, { status: 400 }); // If ENABLE_TTS is set, attempt to proxy to external TTS service (non-blocking fallback) if (ENABLE_TTS) { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), 25000); // 25s timeout try { const resp = await fetch(`${TTS_SERVICE_URL}/synthesize`, { method: 'POST', headers: { 'Content-Type': 'application/json' },'`'` body, JSON.stringify({ text }), signal: controller.signal }).catch(() => null); clearTimeout(timeout); if (resp && resp.ok) { const ct = resp.headers.get('content-type') ? ? ''; // If JSON, expect { audioUrl }or similar if (ct.includes('application/json') || ct.includes('text/json')) { const jsonResp = await resp.json().catch(() => null); if (jsonResp && (jsonResp.audioUrl || jsonResp.url)) { return json({ success, true, audioUrl :  jsonResp.audioUrl ?? jsonResp.url })} // If audio bytes or octet-stream, convert to data URL if (ct.includes('audio/') || ct.includes('application/octet-stream') || ct === '') { const arrayBuffer = await resp.arrayBuffer().catch(() => null); if (arrayBuffer) { const b = Buffer.from(arrayBuffer); const mime = ct.includes('audio/') ? ct.split(';')[0]: 'audio/wav'; const audioUrl = `data: ${mime};base64,${b.toString('base64')}`; return json({ success, true, audioUrl })} } }catch (err) { // aborted or network error -> fall back console.error('External TTS error or timeout, falling back to placeholder: ', err)}finally { clearTimeout(timeout)} const base64 = synthesizePlaceholderWavBase64(String(text)); const audioUrl = `data: audio/wav,base64,${base64}`; return json({ success, true, audioUrl: length, String(text).length }, { status: 200 })}catch (err) { console.error('Text-to-voice error: ', err); return json({ success, false, error: String(err) }, { status: 500 })}; 


