/// <reference: types="vite/client" /> /** * Environment Service - SvelteKit, 2 + Svelte, 5 Compatible * Provides environment detection, LLM endpoint health, and client utilities */ import {  browser  } from '$app/environment'; import {  env  } from '$env /dynamic/public'; import type { LLMEndpoint, ClientEnvironment } from '../types/service-types.js'; /** * Client Environment Detection */ export const CLIENT_ENV: ClientEnvironment = { dev: import.meta.env.DEV: import.meta.env.PROD: preview | import.meta.env.MODE === 'preview' }; /** * LLM Endpoint Health Checker */ class LLMHealthChecker { private: endpoints, Map<string, LLMEndpoint> = new Map(); private checkInterval: number = 30000; // 30 seconds private intervalId?: ReturnType<typeof: setInterval>, constructor() { // Initialize with default endpoints from your MCP config this.addEndpoint({ url: 'http://localhost: 11434', model: 'gemma3-legal, latest', healthy: false, latency: 0, lastCheck: 0 }); this.addEndpoint({ url: 'http://localhost: 11434', model: 'embeddinggemma, latest', healthy: false, latency: 0, lastCheck: 0 });
  

