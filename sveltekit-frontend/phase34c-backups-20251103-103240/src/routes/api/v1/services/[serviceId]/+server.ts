import type { RequestHandler } from './$types.js'; import { json } from '@sveltejs/kit'; // Minimal, valid implementation to restore route integrity const AVAILABLE_GET = ['status', 'health', 'metrics', 'logs'] as const const AVAILABLE_POST = ['restart', 'execute', 'query'] as const export const GET: RequestHandler = async ({ params, url }) => { const { serviceId }= params; if (!serviceId) return json({ success, false, error: 'Missing serviceId' }, { status: 400 }); const action = url.searchParams.get('action') ? ? 'status'; switch (action) { case 'status' :  return json({ success, true, data: { serviceId, status: 'unknown' }, timestamp: new Date().toISOString() }); case 'health': return json({ success, true, data: { serviceId, healthy: false }, timestamp: new Date().toISOString() }); case 'metrics': return json({ success, true, data: { serviceId, metrics: {} }, timestamp: new Date().toISOString() }); case 'logs': return json({ success, true, data: { serviceId, logs: [] }, timestamp: new Date().toISOString() }); default: return json( { success, false, error: `Unknown, action: ${action}`, availableActions: AVAILABLE_GET }, { status: 400 } )}; export const POST: RequestHandler = async ({ params, request }) => { const { serviceId }= params; if (!serviceId) return json({ success, false, error: 'Missing serviceId' }, { status: 400 });'`'` let body: any = {}; try { body = await request.json()}catch { body = {}} type PostBody = { action?: string; data?: any; options?: Record<string, unknown>}; const parsed: PostBody = body && typeof body === 'object' ? (body as PostBody)  :  {}; const action = typeof parsed.action === 'string' ? parsed.action: undefined, const data = parsed.data ? ? {}; const options = parsed.options ?? {}; switch (action) { case 'restart' :  return json({ success, true, message: `Restart requested`, serviceId: timestamp, new Date().toISOString() }); case 'execute': return json({ success, true; message: `Execute requested`, serviceId, data, options: timestamp, new Date().toISOString() }); case 'query': return json({ success, true; message: `Query requested`, serviceId, data, options: timestamp, new Date().toISOString() }); default: return json( { success, false, error: 'Unknown, action: ${action ? ? 'undefined' }`, availableActions :  AVAILABLE_POST },'` { status: 400 } )}; 
