enhacned caching errors_927_25

building and adding webassembly:

yesxt steps I can take (pick one or tell me to proceed)

Align enhanced-caching-service.ts with what the bridge expects (remove the enhancedCacheSafe shim). I will:

Read enhanced-caching-service.ts, confirm method names/signatures, update bridge calls to use the real service, and run tsc to verify.
Risk: if the service has incompatible shapes, I'll add minimal adapters and unit tests where needed.
Start SvelteKit dev server and triage any remaining esbuild/Vite transform errors:

Run npm run dev in sveltekit-frontend and watch Vite logs for parse errors.
Fix errors iteratively until the dev server builds and the /ai/* routes load.

outcome: I'll update vite.config.js to read the HMR port from environment (fallback to 24678) so Vite uses the .env HMR_PORT (3131) instead of hardcoding 24678, avoiding the WebSocket port conflict. Then I'll restart the frontend dev server and watch Vite logs.

ise, prioritized TODO plan you can act on immediately to unblock the frontend, plus the follow-up plan to add a repeatable WASM build + a safe runtime loader with fallback. At the end I'll include copy-paste commands and a minimal TypeScript shim + loader you can drop into simd-json-wrapper.ts to get the dev server running now.

Contract (tiny)
Inputs: existing frontend code that imports simd-json-wrapper.ts and other services; dev environment with Node/npm and Vite/SvelteKit.
Outputs: dev server that starts (Vite), no esbuild parse errors from WASM or broken files; /ai/* routes reachable; later, a reproducible WASM build that the frontend loads with a safe JS fallback.
Error modes: parse-syntax errors, server-only code accidentally imported into client bundle, failing WASM instantiate → fallback to JS shim.
Top-level plan (priority order)
Priority A — Unblock dev server now (fast)

Add a small JS shim for simd-json-wrapper.ts that exports the same API but uses JSON.parse fallback.
Fix or shim any files with syntax/parse errors (fast option: create small shims that preserve exports for these modules):
gemma-embedding.ts (replace with small shim that calls Ollama only if available, else returns a mock or error object).
revolutionary-ai-integration.ts and visual-memory-palace-integration.ts — fix stray punctuation/parameter signatures or create small shims.
Remove/guard server-only imports from client code:
Replace runtime imports of $lib/server/* with import type where only types are needed, or move the logic to +server.ts endpoints and call them via fetch.
Run typecheck and start dev server; iterate until Vite no longer reports parse errors.
Estimated time: 1–2 hours.
Priority B — Add repeatable WASM build + loader (next)

Choose toolchain (recommended: Rust + wasm-pack; alternative: AssemblyScript).
Create wasm subproject:
Minimal API: parse_json(input: string) -> string (or buffer/ptr API depending on wasm-bindgen or asmjs glue).
Add package.json script to build wasm and output to wasm.
Implement runtime loader in simd-json-wrapper.ts:
Try WebAssembly.instantiateStreaming(fetch('/wasm/<name>.wasm'), imports).
If instantiateStreaming/fetch fails, fall back to JS shim.
Expose initWASM(): Promise<void>, isWASMReady: boolean, and the same parsing functions.
Add small test harness and CI job to build wasm and validate existence.
Estimated time: 1–2 days for a basic Rust/wasm-pack flow; more if advanced SIMD and performance tuning are required.
Priority C — Hardening & docs

Add API contract (types, return shapes, error conventions).
Add tests (unit tests for shim + wasm integration).
Add wasm/README.md describing toolchain, build command, and where outputs go.
Enforce server/client import separation in repo conventions.
Optional: telemetry for fallback usage & parse times.
Concrete actionable checklist (copy/paste)
Priority A — Unblock now:

 Create a JS shim loader at simd-json-wrapper.ts (see snippet below).
 Replace or shim gemma-embedding.ts (small implementation that returns predictable results if Ollama is not available).
 Fix syntax in revolutionary-ai-integration.ts and visual-memory-palace-integration.ts (or create shims if quicker).
 Audit and fix client imports of $lib/server/* (use import type or server endpoints).
 Run a quick typecheck and start dev server.
Priority B — WASM build & loader:

 Create wasm project (Rust or AssemblyScript).
 Add build:wasm script to repo package.json.
 Build wasm into wasm.
 Update simd-json-wrapper.ts to load real .wasm file with graceful fallback.
 Add test harness and CI job.
Priority C — Hardening:

 Add TypeScript contract docs and unit tests.
 Add README with build instructions.
 Add CI verification of wasm artifacts.
Commands to run (pwsh)
Typecheck:


npx tsc --noEmit --skipLibCheck
Start dev server:


cd sveltekit-frontendnpm run dev
(Example) Build wasm with wasm-pack (if you choose Rust and installed wasm-pack):


# from repo rootcd wasm/simd_json_accelwasm-pack build --target web --out-dir ../../sveltekit-frontend/static/wasm
Add a package.json script (example):


"scripts": {  "build:wasm": "cd wasm/simd_json_accel && wasm-pack build --target web --out-dir ../../sveltekit-frontend/static/wasm"}
Minimal TypeScript shim + loader (drop into simd-json-wrapper.ts)
This is a safe shim you can paste now to unblock esbuild/Vite. It provides the same function names and a runtime initWASM() you can later replace to try to load real WASM.


// src/lib/wasm/simd-json-wrapper.tsexport interface LegalDocumentJSON {  caseId: string;  documentType: 'contract'|'evidence'|'brief'|'citation';  title: string;  content: string;  metadata: { [k:string]: any; };  embeddings?: number[];}export interface SIMDParsingMetrics {  parseTime: number;  validationTime: number;  compressionTime: number;  compressionRatio: number;  throughput: number;}class ShimSIMD {  private metrics: SIMDParsingMetrics = {    parseTime: 0, validationTime: 0, compressionTime: 0, compressionRatio: 1, throughput: 0  };  async initialize(): Promise<void> { /* no-op */ }  async parseDocumentJSON(jsonString: string): Promise<LegalDocumentJSON> {    const start = Date.now();    const parsed = JSON.parse(jsonString) as LegalDocumentJSON;    this.metrics.parseTime = Date.now() - start;    return parsed;  }  async batchParseDocuments(jsonStrings: string[]): Promise<LegalDocumentJSON[]> {    const start = Date.now();    const results = jsonStrings.map(s => JSON.parse(s) as LegalDocumentJSON);    this.metrics.throughput = results.length / Math.max((Date.now()-start)/1000, 0.001);    return results;  }  async validateDocument(jsonString: string): Promise<boolean> {    try { const doc = JSON.parse(jsonString); return !!(doc && doc.caseId && doc.documentType); }    catch { return false; }  }  getMetrics(): SIMDParsingMetrics { return { ...this.metrics }; }}export const simdJSONAccelerator = new ShimSIMD();export const isWASMReady = false;export async function initWASM(): Promise<boolean> {  // Placeholder: when you add real wasm, try instantiateStreaming here and return true if loaded.  return false;}export async function parseLegalDocumentWithSIMD(s: string) {  return simdJSONAccelerator.parseDocumentJSON(s);}export async function batchParseLegalDocuments(arr: string[]) {  return simdJSONAccelerator.batchParseDocuments(arr);}export async function validateLegalDocumentWithSIMD(s: string) {  return simdJSONAccelerator.validateDocument(s);}export function getSIMDMetrics() { return simdJSONAccelerator.getMetrics(); }
Notes:

This shim keeps API shape stable. Later you will replace the initWASM() to actually load the .wasm file and wire the methods to WASM exports, falling back to shim methods if load fails.
Edge cases & pitfalls to watch
Problem: server-only modules in client code (e.g., $lib/server/cache/redis-cache.ts). Fix by:
Using import type { ... } for types only; or
Moving the logic into a +server.ts endpoint and call via fetch.
Problem: esbuild will stop on any parse error. If a file is large and you don't intend to change it now, temporarily replace with a small shim that preserves exports.
Problem: WASM binary not served from the right path — put compiled .wasm into wasm so it's available at /wasm/<file>.wasm.
Problem: cross-origin/instantiateStreaming: browsers require correct MIME type; Vite serves static files correctly but double-check static folder.
Verification / Quality gates (quick)
After adding shim(s):
Run npx tsc --noEmit --skipLibCheck (should finish without fatal syntax errors).
Start dev: cd sveltekit-frontend && npm run dev and confirm Vite starts without esbuild transform parse errors.
Open the browser to the local Vite URL and visit a /ai/* route. If SSR is used, check server logs for route handling.
After WASM integration:
Run npm run build:wasm (or CI step) to ensure .wasm artifact appears.
Test loader: await initWASM(); console.log(isWASMReady) — should show true if load succeeded.
If you want, I can:

Apply the shim patch to simd-json-wrapper.ts and a minimal shim for gemma-embedding.ts right now and then run the dev server to confirm Vite starts cleanly.
Or produce a Rust wasm-pack template under wasm and add build:wasm scripts.
Which should I do next?