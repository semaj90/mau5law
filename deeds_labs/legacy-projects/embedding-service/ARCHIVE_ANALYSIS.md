# embedding-service/ — Archive Analysis
**Archived:** March 9, 2026

Python embedding generation service with vector dimension migration tools, summary querying, and VLM integration tests.

**Replaced by:** Ollama `embeddinggemma:latest` (768-dim) + Go gRPC embedding server (port 50051) + ONNX WebGPU client-side.

**Files:** embedding_service.py, migrate_vector_dimension.py, query_summaries.py, requirements.txt + 3 more (7 files)
