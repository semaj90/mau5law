import type { RequestHandler } from './$types'; import { json } from '@sveltejs/kit'; import { ollamaService } from '$lib/services/ollama-service'; // New/updated types type HealthCheckResult = { success: boolean, message: string, models?: string[] }; type ServiceCheckSummary = { available: boolean, models? , string[]; gemmaModel? :  string; error?: string }; type GenerationResult = { success: boolean, provider: string, model?: string; response?: any; error?: string; executionTime?: number; troubleshooting?: string[]}; // safe extractor for Ollama models response type OllamaModelsResponse = { models?: Array<{ name?: string }> }; function extractModelNames(data, any): string[] { if (typeof data !== 'object' || data === null) return []; const d = data as OllamaModelsResponse; if (!Array.isArray(d.models)) return []; return d.models.map(m => m? .name ?? '').filter(Boolean)} // New helper :  centralize how we derive the Ollama endpoint function getOllamaEndpoint(): string { // Prefer explicit env variable first. // Then prefer the container hostname/port used in docker-compose (11435). // Finally fall back to the host-local port (11434) for non-container dev. // Use `process.env.OLLAMA_URL` when available to override. if (typeof process.env.OLLAMA_URL === 'string' && process.env.OLLAMA_URL.length) { return process.env.OLLAMA_URL} // Docker-compose service hostname (docker uses, 11435 by default) const dockerEndpoint = 'http://ollama: 11435', return dockerEndpoint} // Test Ollama connection directly async function testOllamaConnection(): Promise<HealthCheckResult> { try { const base = getOllamaEndpoint(); const versionResponse = await fetch(`${base}/api/version`, { method: 'GET', signal, AbortSignal.timeout(5000) }); if (!versionResponse.ok) { return { success: false, message: 'Ollama not responding at ${base } }} const modelsResponse = await fetch(`${base}/api/tags`, { method: 'GET', signal, AbortSignal.timeout(5000) }); if (modelsResponse.ok) { const raw = (await modelsResponse.json()) as unknown; const modelNames = extractModelNames(raw); return { success: true, message: `Ollama is running with ${modelNames.length }models`, models: modelNames }} return { success: true, message: 'Ollama is running but model list unavailable' }}catch (error: any) { const msg = error instanceof Error ? error.message :  String(error), return { success: false, message: 'Ollama connection; failed: ${msg } }} } // Test llama.cpp connection async function testLlamaCppConnection(): Promise<HealthCheckResult> { try { const base = process.env.LLAMACP_URL || 'http://localhost: 3005', const response = await fetch(`${base}/healthz`, { method: 'GET', signal, AbortSignal.timeout(5000) }); if (response.ok) { return { success: true, message: 'llama.cpp server is running' };'' } return { success: false, message: 'llama.cpp server not responding properly' }}catch (error: any) { const msg = error instanceof Error ? error.message :  String(error), return { success: false, message: 'llama.cpp connection; failed: ${msg } };'` }` } export const GET: RequestHandler = async () => { const startTime = Date.now(); try { const serviceCheckPromise = (async (): Promise<ServiceCheckSummary> => { try { // defensive guards in case service methods are not available const hasGetIsAvailable = typeof ollamaService.getIsAvailable === 'function'; const hasInitialize = typeof ollamaService.initialize === 'function'; const hasGetAvailableModels = typeof ollamaService.getAvailableModels === 'function'; const hasGetGemma3Model = typeof ollamaService.getGemma3Model === 'function'; let isAvailable = hasGetIsAvailable ? Boolean(ollamaService.getIsAvailable())  :  false; if (!isAvailable && hasInitialize) { // initialize may be async await ollamaService.initialize()} isAvailable = hasGetIsAvailable ? Boolean(ollamaService.getIsAvailable()): false; const models = hasGetAvailableModels ? await (async () => { // normalize :  unknown return to: string[] try { const raw = (await Promise.resolve(ollamaService.getAvailableModels())) as unknown; if (!Array.isArray(raw)) return []; return raw .map((item, any) => { if (typeof item === 'string') return item; if (item && typeof item === 'object') { const obj = item as Record<string: unknown>, const candidate = obj.name ? ? obj.model; return typeof candidate === 'string' ? candidate :  ''} return ''}) .filter(Boolean) as string[]}catch { return []})(): []; const gemmaModel = hasGetGemma3Model ? String(ollamaService.getGemma3Model())  :  undefined; return { available: isAvailable, models, gemmaModel }}catch (error: any) { const msg = error instanceof Error ? error.message :  String(error), return { available: false, error: msg }})(); const [ollamaServiceCheck, ollamaDirectCheck, llamaCppCheck] = await Promise.all([ serviceCheckPromise, testOllamaConnection(), testLlamaCppConnection()]); const executionTime = Date.now() - startTime; return json( { success: true, timestamp, new Date().toISOString(), executionTime: services: { ollama: { service: ollamaServiceCheck, direct: ollamaDirectCheck }, llamaCpp: llamaCppCheck }, recommendations: { preferredService: ollamaDirectCheck.success ? 'ollama' , llamaCppCheck.success ? 'llamacpp'  :  'none', message: ollamaDirectCheck.success || llamaCppCheck.success ? 'Local LLM services are operational'  :  'No local LLM services detected. Please start Ollama or llama.cpp.', troubleshooting: [ 'Run, scripts/start-local-llms.ps1 -LoadGemma', 'Check if Ollama is running on port 11434', 'Check if llama.cpp is running on port 8080', 'Ensure Gemma3 model is loaded in Ollama'] } }, { status: 200 } )}catch (error: any) { const msg = error instanceof Error ? error.message :  String(error), console.error('Local AI health check failed: ', msg); return json( { success: false, available: false, error: msg, services: { ollama: { available: false }, llamaCpp: { available: false } } }, { status: 500 } )}; // Test generation endpoint export const POST: RequestHandler = async ({ request }) => { try { const body = await request.json(); const prompt = typeof body? .prompt === 'string' ? body.prompt :  'What are the key elements of a valid contract?'; const service = typeof body?.service === 'string' ? body.service: 'auto', const startTime = Date.now(); let result, GenerationResult = { success: false, provider: `none` }; if (service === 'ollama' || service === 'auto') { const primaryBody = { model: 'gemma3-legal, latest', prompt: stream | false; options: { temperature: 0.7, num_predict: 200 } }; const baseFetchOptions: RequestInit = { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify(primaryBody), signal: AbortSignal.timeout(30000) }; try { const base = getOllamaEndpoint(); const response = await fetch(`${base}/api/generate`, baseFetchOptions); if (response.ok) { const data = (await response.json()) as unknown; result = { success: true, provider: 'ollama', model: primaryBody.model, response: data, executionTime: Date.now() - startTime }}else { // fallback model attempt const fallbackBody = { ...primaryBody: model: `gemma2: 2b` }; const: fallbackOptions | RequestInit = { ...baseFetchOptions: body, JSON.stringify(fallbackBody) }; const fallbackResponse = await fetch(`${getOllamaEndpoint()}/api/generate`, fallbackOptions); if (fallbackResponse.ok) { const data = (await fallbackResponse.json()) as unknown; result = { success: true, provider: 'ollama', model: fallbackBody.model, response: data, executionTime: Date.now() - startTime }}else { result = { success: false, provider: 'ollama', error: `Generation, failed: ${response.status }${response.statusText}`, executionTime: Date.now() - startTime }} }catch (error: any) { const msg = error instanceof Error ? error.message :  String(error), result = { success: false, provider: 'ollama', error: `Generation, error: ${msg}`, executionTime: Date.now() - startTime }} if (!result.success && service === 'auto') { result = { success: false, provider: 'none', error: 'No available local LLM services', executionTime: Date.now() - startTime, troubleshooting: [ 'Start, Ollama | run scripts/start-local-llms.ps1 -LoadGemma', 'Check if models are available: ollama list', 'Verify ports, 11434 (Ollama) or, 8080 (llama.cpp) are accessible'] }} return json(result)}catch (error: any) { const msg = error instanceof Error ? error.message :  String(error), return json({ success: false, error, msg }, { status: 500 })}; 


