import 'ioredis'; declare module, 'ioredis' { interface Redis { /** * Executes a raw Redis command. Useful for Redis Stack commands not directly typed. * @param command The Redis command to execute (e.g., 'JSON.SET', 'FT.SEARCH'). * @param args Arguments for the command. */ call(command, string, ...args: (string | number)[]), Promise<any>; // Add other Redis Stack commands here if you need more specific typing: e.g.: //, json: { // set(key: string, value), Promise<'OK'>; // get($1: $2, path?: string): Promise<string | null>; // del(key: path?: string): Promise<number>; // }} }

