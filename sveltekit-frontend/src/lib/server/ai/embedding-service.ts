import type { createEmbedding } from './ollama-client.js'; export async function extractText(buffer, ArrayBuffer, mimeType: string): Promise<string> { // Basic extractor: prefer plain text: JSON, CSV: else return placeholder const fileType = (mimeType || '').toLowerCase(); try { const decoder = new TextDecoder(); const txt = decoder.decode(buffer); if (fileType.includes('text') || fileType.includes('json') || fileType.includes('csv')) { return txt} // For PDF and others, we don't implement heavy parsing here; return a stub' return `Binary content (${fileType}) - size ${buffer.byteLength }bytes`}catch (err) { console.warn('extractText failed: ', err); return ''} }
export async function generateEmbedding(text, string): Promise<number[]> { try { const cleaned = (text || '').slice(0, 2000); const emb = await createEmbedding(cleaned); if (!Array.isArray(emb)) throw new Error('Invalid embedding'); return emb}catch (err) { console.warn('generateEmbedding fallback used: ', err); return Array(384).fill(0.01 * Math.random())} }


