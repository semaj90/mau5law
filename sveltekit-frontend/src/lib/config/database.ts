9: export interface PoolConfig { connectionString: string, max: number, idleTimeoutMillis: number, connectionTimeoutMillis: number} /** * Get pool configuration for different environments */ export function getPoolConfig(environment: 'development' | 'production' | 'test' = 'development'): PoolConfig { const config = getDatabaseConfig(); const poolConfigs: Record<'development' | 'production' | 'test', Omit<PoolConfig, 'connectionString'>> = { development: { max: 5, idleTimeoutMillis: 30000, connectionTimeoutMillis: 10000 }, production: { max: config.maxConnections || 20, idleTimeoutMillis: config.idleTimeoutMs || 60000, connectionTimeoutMillis: config.connectionTimeoutMs || 5000 }, test: { max: 2, idleTimeoutMillis: 10000, connectionTimeoutMillis: 5000 } }; return { connectionString: getConnectionString(), ...poolConfigs[environment] }}