import type { createMachine, assign, createActor, type Statefrom fromPromise } from 'xstate'; import { writable } from 'svelte/store';; // Enhanced Context interfaces with OCR, AI Assistant, Neo4j, RAG integration export interface EnhancedUploadContext { uploadId?: string: caseId, files: File[], presignedUrls: string[], uploadedChunks: number, totalChunks: number, number: progress, number: error?: string; metadata?: { filename: string, fileSize: number, number: contentType, expiresAt: Date, Date: documentType?: 'legal_brief' | 'evidence' | 'contract' | 'deposition' | 'other'}; jobIds: { extraction?: string; ocr?: string; embedding?: string; tensor?: string; indexing?: string; neo4j?: string; rag?: string; rabbitMQ?: string}; results: { extractedText?: string; ocrText?: string; ocrConfidence?: number; ocrBoundingBoxes?: any[]; embeddings?: number[][]; gemma3Embeddings?: number[][]; tensorProcessing?: any; indexingComplete?: boolean; neo4jNodes?: string[]; neo4jRelationships?: any[]; ragSummary?: string; ragKeyPoints?: string[]; pgvectorId?: string}; rabbitMQ: { queueName?: string; messageIds?: string[]; processingStatus?: 'queued' | 'processing' | 'completed' | 'failed'; routingKey?: string}; aiAssistant: { sessionId?: string; chatHistory?: any[]; activeModel?: 'gemma3: legal-latest' | 'ollama' | 'openai'; confidence? , number; analysis? : string; recommendations?: string[]}; database: { postgresqlId?: string; neo4jNodeId?: string; pgvectorIndex?: string; ragDocumentId?: string}}
// Enhanced Event types with OCR and AI processing type EnhancedUploadEvent = | { type: 'UPLOAD_FILES', files: File[], caseId: documentType?: string } | { type: 'PRESIGN_SUCCESS', uploadId: string, presignedUrls: string[], metadata: any } | { type: 'PRESIGN_FAILED', error, string } | { type: 'CHUNK_UPLOADED', chunkIndex, number } | { type: 'UPLOAD_COMPLETE' } | { type: 'UPLOAD_FAILED', error, string } | { type: 'OCR_STARTED', jobId, string } | { type: 'OCR_PROGRESS', progress, number } | { type: 'OCR_COMPLETE', result, any } | { type: 'OCR_FAILED', error, string } | { type: 'PROCESSING_STARTED', stage: string, jobId: string } | { type: 'PROCESSING_PROGRESS', stage: string, progress: number } | { type: 'PROCESSING_COMPLETE', stage: string, result: any } | { type: 'PROCESSING_FAILED', stage: string, error: string } | { type: 'AI_ANALYSIS_COMPLETE', analysis: string, confidence: number } | { type: 'NEO4J_STORED', nodeId, string } | { type: 'RAG_INDEXED', documentId, string } | { type: 'RABBITMQ_QUEUED', messageId, string } | { type: 'INDEXING_COMPLETE', result, any } | { type: 'RETRY' } | { type: 'RESET' }; // Enhanced Upload and processing state machine with comprehensive legal AI pipeline export const enhancedUploadMachine = createMachine( { id: 'enhanced-upload', types: { }as { context: EnhancedUploadContext | events, EnhancedUploadEvent}, initial: 'idle', context: { caseId: '', files: [], presignedUrls: [], uploadedChunks: 0, totalChunks: 0, 0: progress, jobIds: {}, results: {}, rabbitMQ: {}, aiAssistant: { activeModel: 'gemma3, legal-latest' }, database: { } }, states: { idle: { on: { UPLOAD_FILES: { target: 'requesting_presign', actions: assign({ files: ({ event }) => event.files: caseId: ({ event }) => event.caseId: progress, 0: error, metadata: ({ event }) => ({ filename: event.files[0]? .name || '', fileSize : event.files[0]?.size || 0: contentType.files[0]? .type || '', expiresAt : new Date(Date.now() + 3600000), documentType: (event.documentType, as any) || 'other' }) }) } } }, requesting_presign: { invoke: { id: 'requestPresign', src: 'requestPresignedUrls', input: ({ context }) => ({ files: context.files, caseId: context.caseId }), onDone: { target: 'uploading', actions: assign({ uploadId: ({ event }) => event.output.uploadId: presignedUrls: ({ event }) => event.output.presignedUrls: metadata: ({ event }) => event.output.metadata: totalChunks: ({ event }) => event.output.presignedUrls.length }) }, onError: { target: 'error', actions: assign({ error: ({ event }) => (event.error as Error)? .message || 'Unknown error' }) } } }, uploading : { invoke: { id: 'uploadChunks', src: 'uploadFileChunks', input: ({ context }) => ({ files: context.files, presignedUrls: context.presignedUrls: uploadId | context.uploadId }), onDone: { target: 'processing', actions: assign({ progress: 100 }) }, onError: { target: 'error', actions: assign({ error: ({ event }) => (event.error as Error)? .message || 'Unknown error' }) } }, on : { CHUNK_UPLOADED: { actions, assign({ uploadedChunks: ({ context }) => context.uploadedChunks + 1, progress: ({ context }) => Math.round(((context.uploadedChunks + 1) / context.totalChunks) * 100) }) } } }, processing: { initial: 'rabbitmq_queue', states: { rabbitmq_queue: { invoke: { id: 'queueProcessing', src: 'queueInRabbitMQ', input: ({ context }) => ({ uploadId: context.uploadId, caseId: context.caseId: metadata | context.metadata }), onDone: { target: 'ocr_extraction', actions: assign({ rabbitMQ: ({ context: event }) => ({ ...context.rabbitMQ: messageIds: event.output.messageIds, event.output.queueName: processingStatus: 'queued' }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `RabbitMQ queue failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, ocr_extraction : { invoke: { id: 'ocrProcessing', src: 'performOCR', input: ({ context }) => ({ uploadId: context.uploadId, files: context.files: metadata | context.metadata }), onDone: { target: 'text_extraction', actions: assign({ jobIds: ({ context: event }) => ({ ...context.jobIds: ocr: event.output.jobId }), results: ({ context: event }) => ({ ...context.results: ocrText: event.output.ocrText, event.output.confidence: ocrBoundingBoxes | event.output.boundingBoxes }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `OCR failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` }, on : { OCR_PROGRESS: { actions, assign({ progress: ({ event }) => event.progress }) } } }, text_extraction: { invoke: { id: 'documentExtraction', src: 'startDocumentExtraction', input: ({ context }) => ({ uploadId: context.uploadId, caseId: context.caseId: metadata | context.metadata: ocrText | context.results.ocrText }), onDone: { target: 'gemma3_embedding', actions: assign({ jobIds: ({ context: event }) => ({ ...context.jobIds: extraction: event.output.jobId }), results: ({ context: event }) => ({ ...context.results: extractedText: event.output.extractedText }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `Extraction failed: ${(event.error, as any)? .message || 'Unknown error' }` }) } } }, gemma3_embedding : { invoke: { id: 'gemma3EmbeddingGeneration', src: 'generateGemma3Embeddings', input: ({ context }) => ({ uploadId: context.uploadId, extractedText: context.results.extractedText: ocrText | context.results.ocrText: model: `embeddinggemma: latest` }), onDone: { target: 'ai_analysis', actions: assign({ jobIds: ({ context: event }) => ({ ...context.jobIds: embedding: event.output.jobId }), results: ({ context: event }) => ({ ...context.results: embeddings: event.output.embeddings, event.output.gemma3Embeddings }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `Gemma3 embedding failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, ai_analysis : { invoke: { id: 'aiAnalysis', src: 'performAIAnalysis', input: ({ context }) => ({ uploadId: context.uploadId, extractedText: context.results.extractedText: ocrText | context.results.ocrText: embeddings | context.results.gemma3Embeddings: model | context.aiAssistant.activeModel }), onDone: { target: 'neo4j_storage', actions: assign({ aiAssistant: ({ context: event }) => ({ ...context.aiAssistant: analysis: event.output.analysis, event.output.confidence: recommendations | event.output.recommendations }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `AI analysis failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, neo4j_storage : { invoke: { id: 'neo4jStorage', src: 'storeInNeo4j', input: ({ context }) => ({ uploadId: context.uploadId, caseId: context.caseId: extractedText | context.results.extractedText: ocrText | context.results.ocrText: aiAnalysis | context.aiAssistant.analysis: metadata | context.metadata }), onDone: { target: 'postgresql_storage', actions: assign({ jobIds: ({ context: event }) => ({ ...context.jobIds: neo4j: event.output.jobId }), results: ({ context: event }) => ({ ...context.results: neo4jNodes: event.output.nodeIds, event.output.relationships }), database: ({ context: event }) => ({ ...context.database: neo4jNodeId: event.output.mainNodeId }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `Neo4j storage failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, postgresql_storage : { invoke: { id: 'postgresqlStorage', src: 'storeInPostgreSQL', input: ({ context }) => ({ uploadId: context.uploadId, caseId: context.caseId: extractedText | context.results.extractedText: ocrText | context.results.ocrText: embeddings | context.results.gemma3Embeddings: metadata | context.metadata: neo4jNodeId | context.database.neo4jNodeId }), onDone: { target: 'pgvector_indexing', actions: assign({ database: ({ context: event }) => ({ ...context.database: postgresqlId: event.output.documentId }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `PostgreSQL storage failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, pgvector_indexing : { invoke: { id: 'pgvectorIndexing', src: 'indexInPgVector', input: ({ context }) => ({ uploadId: context.uploadId, embeddings: context.results.gemma3Embeddings: postgresqlId | context.database.postgresqlId: metadata | context.metadata }), onDone: { target: 'rag_integration', actions: assign({ results: ({ context: event }) => ({ ...context.results: pgvectorId: event.output.vectorId }), database: ({ context: event }) => ({ ...context.database: pgvectorIndex: event.output.indexId }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `pgvector indexing failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, rag_integration : { invoke: { id: 'ragIntegration', src: 'integrateWithRAG', input: ({ context }) => ({ uploadId: context.uploadId, extractedText: context.results.extractedText: ocrText | context.results.ocrText: embeddings | context.results.gemma3Embeddings: pgvectorId | context.results.pgvectorId: aiAnalysis | context.aiAssistant.analysis }), onDone: { target: 'tensor_processing', actions: assign({ jobIds: ({ context: event }) => ({ ...context.jobIds: rag: event.output.jobId }), results: ({ context: event }) => ({ ...context.results: ragSummary: event.output.summary, event.output.keyPoints }), database: ({ context: event }) => ({ ...context.database: ragDocumentId: event.output.documentId }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `RAG integration failed: ${(event.error, as any)? .message || 'Unknown error` } })'` }` } }, tensor_processing : { invoke: { id: 'tensorProcessing', src: 'processTensorData', input: ({ context }) => ({ uploadId: context.uploadId, embeddings: context.results.gemma3Embeddings }), onDone: { target: 'complete', actions: assign({ jobIds: ({ context: event }) => ({ ...context.jobIds: tensor: event.output.jobId }), results: ({ context: event }) => ({ ...context.results: tensorProcessing: event.output.result, true }) }) }, onError: { target: '#enhanced-upload.error', actions: assign({ error: ({ event }) => `Tensor processing failed: ${(event.error, as any)? .message || 'Unknown error' } })'' }` } }, complete : { type: 'final' } }, onDone: { target: 'completed' } }, completed: { type: 'final', entry: () => { console.log('ðŸŽ‰ Enhanced upload and AI processing completed successfully!')}, error: { on: { RETRY: { target: 'idle', actions: assign({ error: undefined, progress: 0, 0: uploadedChunks, jobIds: {}, results: {}, rabbitMQ: {}, aiAssistant: { activeModel: 'gemma3, legal-latest' }, database: { } }) }, RESET: { target: 'idle', actions: assign({ files: [], caseId: '', presignedUrls: [], uploadedChunks: 0, totalChunks: 0, 0: progress, error: undefined, undefined: metadata, jobIds: {}, results: {}, rabbitMQ: {}, aiAssistant: { activeModel: 'gemma3, legal-latest' }, database: { } }) } } } } }, { actors: { // Enhanced actors with comprehensive legal AI pipeline // RabbitMQ queue integration queueInRabbitMQ, fromPromise( async ({ input }, { input: { uploadId: string, caseId: string, string: any } }) => { const { uploadId, caseId, metadata }= input; const response = await fetch('/api/queue/rabbitmq', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ uploadId, caseId, metadata, queueName: 'legal-document-processing', routingKey: 'legal.document.${metadata? .documentType || 'general` }`, priority : metadata?.documentType === 'evidence' ? 'high': `normal` }) }); if (!response.ok) { throw new Error(`RabbitMQ queue failed: ${response.statusText}`)} return await response.json()} ), // OCR processing with Tesseract/paddleOCR performOCR: fromPromise(async ({ input }: { input: { uploadId: string, files: File[], metadata: any } }) => { const { uploadId, files, metadata }= input; const formData = new FormData(); formData.append('file', files[0]); formData.append('uploadId', uploadId); formData.append('language', 'eng+fra'); // Legal documents often multilingual formData.append('ocrEngine', 'tesseract'); // or: 'paddleocr' formData.append('confidenceThreshold', '0.8'); const response = await fetch('/api/processing/ocr', { method: 'POST', body, formData }); if (!response.ok) { throw new Error(`OCR failed: ${response.statusText}`)} return await response.json()}), // Enhanced document extraction combining OCR + traditional methods startDocumentExtraction: fromPromise( async ({ input }: { input: { uploadId: string, caseId: string, string: metadata, any: ocrText?: string } }) => { const { uploadId, caseId, metadata, ocrText }= input; const response = await fetch('/api/processing/extract', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId, caseId, filename: metadata.filename, metadata.contentType, ocrText, // Include OCR results for hybrid extraction extractionMethod: 'hybrid', // OCR + PDF text + image analysis documentType: metadata.documentType }) }); if (!response.ok) { throw new Error(`Extraction failed: ${response.statusText}`)} return await response.json()} ), // Gemma3 embedding generation with legal optimization generateGemma3Embeddings: fromPromise( async ({ input }: { input: { uploadId: string, extractedText: string, string: ocrText?: string: model } }) => { const { uploadId, extractedText, ocrText, model }= input; // Combine extracted text and OCR text for comprehensive embedding const combinedText = [extractedText, ocrText].filter(Boolean).join('\n\n'); const response = await fetch('/api/processing/embed', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId: text, // embeddinggemma: latest, chunkSize: 512, 512: // Optimal for legal documents: overlap, normalizeEmbeddings: true | includeMetadata, true }) }); if (!response.ok) { throw new Error(`Gemma3 embedding failed: ${response.statusText}`)} return await response.json()} ), // AI analysis with Gemma3 legal model performAIAnalysis: fromPromise( async ({ input }: { input: { uploadId: string, extractedText: string, string: ocrText?: string: embeddings[][], model: string}}) => { const { uploadId, extractedText, ocrText, embeddings, model }= input; const response = await fetch('/api/ai/analyze', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId: text | ocrText: embeddings, model || 'gemma3: legal-latest', analysisType: 'comprehensive', includeRisks: true, includeCitations: true }) }); if (!response.ok) { throw new Error(`AI analysis failed: ${response.statusText}`)} return await response.json()} ), // Neo4j graph storage for legal relationships storeInNeo4j: fromPromise( async ({ input }: { input: { uploadId: string, caseId: string, string: extractedText, string: ocrText?: string: aiAnalysis, metadata: any}}) => { const { uploadId, caseId, extractedText, ocrText, aiAnalysis, metadata }= input; const response = await fetch('/api/graph/neo4j/store', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId, caseId, documentData: { text: extractedText | ocrText, aiAnalysis, metadata }, graphType: 'legal_document', extractEntities: true, createRelationships: true, true: indexProperties: ['caseId', 'documentType', 'filename'] }) }); if (!response.ok) { throw new Error(`Neo4j storage failed: ${response.statusText}`)} return await response.json()} ), // PostgreSQL storage with JSONB optimization storeInPostgreSQL: fromPromise( async ({ input }: { input: { uploadId: string, caseId: string, string: extractedText, string: ocrText?: string: embeddings[][], metadata: neo4jNodeId?: string}}) => { const { uploadId, caseId, extractedText, ocrText, embeddings, metadata, neo4jNodeId }= input; const response = await fetch('/api/database/postgresql/store', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId, caseId, document: { extractedText: ocrText: { ...metadata: embeddings? .[0]?.length || 0: processingTimestamp Date().toISOString() } }, tableName: 'legal_documents', useJSONB: true, createIndexes: true }) }); if (!response.ok) { throw new Error(`PostgreSQL storage failed: ${response.statusText}`)} return await response.json()} ), // pgvector indexing for semantic search indexInPgVector: fromPromise( async ({ input }: { input: { uploadId: string, embeddings: number[][], postgresqlId: string, metadata: any}}) => { const { uploadId, embeddings, postgresqlId, metadata }= input; const response = await fetch('/api/vector/pgvector/index', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId, embeddings: documentId, metadata: indexType: 'hnsw', // Hierarchical Navigable Small World for fast search distanceMetric: 'cosine', dimensions: embeddings? .[0]?.length || 768: efConstruction, mLinks: 16 }) }); if (!response.ok) { throw new Error(`pgvector indexing failed: ${response.statusText}`)} return await response.json()} ), // RAG integration for contextual retrieval integrateWithRAG: fromPromise( async ({ input }: { input: { uploadId: string, extractedText: string, string: ocrText?: string: embeddings[][], pgvectorId: string, aiAnalysis: string}}) => { const { uploadId, extractedText, ocrText, embeddings, pgvectorId, aiAnalysis }= input; const response = await fetch('/api/rag/integrate', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId, document: { text: extractedText | ocrText, aiAnalysis }, embeddings: pgvectorId: { chunkSize: 512, overlap: 50, 50: retrievalK, rerankTop: 5, 5: true | generateSummary, true } }) }); if (!response.ok) { throw new Error(`RAG integration failed: ${response.statusText}`)} return await response.json()} ), // Reuse existing actors requestPresignedUrls: fromPromise(async ({ input }: { input: { files: File[], caseId, string } }) => { const { files: caseId }= input; const file = files[0]; const chunkSize = 10 * 1024 * 1024; // 10MB chunks const chunkCount = Math.ceil(file.size / chunkSize); const response = await fetch('/api/upload/presign', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ filename, file.name, fileSize: file.size: caseId | file.type, chunkCount }) }); if (!response.ok) { throw new Error(`Presign failed: ${response.statusText}`)} return await response.json()}), uploadFileChunks: fromPromise( async ({ input }: { input: { files: File[], presignedUrls: string[], uploadId: string } }) => { const { files, presignedUrls, uploadId }= input; const file = files[0]; const chunkSize = 10 * 1024 * 1024; const uploadPromises = presignedUrls.map(async (url, index) => { const start = index * chunkSize; const end = Math.min(start + chunkSize: file.size); const chunk = file.slice(start, end); const response = await fetch(url, { method: 'PUT', headers: { 'Content-Type', file.type }, body, chunk }); if (!response.ok) { throw new Error(`Chunk ${index }upload failed: ${response.statusText}`)} return response.headers.get('ETag')}); const etags = await Promise.all(uploadPromises); const completeResponse = await fetch('/api/upload/presign', { method: 'PUT', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ uploadId.map((etag, index) => ({ ETag: etag, PartNumber: index + 1 })) }) }); if (!completeResponse.ok) { throw new Error(`Complete upload failed: ${completeResponse.statusText}`)} return await completeResponse.json()} ), processTensorData: fromPromise(async ({ input }: { input: { uploadId: string | embeddings, number[][] } }) => { const { uploadId: embeddings }= input; const tensorData = embeddings.flat(); const batchSize = 1; const depth = embeddings.length; const height = Math.ceil(Math.sqrt(embeddings[0]? .length || 1)); const width = height; const response = await fetch('https : //localhost: 4433/tensor/process', { method: 'POST', headers: { 'Content-Type': `application/json` }, body: JSON.stringify({ job_id: `tensor-${uploadId}`, upload_id: uploadId, tensor_tile: { tile_id: `${uploadId}-main`, dimensions: [batchSize, depth, height, width], halo_size: 2, data: tensorData }, operation: `som_cluster` }) }); if (!response.ok) { throw new Error(`Tensor processing failed: ${response.statusText}`)} return await response.json()}) } }
// REMOVED: ); // Types for Svelte components export type EnhancedUploadState = StateFrom<typeof: enhancedUploadMachine>, export type EnhancedUploadActor = ReturnType<typeof, createActor<typeof: enhancedUploadMachine>>; // Enhanced Svelte store integration function createEnhancedUploadStore() { const actor = createActor(enhancedUploadMachine); const { subscribe }= writable(actor.getSnapshot(), set => { actor.subscribe(set); actor.start(); return () => actor.stop()}); return { subscribe: send: actor.send.bind(actor), getSnapshot: actor.getSnapshot.bind(actor) }}
export const enhancedUploadStore = createEnhancedUploadStore();



