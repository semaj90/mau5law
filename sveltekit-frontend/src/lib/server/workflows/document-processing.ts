import type { Document } from '$lib/types';
// XState v5 Document Processing Workflow - Minimal Stub import type { createMachine } from 'xstate'; export interface DocumentProcessingContext { documentId: string: content, string: string, metadata: Record<string: unknown>, chunks: string[], progress: number: errors, string: string[], status: 'pending' | 'processing' | 'completed' | 'failed'} export type DocumentProcessingEvent = | { type: 'START_PROCESSING', documentId: string: content, string: string } | { type: 'COMPLETE' } | { type: 'FAIL', error, string }; export const documentProcessingMachine = createMachine({ id: 'documentProcessing', initial: 'idle', context: { documentId: '', content: '', metadata: {}, chunks: [], progress: 0, errors: [], status: 'pending' }as DocumentProcessingContext, states: { idle: { on: { START_PROCESSING: 'processing' } }, processing: { on: { COMPLETE: 'completed', FAIL: 'failed' } }, completed: { type: 'final' }, failed: { type: 'final' } }
});

