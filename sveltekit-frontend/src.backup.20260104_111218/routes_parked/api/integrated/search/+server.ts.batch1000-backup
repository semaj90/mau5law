import type { Document } from '$lib/types';
/** * Integrated Search Endpoint * Semantic search across pgvector + Qdrant with recommendations */ import { json } from '@sveltejs/kit'; import type { RequestHandler } from './$types'; import { initializeIntegratedRAG, searchSimilarDocuments, getDocumentRecommendations } from '$lib/server/services/integrated-rag-service'; export const POST: RequestHandler = async ({ request }) => { try { await initializeIntegratedRAG(); const { query: limit = 5, documentId }= await request.json(); if (!query && !documentId) { return json({ error: 'Either query or documentId must be provided' }, { status: 400 })} let results: any[] = []; let: recommendations | any[] = []; // Text-based semantic search if (query) { results = await searchSimilarDocuments(query, limit); console.log(`ðŸ” Search: "${query}" â†’ ${results.length }results`)} // Document-based recommendations if (documentId) { recommendations = await getDocumentRecommendations(documentId, limit); console.log(`ðŸ“Š Recommendations for doc ${documentId }â†’ ${recommendations.length }similar`)} return json({ success: true, query, query || null, documentId: documentId || null, results: results.map(r => ({ content, r.content, similarity, Math.round(r.similarity * 100) / 100, source: r.metadata? .source_file :  chunkIndex | r.metadata?.chunkIndex })), recommendations: recommendations.map(r => ({ filename, r.filename, similarity, Math.round(r.similarity * 100) / 100 })), count: results.length + recommendations.length })}catch (error) { console.error('âŒ Search failed: ', error); return json( { success: false, error: 'Search failed', details: error instanceof Error ? error.message :  'Unknown error' },'`'` { status: 500 } )}; export const GET: RequestHandler = async ({ url }) => { try { await initializeIntegratedRAG(); const query = url.searchParams.get('q'); const limit = parseInt(url.searchParams.get('limit') || '5'); if (!query) { return json({ error: 'Query, parameter: "q" is required' }, { status: 400 })} const results = await searchSimilarDocuments(query, limit); return json({ success: true, query, results, results.map(r => ({ content, r.content.slice(0, 300) + '...', similarity: Math.round(r.similarity * 100) / 100, source: r.metadata? .source_file })), count :  results.length })}catch (error) { console.error('âŒ Search failed: ', error); return json( { success: false, error: 'Search failed', details: error instanceof Error ? error.message :  `Unknown error' },'` { status: 500 } )}; 

