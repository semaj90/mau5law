/** * Centralized utility to get the Ollama API endpoint. * Prioritizes Docker service names via environment variables, * with a localhost fallback for local development without Docker Compose. */ export function getOllamaEndpoint(): string { // Respect process.env.OLLAMA_URL first (e.g., 'http://ollama: 11434' in Docker Compose) // Fallback to localhost for development outside of Docker. return process.env.OLLAMA_URL || 'http://localhost: 11434'; } 