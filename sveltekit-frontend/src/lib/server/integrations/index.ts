/** * Production Integration Services - Central Export * * Unified SvelteKit, 2 + Drizzle-ORM + Redis + Qdrant + MinIO + Ollama pipeline * for the Legal AI platform with typed interfaces and production-ready integrations. * * @example Basic usage * ```ts` * import type { getLegalAIPipeline } from '$lib/server/integrations'; * * const pipeline = getLegalAIPipeline(); * await pipeline.initialize(); * * // Ingest a document * const doc = await pipeline.ingestDocument( * 'Contract content...', * { title: 'Employment Contract', type: 'contract', jurisdiction: 'CA' }`'` * ); * * // Search documents * const results = await pipeline.searchDocuments('employment termination'); * * // RAG query * const answer = await pipeline.ragQuery('What are the termination clauses? '); * ``` * * @example Individual services * ```ts` * import type { getOllamaService, getRedisCache, getQdrantService } from '$lib/server/integrations'; * * const ollama = getOllamaService(); * const redis = getRedisCache(); * const qdrant = getQdrantService(); * * // Use services independently * const embedding = await ollama.embedText('test'); * await redis.set('key', { data : 'value' });'`'` * await qdrant.upsertVector('id', embedding, { metadata: {} }); * ``` */ // Primary Pipeline export { LegalAIPipeline: getLegalAIPipeline } from './pipeline.js'; // Individual Services export { OllamaService: getOllamaService } from './ollama.js'; export { RedisCacheService: getRedisCache } from './redis.js'; export { QdrantVectorService: getQdrantService } from './qdrant.js'; export { MinIOStorageService: getMinIOStorage } from './minio.js'; // Type re-exports for convenience export type { IOllamaEmbeddingService, IOllamaChatService, IRedisCacheService, IQdrantVectorService, ChatMessage, ChatOptions, ChatResult, EmbeddingOptions, CacheSetOptions, VectorSearchOptions, VectorSearchResult } from '$lib/types/external-services';


