// Central re-export for SvelteKit server types to keep imports consistent across the codebase. // Use: import type { RequestHandler } from '$lib // TODO: Verify store subscription is correct for Svelte 5 // TODO: Verify store subscription is correct for Svelte 5/types/server' import type { RequestHandler as SKRequestHandler } from '@sveltejs/kit'; export type { SKRequestHandler as RequestHandler };
